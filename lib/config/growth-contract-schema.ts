/**
 * Compatibility entrypoint. The packaged config boundary owns the single
 * executable Growth Contract parser so root and clean consumers cannot drift.
 */
export * from "@venture-harness/config/growth-contract";
