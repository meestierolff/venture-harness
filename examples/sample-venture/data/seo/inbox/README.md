# SYNTHETIC sample data

`gsc-queries.csv` here is invented for the MeterMate example so
`pnpm weekly -- --data examples/sample-venture/data` has something to
aggregate. It is not real Search Console data.
