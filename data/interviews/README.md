# data/interviews/

Anonymized interview notes (one file per conversation, dated). Verbatim
customer language goes to memory via
`pnpm outcome:add -- --type customer-language ...`.

Never store names, emails, or identifying details here — role and context
only ("ops manager, 40-unit portfolio"). Raw recordings and identifiable
notes stay outside the repository entirely.
