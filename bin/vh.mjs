#!/usr/bin/env node
var __defProp = Object.defineProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};

// scripts/vh-bundle.ts
import { realpathSync as realpathSync11 } from "node:fs";
import { resolve as resolve26 } from "node:path";
import { fileURLToPath as fileURLToPath2 } from "node:url";

// lib/workflow/errors.ts
var WorkflowValidationError = class extends Error {
  issues;
  constructor(issues) {
    super(`Invalid workflow:
- ${issues.join("\n- ")}`);
    this.name = "WorkflowValidationError";
    this.issues = issues;
  }
};
var WorkflowExecutionError = class extends Error {
  code;
  retryable;
  details;
  constructor(code, message, options = {}) {
    super(message);
    this.name = "WorkflowExecutionError";
    this.code = code;
    this.retryable = options.retryable ?? false;
    this.details = options.details;
  }
};
var DuplicateWorkflowRunError = class extends Error {
  constructor(runId2) {
    super(`Workflow run "${runId2}" already exists; use resume instead of starting it again.`);
    this.name = "DuplicateWorkflowRunError";
  }
};
var WorkflowRunNotFoundError = class extends Error {
  constructor(runId2) {
    super(`Workflow run "${runId2}" was not found.`);
    this.name = "WorkflowRunNotFoundError";
  }
};

// lib/workflow/executor.ts
import { randomBytes } from "node:crypto";

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/external.js
var external_exports = {};
__export(external_exports, {
  BRAND: () => BRAND,
  DIRTY: () => DIRTY,
  EMPTY_PATH: () => EMPTY_PATH,
  INVALID: () => INVALID,
  NEVER: () => NEVER,
  OK: () => OK,
  ParseStatus: () => ParseStatus,
  Schema: () => ZodType,
  ZodAny: () => ZodAny,
  ZodArray: () => ZodArray,
  ZodBigInt: () => ZodBigInt,
  ZodBoolean: () => ZodBoolean,
  ZodBranded: () => ZodBranded,
  ZodCatch: () => ZodCatch,
  ZodDate: () => ZodDate,
  ZodDefault: () => ZodDefault,
  ZodDiscriminatedUnion: () => ZodDiscriminatedUnion,
  ZodEffects: () => ZodEffects,
  ZodEnum: () => ZodEnum,
  ZodError: () => ZodError,
  ZodFirstPartyTypeKind: () => ZodFirstPartyTypeKind,
  ZodFunction: () => ZodFunction,
  ZodIntersection: () => ZodIntersection,
  ZodIssueCode: () => ZodIssueCode,
  ZodLazy: () => ZodLazy,
  ZodLiteral: () => ZodLiteral,
  ZodMap: () => ZodMap,
  ZodNaN: () => ZodNaN,
  ZodNativeEnum: () => ZodNativeEnum,
  ZodNever: () => ZodNever,
  ZodNull: () => ZodNull,
  ZodNullable: () => ZodNullable,
  ZodNumber: () => ZodNumber,
  ZodObject: () => ZodObject,
  ZodOptional: () => ZodOptional,
  ZodParsedType: () => ZodParsedType,
  ZodPipeline: () => ZodPipeline,
  ZodPromise: () => ZodPromise,
  ZodReadonly: () => ZodReadonly,
  ZodRecord: () => ZodRecord,
  ZodSchema: () => ZodType,
  ZodSet: () => ZodSet,
  ZodString: () => ZodString,
  ZodSymbol: () => ZodSymbol,
  ZodTransformer: () => ZodEffects,
  ZodTuple: () => ZodTuple,
  ZodType: () => ZodType,
  ZodUndefined: () => ZodUndefined,
  ZodUnion: () => ZodUnion,
  ZodUnknown: () => ZodUnknown,
  ZodVoid: () => ZodVoid,
  addIssueToContext: () => addIssueToContext,
  any: () => anyType,
  array: () => arrayType,
  bigint: () => bigIntType,
  boolean: () => booleanType,
  coerce: () => coerce,
  custom: () => custom,
  date: () => dateType,
  datetimeRegex: () => datetimeRegex,
  defaultErrorMap: () => en_default,
  discriminatedUnion: () => discriminatedUnionType,
  effect: () => effectsType,
  enum: () => enumType,
  function: () => functionType,
  getErrorMap: () => getErrorMap,
  getParsedType: () => getParsedType,
  instanceof: () => instanceOfType,
  intersection: () => intersectionType,
  isAborted: () => isAborted,
  isAsync: () => isAsync,
  isDirty: () => isDirty,
  isValid: () => isValid,
  late: () => late,
  lazy: () => lazyType,
  literal: () => literalType,
  makeIssue: () => makeIssue,
  map: () => mapType,
  nan: () => nanType,
  nativeEnum: () => nativeEnumType,
  never: () => neverType,
  null: () => nullType,
  nullable: () => nullableType,
  number: () => numberType,
  object: () => objectType,
  objectUtil: () => objectUtil,
  oboolean: () => oboolean,
  onumber: () => onumber,
  optional: () => optionalType,
  ostring: () => ostring,
  pipeline: () => pipelineType,
  preprocess: () => preprocessType,
  promise: () => promiseType,
  quotelessJson: () => quotelessJson,
  record: () => recordType,
  set: () => setType,
  setErrorMap: () => setErrorMap,
  strictObject: () => strictObjectType,
  string: () => stringType,
  symbol: () => symbolType,
  transformer: () => effectsType,
  tuple: () => tupleType,
  undefined: () => undefinedType,
  union: () => unionType,
  unknown: () => unknownType,
  util: () => util,
  void: () => voidType
});

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/util.js
var util;
(function(util2) {
  util2.assertEqual = (_) => {
  };
  function assertIs(_arg) {
  }
  util2.assertIs = assertIs;
  function assertNever(_x) {
    throw new Error();
  }
  util2.assertNever = assertNever;
  util2.arrayToEnum = (items) => {
    const obj = {};
    for (const item of items) {
      obj[item] = item;
    }
    return obj;
  };
  util2.getValidEnumValues = (obj) => {
    const validKeys = util2.objectKeys(obj).filter((k) => typeof obj[obj[k]] !== "number");
    const filtered = {};
    for (const k of validKeys) {
      filtered[k] = obj[k];
    }
    return util2.objectValues(filtered);
  };
  util2.objectValues = (obj) => {
    return util2.objectKeys(obj).map(function(e) {
      return obj[e];
    });
  };
  util2.objectKeys = typeof Object.keys === "function" ? (obj) => Object.keys(obj) : (object2) => {
    const keys = [];
    for (const key in object2) {
      if (Object.prototype.hasOwnProperty.call(object2, key)) {
        keys.push(key);
      }
    }
    return keys;
  };
  util2.find = (arr, checker) => {
    for (const item of arr) {
      if (checker(item))
        return item;
    }
    return void 0;
  };
  util2.isInteger = typeof Number.isInteger === "function" ? (val) => Number.isInteger(val) : (val) => typeof val === "number" && Number.isFinite(val) && Math.floor(val) === val;
  function joinValues(array2, separator = " | ") {
    return array2.map((val) => typeof val === "string" ? `'${val}'` : val).join(separator);
  }
  util2.joinValues = joinValues;
  util2.jsonStringifyReplacer = (_, value) => {
    if (typeof value === "bigint") {
      return value.toString();
    }
    return value;
  };
})(util || (util = {}));
var objectUtil;
(function(objectUtil2) {
  objectUtil2.mergeShapes = (first, second) => {
    return {
      ...first,
      ...second
      // second overwrites first
    };
  };
})(objectUtil || (objectUtil = {}));
var ZodParsedType = util.arrayToEnum([
  "string",
  "nan",
  "number",
  "integer",
  "float",
  "boolean",
  "date",
  "bigint",
  "symbol",
  "function",
  "undefined",
  "null",
  "array",
  "object",
  "unknown",
  "promise",
  "void",
  "never",
  "map",
  "set"
]);
var getParsedType = (data) => {
  const t = typeof data;
  switch (t) {
    case "undefined":
      return ZodParsedType.undefined;
    case "string":
      return ZodParsedType.string;
    case "number":
      return Number.isNaN(data) ? ZodParsedType.nan : ZodParsedType.number;
    case "boolean":
      return ZodParsedType.boolean;
    case "function":
      return ZodParsedType.function;
    case "bigint":
      return ZodParsedType.bigint;
    case "symbol":
      return ZodParsedType.symbol;
    case "object":
      if (Array.isArray(data)) {
        return ZodParsedType.array;
      }
      if (data === null) {
        return ZodParsedType.null;
      }
      if (data.then && typeof data.then === "function" && data.catch && typeof data.catch === "function") {
        return ZodParsedType.promise;
      }
      if (typeof Map !== "undefined" && data instanceof Map) {
        return ZodParsedType.map;
      }
      if (typeof Set !== "undefined" && data instanceof Set) {
        return ZodParsedType.set;
      }
      if (typeof Date !== "undefined" && data instanceof Date) {
        return ZodParsedType.date;
      }
      return ZodParsedType.object;
    default:
      return ZodParsedType.unknown;
  }
};

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/ZodError.js
var ZodIssueCode = util.arrayToEnum([
  "invalid_type",
  "invalid_literal",
  "custom",
  "invalid_union",
  "invalid_union_discriminator",
  "invalid_enum_value",
  "unrecognized_keys",
  "invalid_arguments",
  "invalid_return_type",
  "invalid_date",
  "invalid_string",
  "too_small",
  "too_big",
  "invalid_intersection_types",
  "not_multiple_of",
  "not_finite"
]);
var quotelessJson = (obj) => {
  const json2 = JSON.stringify(obj, null, 2);
  return json2.replace(/"([^"]+)":/g, "$1:");
};
var ZodError = class _ZodError extends Error {
  get errors() {
    return this.issues;
  }
  constructor(issues) {
    super();
    this.issues = [];
    this.addIssue = (sub) => {
      this.issues = [...this.issues, sub];
    };
    this.addIssues = (subs = []) => {
      this.issues = [...this.issues, ...subs];
    };
    const actualProto = new.target.prototype;
    if (Object.setPrototypeOf) {
      Object.setPrototypeOf(this, actualProto);
    } else {
      this.__proto__ = actualProto;
    }
    this.name = "ZodError";
    this.issues = issues;
  }
  format(_mapper) {
    const mapper = _mapper || function(issue) {
      return issue.message;
    };
    const fieldErrors = { _errors: [] };
    const processError = (error) => {
      for (const issue of error.issues) {
        if (issue.code === "invalid_union") {
          issue.unionErrors.map(processError);
        } else if (issue.code === "invalid_return_type") {
          processError(issue.returnTypeError);
        } else if (issue.code === "invalid_arguments") {
          processError(issue.argumentsError);
        } else if (issue.path.length === 0) {
          fieldErrors._errors.push(mapper(issue));
        } else {
          let curr = fieldErrors;
          let i = 0;
          while (i < issue.path.length) {
            const el = issue.path[i];
            const terminal = i === issue.path.length - 1;
            if (!terminal) {
              curr[el] = curr[el] || { _errors: [] };
            } else {
              curr[el] = curr[el] || { _errors: [] };
              curr[el]._errors.push(mapper(issue));
            }
            curr = curr[el];
            i++;
          }
        }
      }
    };
    processError(this);
    return fieldErrors;
  }
  static assert(value) {
    if (!(value instanceof _ZodError)) {
      throw new Error(`Not a ZodError: ${value}`);
    }
  }
  toString() {
    return this.message;
  }
  get message() {
    return JSON.stringify(this.issues, util.jsonStringifyReplacer, 2);
  }
  get isEmpty() {
    return this.issues.length === 0;
  }
  flatten(mapper = (issue) => issue.message) {
    const fieldErrors = {};
    const formErrors = [];
    for (const sub of this.issues) {
      if (sub.path.length > 0) {
        const firstEl = sub.path[0];
        fieldErrors[firstEl] = fieldErrors[firstEl] || [];
        fieldErrors[firstEl].push(mapper(sub));
      } else {
        formErrors.push(mapper(sub));
      }
    }
    return { formErrors, fieldErrors };
  }
  get formErrors() {
    return this.flatten();
  }
};
ZodError.create = (issues) => {
  const error = new ZodError(issues);
  return error;
};

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/locales/en.js
var errorMap = (issue, _ctx) => {
  let message;
  switch (issue.code) {
    case ZodIssueCode.invalid_type:
      if (issue.received === ZodParsedType.undefined) {
        message = "Required";
      } else {
        message = `Expected ${issue.expected}, received ${issue.received}`;
      }
      break;
    case ZodIssueCode.invalid_literal:
      message = `Invalid literal value, expected ${JSON.stringify(issue.expected, util.jsonStringifyReplacer)}`;
      break;
    case ZodIssueCode.unrecognized_keys:
      message = `Unrecognized key(s) in object: ${util.joinValues(issue.keys, ", ")}`;
      break;
    case ZodIssueCode.invalid_union:
      message = `Invalid input`;
      break;
    case ZodIssueCode.invalid_union_discriminator:
      message = `Invalid discriminator value. Expected ${util.joinValues(issue.options)}`;
      break;
    case ZodIssueCode.invalid_enum_value:
      message = `Invalid enum value. Expected ${util.joinValues(issue.options)}, received '${issue.received}'`;
      break;
    case ZodIssueCode.invalid_arguments:
      message = `Invalid function arguments`;
      break;
    case ZodIssueCode.invalid_return_type:
      message = `Invalid function return type`;
      break;
    case ZodIssueCode.invalid_date:
      message = `Invalid date`;
      break;
    case ZodIssueCode.invalid_string:
      if (typeof issue.validation === "object") {
        if ("includes" in issue.validation) {
          message = `Invalid input: must include "${issue.validation.includes}"`;
          if (typeof issue.validation.position === "number") {
            message = `${message} at one or more positions greater than or equal to ${issue.validation.position}`;
          }
        } else if ("startsWith" in issue.validation) {
          message = `Invalid input: must start with "${issue.validation.startsWith}"`;
        } else if ("endsWith" in issue.validation) {
          message = `Invalid input: must end with "${issue.validation.endsWith}"`;
        } else {
          util.assertNever(issue.validation);
        }
      } else if (issue.validation !== "regex") {
        message = `Invalid ${issue.validation}`;
      } else {
        message = "Invalid";
      }
      break;
    case ZodIssueCode.too_small:
      if (issue.type === "array")
        message = `Array must contain ${issue.exact ? "exactly" : issue.inclusive ? `at least` : `more than`} ${issue.minimum} element(s)`;
      else if (issue.type === "string")
        message = `String must contain ${issue.exact ? "exactly" : issue.inclusive ? `at least` : `over`} ${issue.minimum} character(s)`;
      else if (issue.type === "number")
        message = `Number must be ${issue.exact ? `exactly equal to ` : issue.inclusive ? `greater than or equal to ` : `greater than `}${issue.minimum}`;
      else if (issue.type === "bigint")
        message = `Number must be ${issue.exact ? `exactly equal to ` : issue.inclusive ? `greater than or equal to ` : `greater than `}${issue.minimum}`;
      else if (issue.type === "date")
        message = `Date must be ${issue.exact ? `exactly equal to ` : issue.inclusive ? `greater than or equal to ` : `greater than `}${new Date(Number(issue.minimum))}`;
      else
        message = "Invalid input";
      break;
    case ZodIssueCode.too_big:
      if (issue.type === "array")
        message = `Array must contain ${issue.exact ? `exactly` : issue.inclusive ? `at most` : `less than`} ${issue.maximum} element(s)`;
      else if (issue.type === "string")
        message = `String must contain ${issue.exact ? `exactly` : issue.inclusive ? `at most` : `under`} ${issue.maximum} character(s)`;
      else if (issue.type === "number")
        message = `Number must be ${issue.exact ? `exactly` : issue.inclusive ? `less than or equal to` : `less than`} ${issue.maximum}`;
      else if (issue.type === "bigint")
        message = `BigInt must be ${issue.exact ? `exactly` : issue.inclusive ? `less than or equal to` : `less than`} ${issue.maximum}`;
      else if (issue.type === "date")
        message = `Date must be ${issue.exact ? `exactly` : issue.inclusive ? `smaller than or equal to` : `smaller than`} ${new Date(Number(issue.maximum))}`;
      else
        message = "Invalid input";
      break;
    case ZodIssueCode.custom:
      message = `Invalid input`;
      break;
    case ZodIssueCode.invalid_intersection_types:
      message = `Intersection results could not be merged`;
      break;
    case ZodIssueCode.not_multiple_of:
      message = `Number must be a multiple of ${issue.multipleOf}`;
      break;
    case ZodIssueCode.not_finite:
      message = "Number must be finite";
      break;
    default:
      message = _ctx.defaultError;
      util.assertNever(issue);
  }
  return { message };
};
var en_default = errorMap;

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/errors.js
var overrideErrorMap = en_default;
function setErrorMap(map) {
  overrideErrorMap = map;
}
function getErrorMap() {
  return overrideErrorMap;
}

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/parseUtil.js
var makeIssue = (params) => {
  const { data, path, errorMaps, issueData } = params;
  const fullPath = [...path, ...issueData.path || []];
  const fullIssue = {
    ...issueData,
    path: fullPath
  };
  if (issueData.message !== void 0) {
    return {
      ...issueData,
      path: fullPath,
      message: issueData.message
    };
  }
  let errorMessage = "";
  const maps = errorMaps.filter((m) => !!m).slice().reverse();
  for (const map of maps) {
    errorMessage = map(fullIssue, { data, defaultError: errorMessage }).message;
  }
  return {
    ...issueData,
    path: fullPath,
    message: errorMessage
  };
};
var EMPTY_PATH = [];
function addIssueToContext(ctx, issueData) {
  const overrideMap = getErrorMap();
  const issue = makeIssue({
    issueData,
    data: ctx.data,
    path: ctx.path,
    errorMaps: [
      ctx.common.contextualErrorMap,
      // contextual error map is first priority
      ctx.schemaErrorMap,
      // then schema-bound map if available
      overrideMap,
      // then global override map
      overrideMap === en_default ? void 0 : en_default
      // then global default map
    ].filter((x) => !!x)
  });
  ctx.common.issues.push(issue);
}
var ParseStatus = class _ParseStatus {
  constructor() {
    this.value = "valid";
  }
  dirty() {
    if (this.value === "valid")
      this.value = "dirty";
  }
  abort() {
    if (this.value !== "aborted")
      this.value = "aborted";
  }
  static mergeArray(status, results) {
    const arrayValue = [];
    for (const s of results) {
      if (s.status === "aborted")
        return INVALID;
      if (s.status === "dirty")
        status.dirty();
      arrayValue.push(s.value);
    }
    return { status: status.value, value: arrayValue };
  }
  static async mergeObjectAsync(status, pairs) {
    const syncPairs = [];
    for (const pair of pairs) {
      const key = await pair.key;
      const value = await pair.value;
      syncPairs.push({
        key,
        value
      });
    }
    return _ParseStatus.mergeObjectSync(status, syncPairs);
  }
  static mergeObjectSync(status, pairs) {
    const finalObject = {};
    for (const pair of pairs) {
      const { key, value } = pair;
      if (key.status === "aborted")
        return INVALID;
      if (value.status === "aborted")
        return INVALID;
      if (key.status === "dirty")
        status.dirty();
      if (value.status === "dirty")
        status.dirty();
      if (key.value !== "__proto__" && (typeof value.value !== "undefined" || pair.alwaysSet)) {
        finalObject[key.value] = value.value;
      }
    }
    return { status: status.value, value: finalObject };
  }
};
var INVALID = Object.freeze({
  status: "aborted"
});
var DIRTY = (value) => ({ status: "dirty", value });
var OK = (value) => ({ status: "valid", value });
var isAborted = (x) => x.status === "aborted";
var isDirty = (x) => x.status === "dirty";
var isValid = (x) => x.status === "valid";
var isAsync = (x) => typeof Promise !== "undefined" && x instanceof Promise;

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/errorUtil.js
var errorUtil;
(function(errorUtil2) {
  errorUtil2.errToObj = (message) => typeof message === "string" ? { message } : message || {};
  errorUtil2.toString = (message) => typeof message === "string" ? message : message?.message;
})(errorUtil || (errorUtil = {}));

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/types.js
var ParseInputLazyPath = class {
  constructor(parent, value, path, key) {
    this._cachedPath = [];
    this.parent = parent;
    this.data = value;
    this._path = path;
    this._key = key;
  }
  get path() {
    if (!this._cachedPath.length) {
      if (Array.isArray(this._key)) {
        this._cachedPath.push(...this._path, ...this._key);
      } else {
        this._cachedPath.push(...this._path, this._key);
      }
    }
    return this._cachedPath;
  }
};
var handleResult = (ctx, result2) => {
  if (isValid(result2)) {
    return { success: true, data: result2.value };
  } else {
    if (!ctx.common.issues.length) {
      throw new Error("Validation failed but no issues detected.");
    }
    return {
      success: false,
      get error() {
        if (this._error)
          return this._error;
        const error = new ZodError(ctx.common.issues);
        this._error = error;
        return this._error;
      }
    };
  }
};
function processCreateParams(params) {
  if (!params)
    return {};
  const { errorMap: errorMap2, invalid_type_error, required_error, description } = params;
  if (errorMap2 && (invalid_type_error || required_error)) {
    throw new Error(`Can't use "invalid_type_error" or "required_error" in conjunction with custom error map.`);
  }
  if (errorMap2)
    return { errorMap: errorMap2, description };
  const customMap = (iss, ctx) => {
    const { message } = params;
    if (iss.code === "invalid_enum_value") {
      return { message: message ?? ctx.defaultError };
    }
    if (typeof ctx.data === "undefined") {
      return { message: message ?? required_error ?? ctx.defaultError };
    }
    if (iss.code !== "invalid_type")
      return { message: ctx.defaultError };
    return { message: message ?? invalid_type_error ?? ctx.defaultError };
  };
  return { errorMap: customMap, description };
}
var ZodType = class {
  get description() {
    return this._def.description;
  }
  _getType(input) {
    return getParsedType(input.data);
  }
  _getOrReturnCtx(input, ctx) {
    return ctx || {
      common: input.parent.common,
      data: input.data,
      parsedType: getParsedType(input.data),
      schemaErrorMap: this._def.errorMap,
      path: input.path,
      parent: input.parent
    };
  }
  _processInputParams(input) {
    return {
      status: new ParseStatus(),
      ctx: {
        common: input.parent.common,
        data: input.data,
        parsedType: getParsedType(input.data),
        schemaErrorMap: this._def.errorMap,
        path: input.path,
        parent: input.parent
      }
    };
  }
  _parseSync(input) {
    const result2 = this._parse(input);
    if (isAsync(result2)) {
      throw new Error("Synchronous parse encountered promise.");
    }
    return result2;
  }
  _parseAsync(input) {
    const result2 = this._parse(input);
    return Promise.resolve(result2);
  }
  parse(data, params) {
    const result2 = this.safeParse(data, params);
    if (result2.success)
      return result2.data;
    throw result2.error;
  }
  safeParse(data, params) {
    const ctx = {
      common: {
        issues: [],
        async: params?.async ?? false,
        contextualErrorMap: params?.errorMap
      },
      path: params?.path || [],
      schemaErrorMap: this._def.errorMap,
      parent: null,
      data,
      parsedType: getParsedType(data)
    };
    const result2 = this._parseSync({ data, path: ctx.path, parent: ctx });
    return handleResult(ctx, result2);
  }
  "~validate"(data) {
    const ctx = {
      common: {
        issues: [],
        async: !!this["~standard"].async
      },
      path: [],
      schemaErrorMap: this._def.errorMap,
      parent: null,
      data,
      parsedType: getParsedType(data)
    };
    if (!this["~standard"].async) {
      try {
        const result2 = this._parseSync({ data, path: [], parent: ctx });
        return isValid(result2) ? {
          value: result2.value
        } : {
          issues: ctx.common.issues
        };
      } catch (err) {
        if (err?.message?.toLowerCase()?.includes("encountered")) {
          this["~standard"].async = true;
        }
        ctx.common = {
          issues: [],
          async: true
        };
      }
    }
    return this._parseAsync({ data, path: [], parent: ctx }).then((result2) => isValid(result2) ? {
      value: result2.value
    } : {
      issues: ctx.common.issues
    });
  }
  async parseAsync(data, params) {
    const result2 = await this.safeParseAsync(data, params);
    if (result2.success)
      return result2.data;
    throw result2.error;
  }
  async safeParseAsync(data, params) {
    const ctx = {
      common: {
        issues: [],
        contextualErrorMap: params?.errorMap,
        async: true
      },
      path: params?.path || [],
      schemaErrorMap: this._def.errorMap,
      parent: null,
      data,
      parsedType: getParsedType(data)
    };
    const maybeAsyncResult = this._parse({ data, path: ctx.path, parent: ctx });
    const result2 = await (isAsync(maybeAsyncResult) ? maybeAsyncResult : Promise.resolve(maybeAsyncResult));
    return handleResult(ctx, result2);
  }
  refine(check, message) {
    const getIssueProperties = (val) => {
      if (typeof message === "string" || typeof message === "undefined") {
        return { message };
      } else if (typeof message === "function") {
        return message(val);
      } else {
        return message;
      }
    };
    return this._refinement((val, ctx) => {
      const result2 = check(val);
      const setError = () => ctx.addIssue({
        code: ZodIssueCode.custom,
        ...getIssueProperties(val)
      });
      if (typeof Promise !== "undefined" && result2 instanceof Promise) {
        return result2.then((data) => {
          if (!data) {
            setError();
            return false;
          } else {
            return true;
          }
        });
      }
      if (!result2) {
        setError();
        return false;
      } else {
        return true;
      }
    });
  }
  refinement(check, refinementData) {
    return this._refinement((val, ctx) => {
      if (!check(val)) {
        ctx.addIssue(typeof refinementData === "function" ? refinementData(val, ctx) : refinementData);
        return false;
      } else {
        return true;
      }
    });
  }
  _refinement(refinement) {
    return new ZodEffects({
      schema: this,
      typeName: ZodFirstPartyTypeKind.ZodEffects,
      effect: { type: "refinement", refinement }
    });
  }
  superRefine(refinement) {
    return this._refinement(refinement);
  }
  constructor(def) {
    this.spa = this.safeParseAsync;
    this._def = def;
    this.parse = this.parse.bind(this);
    this.safeParse = this.safeParse.bind(this);
    this.parseAsync = this.parseAsync.bind(this);
    this.safeParseAsync = this.safeParseAsync.bind(this);
    this.spa = this.spa.bind(this);
    this.refine = this.refine.bind(this);
    this.refinement = this.refinement.bind(this);
    this.superRefine = this.superRefine.bind(this);
    this.optional = this.optional.bind(this);
    this.nullable = this.nullable.bind(this);
    this.nullish = this.nullish.bind(this);
    this.array = this.array.bind(this);
    this.promise = this.promise.bind(this);
    this.or = this.or.bind(this);
    this.and = this.and.bind(this);
    this.transform = this.transform.bind(this);
    this.brand = this.brand.bind(this);
    this.default = this.default.bind(this);
    this.catch = this.catch.bind(this);
    this.describe = this.describe.bind(this);
    this.pipe = this.pipe.bind(this);
    this.readonly = this.readonly.bind(this);
    this.isNullable = this.isNullable.bind(this);
    this.isOptional = this.isOptional.bind(this);
    this["~standard"] = {
      version: 1,
      vendor: "zod",
      validate: (data) => this["~validate"](data)
    };
  }
  optional() {
    return ZodOptional.create(this, this._def);
  }
  nullable() {
    return ZodNullable.create(this, this._def);
  }
  nullish() {
    return this.nullable().optional();
  }
  array() {
    return ZodArray.create(this);
  }
  promise() {
    return ZodPromise.create(this, this._def);
  }
  or(option) {
    return ZodUnion.create([this, option], this._def);
  }
  and(incoming) {
    return ZodIntersection.create(this, incoming, this._def);
  }
  transform(transform) {
    return new ZodEffects({
      ...processCreateParams(this._def),
      schema: this,
      typeName: ZodFirstPartyTypeKind.ZodEffects,
      effect: { type: "transform", transform }
    });
  }
  default(def) {
    const defaultValueFunc = typeof def === "function" ? def : () => def;
    return new ZodDefault({
      ...processCreateParams(this._def),
      innerType: this,
      defaultValue: defaultValueFunc,
      typeName: ZodFirstPartyTypeKind.ZodDefault
    });
  }
  brand() {
    return new ZodBranded({
      typeName: ZodFirstPartyTypeKind.ZodBranded,
      type: this,
      ...processCreateParams(this._def)
    });
  }
  catch(def) {
    const catchValueFunc = typeof def === "function" ? def : () => def;
    return new ZodCatch({
      ...processCreateParams(this._def),
      innerType: this,
      catchValue: catchValueFunc,
      typeName: ZodFirstPartyTypeKind.ZodCatch
    });
  }
  describe(description) {
    const This = this.constructor;
    return new This({
      ...this._def,
      description
    });
  }
  pipe(target) {
    return ZodPipeline.create(this, target);
  }
  readonly() {
    return ZodReadonly.create(this);
  }
  isOptional() {
    return this.safeParse(void 0).success;
  }
  isNullable() {
    return this.safeParse(null).success;
  }
};
var cuidRegex = /^c[^\s-]{8,}$/i;
var cuid2Regex = /^[0-9a-z]+$/;
var ulidRegex = /^[0-9A-HJKMNP-TV-Z]{26}$/i;
var uuidRegex = /^[0-9a-fA-F]{8}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{12}$/i;
var nanoidRegex = /^[a-z0-9_-]{21}$/i;
var jwtRegex = /^[A-Za-z0-9-_]+\.[A-Za-z0-9-_]+\.[A-Za-z0-9-_]*$/;
var durationRegex = /^[-+]?P(?!$)(?:(?:[-+]?\d+Y)|(?:[-+]?\d+[.,]\d+Y$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:(?:[-+]?\d+W)|(?:[-+]?\d+[.,]\d+W$))?(?:(?:[-+]?\d+D)|(?:[-+]?\d+[.,]\d+D$))?(?:T(?=[\d+-])(?:(?:[-+]?\d+H)|(?:[-+]?\d+[.,]\d+H$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:[-+]?\d+(?:[.,]\d+)?S)?)??$/;
var emailRegex = /^(?!\.)(?!.*\.\.)([A-Z0-9_'+\-\.]*)[A-Z0-9_+-]@([A-Z0-9][A-Z0-9\-]*\.)+[A-Z]{2,}$/i;
var _emojiRegex = `^(\\p{Extended_Pictographic}|\\p{Emoji_Component})+$`;
var emojiRegex;
var ipv4Regex = /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])$/;
var ipv4CidrRegex = /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\/(3[0-2]|[12]?[0-9])$/;
var ipv6Regex = /^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))$/;
var ipv6CidrRegex = /^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))\/(12[0-8]|1[01][0-9]|[1-9]?[0-9])$/;
var base64Regex = /^([0-9a-zA-Z+/]{4})*(([0-9a-zA-Z+/]{2}==)|([0-9a-zA-Z+/]{3}=))?$/;
var base64urlRegex = /^([0-9a-zA-Z-_]{4})*(([0-9a-zA-Z-_]{2}(==)?)|([0-9a-zA-Z-_]{3}(=)?))?$/;
var dateRegexSource = `((\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-((0[13578]|1[02])-(0[1-9]|[12]\\d|3[01])|(0[469]|11)-(0[1-9]|[12]\\d|30)|(02)-(0[1-9]|1\\d|2[0-8])))`;
var dateRegex = new RegExp(`^${dateRegexSource}$`);
function timeRegexSource(args) {
  let secondsRegexSource = `[0-5]\\d`;
  if (args.precision) {
    secondsRegexSource = `${secondsRegexSource}\\.\\d{${args.precision}}`;
  } else if (args.precision == null) {
    secondsRegexSource = `${secondsRegexSource}(\\.\\d+)?`;
  }
  const secondsQuantifier = args.precision ? "+" : "?";
  return `([01]\\d|2[0-3]):[0-5]\\d(:${secondsRegexSource})${secondsQuantifier}`;
}
function timeRegex(args) {
  return new RegExp(`^${timeRegexSource(args)}$`);
}
function datetimeRegex(args) {
  let regex = `${dateRegexSource}T${timeRegexSource(args)}`;
  const opts = [];
  opts.push(args.local ? `Z?` : `Z`);
  if (args.offset)
    opts.push(`([+-]\\d{2}:?\\d{2})`);
  regex = `${regex}(${opts.join("|")})`;
  return new RegExp(`^${regex}$`);
}
function isValidIP(ip, version) {
  if ((version === "v4" || !version) && ipv4Regex.test(ip)) {
    return true;
  }
  if ((version === "v6" || !version) && ipv6Regex.test(ip)) {
    return true;
  }
  return false;
}
function isValidJWT(jwt, alg) {
  if (!jwtRegex.test(jwt))
    return false;
  try {
    const [header] = jwt.split(".");
    if (!header)
      return false;
    const base64 = header.replace(/-/g, "+").replace(/_/g, "/").padEnd(header.length + (4 - header.length % 4) % 4, "=");
    const decoded2 = JSON.parse(atob(base64));
    if (typeof decoded2 !== "object" || decoded2 === null)
      return false;
    if ("typ" in decoded2 && decoded2?.typ !== "JWT")
      return false;
    if (!decoded2.alg)
      return false;
    if (alg && decoded2.alg !== alg)
      return false;
    return true;
  } catch {
    return false;
  }
}
function isValidCidr(ip, version) {
  if ((version === "v4" || !version) && ipv4CidrRegex.test(ip)) {
    return true;
  }
  if ((version === "v6" || !version) && ipv6CidrRegex.test(ip)) {
    return true;
  }
  return false;
}
var ZodString = class _ZodString extends ZodType {
  _parse(input) {
    if (this._def.coerce) {
      input.data = String(input.data);
    }
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.string) {
      const ctx2 = this._getOrReturnCtx(input);
      addIssueToContext(ctx2, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.string,
        received: ctx2.parsedType
      });
      return INVALID;
    }
    const status = new ParseStatus();
    let ctx = void 0;
    for (const check of this._def.checks) {
      if (check.kind === "min") {
        if (input.data.length < check.value) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            minimum: check.value,
            type: "string",
            inclusive: true,
            exact: false,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "max") {
        if (input.data.length > check.value) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            maximum: check.value,
            type: "string",
            inclusive: true,
            exact: false,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "length") {
        const tooBig = input.data.length > check.value;
        const tooSmall = input.data.length < check.value;
        if (tooBig || tooSmall) {
          ctx = this._getOrReturnCtx(input, ctx);
          if (tooBig) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.too_big,
              maximum: check.value,
              type: "string",
              inclusive: true,
              exact: true,
              message: check.message
            });
          } else if (tooSmall) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.too_small,
              minimum: check.value,
              type: "string",
              inclusive: true,
              exact: true,
              message: check.message
            });
          }
          status.dirty();
        }
      } else if (check.kind === "email") {
        if (!emailRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "email",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "emoji") {
        if (!emojiRegex) {
          emojiRegex = new RegExp(_emojiRegex, "u");
        }
        if (!emojiRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "emoji",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "uuid") {
        if (!uuidRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "uuid",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "nanoid") {
        if (!nanoidRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "nanoid",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "cuid") {
        if (!cuidRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "cuid",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "cuid2") {
        if (!cuid2Regex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "cuid2",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "ulid") {
        if (!ulidRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "ulid",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "url") {
        try {
          new URL(input.data);
        } catch {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "url",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "regex") {
        check.regex.lastIndex = 0;
        const testResult = check.regex.test(input.data);
        if (!testResult) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "regex",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "trim") {
        input.data = input.data.trim();
      } else if (check.kind === "includes") {
        if (!input.data.includes(check.value, check.position)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: { includes: check.value, position: check.position },
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "toLowerCase") {
        input.data = input.data.toLowerCase();
      } else if (check.kind === "toUpperCase") {
        input.data = input.data.toUpperCase();
      } else if (check.kind === "startsWith") {
        if (!input.data.startsWith(check.value)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: { startsWith: check.value },
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "endsWith") {
        if (!input.data.endsWith(check.value)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: { endsWith: check.value },
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "datetime") {
        const regex = datetimeRegex(check);
        if (!regex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: "datetime",
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "date") {
        const regex = dateRegex;
        if (!regex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: "date",
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "time") {
        const regex = timeRegex(check);
        if (!regex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: "time",
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "duration") {
        if (!durationRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "duration",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "ip") {
        if (!isValidIP(input.data, check.version)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "ip",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "jwt") {
        if (!isValidJWT(input.data, check.alg)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "jwt",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "cidr") {
        if (!isValidCidr(input.data, check.version)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "cidr",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "base64") {
        if (!base64Regex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "base64",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "base64url") {
        if (!base64urlRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "base64url",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else {
        util.assertNever(check);
      }
    }
    return { status: status.value, value: input.data };
  }
  _regex(regex, validation, message) {
    return this.refinement((data) => regex.test(data), {
      validation,
      code: ZodIssueCode.invalid_string,
      ...errorUtil.errToObj(message)
    });
  }
  _addCheck(check) {
    return new _ZodString({
      ...this._def,
      checks: [...this._def.checks, check]
    });
  }
  email(message) {
    return this._addCheck({ kind: "email", ...errorUtil.errToObj(message) });
  }
  url(message) {
    return this._addCheck({ kind: "url", ...errorUtil.errToObj(message) });
  }
  emoji(message) {
    return this._addCheck({ kind: "emoji", ...errorUtil.errToObj(message) });
  }
  uuid(message) {
    return this._addCheck({ kind: "uuid", ...errorUtil.errToObj(message) });
  }
  nanoid(message) {
    return this._addCheck({ kind: "nanoid", ...errorUtil.errToObj(message) });
  }
  cuid(message) {
    return this._addCheck({ kind: "cuid", ...errorUtil.errToObj(message) });
  }
  cuid2(message) {
    return this._addCheck({ kind: "cuid2", ...errorUtil.errToObj(message) });
  }
  ulid(message) {
    return this._addCheck({ kind: "ulid", ...errorUtil.errToObj(message) });
  }
  base64(message) {
    return this._addCheck({ kind: "base64", ...errorUtil.errToObj(message) });
  }
  base64url(message) {
    return this._addCheck({
      kind: "base64url",
      ...errorUtil.errToObj(message)
    });
  }
  jwt(options) {
    return this._addCheck({ kind: "jwt", ...errorUtil.errToObj(options) });
  }
  ip(options) {
    return this._addCheck({ kind: "ip", ...errorUtil.errToObj(options) });
  }
  cidr(options) {
    return this._addCheck({ kind: "cidr", ...errorUtil.errToObj(options) });
  }
  datetime(options) {
    if (typeof options === "string") {
      return this._addCheck({
        kind: "datetime",
        precision: null,
        offset: false,
        local: false,
        message: options
      });
    }
    return this._addCheck({
      kind: "datetime",
      precision: typeof options?.precision === "undefined" ? null : options?.precision,
      offset: options?.offset ?? false,
      local: options?.local ?? false,
      ...errorUtil.errToObj(options?.message)
    });
  }
  date(message) {
    return this._addCheck({ kind: "date", message });
  }
  time(options) {
    if (typeof options === "string") {
      return this._addCheck({
        kind: "time",
        precision: null,
        message: options
      });
    }
    return this._addCheck({
      kind: "time",
      precision: typeof options?.precision === "undefined" ? null : options?.precision,
      ...errorUtil.errToObj(options?.message)
    });
  }
  duration(message) {
    return this._addCheck({ kind: "duration", ...errorUtil.errToObj(message) });
  }
  regex(regex, message) {
    return this._addCheck({
      kind: "regex",
      regex,
      ...errorUtil.errToObj(message)
    });
  }
  includes(value, options) {
    return this._addCheck({
      kind: "includes",
      value,
      position: options?.position,
      ...errorUtil.errToObj(options?.message)
    });
  }
  startsWith(value, message) {
    return this._addCheck({
      kind: "startsWith",
      value,
      ...errorUtil.errToObj(message)
    });
  }
  endsWith(value, message) {
    return this._addCheck({
      kind: "endsWith",
      value,
      ...errorUtil.errToObj(message)
    });
  }
  min(minLength, message) {
    return this._addCheck({
      kind: "min",
      value: minLength,
      ...errorUtil.errToObj(message)
    });
  }
  max(maxLength, message) {
    return this._addCheck({
      kind: "max",
      value: maxLength,
      ...errorUtil.errToObj(message)
    });
  }
  length(len, message) {
    return this._addCheck({
      kind: "length",
      value: len,
      ...errorUtil.errToObj(message)
    });
  }
  /**
   * Equivalent to `.min(1)`
   */
  nonempty(message) {
    return this.min(1, errorUtil.errToObj(message));
  }
  trim() {
    return new _ZodString({
      ...this._def,
      checks: [...this._def.checks, { kind: "trim" }]
    });
  }
  toLowerCase() {
    return new _ZodString({
      ...this._def,
      checks: [...this._def.checks, { kind: "toLowerCase" }]
    });
  }
  toUpperCase() {
    return new _ZodString({
      ...this._def,
      checks: [...this._def.checks, { kind: "toUpperCase" }]
    });
  }
  get isDatetime() {
    return !!this._def.checks.find((ch) => ch.kind === "datetime");
  }
  get isDate() {
    return !!this._def.checks.find((ch) => ch.kind === "date");
  }
  get isTime() {
    return !!this._def.checks.find((ch) => ch.kind === "time");
  }
  get isDuration() {
    return !!this._def.checks.find((ch) => ch.kind === "duration");
  }
  get isEmail() {
    return !!this._def.checks.find((ch) => ch.kind === "email");
  }
  get isURL() {
    return !!this._def.checks.find((ch) => ch.kind === "url");
  }
  get isEmoji() {
    return !!this._def.checks.find((ch) => ch.kind === "emoji");
  }
  get isUUID() {
    return !!this._def.checks.find((ch) => ch.kind === "uuid");
  }
  get isNANOID() {
    return !!this._def.checks.find((ch) => ch.kind === "nanoid");
  }
  get isCUID() {
    return !!this._def.checks.find((ch) => ch.kind === "cuid");
  }
  get isCUID2() {
    return !!this._def.checks.find((ch) => ch.kind === "cuid2");
  }
  get isULID() {
    return !!this._def.checks.find((ch) => ch.kind === "ulid");
  }
  get isIP() {
    return !!this._def.checks.find((ch) => ch.kind === "ip");
  }
  get isCIDR() {
    return !!this._def.checks.find((ch) => ch.kind === "cidr");
  }
  get isBase64() {
    return !!this._def.checks.find((ch) => ch.kind === "base64");
  }
  get isBase64url() {
    return !!this._def.checks.find((ch) => ch.kind === "base64url");
  }
  get minLength() {
    let min = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "min") {
        if (min === null || ch.value > min)
          min = ch.value;
      }
    }
    return min;
  }
  get maxLength() {
    let max = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "max") {
        if (max === null || ch.value < max)
          max = ch.value;
      }
    }
    return max;
  }
};
ZodString.create = (params) => {
  return new ZodString({
    checks: [],
    typeName: ZodFirstPartyTypeKind.ZodString,
    coerce: params?.coerce ?? false,
    ...processCreateParams(params)
  });
};
function floatSafeRemainder(val, step) {
  const valDecCount = (val.toString().split(".")[1] || "").length;
  const stepDecCount = (step.toString().split(".")[1] || "").length;
  const decCount = valDecCount > stepDecCount ? valDecCount : stepDecCount;
  const valInt = Number.parseInt(val.toFixed(decCount).replace(".", ""));
  const stepInt = Number.parseInt(step.toFixed(decCount).replace(".", ""));
  return valInt % stepInt / 10 ** decCount;
}
var ZodNumber = class _ZodNumber extends ZodType {
  constructor() {
    super(...arguments);
    this.min = this.gte;
    this.max = this.lte;
    this.step = this.multipleOf;
  }
  _parse(input) {
    if (this._def.coerce) {
      input.data = Number(input.data);
    }
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.number) {
      const ctx2 = this._getOrReturnCtx(input);
      addIssueToContext(ctx2, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.number,
        received: ctx2.parsedType
      });
      return INVALID;
    }
    let ctx = void 0;
    const status = new ParseStatus();
    for (const check of this._def.checks) {
      if (check.kind === "int") {
        if (!util.isInteger(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: "integer",
            received: "float",
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "min") {
        const tooSmall = check.inclusive ? input.data < check.value : input.data <= check.value;
        if (tooSmall) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            minimum: check.value,
            type: "number",
            inclusive: check.inclusive,
            exact: false,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "max") {
        const tooBig = check.inclusive ? input.data > check.value : input.data >= check.value;
        if (tooBig) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            maximum: check.value,
            type: "number",
            inclusive: check.inclusive,
            exact: false,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "multipleOf") {
        if (floatSafeRemainder(input.data, check.value) !== 0) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.not_multiple_of,
            multipleOf: check.value,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "finite") {
        if (!Number.isFinite(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.not_finite,
            message: check.message
          });
          status.dirty();
        }
      } else {
        util.assertNever(check);
      }
    }
    return { status: status.value, value: input.data };
  }
  gte(value, message) {
    return this.setLimit("min", value, true, errorUtil.toString(message));
  }
  gt(value, message) {
    return this.setLimit("min", value, false, errorUtil.toString(message));
  }
  lte(value, message) {
    return this.setLimit("max", value, true, errorUtil.toString(message));
  }
  lt(value, message) {
    return this.setLimit("max", value, false, errorUtil.toString(message));
  }
  setLimit(kind, value, inclusive, message) {
    return new _ZodNumber({
      ...this._def,
      checks: [
        ...this._def.checks,
        {
          kind,
          value,
          inclusive,
          message: errorUtil.toString(message)
        }
      ]
    });
  }
  _addCheck(check) {
    return new _ZodNumber({
      ...this._def,
      checks: [...this._def.checks, check]
    });
  }
  int(message) {
    return this._addCheck({
      kind: "int",
      message: errorUtil.toString(message)
    });
  }
  positive(message) {
    return this._addCheck({
      kind: "min",
      value: 0,
      inclusive: false,
      message: errorUtil.toString(message)
    });
  }
  negative(message) {
    return this._addCheck({
      kind: "max",
      value: 0,
      inclusive: false,
      message: errorUtil.toString(message)
    });
  }
  nonpositive(message) {
    return this._addCheck({
      kind: "max",
      value: 0,
      inclusive: true,
      message: errorUtil.toString(message)
    });
  }
  nonnegative(message) {
    return this._addCheck({
      kind: "min",
      value: 0,
      inclusive: true,
      message: errorUtil.toString(message)
    });
  }
  multipleOf(value, message) {
    return this._addCheck({
      kind: "multipleOf",
      value,
      message: errorUtil.toString(message)
    });
  }
  finite(message) {
    return this._addCheck({
      kind: "finite",
      message: errorUtil.toString(message)
    });
  }
  safe(message) {
    return this._addCheck({
      kind: "min",
      inclusive: true,
      value: Number.MIN_SAFE_INTEGER,
      message: errorUtil.toString(message)
    })._addCheck({
      kind: "max",
      inclusive: true,
      value: Number.MAX_SAFE_INTEGER,
      message: errorUtil.toString(message)
    });
  }
  get minValue() {
    let min = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "min") {
        if (min === null || ch.value > min)
          min = ch.value;
      }
    }
    return min;
  }
  get maxValue() {
    let max = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "max") {
        if (max === null || ch.value < max)
          max = ch.value;
      }
    }
    return max;
  }
  get isInt() {
    return !!this._def.checks.find((ch) => ch.kind === "int" || ch.kind === "multipleOf" && util.isInteger(ch.value));
  }
  get isFinite() {
    let max = null;
    let min = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "finite" || ch.kind === "int" || ch.kind === "multipleOf") {
        return true;
      } else if (ch.kind === "min") {
        if (min === null || ch.value > min)
          min = ch.value;
      } else if (ch.kind === "max") {
        if (max === null || ch.value < max)
          max = ch.value;
      }
    }
    return Number.isFinite(min) && Number.isFinite(max);
  }
};
ZodNumber.create = (params) => {
  return new ZodNumber({
    checks: [],
    typeName: ZodFirstPartyTypeKind.ZodNumber,
    coerce: params?.coerce || false,
    ...processCreateParams(params)
  });
};
var ZodBigInt = class _ZodBigInt extends ZodType {
  constructor() {
    super(...arguments);
    this.min = this.gte;
    this.max = this.lte;
  }
  _parse(input) {
    if (this._def.coerce) {
      try {
        input.data = BigInt(input.data);
      } catch {
        return this._getInvalidInput(input);
      }
    }
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.bigint) {
      return this._getInvalidInput(input);
    }
    let ctx = void 0;
    const status = new ParseStatus();
    for (const check of this._def.checks) {
      if (check.kind === "min") {
        const tooSmall = check.inclusive ? input.data < check.value : input.data <= check.value;
        if (tooSmall) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            type: "bigint",
            minimum: check.value,
            inclusive: check.inclusive,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "max") {
        const tooBig = check.inclusive ? input.data > check.value : input.data >= check.value;
        if (tooBig) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            type: "bigint",
            maximum: check.value,
            inclusive: check.inclusive,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "multipleOf") {
        if (input.data % check.value !== BigInt(0)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.not_multiple_of,
            multipleOf: check.value,
            message: check.message
          });
          status.dirty();
        }
      } else {
        util.assertNever(check);
      }
    }
    return { status: status.value, value: input.data };
  }
  _getInvalidInput(input) {
    const ctx = this._getOrReturnCtx(input);
    addIssueToContext(ctx, {
      code: ZodIssueCode.invalid_type,
      expected: ZodParsedType.bigint,
      received: ctx.parsedType
    });
    return INVALID;
  }
  gte(value, message) {
    return this.setLimit("min", value, true, errorUtil.toString(message));
  }
  gt(value, message) {
    return this.setLimit("min", value, false, errorUtil.toString(message));
  }
  lte(value, message) {
    return this.setLimit("max", value, true, errorUtil.toString(message));
  }
  lt(value, message) {
    return this.setLimit("max", value, false, errorUtil.toString(message));
  }
  setLimit(kind, value, inclusive, message) {
    return new _ZodBigInt({
      ...this._def,
      checks: [
        ...this._def.checks,
        {
          kind,
          value,
          inclusive,
          message: errorUtil.toString(message)
        }
      ]
    });
  }
  _addCheck(check) {
    return new _ZodBigInt({
      ...this._def,
      checks: [...this._def.checks, check]
    });
  }
  positive(message) {
    return this._addCheck({
      kind: "min",
      value: BigInt(0),
      inclusive: false,
      message: errorUtil.toString(message)
    });
  }
  negative(message) {
    return this._addCheck({
      kind: "max",
      value: BigInt(0),
      inclusive: false,
      message: errorUtil.toString(message)
    });
  }
  nonpositive(message) {
    return this._addCheck({
      kind: "max",
      value: BigInt(0),
      inclusive: true,
      message: errorUtil.toString(message)
    });
  }
  nonnegative(message) {
    return this._addCheck({
      kind: "min",
      value: BigInt(0),
      inclusive: true,
      message: errorUtil.toString(message)
    });
  }
  multipleOf(value, message) {
    return this._addCheck({
      kind: "multipleOf",
      value,
      message: errorUtil.toString(message)
    });
  }
  get minValue() {
    let min = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "min") {
        if (min === null || ch.value > min)
          min = ch.value;
      }
    }
    return min;
  }
  get maxValue() {
    let max = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "max") {
        if (max === null || ch.value < max)
          max = ch.value;
      }
    }
    return max;
  }
};
ZodBigInt.create = (params) => {
  return new ZodBigInt({
    checks: [],
    typeName: ZodFirstPartyTypeKind.ZodBigInt,
    coerce: params?.coerce ?? false,
    ...processCreateParams(params)
  });
};
var ZodBoolean = class extends ZodType {
  _parse(input) {
    if (this._def.coerce) {
      input.data = Boolean(input.data);
    }
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.boolean) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.boolean,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return OK(input.data);
  }
};
ZodBoolean.create = (params) => {
  return new ZodBoolean({
    typeName: ZodFirstPartyTypeKind.ZodBoolean,
    coerce: params?.coerce || false,
    ...processCreateParams(params)
  });
};
var ZodDate = class _ZodDate extends ZodType {
  _parse(input) {
    if (this._def.coerce) {
      input.data = new Date(input.data);
    }
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.date) {
      const ctx2 = this._getOrReturnCtx(input);
      addIssueToContext(ctx2, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.date,
        received: ctx2.parsedType
      });
      return INVALID;
    }
    if (Number.isNaN(input.data.getTime())) {
      const ctx2 = this._getOrReturnCtx(input);
      addIssueToContext(ctx2, {
        code: ZodIssueCode.invalid_date
      });
      return INVALID;
    }
    const status = new ParseStatus();
    let ctx = void 0;
    for (const check of this._def.checks) {
      if (check.kind === "min") {
        if (input.data.getTime() < check.value) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            message: check.message,
            inclusive: true,
            exact: false,
            minimum: check.value,
            type: "date"
          });
          status.dirty();
        }
      } else if (check.kind === "max") {
        if (input.data.getTime() > check.value) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            message: check.message,
            inclusive: true,
            exact: false,
            maximum: check.value,
            type: "date"
          });
          status.dirty();
        }
      } else {
        util.assertNever(check);
      }
    }
    return {
      status: status.value,
      value: new Date(input.data.getTime())
    };
  }
  _addCheck(check) {
    return new _ZodDate({
      ...this._def,
      checks: [...this._def.checks, check]
    });
  }
  min(minDate, message) {
    return this._addCheck({
      kind: "min",
      value: minDate.getTime(),
      message: errorUtil.toString(message)
    });
  }
  max(maxDate, message) {
    return this._addCheck({
      kind: "max",
      value: maxDate.getTime(),
      message: errorUtil.toString(message)
    });
  }
  get minDate() {
    let min = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "min") {
        if (min === null || ch.value > min)
          min = ch.value;
      }
    }
    return min != null ? new Date(min) : null;
  }
  get maxDate() {
    let max = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "max") {
        if (max === null || ch.value < max)
          max = ch.value;
      }
    }
    return max != null ? new Date(max) : null;
  }
};
ZodDate.create = (params) => {
  return new ZodDate({
    checks: [],
    coerce: params?.coerce || false,
    typeName: ZodFirstPartyTypeKind.ZodDate,
    ...processCreateParams(params)
  });
};
var ZodSymbol = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.symbol) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.symbol,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return OK(input.data);
  }
};
ZodSymbol.create = (params) => {
  return new ZodSymbol({
    typeName: ZodFirstPartyTypeKind.ZodSymbol,
    ...processCreateParams(params)
  });
};
var ZodUndefined = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.undefined) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.undefined,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return OK(input.data);
  }
};
ZodUndefined.create = (params) => {
  return new ZodUndefined({
    typeName: ZodFirstPartyTypeKind.ZodUndefined,
    ...processCreateParams(params)
  });
};
var ZodNull = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.null) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.null,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return OK(input.data);
  }
};
ZodNull.create = (params) => {
  return new ZodNull({
    typeName: ZodFirstPartyTypeKind.ZodNull,
    ...processCreateParams(params)
  });
};
var ZodAny = class extends ZodType {
  constructor() {
    super(...arguments);
    this._any = true;
  }
  _parse(input) {
    return OK(input.data);
  }
};
ZodAny.create = (params) => {
  return new ZodAny({
    typeName: ZodFirstPartyTypeKind.ZodAny,
    ...processCreateParams(params)
  });
};
var ZodUnknown = class extends ZodType {
  constructor() {
    super(...arguments);
    this._unknown = true;
  }
  _parse(input) {
    return OK(input.data);
  }
};
ZodUnknown.create = (params) => {
  return new ZodUnknown({
    typeName: ZodFirstPartyTypeKind.ZodUnknown,
    ...processCreateParams(params)
  });
};
var ZodNever = class extends ZodType {
  _parse(input) {
    const ctx = this._getOrReturnCtx(input);
    addIssueToContext(ctx, {
      code: ZodIssueCode.invalid_type,
      expected: ZodParsedType.never,
      received: ctx.parsedType
    });
    return INVALID;
  }
};
ZodNever.create = (params) => {
  return new ZodNever({
    typeName: ZodFirstPartyTypeKind.ZodNever,
    ...processCreateParams(params)
  });
};
var ZodVoid = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.undefined) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.void,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return OK(input.data);
  }
};
ZodVoid.create = (params) => {
  return new ZodVoid({
    typeName: ZodFirstPartyTypeKind.ZodVoid,
    ...processCreateParams(params)
  });
};
var ZodArray = class _ZodArray extends ZodType {
  _parse(input) {
    const { ctx, status } = this._processInputParams(input);
    const def = this._def;
    if (ctx.parsedType !== ZodParsedType.array) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.array,
        received: ctx.parsedType
      });
      return INVALID;
    }
    if (def.exactLength !== null) {
      const tooBig = ctx.data.length > def.exactLength.value;
      const tooSmall = ctx.data.length < def.exactLength.value;
      if (tooBig || tooSmall) {
        addIssueToContext(ctx, {
          code: tooBig ? ZodIssueCode.too_big : ZodIssueCode.too_small,
          minimum: tooSmall ? def.exactLength.value : void 0,
          maximum: tooBig ? def.exactLength.value : void 0,
          type: "array",
          inclusive: true,
          exact: true,
          message: def.exactLength.message
        });
        status.dirty();
      }
    }
    if (def.minLength !== null) {
      if (ctx.data.length < def.minLength.value) {
        addIssueToContext(ctx, {
          code: ZodIssueCode.too_small,
          minimum: def.minLength.value,
          type: "array",
          inclusive: true,
          exact: false,
          message: def.minLength.message
        });
        status.dirty();
      }
    }
    if (def.maxLength !== null) {
      if (ctx.data.length > def.maxLength.value) {
        addIssueToContext(ctx, {
          code: ZodIssueCode.too_big,
          maximum: def.maxLength.value,
          type: "array",
          inclusive: true,
          exact: false,
          message: def.maxLength.message
        });
        status.dirty();
      }
    }
    if (ctx.common.async) {
      return Promise.all([...ctx.data].map((item, i) => {
        return def.type._parseAsync(new ParseInputLazyPath(ctx, item, ctx.path, i));
      })).then((result3) => {
        return ParseStatus.mergeArray(status, result3);
      });
    }
    const result2 = [...ctx.data].map((item, i) => {
      return def.type._parseSync(new ParseInputLazyPath(ctx, item, ctx.path, i));
    });
    return ParseStatus.mergeArray(status, result2);
  }
  get element() {
    return this._def.type;
  }
  min(minLength, message) {
    return new _ZodArray({
      ...this._def,
      minLength: { value: minLength, message: errorUtil.toString(message) }
    });
  }
  max(maxLength, message) {
    return new _ZodArray({
      ...this._def,
      maxLength: { value: maxLength, message: errorUtil.toString(message) }
    });
  }
  length(len, message) {
    return new _ZodArray({
      ...this._def,
      exactLength: { value: len, message: errorUtil.toString(message) }
    });
  }
  nonempty(message) {
    return this.min(1, message);
  }
};
ZodArray.create = (schema, params) => {
  return new ZodArray({
    type: schema,
    minLength: null,
    maxLength: null,
    exactLength: null,
    typeName: ZodFirstPartyTypeKind.ZodArray,
    ...processCreateParams(params)
  });
};
function deepPartialify(schema) {
  if (schema instanceof ZodObject) {
    const newShape = {};
    for (const key in schema.shape) {
      const fieldSchema = schema.shape[key];
      newShape[key] = ZodOptional.create(deepPartialify(fieldSchema));
    }
    return new ZodObject({
      ...schema._def,
      shape: () => newShape
    });
  } else if (schema instanceof ZodArray) {
    return new ZodArray({
      ...schema._def,
      type: deepPartialify(schema.element)
    });
  } else if (schema instanceof ZodOptional) {
    return ZodOptional.create(deepPartialify(schema.unwrap()));
  } else if (schema instanceof ZodNullable) {
    return ZodNullable.create(deepPartialify(schema.unwrap()));
  } else if (schema instanceof ZodTuple) {
    return ZodTuple.create(schema.items.map((item) => deepPartialify(item)));
  } else {
    return schema;
  }
}
var ZodObject = class _ZodObject extends ZodType {
  constructor() {
    super(...arguments);
    this._cached = null;
    this.nonstrict = this.passthrough;
    this.augment = this.extend;
  }
  _getCached() {
    if (this._cached !== null)
      return this._cached;
    const shape = this._def.shape();
    const keys = util.objectKeys(shape);
    this._cached = { shape, keys };
    return this._cached;
  }
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.object) {
      const ctx2 = this._getOrReturnCtx(input);
      addIssueToContext(ctx2, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.object,
        received: ctx2.parsedType
      });
      return INVALID;
    }
    const { status, ctx } = this._processInputParams(input);
    const { shape, keys: shapeKeys } = this._getCached();
    const extraKeys = [];
    if (!(this._def.catchall instanceof ZodNever && this._def.unknownKeys === "strip")) {
      for (const key in ctx.data) {
        if (!shapeKeys.includes(key)) {
          extraKeys.push(key);
        }
      }
    }
    const pairs = [];
    for (const key of shapeKeys) {
      const keyValidator = shape[key];
      const value = ctx.data[key];
      pairs.push({
        key: { status: "valid", value: key },
        value: keyValidator._parse(new ParseInputLazyPath(ctx, value, ctx.path, key)),
        alwaysSet: key in ctx.data
      });
    }
    if (this._def.catchall instanceof ZodNever) {
      const unknownKeys = this._def.unknownKeys;
      if (unknownKeys === "passthrough") {
        for (const key of extraKeys) {
          pairs.push({
            key: { status: "valid", value: key },
            value: { status: "valid", value: ctx.data[key] }
          });
        }
      } else if (unknownKeys === "strict") {
        if (extraKeys.length > 0) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.unrecognized_keys,
            keys: extraKeys
          });
          status.dirty();
        }
      } else if (unknownKeys === "strip") {
      } else {
        throw new Error(`Internal ZodObject error: invalid unknownKeys value.`);
      }
    } else {
      const catchall = this._def.catchall;
      for (const key of extraKeys) {
        const value = ctx.data[key];
        pairs.push({
          key: { status: "valid", value: key },
          value: catchall._parse(
            new ParseInputLazyPath(ctx, value, ctx.path, key)
            //, ctx.child(key), value, getParsedType(value)
          ),
          alwaysSet: key in ctx.data
        });
      }
    }
    if (ctx.common.async) {
      return Promise.resolve().then(async () => {
        const syncPairs = [];
        for (const pair of pairs) {
          const key = await pair.key;
          const value = await pair.value;
          syncPairs.push({
            key,
            value,
            alwaysSet: pair.alwaysSet
          });
        }
        return syncPairs;
      }).then((syncPairs) => {
        return ParseStatus.mergeObjectSync(status, syncPairs);
      });
    } else {
      return ParseStatus.mergeObjectSync(status, pairs);
    }
  }
  get shape() {
    return this._def.shape();
  }
  strict(message) {
    errorUtil.errToObj;
    return new _ZodObject({
      ...this._def,
      unknownKeys: "strict",
      ...message !== void 0 ? {
        errorMap: (issue, ctx) => {
          const defaultError = this._def.errorMap?.(issue, ctx).message ?? ctx.defaultError;
          if (issue.code === "unrecognized_keys")
            return {
              message: errorUtil.errToObj(message).message ?? defaultError
            };
          return {
            message: defaultError
          };
        }
      } : {}
    });
  }
  strip() {
    return new _ZodObject({
      ...this._def,
      unknownKeys: "strip"
    });
  }
  passthrough() {
    return new _ZodObject({
      ...this._def,
      unknownKeys: "passthrough"
    });
  }
  // const AugmentFactory =
  //   <Def extends ZodObjectDef>(def: Def) =>
  //   <Augmentation extends ZodRawShape>(
  //     augmentation: Augmentation
  //   ): ZodObject<
  //     extendShape<ReturnType<Def["shape"]>, Augmentation>,
  //     Def["unknownKeys"],
  //     Def["catchall"]
  //   > => {
  //     return new ZodObject({
  //       ...def,
  //       shape: () => ({
  //         ...def.shape(),
  //         ...augmentation,
  //       }),
  //     }) as any;
  //   };
  extend(augmentation) {
    return new _ZodObject({
      ...this._def,
      shape: () => ({
        ...this._def.shape(),
        ...augmentation
      })
    });
  }
  /**
   * Prior to zod@1.0.12 there was a bug in the
   * inferred type of merged objects. Please
   * upgrade if you are experiencing issues.
   */
  merge(merging) {
    const merged = new _ZodObject({
      unknownKeys: merging._def.unknownKeys,
      catchall: merging._def.catchall,
      shape: () => ({
        ...this._def.shape(),
        ...merging._def.shape()
      }),
      typeName: ZodFirstPartyTypeKind.ZodObject
    });
    return merged;
  }
  // merge<
  //   Incoming extends AnyZodObject,
  //   Augmentation extends Incoming["shape"],
  //   NewOutput extends {
  //     [k in keyof Augmentation | keyof Output]: k extends keyof Augmentation
  //       ? Augmentation[k]["_output"]
  //       : k extends keyof Output
  //       ? Output[k]
  //       : never;
  //   },
  //   NewInput extends {
  //     [k in keyof Augmentation | keyof Input]: k extends keyof Augmentation
  //       ? Augmentation[k]["_input"]
  //       : k extends keyof Input
  //       ? Input[k]
  //       : never;
  //   }
  // >(
  //   merging: Incoming
  // ): ZodObject<
  //   extendShape<T, ReturnType<Incoming["_def"]["shape"]>>,
  //   Incoming["_def"]["unknownKeys"],
  //   Incoming["_def"]["catchall"],
  //   NewOutput,
  //   NewInput
  // > {
  //   const merged: any = new ZodObject({
  //     unknownKeys: merging._def.unknownKeys,
  //     catchall: merging._def.catchall,
  //     shape: () =>
  //       objectUtil.mergeShapes(this._def.shape(), merging._def.shape()),
  //     typeName: ZodFirstPartyTypeKind.ZodObject,
  //   }) as any;
  //   return merged;
  // }
  setKey(key, schema) {
    return this.augment({ [key]: schema });
  }
  // merge<Incoming extends AnyZodObject>(
  //   merging: Incoming
  // ): //ZodObject<T & Incoming["_shape"], UnknownKeys, Catchall> = (merging) => {
  // ZodObject<
  //   extendShape<T, ReturnType<Incoming["_def"]["shape"]>>,
  //   Incoming["_def"]["unknownKeys"],
  //   Incoming["_def"]["catchall"]
  // > {
  //   // const mergedShape = objectUtil.mergeShapes(
  //   //   this._def.shape(),
  //   //   merging._def.shape()
  //   // );
  //   const merged: any = new ZodObject({
  //     unknownKeys: merging._def.unknownKeys,
  //     catchall: merging._def.catchall,
  //     shape: () =>
  //       objectUtil.mergeShapes(this._def.shape(), merging._def.shape()),
  //     typeName: ZodFirstPartyTypeKind.ZodObject,
  //   }) as any;
  //   return merged;
  // }
  catchall(index) {
    return new _ZodObject({
      ...this._def,
      catchall: index
    });
  }
  pick(mask) {
    const shape = {};
    for (const key of util.objectKeys(mask)) {
      if (mask[key] && this.shape[key]) {
        shape[key] = this.shape[key];
      }
    }
    return new _ZodObject({
      ...this._def,
      shape: () => shape
    });
  }
  omit(mask) {
    const shape = {};
    for (const key of util.objectKeys(this.shape)) {
      if (!mask[key]) {
        shape[key] = this.shape[key];
      }
    }
    return new _ZodObject({
      ...this._def,
      shape: () => shape
    });
  }
  /**
   * @deprecated
   */
  deepPartial() {
    return deepPartialify(this);
  }
  partial(mask) {
    const newShape = {};
    for (const key of util.objectKeys(this.shape)) {
      const fieldSchema = this.shape[key];
      if (mask && !mask[key]) {
        newShape[key] = fieldSchema;
      } else {
        newShape[key] = fieldSchema.optional();
      }
    }
    return new _ZodObject({
      ...this._def,
      shape: () => newShape
    });
  }
  required(mask) {
    const newShape = {};
    for (const key of util.objectKeys(this.shape)) {
      if (mask && !mask[key]) {
        newShape[key] = this.shape[key];
      } else {
        const fieldSchema = this.shape[key];
        let newField = fieldSchema;
        while (newField instanceof ZodOptional) {
          newField = newField._def.innerType;
        }
        newShape[key] = newField;
      }
    }
    return new _ZodObject({
      ...this._def,
      shape: () => newShape
    });
  }
  keyof() {
    return createZodEnum(util.objectKeys(this.shape));
  }
};
ZodObject.create = (shape, params) => {
  return new ZodObject({
    shape: () => shape,
    unknownKeys: "strip",
    catchall: ZodNever.create(),
    typeName: ZodFirstPartyTypeKind.ZodObject,
    ...processCreateParams(params)
  });
};
ZodObject.strictCreate = (shape, params) => {
  return new ZodObject({
    shape: () => shape,
    unknownKeys: "strict",
    catchall: ZodNever.create(),
    typeName: ZodFirstPartyTypeKind.ZodObject,
    ...processCreateParams(params)
  });
};
ZodObject.lazycreate = (shape, params) => {
  return new ZodObject({
    shape,
    unknownKeys: "strip",
    catchall: ZodNever.create(),
    typeName: ZodFirstPartyTypeKind.ZodObject,
    ...processCreateParams(params)
  });
};
var ZodUnion = class extends ZodType {
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    const options = this._def.options;
    function handleResults(results) {
      for (const result2 of results) {
        if (result2.result.status === "valid") {
          return result2.result;
        }
      }
      for (const result2 of results) {
        if (result2.result.status === "dirty") {
          ctx.common.issues.push(...result2.ctx.common.issues);
          return result2.result;
        }
      }
      const unionErrors = results.map((result2) => new ZodError(result2.ctx.common.issues));
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_union,
        unionErrors
      });
      return INVALID;
    }
    if (ctx.common.async) {
      return Promise.all(options.map(async (option) => {
        const childCtx = {
          ...ctx,
          common: {
            ...ctx.common,
            issues: []
          },
          parent: null
        };
        return {
          result: await option._parseAsync({
            data: ctx.data,
            path: ctx.path,
            parent: childCtx
          }),
          ctx: childCtx
        };
      })).then(handleResults);
    } else {
      let dirty = void 0;
      const issues = [];
      for (const option of options) {
        const childCtx = {
          ...ctx,
          common: {
            ...ctx.common,
            issues: []
          },
          parent: null
        };
        const result2 = option._parseSync({
          data: ctx.data,
          path: ctx.path,
          parent: childCtx
        });
        if (result2.status === "valid") {
          return result2;
        } else if (result2.status === "dirty" && !dirty) {
          dirty = { result: result2, ctx: childCtx };
        }
        if (childCtx.common.issues.length) {
          issues.push(childCtx.common.issues);
        }
      }
      if (dirty) {
        ctx.common.issues.push(...dirty.ctx.common.issues);
        return dirty.result;
      }
      const unionErrors = issues.map((issues2) => new ZodError(issues2));
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_union,
        unionErrors
      });
      return INVALID;
    }
  }
  get options() {
    return this._def.options;
  }
};
ZodUnion.create = (types, params) => {
  return new ZodUnion({
    options: types,
    typeName: ZodFirstPartyTypeKind.ZodUnion,
    ...processCreateParams(params)
  });
};
var getDiscriminator = (type) => {
  if (type instanceof ZodLazy) {
    return getDiscriminator(type.schema);
  } else if (type instanceof ZodEffects) {
    return getDiscriminator(type.innerType());
  } else if (type instanceof ZodLiteral) {
    return [type.value];
  } else if (type instanceof ZodEnum) {
    return type.options;
  } else if (type instanceof ZodNativeEnum) {
    return util.objectValues(type.enum);
  } else if (type instanceof ZodDefault) {
    return getDiscriminator(type._def.innerType);
  } else if (type instanceof ZodUndefined) {
    return [void 0];
  } else if (type instanceof ZodNull) {
    return [null];
  } else if (type instanceof ZodOptional) {
    return [void 0, ...getDiscriminator(type.unwrap())];
  } else if (type instanceof ZodNullable) {
    return [null, ...getDiscriminator(type.unwrap())];
  } else if (type instanceof ZodBranded) {
    return getDiscriminator(type.unwrap());
  } else if (type instanceof ZodReadonly) {
    return getDiscriminator(type.unwrap());
  } else if (type instanceof ZodCatch) {
    return getDiscriminator(type._def.innerType);
  } else {
    return [];
  }
};
var ZodDiscriminatedUnion = class _ZodDiscriminatedUnion extends ZodType {
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.object) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.object,
        received: ctx.parsedType
      });
      return INVALID;
    }
    const discriminator = this.discriminator;
    const discriminatorValue = ctx.data[discriminator];
    const option = this.optionsMap.get(discriminatorValue);
    if (!option) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_union_discriminator,
        options: Array.from(this.optionsMap.keys()),
        path: [discriminator]
      });
      return INVALID;
    }
    if (ctx.common.async) {
      return option._parseAsync({
        data: ctx.data,
        path: ctx.path,
        parent: ctx
      });
    } else {
      return option._parseSync({
        data: ctx.data,
        path: ctx.path,
        parent: ctx
      });
    }
  }
  get discriminator() {
    return this._def.discriminator;
  }
  get options() {
    return this._def.options;
  }
  get optionsMap() {
    return this._def.optionsMap;
  }
  /**
   * The constructor of the discriminated union schema. Its behaviour is very similar to that of the normal z.union() constructor.
   * However, it only allows a union of objects, all of which need to share a discriminator property. This property must
   * have a different value for each object in the union.
   * @param discriminator the name of the discriminator property
   * @param types an array of object schemas
   * @param params
   */
  static create(discriminator, options, params) {
    const optionsMap = /* @__PURE__ */ new Map();
    for (const type of options) {
      const discriminatorValues = getDiscriminator(type.shape[discriminator]);
      if (!discriminatorValues.length) {
        throw new Error(`A discriminator value for key \`${discriminator}\` could not be extracted from all schema options`);
      }
      for (const value of discriminatorValues) {
        if (optionsMap.has(value)) {
          throw new Error(`Discriminator property ${String(discriminator)} has duplicate value ${String(value)}`);
        }
        optionsMap.set(value, type);
      }
    }
    return new _ZodDiscriminatedUnion({
      typeName: ZodFirstPartyTypeKind.ZodDiscriminatedUnion,
      discriminator,
      options,
      optionsMap,
      ...processCreateParams(params)
    });
  }
};
function mergeValues(a, b) {
  const aType = getParsedType(a);
  const bType = getParsedType(b);
  if (a === b) {
    return { valid: true, data: a };
  } else if (aType === ZodParsedType.object && bType === ZodParsedType.object) {
    const bKeys = util.objectKeys(b);
    const sharedKeys = util.objectKeys(a).filter((key) => bKeys.indexOf(key) !== -1);
    const newObj = { ...a, ...b };
    for (const key of sharedKeys) {
      const sharedValue = mergeValues(a[key], b[key]);
      if (!sharedValue.valid) {
        return { valid: false };
      }
      newObj[key] = sharedValue.data;
    }
    return { valid: true, data: newObj };
  } else if (aType === ZodParsedType.array && bType === ZodParsedType.array) {
    if (a.length !== b.length) {
      return { valid: false };
    }
    const newArray = [];
    for (let index = 0; index < a.length; index++) {
      const itemA = a[index];
      const itemB = b[index];
      const sharedValue = mergeValues(itemA, itemB);
      if (!sharedValue.valid) {
        return { valid: false };
      }
      newArray.push(sharedValue.data);
    }
    return { valid: true, data: newArray };
  } else if (aType === ZodParsedType.date && bType === ZodParsedType.date && +a === +b) {
    return { valid: true, data: a };
  } else {
    return { valid: false };
  }
}
var ZodIntersection = class extends ZodType {
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    const handleParsed = (parsedLeft, parsedRight) => {
      if (isAborted(parsedLeft) || isAborted(parsedRight)) {
        return INVALID;
      }
      const merged = mergeValues(parsedLeft.value, parsedRight.value);
      if (!merged.valid) {
        addIssueToContext(ctx, {
          code: ZodIssueCode.invalid_intersection_types
        });
        return INVALID;
      }
      if (isDirty(parsedLeft) || isDirty(parsedRight)) {
        status.dirty();
      }
      return { status: status.value, value: merged.data };
    };
    if (ctx.common.async) {
      return Promise.all([
        this._def.left._parseAsync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        }),
        this._def.right._parseAsync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        })
      ]).then(([left, right]) => handleParsed(left, right));
    } else {
      return handleParsed(this._def.left._parseSync({
        data: ctx.data,
        path: ctx.path,
        parent: ctx
      }), this._def.right._parseSync({
        data: ctx.data,
        path: ctx.path,
        parent: ctx
      }));
    }
  }
};
ZodIntersection.create = (left, right, params) => {
  return new ZodIntersection({
    left,
    right,
    typeName: ZodFirstPartyTypeKind.ZodIntersection,
    ...processCreateParams(params)
  });
};
var ZodTuple = class _ZodTuple extends ZodType {
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.array) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.array,
        received: ctx.parsedType
      });
      return INVALID;
    }
    if (ctx.data.length < this._def.items.length) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.too_small,
        minimum: this._def.items.length,
        inclusive: true,
        exact: false,
        type: "array"
      });
      return INVALID;
    }
    const rest = this._def.rest;
    if (!rest && ctx.data.length > this._def.items.length) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.too_big,
        maximum: this._def.items.length,
        inclusive: true,
        exact: false,
        type: "array"
      });
      status.dirty();
    }
    const items = [...ctx.data].map((item, itemIndex) => {
      const schema = this._def.items[itemIndex] || this._def.rest;
      if (!schema)
        return null;
      return schema._parse(new ParseInputLazyPath(ctx, item, ctx.path, itemIndex));
    }).filter((x) => !!x);
    if (ctx.common.async) {
      return Promise.all(items).then((results) => {
        return ParseStatus.mergeArray(status, results);
      });
    } else {
      return ParseStatus.mergeArray(status, items);
    }
  }
  get items() {
    return this._def.items;
  }
  rest(rest) {
    return new _ZodTuple({
      ...this._def,
      rest
    });
  }
};
ZodTuple.create = (schemas, params) => {
  if (!Array.isArray(schemas)) {
    throw new Error("You must pass an array of schemas to z.tuple([ ... ])");
  }
  return new ZodTuple({
    items: schemas,
    typeName: ZodFirstPartyTypeKind.ZodTuple,
    rest: null,
    ...processCreateParams(params)
  });
};
var ZodRecord = class _ZodRecord extends ZodType {
  get keySchema() {
    return this._def.keyType;
  }
  get valueSchema() {
    return this._def.valueType;
  }
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.object) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.object,
        received: ctx.parsedType
      });
      return INVALID;
    }
    const pairs = [];
    const keyType = this._def.keyType;
    const valueType = this._def.valueType;
    for (const key in ctx.data) {
      pairs.push({
        key: keyType._parse(new ParseInputLazyPath(ctx, key, ctx.path, key)),
        value: valueType._parse(new ParseInputLazyPath(ctx, ctx.data[key], ctx.path, key)),
        alwaysSet: key in ctx.data
      });
    }
    if (ctx.common.async) {
      return ParseStatus.mergeObjectAsync(status, pairs);
    } else {
      return ParseStatus.mergeObjectSync(status, pairs);
    }
  }
  get element() {
    return this._def.valueType;
  }
  static create(first, second, third) {
    if (second instanceof ZodType) {
      return new _ZodRecord({
        keyType: first,
        valueType: second,
        typeName: ZodFirstPartyTypeKind.ZodRecord,
        ...processCreateParams(third)
      });
    }
    return new _ZodRecord({
      keyType: ZodString.create(),
      valueType: first,
      typeName: ZodFirstPartyTypeKind.ZodRecord,
      ...processCreateParams(second)
    });
  }
};
var ZodMap = class extends ZodType {
  get keySchema() {
    return this._def.keyType;
  }
  get valueSchema() {
    return this._def.valueType;
  }
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.map) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.map,
        received: ctx.parsedType
      });
      return INVALID;
    }
    const keyType = this._def.keyType;
    const valueType = this._def.valueType;
    const pairs = [...ctx.data.entries()].map(([key, value], index) => {
      return {
        key: keyType._parse(new ParseInputLazyPath(ctx, key, ctx.path, [index, "key"])),
        value: valueType._parse(new ParseInputLazyPath(ctx, value, ctx.path, [index, "value"]))
      };
    });
    if (ctx.common.async) {
      const finalMap = /* @__PURE__ */ new Map();
      return Promise.resolve().then(async () => {
        for (const pair of pairs) {
          const key = await pair.key;
          const value = await pair.value;
          if (key.status === "aborted" || value.status === "aborted") {
            return INVALID;
          }
          if (key.status === "dirty" || value.status === "dirty") {
            status.dirty();
          }
          finalMap.set(key.value, value.value);
        }
        return { status: status.value, value: finalMap };
      });
    } else {
      const finalMap = /* @__PURE__ */ new Map();
      for (const pair of pairs) {
        const key = pair.key;
        const value = pair.value;
        if (key.status === "aborted" || value.status === "aborted") {
          return INVALID;
        }
        if (key.status === "dirty" || value.status === "dirty") {
          status.dirty();
        }
        finalMap.set(key.value, value.value);
      }
      return { status: status.value, value: finalMap };
    }
  }
};
ZodMap.create = (keyType, valueType, params) => {
  return new ZodMap({
    valueType,
    keyType,
    typeName: ZodFirstPartyTypeKind.ZodMap,
    ...processCreateParams(params)
  });
};
var ZodSet = class _ZodSet extends ZodType {
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.set) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.set,
        received: ctx.parsedType
      });
      return INVALID;
    }
    const def = this._def;
    if (def.minSize !== null) {
      if (ctx.data.size < def.minSize.value) {
        addIssueToContext(ctx, {
          code: ZodIssueCode.too_small,
          minimum: def.minSize.value,
          type: "set",
          inclusive: true,
          exact: false,
          message: def.minSize.message
        });
        status.dirty();
      }
    }
    if (def.maxSize !== null) {
      if (ctx.data.size > def.maxSize.value) {
        addIssueToContext(ctx, {
          code: ZodIssueCode.too_big,
          maximum: def.maxSize.value,
          type: "set",
          inclusive: true,
          exact: false,
          message: def.maxSize.message
        });
        status.dirty();
      }
    }
    const valueType = this._def.valueType;
    function finalizeSet(elements2) {
      const parsedSet = /* @__PURE__ */ new Set();
      for (const element of elements2) {
        if (element.status === "aborted")
          return INVALID;
        if (element.status === "dirty")
          status.dirty();
        parsedSet.add(element.value);
      }
      return { status: status.value, value: parsedSet };
    }
    const elements = [...ctx.data.values()].map((item, i) => valueType._parse(new ParseInputLazyPath(ctx, item, ctx.path, i)));
    if (ctx.common.async) {
      return Promise.all(elements).then((elements2) => finalizeSet(elements2));
    } else {
      return finalizeSet(elements);
    }
  }
  min(minSize, message) {
    return new _ZodSet({
      ...this._def,
      minSize: { value: minSize, message: errorUtil.toString(message) }
    });
  }
  max(maxSize, message) {
    return new _ZodSet({
      ...this._def,
      maxSize: { value: maxSize, message: errorUtil.toString(message) }
    });
  }
  size(size, message) {
    return this.min(size, message).max(size, message);
  }
  nonempty(message) {
    return this.min(1, message);
  }
};
ZodSet.create = (valueType, params) => {
  return new ZodSet({
    valueType,
    minSize: null,
    maxSize: null,
    typeName: ZodFirstPartyTypeKind.ZodSet,
    ...processCreateParams(params)
  });
};
var ZodFunction = class _ZodFunction extends ZodType {
  constructor() {
    super(...arguments);
    this.validate = this.implement;
  }
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.function) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.function,
        received: ctx.parsedType
      });
      return INVALID;
    }
    function makeArgsIssue(args, error) {
      return makeIssue({
        data: args,
        path: ctx.path,
        errorMaps: [ctx.common.contextualErrorMap, ctx.schemaErrorMap, getErrorMap(), en_default].filter((x) => !!x),
        issueData: {
          code: ZodIssueCode.invalid_arguments,
          argumentsError: error
        }
      });
    }
    function makeReturnsIssue(returns, error) {
      return makeIssue({
        data: returns,
        path: ctx.path,
        errorMaps: [ctx.common.contextualErrorMap, ctx.schemaErrorMap, getErrorMap(), en_default].filter((x) => !!x),
        issueData: {
          code: ZodIssueCode.invalid_return_type,
          returnTypeError: error
        }
      });
    }
    const params = { errorMap: ctx.common.contextualErrorMap };
    const fn = ctx.data;
    if (this._def.returns instanceof ZodPromise) {
      const me = this;
      return OK(async function(...args) {
        const error = new ZodError([]);
        const parsedArgs = await me._def.args.parseAsync(args, params).catch((e) => {
          error.addIssue(makeArgsIssue(args, e));
          throw error;
        });
        const result2 = await Reflect.apply(fn, this, parsedArgs);
        const parsedReturns = await me._def.returns._def.type.parseAsync(result2, params).catch((e) => {
          error.addIssue(makeReturnsIssue(result2, e));
          throw error;
        });
        return parsedReturns;
      });
    } else {
      const me = this;
      return OK(function(...args) {
        const parsedArgs = me._def.args.safeParse(args, params);
        if (!parsedArgs.success) {
          throw new ZodError([makeArgsIssue(args, parsedArgs.error)]);
        }
        const result2 = Reflect.apply(fn, this, parsedArgs.data);
        const parsedReturns = me._def.returns.safeParse(result2, params);
        if (!parsedReturns.success) {
          throw new ZodError([makeReturnsIssue(result2, parsedReturns.error)]);
        }
        return parsedReturns.data;
      });
    }
  }
  parameters() {
    return this._def.args;
  }
  returnType() {
    return this._def.returns;
  }
  args(...items) {
    return new _ZodFunction({
      ...this._def,
      args: ZodTuple.create(items).rest(ZodUnknown.create())
    });
  }
  returns(returnType) {
    return new _ZodFunction({
      ...this._def,
      returns: returnType
    });
  }
  implement(func) {
    const validatedFunc = this.parse(func);
    return validatedFunc;
  }
  strictImplement(func) {
    const validatedFunc = this.parse(func);
    return validatedFunc;
  }
  static create(args, returns, params) {
    return new _ZodFunction({
      args: args ? args : ZodTuple.create([]).rest(ZodUnknown.create()),
      returns: returns || ZodUnknown.create(),
      typeName: ZodFirstPartyTypeKind.ZodFunction,
      ...processCreateParams(params)
    });
  }
};
var ZodLazy = class extends ZodType {
  get schema() {
    return this._def.getter();
  }
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    const lazySchema = this._def.getter();
    return lazySchema._parse({ data: ctx.data, path: ctx.path, parent: ctx });
  }
};
ZodLazy.create = (getter, params) => {
  return new ZodLazy({
    getter,
    typeName: ZodFirstPartyTypeKind.ZodLazy,
    ...processCreateParams(params)
  });
};
var ZodLiteral = class extends ZodType {
  _parse(input) {
    if (input.data !== this._def.value) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        received: ctx.data,
        code: ZodIssueCode.invalid_literal,
        expected: this._def.value
      });
      return INVALID;
    }
    return { status: "valid", value: input.data };
  }
  get value() {
    return this._def.value;
  }
};
ZodLiteral.create = (value, params) => {
  return new ZodLiteral({
    value,
    typeName: ZodFirstPartyTypeKind.ZodLiteral,
    ...processCreateParams(params)
  });
};
function createZodEnum(values, params) {
  return new ZodEnum({
    values,
    typeName: ZodFirstPartyTypeKind.ZodEnum,
    ...processCreateParams(params)
  });
}
var ZodEnum = class _ZodEnum extends ZodType {
  _parse(input) {
    if (typeof input.data !== "string") {
      const ctx = this._getOrReturnCtx(input);
      const expectedValues = this._def.values;
      addIssueToContext(ctx, {
        expected: util.joinValues(expectedValues),
        received: ctx.parsedType,
        code: ZodIssueCode.invalid_type
      });
      return INVALID;
    }
    if (!this._cache) {
      this._cache = new Set(this._def.values);
    }
    if (!this._cache.has(input.data)) {
      const ctx = this._getOrReturnCtx(input);
      const expectedValues = this._def.values;
      addIssueToContext(ctx, {
        received: ctx.data,
        code: ZodIssueCode.invalid_enum_value,
        options: expectedValues
      });
      return INVALID;
    }
    return OK(input.data);
  }
  get options() {
    return this._def.values;
  }
  get enum() {
    const enumValues = {};
    for (const val of this._def.values) {
      enumValues[val] = val;
    }
    return enumValues;
  }
  get Values() {
    const enumValues = {};
    for (const val of this._def.values) {
      enumValues[val] = val;
    }
    return enumValues;
  }
  get Enum() {
    const enumValues = {};
    for (const val of this._def.values) {
      enumValues[val] = val;
    }
    return enumValues;
  }
  extract(values, newDef = this._def) {
    return _ZodEnum.create(values, {
      ...this._def,
      ...newDef
    });
  }
  exclude(values, newDef = this._def) {
    return _ZodEnum.create(this.options.filter((opt) => !values.includes(opt)), {
      ...this._def,
      ...newDef
    });
  }
};
ZodEnum.create = createZodEnum;
var ZodNativeEnum = class extends ZodType {
  _parse(input) {
    const nativeEnumValues = util.getValidEnumValues(this._def.values);
    const ctx = this._getOrReturnCtx(input);
    if (ctx.parsedType !== ZodParsedType.string && ctx.parsedType !== ZodParsedType.number) {
      const expectedValues = util.objectValues(nativeEnumValues);
      addIssueToContext(ctx, {
        expected: util.joinValues(expectedValues),
        received: ctx.parsedType,
        code: ZodIssueCode.invalid_type
      });
      return INVALID;
    }
    if (!this._cache) {
      this._cache = new Set(util.getValidEnumValues(this._def.values));
    }
    if (!this._cache.has(input.data)) {
      const expectedValues = util.objectValues(nativeEnumValues);
      addIssueToContext(ctx, {
        received: ctx.data,
        code: ZodIssueCode.invalid_enum_value,
        options: expectedValues
      });
      return INVALID;
    }
    return OK(input.data);
  }
  get enum() {
    return this._def.values;
  }
};
ZodNativeEnum.create = (values, params) => {
  return new ZodNativeEnum({
    values,
    typeName: ZodFirstPartyTypeKind.ZodNativeEnum,
    ...processCreateParams(params)
  });
};
var ZodPromise = class extends ZodType {
  unwrap() {
    return this._def.type;
  }
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.promise && ctx.common.async === false) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.promise,
        received: ctx.parsedType
      });
      return INVALID;
    }
    const promisified = ctx.parsedType === ZodParsedType.promise ? ctx.data : Promise.resolve(ctx.data);
    return OK(promisified.then((data) => {
      return this._def.type.parseAsync(data, {
        path: ctx.path,
        errorMap: ctx.common.contextualErrorMap
      });
    }));
  }
};
ZodPromise.create = (schema, params) => {
  return new ZodPromise({
    type: schema,
    typeName: ZodFirstPartyTypeKind.ZodPromise,
    ...processCreateParams(params)
  });
};
var ZodEffects = class extends ZodType {
  innerType() {
    return this._def.schema;
  }
  sourceType() {
    return this._def.schema._def.typeName === ZodFirstPartyTypeKind.ZodEffects ? this._def.schema.sourceType() : this._def.schema;
  }
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    const effect = this._def.effect || null;
    const checkCtx = {
      addIssue: (arg) => {
        addIssueToContext(ctx, arg);
        if (arg.fatal) {
          status.abort();
        } else {
          status.dirty();
        }
      },
      get path() {
        return ctx.path;
      }
    };
    checkCtx.addIssue = checkCtx.addIssue.bind(checkCtx);
    if (effect.type === "preprocess") {
      const processed = effect.transform(ctx.data, checkCtx);
      if (ctx.common.async) {
        return Promise.resolve(processed).then(async (processed2) => {
          if (status.value === "aborted")
            return INVALID;
          const result2 = await this._def.schema._parseAsync({
            data: processed2,
            path: ctx.path,
            parent: ctx
          });
          if (result2.status === "aborted")
            return INVALID;
          if (result2.status === "dirty")
            return DIRTY(result2.value);
          if (status.value === "dirty")
            return DIRTY(result2.value);
          return result2;
        });
      } else {
        if (status.value === "aborted")
          return INVALID;
        const result2 = this._def.schema._parseSync({
          data: processed,
          path: ctx.path,
          parent: ctx
        });
        if (result2.status === "aborted")
          return INVALID;
        if (result2.status === "dirty")
          return DIRTY(result2.value);
        if (status.value === "dirty")
          return DIRTY(result2.value);
        return result2;
      }
    }
    if (effect.type === "refinement") {
      const executeRefinement = (acc) => {
        const result2 = effect.refinement(acc, checkCtx);
        if (ctx.common.async) {
          return Promise.resolve(result2);
        }
        if (result2 instanceof Promise) {
          throw new Error("Async refinement encountered during synchronous parse operation. Use .parseAsync instead.");
        }
        return acc;
      };
      if (ctx.common.async === false) {
        const inner = this._def.schema._parseSync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        });
        if (inner.status === "aborted")
          return INVALID;
        if (inner.status === "dirty")
          status.dirty();
        executeRefinement(inner.value);
        return { status: status.value, value: inner.value };
      } else {
        return this._def.schema._parseAsync({ data: ctx.data, path: ctx.path, parent: ctx }).then((inner) => {
          if (inner.status === "aborted")
            return INVALID;
          if (inner.status === "dirty")
            status.dirty();
          return executeRefinement(inner.value).then(() => {
            return { status: status.value, value: inner.value };
          });
        });
      }
    }
    if (effect.type === "transform") {
      if (ctx.common.async === false) {
        const base = this._def.schema._parseSync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        });
        if (!isValid(base))
          return INVALID;
        const result2 = effect.transform(base.value, checkCtx);
        if (result2 instanceof Promise) {
          throw new Error(`Asynchronous transform encountered during synchronous parse operation. Use .parseAsync instead.`);
        }
        return { status: status.value, value: result2 };
      } else {
        return this._def.schema._parseAsync({ data: ctx.data, path: ctx.path, parent: ctx }).then((base) => {
          if (!isValid(base))
            return INVALID;
          return Promise.resolve(effect.transform(base.value, checkCtx)).then((result2) => ({
            status: status.value,
            value: result2
          }));
        });
      }
    }
    util.assertNever(effect);
  }
};
ZodEffects.create = (schema, effect, params) => {
  return new ZodEffects({
    schema,
    typeName: ZodFirstPartyTypeKind.ZodEffects,
    effect,
    ...processCreateParams(params)
  });
};
ZodEffects.createWithPreprocess = (preprocess, schema, params) => {
  return new ZodEffects({
    schema,
    effect: { type: "preprocess", transform: preprocess },
    typeName: ZodFirstPartyTypeKind.ZodEffects,
    ...processCreateParams(params)
  });
};
var ZodOptional = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType === ZodParsedType.undefined) {
      return OK(void 0);
    }
    return this._def.innerType._parse(input);
  }
  unwrap() {
    return this._def.innerType;
  }
};
ZodOptional.create = (type, params) => {
  return new ZodOptional({
    innerType: type,
    typeName: ZodFirstPartyTypeKind.ZodOptional,
    ...processCreateParams(params)
  });
};
var ZodNullable = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType === ZodParsedType.null) {
      return OK(null);
    }
    return this._def.innerType._parse(input);
  }
  unwrap() {
    return this._def.innerType;
  }
};
ZodNullable.create = (type, params) => {
  return new ZodNullable({
    innerType: type,
    typeName: ZodFirstPartyTypeKind.ZodNullable,
    ...processCreateParams(params)
  });
};
var ZodDefault = class extends ZodType {
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    let data = ctx.data;
    if (ctx.parsedType === ZodParsedType.undefined) {
      data = this._def.defaultValue();
    }
    return this._def.innerType._parse({
      data,
      path: ctx.path,
      parent: ctx
    });
  }
  removeDefault() {
    return this._def.innerType;
  }
};
ZodDefault.create = (type, params) => {
  return new ZodDefault({
    innerType: type,
    typeName: ZodFirstPartyTypeKind.ZodDefault,
    defaultValue: typeof params.default === "function" ? params.default : () => params.default,
    ...processCreateParams(params)
  });
};
var ZodCatch = class extends ZodType {
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    const newCtx = {
      ...ctx,
      common: {
        ...ctx.common,
        issues: []
      }
    };
    const result2 = this._def.innerType._parse({
      data: newCtx.data,
      path: newCtx.path,
      parent: {
        ...newCtx
      }
    });
    if (isAsync(result2)) {
      return result2.then((result3) => {
        return {
          status: "valid",
          value: result3.status === "valid" ? result3.value : this._def.catchValue({
            get error() {
              return new ZodError(newCtx.common.issues);
            },
            input: newCtx.data
          })
        };
      });
    } else {
      return {
        status: "valid",
        value: result2.status === "valid" ? result2.value : this._def.catchValue({
          get error() {
            return new ZodError(newCtx.common.issues);
          },
          input: newCtx.data
        })
      };
    }
  }
  removeCatch() {
    return this._def.innerType;
  }
};
ZodCatch.create = (type, params) => {
  return new ZodCatch({
    innerType: type,
    typeName: ZodFirstPartyTypeKind.ZodCatch,
    catchValue: typeof params.catch === "function" ? params.catch : () => params.catch,
    ...processCreateParams(params)
  });
};
var ZodNaN = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.nan) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.nan,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return { status: "valid", value: input.data };
  }
};
ZodNaN.create = (params) => {
  return new ZodNaN({
    typeName: ZodFirstPartyTypeKind.ZodNaN,
    ...processCreateParams(params)
  });
};
var BRAND = /* @__PURE__ */ Symbol("zod_brand");
var ZodBranded = class extends ZodType {
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    const data = ctx.data;
    return this._def.type._parse({
      data,
      path: ctx.path,
      parent: ctx
    });
  }
  unwrap() {
    return this._def.type;
  }
};
var ZodPipeline = class _ZodPipeline extends ZodType {
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    if (ctx.common.async) {
      const handleAsync = async () => {
        const inResult = await this._def.in._parseAsync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        });
        if (inResult.status === "aborted")
          return INVALID;
        if (inResult.status === "dirty") {
          status.dirty();
          return DIRTY(inResult.value);
        } else {
          return this._def.out._parseAsync({
            data: inResult.value,
            path: ctx.path,
            parent: ctx
          });
        }
      };
      return handleAsync();
    } else {
      const inResult = this._def.in._parseSync({
        data: ctx.data,
        path: ctx.path,
        parent: ctx
      });
      if (inResult.status === "aborted")
        return INVALID;
      if (inResult.status === "dirty") {
        status.dirty();
        return {
          status: "dirty",
          value: inResult.value
        };
      } else {
        return this._def.out._parseSync({
          data: inResult.value,
          path: ctx.path,
          parent: ctx
        });
      }
    }
  }
  static create(a, b) {
    return new _ZodPipeline({
      in: a,
      out: b,
      typeName: ZodFirstPartyTypeKind.ZodPipeline
    });
  }
};
var ZodReadonly = class extends ZodType {
  _parse(input) {
    const result2 = this._def.innerType._parse(input);
    const freeze2 = (data) => {
      if (isValid(data)) {
        data.value = Object.freeze(data.value);
      }
      return data;
    };
    return isAsync(result2) ? result2.then((data) => freeze2(data)) : freeze2(result2);
  }
  unwrap() {
    return this._def.innerType;
  }
};
ZodReadonly.create = (type, params) => {
  return new ZodReadonly({
    innerType: type,
    typeName: ZodFirstPartyTypeKind.ZodReadonly,
    ...processCreateParams(params)
  });
};
function cleanParams(params, data) {
  const p = typeof params === "function" ? params(data) : typeof params === "string" ? { message: params } : params;
  const p2 = typeof p === "string" ? { message: p } : p;
  return p2;
}
function custom(check, _params = {}, fatal) {
  if (check)
    return ZodAny.create().superRefine((data, ctx) => {
      const r = check(data);
      if (r instanceof Promise) {
        return r.then((r2) => {
          if (!r2) {
            const params = cleanParams(_params, data);
            const _fatal = params.fatal ?? fatal ?? true;
            ctx.addIssue({ code: "custom", ...params, fatal: _fatal });
          }
        });
      }
      if (!r) {
        const params = cleanParams(_params, data);
        const _fatal = params.fatal ?? fatal ?? true;
        ctx.addIssue({ code: "custom", ...params, fatal: _fatal });
      }
      return;
    });
  return ZodAny.create();
}
var late = {
  object: ZodObject.lazycreate
};
var ZodFirstPartyTypeKind;
(function(ZodFirstPartyTypeKind2) {
  ZodFirstPartyTypeKind2["ZodString"] = "ZodString";
  ZodFirstPartyTypeKind2["ZodNumber"] = "ZodNumber";
  ZodFirstPartyTypeKind2["ZodNaN"] = "ZodNaN";
  ZodFirstPartyTypeKind2["ZodBigInt"] = "ZodBigInt";
  ZodFirstPartyTypeKind2["ZodBoolean"] = "ZodBoolean";
  ZodFirstPartyTypeKind2["ZodDate"] = "ZodDate";
  ZodFirstPartyTypeKind2["ZodSymbol"] = "ZodSymbol";
  ZodFirstPartyTypeKind2["ZodUndefined"] = "ZodUndefined";
  ZodFirstPartyTypeKind2["ZodNull"] = "ZodNull";
  ZodFirstPartyTypeKind2["ZodAny"] = "ZodAny";
  ZodFirstPartyTypeKind2["ZodUnknown"] = "ZodUnknown";
  ZodFirstPartyTypeKind2["ZodNever"] = "ZodNever";
  ZodFirstPartyTypeKind2["ZodVoid"] = "ZodVoid";
  ZodFirstPartyTypeKind2["ZodArray"] = "ZodArray";
  ZodFirstPartyTypeKind2["ZodObject"] = "ZodObject";
  ZodFirstPartyTypeKind2["ZodUnion"] = "ZodUnion";
  ZodFirstPartyTypeKind2["ZodDiscriminatedUnion"] = "ZodDiscriminatedUnion";
  ZodFirstPartyTypeKind2["ZodIntersection"] = "ZodIntersection";
  ZodFirstPartyTypeKind2["ZodTuple"] = "ZodTuple";
  ZodFirstPartyTypeKind2["ZodRecord"] = "ZodRecord";
  ZodFirstPartyTypeKind2["ZodMap"] = "ZodMap";
  ZodFirstPartyTypeKind2["ZodSet"] = "ZodSet";
  ZodFirstPartyTypeKind2["ZodFunction"] = "ZodFunction";
  ZodFirstPartyTypeKind2["ZodLazy"] = "ZodLazy";
  ZodFirstPartyTypeKind2["ZodLiteral"] = "ZodLiteral";
  ZodFirstPartyTypeKind2["ZodEnum"] = "ZodEnum";
  ZodFirstPartyTypeKind2["ZodEffects"] = "ZodEffects";
  ZodFirstPartyTypeKind2["ZodNativeEnum"] = "ZodNativeEnum";
  ZodFirstPartyTypeKind2["ZodOptional"] = "ZodOptional";
  ZodFirstPartyTypeKind2["ZodNullable"] = "ZodNullable";
  ZodFirstPartyTypeKind2["ZodDefault"] = "ZodDefault";
  ZodFirstPartyTypeKind2["ZodCatch"] = "ZodCatch";
  ZodFirstPartyTypeKind2["ZodPromise"] = "ZodPromise";
  ZodFirstPartyTypeKind2["ZodBranded"] = "ZodBranded";
  ZodFirstPartyTypeKind2["ZodPipeline"] = "ZodPipeline";
  ZodFirstPartyTypeKind2["ZodReadonly"] = "ZodReadonly";
})(ZodFirstPartyTypeKind || (ZodFirstPartyTypeKind = {}));
var instanceOfType = (cls, params = {
  message: `Input not instance of ${cls.name}`
}) => custom((data) => data instanceof cls, params);
var stringType = ZodString.create;
var numberType = ZodNumber.create;
var nanType = ZodNaN.create;
var bigIntType = ZodBigInt.create;
var booleanType = ZodBoolean.create;
var dateType = ZodDate.create;
var symbolType = ZodSymbol.create;
var undefinedType = ZodUndefined.create;
var nullType = ZodNull.create;
var anyType = ZodAny.create;
var unknownType = ZodUnknown.create;
var neverType = ZodNever.create;
var voidType = ZodVoid.create;
var arrayType = ZodArray.create;
var objectType = ZodObject.create;
var strictObjectType = ZodObject.strictCreate;
var unionType = ZodUnion.create;
var discriminatedUnionType = ZodDiscriminatedUnion.create;
var intersectionType = ZodIntersection.create;
var tupleType = ZodTuple.create;
var recordType = ZodRecord.create;
var mapType = ZodMap.create;
var setType = ZodSet.create;
var functionType = ZodFunction.create;
var lazyType = ZodLazy.create;
var literalType = ZodLiteral.create;
var enumType = ZodEnum.create;
var nativeEnumType = ZodNativeEnum.create;
var promiseType = ZodPromise.create;
var effectsType = ZodEffects.create;
var optionalType = ZodOptional.create;
var nullableType = ZodNullable.create;
var preprocessType = ZodEffects.createWithPreprocess;
var pipelineType = ZodPipeline.create;
var ostring = () => stringType().optional();
var onumber = () => numberType().optional();
var oboolean = () => booleanType().optional();
var coerce = {
  string: ((arg) => ZodString.create({ ...arg, coerce: true })),
  number: ((arg) => ZodNumber.create({ ...arg, coerce: true })),
  boolean: ((arg) => ZodBoolean.create({
    ...arg,
    coerce: true
  })),
  bigint: ((arg) => ZodBigInt.create({ ...arg, coerce: true })),
  date: ((arg) => ZodDate.create({ ...arg, coerce: true }))
};
var NEVER = INVALID;

// packages/core/src/index.ts
var CREDENTIAL_VALUE_PATTERNS = [
  /\bwhsec_[a-z0-9_-]{8,}/iu,
  /\b(?:sk|rk|pk|atk)_(?:live|test)?_?[a-z0-9_-]{8,}/iu,
  /\bsk-[a-z0-9_-]{16,}/iu,
  /\bxkeysib-[a-z0-9_-]{12,}/iu,
  /\bAIza[a-z0-9_-]{30,}/iu,
  /\b(?:gh[pousr]_[a-z0-9]{20,}|github_pat_[a-z0-9_]{20,})\b/iu,
  /\bxox[baprs]-[a-z0-9-]{10,}/iu,
  /\b(?:AKIA|ASIA)[A-Z0-9]{16}\b/u,
  /\bbearer\s+[a-z0-9._~+/=-]{8,}/iu,
  /-----BEGIN [A-Z ]*PRIVATE KEY-----/u,
  /\beyJ[a-z0-9_-]{8,}\.[a-z0-9_-]{8,}\.[a-z0-9_-]{8,}\b/iu,
  /[?&](?:access_token|api_key|token|secret)=[^&\s]{6,}/iu,
  /\b(?:(?:vh|credential)[_-])canary[_-][a-z0-9_-]{6,}/iu
];
function credentialFieldWords(field) {
  return field.replace(/([a-z0-9])([A-Z])/gu, "$1_$2").toLowerCase().split(/[^a-z0-9]+/u).filter(Boolean);
}
function secretBearingField(field) {
  const words = credentialFieldWords(field);
  if (["secret", "password", "token", "credential", "authorization"].some(
    (word) => words.includes(word)
  )) {
    return true;
  }
  const joined = words.join("");
  return ["apikey", "privatekey", "signingkey"].some((marker) => joined.includes(marker));
}
function findCredentialMaterial(value, options = {}) {
  const canaries = [...options.canaries ?? []].filter(Boolean);
  const referenceKeys = new Set(options.allowedCredentialReferenceKeys ?? []);
  const visited = /* @__PURE__ */ new WeakSet();
  const inspect = (candidate, path) => {
    if (typeof candidate === "string") {
      if (canaries.some((canary) => candidate.includes(canary))) {
        return { kind: "registered_canary", path };
      }
      if (CREDENTIAL_VALUE_PATTERNS.some((pattern) => pattern.test(candidate))) {
        return { kind: "credential_pattern", path };
      }
      return null;
    }
    if (candidate === null || typeof candidate === "boolean") return null;
    if (typeof candidate === "number") {
      return Number.isFinite(candidate) ? null : { kind: "non_json_value", path };
    }
    if (typeof candidate !== "object") return { kind: "non_json_value", path };
    if (visited.has(candidate)) return { kind: "non_json_value", path };
    visited.add(candidate);
    if (Array.isArray(candidate)) {
      for (const [index, entry] of candidate.entries()) {
        const finding = inspect(entry, `${path}[${index}]`);
        if (finding) return finding;
      }
      return null;
    }
    const prototype = Object.getPrototypeOf(candidate);
    if (prototype !== Object.prototype && prototype !== null) {
      return { kind: "non_json_value", path };
    }
    for (const [field, entry] of Object.entries(candidate)) {
      const childPath = `${path}.${field}`;
      if (referenceKeys.has(field)) {
        if (typeof entry !== "string" || !/^cred:\/\/[A-Za-z0-9][A-Za-z0-9/_:.-]*$/u.test(entry)) {
          return { kind: "invalid_credential_reference", path: childPath };
        }
      } else if (secretBearingField(field)) {
        return { kind: "secret_bearing_field", path: childPath };
      }
      const finding = inspect(entry, childPath);
      if (finding) return finding;
    }
    return null;
  };
  return inspect(value, "$");
}

// lib/config/contracts.ts
var semverSchema = external_exports.string().regex(/^\d+\.\d+\.\d+$/, "expected semantic version");
var appKindSchema = external_exports.enum(["web", "mobile_ios", "mobile_cross_platform", "hybrid"]);
var launchModeSchema = external_exports.enum([
  "validate_first",
  "thin_mvp",
  "product_first",
  "concierge_first"
]);
var mobileStackSchema = external_exports.enum(["none", "expo_react_native", "swiftui", "auto"]);
var knownCapabilityIdSchema = external_exports.enum([
  "public_website",
  "authenticated_product",
  "database",
  "file_storage",
  "transactional_email",
  "lifecycle_email",
  "stripe",
  "revenuecat",
  "ga4",
  "gsc",
  "bing_webmaster",
  "vercel_analytics",
  "web_seo_aeo_geo",
  "ios_aso",
  "feedback_intake",
  "scheduled_learning_loops",
  "app_store_connect",
  "eas"
]);
var openCapabilityIdSchema = external_exports.string().regex(
  /^x\.[a-z0-9]+(?:[._-][a-z0-9]+)*$/,
  'open capability IDs must be namespaced, for example "x.vendor.capability"'
);
var capabilityIdSchema = external_exports.union([knownCapabilityIdSchema, openCapabilityIdSchema]);
var providerIdSchema = external_exports.string().regex(/^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*$/, "expected a stable provider ID");
var extensionsSchema = external_exports.record(external_exports.unknown()).default({});
function unique(values) {
  return new Set(values).size === values.length;
}
function uniqueArray(item) {
  return external_exports.array(item).refine(unique, "values must be unique");
}
var FORBIDDEN_CREDENTIAL_KEY = /^(access_token|refresh_token|id_token|api_key|secret|client_secret|private_key|password|credential_value)$/i;
function looksLikeCredentialValue(value) {
  return findCredentialMaterial(value) !== null;
}
function rejectCredentialMaterial(value, ctx) {
  const visit = (candidate, path) => {
    if (typeof candidate === "string") {
      if (looksLikeCredentialValue(candidate)) {
        ctx.addIssue({
          code: external_exports.ZodIssueCode.custom,
          path,
          message: "credential values are forbidden; store only a credential_ref"
        });
      }
      return;
    }
    if (Array.isArray(candidate)) {
      candidate.forEach((entry, index) => visit(entry, [...path, index]));
      return;
    }
    if (!candidate || typeof candidate !== "object") return;
    for (const [key, entry] of Object.entries(candidate)) {
      const entryPath = [...path, key];
      if (FORBIDDEN_CREDENTIAL_KEY.test(key)) {
        ctx.addIssue({
          code: external_exports.ZodIssueCode.custom,
          path: entryPath,
          message: "credential value fields are forbidden; use credential_ref"
        });
      } else {
        visit(entry, entryPath);
      }
    }
  };
  visit(value, []);
}
var credentialReferenceSchema = external_exports.string().min(3).max(300).regex(
  /^cred:\/\/[A-Za-z0-9][A-Za-z0-9._/-]*$/,
  "credential_ref must use a logical cred:// reference and contain no credential value"
).refine(
  (value) => !looksLikeCredentialValue(value),
  "credential_ref contains credential material"
);
var artifactReferenceSchema = external_exports.string().min(1).max(500).refine((value) => !value.startsWith("/") && !value.split("/").includes(".."), {
  message: "artifact references must be repository-relative without parent traversal"
});

// lib/config/policy-schema.ts
var authorizationProfileIdSchema = external_exports.enum([
  "read_only",
  "build_local",
  "preview_launch",
  "standard_launch",
  "live_commerce_launch",
  "mobile_testflight",
  "autofix_low_risk"
]);
var riskClassSchema = external_exports.enum(["low", "moderate", "high", "critical"]);
var sideEffectClassSchema = external_exports.enum([
  "none",
  "local_write",
  "git_write",
  "external_read",
  "reversible_external_write",
  "preview_deploy",
  "production_deploy",
  "live_commerce_config",
  "customer_charge",
  "transactional_email",
  "bulk_communication",
  "dns_addition",
  "nameserver_change",
  "destructive_data_change",
  "testflight_upload",
  "app_store_publication",
  "external_delete"
]);
var environmentSchema = external_exports.enum(["local", "test", "preview", "production"]);
var spendLimitSchema = external_exports.object({
  amount: external_exports.number().nonnegative(),
  currency: external_exports.string().regex(/^[A-Z]{3}$/)
}).strict();
var authorizationCapabilitySchema = external_exports.union([
  external_exports.literal("*"),
  capabilityIdSchema,
  external_exports.string().regex(
    /^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*$/,
    "expected a stable graph or provider capability ID"
  )
]);
var authorizationProfileSchema = external_exports.object({
  allowed_capabilities: uniqueArray(authorizationCapabilitySchema),
  allowed_side_effect_classes: uniqueArray(sideEffectClassSchema),
  allowed_risk_classes: uniqueArray(riskClassSchema),
  allowed_environments: uniqueArray(environmentSchema),
  max_estimated_spend: spendLimitSchema.default({ amount: 0, currency: "EUR" }),
  unknown_external_costs_allowed: external_exports.boolean().default(false),
  max_email_recipients: external_exports.number().int().nonnegative().default(0),
  production_deploy_allowed: external_exports.boolean().default(false),
  live_products_and_prices_allowed: external_exports.boolean().default(false),
  actual_charges_allowed: external_exports.boolean().default(false),
  transactional_test_email_allowed: external_exports.boolean().default(false),
  dns_additions_allowed: external_exports.boolean().default(false),
  nameserver_changes_allowed: external_exports.boolean().default(false),
  app_store_submission_allowed: external_exports.boolean().default(false),
  extensions: extensionsSchema
}).strict();
var authorizationEnvelopeSchema = external_exports.object({
  run_id: external_exports.string().regex(/^[a-z0-9][a-z0-9-]{5,100}$/),
  profile: authorizationProfileIdSchema,
  allowed_capabilities: uniqueArray(authorizationCapabilitySchema),
  allowed_side_effect_classes: uniqueArray(sideEffectClassSchema),
  providers: uniqueArray(providerIdSchema),
  environments: uniqueArray(environmentSchema),
  issued_at: external_exports.string().datetime({ offset: true }),
  expires_at: external_exports.string().datetime({ offset: true }),
  max_estimated_spend: spendLimitSchema,
  unknown_external_costs_allowed: external_exports.boolean().default(false),
  max_email_recipients: external_exports.number().int().nonnegative(),
  production_deploy_allowed: external_exports.boolean(),
  live_products_and_prices_allowed: external_exports.boolean(),
  actual_charges_allowed: external_exports.boolean(),
  transactional_test_email_allowed: external_exports.boolean(),
  dns_additions_allowed: external_exports.boolean(),
  nameserver_changes_allowed: external_exports.boolean(),
  app_store_submission_allowed: external_exports.boolean(),
  explicitly_forbidden_actions: uniqueArray(external_exports.string().min(1).max(300)),
  approval_ref: external_exports.string().min(1).max(300),
  extensions: extensionsSchema
}).strict().superRefine((value, ctx) => {
  if (Date.parse(value.expires_at) <= Date.parse(value.issued_at)) {
    ctx.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["expires_at"],
      message: "authorization expiry must be after issue time"
    });
  }
  if (value.actual_charges_allowed && !value.live_products_and_prices_allowed) {
    ctx.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["actual_charges_allowed"],
      message: "actual charges require live product and price configuration permission"
    });
  }
});
var profileSetSchema = external_exports.object({
  read_only: authorizationProfileSchema,
  build_local: authorizationProfileSchema,
  preview_launch: authorizationProfileSchema,
  standard_launch: authorizationProfileSchema,
  live_commerce_launch: authorizationProfileSchema,
  mobile_testflight: authorizationProfileSchema,
  autofix_low_risk: authorizationProfileSchema
}).strict();
var policiesSchema = external_exports.object({
  contract_version: external_exports.literal(1),
  authorization: external_exports.object({
    default_profile: authorizationProfileIdSchema,
    profiles: profileSetSchema,
    active_envelopes: external_exports.array(authorizationEnvelopeSchema),
    always_require_distinct_checkpoint_for: uniqueArray(sideEffectClassSchema),
    extensions: extensionsSchema
  }).strict(),
  extensions: extensionsSchema
}).strict();
function profile(overrides = {}) {
  return {
    allowed_capabilities: ["*"],
    allowed_side_effect_classes: ["none"],
    allowed_risk_classes: ["low"],
    allowed_environments: ["local"],
    max_estimated_spend: { amount: 0, currency: "EUR" },
    unknown_external_costs_allowed: false,
    max_email_recipients: 0,
    production_deploy_allowed: false,
    live_products_and_prices_allowed: false,
    actual_charges_allowed: false,
    transactional_test_email_allowed: false,
    dns_additions_allowed: false,
    nameserver_changes_allowed: false,
    app_store_submission_allowed: false,
    extensions: {},
    ...overrides
  };
}
function createDefaultPoliciesConfig() {
  return policiesSchema.parse({
    contract_version: 1,
    authorization: {
      default_profile: "read_only",
      profiles: {
        read_only: profile({
          allowed_side_effect_classes: ["none", "external_read"],
          allowed_environments: ["local", "test", "preview", "production"]
        }),
        build_local: profile({
          allowed_side_effect_classes: ["none", "local_write", "git_write"],
          allowed_risk_classes: ["low", "moderate"],
          allowed_environments: ["local", "test"]
        }),
        preview_launch: profile({
          allowed_side_effect_classes: [
            "none",
            "local_write",
            "git_write",
            "external_read",
            "reversible_external_write",
            "preview_deploy"
          ],
          allowed_risk_classes: ["low", "moderate", "high"],
          allowed_environments: ["local", "test", "preview"],
          unknown_external_costs_allowed: true
        }),
        standard_launch: profile({
          allowed_side_effect_classes: [
            "none",
            "local_write",
            "git_write",
            "external_read",
            "reversible_external_write",
            "preview_deploy",
            "production_deploy",
            "transactional_email",
            "dns_addition"
          ],
          allowed_risk_classes: ["low", "moderate", "high", "critical"],
          allowed_environments: ["local", "test", "preview", "production"],
          max_email_recipients: 1,
          unknown_external_costs_allowed: true,
          production_deploy_allowed: true,
          transactional_test_email_allowed: true,
          dns_additions_allowed: true
        }),
        live_commerce_launch: profile({
          allowed_side_effect_classes: [
            "none",
            "local_write",
            "git_write",
            "external_read",
            "reversible_external_write",
            "preview_deploy",
            "production_deploy",
            "live_commerce_config",
            "customer_charge",
            "transactional_email",
            "dns_addition"
          ],
          allowed_risk_classes: ["low", "moderate", "high", "critical"],
          allowed_environments: ["local", "test", "preview", "production"],
          max_email_recipients: 1,
          unknown_external_costs_allowed: true,
          production_deploy_allowed: true,
          live_products_and_prices_allowed: true,
          actual_charges_allowed: true,
          transactional_test_email_allowed: true,
          dns_additions_allowed: true
        }),
        mobile_testflight: profile({
          allowed_side_effect_classes: [
            "none",
            "local_write",
            "git_write",
            "external_read",
            "reversible_external_write",
            "preview_deploy",
            "production_deploy",
            "dns_addition",
            "testflight_upload"
          ],
          allowed_risk_classes: ["low", "moderate", "high", "critical"],
          allowed_environments: ["local", "test", "preview", "production"],
          production_deploy_allowed: true,
          unknown_external_costs_allowed: true,
          dns_additions_allowed: true
        }),
        autofix_low_risk: profile({
          allowed_side_effect_classes: ["none", "local_write", "git_write"],
          allowed_environments: ["local", "test"]
        })
      },
      active_envelopes: [],
      always_require_distinct_checkpoint_for: [
        "external_delete",
        "destructive_data_change",
        "nameserver_change",
        "bulk_communication",
        "customer_charge",
        "app_store_publication"
      ],
      extensions: {}
    },
    extensions: {}
  });
}

// lib/authorization/checkpoint-grant.ts
var authorizationCheckpointEvidenceSchema = external_exports.object({
  schema_version: external_exports.literal(1),
  kind: external_exports.literal("authorization_checkpoint_grant"),
  run_id: external_exports.string().min(1).max(128),
  node_id: external_exports.string().min(1).max(128),
  effect: sideEffectClassSchema,
  operation_id: external_exports.string().min(1).max(500),
  status: external_exports.literal("approved"),
  approved_by: external_exports.string().min(1).max(300),
  approved_at: external_exports.string().datetime({ offset: true }),
  reason: external_exports.string().min(1).max(2e3),
  limitations: external_exports.array(external_exports.string().min(1).max(1e3)).max(20).default([])
}).strict().superRefine(rejectCredentialMaterial);
var oneShotCheckpointGrantSchema = external_exports.object({
  grantId: external_exports.string().regex(/^checkpoint-[a-f0-9]{16,64}$/),
  runId: external_exports.string().min(1).max(128),
  nodeId: external_exports.string().min(1).max(128),
  effect: sideEffectClassSchema,
  operationId: external_exports.string().min(1).max(500),
  approvedBy: external_exports.string().min(1).max(300),
  approvedAt: external_exports.string().datetime({ offset: true }),
  evidenceArtifact: artifactReferenceSchema,
  issuedAt: external_exports.string().datetime({ offset: true }),
  expiresAt: external_exports.string().datetime({ offset: true }),
  consumedAt: external_exports.string().datetime({ offset: true }).nullable(),
  consumedAttempt: external_exports.number().int().positive().nullable()
}).strict().superRefine((grant, context2) => {
  if (Date.parse(grant.expiresAt) <= Date.parse(grant.issuedAt)) {
    context2.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["expiresAt"],
      message: "checkpoint grant expiry must be after issue time"
    });
  }
  if (grant.consumedAt === null !== (grant.consumedAttempt === null)) {
    context2.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["consumedAt"],
      message: "checkpoint grant consumption timestamp and attempt must be set together"
    });
  }
});
var CheckpointGrantError = class extends Error {
  constructor(message, code) {
    super(message);
    this.code = code;
    this.name = "CheckpointGrantError";
  }
  code;
};
function issueOneShotCheckpointGrant(input) {
  const grant = oneShotCheckpointGrantSchema.parse({
    grantId: input.grantId,
    runId: input.scope.runId,
    nodeId: input.scope.nodeId,
    effect: input.scope.effect,
    operationId: input.scope.operationId,
    approvedBy: input.approvedBy,
    approvedAt: input.approvedAt,
    evidenceArtifact: input.evidenceArtifact,
    issuedAt: input.issuedAt,
    expiresAt: input.expiresAt,
    consumedAt: null,
    consumedAttempt: null
  });
  if (Date.parse(grant.approvedAt) > Date.parse(grant.issuedAt)) {
    throw new CheckpointGrantError(
      `Checkpoint approval ${grant.approvedAt} is later than issue time ${grant.issuedAt}.`,
      "checkpoint_grant_invalid"
    );
  }
  return grant;
}
function assertScope(grant, scope) {
  if (grant.runId !== scope.runId || grant.nodeId !== scope.nodeId || grant.effect !== scope.effect || grant.operationId !== scope.operationId) {
    throw new CheckpointGrantError(
      `Checkpoint grant ${grant.grantId} does not match run ${scope.runId}, node ${scope.nodeId}, effect ${scope.effect}, and operation ${scope.operationId}.`,
      "checkpoint_grant_scope_mismatch"
    );
  }
}
function consumeOneShotCheckpointGrant(grantInput, input) {
  const grant = oneShotCheckpointGrantSchema.parse(grantInput);
  assertScope(grant, input.scope);
  if (grant.consumedAt !== null) {
    throw new CheckpointGrantError(
      `Checkpoint grant ${grant.grantId} was already consumed at ${grant.consumedAt}.`,
      "checkpoint_grant_consumed"
    );
  }
  if (Date.parse(grant.expiresAt) <= Date.parse(input.now)) {
    throw new CheckpointGrantError(
      `Checkpoint grant ${grant.grantId} expired at ${grant.expiresAt}.`,
      "checkpoint_grant_expired"
    );
  }
  if (!Number.isInteger(input.attempt) || input.attempt < 1) {
    throw new CheckpointGrantError(
      `Checkpoint grant ${grant.grantId} needs a positive workflow attempt.`,
      "checkpoint_grant_invalid"
    );
  }
  return oneShotCheckpointGrantSchema.parse({
    ...grant,
    consumedAt: input.now,
    consumedAttempt: input.attempt
  });
}
function assertConsumedCheckpointGrant(grantInput, input) {
  const grant = oneShotCheckpointGrantSchema.parse(grantInput);
  assertScope(grant, input.scope);
  if (grant.consumedAt === null || grant.consumedAttempt === null) {
    throw new CheckpointGrantError(
      `Checkpoint grant ${grant.grantId} was not atomically consumed before provider execution.`,
      "checkpoint_grant_invalid"
    );
  }
  if (Date.parse(grant.consumedAt) < Date.parse(grant.issuedAt) || Date.parse(grant.consumedAt) >= Date.parse(grant.expiresAt) || Date.parse(grant.consumedAt) > Date.parse(input.now) || Date.parse(grant.expiresAt) <= Date.parse(input.now)) {
    throw new CheckpointGrantError(
      `Checkpoint grant ${grant.grantId} has an invalid consumption timestamp.`,
      "checkpoint_grant_invalid"
    );
  }
}

// lib/workflow/redaction.ts
var SECRET_KEY = /(^|[_-])(cookie|credential|password|private[_-]?key|secret|token|api[_-]?key)($|[_-])/i;
var REFERENCE_KEY = /(^|[_-])(credential|secret|token|api[_-]?key)[_-]?(ref|reference)$/i;
var BEARER = /\bBearer\s+[A-Za-z0-9._~+/=-]{8,}/gi;
var ASSIGNMENT = /\b(password|secret|token|api[_-]?key)\s*[=:]\s*[^\s,;]+/gi;
function redactString(value, secrets) {
  let redacted = value.replace(BEARER, "Bearer [REDACTED]").replace(ASSIGNMENT, "$1=[REDACTED]");
  for (const secret of secrets) {
    if (secret.length >= 4) redacted = redacted.split(secret).join("[REDACTED]");
  }
  return redacted;
}
function sanitizeJson(value, secrets = []) {
  if (value === null || value === void 0) return null;
  if (typeof value === "string") return redactString(value, secrets);
  if (typeof value === "number") return Number.isFinite(value) ? value : String(value);
  if (typeof value === "boolean") return value;
  if (typeof value === "bigint") return value.toString();
  if (value instanceof Date) return value.toISOString();
  if (value instanceof Error) {
    return {
      name: value.name,
      message: redactString(value.message, secrets)
    };
  }
  if (Array.isArray(value)) return value.map((item) => sanitizeJson(item, secrets));
  if (typeof value === "object") {
    const out = {};
    for (const [key, item] of Object.entries(value)) {
      if (item === void 0) continue;
      const isReference = REFERENCE_KEY.test(key) || /(credential|secret|token|apiKey)Ref$/i.test(key);
      out[key] = SECRET_KEY.test(key) && !isReference ? "[REDACTED]" : sanitizeJson(item, secrets);
    }
    return out;
  }
  return redactString(String(value), secrets);
}

// lib/workflow/validation.ts
import { createHash } from "node:crypto";
var ID = /^[a-z0-9][a-z0-9._-]*$/;
function stable(value) {
  if (Array.isArray(value)) return value.map(stable);
  if (value && typeof value === "object") {
    return Object.fromEntries(
      Object.entries(value).sort(([a], [b]) => a.localeCompare(b)).map(([key, item]) => [key, stable(item)])
    );
  }
  return value;
}
function workflowFingerprint(definition2) {
  return createHash("sha256").update(JSON.stringify(stable(definition2))).digest("hex");
}
function topologicalOrder(definition2) {
  const inDegree = /* @__PURE__ */ new Map();
  const dependents = /* @__PURE__ */ new Map();
  for (const node of definition2.nodes) {
    inDegree.set(node.id, node.dependencies.length);
    for (const dependency of node.dependencies) {
      const list = dependents.get(dependency) ?? [];
      list.push(node.id);
      dependents.set(dependency, list);
    }
  }
  const ready = [...inDegree.entries()].filter(([, degree]) => degree === 0).map(([id]) => id).sort();
  const order = [];
  while (ready.length > 0) {
    const id = ready.shift();
    order.push(id);
    for (const dependent of (dependents.get(id) ?? []).sort()) {
      const next = (inDegree.get(dependent) ?? 0) - 1;
      inDegree.set(dependent, next);
      if (next === 0) {
        ready.push(dependent);
        ready.sort();
      }
    }
  }
  return order;
}
function validateWorkflow(definition2) {
  const issues = [];
  if (!ID.test(definition2.id))
    issues.push(`graph id "${definition2.id}" must be lowercase kebab-safe`);
  if (!definition2.name.trim()) issues.push("graph name is required");
  if (!definition2.version.trim()) issues.push("graph version is required");
  if (!Number.isInteger(definition2.maxParallel) || definition2.maxParallel < 1)
    issues.push("maxParallel must be a positive integer");
  if (!Number.isInteger(definition2.maxIterations) || definition2.maxIterations < 1)
    issues.push("maxIterations must be a positive integer");
  if (definition2.nodes.length === 0) issues.push("at least one node is required");
  const ids = /* @__PURE__ */ new Set();
  const idempotencyKeys = /* @__PURE__ */ new Set();
  for (const node of definition2.nodes) {
    validateNode(node, issues);
    if (ids.has(node.id)) issues.push(`duplicate node id "${node.id}"`);
    ids.add(node.id);
    if (node.effect !== "none" && node.effect !== "read") {
      if (idempotencyKeys.has(node.idempotencyKey))
        issues.push(`duplicate side-effect idempotency key "${node.idempotencyKey}"`);
      idempotencyKeys.add(node.idempotencyKey);
      if (node.cache.mode !== "none")
        issues.push(`node "${node.id}" cannot cache a side effect; use verifiedEffects instead`);
    }
  }
  for (const node of definition2.nodes) {
    const dependencySet = /* @__PURE__ */ new Set();
    for (const dependency of node.dependencies) {
      if (!ids.has(dependency))
        issues.push(`node "${node.id}" depends on missing node "${dependency}"`);
      if (dependency === node.id) issues.push(`node "${node.id}" cannot depend on itself`);
      if (dependencySet.has(dependency))
        issues.push(`node "${node.id}" repeats dependency "${dependency}"`);
      dependencySet.add(dependency);
    }
  }
  if (issues.length === 0) {
    const order = topologicalOrder(definition2);
    if (order.length !== definition2.nodes.length) {
      const ordered = new Set(order);
      const cyclic = definition2.nodes.map((node) => node.id).filter((id) => !ordered.has(id)).sort();
      issues.push(`graph contains a cycle involving: ${cyclic.join(", ")}`);
    }
  }
  for (const [category, amount] of Object.entries(definition2.budgets)) {
    if (!category.trim()) issues.push("budget categories cannot be empty");
    if (!Number.isFinite(amount) || amount < 0)
      issues.push(`budget "${category}" must be a non-negative finite number`);
  }
  if (issues.length > 0) throw new WorkflowValidationError(issues);
}
function validateNode(node, issues) {
  const prefix = `node "${node.id}"`;
  if (!ID.test(node.id)) issues.push(`${prefix} id must be lowercase kebab-safe`);
  if (!node.purpose.trim()) issues.push(`${prefix} purpose is required`);
  if (!node.capability.trim()) issues.push(`${prefix} capability is required`);
  if (!node.idempotencyKey.trim()) issues.push(`${prefix} idempotencyKey is required`);
  if (!node.concurrencyGroup.trim()) issues.push(`${prefix} concurrencyGroup is required`);
  if (!node.budgetCategory.trim()) issues.push(`${prefix} budgetCategory is required`);
  if (!Number.isInteger(node.timeoutMs) || node.timeoutMs < 1)
    issues.push(`${prefix} timeoutMs must be a positive integer`);
  if (!Number.isInteger(node.retry.maxAttempts) || node.retry.maxAttempts < 1)
    issues.push(`${prefix} retry.maxAttempts must be a positive integer`);
  if (!Number.isFinite(node.retry.backoff.initialMs) || !Number.isFinite(node.retry.backoff.maxMs) || node.retry.backoff.initialMs < 0 || node.retry.backoff.maxMs < 0)
    issues.push(`${prefix} retry backoff values must be non-negative and finite`);
  if (node.retry.backoff.maxMs < node.retry.backoff.initialMs)
    issues.push(`${prefix} retry.backoff.maxMs cannot be below initialMs`);
  if (!Number.isFinite(node.retry.backoff.multiplier) || node.retry.backoff.multiplier < 1)
    issues.push(`${prefix} retry.backoff.multiplier must be finite and at least 1`);
  if (!Number.isFinite(node.cost.amount) || node.cost.amount < 0)
    issues.push(`${prefix} cost.amount must be a non-negative finite number`);
  if (!node.cost.unit.trim()) issues.push(`${prefix} cost.unit is required`);
  if (node.condition.kind === "handler" && !node.condition.handler)
    issues.push(`${prefix} handler condition requires condition.handler`);
  if (node.kind === "human_approval" && node.transport !== "human_approval")
    issues.push(`${prefix} human_approval nodes must use human_approval transport`);
  if (node.kind === "manual_action" && node.transport !== "manual")
    issues.push(`${prefix} manual_action nodes must use manual transport`);
  if (node.effect === "external_irreversible" && !node.authorization.required)
    issues.push(`${prefix} external irreversible effects require authorization`);
  if (node.reconciliation) {
    if (!node.reconciliation.handler.trim())
      issues.push(`${prefix} reconciliation.handler is required`);
    if (!Number.isInteger(node.reconciliation.pollIntervalMs) || node.reconciliation.pollIntervalMs < 0) {
      issues.push(`${prefix} reconciliation.pollIntervalMs must be a non-negative integer`);
    }
    if (!Number.isInteger(node.reconciliation.maxPollAttempts) || node.reconciliation.maxPollAttempts < 1) {
      issues.push(`${prefix} reconciliation.maxPollAttempts must be a positive integer`);
    }
  }
  if (node.loop && (!Number.isInteger(node.loop.maxIterations) || node.loop.maxIterations < 1)) {
    issues.push(`${prefix} loop.maxIterations must be a positive integer`);
  }
  if (node.loop && node.effect !== "none" && node.effect !== "read") {
    issues.push(
      `${prefix} effectful loops require separate DAG nodes with unique idempotency keys per effect`
    );
  }
}
function defineWorkflow(definition2) {
  validateWorkflow(definition2);
  return definition2;
}
function workflowNode(id, overrides = {}) {
  const base = {
    id,
    purpose: id,
    kind: "code",
    capability: "workflow.test",
    dependencies: [],
    condition: { kind: "dependencies_succeeded" },
    input: {},
    output: {},
    transport: "code",
    handler: id,
    model: { tier: "none" },
    effect: "none",
    risk: "low",
    authorization: { required: false, scopes: [] },
    idempotencyKey: id,
    timeoutMs: 3e4,
    retry: {
      maxAttempts: 1,
      retryableCodes: [],
      backoff: { strategy: "none", initialMs: 0, maxMs: 0, multiplier: 1 }
    },
    concurrencyGroup: id,
    cost: { amount: 0, unit: "credits" },
    budgetCategory: "default",
    cache: { mode: "none" },
    isolation: "none",
    compensation: null,
    evidence: { required: false },
    completion: { description: `${id} completed` }
  };
  return { ...base, ...overrides };
}

// lib/workflow/executor.ts
var TERMINAL_NODE_STATES = /* @__PURE__ */ new Set([
  "succeeded",
  "failed_terminal",
  "skipped",
  "compensated",
  "cancelled"
]);
function isTerminalNode(record3) {
  return TERMINAL_NODE_STATES.has(record3.state);
}
function successfulDependency(record3) {
  return record3.state === "succeeded" || record3.state === "compensated";
}
function runWasCancelled(state) {
  return state.status === "cancelled" || state.status === "superseded";
}
function nodeWasSkipped(record3) {
  return record3.state === "skipped" || record3.state === "cancelled";
}
function currentNodeState(record3) {
  return record3.state;
}
function isProviderPlanCheckpoint(value) {
  return value !== null && value !== void 0 && !Array.isArray(value) && typeof value === "object" && value.kind === "provider_plan";
}
function normalizedValidatorResult(result2) {
  return typeof result2 === "boolean" ? { ok: result2 } : result2;
}
var WorkflowExecutor = class {
  store;
  bindings;
  now;
  sleep;
  activeControllers = /* @__PURE__ */ new Map();
  activeStates = /* @__PURE__ */ new Map();
  constructor(options) {
    this.store = options.store;
    this.bindings = {
      handlers: options.bindings?.handlers ?? {},
      validators: options.bindings?.validators ?? {},
      conditions: options.bindings?.conditions ?? {},
      compensators: options.bindings?.compensators ?? {},
      reconcilers: options.bindings?.reconcilers ?? {},
      workspaceFactory: options.bindings?.workspaceFactory,
      interruptEvidenceVerifier: options.bindings?.interruptEvidenceVerifier,
      checkpointEvidenceVerifier: options.bindings?.checkpointEvidenceVerifier,
      secrets: options.bindings?.secrets ?? []
    };
    this.now = options.now ?? (() => /* @__PURE__ */ new Date());
    this.sleep = options.sleep ?? ((milliseconds) => new Promise((resolve27) => setTimeout(resolve27, milliseconds)));
  }
  create(definition2, options = {}) {
    validateWorkflow(definition2);
    const runId2 = options.runId ?? this.generateRunId(definition2.id);
    const timestamp2 = this.timestamp();
    const limits = { ...definition2.budgets, ...options.budgets };
    const maxIterations = options.maxIterations ?? definition2.maxIterations;
    const maxParallel = options.maxParallel ?? definition2.maxParallel;
    if (!Number.isInteger(maxIterations) || maxIterations < 1) {
      throw new Error("Workflow maxIterations override must be a positive integer.");
    }
    if (!Number.isInteger(maxParallel) || maxParallel < 1) {
      throw new Error("Workflow maxParallel override must be a positive integer.");
    }
    for (const [category, limit] of Object.entries(limits)) {
      if (!category.trim() || !Number.isFinite(limit) || limit < 0) {
        throw new Error(`Workflow budget override "${category}" must be non-negative and finite.`);
      }
    }
    const consumed = Object.fromEntries(
      [...new Set(definition2.nodes.map((node) => node.budgetCategory))].map((category) => [
        category,
        0
      ])
    );
    const state = {
      schemaVersion: 1,
      runId: runId2,
      graph: {
        id: definition2.id,
        name: definition2.name,
        version: definition2.version,
        fingerprint: workflowFingerprint(definition2)
      },
      status: "created",
      nodes: Object.fromEntries(
        definition2.nodes.map((node) => [
          node.id,
          {
            definition: sanitizeJson(
              node,
              this.bindings.secrets
            ),
            state: "pending",
            attempts: 0,
            iterationAttempts: 0,
            effectVerified: false,
            cost: 0,
            revision: 0,
            loopIterations: 0,
            costEntries: []
          }
        ])
      ),
      verifiedEffects: {},
      checkpointGrants: {},
      authorizationSpend: { currency: null, totalAmount: 0, reservations: {} },
      cache: {},
      budget: { limits, consumed },
      iterations: 0,
      maxIterations,
      maxParallel,
      eventSequence: 0,
      steeringRevision: 0,
      costs: [],
      supersedesRunId: options.supersedesRunId,
      createdAt: timestamp2,
      updatedAt: timestamp2
    };
    this.store.create(state);
    this.record(state, "run_created", void 0, {
      graphId: definition2.id,
      graphVersion: definition2.version
    });
    return state;
  }
  async start(definition2, options = {}) {
    const state = this.create(definition2, options);
    return this.execute(definition2, state, false);
  }
  enqueue(definition2, options = {}) {
    const state = this.create(definition2, options);
    state.status = "queued";
    state.queuedAt = this.timestamp();
    this.record(state, "run_queued", void 0, {
      queuedAt: state.queuedAt,
      supersedesRunId: state.supersedesRunId ?? null
    });
    return state;
  }
  listQueue() {
    return this.store.listRuns().map((runId2) => this.getState(runId2)).filter(({ status }) => status === "queued").sort(
      (left, right) => (left.queuedAt ?? left.createdAt).localeCompare(right.queuedAt ?? right.createdAt)
    );
  }
  async startQueued(definition2, runId2) {
    const state = this.store.load(runId2);
    this.normalizeState(state);
    this.assertGraphMatch(definition2, state);
    if (state.status !== "queued" && state.status !== "created") {
      throw new Error(`Workflow run "${runId2}" is ${state.status}; only queued runs can start.`);
    }
    return this.execute(definition2, state, false);
  }
  async resume(definition2, runId2) {
    validateWorkflow(definition2);
    const state = this.store.load(runId2);
    this.normalizeState(state);
    this.assertGraphMatch(definition2, state);
    if (state.status === "succeeded" || state.status === "failed" || state.status === "cancelled" || state.status === "superseded") {
      return state;
    }
    return this.execute(definition2, state, true);
  }
  getState(runId2) {
    const state = this.store.load(runId2);
    this.normalizeState(state);
    return state;
  }
  async approve(runId2, nodeId, resolution) {
    return this.resolveInterrupt(runId2, nodeId, "waiting_for_approval", resolution);
  }
  async completeManualAction(runId2, nodeId, resolution) {
    return this.resolveInterrupt(runId2, nodeId, "waiting_for_manual_action", resolution);
  }
  async refreshAuthorization(runId2, nodeId, resolution) {
    const state = this.store.load(runId2);
    this.normalizeState(state);
    const record3 = state.nodes[nodeId];
    if (!record3) throw new Error(`Workflow run "${runId2}" has no node "${nodeId}".`);
    if (record3.state !== "waiting_for_auth") {
      throw new Error(`Node "${nodeId}" is ${record3.state}; expected waiting_for_auth.`);
    }
    if (!resolution.authorizedBy.trim()) {
      throw new Error(`Authorization refresh for node "${nodeId}" requires an identity.`);
    }
    if (resolution.credentialRef !== void 0 && !/^cred:\/\/[a-z0-9][a-z0-9._/-]*$/i.test(resolution.credentialRef)) {
      throw new Error("Authorization refresh accepts only a credential reference, never a value.");
    }
    if (resolution.expiresAt !== void 0 && (!Number.isFinite(Date.parse(resolution.expiresAt)) || Date.parse(resolution.expiresAt) <= Date.parse(this.timestamp()))) {
      throw new Error("Authorization refresh expiry must be a future ISO timestamp.");
    }
    record3.state = record3.operation ? "failed_retryable" : "pending";
    record3.error = void 0;
    record3.waiting = void 0;
    record3.startedAt = void 0;
    record3.finishedAt = void 0;
    state.status = "created";
    state.finishedAt = void 0;
    this.record(state, "interrupt_resolved", nodeId, {
      kind: "auth",
      authorizedBy: resolution.authorizedBy,
      credentialRef: resolution.credentialRef ?? null,
      expiresAt: resolution.expiresAt ?? null,
      note: resolution.note ?? null
    });
    return state;
  }
  async reconcile(runId2, nodeId, reason = "poll") {
    const state = this.activeStates.get(runId2) ?? this.store.load(runId2);
    this.normalizeState(state);
    const record3 = state.nodes[nodeId];
    if (!record3) throw new Error(`Workflow run "${runId2}" has no node "${nodeId}".`);
    await this.reconcileNode(state, record3, reason);
    return state;
  }
  async steer(definition2, runId2, request2) {
    validateWorkflow(definition2);
    const state = this.activeStates.get(runId2) ?? this.store.load(runId2);
    this.normalizeState(state);
    this.assertGraphMatch(definition2, state);
    if (!request2.reason.trim() || !request2.steeredBy.trim()) {
      throw new Error("A steer requires both reason and steeredBy.");
    }
    const targets = Object.keys(request2.inputs).sort();
    if (targets.length === 0) throw new Error("A steer requires at least one node input.");
    for (const target of targets) {
      if (!state.nodes[target]) throw new Error(`Workflow run "${runId2}" has no node "${target}".`);
    }
    const affected = this.affectedDescendants(state, targets);
    for (const nodeId of affected) {
      const record3 = state.nodes[nodeId];
      const effectful = record3.definition.effect !== "none" && record3.definition.effect !== "read";
      if (effectful && (record3.effectVerified || Boolean(state.verifiedEffects[record3.definition.idempotencyKey]) || record3.attempts > 0 || Boolean(record3.operation))) {
        throw new Error(
          `Steer would invalidate effectful node "${nodeId}" after execution began; supersede the run instead.`
        );
      }
    }
    for (const nodeId of affected) {
      this.activeControllers.get(`${runId2}:${nodeId}`)?.abort("workflow steered");
      const record3 = state.nodes[nodeId];
      record3.revision = (record3.revision ?? 0) + 1;
      record3.state = "pending";
      record3.attempts = 0;
      record3.loopIterations = 0;
      record3.startedAt = void 0;
      record3.finishedAt = void 0;
      record3.output = void 0;
      record3.error = void 0;
      record3.effectVerified = false;
      record3.evidenceArtifact = void 0;
      record3.skipReason = void 0;
      record3.operation = void 0;
      record3.waiting = void 0;
      record3.workspace = void 0;
      const cacheKey = record3.definition.cache.key ?? record3.definition.idempotencyKey;
      delete state.cache[cacheKey];
      if (Object.hasOwn(request2.inputs, nodeId)) {
        record3.definition = {
          ...record3.definition,
          input: { ...record3.definition.input, value: request2.inputs[nodeId] }
        };
      }
    }
    state.steeringRevision = (state.steeringRevision ?? 0) + 1;
    state.finishedAt = void 0;
    if (!this.activeStates.has(runId2)) {
      state.status = state.status === "queued" ? "queued" : "created";
    }
    const preserved = Object.keys(state.nodes).filter((nodeId) => !affected.has(nodeId)).sort();
    this.record(state, "run_steered", void 0, {
      revision: state.steeringRevision,
      steeredBy: request2.steeredBy,
      reason: request2.reason,
      targets,
      invalidated: [...affected].sort(),
      preserved
    });
    return state;
  }
  async supersede(runId2, currentDefinition, replacementDefinition, options) {
    validateWorkflow(currentDefinition);
    validateWorkflow(replacementDefinition);
    const state = this.activeStates.get(runId2) ?? this.store.load(runId2);
    this.normalizeState(state);
    this.assertGraphMatch(currentDefinition, state);
    if (["succeeded", "failed", "cancelled", "superseded"].includes(state.status)) {
      throw new Error(`Workflow run "${runId2}" is already terminal (${state.status}).`);
    }
    if (!options.reason.trim() || !options.supersededBy.trim()) {
      throw new Error("Supersede requires both reason and supersededBy.");
    }
    const replacementOptions = {
      runId: options.runId,
      maxParallel: options.maxParallel,
      maxIterations: options.maxIterations,
      budgets: options.budgets,
      supersedesRunId: runId2
    };
    const replacement = options.queue === false ? this.create(replacementDefinition, replacementOptions) : this.enqueue(replacementDefinition, replacementOptions);
    state.status = "superseded";
    state.supersededByRunId = replacement.runId;
    state.finishedAt = this.timestamp();
    const cancelledNodes = [];
    const skippedNodes = [];
    const interruptedEffects = [];
    for (const record3 of Object.values(state.nodes)) {
      if (isTerminalNode(record3)) continue;
      const wasRunning = record3.state === "running";
      record3.state = wasRunning ? "cancelled" : "skipped";
      record3.skipReason = `run superseded by ${replacement.runId}: ${options.reason}`;
      record3.finishedAt = this.timestamp();
      this.activeControllers.get(`${runId2}:${record3.definition.id}`)?.abort("workflow superseded");
      (wasRunning ? cancelledNodes : skippedNodes).push(record3.definition.id);
      if (wasRunning && this.isEffectful(record3.definition)) interruptedEffects.push(record3);
    }
    this.record(state, "run_superseded", void 0, {
      replacementRunId: replacement.runId,
      supersededBy: options.supersededBy,
      reason: options.reason,
      cancelledNodes: cancelledNodes.sort(),
      skippedNodes: skippedNodes.sort()
    });
    for (const nodeId of cancelledNodes) {
      this.record(state, "node_cancelled", nodeId, {
        reason: `run superseded by ${replacement.runId}`
      });
    }
    for (const nodeId of skippedNodes) {
      this.record(state, "node_skipped", nodeId, {
        reason: `run superseded by ${replacement.runId}`
      });
    }
    for (const record3 of interruptedEffects) {
      await this.reconcileNode(state, record3, "cancel");
    }
    const readyReplacement = options.queue === false ? await this.execute(replacementDefinition, replacement, false) : replacement;
    return { superseded: state, replacement: readyReplacement };
  }
  async grantAuthorizationCheckpoint(runId2, nodeId, resolution) {
    const state = this.store.load(runId2);
    const record3 = state.nodes[nodeId];
    if (!record3) throw new Error(`Workflow run "${runId2}" has no node "${nodeId}".`);
    if (record3.state !== "waiting_for_approval" || record3.error?.code !== "authorization_checkpoint_required") {
      throw new Error(`Node "${nodeId}" is not waiting for a provider authorization checkpoint.`);
    }
    const requirements = this.checkpointRequirements(record3.error.details);
    const requestedAt = this.checkpointRequestedAt(record3.error.details);
    if (!requirements || !requestedAt) {
      throw new Error(`Node "${nodeId}" has invalid persisted checkpoint requirements.`);
    }
    const requirement = requirements.find(
      ({ effect, operationId: operationId2 }) => effect === resolution.effect && operationId2 === resolution.operationId
    );
    if (!requirement) {
      throw new Error(
        `Checkpoint scope ${resolution.effect}/${resolution.operationId} was not requested by node "${nodeId}".`
      );
    }
    if (!resolution.approvedBy.trim()) {
      throw new Error(`Checkpoint for node "${nodeId}" requires an approvedBy identity.`);
    }
    if (Date.parse(resolution.approvedAt) < Date.parse(requestedAt)) {
      throw new Error(
        `Checkpoint approval for node "${nodeId}" predates the current request at ${requestedAt}.`
      );
    }
    const verifyEvidence = this.bindings.checkpointEvidenceVerifier;
    if (!verifyEvidence) {
      throw new Error(
        `Node "${nodeId}" has a dangerous effect but no checkpoint evidence verifier is registered.`
      );
    }
    const verified = normalizedValidatorResult(
      await verifyEvidence({
        runId: runId2,
        node: record3.definition,
        effect: resolution.effect,
        operationId: resolution.operationId,
        evidenceArtifact: resolution.evidenceArtifact,
        approvedBy: resolution.approvedBy,
        approvedAt: resolution.approvedAt
      })
    );
    if (!verified.ok) {
      throw new Error(
        verified.message ?? `Checkpoint evidence for node "${nodeId}" was not verified.`
      );
    }
    const now = this.timestamp();
    const scope = {
      runId: runId2,
      nodeId,
      effect: resolution.effect,
      operationId: resolution.operationId
    };
    const priorGrants = Object.values(state.checkpointGrants ?? {});
    if (priorGrants.some(({ evidenceArtifact }) => evidenceArtifact === resolution.evidenceArtifact)) {
      throw new Error(
        `Checkpoint evidence ${resolution.evidenceArtifact} has already issued a one-shot grant.`
      );
    }
    if (priorGrants.some(
      (grant2) => grant2.runId === runId2 && grant2.nodeId === nodeId && grant2.effect === resolution.effect && grant2.operationId === resolution.operationId && Date.parse(grant2.approvedAt) >= Date.parse(resolution.approvedAt)
    )) {
      throw new Error(
        `Checkpoint for node "${nodeId}" requires a fresh approval after the prior one-shot grant.`
      );
    }
    const existing = this.availableCheckpointGrant(state, scope, now);
    if (existing) {
      throw new Error(
        `Node "${nodeId}" already has an unused checkpoint grant for ${resolution.effect}/${resolution.operationId}.`
      );
    }
    const grant = issueOneShotCheckpointGrant({
      grantId: `checkpoint-${randomBytes(16).toString("hex")}`,
      scope,
      approvedBy: resolution.approvedBy,
      approvedAt: resolution.approvedAt,
      evidenceArtifact: resolution.evidenceArtifact,
      issuedAt: now,
      expiresAt: resolution.expiresAt
    });
    state.checkpointGrants ??= {};
    state.checkpointGrants[grant.grantId] = grant;
    const allGranted = requirements.every(
      ({ effect, operationId: operationId2 }) => Boolean(this.availableCheckpointGrant(state, { runId: runId2, nodeId, effect, operationId: operationId2 }, now))
    );
    if (allGranted) {
      record3.state = "pending";
      record3.error = void 0;
      record3.startedAt = void 0;
      record3.finishedAt = void 0;
      state.status = "created";
      state.finishedAt = void 0;
    }
    this.record(state, "checkpoint_grant_issued", nodeId, {
      grantId: grant.grantId,
      effect: grant.effect,
      operationId: grant.operationId,
      approvedBy: grant.approvedBy,
      approvedAt: grant.approvedAt,
      evidenceArtifact: grant.evidenceArtifact,
      expiresAt: grant.expiresAt,
      ready: allGranted
    });
    return state;
  }
  async cancel(runId2, reason, definition2) {
    const state = this.activeStates.get(runId2) ?? this.store.load(runId2);
    this.normalizeState(state);
    if (state.status === "succeeded" || state.status === "failed" || state.status === "cancelled" || state.status === "superseded") {
      return state;
    }
    if (definition2) this.assertGraphMatch(definition2, state);
    if (!reason.trim()) throw new Error("Workflow cancellation requires a reason.");
    state.status = "cancelled";
    state.cancellationReason = reason;
    state.finishedAt = this.timestamp();
    const interruptedEffects = [];
    const cancelledNodes = [];
    const skippedNodes = [];
    for (const record3 of Object.values(state.nodes)) {
      if (!isTerminalNode(record3)) {
        const wasRunning = record3.state === "running";
        record3.state = wasRunning ? "cancelled" : "skipped";
        record3.skipReason = `run cancelled: ${reason}`;
        record3.finishedAt = this.timestamp();
        if (wasRunning && this.isEffectful(record3.definition)) interruptedEffects.push(record3);
        (wasRunning ? cancelledNodes : skippedNodes).push(record3.definition.id);
      }
    }
    for (const [key, controller] of this.activeControllers) {
      if (key.startsWith(`${runId2}:`)) controller.abort(reason);
    }
    this.record(state, "run_cancelled", void 0, {
      reason,
      cancelledNodes: cancelledNodes.sort(),
      skippedNodes: skippedNodes.sort()
    });
    for (const nodeId of cancelledNodes) {
      this.record(state, "node_cancelled", nodeId, { reason });
    }
    for (const nodeId of skippedNodes) {
      this.record(state, "node_skipped", nodeId, { reason: `run cancelled: ${reason}` });
    }
    for (const record3 of interruptedEffects) {
      await this.reconcileNode(state, record3, "cancel");
    }
    if (definition2) await this.compensate(definition2, state, "cancel");
    return state;
  }
  async execute(definition2, state, recovering) {
    this.assertGraphMatch(definition2, state);
    this.activeStates.set(state.runId, state);
    try {
      this.normalizeState(state);
      if (recovering) await this.recoverInterruptedNodes(state);
      state.status = "running";
      this.record(state, "run_started", void 0, { recovering });
      const order = topologicalOrder(definition2);
      while (state.status === "running") {
        if (state.iterations >= state.maxIterations) {
          this.failUnfinishedNodes(
            state,
            "MAX_ITERATIONS_EXCEEDED",
            `Workflow exceeded ${state.maxIterations} scheduler iterations.`
          );
          break;
        }
        await this.prepareNodes(order, state);
        const batch = this.selectReadyBatch(order, state);
        if (batch.length > 0) {
          state.iterations += 1;
          await Promise.all(batch.map((nodeId) => this.executeNode(state, nodeId)));
          continue;
        }
        const records = Object.values(state.nodes);
        if (records.some((record3) => record3.state === "running")) continue;
        if (records.some((record3) => record3.state === "failed_terminal")) {
          this.skipUnfinishedNodesAfterFailure(state);
          break;
        }
        if (records.some(
          (record3) => record3.state === "waiting_for_approval" || record3.state === "waiting_for_manual_action" || record3.state === "waiting_for_auth" || record3.state === "waiting_for_external_action"
        )) {
          state.status = "waiting";
          this.record(state, "run_waiting", void 0, {
            nodes: records.filter(
              (record3) => record3.state === "waiting_for_approval" || record3.state === "waiting_for_manual_action" || record3.state === "waiting_for_auth" || record3.state === "waiting_for_external_action"
            ).map((record3) => record3.definition.id)
          });
          return state;
        }
        if (records.every(isTerminalNode)) {
          state.status = "succeeded";
          state.finishedAt = this.timestamp();
          this.record(state, "run_succeeded");
          return state;
        }
        this.failUnfinishedNodes(
          state,
          "SCHEDULER_DEADLOCK",
          "No node was runnable and no persisted interrupt explained the pause."
        );
        break;
      }
      if (!runWasCancelled(state)) {
        await this.compensate(definition2, state, "failure");
        state.status = "failed";
        state.finishedAt = this.timestamp();
        this.record(state, "run_failed");
      }
      return state;
    } finally {
      this.activeStates.delete(state.runId);
    }
  }
  async recoverInterruptedNodes(state) {
    const recovered = [];
    for (const record3 of Object.values(state.nodes)) {
      if (record3.state === "running" || record3.state === "failed_retryable") {
        const verified = state.verifiedEffects[record3.definition.idempotencyKey];
        if (verified) {
          record3.state = "succeeded";
          record3.output = verified.output;
          record3.evidenceArtifact = verified.evidenceArtifact;
          record3.effectVerified = true;
          record3.finishedAt = verified.verifiedAt;
          if (record3.operation) {
            record3.operation.phase = "verified";
            record3.operation.updatedAt = verified.verifiedAt;
          }
        } else if (this.requiresOutcomeReconciliation(record3)) {
          if (!record3.operation) {
            const timestamp2 = this.timestamp();
            record3.operation = {
              attempt: Math.max(1, record3.attempts),
              idempotencyKey: record3.definition.idempotencyKey,
              phase: "prepared",
              preparedAt: record3.startedAt ?? timestamp2,
              updatedAt: timestamp2,
              reconcileAttempts: 0
            };
          }
          await this.reconcileNode(state, record3, "restart");
        } else {
          record3.state = "pending";
          record3.startedAt = void 0;
        }
        recovered.push(record3.definition.id);
      } else if (record3.state === "waiting_for_external_action") {
        await this.reconcileNode(state, record3, "poll");
        recovered.push(record3.definition.id);
      }
    }
    if (recovered.length > 0) this.record(state, "run_recovered", void 0, { nodes: recovered });
  }
  async prepareNodes(order, state) {
    for (const nodeId of order) {
      const record3 = state.nodes[nodeId];
      if (record3.state !== "pending") continue;
      const dependencies = record3.definition.dependencies.map((id) => state.nodes[id]);
      if (dependencies.some(
        (dependency) => dependency.state === "failed_terminal" || dependency.state === "skipped" || dependency.state === "cancelled"
      )) {
        record3.state = "skipped";
        record3.skipReason = "a dependency did not complete successfully";
        record3.finishedAt = this.timestamp();
        this.record(state, "node_skipped", nodeId, { reason: record3.skipReason });
        continue;
      }
      if (!dependencies.every(successfulDependency)) continue;
      try {
        const shouldRun = await this.evaluateCondition(state, record3.definition);
        if (!shouldRun) {
          record3.state = "skipped";
          record3.skipReason = "condition evaluated to false";
          record3.finishedAt = this.timestamp();
          this.record(state, "node_skipped", nodeId, { reason: record3.skipReason });
          continue;
        }
      } catch (error) {
        this.markTerminalFailure(state, record3, this.normalizeError(error, "CONDITION_FAILED"));
        continue;
      }
      record3.state = "ready";
      this.record(state, "node_ready", nodeId);
    }
  }
  selectReadyBatch(order, state) {
    const selected = [];
    const groups = /* @__PURE__ */ new Set();
    for (const nodeId of order) {
      const record3 = state.nodes[nodeId];
      if (record3.state !== "ready") continue;
      if (groups.has(record3.definition.concurrencyGroup)) continue;
      selected.push(nodeId);
      groups.add(record3.definition.concurrencyGroup);
      if (selected.length >= state.maxParallel) break;
    }
    return selected;
  }
  async executeNode(state, nodeId) {
    const record3 = state.nodes[nodeId];
    const node = record3.definition;
    const verified = state.verifiedEffects[node.idempotencyKey];
    if (verified) {
      record3.state = "succeeded";
      record3.output = verified.output;
      record3.evidenceArtifact = verified.evidenceArtifact;
      record3.effectVerified = true;
      record3.finishedAt = verified.verifiedAt;
      this.record(state, "node_reused", nodeId, { idempotencyKey: node.idempotencyKey });
      return;
    }
    const cacheKey = node.cache.key ?? node.idempotencyKey;
    if (node.cache.mode !== "none" && Object.hasOwn(state.cache, cacheKey)) {
      record3.state = "succeeded";
      record3.output = state.cache[cacheKey];
      record3.finishedAt = this.timestamp();
      this.record(state, "node_reused", nodeId, { cacheKey });
      return;
    }
    if (node.kind === "human_approval" || node.transport === "human_approval") {
      record3.state = "waiting_for_approval";
      this.record(state, "node_waiting", nodeId, {
        state: record3.state,
        requiredAuthorization: node.authorization
      });
      return;
    }
    if (node.kind === "manual_action" || node.transport === "manual") {
      record3.state = "waiting_for_manual_action";
      this.record(state, "node_waiting", nodeId, {
        state: record3.state,
        completion: node.completion.description
      });
      return;
    }
    const loopLimit = node.loop?.maxIterations ?? 1;
    if ((record3.loopIterations ?? 0) >= loopLimit) {
      this.markTerminalFailure(state, record3, {
        code: "LOOP_LIMIT_EXCEEDED",
        message: `Node "${node.id}" exceeded its ${loopLimit}-iteration loop limit.`,
        retryable: false
      });
      return;
    }
    if ((record3.iterationAttempts ?? 0) >= node.retry.maxAttempts) {
      this.markTerminalFailure(state, record3, {
        code: "RETRY_LIMIT_EXCEEDED",
        message: `Node "${node.id}" exhausted ${node.retry.maxAttempts} attempts.`,
        retryable: false
      });
      return;
    }
    const budgetError = this.budgetError(state, node);
    if (budgetError) {
      this.markTerminalFailure(state, record3, budgetError);
      return;
    }
    record3.attempts += 1;
    record3.iterationAttempts = (record3.iterationAttempts ?? 0) + 1;
    record3.state = "running";
    record3.startedAt = this.timestamp();
    record3.error = void 0;
    record3.waiting = void 0;
    const executionRevision = record3.revision ?? 0;
    this.record(state, "node_started", nodeId, {
      attempt: record3.attempts,
      iterationAttempt: record3.iterationAttempts,
      loopIteration: (record3.loopIterations ?? 0) + 1,
      revision: executionRevision
    });
    try {
      const costBefore = record3.cost;
      const result2 = await this.invokeHandler(state, record3);
      if (runWasCancelled(state) || nodeWasSkipped(record3) || record3.state !== "running" || (record3.revision ?? 0) !== executionRevision) {
        return;
      }
      if (record3.operation) {
        record3.operation.phase = "handler_completed";
        record3.operation.updatedAt = this.timestamp();
        this.record(state, "node_operation_checkpointed", nodeId, {
          attempt: record3.attempts,
          phase: record3.operation.phase
        });
      }
      if (result2.wait) {
        this.markWaiting(state, record3, result2.wait);
        return;
      }
      await this.validateResult(state, node, result2);
      if (this.isEffectful(node) && result2.effectVerified !== true) {
        throw new WorkflowExecutionError(
          "EFFECT_UNVERIFIED",
          `Node "${node.id}" reported an effect without verified read-back evidence.`
        );
      }
      if (node.evidence.required && !result2.evidenceArtifact) {
        throw new WorkflowExecutionError(
          "EVIDENCE_MISSING",
          `Node "${node.id}" requires an evidence artifact before completion.`
        );
      }
      record3.output = result2.output === void 0 ? void 0 : sanitizeJson(result2.output, this.bindings.secrets);
      record3.effectVerified = result2.effectVerified ?? false;
      record3.evidenceArtifact = result2.evidenceArtifact;
      record3.finishedAt = this.timestamp();
      if (record3.effectVerified) {
        state.verifiedEffects[node.idempotencyKey] = {
          nodeId,
          output: record3.output,
          evidenceArtifact: record3.evidenceArtifact,
          verifiedAt: record3.finishedAt
        };
        if (record3.operation) {
          record3.operation.phase = "verified";
          record3.operation.updatedAt = record3.finishedAt;
        }
        this.record(state, "node_operation_checkpointed", nodeId, {
          attempt: record3.attempts,
          phase: "verified",
          evidenceArtifact: record3.evidenceArtifact ?? null
        });
      }
      for (const charge of result2.costs ?? []) this.recordCost(state, record3, charge);
      if (result2.cost !== void 0) {
        this.recordCost(state, record3, {
          kind: node.kind === "model" ? "model" : node.kind === "provider" ? "provider" : "code",
          category: node.budgetCategory,
          amount: result2.cost,
          unit: node.cost.unit
        });
      } else if ((result2.costs?.length ?? 0) === 0 && record3.cost === costBefore) {
        this.recordCost(state, record3, {
          kind: node.kind === "model" ? "model" : node.kind === "provider" ? "provider" : "code",
          category: node.budgetCategory,
          amount: node.cost.amount,
          unit: node.cost.unit
        });
      }
      record3.loopIterations = (record3.loopIterations ?? 0) + 1;
      record3.iterationAttempts = 0;
      if (result2.continueLoop) {
        if (!node.loop) {
          this.markTerminalFailure(state, record3, {
            code: "LOOP_NOT_DECLARED",
            message: `Node "${node.id}" requested another iteration without a loop policy.`,
            retryable: false
          });
          return;
        }
        if (record3.loopIterations >= node.loop.maxIterations) {
          this.markTerminalFailure(state, record3, {
            code: "LOOP_LIMIT_EXCEEDED",
            message: `Node "${node.id}" requested iteration ${record3.loopIterations + 1}, beyond its ${node.loop.maxIterations}-iteration limit.`,
            retryable: false
          });
          return;
        }
        record3.state = "ready";
        record3.startedAt = void 0;
        record3.finishedAt = void 0;
        record3.operation = void 0;
        this.record(state, "node_ready", nodeId, {
          loopIteration: record3.loopIterations + 1
        });
        return;
      }
      if (node.cache.mode !== "none" && record3.output !== void 0) {
        state.cache[cacheKey] = record3.output;
      }
      record3.state = "succeeded";
      this.record(state, "node_succeeded", nodeId, {
        attempt: record3.attempts,
        loopIterations: record3.loopIterations,
        effectVerified: record3.effectVerified,
        evidenceArtifact: record3.evidenceArtifact ?? null
      });
    } catch (error) {
      if (runWasCancelled(state) || nodeWasSkipped(record3) || (record3.revision ?? 0) !== executionRevision) {
        return;
      }
      const normalized = this.normalizeError(error);
      if (this.errorConfirmsNoWrite(normalized)) {
        this.checkpointNoWrite(state, record3, "handler reported a definitive no-write outcome");
      }
      if (normalized.code === "authorization_checkpoint_required" && this.checkpointRequirements(normalized.details)) {
        record3.attempts = Math.max(0, record3.attempts - 1);
        record3.iterationAttempts = Math.max(0, (record3.iterationAttempts ?? 1) - 1);
        record3.state = "waiting_for_approval";
        record3.startedAt = void 0;
        record3.finishedAt = void 0;
        record3.error = normalized;
        this.record(state, "node_waiting", nodeId, {
          state: record3.state,
          checkpoint: normalized.details
        });
        return;
      }
      if (this.isAuthorizationError(normalized)) {
        this.markWaiting(state, record3, {
          kind: "auth",
          reason: normalized.message
        });
        record3.error = normalized;
        return;
      }
      if (this.isExternalWaitError(normalized)) {
        this.markWaiting(state, record3, {
          kind: "external",
          reason: normalized.message
        });
        record3.error = normalized;
        return;
      }
      const permittedCode = node.retry.retryableCodes.length === 0 || node.retry.retryableCodes.includes(normalized.code);
      const canRetry = normalized.retryable && permittedCode && (record3.iterationAttempts ?? 0) < node.retry.maxAttempts;
      if (canRetry) {
        record3.state = "failed_retryable";
        record3.error = normalized;
        this.record(state, "node_retryable_failure", nodeId, {
          attempt: record3.attempts,
          error: normalized
        });
        if (this.requiresOutcomeReconciliation(record3)) {
          await this.reconcileNode(state, record3, "retry");
          if (currentNodeState(record3) !== "pending") return;
        }
        const delay = this.backoffDelay(node, record3.iterationAttempts ?? 1);
        if (delay > 0) await this.sleep(delay);
        if (state.status !== "running") return;
        record3.state = "ready";
        this.record(state, "node_ready", nodeId, { retry: (record3.iterationAttempts ?? 0) + 1 });
        return;
      }
      if (this.requiresOutcomeReconciliation(record3)) {
        await this.reconcileNode(state, record3, "retry");
        if (currentNodeState(record3) === "succeeded" || currentNodeState(record3) === "waiting_for_external_action" || currentNodeState(record3) === "waiting_for_auth" || currentNodeState(record3) === "pending")
          return;
      }
      this.markTerminalFailure(state, record3, normalized);
    }
  }
  async invokeHandler(state, record3) {
    const node = record3.definition;
    const handlerName = node.handler ?? node.transport;
    const handler = this.bindings.handlers[handlerName];
    if (!handler) {
      throw new WorkflowExecutionError(
        "HANDLER_NOT_REGISTERED",
        `No handler is registered for "${handlerName}". Register it in WorkflowBindings.handlers and resume the run.`
      );
    }
    const dependencyOutputs = this.dependencyOutputs(state, node);
    await this.runValidator(
      node.input.validator,
      {
        runId: state.runId,
        node,
        input: node.input.value,
        dependencyOutputs
      },
      "INPUT_VALIDATION_FAILED"
    );
    const workspacePath = await this.ensureWorkspace(state, record3);
    if (this.isEffectful(node)) {
      const timestamp2 = this.timestamp();
      const previousOperation = record3.operation;
      const providerCheckpoint = isProviderPlanCheckpoint(previousOperation?.checkpoint) ? previousOperation?.checkpoint : void 0;
      record3.operation = {
        attempt: record3.attempts,
        idempotencyKey: node.idempotencyKey,
        phase: previousOperation?.phase === "partially_applied" ? "partially_applied" : "prepared",
        preparedAt: timestamp2,
        updatedAt: timestamp2,
        reconcileAttempts: 0,
        ...providerCheckpoint === void 0 ? {} : { checkpoint: providerCheckpoint }
      };
      this.record(state, "node_operation_prepared", node.id, {
        attempt: record3.attempts,
        idempotencyKey: node.idempotencyKey
      });
    }
    const controller = new AbortController();
    const controllerKey = `${state.runId}:${node.id}`;
    this.activeControllers.set(controllerKey, controller);
    let timer;
    const context2 = {
      runId: state.runId,
      node,
      attempt: record3.attempts,
      input: node.input.value,
      dependencyOutputs,
      idempotencyKey: node.idempotencyKey,
      loopIteration: (record3.loopIterations ?? 0) + 1,
      workspacePath,
      signal: controller.signal,
      trace: (details) => this.record(state, "node_trace", node.id, details),
      checkpointOperation: (details) => {
        if (!record3.operation) {
          throw new WorkflowExecutionError(
            "OPERATION_NOT_PREPARED",
            `Node "${node.id}" cannot checkpoint an operation target because it is not effectful.`
          );
        }
        const checkpoint = sanitizeJson(details, this.bindings.secrets);
        if (isProviderPlanCheckpoint(record3.operation.checkpoint) && JSON.stringify(record3.operation.checkpoint) !== JSON.stringify(checkpoint)) {
          throw new WorkflowExecutionError(
            "OPERATION_CHECKPOINT_MISMATCH",
            `Node "${node.id}" cannot replace its immutable provider-plan checkpoint on retry.`
          );
        }
        record3.operation.checkpoint = checkpoint;
        record3.operation.updatedAt = this.timestamp();
        this.record(state, "node_operation_checkpointed", node.id, {
          attempt: record3.attempts,
          phase: record3.operation.phase,
          checkpoint: record3.operation.checkpoint
        });
      },
      checkpointExternalEffect: (details) => {
        if (!record3.operation) {
          throw new WorkflowExecutionError(
            "OPERATION_NOT_PREPARED",
            `Node "${node.id}" cannot checkpoint an external effect because it is not effectful.`
          );
        }
        record3.operation.phase = "external_write_acknowledged";
        record3.operation.updatedAt = this.timestamp();
        record3.operation.checkpoint = details === void 0 ? void 0 : sanitizeJson(details, this.bindings.secrets);
        this.record(state, "node_operation_checkpointed", node.id, {
          attempt: record3.attempts,
          phase: record3.operation.phase,
          checkpoint: record3.operation.checkpoint ?? null
        });
      },
      recordCost: (charge) => this.recordCost(state, record3, charge),
      claimAuthorizationCheckpoints: (requirements) => this.claimAuthorizationCheckpoints(state, record3, requirements),
      reserveAuthorizationSpend: (reservation) => this.reserveAuthorizationSpend(state, record3, reservation)
    };
    try {
      const timeout = new Promise((_, reject) => {
        timer = setTimeout(() => {
          reject(
            new WorkflowExecutionError(
              "TIMEOUT",
              `Node "${node.id}" exceeded its ${node.timeoutMs}ms timeout.`,
              { retryable: true }
            )
          );
          controller.abort("timeout");
        }, node.timeoutMs);
      });
      return await Promise.race([Promise.resolve(handler(context2)), timeout]);
    } finally {
      if (timer) clearTimeout(timer);
      this.activeControllers.delete(controllerKey);
    }
  }
  async validateResult(state, node, result2) {
    const context2 = {
      runId: state.runId,
      node,
      input: node.input.value,
      output: result2.output,
      dependencyOutputs: this.dependencyOutputs(state, node)
    };
    await this.runValidator(node.output.validator, context2, "OUTPUT_VALIDATION_FAILED");
    await this.runValidator(node.completion.validator, context2, "COMPLETION_CRITERION_FAILED");
  }
  async runValidator(name, context2, errorCode) {
    if (!name) return;
    const validator = this.bindings.validators[name];
    if (!validator) {
      throw new WorkflowExecutionError(
        "VALIDATOR_NOT_REGISTERED",
        `Validator "${name}" is not registered. Register it and resume the run.`
      );
    }
    const result2 = normalizedValidatorResult(await validator(context2));
    if (!result2.ok) {
      throw new WorkflowExecutionError(
        errorCode,
        result2.message ?? `Validator "${name}" rejected node "${context2.node.id}".`
      );
    }
  }
  async evaluateCondition(state, node) {
    if (node.condition.kind !== "handler") return true;
    const name = node.condition.handler;
    const handler = this.bindings.conditions[name];
    if (!handler) {
      throw new WorkflowExecutionError(
        "CONDITION_NOT_REGISTERED",
        `Condition "${name}" is not registered. Register it and resume the run.`
      );
    }
    return handler({
      runId: state.runId,
      node,
      input: node.input.value,
      dependencyOutputs: this.dependencyOutputs(state, node)
    });
  }
  dependencyOutputs(state, node) {
    return Object.fromEntries(
      node.dependencies.map((dependency) => [dependency, state.nodes[dependency].output])
    );
  }
  isEffectful(node) {
    return node.effect !== "none" && node.effect !== "read";
  }
  requiresOutcomeReconciliation(record3) {
    const phase = record3.operation?.phase;
    return this.isEffectful(record3.definition) && phase !== void 0 && !["not_applied", "verified", "cancelled"].includes(phase);
  }
  checkpointNoWrite(state, record3, reason) {
    if (!record3.operation) return;
    const preservesEarlierWrite = record3.operation.phase === "partially_applied";
    if (!preservesEarlierWrite) record3.operation.phase = "not_applied";
    record3.operation.updatedAt = this.timestamp();
    if (!isProviderPlanCheckpoint(record3.operation.checkpoint)) {
      record3.operation.checkpoint = { reason };
    }
    this.record(state, "node_operation_checkpointed", record3.definition.id, {
      attempt: record3.attempts,
      phase: record3.operation.phase,
      reason
    });
  }
  isAuthorizationError(error) {
    if ([
      "AUTH_REQUIRED",
      "AUTH_EXPIRED",
      "AUTHORIZATION_EXPIRED",
      "CREDENTIAL_EXPIRED",
      "CREDENTIAL_UNAVAILABLE",
      "PROVIDER_RECONCILIATION_AUTHORIZATION_REJECTED"
    ].includes(error.code.toUpperCase())) {
      return true;
    }
    if (error.code.toUpperCase() !== "AUTHORIZATION_REJECTED") return false;
    const details = error.details;
    return details !== null && details !== void 0 && !Array.isArray(details) && typeof details === "object" && ["envelope_expired", "credential_expired", "credential_not_found"].includes(
      String(details.reason)
    );
  }
  isExternalWaitError(error) {
    return [
      "EXTERNAL_ACTION_REQUIRED",
      "EXTERNAL_PENDING",
      "PROVIDER_PENDING",
      "POLL_PENDING"
    ].includes(error.code.toUpperCase());
  }
  errorConfirmsNoWrite(error) {
    if ([
      "provider_plan_factory_failed",
      "provider_plan_failed",
      "provider_plan_only",
      "provider_plan_empty"
    ].includes(error.code.toLowerCase())) {
      return true;
    }
    const details = error.details;
    if (!details || Array.isArray(details) || typeof details !== "object") return false;
    if (details.effectOutcome === "confirmed_no_write") return true;
    if (!Array.isArray(details.outcomes) || details.outcomes.length === 0) return false;
    return details.outcomes.every((outcome) => {
      if (outcome === null || Array.isArray(outcome) || typeof outcome !== "object") {
        return false;
      }
      if (outcome.effectOutcome === "confirmed_no_write") return true;
      if (outcome.status === "skipped") return true;
      return ["retryable_rate_limit", "terminal_auth", "terminal_validation"].includes(
        String(outcome.providerCode)
      );
    });
  }
  markWaiting(state, record3, wait) {
    if (!wait.reason.trim()) {
      throw new WorkflowExecutionError("WAIT_REASON_REQUIRED", "A durable wait requires a reason.");
    }
    if (wait.pollAfterMs !== void 0 && (!Number.isInteger(wait.pollAfterMs) || wait.pollAfterMs < 0)) {
      throw new WorkflowExecutionError(
        "WAIT_POLL_INVALID",
        "A durable wait pollAfterMs must be a non-negative integer."
      );
    }
    record3.state = wait.kind === "auth" ? "waiting_for_auth" : wait.kind === "external" ? "waiting_for_external_action" : "waiting_for_approval";
    record3.waiting = {
      kind: wait.kind,
      reason: sanitizeJson(wait.reason, this.bindings.secrets),
      requestedAt: this.timestamp(),
      pollAfterMs: wait.pollAfterMs,
      externalReference: wait.externalReference === void 0 ? void 0 : sanitizeJson(wait.externalReference, this.bindings.secrets)
    };
    record3.finishedAt = void 0;
    if (wait.kind === "external" && record3.operation) {
      record3.operation.phase = "pending_external";
      record3.operation.updatedAt = this.timestamp();
      record3.operation.externalReference = record3.waiting.externalReference;
    }
    this.record(state, "node_waiting", record3.definition.id, {
      state: record3.state,
      reason: record3.waiting.reason,
      pollAfterMs: record3.waiting.pollAfterMs ?? null,
      externalReference: record3.waiting.externalReference ?? null
    });
  }
  async ensureWorkspace(state, record3) {
    const mode = record3.definition.isolation;
    if (mode === "none") return void 0;
    if (record3.workspace?.attempt === record3.attempts && record3.workspace.mode === mode) {
      return record3.workspace.path;
    }
    const context2 = {
      runId: state.runId,
      node: record3.definition,
      attempt: record3.attempts,
      mode
    };
    const factory = this.bindings.workspaceFactory;
    const path = factory ? await factory(context2) : this.store.createWorkspace ? this.store.createWorkspace(context2) : void 0;
    if (!path?.trim()) {
      throw new WorkflowExecutionError(
        "WORKSPACE_FACTORY_NOT_REGISTERED",
        `Node "${record3.definition.id}" requires ${mode} isolation, but the store and bindings provide no workspace factory.`
      );
    }
    record3.workspace = {
      mode,
      path: sanitizeJson(path, this.bindings.secrets),
      attempt: record3.attempts,
      createdAt: this.timestamp()
    };
    this.record(state, "node_trace", record3.definition.id, {
      workspace: { mode, path: record3.workspace.path, attempt: record3.attempts }
    });
    return record3.workspace.path;
  }
  recordCost(state, record3, charge) {
    if (!charge.category.trim() || !charge.unit.trim() || !Number.isFinite(charge.amount) || charge.amount < 0 || charge.inputTokens !== void 0 && (!Number.isInteger(charge.inputTokens) || charge.inputTokens < 0) || charge.outputTokens !== void 0 && (!Number.isInteger(charge.outputTokens) || charge.outputTokens < 0)) {
      throw new WorkflowExecutionError(
        "COST_RECORD_INVALID",
        `Node "${record3.definition.id}" produced an invalid cost record.`
      );
    }
    if (charge.amount === 0 && !charge.inputTokens && !charge.outputTokens) return;
    state.costs ??= [];
    record3.costEntries ??= [];
    const entryId = `${record3.definition.id}:${record3.attempts}:${record3.loopIterations ?? 0}:${state.costs.length + 1}`;
    const cost = {
      kind: charge.kind,
      category: charge.category,
      amount: charge.amount,
      unit: charge.unit,
      entryId,
      nodeId: record3.definition.id,
      attempt: record3.attempts,
      loopIteration: (record3.loopIterations ?? 0) + 1,
      recordedAt: this.timestamp()
    };
    if (charge.inputTokens !== void 0) cost.inputTokens = charge.inputTokens;
    if (charge.outputTokens !== void 0) cost.outputTokens = charge.outputTokens;
    if (charge.tool !== void 0) cost.tool = charge.tool;
    if (charge.model !== void 0) cost.model = charge.model;
    if (charge.metadata !== void 0) {
      cost.metadata = sanitizeJson(charge.metadata, this.bindings.secrets);
    }
    state.costs.push(cost);
    record3.costEntries.push(entryId);
    record3.cost += charge.amount;
    state.budget.consumed[charge.category] = (state.budget.consumed[charge.category] ?? 0) + charge.amount;
    this.record(state, "node_cost_recorded", record3.definition.id, cost);
    const limit = state.budget.limits[charge.category];
    if (limit !== void 0 && state.budget.consumed[charge.category] > limit) {
      throw new WorkflowExecutionError(
        "BUDGET_EXCEEDED_AFTER_EXECUTION",
        `Node "${record3.definition.id}" recorded ${charge.amount} ${charge.unit}; category "${charge.category}" is now ${state.budget.consumed[charge.category]}/${limit}.`
      );
    }
  }
  async reconcileNode(state, record3, reason) {
    const node = record3.definition;
    const verified = state.verifiedEffects[node.idempotencyKey];
    if (verified) {
      record3.state = "succeeded";
      record3.output = verified.output;
      record3.evidenceArtifact = verified.evidenceArtifact;
      record3.effectVerified = true;
      record3.error = void 0;
      record3.waiting = void 0;
      record3.finishedAt = verified.verifiedAt;
      if (record3.operation) {
        record3.operation.phase = "verified";
        record3.operation.updatedAt = verified.verifiedAt;
      }
      this.record(state, "node_reconciled", node.id, { status: "verified", source: "ledger" });
      return;
    }
    if (!record3.operation) {
      if (reason !== "cancel") {
        record3.state = "pending";
        record3.startedAt = void 0;
        record3.iterationAttempts = Math.max(0, (record3.iterationAttempts ?? 0) - 1);
      }
      return;
    }
    const policy = node.reconciliation;
    const handlerName = policy?.handler ?? node.handler ?? node.transport;
    const reconciler = this.bindings.reconcilers[handlerName];
    if (!reconciler) {
      record3.operation.phase = "unknown";
      record3.operation.updatedAt = this.timestamp();
      record3.error = {
        code: "UNKNOWN_OUTCOME_RECONCILIATION_REQUIRED",
        message: `Node "${node.id}" may have produced an effect; register reconciler "${handlerName}" before retrying.`,
        retryable: false
      };
      if (reason !== "cancel") {
        this.markWaiting(state, record3, {
          kind: "external",
          reason: record3.error.message,
          externalReference: record3.operation.externalReference
        });
      } else {
        this.record(state, "node_reconciled", node.id, {
          status: "unknown",
          reason: "reconciler_not_registered"
        });
      }
      return;
    }
    const maximum = policy?.maxPollAttempts ?? 1;
    if (record3.operation.reconcileAttempts >= maximum) {
      this.markTerminalFailure(state, record3, {
        code: "RECONCILIATION_LIMIT_EXCEEDED",
        message: `Node "${node.id}" exceeded ${maximum} reconciliation attempts without a verified outcome.`,
        retryable: false
      });
      return;
    }
    record3.operation.reconcileAttempts += 1;
    record3.operation.lastReconciledAt = this.timestamp();
    record3.operation.updatedAt = record3.operation.lastReconciledAt;
    this.record(state, "node_reconciliation_started", node.id, {
      reason,
      attempt: record3.operation.reconcileAttempts,
      maximum
    });
    const controller = new AbortController();
    const controllerKey = `${state.runId}:${node.id}:reconcile`;
    this.activeControllers.set(controllerKey, controller);
    let timer;
    let result2;
    try {
      const timeout = new Promise((_, reject) => {
        timer = setTimeout(() => {
          controller.abort("reconciliation timeout");
          reject(
            new WorkflowExecutionError(
              "RECONCILIATION_TIMEOUT",
              `Reconciliation for node "${node.id}" exceeded ${node.timeoutMs}ms.`,
              { retryable: true }
            )
          );
        }, node.timeoutMs);
      });
      result2 = await Promise.race([
        Promise.resolve(
          reconciler({
            runId: state.runId,
            node,
            attempt: record3.attempts,
            input: node.input.value,
            dependencyOutputs: this.dependencyOutputs(state, node),
            idempotencyKey: node.idempotencyKey,
            operation: structuredClone(record3.operation),
            reason,
            signal: controller.signal,
            trace: (details) => this.record(state, "node_trace", node.id, details)
          })
        ),
        timeout
      ]);
    } catch (error) {
      const normalized = this.normalizeError(error, "RECONCILIATION_FAILED");
      if (record3.operation.reconcileAttempts >= maximum) {
        record3.operation.phase = "unknown";
        record3.operation.updatedAt = this.timestamp();
        this.markTerminalFailure(state, record3, { ...normalized, retryable: false });
      } else if (reason !== "cancel") {
        record3.error = normalized;
        this.markWaiting(state, record3, {
          kind: "external",
          reason: normalized.message,
          pollAfterMs: policy?.pollIntervalMs,
          externalReference: record3.operation.externalReference
        });
      }
      return;
    } finally {
      if (timer) clearTimeout(timer);
      this.activeControllers.delete(controllerKey);
    }
    if (!this.isReconciliationResult(result2)) {
      record3.operation.phase = "unknown";
      record3.operation.updatedAt = this.timestamp();
      this.markTerminalFailure(state, record3, {
        code: "RECONCILIATION_RESULT_INVALID",
        message: `Reconciler "${handlerName}" returned an invalid result for node "${node.id}".`,
        retryable: false
      });
      return;
    }
    record3.operation.lastReconciledAt = this.timestamp();
    record3.operation.updatedAt = record3.operation.lastReconciledAt;
    if (result2.status === "verified") {
      if (node.evidence.required && !result2.evidenceArtifact) {
        this.markTerminalFailure(state, record3, {
          code: "RECONCILIATION_EVIDENCE_MISSING",
          message: `Reconciler for node "${node.id}" verified an effect without required evidence.`,
          retryable: false
        });
        return;
      }
      record3.output = result2.output === void 0 ? void 0 : sanitizeJson(result2.output, this.bindings.secrets);
      record3.effectVerified = true;
      record3.evidenceArtifact = result2.evidenceArtifact;
      record3.error = void 0;
      record3.waiting = void 0;
      record3.state = "succeeded";
      record3.finishedAt = this.timestamp();
      record3.operation.phase = "verified";
      record3.operation.externalReference = result2.externalReference === void 0 ? void 0 : sanitizeJson(result2.externalReference, this.bindings.secrets);
      state.verifiedEffects[node.idempotencyKey] = {
        nodeId: node.id,
        output: record3.output,
        evidenceArtifact: record3.evidenceArtifact,
        verifiedAt: record3.finishedAt
      };
      this.record(state, "node_reconciled", node.id, {
        status: "verified",
        evidenceArtifact: record3.evidenceArtifact ?? null
      });
      try {
        await this.validateResult(state, node, {
          output: result2.output,
          effectVerified: true,
          evidenceArtifact: result2.evidenceArtifact
        });
      } catch (error) {
        this.markTerminalFailure(
          state,
          record3,
          this.normalizeError(error, "RECONCILIATION_RESULT_INVALID")
        );
        return;
      }
      try {
        for (const charge of result2.costs ?? []) this.recordCost(state, record3, charge);
        if (result2.cost !== void 0) {
          this.recordCost(state, record3, {
            kind: node.kind === "model" ? "model" : "provider",
            category: node.budgetCategory,
            amount: result2.cost,
            unit: node.cost.unit
          });
        }
      } catch (error) {
        this.markTerminalFailure(
          state,
          record3,
          this.normalizeError(error, "RECONCILIATION_COST_INVALID")
        );
      }
      return;
    }
    if (result2.status === "not_applied") {
      record3.operation.phase = "not_applied";
      record3.error = void 0;
      record3.waiting = void 0;
      if (reason === "cancel") {
        record3.state = "cancelled";
      } else {
        record3.state = "pending";
        record3.startedAt = void 0;
        record3.finishedAt = void 0;
        if (reason === "restart") {
          record3.iterationAttempts = Math.max(0, (record3.iterationAttempts ?? 0) - 1);
        }
      }
      this.record(state, "node_reconciled", node.id, { status: "not_applied" });
      return;
    }
    if (result2.status === "partially_applied") {
      record3.operation.phase = "partially_applied";
      record3.operation.externalReference = result2.externalReference === void 0 ? void 0 : sanitizeJson(result2.externalReference, this.bindings.secrets);
      record3.error = void 0;
      record3.waiting = void 0;
      record3.startedAt = void 0;
      record3.finishedAt = void 0;
      if (reason === "cancel") {
        record3.state = "cancelled";
        record3.error = {
          code: "CANCELLED_WITH_PARTIAL_EFFECT",
          message: sanitizeJson(
            result2.message ?? "The run was cancelled after only part of the provider plan applied.",
            this.bindings.secrets
          ),
          retryable: false
        };
      } else {
        record3.state = "pending";
      }
      this.record(state, "node_reconciled", node.id, {
        status: "partially_applied",
        cancelled: reason === "cancel"
      });
      return;
    }
    if (result2.status === "failed") {
      const effectState = result2.effectState ?? "unknown";
      record3.operation.phase = effectState === "confirmed_no_write" ? "not_applied" : effectState === "partial_write" ? "partially_applied" : effectState === "confirmed_write" ? "external_write_acknowledged" : "unknown";
      const error = {
        code: result2.code,
        message: sanitizeJson(result2.message, this.bindings.secrets),
        retryable: result2.retryable ?? false
      };
      if (reason === "cancel") {
        record3.state = "cancelled";
        record3.error = error;
        record3.waiting = void 0;
        this.record(state, "node_reconciled", node.id, {
          status: "failed",
          effectState,
          cancelled: true
        });
        return;
      }
      if (this.isAuthorizationError(error)) {
        record3.operation.reconcileAttempts = Math.max(0, record3.operation.reconcileAttempts - 1);
        this.markWaiting(state, record3, { kind: "auth", reason: error.message });
        record3.error = error;
        this.record(state, "node_reconciled", node.id, {
          status: "failed_authorization",
          effectState
        });
        return;
      }
      if (effectState === "confirmed_no_write" && error.retryable && (record3.iterationAttempts ?? 0) < node.retry.maxAttempts) {
        record3.state = "pending";
        record3.error = error;
        record3.waiting = void 0;
        this.record(state, "node_reconciled", node.id, { status: "failed_retryable", error });
      } else {
        this.markTerminalFailure(state, record3, error);
      }
      return;
    }
    record3.operation.phase = result2.status === "pending" ? "pending_external" : "unknown";
    record3.operation.externalReference = result2.externalReference;
    const exhausted = record3.operation.reconcileAttempts >= maximum;
    if (exhausted) {
      this.markTerminalFailure(state, record3, {
        code: result2.status === "pending" ? "RECONCILIATION_POLL_EXHAUSTED" : "UNKNOWN_OUTCOME_UNRESOLVED",
        message: result2.status === "pending" ? `Node "${node.id}" remained pending after ${maximum} reconciliation attempts.` : sanitizeJson(
          result2.message ?? `Node "${node.id}" has an unresolved external outcome.`,
          this.bindings.secrets
        ),
        retryable: false
      });
      return;
    }
    if (reason === "cancel") {
      record3.state = "cancelled";
      record3.error = {
        code: "CANCELLED_WITH_UNRESOLVED_OUTCOME",
        message: result2.status === "unknown" ? sanitizeJson(
          result2.message ?? "External outcome remains unknown after cancellation.",
          this.bindings.secrets
        ) : "External operation is still pending after cancellation.",
        retryable: false
      };
      this.record(state, "node_reconciled", node.id, {
        status: result2.status,
        cancelled: true
      });
      return;
    }
    this.markWaiting(state, record3, {
      kind: "external",
      reason: result2.status === "unknown" ? sanitizeJson(
        result2.message ?? `Node "${node.id}" has an unknown external outcome.`,
        this.bindings.secrets
      ) : `Node "${node.id}" is still pending provider read-back.`,
      pollAfterMs: result2.status === "pending" ? result2.pollAfterMs ?? policy?.pollIntervalMs : policy?.pollIntervalMs,
      externalReference: result2.externalReference
    });
    this.record(state, "node_reconciled", node.id, { status: result2.status });
  }
  affectedDescendants(state, roots) {
    const affected = new Set(roots);
    let changed = true;
    while (changed) {
      changed = false;
      for (const record3 of Object.values(state.nodes)) {
        if (affected.has(record3.definition.id)) continue;
        if (record3.definition.dependencies.some((dependency) => affected.has(dependency))) {
          affected.add(record3.definition.id);
          changed = true;
        }
      }
    }
    return affected;
  }
  isReconciliationResult(value) {
    if (!value || Array.isArray(value) || typeof value !== "object") return false;
    const candidate = value;
    const hasOnly = (...allowed) => Object.keys(candidate).every((key) => allowed.includes(key));
    if (candidate.status === "verified") {
      return hasOnly("status", "output", "evidenceArtifact", "cost", "costs", "externalReference") && (candidate.externalReference === void 0 || typeof candidate.externalReference === "string") && (candidate.evidenceArtifact === void 0 || typeof candidate.evidenceArtifact === "string") && (candidate.cost === void 0 || typeof candidate.cost === "number" && Number.isFinite(candidate.cost)) && (candidate.costs === void 0 || Array.isArray(candidate.costs));
    }
    if (candidate.status === "not_applied") return hasOnly("status");
    if (candidate.status === "partially_applied") {
      return hasOnly("status", "message", "externalReference") && (candidate.message === void 0 || typeof candidate.message === "string") && (candidate.externalReference === void 0 || typeof candidate.externalReference === "string");
    }
    if (candidate.status === "pending") {
      return hasOnly("status", "pollAfterMs", "externalReference") && (candidate.pollAfterMs === void 0 || Number.isInteger(candidate.pollAfterMs) && Number(candidate.pollAfterMs) >= 0) && (candidate.externalReference === void 0 || typeof candidate.externalReference === "string");
    }
    if (candidate.status === "unknown") {
      return hasOnly("status", "message", "externalReference") && (candidate.message === void 0 || typeof candidate.message === "string") && (candidate.externalReference === void 0 || typeof candidate.externalReference === "string");
    }
    if (candidate.status === "failed") {
      return hasOnly("status", "code", "message", "retryable", "effectState") && typeof candidate.code === "string" && candidate.code.length > 0 && typeof candidate.message === "string" && (candidate.retryable === void 0 || typeof candidate.retryable === "boolean") && (candidate.effectState === void 0 || typeof candidate.effectState === "string" && ["confirmed_no_write", "partial_write", "confirmed_write", "unknown"].includes(
        candidate.effectState
      ));
    }
    return false;
  }
  normalizeState(state) {
    state.verifiedEffects ??= {};
    state.cache ??= {};
    state.costs ??= [];
    state.steeringRevision ??= 0;
    state.checkpointGrants ??= {};
    state.authorizationSpend ??= { currency: null, totalAmount: 0, reservations: {} };
    for (const record3 of Object.values(state.nodes)) {
      record3.revision ??= 0;
      record3.loopIterations ??= 0;
      record3.iterationAttempts ??= 0;
      record3.costEntries ??= [];
    }
  }
  checkpointRequirements(details) {
    if (!details || Array.isArray(details) || typeof details !== "object") return null;
    const raw = details.requirements;
    if (!Array.isArray(raw) || raw.length === 0) return null;
    const parsed = [];
    const scopes = /* @__PURE__ */ new Set();
    for (const candidate of raw) {
      if (!candidate || Array.isArray(candidate) || typeof candidate !== "object") return null;
      const effect = sideEffectClassSchema.safeParse(candidate.effect);
      if (!effect.success || typeof candidate.operationId !== "string" || candidate.operationId.length === 0 || candidate.operationId.length > 500 || typeof candidate.provider !== "string" || candidate.provider.length === 0 || candidate.provider.length > 100 || typeof candidate.action !== "string" || candidate.action.length === 0 || candidate.action.length > 500) {
        return null;
      }
      const key = `${effect.data}\0${candidate.operationId}`;
      if (scopes.has(key)) return null;
      scopes.add(key);
      parsed.push({
        effect: effect.data,
        operationId: candidate.operationId,
        provider: candidate.provider,
        action: candidate.action
      });
    }
    return parsed;
  }
  checkpointRequestedAt(details) {
    if (!details || Array.isArray(details) || typeof details !== "object") return null;
    const requestedAt = details.requestedAt;
    return typeof requestedAt === "string" && Number.isFinite(Date.parse(requestedAt)) ? requestedAt : null;
  }
  availableCheckpointGrant(state, scope, now) {
    for (const candidate of Object.values(state.checkpointGrants ?? {})) {
      const parsed = oneShotCheckpointGrantSchema.safeParse(candidate);
      if (!parsed.success) continue;
      const grant = parsed.data;
      if (grant.runId === scope.runId && grant.nodeId === scope.nodeId && grant.effect === scope.effect && grant.operationId === scope.operationId && grant.consumedAt === null && Date.parse(grant.expiresAt) > Date.parse(now)) {
        return grant;
      }
    }
    return void 0;
  }
  claimAuthorizationCheckpoints(state, record3, requirements) {
    const now = this.timestamp();
    const details = {
      requestedAt: now,
      requirements: requirements.map(({ effect, operationId: operationId2, provider, action }) => ({
        effect,
        operationId: operationId2,
        provider,
        action
      }))
    };
    const parsed = this.checkpointRequirements(details);
    if (!parsed) {
      throw new WorkflowExecutionError(
        "authorization_checkpoint_requirements_invalid",
        `Node "${record3.definition.id}" produced invalid authorization checkpoint requirements.`
      );
    }
    const scopes = parsed.map(({ effect, operationId: operationId2 }) => ({
      runId: state.runId,
      nodeId: record3.definition.id,
      effect,
      operationId: operationId2
    }));
    const grants = scopes.map((scope) => this.availableCheckpointGrant(state, scope, now));
    if (grants.some((grant) => grant === void 0)) {
      throw new WorkflowExecutionError(
        "authorization_checkpoint_required",
        `Node "${record3.definition.id}" requires one-shot human authorization before provider execution.`,
        { details }
      );
    }
    const consumed = grants.map(
      (grant, index) => consumeOneShotCheckpointGrant(grant, {
        scope: scopes[index],
        attempt: record3.attempts,
        now
      })
    );
    state.checkpointGrants ??= {};
    for (const grant of consumed) state.checkpointGrants[grant.grantId] = grant;
    this.record(state, "checkpoint_grant_consumed", record3.definition.id, {
      attempt: record3.attempts,
      grants: consumed.map(({ grantId, effect, operationId: operationId2, consumedAt }) => ({
        grantId,
        effect,
        operationId: operationId2,
        consumedAt
      }))
    });
    return consumed;
  }
  reserveAuthorizationSpend(state, record3, reservation) {
    if (!reservation.reservationId.trim() || reservation.reservationId.length > 500 || !/^[A-Z]{3}$/.test(reservation.currency) || !Number.isFinite(reservation.maxAmount) || reservation.maxAmount < 0 || reservation.operations.length === 0) {
      this.checkpointNoWrite(state, record3, "authorization spend reservation was invalid");
      throw new WorkflowExecutionError(
        "authorization_spend_reservation_invalid",
        `Node "${record3.definition.id}" produced an invalid authorization spend reservation.`
      );
    }
    const operationIds = /* @__PURE__ */ new Set();
    let amount = 0;
    for (const operation2 of reservation.operations) {
      if (!operation2.operationId || operation2.operationId.length > 500 || operationIds.has(operation2.operationId) || !Number.isFinite(operation2.amount) || operation2.amount < 0) {
        this.checkpointNoWrite(state, record3, "estimated-cost operation was invalid");
        throw new WorkflowExecutionError(
          "authorization_spend_reservation_invalid",
          `Node "${record3.definition.id}" produced an invalid estimated-cost operation.`
        );
      }
      operationIds.add(operation2.operationId);
      amount += operation2.amount;
    }
    if (!Number.isFinite(amount)) {
      this.checkpointNoWrite(state, record3, "cumulative estimate was non-finite");
      throw new WorkflowExecutionError(
        "authorization_spend_reservation_invalid",
        `Node "${record3.definition.id}" produced a non-finite cumulative estimate.`
      );
    }
    const spend = state.authorizationSpend ??= {
      currency: null,
      totalAmount: 0,
      reservations: {}
    };
    if (!Number.isFinite(spend.totalAmount) || spend.totalAmount < 0) {
      this.checkpointNoWrite(state, record3, "persisted authorization spend was invalid");
      throw new WorkflowExecutionError(
        "authorization_spend_state_invalid",
        `Run "${state.runId}" has invalid persisted authorization spend state.`
      );
    }
    if (spend.currency !== null && spend.currency !== reservation.currency) {
      this.checkpointNoWrite(state, record3, "spend currency did not match");
      throw new WorkflowExecutionError(
        "spend_currency_mismatch",
        `Run "${state.runId}" already reserved ${spend.currency}; ${reservation.currency} cannot be accumulated without conversion.`
      );
    }
    const existing = spend.reservations[reservation.reservationId];
    if (existing) {
      if (existing.nodeId === record3.definition.id && existing.amount === amount && existing.operationIds.length === operationIds.size && existing.operationIds.every((operationId2) => operationIds.has(operationId2))) {
        if (spend.totalAmount > reservation.maxAmount) {
          this.checkpointNoWrite(
            state,
            record3,
            "current authorization spend ceiling rejected the existing reservation"
          );
          throw new WorkflowExecutionError(
            "spend_limit_exceeded",
            `Run "${state.runId}" has already reserved ${spend.totalAmount} ${reservation.currency}, exceeding the current cumulative ceiling of ${reservation.maxAmount} ${reservation.currency}.`,
            {
              details: {
                currency: reservation.currency,
                previouslyReserved: spend.totalAmount,
                requested: 0,
                ceiling: reservation.maxAmount
              }
            }
          );
        }
        return;
      }
      this.checkpointNoWrite(state, record3, "reservation id was reused with different details");
      throw new WorkflowExecutionError(
        "authorization_spend_reservation_invalid",
        `Reservation "${reservation.reservationId}" was reused with different spend details.`
      );
    }
    const cumulative = spend.totalAmount + amount;
    if (!Number.isFinite(cumulative) || cumulative > reservation.maxAmount) {
      this.checkpointNoWrite(state, record3, "cumulative spend ceiling rejected the operation");
      throw new WorkflowExecutionError(
        "spend_limit_exceeded",
        `Run "${state.runId}" would reserve ${cumulative} ${reservation.currency}, exceeding its cumulative ceiling of ${reservation.maxAmount} ${reservation.currency}.`,
        {
          details: {
            currency: reservation.currency,
            previouslyReserved: spend.totalAmount,
            requested: amount,
            ceiling: reservation.maxAmount
          }
        }
      );
    }
    const reservedAt = this.timestamp();
    spend.currency = reservation.currency;
    spend.totalAmount = cumulative;
    spend.reservations[reservation.reservationId] = {
      nodeId: record3.definition.id,
      attempt: record3.attempts,
      amount,
      operationIds: [...operationIds].sort(),
      reservedAt
    };
    this.record(state, "authorization_spend_reserved", record3.definition.id, {
      reservationId: reservation.reservationId,
      attempt: record3.attempts,
      amount,
      currency: reservation.currency,
      cumulative,
      ceiling: reservation.maxAmount,
      operationIds: [...operationIds].sort()
    });
  }
  budgetError(state, node) {
    const limit = state.budget.limits[node.budgetCategory];
    if (limit === void 0) return null;
    const consumed = state.budget.consumed[node.budgetCategory] ?? 0;
    if (consumed + node.cost.amount <= limit) return null;
    return {
      code: "BUDGET_EXHAUSTED",
      message: `Node "${node.id}" needs ${node.cost.amount} ${node.cost.unit}, but ${consumed}/${limit} is already consumed in "${node.budgetCategory}".`,
      retryable: false
    };
  }
  backoffDelay(node, failedAttempt) {
    const policy = node.retry.backoff;
    if (policy.strategy === "none") return 0;
    if (policy.strategy === "fixed") return policy.initialMs;
    return Math.min(
      policy.maxMs,
      policy.initialMs * Math.pow(policy.multiplier, Math.max(0, failedAttempt - 1))
    );
  }
  markTerminalFailure(state, record3, error) {
    record3.state = "failed_terminal";
    record3.error = error;
    record3.finishedAt = this.timestamp();
    this.record(state, "node_terminal_failure", record3.definition.id, { error });
  }
  failUnfinishedNodes(state, code, message) {
    for (const record3 of Object.values(state.nodes)) {
      if (isTerminalNode(record3)) continue;
      this.markTerminalFailure(state, record3, { code, message, retryable: false });
    }
  }
  skipUnfinishedNodesAfterFailure(state) {
    for (const record3 of Object.values(state.nodes)) {
      if (isTerminalNode(record3)) continue;
      record3.state = "skipped";
      record3.skipReason = "run stopped after a terminal node failure";
      record3.finishedAt = this.timestamp();
      this.record(state, "node_skipped", record3.definition.id, { reason: record3.skipReason });
    }
  }
  normalizeError(error, fallbackCode = "NODE_FAILED") {
    if (error instanceof WorkflowExecutionError) {
      return {
        code: error.code,
        message: sanitizeJson(error.message, this.bindings.secrets),
        retryable: error.retryable,
        details: error.details === void 0 ? void 0 : sanitizeJson(error.details, this.bindings.secrets)
      };
    }
    const message = error instanceof Error ? error.message : String(error);
    return {
      code: fallbackCode,
      message: sanitizeJson(message, this.bindings.secrets),
      retryable: false
    };
  }
  async compensate(definition2, state, reason) {
    const wanted = reason === "cancel" ? "on_cancel" : reason === "failure" ? "on_failure" : "explicit";
    for (const nodeId of topologicalOrder(definition2).reverse()) {
      const record3 = state.nodes[nodeId];
      const compensation = record3.definition.compensation;
      const compensableState = record3.state === "succeeded" || record3.state === "failed_terminal" && record3.effectVerified;
      if (!compensableState || !compensation || compensation.when !== wanted) continue;
      const handler = this.bindings.compensators[compensation.handler];
      if (!handler) {
        this.record(state, "node_trace", nodeId, {
          compensation: "not run",
          nextAction: `register compensator "${compensation.handler}" and run explicit compensation`
        });
        continue;
      }
      const controller = new AbortController();
      const context2 = {
        runId: state.runId,
        node: record3.definition,
        output: record3.output,
        reason,
        signal: controller.signal
      };
      try {
        await handler(context2);
        record3.state = "compensated";
        record3.finishedAt = this.timestamp();
        this.record(state, "node_compensated", nodeId, { reason });
      } catch (error) {
        this.record(state, "node_trace", nodeId, {
          compensation: "failed",
          error: this.normalizeError(error, "COMPENSATION_FAILED")
        });
      }
    }
  }
  async resolveInterrupt(runId2, nodeId, expectedState, resolution) {
    const state = this.store.load(runId2);
    const record3 = state.nodes[nodeId];
    if (!record3) throw new Error(`Workflow run "${runId2}" has no node "${nodeId}".`);
    if (record3.state !== expectedState) {
      throw new Error(
        `Node "${nodeId}" is ${record3.state}; expected ${expectedState} before resolving it.`
      );
    }
    if (record3.definition.evidence.required && !resolution.evidenceArtifact) {
      throw new Error(`Node "${nodeId}" requires an evidence artifact before it can resume.`);
    }
    if (!resolution.approvedBy?.trim()) {
      throw new Error(`Node "${nodeId}" requires an explicit approvedBy identity.`);
    }
    const dependencyOutputs = this.dependencyOutputs(state, record3.definition);
    const validationContext = {
      runId: runId2,
      node: record3.definition,
      input: record3.definition.input.value,
      output: resolution.output,
      dependencyOutputs
    };
    await this.runValidator(
      record3.definition.output.validator,
      validationContext,
      "OUTPUT_VALIDATION_FAILED"
    );
    await this.runValidator(
      record3.definition.completion.validator,
      validationContext,
      "COMPLETION_CRITERION_FAILED"
    );
    const effectful = record3.definition.effect !== "none" && record3.definition.effect !== "read";
    if (effectful) {
      if (!resolution.evidenceArtifact) {
        throw new Error(
          `Node "${nodeId}" has an external effect and requires verified evidence before it can resume.`
        );
      }
      const verifyEvidence = this.bindings.interruptEvidenceVerifier;
      if (!verifyEvidence) {
        throw new Error(
          `Node "${nodeId}" has an external effect but no interrupt evidence verifier is registered.`
        );
      }
      const verified = normalizedValidatorResult(
        await verifyEvidence({
          runId: runId2,
          node: record3.definition,
          output: resolution.output,
          evidenceArtifact: resolution.evidenceArtifact,
          approvedBy: resolution.approvedBy,
          note: resolution.note
        })
      );
      if (!verified.ok) {
        throw new Error(verified.message ?? `Evidence for node "${nodeId}" was not verified.`);
      }
    }
    record3.output = resolution.output === void 0 ? void 0 : sanitizeJson(resolution.output, this.bindings.secrets);
    record3.evidenceArtifact = resolution.evidenceArtifact;
    record3.effectVerified = effectful;
    record3.state = "succeeded";
    record3.finishedAt = this.timestamp();
    if (record3.effectVerified) {
      state.verifiedEffects[record3.definition.idempotencyKey] = {
        nodeId,
        output: record3.output,
        evidenceArtifact: record3.evidenceArtifact,
        verifiedAt: record3.finishedAt
      };
    }
    state.status = "created";
    state.finishedAt = void 0;
    this.record(state, "interrupt_resolved", nodeId, {
      approvedBy: resolution.approvedBy ?? null,
      note: resolution.note ?? null,
      evidenceArtifact: resolution.evidenceArtifact ?? null
    });
    return state;
  }
  assertGraphMatch(definition2, state) {
    const fingerprint = workflowFingerprint(definition2);
    if (state.graph.fingerprint !== fingerprint) {
      throw new Error(
        `Workflow definition changed for run "${state.runId}". Resume with graph ${state.graph.id}@${state.graph.version} (${state.graph.fingerprint.slice(0, 12)}).`
      );
    }
  }
  record(state, type, nodeId, details) {
    state.eventSequence += 1;
    state.updatedAt = this.timestamp();
    const event = {
      sequence: state.eventSequence,
      timestamp: state.updatedAt,
      runId: state.runId,
      type,
      nodeId,
      details: details === void 0 ? void 0 : sanitizeJson(details, this.bindings.secrets)
    };
    if (this.store.checkpoint) this.store.checkpoint(state, event);
    else {
      this.store.save(state);
      this.store.appendEvent(event);
    }
  }
  timestamp() {
    return this.now().toISOString();
  }
  generateRunId(graphId) {
    const timestamp2 = this.timestamp().replace(/[-:.TZ]/g, "").slice(0, 14);
    return `${graphId}-${timestamp2}-${randomBytes(4).toString("hex")}`;
  }
};

// lib/workflow/store.ts
import {
  appendFileSync,
  closeSync,
  existsSync,
  fsyncSync,
  mkdirSync,
  openSync,
  readFileSync,
  readdirSync,
  renameSync,
  unlinkSync,
  writeFileSync
} from "node:fs";
import { join, resolve } from "node:path";
var RUN_ID = /^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$/;
var FileWorkflowStore = class {
  rootDir;
  secrets;
  lastSequences = /* @__PURE__ */ new Map();
  constructor(options = {}) {
    this.rootDir = resolve(options.rootDir ?? ".venture/runs");
    this.secrets = options.secrets ?? [];
  }
  create(state) {
    this.assertRunId(state.runId);
    mkdirSync(this.rootDir, { recursive: true, mode: 448 });
    try {
      mkdirSync(this.runDir(state.runId), { mode: 448 });
    } catch (error) {
      if (error.code === "EEXIST") {
        throw new DuplicateWorkflowRunError(state.runId);
      }
      throw error;
    }
    this.writeState(state);
    writeFileSync(this.eventsPath(state.runId), "", { encoding: "utf8", mode: 384 });
    this.lastSequences.set(state.runId, 0);
  }
  load(runId2) {
    this.assertRunId(runId2);
    const path = this.statePath(runId2);
    if (!existsSync(path)) throw new WorkflowRunNotFoundError(runId2);
    const state = JSON.parse(readFileSync(path, "utf8"));
    if (state.pendingEvent) {
      const pending = state.pendingEvent;
      const existing = this.readEvents(runId2).find(({ sequence }) => sequence === pending.sequence);
      if (!existing) this.appendEvent(pending);
      else if (JSON.stringify(existing) !== JSON.stringify(sanitizeJson(pending, this.secrets))) {
        throw new Error(
          `Workflow event ${pending.sequence} for run "${runId2}" conflicts with its durable checkpoint.`
        );
      }
      delete state.pendingEvent;
    }
    return state;
  }
  save(state) {
    this.assertRunId(state.runId);
    if (!existsSync(this.runDir(state.runId))) throw new WorkflowRunNotFoundError(state.runId);
    this.writeState(state);
  }
  appendEvent(event) {
    this.assertRunId(event.runId);
    const path = this.eventsPath(event.runId);
    if (!existsSync(path)) throw new WorkflowRunNotFoundError(event.runId);
    const expectedSequence = this.lastSequence(event.runId) + 1;
    if (event.sequence !== expectedSequence) {
      throw new Error(
        `Workflow event sequence for run "${event.runId}" must be ${expectedSequence}, received ${event.sequence}.`
      );
    }
    const sanitized = sanitizeJson(event, this.secrets);
    const descriptor2 = openSync(path, "a", 384);
    try {
      appendFileSync(descriptor2, `${JSON.stringify(sanitized)}
`, { encoding: "utf8" });
      fsyncSync(descriptor2);
    } finally {
      closeSync(descriptor2);
    }
    this.lastSequences.set(event.runId, event.sequence);
  }
  checkpoint(state, event) {
    state.pendingEvent = event;
    this.writeState(state);
    const lastSequence = this.lastSequence(state.runId);
    if (lastSequence < event.sequence) this.appendEvent(event);
    else if (lastSequence === event.sequence) {
      const existing = this.readEvents(state.runId).at(-1);
      if (JSON.stringify(existing) !== JSON.stringify(sanitizeJson(event, this.secrets))) {
        throw new Error(
          `Workflow event ${event.sequence} for run "${state.runId}" conflicts with its durable checkpoint.`
        );
      }
    } else {
      throw new Error(
        `Workflow event ${event.sequence} for run "${state.runId}" is behind persisted sequence ${lastSequence}.`
      );
    }
    delete state.pendingEvent;
  }
  readEvents(runId2) {
    this.assertRunId(runId2);
    const path = this.eventsPath(runId2);
    if (!existsSync(path)) throw new WorkflowRunNotFoundError(runId2);
    const events = readFileSync(path, "utf8").split("\n").filter(Boolean).map((line) => JSON.parse(line));
    for (const [index, event] of events.entries()) {
      if (event.runId !== runId2 || event.sequence !== index + 1) {
        throw new Error(`Workflow event log for run "${runId2}" is corrupt at line ${index + 1}.`);
      }
    }
    this.lastSequences.set(runId2, events.at(-1)?.sequence ?? 0);
    return events;
  }
  exists(runId2) {
    this.assertRunId(runId2);
    return existsSync(this.statePath(runId2));
  }
  listRuns() {
    if (!existsSync(this.rootDir)) return [];
    return readdirSync(this.rootDir, { withFileTypes: true }).filter((entry) => entry.isDirectory() && RUN_ID.test(entry.name)).map((entry) => entry.name).filter((runId2) => existsSync(this.statePath(runId2))).sort();
  }
  createWorkspace(context2) {
    this.assertRunId(context2.runId);
    const directory = join(
      this.runDir(context2.runId),
      "workspaces",
      context2.node.id,
      `attempt-${context2.attempt}`
    );
    mkdirSync(directory, { recursive: true, mode: 448 });
    return directory;
  }
  async *streamEvents(runId2, options = {}) {
    this.assertRunId(runId2);
    let cursor = options.afterSequence ?? 0;
    const follow = options.follow ?? true;
    const pollIntervalMs = options.pollIntervalMs ?? 25;
    if (!Number.isInteger(cursor) || cursor < 0) {
      throw new Error("afterSequence must be a non-negative integer.");
    }
    if (!Number.isInteger(pollIntervalMs) || pollIntervalMs < 1) {
      throw new Error("pollIntervalMs must be a positive integer.");
    }
    while (!options.signal?.aborted) {
      const unseen = this.readEvents(runId2).filter((event) => event.sequence > cursor);
      for (const event of unseen) {
        cursor = event.sequence;
        yield event;
      }
      if (!follow) return;
      if (options.stopWhenRunFinishes) {
        const status = this.load(runId2).status;
        if (["succeeded", "failed", "cancelled", "superseded"].includes(status)) return;
      }
      await new Promise((resolvePromise) => {
        const finish = () => {
          clearTimeout(timer);
          options.signal?.removeEventListener("abort", finish);
          resolvePromise();
        };
        const timer = setTimeout(finish, pollIntervalMs);
        options.signal?.addEventListener("abort", finish, { once: true });
      });
    }
  }
  writeState(state) {
    const path = this.statePath(state.runId);
    const temporary = join(
      this.runDir(state.runId),
      `.state.${process.pid}.${Date.now()}.${Math.random().toString(16).slice(2)}.tmp`
    );
    const sanitized = sanitizeJson(state, this.secrets);
    const descriptor2 = openSync(temporary, "wx", 384);
    try {
      writeFileSync(descriptor2, `${JSON.stringify(sanitized, null, 2)}
`, {
        encoding: "utf8"
      });
      fsyncSync(descriptor2);
    } catch (error) {
      closeSync(descriptor2);
      if (existsSync(temporary)) unlinkSync(temporary);
      throw error;
    }
    closeSync(descriptor2);
    renameSync(temporary, path);
    const directoryDescriptor = openSync(this.runDir(state.runId), "r");
    try {
      fsyncSync(directoryDescriptor);
    } finally {
      closeSync(directoryDescriptor);
    }
  }
  lastSequence(runId2) {
    const known = this.lastSequences.get(runId2);
    if (known !== void 0) return known;
    return this.readEvents(runId2).at(-1)?.sequence ?? 0;
  }
  assertRunId(runId2) {
    if (!RUN_ID.test(runId2)) {
      throw new Error(
        `Invalid workflow run id "${runId2}"; use 1-128 letters, numbers, dots, underscores, or hyphens.`
      );
    }
  }
  runDir(runId2) {
    return join(this.rootDir, runId2);
  }
  statePath(runId2) {
    return join(this.runDir(runId2), "state.json");
  }
  eventsPath(runId2) {
    return join(this.runDir(runId2), "events.jsonl");
  }
};

// lib/cli/default-services.ts
import { createHash as createHash17 } from "node:crypto";
import { existsSync as existsSync12, mkdirSync as mkdirSync8, readFileSync as readFileSync14, renameSync as renameSync6, writeFileSync as writeFileSync8 } from "node:fs";
import { dirname as dirname11, isAbsolute as isAbsolute6, relative as relative13, resolve as resolve21, sep as sep13 } from "node:path";
import { isDeepStrictEqual as isDeepStrictEqual2 } from "node:util";
import { parse as parse7, stringify as stringify6 } from "yaml";

// lib/analytics/packs.ts
var EVENT_PACK_IDS = [
  "core_product",
  "web_acquisition",
  "lead_generation",
  "onboarding",
  "authentication",
  "subscription",
  "one_time_payment",
  "content",
  "experiment",
  "mobile",
  "feedback",
  "reliability"
];
var EVENT_PACKS = {
  core_product: {
    id: "core_product",
    description: "Smallest useful journey, proposition, consent, and authoritative outcome.",
    stages: ["build", "launch", "operate"],
    capabilitiesAny: [],
    requiredDestinations: ["neon"],
    freshnessSources: ["neon_commercial_evidence"],
    events: [
      "core_journey_started",
      "core_journey_completed",
      "hero_primary_cta_click",
      "hero_secondary_cta_click",
      "proposition_detail_open",
      "how_it_works_view",
      "use_case_view",
      "consent_banner_view",
      "analytics_accepted",
      "analytics_declined",
      "consent_settings_opened",
      "consent_changed",
      "consent_withdrawn"
    ],
    privacy: { piiAllowed: false, rawFormValuesAllowed: false, exactPriceRequired: false }
  },
  web_acquisition: {
    id: "web_acquisition",
    description: "Public-site visits, routes, attribution, navigation, and return behaviour.",
    stages: ["launch", "operate"],
    capabilitiesAny: ["public_website"],
    requiredDestinations: ["vercel", "ga4"],
    freshnessSources: ["ga4"],
    events: [
      "site_visit",
      "page_view",
      "landing_page_view",
      "route_change",
      "outbound_link_click",
      "navigation_click",
      "mobile_navigation_open",
      "return_visit",
      "repeat_pricing_view",
      "repeat_high_intent_visit"
    ],
    privacy: { piiAllowed: false, rawFormValuesAllowed: false, exactPriceRequired: false }
  },
  lead_generation: {
    id: "lead_generation",
    description: "Lead or concierge intent from first form view through server confirmation.",
    stages: ["launch", "operate"],
    capabilitiesAny: ["public_website", "feedback_intake"],
    requiredDestinations: ["ga4", "neon"],
    freshnessSources: ["ga4", "neon_commercial_evidence"],
    events: [
      "pilot_selected",
      "enterprise_contact_selected",
      "reservation_intent",
      "form_view",
      "form_started",
      "form_step_completed",
      "form_validation_error",
      "form_abandoned",
      "form_submitted",
      "form_submission_confirmed",
      "qualification_completed"
    ],
    privacy: { piiAllowed: false, rawFormValuesAllowed: false, exactPriceRequired: true }
  },
  onboarding: {
    id: "onboarding",
    description: "Product onboarding and first activation without entered field values.",
    stages: ["launch", "operate"],
    capabilitiesAny: ["authenticated_product"],
    requiredDestinations: ["neon"],
    freshnessSources: ["neon_commercial_evidence"],
    events: [
      "onboarding_started",
      "onboarding_step_completed",
      "onboarding_completed",
      "activation_completed"
    ],
    privacy: { piiAllowed: false, rawFormValuesAllowed: false, exactPriceRequired: false }
  },
  authentication: {
    id: "authentication",
    description: "Authentication attempts, confirmed outcomes, and safe failure categories.",
    stages: ["launch", "operate"],
    capabilitiesAny: ["authenticated_product"],
    requiredDestinations: ["neon"],
    freshnessSources: ["neon_commercial_evidence"],
    events: ["authentication_started", "authentication_succeeded", "authentication_failed"],
    privacy: { piiAllowed: false, rawFormValuesAllowed: false, exactPriceRequired: false }
  },
  subscription: {
    id: "subscription",
    description: "Exact-price subscription checkout and server-confirmed lifecycle changes.",
    stages: ["launch", "operate"],
    capabilitiesAny: ["stripe", "revenuecat"],
    requiredDestinations: ["neon"],
    freshnessSources: ["neon_commercial_evidence"],
    events: [
      "pricing_page_view",
      "pricing_section_view",
      "pricing_variant_exposed",
      "billing_period_changed",
      "pricing_details_open",
      "setup_fee_explanation_open",
      "guarantee_explanation_open",
      "plan_selected",
      "monthly_plan_selected",
      "annual_plan_selected",
      "checkout_intent",
      "subscription_checkout_started",
      "subscription_started",
      "subscription_renewed",
      "subscription_payment_failed",
      "subscription_canceled"
    ],
    privacy: { piiAllowed: false, rawFormValuesAllowed: false, exactPriceRequired: true }
  },
  one_time_payment: {
    id: "one_time_payment",
    description: "Exact-price one-time checkout and server-confirmed payment outcome.",
    stages: ["launch", "operate"],
    capabilitiesAny: ["stripe"],
    requiredDestinations: ["neon"],
    freshnessSources: ["neon_commercial_evidence", "stripe"],
    events: [
      "checkout_intent",
      "one_time_checkout_started",
      "one_time_payment_completed",
      "one_time_payment_failed"
    ],
    privacy: { piiAllowed: false, rawFormValuesAllowed: false, exactPriceRequired: true }
  },
  content: {
    id: "content",
    description: "Intentional content, proof, samples, resources, and classified site search.",
    stages: ["launch", "operate"],
    capabilitiesAny: ["web_seo_aeo_geo"],
    requiredDestinations: ["ga4"],
    freshnessSources: ["ga4", "gsc"],
    events: [
      "section_view",
      "proof_view",
      "sample_open",
      "sample_download",
      "demo_interaction",
      "comparison_view",
      "faq_open",
      "video_start",
      "video_complete",
      "resource_open",
      "site_search_started",
      "site_search_submitted",
      "site_search_no_results",
      "site_search_result_selected"
    ],
    privacy: { piiAllowed: false, rawFormValuesAllowed: false, exactPriceRequired: false }
  },
  experiment: {
    id: "experiment",
    description: "Optional eligibility, assignment, exposure, conversion, and guardrails.",
    stages: ["operate"],
    capabilitiesAny: [],
    requiredDestinations: ["neon"],
    freshnessSources: ["neon_commercial_evidence"],
    events: [
      "hero_variant_exposed",
      "icp_variant_exposed",
      "experiment_eligible",
      "experiment_assigned",
      "experiment_exposed",
      "experiment_primary_conversion",
      "experiment_guardrail_event"
    ],
    privacy: { piiAllowed: false, rawFormValuesAllowed: false, exactPriceRequired: true }
  },
  mobile: {
    id: "mobile",
    description: "Mobile app usage, release-aware core outcome, and store-subscription intent.",
    stages: ["launch", "operate"],
    capabilitiesAny: ["app_store_connect", "eas", "ios_aso"],
    requiredDestinations: ["ga4", "neon"],
    freshnessSources: ["app_store_connect_analytics", "release_log"],
    events: [
      "mobile_app_opened",
      "mobile_core_journey_completed",
      "mobile_store_subscription_started"
    ],
    privacy: { piiAllowed: false, rawFormValuesAllowed: false, exactPriceRequired: true }
  },
  feedback: {
    id: "feedback",
    description: "Feedback entry and server-confirmed de-identified classification only.",
    stages: ["launch", "operate"],
    capabilitiesAny: ["feedback_intake"],
    requiredDestinations: ["neon"],
    freshnessSources: ["feedback"],
    events: ["feedback_intake_opened", "feedback_submitted"],
    privacy: { piiAllowed: false, rawFormValuesAllowed: false, exactPriceRequired: false }
  },
  reliability: {
    id: "reliability",
    description: "Runtime and critical-journey failures classified without private payloads.",
    stages: ["build", "launch", "operate"],
    capabilitiesAny: [],
    requiredDestinations: ["neon"],
    freshnessSources: ["release_log"],
    events: ["page_error", "critical_journey_failed"],
    privacy: { piiAllowed: false, rawFormValuesAllowed: false, exactPriceRequired: false }
  }
};
function resolveActiveEventPacks(context2) {
  const capabilities = new Set(context2.capabilities);
  const active = /* @__PURE__ */ new Set(["core_product", "reliability"]);
  if (capabilities.has("public_website")) active.add("web_acquisition");
  if (capabilities.has("web_seo_aeo_geo")) active.add("content");
  if (capabilities.has("authenticated_product")) {
    active.add("authentication");
    active.add("onboarding");
  }
  if (context2.leadJourney || ["lead_generation", "services"].includes(context2.monetizationModel)) {
    active.add("lead_generation");
  }
  if (["subscription", "hybrid"].includes(context2.monetizationModel) && (capabilities.has("stripe") || capabilities.has("revenuecat"))) {
    active.add("subscription");
  }
  if (["one_time", "hybrid"].includes(context2.monetizationModel) && capabilities.has("stripe")) {
    active.add("one_time_payment");
  }
  if (context2.appKind !== "web") active.add("mobile");
  if (capabilities.has("feedback_intake")) active.add("feedback");
  if (context2.experimentRunning) active.add("experiment");
  for (const pack2 of context2.explicitPacks ?? []) active.add(pack2);
  return EVENT_PACK_IDS.filter((pack2) => active.has(pack2));
}
var CORE_JOURNEYS = {
  core_product: {
    requiredPacks: ["core_product"],
    startEvents: ["core_journey_started"],
    outcomeEvents: ["core_journey_completed"]
  },
  lead_generation: {
    requiredPacks: ["lead_generation"],
    startEvents: ["form_started"],
    outcomeEvents: ["form_submission_confirmed", "qualification_completed"]
  },
  authenticated_product: {
    requiredPacks: ["authentication", "onboarding"],
    startEvents: ["authentication_started", "onboarding_started"],
    outcomeEvents: ["authentication_succeeded", "activation_completed"]
  },
  subscription: {
    requiredPacks: ["subscription"],
    startEvents: ["subscription_checkout_started"],
    outcomeEvents: ["subscription_started"]
  },
  one_time_payment: {
    requiredPacks: ["one_time_payment"],
    startEvents: ["one_time_checkout_started"],
    outcomeEvents: ["one_time_payment_completed"]
  },
  mobile: {
    requiredPacks: ["mobile"],
    startEvents: ["mobile_app_opened"],
    outcomeEvents: ["mobile_core_journey_completed"]
  },
  feedback: {
    requiredPacks: ["feedback"],
    startEvents: ["feedback_intake_opened"],
    outcomeEvents: ["feedback_submitted"]
  }
};
function resolveActiveCoreJourneys(activePacks) {
  const active = new Set(activePacks);
  return Object.keys(CORE_JOURNEYS).filter(
    (journeyId) => CORE_JOURNEYS[journeyId].requiredPacks.every((pack2) => active.has(pack2))
  );
}

// lib/data/normalize.ts
import { createHash as createHash2 } from "node:crypto";

// lib/data/types.ts
var DATA_SOURCE_IDS = [
  "gsc",
  "ga4",
  "bing_webmaster",
  "bing_ai_performance",
  "neon_commercial_evidence",
  "stripe",
  "revenuecat",
  "brevo",
  "app_store_connect_analytics",
  "release_log",
  "feedback",
  "interviews",
  "support"
];
var DataNormalizationError = class extends Error {
  constructor(message, source) {
    super(message);
    this.source = source;
    this.name = "DataNormalizationError";
  }
  source;
};
var DataConnectorNotConfiguredError = class extends Error {
  constructor(message, source, nextAction2) {
    super(message);
    this.source = source;
    this.nextAction = nextAction2;
    this.name = "DataConnectorNotConfiguredError";
  }
  source;
  nextAction;
};
var DataProviderFetchError = class extends Error {
  constructor(message, source, retryable, nextAction2) {
    super(message);
    this.source = source;
    this.retryable = retryable;
    this.nextAction = nextAction2;
    this.name = "DataProviderFetchError";
  }
  source;
  retryable;
  nextAction;
};

// lib/data/normalize.ts
var FORBIDDEN_KEYS = /^(email|name|first_name|last_name|phone|address|message|search_query|query_text|form_value|password|token|idea|payload|body|free_text|user_content|(?:user|customer|contact|recipient|sender)_(?:email|name|phone|address)|(?:message|email|form|request|response|user)_(?:body|payload|content|text))$/i;
var EMAIL_VALUE = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
function rejectPrivateValue(value, key, source) {
  if (typeof value === "string") {
    if (EMAIL_VALUE.test(value) || looksLikeCredentialValue(value)) {
      throw new DataNormalizationError(
        `Source ${source} returned prohibited private or credential-shaped data in "${key}".`,
        source
      );
    }
    return;
  }
  if (Array.isArray(value)) {
    value.forEach((entry, index) => rejectPrivateValue(entry, `${key}[${index}]`, source));
    return;
  }
  if (!value || typeof value !== "object") return;
  for (const [nestedKey, nestedValue] of Object.entries(value)) {
    if (FORBIDDEN_KEYS.test(nestedKey)) {
      throw new DataNormalizationError(
        `Source ${source} returned prohibited private field "${key}.${nestedKey}"; classify/de-identify it before sync.`,
        source
      );
    }
    rejectPrivateValue(nestedValue, `${key}.${nestedKey}`, source);
  }
}
function scalar(value, key, source) {
  rejectPrivateValue(value, key, source);
  if (value === null || typeof value === "string" || typeof value === "number" || typeof value === "boolean") {
    if (typeof value === "number" && !Number.isFinite(value)) {
      throw new Error(`${key} contains a non-finite number`);
    }
    return value;
  }
  if (value === void 0) return null;
  return JSON.stringify(value);
}
function normalizeRow(row, source) {
  const normalized = {};
  for (const [key, value] of Object.entries(row).sort(([a], [b]) => a.localeCompare(b))) {
    if (FORBIDDEN_KEYS.test(key)) {
      throw new DataNormalizationError(
        `Source ${source} returned prohibited private field "${key}"; classify/de-identify it before sync.`,
        source
      );
    }
    normalized[key] = scalar(value, key, source);
  }
  return normalized;
}
function assertTimestamp(value, label, source) {
  if (!Number.isFinite(Date.parse(value))) {
    throw new DataNormalizationError(`${label} is not a valid timestamp: ${value}`, source);
  }
}
function normalizeDataset(raw) {
  if (!raw.sourceAccount.trim()) {
    throw new DataNormalizationError("source account/property/site is required", raw.source);
  }
  if (EMAIL_VALUE.test(raw.sourceAccount) || looksLikeCredentialValue(raw.sourceAccount)) {
    throw new DataNormalizationError(
      "source account must be a non-personal identifier and contain no credential material",
      raw.source
    );
  }
  assertTimestamp(raw.fetchedAt, "fetchedAt", raw.source);
  assertTimestamp(raw.reportingWindow.start, "reportingWindow.start", raw.source);
  assertTimestamp(raw.reportingWindow.end, "reportingWindow.end", raw.source);
  if (Date.parse(raw.reportingWindow.end) < Date.parse(raw.reportingWindow.start)) {
    throw new DataNormalizationError("reporting window end precedes start", raw.source);
  }
  if (!raw.timezone.trim()) throw new DataNormalizationError("timezone is required", raw.source);
  for (const dimension of raw.dimensions) {
    if (FORBIDDEN_KEYS.test(dimension)) {
      throw new DataNormalizationError(
        `Source ${raw.source} declared prohibited private dimension "${dimension}".`,
        raw.source
      );
    }
  }
  for (const limitation of raw.limitations ?? []) {
    if (EMAIL_VALUE.test(limitation) || looksLikeCredentialValue(limitation)) {
      throw new DataNormalizationError(
        `Source ${raw.source} limitation contains prohibited private or credential-shaped data.`,
        raw.source
      );
    }
  }
  const rows = raw.rows.map((row) => normalizeRow(row, raw.source));
  const fingerprint = createHash2("sha256").update(
    JSON.stringify({
      source: raw.source,
      account: raw.sourceAccount,
      window: raw.reportingWindow,
      dimensions: [...raw.dimensions].sort(),
      rows
    })
  ).digest("hex").slice(0, 16);
  return {
    id: `${raw.source}-${fingerprint}`,
    provenance: {
      source: raw.source,
      sourceAccount: raw.sourceAccount,
      fetchedAt: raw.fetchedAt,
      reportingWindow: raw.reportingWindow,
      timezone: raw.timezone,
      dimensions: [...new Set(raw.dimensions)].sort(),
      quality: raw.quality ?? "complete",
      limitations: [...new Set(raw.limitations ?? [])].sort(),
      releaseVersion: raw.releaseVersion ?? null
    },
    rows
  };
}

// lib/data/sync.ts
function ageHours(now, fetchedAt) {
  return Math.max(0, (now.getTime() - Date.parse(fetchedAt)) / 36e5);
}
function buildFreshnessReport(requirements, datasets, now) {
  return requirements.map((requirement) => {
    const candidates = datasets.filter((dataset) => dataset.provenance.source === requirement.source).sort((a, b) => Date.parse(b.provenance.fetchedAt) - Date.parse(a.provenance.fetchedAt));
    const latest = candidates[0];
    if (!latest) {
      return {
        source: requirement.source,
        status: "missing",
        fetchedAt: null,
        ageHours: null,
        freshnessHours: requirement.freshnessHours,
        required: requirement.required,
        limitation: "No normalized dataset was fetched; missing is not zero."
      };
    }
    const age = ageHours(now, latest.provenance.fetchedAt);
    const stale = age > requirement.freshnessHours;
    return {
      source: requirement.source,
      status: stale ? "stale" : "fresh",
      fetchedAt: latest.provenance.fetchedAt,
      ageHours: Number(age.toFixed(2)),
      freshnessHours: requirement.freshnessHours,
      required: requirement.required,
      limitation: stale ? `Dataset age ${age.toFixed(1)}h exceeds ${requirement.freshnessHours}h.` : latest.provenance.limitations[0] ?? null
    };
  });
}
async function syncDataSources(connectors, requirements, options = {}) {
  const now = options.now ?? /* @__PURE__ */ new Date();
  const outcomes = await Promise.all(
    connectors.map(async (connector) => {
      const credentialRef2 = options.credentialRefs?.[connector.source];
      if (connector.credentialRequired && !credentialRef2) {
        return {
          failure: {
            source: connector.source,
            code: "credential_missing",
            message: `No credential_ref is configured for ${connector.source}.`,
            retryable: false,
            nextAction: `Run vh auth login for the provider backing ${connector.source}, then vh data sync.`
          }
        };
      }
      try {
        const raw = await connector.fetch({ now, credentialRef: credentialRef2, signal: options.signal });
        return { dataset: normalizeDataset(raw) };
      } catch (error) {
        const message = error instanceof Error ? error.message : String(error);
        if (error instanceof DataConnectorNotConfiguredError) {
          return {
            failure: {
              source: connector.source,
              code: "connector_not_configured",
              message,
              retryable: false,
              nextAction: error.nextAction
            }
          };
        }
        if (error instanceof DataProviderFetchError) {
          return {
            failure: {
              source: connector.source,
              code: "provider_failed",
              message,
              retryable: error.retryable,
              nextAction: error.nextAction
            }
          };
        }
        const invalid = error instanceof DataNormalizationError || /prohibited|invalid data|timestamp|window|source account|timezone|expected/.test(message);
        return {
          failure: {
            source: connector.source,
            code: invalid ? "invalid_data" : "provider_failed",
            message,
            retryable: !invalid,
            nextAction: invalid ? `Fix the ${connector.source} normalization contract and rerun vh data sync.` : `Check provider doctor/status for ${connector.source} and retry within policy.`
          }
        };
      }
    })
  );
  const datasets = outcomes.flatMap((outcome) => outcome.dataset ? [outcome.dataset] : []);
  const failures = outcomes.flatMap((outcome) => outcome.failure ? [outcome.failure] : []);
  return { datasets, failures, freshness: buildFreshnessReport(requirements, datasets, now) };
}

// lib/authorization/engine.ts
var AuthorizationError = class extends Error {
  constructor(message, code) {
    super(message);
    this.code = code;
    this.name = "AuthorizationError";
  }
  code;
};
function canonicalProfile(value) {
  return value.replaceAll("-", "_");
}
function issueAuthorizationEnvelope(input) {
  const profileName = canonicalProfile(input.profile);
  const parsedProfile = authorizationProfileIdSchema.safeParse(profileName);
  if (!parsedProfile.success) {
    throw new AuthorizationError(
      `Unknown authorization profile "${input.profile}".`,
      "profile_unknown"
    );
  }
  const profileId = parsedProfile.data;
  const profile2 = input.policies.authorization.profiles[profileId];
  const unsupportedEnvironments = input.environments.filter(
    (environment) => !profile2.allowed_environments.includes(environment)
  );
  if (unsupportedEnvironments.length > 0) {
    throw new AuthorizationError(
      `${profileId} does not allow environment(s): ${unsupportedEnvironments.join(", ")}.`,
      "environment_forbidden"
    );
  }
  const now = input.now ?? /* @__PURE__ */ new Date();
  const expires = new Date(now.getTime() + (input.ttlMs ?? 60 * 60 * 1e3));
  const requestedCapabilities = input.capabilities ? [...new Set(input.capabilities)].sort() : void 0;
  if (requestedCapabilities?.includes("*")) {
    throw new AuthorizationError(
      "A graph-scoped authorization request must name exact capabilities, not wildcard '*'.",
      "capability_forbidden"
    );
  }
  if (requestedCapabilities && !profile2.allowed_capabilities.includes("*")) {
    const unsupported2 = requestedCapabilities.filter(
      (capability) => !profile2.allowed_capabilities.includes(capability)
    );
    if (unsupported2.length > 0) {
      throw new AuthorizationError(
        `${profileId} does not allow graph capability/capabilities: ${unsupported2.join(", ")}.`,
        "capability_forbidden"
      );
    }
  }
  return authorizationEnvelopeSchema.parse({
    run_id: input.runId,
    profile: profileId,
    allowed_capabilities: requestedCapabilities ?? profile2.allowed_capabilities,
    allowed_side_effect_classes: profile2.allowed_side_effect_classes,
    providers: input.providers,
    environments: input.environments,
    issued_at: now.toISOString(),
    expires_at: expires.toISOString(),
    max_estimated_spend: profile2.max_estimated_spend,
    unknown_external_costs_allowed: profile2.unknown_external_costs_allowed,
    max_email_recipients: profile2.max_email_recipients,
    production_deploy_allowed: profile2.production_deploy_allowed,
    live_products_and_prices_allowed: profile2.live_products_and_prices_allowed,
    actual_charges_allowed: profile2.actual_charges_allowed,
    transactional_test_email_allowed: profile2.transactional_test_email_allowed,
    dns_additions_allowed: profile2.dns_additions_allowed,
    nameserver_changes_allowed: profile2.nameserver_changes_allowed,
    app_store_submission_allowed: profile2.app_store_submission_allowed,
    explicitly_forbidden_actions: input.policies.authorization.always_require_distinct_checkpoint_for,
    approval_ref: input.approvalRef,
    extensions: {}
  });
}
function classified(effect, reason, ...allowedProviderEffects) {
  return { effect, reason, allowedProviderEffects };
}
function operationEffect(operation2) {
  const action = operation2.action.toLowerCase();
  const actionParts = new Set(action.split(/[._:-]+/));
  const hasAnyPart = (...parts) => parts.some((part) => actionParts.has(part));
  const writesDataDestructively = hasAnyPart("truncate", "drop", "purge") || action.includes("destructive_data") || action.includes("data.destroy");
  if (action.includes("nameserver")) {
    return classified(
      "nameserver_change",
      "the action changes authoritative nameservers",
      "irreversible_external",
      "manual"
    );
  }
  if (writesDataDestructively) {
    return classified(
      "destructive_data_change",
      "the action destructively changes stored data or schema",
      "irreversible_external",
      "manual"
    );
  }
  if (hasAnyPart("delete", "remove", "destroy")) {
    return classified(
      "external_delete",
      "the action deletes an external resource",
      "irreversible_external",
      "manual"
    );
  }
  if (action.includes("app_store.release") || action.includes("app_store.publish") || operation2.provider === "app_store_connect" && hasAnyPart("release", "publish", "publication")) {
    return classified(
      "app_store_publication",
      "the action publishes an App Store release",
      "irreversible_external",
      "manual"
    );
  }
  if (action === "ios.submit" || action.includes("testflight.upload") || action.includes("build.submit")) {
    return classified(
      "testflight_upload",
      "the action submits a build for TestFlight processing",
      "reversible_external",
      "irreversible_external",
      "manual"
    );
  }
  if (hasAnyPart("charge", "capture") || action.includes("payment_intent")) {
    return classified(
      operation2.environment === "production" ? "customer_charge" : "reversible_external_write",
      "the action creates or captures a customer payment",
      "financial"
    );
  }
  const isFinancialConfiguration = operation2.provider === "stripe" && (hasAnyPart("product", "price") || action.includes("billing_portal.configuration")) || operation2.provider === "revenuecat" && hasAnyPart("app", "entitlement", "offering");
  if (isFinancialConfiguration) {
    return classified(
      operation2.environment === "production" ? "live_commerce_config" : "reversible_external_write",
      "the action configures products, prices, billing, or entitlements",
      "financial"
    );
  }
  if (action.includes("deployment.production")) {
    return classified(
      "production_deploy",
      "the action creates a production deployment",
      "reversible_external",
      "irreversible_external"
    );
  }
  if (action.includes("deployment.preview")) {
    return classified(
      "preview_deploy",
      "the action creates a preview deployment",
      "reversible_external",
      "irreversible_external"
    );
  }
  if ((operation2.provider === "dns" || operation2.provider === "mijndomein") && (action.includes("dns") || action.includes("record"))) {
    return classified(
      "dns_addition",
      "the action adds or changes an individual DNS record",
      "reversible_external",
      "manual"
    );
  }
  if (operation2.provider === "mijndomein" && action.includes("domain.attach")) {
    return classified(
      "dns_addition",
      "the manual domain attachment changes individual DNS records",
      "manual"
    );
  }
  if (operation2.provider === "vercel" && action === "web_analytics.enable_manual") {
    return classified(
      "reversible_external_write",
      "the manual dashboard action enables project analytics without publishing or sending data",
      "manual"
    );
  }
  const sendsCommunication = hasAnyPart("send", "email", "message", "deliver");
  if (sendsCommunication) {
    const bulk = hasAnyPart("bulk", "campaign", "broadcast", "cold");
    return classified(
      bulk ? "bulk_communication" : "transactional_email",
      bulk ? "the action sends a bulk communication" : "the action sends transactional email",
      "communication"
    );
  }
  const configuresCommunication = action.includes("webhook") || action.includes("sending_domain") || action.startsWith("sender.") || action.startsWith("template.") || action.includes("beta_review_details");
  if (configuresCommunication) {
    return classified(
      "reversible_external_write",
      "the action configures communication metadata without sending",
      "communication"
    );
  }
  if (operation2.effectClass === "local_write" && (action === "project.link" || hasAnyPart("local", "file", "generate"))) {
    return classified("local_write", "the action writes only local state", "local_write");
  }
  if (operation2.provider === "github" && action === "repository.create_from_source") {
    return classified(
      "reversible_external_write",
      "the action creates or reconciles a GitHub repository branch to an exact locally verified source tree",
      "reversible_external"
    );
  }
  const writesExternalState = hasAnyPart(
    "add",
    "attach",
    "bootstrap",
    "build",
    "change",
    "confirm",
    "create",
    "migrate",
    "set",
    "submit",
    "update",
    "write"
  );
  if (writesExternalState) {
    if (operation2.transport === "manual") {
      return classified(
        "reversible_external_write",
        "the manual action creates or changes external state",
        "manual"
      );
    }
    if (operation2.effectClass === "irreversible_external") {
      throw new AuthorizationError(
        `Cannot classify irreversible action "${operation2.action}". Add a precise destructive, publication, TestFlight, or reversible action mapping before authorizing it.`,
        "operation_effect_unknown"
      );
    }
    return classified(
      "reversible_external_write",
      "the action creates or changes external state",
      "reversible_external"
    );
  }
  const readsExternalState = hasAnyPart(
    "check",
    "get",
    "inspect",
    "list",
    "read",
    "status",
    "view"
  );
  if (readsExternalState) {
    return classified("external_read", "the action only reads external state", "read");
  }
  switch (operation2.effectClass) {
    case "read":
      return classified("external_read", "the provider declares a read", "read");
    case "local_write":
      throw new AuthorizationError(
        `Cannot classify local write action "${operation2.action}". Local provider writes need an explicit local action mapping.`,
        "operation_effect_unknown"
      );
    case "reversible_external":
      return classified(
        "reversible_external_write",
        "the provider declares a reversible external write",
        "reversible_external"
      );
    case "irreversible_external":
      throw new AuthorizationError(
        `Cannot classify irreversible action "${operation2.action}". Add a precise destructive, publication, TestFlight, or reversible action mapping before authorizing it.`,
        "operation_effect_unknown"
      );
    case "financial":
      throw new AuthorizationError(
        `Cannot classify financial action "${operation2.action}". Add an explicit commerce or charge action mapping before authorizing it.`,
        "operation_effect_unknown"
      );
    case "communication":
      throw new AuthorizationError(
        `Cannot classify communication action "${operation2.action}". Distinguish configuration from an actual send before authorizing it.`,
        "operation_effect_unknown"
      );
    case "manual":
      throw new AuthorizationError(
        `Cannot classify manual action "${operation2.action}". Add a precise action mapping before authorizing it.`,
        "operation_effect_unknown"
      );
  }
}
function assertEffectConsistency(operation2, classification) {
  if (classification.allowedProviderEffects.includes(operation2.effectClass)) return;
  throw new AuthorizationError(
    `Action "${operation2.action}" is classified as ${classification.effect} because ${classification.reason}, but it declares provider effect ${operation2.effectClass}. Expected ${classification.allowedProviderEffects.join(" or ")}.`,
    "operation_effect_mismatch"
  );
}
var PERMISSION_FLAG_BY_EFFECT = {
  production_deploy: "production_deploy_allowed",
  live_commerce_config: "live_products_and_prices_allowed",
  customer_charge: "actual_charges_allowed",
  transactional_email: "transactional_test_email_allowed",
  dns_addition: "dns_additions_allowed",
  nameserver_change: "nameserver_changes_allowed",
  app_store_publication: "app_store_submission_allowed"
};
function assertPermissionFlag(envelope, operation2, effect) {
  const flag4 = PERMISSION_FLAG_BY_EFFECT[effect];
  if (!flag4 || envelope[flag4] === true) return;
  throw new AuthorizationError(
    `${flag4}=false blocks ${effect} action "${operation2.action}" in ${envelope.profile}.`,
    "permission_flag_required"
  );
}
function assertSpendWithinEnvelope(envelope, operation2, effect) {
  const estimate = operation2.estimatedCost;
  if (!estimate) {
    if (effect === "customer_charge") {
      throw new AuthorizationError(
        `Customer charge action "${operation2.action}" needs an estimatedCost before authorization.`,
        "spend_estimate_invalid"
      );
    }
    const externalWrite = !["none", "local_write", "git_write", "external_read"].includes(effect);
    if (externalWrite && !envelope.unknown_external_costs_allowed) {
      throw new AuthorizationError(
        `External write action "${operation2.action}" needs an estimatedCost or an explicit unknown_external_costs_allowed policy exception.`,
        "spend_estimate_required"
      );
    }
    return;
  }
  if (!Number.isFinite(estimate.amount) || estimate.amount < 0 || !/^[A-Z]{3}$/.test(estimate.currency)) {
    throw new AuthorizationError(
      `Action "${operation2.action}" has an invalid estimatedCost; use a non-negative finite amount and uppercase ISO-4217 currency.`,
      "spend_estimate_invalid"
    );
  }
  if (estimate.currency !== envelope.max_estimated_spend.currency) {
    throw new AuthorizationError(
      `Action "${operation2.action}" estimates ${estimate.currency}, but the run ceiling is denominated in ${envelope.max_estimated_spend.currency}; no currency conversion is authorized.`,
      "spend_currency_mismatch"
    );
  }
  if (estimate.amount > envelope.max_estimated_spend.amount) {
    throw new AuthorizationError(
      `Action "${operation2.action}" estimates ${estimate.amount} ${estimate.currency}, exceeding the run ceiling of ${envelope.max_estimated_spend.amount} ${envelope.max_estimated_spend.currency}.`,
      "spend_limit_exceeded"
    );
  }
}
function assertRecipientsWithinEnvelope(envelope, operation2, effect) {
  const recipients = operation2.emailRecipientCount;
  const sendsEmail = effect === "transactional_email" || effect === "bulk_communication";
  if (recipients === void 0) {
    if (sendsEmail) {
      throw new AuthorizationError(
        `Email action "${operation2.action}" needs an exact emailRecipientCount before authorization.`,
        "recipient_count_required"
      );
    }
    return;
  }
  if (!Number.isInteger(recipients) || recipients < 0) {
    throw new AuthorizationError(
      `Action "${operation2.action}" has invalid emailRecipientCount ${String(recipients)}; use a non-negative integer.`,
      "recipient_count_invalid"
    );
  }
  if (!sendsEmail) {
    throw new AuthorizationError(
      `Action "${operation2.action}" declares email recipients but is classified as ${effect}, not an email send.`,
      "operation_effect_mismatch"
    );
  }
  if (recipients === 0) {
    throw new AuthorizationError(
      `Email action "${operation2.action}" must name at least one recipient.`,
      "recipient_count_invalid"
    );
  }
  if (recipients > envelope.max_email_recipients) {
    throw new AuthorizationError(
      `Email action "${operation2.action}" targets ${recipients} recipients, exceeding the run ceiling of ${envelope.max_email_recipients}.`,
      "recipient_limit_exceeded"
    );
  }
}
function operationEnvironment(operation2) {
  if (operation2.environment === "sandbox") return "test";
  if (operation2.environment === "testflight") return "production";
  return operation2.environment;
}
function operationRisk(operation2) {
  return operation2.riskClass === "medium" ? "moderate" : operation2.riskClass;
}
function assertEnvelopeScope(envelope, operation2, now) {
  if (Date.parse(envelope.expires_at) <= now.getTime()) {
    throw new AuthorizationError(
      `Authorization envelope for ${envelope.run_id} expired at ${envelope.expires_at}.`,
      "envelope_expired"
    );
  }
  if (!envelope.providers.includes(operation2.provider)) {
    throw new AuthorizationError(
      `${operation2.provider} is outside the run authorization envelope.`,
      "provider_forbidden"
    );
  }
  const environment = operationEnvironment(operation2);
  if (!envelope.environments.includes(environment)) {
    throw new AuthorizationError(
      `${operation2.environment} is outside the run authorization envelope.`,
      "environment_forbidden"
    );
  }
  if (!envelope.allowed_capabilities.includes("*") && !envelope.allowed_capabilities.includes(operation2.capability)) {
    throw new AuthorizationError(
      `${operation2.capability} is outside the run authorization envelope.`,
      "capability_forbidden"
    );
  }
}
function requiresDistinctCheckpoint(envelope, policies, effect) {
  return (/* @__PURE__ */ new Set([
    ...policies.authorization.always_require_distinct_checkpoint_for,
    ...envelope.explicitly_forbidden_actions
  ])).has(effect);
}
function inspectOperationAuthorization(envelope, operation2, policies, now = /* @__PURE__ */ new Date()) {
  assertEnvelopeScope(envelope, operation2, now);
  const classification = operationEffect(operation2);
  const effect = classification.effect;
  const checkpointRequired = requiresDistinctCheckpoint(envelope, policies, effect);
  assertEffectConsistency(operation2, classification);
  if (!checkpointRequired) {
    if (!envelope.allowed_side_effect_classes.includes(effect)) {
      throw new AuthorizationError(
        `${effect} is not allowed by ${envelope.profile}.`,
        "effect_forbidden"
      );
    }
    assertPermissionFlag(envelope, operation2, effect);
  }
  assertSpendWithinEnvelope(envelope, operation2, effect);
  assertRecipientsWithinEnvelope(envelope, operation2, effect);
  const risk = operationRisk(operation2);
  const profile2 = policies.authorization.profiles[envelope.profile];
  if (!profile2.allowed_risk_classes.includes(risk)) {
    throw new AuthorizationError(
      `${risk} risk is not allowed by ${envelope.profile}.`,
      "risk_forbidden"
    );
  }
  return { effect, checkpointRequired };
}
function assertOperationAuthorized(envelope, operation2, policies, now = /* @__PURE__ */ new Date(), options = {}) {
  assertEnvelopeScope(envelope, operation2, now);
  const classification = operationEffect(operation2);
  const effect = classification.effect;
  const checkpointRequired = requiresDistinctCheckpoint(envelope, policies, effect);
  if (checkpointRequired && (!options.nodeId || !options.checkpointGrant)) {
    throw new AuthorizationError(
      `${effect} requires a distinct human checkpoint and cannot inherit session authorization.`,
      "distinct_checkpoint_required"
    );
  }
  inspectOperationAuthorization(envelope, operation2, policies, now);
  if (checkpointRequired) {
    try {
      assertConsumedCheckpointGrant(options.checkpointGrant, {
        scope: {
          runId: envelope.run_id,
          nodeId: options.nodeId,
          effect,
          operationId: operation2.id
        },
        now: now.toISOString()
      });
    } catch (error) {
      if (error instanceof CheckpointGrantError) {
        throw new AuthorizationError(error.message, "checkpoint_grant_invalid");
      }
      throw error;
    }
  }
}

// lib/config/harness-lock.ts
import { createHash as createHash3 } from "node:crypto";
import { readFileSync as readFileSync2 } from "node:fs";
import { resolve as resolve2 } from "node:path";
import { parse } from "yaml";
var managedFileOwnershipSchema = external_exports.enum([
  "harness",
  "project",
  "generated",
  "core_owned",
  "merge_managed",
  "venture_owned"
]);
var managedFileSchema = external_exports.object({
  path: external_exports.string().min(1).refine((value) => !value.startsWith("/") && !value.split("/").includes(".."), {
    message: "managed file paths must be repository-relative"
  }),
  ownership: managedFileOwnershipSchema,
  sha256: external_exports.string().regex(/^[a-f0-9]{64}$/).nullable(),
  base_sha256: external_exports.string().regex(/^[a-f0-9]{64}$/).nullable().optional()
}).strict();
var appliedMigrationSchema = external_exports.object({
  id: external_exports.string().regex(/^\d{3}-[a-z0-9-]+$/),
  from_version: semverSchema,
  to_version: semverSchema,
  applied_at: external_exports.string().datetime({ offset: true })
}).strict();
var legacyHarnessLockSchema = external_exports.object({
  lock_version: external_exports.literal(1),
  harness_version: semverSchema,
  config_contract_version: external_exports.number().int().positive(),
  source: external_exports.object({
    kind: external_exports.enum(["template", "release", "local"]),
    ref: external_exports.string().min(1).nullable()
  }).strict(),
  managed_files: uniqueArray(managedFileSchema),
  applied_migrations: uniqueArray(appliedMigrationSchema),
  extensions: extensionsSchema
}).strict();
var versionedComponentSchema = external_exports.record(external_exports.string().min(1), semverSchema).refine((value) => Object.keys(value).length > 0, {
  message: "at least one versioned component is required"
});
var ventureHarnessLockSchema = external_exports.object({
  lock_version: external_exports.literal(2),
  harness_version: semverSchema,
  core_version: semverSchema,
  config_contract_version: external_exports.number().int().positive(),
  source: external_exports.object({
    kind: external_exports.enum(["seed", "release", "local"]),
    ref: external_exports.string().min(1)
  }).strict(),
  seed: external_exports.object({
    id: external_exports.string().regex(/^[a-z][a-z0-9-]+$/),
    version: semverSchema
  }).strict(),
  runtime_packages: versionedComponentSchema,
  provider_adapters: external_exports.record(external_exports.string().min(1), semverSchema),
  generators: versionedComponentSchema,
  managed_files: uniqueArray(managedFileSchema),
  applied_migrations: uniqueArray(appliedMigrationSchema),
  migration_state: external_exports.array(external_exports.string().min(1)),
  update_channel: external_exports.enum(["stable", "candidate", "canary"]),
  workflow_ref_sha: external_exports.string().regex(/^[a-f0-9]{40}$/),
  last_verified_upgrade: external_exports.string().datetime({ offset: true }).nullable(),
  extensions: extensionsSchema
}).strict().superRefine((lock, context2) => {
  if (lock.harness_version !== lock.core_version) {
    context2.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["core_version"],
      message: "core_version must equal harness_version"
    });
  }
  for (const [index, file2] of lock.managed_files.entries()) {
    if (["harness", "project", "generated"].includes(file2.ownership)) {
      context2.addIssue({
        code: external_exports.ZodIssueCode.custom,
        path: ["managed_files", index, "ownership"],
        message: "v2 locks require core_owned, merge_managed, or venture_owned"
      });
    }
  }
});
var harnessLockSchema = external_exports.union([legacyHarnessLockSchema, ventureHarnessLockSchema]);
var HARNESS_FRAMEWORK_CONFIG_PATH = "config/framework.yaml";
function createManagedFileLockEntry(options) {
  return managedFileSchema.parse({
    path: options.path,
    ownership: options.ownership,
    sha256: createHash3("sha256").update(options.content).digest("hex")
  });
}
function parseHarnessLock(text2) {
  return harnessLockSchema.parse(parse(text2));
}
function loadHarnessLock(path = "harness.lock") {
  return parseHarnessLock(readFileSync2(resolve2(path), "utf8"));
}
function createHarnessLock(overrides = {}) {
  return legacyHarnessLockSchema.parse({
    lock_version: 1,
    harness_version: "0.2.0",
    config_contract_version: 2,
    source: { kind: "template", ref: null },
    managed_files: [],
    applied_migrations: [],
    extensions: {},
    ...overrides
  });
}
function createVentureHarnessLock(input) {
  return ventureHarnessLockSchema.parse({ lock_version: 2, ...input });
}

// lib/config/launch-schema.ts
var rejectedAlternativeSchema = external_exports.object({
  mode: launchModeSchema,
  reason: external_exports.string().min(1)
}).strict();
var routeFactorSchema = external_exports.object({
  level: external_exports.enum(["unknown", "low", "moderate", "high"]),
  rationale: external_exports.string().min(1)
}).strict();
var launchSchema = external_exports.object({
  contract_version: external_exports.literal(1),
  launch: external_exports.object({
    selected_mode: launchModeSchema,
    confidence: external_exports.number().min(0).max(1),
    rationale: external_exports.string().min(1),
    rejected_alternatives: uniqueArray(rejectedAlternativeSchema),
    assumptions: uniqueArray(external_exports.string().min(1)),
    evidence_that_could_change_choice: uniqueArray(external_exports.string().min(1)),
    rail: external_exports.object({
      app_kind: appKindSchema,
      mobile_stack: mobileStackSchema,
      rationale: external_exports.string().min(1)
    }).strict(),
    routing_factors: external_exports.object({
      smallest_useful_build_cost: routeFactorSchema,
      smallest_useful_build_time: routeFactorSchema,
      reversibility: routeFactorSchema,
      regulatory_or_safety_risk: routeFactorSchema,
      real_usage_required: routeFactorSchema,
      marketplace_cold_start: routeFactorSchema,
      operational_burden: routeFactorSchema,
      founder_evidence: routeFactorSchema,
      concierge_delivery_fit: routeFactorSchema,
      app_store_required: routeFactorSchema
    }).strict(),
    progressive_commitment: external_exports.object({
      specific_user_or_audience: external_exports.string().min(1).nullable(),
      problem_or_job: external_exports.string().min(1).nullable(),
      intended_outcome: external_exports.string().min(1).nullable(),
      smallest_core_journey: external_exports.string().min(1).nullable(),
      primary_success_signal: external_exports.string().min(1).nullable(),
      material_constraints: uniqueArray(external_exports.string().min(1)),
      known_truths: uniqueArray(external_exports.string().min(1)),
      unresolved_assumptions: uniqueArray(external_exports.string().min(1)),
      blocking_issues: uniqueArray(external_exports.string().min(1))
    }).strict(),
    extensions: extensionsSchema
  }).strict(),
  extensions: extensionsSchema
}).strict().superRefine((value, ctx) => {
  for (const [index, alternative] of value.launch.rejected_alternatives.entries()) {
    if (alternative.mode === value.launch.selected_mode) {
      ctx.addIssue({
        code: external_exports.ZodIssueCode.custom,
        path: ["launch", "rejected_alternatives", index, "mode"],
        message: "the selected mode cannot also be a rejected alternative"
      });
    }
  }
});
var unknownFactor = (rationale) => ({ level: "unknown", rationale });
function createDefaultLaunchConfig() {
  return launchSchema.parse({
    contract_version: 1,
    launch: {
      selected_mode: "validate_first",
      confidence: 0,
      rationale: "Conservative compatibility default; reroute from the founder brief before launch.",
      rejected_alternatives: [],
      assumptions: [
        "The v0.1 validation-first strategy remains active until the launch router runs."
      ],
      evidence_that_could_change_choice: [
        "A complete founder brief and the smallest-useful-product assessment."
      ],
      rail: {
        app_kind: "web",
        mobile_stack: "none",
        rationale: "The v0.1 harness contains a Next.js web foundation and no mobile project."
      },
      routing_factors: {
        smallest_useful_build_cost: unknownFactor("Assess from the founder brief."),
        smallest_useful_build_time: unknownFactor("Assess from the founder brief."),
        reversibility: unknownFactor("Assess the smallest useful implementation."),
        regulatory_or_safety_risk: unknownFactor("No venture risk assessment exists yet."),
        real_usage_required: unknownFactor(
          "Determine whether usage is needed to demonstrate value."
        ),
        marketplace_cold_start: unknownFactor("Determine whether the venture is a marketplace."),
        operational_burden: unknownFactor("Assess delivery and support burden."),
        founder_evidence: unknownFactor("No venture evidence is loaded in template state."),
        concierge_delivery_fit: unknownFactor("Assess whether concierge delivery can be honest."),
        app_store_required: unknownFactor("Determine whether an installed app is required.")
      },
      progressive_commitment: {
        specific_user_or_audience: null,
        problem_or_job: null,
        intended_outcome: null,
        smallest_core_journey: null,
        primary_success_signal: null,
        material_constraints: [],
        known_truths: [],
        unresolved_assumptions: [],
        blocking_issues: []
      },
      extensions: {}
    },
    extensions: {}
  });
}

// lib/config/loop-schema.ts
var loopAutonomySchema = external_exports.enum([
  "observe",
  "report",
  "propose",
  "open_pr",
  "autofix_low_risk"
]);
var loopInputSchema = external_exports.object({
  source: external_exports.enum(DATA_SOURCE_IDS),
  freshness_hours: external_exports.number().int().positive(),
  required: external_exports.boolean()
}).strict();
var loopMetricSchema = external_exports.object({
  id: external_exports.string().min(1),
  direction: external_exports.enum(["increase", "decrease", "maintain", "observe"])
}).strict();
var normalizedScalarSchema = external_exports.union([external_exports.string(), external_exports.number(), external_exports.boolean(), external_exports.null()]);
var metricDefinitionSchema = external_exports.object({
  id: external_exports.string().min(1),
  source: external_exports.enum(DATA_SOURCE_IDS),
  filter: external_exports.record(normalizedScalarSchema).default({}),
  operation: external_exports.enum(["sum", "count_rows"]),
  field: external_exports.string().min(1).nullable().default(null),
  sample_size_field: external_exports.string().min(1).nullable().default(null),
  limitation: external_exports.string().min(1).nullable().default(null)
}).strict().superRefine((value, ctx) => {
  if (value.operation === "sum" && !value.field) {
    ctx.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["field"],
      message: "sum metric definitions require field"
    });
  }
  if (value.operation === "count_rows" && value.field) {
    ctx.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["field"],
      message: "count_rows metric definitions must not declare field"
    });
  }
});
var candidateRuleSchema = external_exports.object({
  id: external_exports.string().min(1),
  metric_id: external_exports.string().min(1),
  comparator: external_exports.enum(["lt", "lte", "gt", "gte", "eq"]),
  threshold: external_exports.number().finite(),
  minimum_sample_size: external_exports.number().int().nonnegative(),
  journey: external_exports.string().min(1),
  title: external_exports.string().min(1),
  confidence: external_exports.number().min(0).max(1),
  risk: external_exports.enum(["low", "moderate", "high"]),
  effect_types: uniqueArray(sideEffectClassSchema).default(["local_write"]),
  protects_winner: external_exports.boolean().default(false)
}).strict();
var loopDefinitionSchema = external_exports.object({
  cadence: external_exports.enum(["daily", "weekly", "biweekly", "monthly"]),
  enabled: external_exports.boolean().default(false),
  trigger: external_exports.object({
    kind: external_exports.enum(["cron", "manual", "event"]),
    expression: external_exports.string().min(1)
  }).strict(),
  inputs: external_exports.array(loopInputSchema).default([]),
  primary_metrics: external_exports.array(loopMetricSchema).default([]),
  guardrail_metrics: external_exports.array(loopMetricSchema).default([]),
  metric_definitions: external_exports.array(metricDefinitionSchema).default([]),
  candidate_rules: external_exports.array(candidateRuleSchema).default([]),
  decision_rules: uniqueArray(external_exports.string().min(1)).default([]),
  maximum_actions: external_exports.number().int().nonnegative().default(1),
  maximum_iterations: external_exports.number().int().positive().default(1),
  autonomy: loopAutonomySchema.default("propose"),
  authorized_effect_types: uniqueArray(sideEffectClassSchema).default([
    "none",
    "local_write",
    "git_write"
  ]),
  output_destination: external_exports.string().min(1),
  next_run_at: external_exports.string().datetime({ offset: true }).nullable().default(null),
  stop_condition: external_exports.string().min(1).default("Stop when required data is missing or stale; report the exact missing source."),
  extensions: extensionsSchema
}).strict();
var loopsSchema = external_exports.object({
  contract_version: external_exports.literal(1),
  loops: external_exports.record(external_exports.string().regex(/^[a-z][a-z0-9_-]+$/), loopDefinitionSchema),
  extensions: extensionsSchema
}).strict();
function loop(cadence, expression, output) {
  return {
    cadence,
    enabled: false,
    trigger: { kind: "cron", expression },
    inputs: [],
    primary_metrics: [],
    guardrail_metrics: [],
    metric_definitions: [],
    candidate_rules: [],
    decision_rules: [],
    maximum_actions: cadence === "weekly" ? 3 : 1,
    maximum_iterations: 1,
    autonomy: "propose",
    authorized_effect_types: ["none", "local_write", "git_write"],
    output_destination: output,
    next_run_at: null,
    stop_condition: "Stop when required data is missing or stale; report the exact missing source.",
    extensions: {}
  };
}
function createDefaultLoopsConfig() {
  return loopsSchema.parse({
    contract_version: 1,
    loops: {
      daily_early_signal: loop("daily", "0 6 * * *", "reports/learning/daily"),
      weekly_growth: loop("weekly", "0 6 * * 1", "reports/learning/weekly"),
      biweekly_product: loop("biweekly", "0 7 1,15 * *", "reports/learning/biweekly"),
      monthly_strategy: loop("monthly", "0 8 1 * *", "reports/learning/monthly")
    },
    extensions: {}
  });
}

// lib/config/mobile-schema.ts
var mobileSchema = external_exports.object({
  contract_version: external_exports.literal(1),
  mobile: external_exports.object({
    stack: mobileStackSchema,
    rationale: external_exports.string().min(1),
    bundle_identifier: external_exports.string().regex(/^[A-Za-z][A-Za-z0-9.-]+$/).nullable(),
    app_scheme: external_exports.string().regex(/^[a-z][a-z0-9+.-]*$/).nullable(),
    app_store_connect: external_exports.object({
      team_id: external_exports.string().min(1).nullable(),
      app_id: external_exports.string().min(1).nullable(),
      sku: external_exports.string().min(1).nullable(),
      primary_language: external_exports.string().regex(/^[A-Za-z]{2,3}(?:-[A-Za-z0-9]{2,8})*$/),
      credential_ref: credentialReferenceSchema.nullable(),
      first_app_record: external_exports.object({
        state: external_exports.enum(["not_required", "required", "waiting", "complete"]),
        manual_action_ref: artifactReferenceSchema.nullable()
      }).strict()
    }).strict(),
    eas: external_exports.object({
      project_id: external_exports.string().min(1).nullable(),
      credential_ref: credentialReferenceSchema.nullable(),
      build_profiles: uniqueArray(external_exports.enum(["development", "preview", "production"])),
      submit_enabled: external_exports.boolean()
    }).strict(),
    signing: external_exports.object({
      credential_ref: credentialReferenceSchema.nullable(),
      certificates_in_git_forbidden: external_exports.literal(true)
    }).strict(),
    metadata_artifact_ref: artifactReferenceSchema.nullable(),
    screenshot_flow_artifact_ref: artifactReferenceSchema.nullable(),
    extensions: extensionsSchema
  }).strict(),
  extensions: extensionsSchema
}).strict().superRefine((value, ctx) => {
  rejectCredentialMaterial(value, ctx);
  const firstRecord = value.mobile.app_store_connect.first_app_record;
  if ((firstRecord.state === "required" || firstRecord.state === "waiting") && !firstRecord.manual_action_ref) {
    ctx.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["mobile", "app_store_connect", "first_app_record", "manual_action_ref"],
      message: `${firstRecord.state} first app record requires a manual_action_ref`
    });
  }
  if (value.mobile.stack === "none" && firstRecord.state !== "not_required") {
    ctx.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["mobile", "app_store_connect", "first_app_record", "state"],
      message: "a venture without a mobile rail cannot require an App Store record"
    });
  }
});
function createDefaultMobileConfig() {
  return mobileSchema.parse({
    contract_version: 1,
    mobile: {
      stack: "none",
      rationale: "The migrated v0.1 venture has no mobile project; reroute before mobile work.",
      bundle_identifier: null,
      app_scheme: null,
      app_store_connect: {
        team_id: null,
        app_id: null,
        sku: null,
        primary_language: "en",
        credential_ref: null,
        first_app_record: { state: "not_required", manual_action_ref: null }
      },
      eas: {
        project_id: null,
        credential_ref: null,
        build_profiles: ["development", "preview", "production"],
        submit_enabled: false
      },
      signing: { credential_ref: null, certificates_in_git_forbidden: true },
      metadata_artifact_ref: null,
      screenshot_flow_artifact_ref: null,
      extensions: {}
    },
    extensions: {}
  });
}

// lib/config/provider-schema.ts
var providerLifecycleStateSchema = external_exports.enum([
  "unconfigured",
  "auth_required",
  "planned",
  "applying",
  "waiting_manual_action",
  "configured",
  "verified",
  "degraded",
  "failed",
  "disabled"
]);
var providerTransportSchema = external_exports.enum([
  "none",
  "mcp",
  "cli",
  "rest_api",
  "graphql_api",
  "manual"
]);
var providerStateSchema = external_exports.object({
  state: providerLifecycleStateSchema.default("unconfigured"),
  capability_ids: uniqueArray(capabilityIdSchema),
  external_resource_ids: external_exports.record(external_exports.string().min(1).max(300)).default({}),
  account_id: external_exports.string().min(1).max(300).nullable().default(null),
  team_id: external_exports.string().min(1).max(300).nullable().default(null),
  region: external_exports.string().min(1).max(100).nullable().default(null),
  selected_transport: providerTransportSchema.default("none"),
  credential_ref: credentialReferenceSchema.nullable().default(null),
  required_scopes: uniqueArray(external_exports.string().min(1).max(200)).default([]),
  last_verified_at: external_exports.string().datetime({ offset: true }).nullable().default(null),
  evidence_artifact_ref: artifactReferenceSchema.nullable().default(null),
  error_code: external_exports.string().min(1).max(100).nullable().default(null),
  error_message: external_exports.string().min(1).max(1e3).nullable().default(null),
  retryable: external_exports.boolean().nullable().default(null),
  next_action: external_exports.string().min(1).max(1e3).nullable().default(null),
  manual_action_ref: artifactReferenceSchema.nullable().default(null),
  extensions: extensionsSchema
}).strict().superRefine((value, ctx) => {
  rejectCredentialMaterial(value, ctx);
  if ((value.state === "failed" || value.state === "degraded") && !value.error_code) {
    ctx.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["error_code"],
      message: `${value.state} provider state requires an error_code`
    });
  }
  if (value.state === "waiting_manual_action" && !value.manual_action_ref) {
    ctx.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["manual_action_ref"],
      message: "waiting_manual_action requires a manual_action_ref"
    });
  }
  if (value.state === "verified" && (!value.last_verified_at || !value.evidence_artifact_ref)) {
    ctx.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["state"],
      message: "verified state requires last_verified_at and evidence_artifact_ref"
    });
  }
});
var providersSchema = external_exports.object({
  contract_version: external_exports.literal(1),
  providers: external_exports.record(providerIdSchema, providerStateSchema),
  extensions: extensionsSchema
}).strict().superRefine(rejectCredentialMaterial);
function createProviderState(seed) {
  return {
    state: "unconfigured",
    capability_ids: seed.capability_ids,
    external_resource_ids: {},
    account_id: null,
    team_id: null,
    region: null,
    selected_transport: seed.selected_transport ?? "none",
    credential_ref: null,
    required_scopes: [],
    last_verified_at: null,
    evidence_artifact_ref: null,
    error_code: null,
    error_message: null,
    retryable: null,
    next_action: null,
    manual_action_ref: null,
    extensions: {}
  };
}
function createDefaultProvidersConfig() {
  return providersSchema.parse({
    contract_version: 1,
    providers: {
      github: createProviderState({ capability_ids: [] }),
      vercel: createProviderState({ capability_ids: ["public_website", "vercel_analytics"] }),
      neon: createProviderState({ capability_ids: ["database"] }),
      stripe: createProviderState({ capability_ids: ["stripe"] }),
      revenuecat: createProviderState({ capability_ids: ["revenuecat"] }),
      brevo: createProviderState({
        capability_ids: ["transactional_email", "lifecycle_email"]
      }),
      google: createProviderState({ capability_ids: ["ga4", "gsc"] }),
      bing: createProviderState({ capability_ids: ["bing_webmaster"] }),
      dns: createProviderState({
        capability_ids: ["public_website"],
        selected_transport: "manual"
      }),
      mijndomein: createProviderState({
        capability_ids: ["public_website"],
        selected_transport: "manual"
      }),
      app_store_connect: createProviderState({ capability_ids: ["app_store_connect", "ios_aso"] }),
      eas: createProviderState({ capability_ids: ["eas"] })
    },
    extensions: {}
  });
}

// lib/config/venture-schema.ts
var legacyVentureSchema = external_exports.object({
  venture: external_exports.object({
    name: external_exports.string().nullable(),
    domain: external_exports.string().nullable(),
    stage: external_exports.enum([
      "template",
      "ideation",
      "demand_validation",
      "build",
      "iterate",
      "reposition",
      "stopped"
    ]),
    language: external_exports.string(),
    currency: external_exports.string(),
    timezone: external_exports.string()
  }).passthrough(),
  validation: external_exports.object({
    minimum_days: external_exports.number().int().min(1),
    target_days: external_exports.number().int().min(1),
    maximum_days: external_exports.number().int().min(1),
    launch_date: external_exports.string().nullable(),
    primary_conversion: external_exports.string().nullable(),
    build_threshold: external_exports.string().nullable(),
    stop_threshold: external_exports.string().nullable()
  }).passthrough(),
  infrastructure: external_exports.record(external_exports.boolean())
}).passthrough();
var outcomeSchema = external_exports.object({
  statement: external_exports.string().min(1).nullable(),
  success_signal: external_exports.string().min(1).nullable()
}).strict();
var ventureV02Schema = external_exports.object({
  venture: external_exports.object({
    name: external_exports.string().nullable(),
    legal_name: external_exports.string().nullable(),
    domain: external_exports.string().nullable(),
    market: external_exports.string().nullable(),
    target_market: external_exports.string().nullable(),
    language: external_exports.string().min(2),
    locale: external_exports.string().regex(/^[A-Za-z]{2,3}(?:-[A-Za-z0-9]{2,8})*$/),
    currency: external_exports.string().regex(/^[A-Z]{3}$/),
    timezone: external_exports.string().min(1),
    stage: external_exports.enum([
      "template",
      "ideation",
      "demand_validation",
      "build",
      "iterate",
      "reposition",
      "stopped"
    ]),
    repository_visibility: external_exports.enum(["private", "public"]),
    production_status: external_exports.enum([
      "none",
      "preview_live",
      "validation_site_live",
      "product_live",
      "testflight_live"
    ]),
    harness_version: semverSchema,
    app_kind: appKindSchema,
    launch_mode: launchModeSchema,
    business_model: external_exports.enum([
      "unselected",
      "b2b",
      "b2c",
      "b2b2c",
      "marketplace",
      "internal",
      "nonprofit"
    ]),
    monetization_model: external_exports.enum([
      "unselected",
      "none",
      "subscription",
      "one_time",
      "usage_based",
      "transaction_fee",
      "lead_generation",
      "services",
      "hybrid"
    ]),
    risk_profile: external_exports.enum(["unassessed", "low", "moderate", "high", "regulated"]),
    privacy_profile: external_exports.enum([
      "unassessed",
      "minimal",
      "standard",
      "sensitive",
      "special_category"
    ]),
    mobile_stack: mobileStackSchema,
    outcomes: external_exports.object({
      primary: outcomeSchema,
      secondary: external_exports.array(outcomeSchema)
    }).strict(),
    capabilities: external_exports.object({
      active: uniqueArray(knownCapabilityIdSchema),
      open: uniqueArray(openCapabilityIdSchema)
    }).strict(),
    extensions: extensionsSchema
  }).passthrough(),
  // Kept as an optional strategy for validate_first and as a compatibility
  // surface for v0.1 tooling. It is no longer a universal launch gate.
  validation: legacyVentureSchema.shape.validation,
  // Deprecated compatibility view. Provider lifecycle state lives in
  // config/providers.yaml; this record is retained until downstream scripts migrate.
  infrastructure: external_exports.record(external_exports.boolean()),
  extensions: extensionsSchema
}).passthrough();
var ventureSchema = ventureV02Schema;

// lib/config/schemas.ts
var frameworkSchema = external_exports.object({
  framework: external_exports.object({
    name: external_exports.string().min(1),
    version: external_exports.string().regex(/^\d+\.\d+\.\d+$/),
    public_template: external_exports.boolean(),
    license: external_exports.string()
  }),
  supported_agents: external_exports.array(external_exports.string()).min(1),
  package_manager: external_exports.literal("pnpm"),
  generated_paths: external_exports.array(external_exports.string()).min(1),
  sync_excludes: external_exports.object({
    claude: external_exports.array(external_exports.string()),
    codex: external_exports.array(external_exports.string())
  }),
  verification: external_exports.object({ primary: external_exports.string() }).passthrough()
}).passthrough();
var offerSchema = external_exports.object({
  icp: external_exports.object({ description: external_exports.string().nullable() }).passthrough(),
  offer: external_exports.object({ sentence: external_exports.string().nullable() }).passthrough(),
  pricing: external_exports.object({
    currency: external_exports.string(),
    monthly_price: external_exports.number().nullable(),
    annual_price: external_exports.number().nullable(),
    implementation_fee: external_exports.number().nullable()
  }).passthrough(),
  economics: external_exports.object({
    cac_assumption: external_exports.number().nullable(),
    delivery_cost_monthly: external_exports.number().nullable(),
    payback_target_days: external_exports.number()
  }).passthrough()
}).passthrough();
var experimentVariantSchema = external_exports.object({
  key: external_exports.string().min(1),
  weight: external_exports.number().gt(0).lte(1),
  displayed_offer: external_exports.string().min(1),
  displayed_price: external_exports.string().min(1)
});
var experimentSchema = external_exports.object({
  id: external_exports.string().regex(/^exp-\d{3}-[a-z0-9-]+$/),
  name: external_exports.string().min(1),
  owner: external_exports.string().min(1),
  status: external_exports.enum(["draft", "approved", "running", "stopped", "decided"]),
  type: external_exports.enum(["icp", "hero", "proof", "cta", "pricing", "fake_door"]),
  hypothesis: external_exports.string().min(20),
  routes: external_exports.array(external_exports.string()).min(1),
  activation_event: external_exports.string().min(1),
  exposure_event: external_exports.string().min(1),
  variants: external_exports.array(experimentVariantSchema).min(2),
  primary_metric: external_exports.string().min(1),
  secondary_metrics: external_exports.array(external_exports.string()),
  guardrails: external_exports.array(external_exports.object({ metric: external_exports.string(), rule: external_exports.string() })),
  min_duration_days: external_exports.number().int().min(1),
  max_duration_days: external_exports.number().int().min(1),
  min_observations_per_variant: external_exports.number().int().min(1),
  stop_rule: external_exports.string().min(10),
  result: external_exports.string().nullable(),
  limitations: external_exports.string().nullable(),
  final_decision: external_exports.enum(["adopt", "reject", "rerun", "inconclusive"]).nullable()
}).passthrough();
var experimentsSchema = external_exports.object({
  defaults: external_exports.object({}).passthrough(),
  experiments: external_exports.array(experimentSchema)
}).passthrough();
var analyticsEventSchema = external_exports.object({
  purpose: external_exports.string().min(5),
  trigger: external_exports.string().min(3),
  destinations: external_exports.array(external_exports.enum(["vercel", "ga4", "neon"])),
  consent: external_exports.enum(["none", "analytics"]),
  props: external_exports.array(external_exports.string()),
  neon: external_exports.boolean(),
  experiment: external_exports.boolean()
});
var analyticsSchema = external_exports.object({
  providers: external_exports.record(external_exports.object({ layer: external_exports.number(), purpose: external_exports.string() }).passthrough()),
  consent: external_exports.object({
    default_mode: external_exports.enum(["strict", "basic"]),
    google_analytics: external_exports.enum(["opt_in", "opt_out"]),
    vercel_analytics: external_exports.enum(["opt_in", "opt_out"]),
    allow_withdrawal: external_exports.literal(true),
    settings_link_required: external_exports.literal(true)
  }),
  collection: external_exports.object({
    track_material_behaviour: external_exports.literal(true),
    collect_keystrokes: external_exports.literal(false),
    collect_mouse_movement: external_exports.literal(false),
    session_replay: external_exports.literal(false),
    send_form_values_to_analytics: external_exports.literal(false),
    send_search_text_to_third_parties: external_exports.literal(false),
    send_email_to_analytics: external_exports.literal(false),
    enable_advertising_features: external_exports.literal(false)
  }),
  prohibited_properties: external_exports.array(external_exports.string()).min(5),
  events: external_exports.record(analyticsEventSchema)
}).passthrough();
var qualityGapSchema = external_exports.object({
  why: external_exports.string().min(10),
  missing: external_exports.string().min(10),
  exact_command: external_exports.string().min(3),
  expected_evidence: external_exports.string().min(10)
}).strict();
var qualityCheckSchema = external_exports.object({
  kind: external_exports.enum([
    "command",
    "changed_format",
    "changed_lint",
    "changed_tests",
    "tracked_secret_scan",
    "analytics_readiness",
    "raw_html",
    "manual",
    "provider_readback"
  ]),
  phase: external_exports.number().int().positive(),
  command: external_exports.union([external_exports.string().min(1), external_exports.array(external_exports.string().min(1)).min(1)]).optional(),
  when_paths: external_exports.array(external_exports.string().min(1)).min(1).optional(),
  provider: external_exports.string().min(1).optional(),
  gap: qualityGapSchema.optional()
}).strict().superRefine((value, ctx) => {
  if (["manual", "provider_readback"].includes(value.kind) && !value.gap) {
    ctx.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["gap"],
      message: `${value.kind} checks require an actionable gap`
    });
  }
  if (value.kind === "provider_readback" && !value.provider) {
    ctx.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["provider"],
      message: "provider_readback checks require a provider"
    });
  }
});
var qualityProfileSchema = external_exports.object({
  description: external_exports.string().min(10),
  checks: external_exports.array(external_exports.string().min(1)).min(1)
}).strict();
var qualityCapabilityProfileSchema = external_exports.object({
  fast: external_exports.array(external_exports.string().min(1)).optional(),
  mvp: external_exports.array(external_exports.string().min(1)).optional(),
  release: external_exports.array(external_exports.string().min(1)).optional(),
  live: external_exports.array(external_exports.string().min(1)).optional(),
  stable: external_exports.array(external_exports.string().min(1)).optional()
}).strict();
var qualitySchema = external_exports.object({
  required_commands: external_exports.object({
    always: external_exports.array(external_exports.string().min(1)).min(5),
    ci_additional: external_exports.array(external_exports.string().min(1))
  }).strict(),
  profiles: external_exports.object({
    fast: qualityProfileSchema,
    mvp: qualityProfileSchema,
    // `release` proves code and fixtures without a connected provider,
    // `live` proves only provider read-back, `stable` requires both.
    release: qualityProfileSchema,
    live: qualityProfileSchema,
    stable: qualityProfileSchema
  }).strict(),
  checks: external_exports.record(external_exports.string().min(1), qualityCheckSchema),
  capability_checks: external_exports.record(external_exports.string().min(1), qualityCapabilityProfileSchema),
  required_documents: external_exports.array(external_exports.string().min(1)).min(1),
  thresholds: external_exports.object({
    performance: external_exports.object({
      lcp_ms: external_exports.number().positive(),
      cls: external_exports.number().nonnegative(),
      inp_ms: external_exports.number().positive()
    }).strict(),
    accessibility: external_exports.object({
      standard: external_exports.string().min(1),
      min_contrast_normal_text: external_exports.number().positive(),
      min_contrast_large_text: external_exports.number().positive(),
      keyboard_navigable: external_exports.boolean()
    }).strict(),
    crawler: external_exports.object({
      raw_html_must_contain: external_exports.array(external_exports.string().min(1)).min(1),
      user_agents: external_exports.array(external_exports.string().min(1)).min(1)
    }).strict()
  }).strict(),
  release_checks: external_exports.object({
    no_secrets: external_exports.boolean(),
    no_real_pii: external_exports.boolean(),
    no_unlabeled_synthetic_data: external_exports.boolean(),
    license_present: external_exports.boolean(),
    generated_dirs_in_sync: external_exports.boolean()
  }).strict()
}).strict().superRefine((value, ctx) => {
  const knownChecks = new Set(Object.keys(value.checks));
  for (const [profile2, definition2] of Object.entries(value.profiles)) {
    for (const check of definition2.checks) {
      if (!knownChecks.has(check)) {
        ctx.addIssue({
          code: external_exports.ZodIssueCode.custom,
          path: ["profiles", profile2, "checks"],
          message: `references unknown check ${check}`
        });
      }
    }
  }
  for (const [capability, profiles] of Object.entries(value.capability_checks)) {
    for (const [profile2, checks] of Object.entries(profiles)) {
      for (const check of checks ?? []) {
        if (!knownChecks.has(check)) {
          ctx.addIssue({
            code: external_exports.ZodIssueCode.custom,
            path: ["capability_checks", capability, profile2],
            message: `references unknown check ${check}`
          });
        }
      }
    }
  }
  const readbacks = new Set(
    Object.entries(value.checks).filter(([, check]) => check.kind === "provider_readback").map(([id]) => id)
  );
  for (const check of value.profiles.release.checks) {
    if (readbacks.has(check)) {
      ctx.addIssue({
        code: external_exports.ZodIssueCode.custom,
        path: ["profiles", "release", "checks"],
        message: `${check} is a live provider read-back and must move to the live and stable profiles`
      });
    }
  }
  for (const [capability, profiles] of Object.entries(value.capability_checks)) {
    for (const check of profiles.release ?? []) {
      if (readbacks.has(check)) {
        ctx.addIssue({
          code: external_exports.ZodIssueCode.custom,
          path: ["capability_checks", capability, "release"],
          message: `${check} is a live provider read-back and must move to the live and stable profiles`
        });
      }
    }
  }
});
var contentSchema = external_exports.object({
  banned_phrases: external_exports.array(external_exports.string()).min(1),
  answer_block_rules: external_exports.object({ max_words: external_exports.number() }).passthrough()
}).passthrough();
var distributionSchema = external_exports.object({
  channel_priority: external_exports.array(external_exports.string()).min(2),
  approval: external_exports.object({
    human_approval_required_for: external_exports.array(external_exports.string()).min(3)
  }).passthrough(),
  outreach_rules: external_exports.object({
    automated_sending: external_exports.literal("forbidden"),
    mass_generic_messages: external_exports.literal("forbidden")
  }).passthrough(),
  posting_rules: external_exports.object({
    automated_posting: external_exports.literal("forbidden"),
    engagement_manipulation: external_exports.literal("forbidden")
  }).passthrough()
}).passthrough();

// lib/credentials/types.ts
var credentialKinds = [
  "oauth",
  "api_key",
  "restricted_api_key",
  "jwt_private_key",
  "cli_session",
  "service_account",
  "connection_string",
  "ci_secret"
];
var CredentialError = class extends Error {
  constructor(message, code) {
    super(message);
    this.code = code;
    this.name = "CredentialError";
  }
  code;
};
function assertCredentialRef(ref) {
  if (!/^cred:\/\/[a-z0-9][a-z0-9._/-]*$/i.test(ref)) {
    throw new CredentialError(
      `Credential reference must use the cred:// scheme: ${ref}`,
      "invalid_reference"
    );
  }
}

// lib/credentials/backends.ts
function commandFailure(backend, action, result2) {
  return new CredentialError(
    `${backend} could not ${action} credential (exit ${result2.exitCode})`,
    "backend_failure"
  );
}
function assertNoShell(invocation2) {
  const binary = invocation2.command.trim();
  if (binary.length === 0 || /\s/.test(binary) || ["sh", "bash", "zsh", "fish", "cmd", "powershell", "pwsh"].includes(
    binary.split("/").at(-1) ?? binary
  )) {
    throw new CredentialError(
      `Credential commands must call a binary directly: ${invocation2.command}`,
      "backend_failure"
    );
  }
}
async function runDirect(runner, invocation2) {
  assertNoShell(invocation2);
  return runner.run(invocation2);
}
var EnvironmentCredentialBackend = class {
  id;
  writable = false;
  env;
  variableForRef;
  constructor(options = {}) {
    this.id = options.id ?? "environment";
    this.env = options.env ?? process.env;
    this.variableForRef = options.variableForRef ?? ((ref) => ref.replace(/^cred:\/\//, "").replace(/[^a-z0-9]+/gi, "_").toUpperCase());
  }
  variable(reference) {
    return typeof this.variableForRef === "function" ? this.variableForRef(reference.ref) : this.variableForRef[reference.ref] ?? "";
  }
  async get(reference) {
    const variable = this.variable(reference);
    return variable ? this.env[variable] ?? null : null;
  }
  async set() {
    throw new CredentialError(
      "Environment credentials are read-only; update the process environment",
      "backend_read_only"
    );
  }
  async delete() {
    return false;
  }
  async inspect(reference) {
    const variable = this.variable(reference);
    return {
      status: variable && this.env[variable] ? "available" : "missing",
      writable: false,
      message: variable ? `Resolved from environment variable ${variable}` : "No environment variable is mapped to this credential reference"
    };
  }
};
var MacOSKeychainCredentialBackend = class {
  id;
  writable = true;
  runner;
  service;
  binary;
  constructor(options) {
    this.id = options.id ?? "macos_keychain";
    this.runner = options.runner;
    this.service = options.service ?? "venture-harness";
    this.binary = options.binary ?? "/usr/bin/security";
  }
  async get(reference) {
    const result2 = await runDirect(this.runner, {
      command: this.binary,
      args: ["find-generic-password", "-s", this.service, "-a", reference.ref, "-w"]
    });
    if (result2.exitCode === 44) return null;
    if (result2.exitCode !== 0) throw commandFailure(this.id, "read", result2);
    return result2.stdout.replace(/\r?\n$/, "");
  }
  async set(reference, value) {
    const args = [
      "add-generic-password",
      "-U",
      "-s",
      this.service,
      "-a",
      reference.ref,
      "-w",
      value
    ];
    const result2 = await runDirect(this.runner, {
      command: this.binary,
      args,
      sensitiveArgs: [args.length - 1]
    });
    if (result2.exitCode !== 0) throw commandFailure(this.id, "store", result2);
  }
  async delete(reference) {
    const result2 = await runDirect(this.runner, {
      command: this.binary,
      args: ["delete-generic-password", "-s", this.service, "-a", reference.ref]
    });
    if (result2.exitCode === 44) return false;
    if (result2.exitCode !== 0) throw commandFailure(this.id, "delete", result2);
    return true;
  }
  async inspect(reference) {
    const result2 = await runDirect(this.runner, {
      command: this.binary,
      args: ["find-generic-password", "-s", this.service, "-a", reference.ref]
    });
    if (result2.exitCode === 44) return { status: "missing", writable: true };
    if (result2.exitCode !== 0) {
      return {
        status: "unavailable",
        writable: true,
        message: `Keychain inspection failed with exit ${result2.exitCode}`
      };
    }
    return { status: "available", writable: true };
  }
};
var OnePasswordCredentialBackend = class {
  id;
  writable = true;
  runner;
  itemForRef;
  field;
  vault;
  binary;
  constructor(options) {
    this.id = options.id ?? "onepassword";
    this.runner = options.runner;
    this.itemForRef = options.itemForRef ?? ((ref) => ref);
    this.field = options.field ?? "credential";
    this.vault = options.vault;
    this.binary = options.binary ?? "op";
  }
  vaultArgs() {
    return this.vault ? ["--vault", this.vault] : [];
  }
  async get(reference) {
    const result2 = await runDirect(this.runner, {
      command: this.binary,
      args: [
        "item",
        "get",
        this.itemForRef(reference.ref),
        ...this.vaultArgs(),
        "--fields",
        `label=${this.field}`,
        "--reveal"
      ]
    });
    if (result2.exitCode === 1) return null;
    if (result2.exitCode !== 0) throw commandFailure(this.id, "read", result2);
    return result2.stdout.replace(/\r?\n$/, "");
  }
  async set(reference, value) {
    const assignment = `${this.field}=${value}`;
    const args = ["item", "edit", this.itemForRef(reference.ref), ...this.vaultArgs(), assignment];
    const result2 = await runDirect(this.runner, {
      command: this.binary,
      args,
      sensitiveArgs: [args.length - 1]
    });
    if (result2.exitCode !== 0) throw commandFailure(this.id, "store", result2);
  }
  async delete(reference) {
    const result2 = await runDirect(this.runner, {
      command: this.binary,
      args: ["item", "delete", this.itemForRef(reference.ref), ...this.vaultArgs(), "--archive"]
    });
    if (result2.exitCode === 1) return false;
    if (result2.exitCode !== 0) throw commandFailure(this.id, "archive", result2);
    return true;
  }
  async inspect(reference) {
    const result2 = await runDirect(this.runner, {
      command: this.binary,
      args: [
        "item",
        "get",
        this.itemForRef(reference.ref),
        ...this.vaultArgs(),
        "--format",
        "json"
      ]
    });
    if (result2.exitCode === 1) return { status: "missing", writable: true };
    if (result2.exitCode !== 0) {
      return {
        status: "unavailable",
        writable: true,
        message: `1Password CLI inspection failed with exit ${result2.exitCode}`
      };
    }
    return { status: "available", writable: true };
  }
};

// lib/credentials/redaction.ts
var SENSITIVE_KEYS = /* @__PURE__ */ new Set([
  "access_token",
  "api-key",
  "api_key",
  "apikey",
  "authorization",
  "cookie",
  "connection_string",
  "connection_uri",
  "credential",
  "credentials",
  "jwt",
  "password",
  "private_key",
  "refresh_token",
  "secret",
  "token",
  "value"
]);
var REDACTED = "[REDACTED]";
function isSensitiveKey(key) {
  const lower = key.toLowerCase();
  if (SENSITIVE_KEYS.has(lower)) return true;
  const compact = lower.replace(/[-_]/g, "");
  return [
    "accesstoken",
    "apikey",
    "authorization",
    "authorizationheader",
    "clientsecret",
    "connectionstring",
    "connectionuri",
    "credential",
    "credentialvalue",
    "idtoken",
    "password",
    "privatekey",
    "refreshtoken",
    "sharedsecret",
    "signingsecret",
    "subscriptionprivatekey"
  ].includes(compact);
}
var Redactor = class {
  knownSecrets = /* @__PURE__ */ new Set();
  addSecret(secret) {
    if (secret.length > 0) {
      this.knownSecrets.add(secret);
    }
  }
  redactText(input) {
    let output = input;
    for (const secret of [...this.knownSecrets].sort((left, right) => right.length - left.length)) {
      output = output.split(secret).join(REDACTED);
    }
    output = output.replace(
      /\b(Bearer|Basic)\s+[A-Za-z0-9._~+/=-]+/gi,
      (_match, scheme) => `${scheme} ${REDACTED}`
    );
    output = output.replace(
      /\bauthorization\b\s*[:=]\s*(?:(?:Bearer|Basic)\s+)?[^\s,;&]+/gi,
      `Authorization=${REDACTED}`
    );
    output = output.replace(
      /\b(api[-_]?key|password|private[-_]?key|refresh[-_]?token|secret|token)\b\s*[:=]\s*([^\s,;&]+)/gi,
      (_match, key) => `${key}=${REDACTED}`
    );
    output = output.replace(/(https?:\/\/[^\s/:]+:)([^@\s/]+)(@)/gi, `$1${REDACTED}$3`);
    return output;
  }
  redact(input) {
    return this.redactValue(input, /* @__PURE__ */ new WeakSet());
  }
  redactValue(input, seen) {
    if (typeof input === "string") {
      return this.redactText(input);
    }
    if (input === null || typeof input !== "object") {
      return input;
    }
    if (seen.has(input)) {
      return "[CIRCULAR]";
    }
    seen.add(input);
    if (Array.isArray(input)) {
      return input.map((value) => this.redactValue(value, seen));
    }
    const result2 = {};
    for (const [key, value] of Object.entries(input)) {
      result2[key] = isSensitiveKey(key) ? REDACTED : this.redactValue(value, seen);
    }
    return result2;
  }
};

// lib/credentials/broker.ts
function publicReference(input) {
  assertCredentialRef(input.ref);
  if (input.testedAt === void 0 !== (input.testStatus === void 0)) {
    throw new CredentialError(
      `Credential test evidence requires both testedAt and testStatus: ${input.ref}`,
      "invalid_reference"
    );
  }
  for (const [field, value] of [
    ["testedAt", input.testedAt],
    ["revokedAt", input.revokedAt]
  ]) {
    if (value !== void 0 && Number.isNaN(Date.parse(value))) {
      throw new CredentialError(
        `Credential ${field} must be an ISO timestamp: ${input.ref}`,
        "invalid_reference"
      );
    }
  }
  return {
    ref: input.ref,
    provider: input.provider,
    kind: input.kind,
    backend: input.backend,
    label: input.label,
    scopes: [...input.scopes ?? []],
    accountId: input.accountId,
    expiresAt: input.expiresAt,
    testedAt: input.testedAt,
    testStatus: input.testStatus,
    revokedAt: input.revokedAt
  };
}
var CredentialBroker = class {
  redactor;
  backends = /* @__PURE__ */ new Map();
  references = /* @__PURE__ */ new Map();
  revoked = /* @__PURE__ */ new Set();
  constructor(backends = [], redactor = new Redactor()) {
    this.redactor = redactor;
    for (const backend of backends) this.addBackend(backend);
  }
  addBackend(backend) {
    if (this.backends.has(backend.id)) {
      throw new CredentialError(
        `Credential backend already registered: ${backend.id}`,
        "backend_failure"
      );
    }
    this.backends.set(backend.id, backend);
  }
  register(input) {
    const reference = publicReference(input);
    this.requireBackend(reference.backend);
    this.references.set(reference.ref, reference);
    if (reference.revokedAt) this.revoked.add(reference.ref);
    else this.revoked.delete(reference.ref);
    return { ...reference, scopes: [...reference.scopes] };
  }
  async store(input) {
    const { value, ...metadata } = input;
    const reference = publicReference(metadata);
    const backend = this.requireBackend(reference.backend);
    if (!backend.writable) {
      throw new CredentialError(
        `Credential backend is read-only: ${backend.id}`,
        "backend_read_only"
      );
    }
    await backend.set(reference, value);
    this.redactor.addSecret(value);
    this.references.set(reference.ref, reference);
    if (reference.revokedAt) this.revoked.add(reference.ref);
    else this.revoked.delete(reference.ref);
    return { ...reference, scopes: [...reference.scopes] };
  }
  getReference(ref) {
    const reference = this.references.get(ref);
    return reference ? { ...reference, scopes: [...reference.scopes] } : null;
  }
  list() {
    return [...this.references.values()].map((reference) => ({
      ...reference,
      scopes: [...reference.scopes]
    }));
  }
  async inspect(ref) {
    const reference = this.requireReference(ref);
    const backend = this.requireBackend(reference.backend);
    if (this.revoked.has(ref)) {
      return {
        ...reference,
        status: "revoked",
        writable: backend.writable
      };
    }
    const backendInspection = await backend.inspect(reference);
    const expired = reference.expiresAt !== void 0 && new Date(reference.expiresAt).getTime() <= Date.now();
    return {
      ...reference,
      status: backendInspection.status === "available" && expired ? "expired" : backendInspection.status,
      writable: backendInspection.writable,
      message: backendInspection.message ? this.redactor.redactText(backendInspection.message) : void 0
    };
  }
  async withSecret(ref, consume) {
    const reference = this.requireReference(ref);
    if (this.revoked.has(ref) || reference.revokedAt) {
      throw new CredentialError(`Credential is revoked: ${ref}`, "credential_not_found");
    }
    const backend = this.requireBackend(reference.backend);
    const secret = await backend.get(reference);
    if (secret === null || secret.length === 0) {
      throw new CredentialError(`Credential is unavailable: ${ref}`, "credential_not_found");
    }
    this.redactor.addSecret(secret);
    return consume(secret, { ...reference, scopes: [...reference.scopes] });
  }
  async test(ref, tester) {
    let result2;
    try {
      result2 = await this.withSecret(ref, tester);
    } catch (error) {
      throw new CredentialError(
        this.redactor.redactText(error instanceof Error ? error.message : String(error)),
        "backend_failure"
      );
    }
    const testedAt = (/* @__PURE__ */ new Date()).toISOString();
    const current = this.requireReference(ref);
    this.references.set(ref, {
      ...current,
      testedAt,
      testStatus: result2.ok ? "passed" : "failed",
      accountId: result2.ok ? result2.accountId ?? current.accountId : current.accountId,
      scopes: result2.ok && result2.scopes ? [...result2.scopes] : current.scopes,
      expiresAt: result2.ok ? result2.expiresAt ?? current.expiresAt : current.expiresAt
    });
    return this.redactor.redact(result2);
  }
  async revoke(ref) {
    const reference = this.requireReference(ref);
    const backend = this.requireBackend(reference.backend);
    let removed = false;
    let localRemovalError;
    try {
      removed = await backend.delete(reference);
    } catch (error) {
      localRemovalError = this.redactor.redactText(
        error instanceof Error ? error.message : String(error)
      );
    }
    const revokedAt = (/* @__PURE__ */ new Date()).toISOString();
    this.references.set(ref, { ...reference, revokedAt });
    this.revoked.add(ref);
    return {
      ref,
      removed,
      localAccessDisabled: true,
      revokedAt,
      ...localRemovalError ? { localRemovalError } : {}
    };
  }
  requireBackend(id) {
    const backend = this.backends.get(id);
    if (!backend) {
      throw new CredentialError(`Credential backend is not registered: ${id}`, "backend_not_found");
    }
    return backend;
  }
  requireReference(ref) {
    assertCredentialRef(ref);
    const reference = this.references.get(ref);
    if (!reference) {
      throw new CredentialError(
        `Credential reference is not registered: ${ref}`,
        "credential_not_found"
      );
    }
    return reference;
  }
};

// lib/credentials/catalog.ts
import { existsSync as existsSync2, mkdirSync as mkdirSync2, readFileSync as readFileSync3, renameSync as renameSync2, writeFileSync as writeFileSync2 } from "node:fs";
import { homedir } from "node:os";
import { dirname, join as join2, resolve as resolve3 } from "node:path";
var FORBIDDEN_KEYS2 = /token|secret|password|private.?key|api.?key|credential.?value/i;
function parseReference(value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    throw new Error("credential catalog reference must be an object");
  }
  const input = value;
  for (const key of Object.keys(input)) {
    if (FORBIDDEN_KEYS2.test(key)) {
      throw new Error(`credential catalog contains forbidden field ${key}`);
    }
  }
  if (typeof input.ref !== "string") throw new Error("credential catalog ref is required");
  assertCredentialRef(input.ref);
  if (typeof input.provider !== "string" || !input.provider) {
    throw new Error(`credential ${input.ref} requires provider metadata`);
  }
  if (typeof input.kind !== "string" || !credentialKinds.includes(input.kind)) {
    throw new Error(`credential ${input.ref} has unsupported kind`);
  }
  if (typeof input.backend !== "string" || !input.backend) {
    throw new Error(`credential ${input.ref} requires backend metadata`);
  }
  if (!Array.isArray(input.scopes) || input.scopes.some((scope) => typeof scope !== "string")) {
    throw new Error(`credential ${input.ref} scopes must be strings`);
  }
  for (const optional of ["label", "accountId", "expiresAt", "testedAt", "revokedAt"]) {
    if (input[optional] !== void 0 && typeof input[optional] !== "string") {
      throw new Error(`credential ${input.ref} ${optional} must be a string`);
    }
  }
  if (input.testStatus !== void 0 && input.testStatus !== "passed" && input.testStatus !== "failed") {
    throw new Error(`credential ${input.ref} testStatus must be passed or failed`);
  }
  if (input.testedAt === void 0 !== (input.testStatus === void 0)) {
    throw new Error(`credential ${input.ref} requires testedAt and testStatus together`);
  }
  for (const timestamp2 of ["testedAt", "revokedAt"]) {
    if (typeof input[timestamp2] === "string" && Number.isNaN(Date.parse(input[timestamp2]))) {
      throw new Error(`credential ${input.ref} ${timestamp2} must be an ISO timestamp`);
    }
  }
  return {
    ref: input.ref,
    provider: input.provider,
    kind: input.kind,
    backend: input.backend,
    label: input.label,
    scopes: [...input.scopes],
    accountId: input.accountId,
    expiresAt: input.expiresAt,
    testedAt: input.testedAt,
    testStatus: input.testStatus,
    revokedAt: input.revokedAt
  };
}
function defaultCredentialCatalogPath(options = {}) {
  const configRoot = options.xdgConfigHome ? resolve3(options.xdgConfigHome) : join2(resolve3(options.homeDirectory ?? homedir()), ".config");
  return join2(configRoot, "venture-harness", "credentials.json");
}
function loadCredentialCatalog(path = defaultCredentialCatalogPath()) {
  const absolute = resolve3(path);
  if (!existsSync2(absolute)) return { schemaVersion: 1, references: [] };
  const value = JSON.parse(readFileSync3(absolute, "utf8"));
  if (value.schemaVersion !== 1 || !Array.isArray(value.references)) {
    throw new Error("unsupported credential catalog; expected schemaVersion 1 and references[]");
  }
  const references = value.references.map(parseReference);
  if (new Set(references.map((reference) => reference.ref)).size !== references.length) {
    throw new Error("credential catalog contains duplicate refs");
  }
  return { schemaVersion: 1, references };
}
function saveCredentialCatalog(catalog, path = defaultCredentialCatalogPath()) {
  const references = catalog.references.map(parseReference).sort((a, b) => a.ref.localeCompare(b.ref));
  if (new Set(references.map((reference) => reference.ref)).size !== references.length) {
    throw new Error("credential catalog contains duplicate refs");
  }
  const absolute = resolve3(path);
  mkdirSync2(dirname(absolute), { recursive: true, mode: 448 });
  const temporary = `${absolute}.next-${process.pid}-${Date.now()}`;
  writeFileSync2(temporary, `${JSON.stringify({ schemaVersion: 1, references }, null, 2)}
`, {
    encoding: "utf8",
    mode: 384
  });
  renameSync2(temporary, absolute);
}
function upsertCredentialReference(catalog, reference) {
  const parsed = parseReference(reference);
  return {
    schemaVersion: 1,
    references: [...catalog.references.filter((candidate) => candidate.ref !== parsed.ref), parsed]
  };
}
function removeCredentialReferences(catalog, refs) {
  const removed = new Set(refs);
  return {
    schemaVersion: 1,
    references: catalog.references.filter((reference) => !removed.has(reference.ref))
  };
}

// lib/credentials/cli-doctor.ts
var CLI_PREREQUISITES = [
  { id: "github", binary: "gh", args: ["--version"], purpose: "GitHub provider operations" },
  {
    id: "vercel",
    binary: "vercel",
    args: ["--version"],
    purpose: "Vercel project and deployment operations"
  },
  {
    id: "stripe",
    binary: "stripe",
    args: ["version"],
    purpose: "Stripe authentication and local webhook checks"
  },
  {
    id: "neon",
    binary: "neonctl",
    args: ["--version"],
    purpose: "Neon control-plane operations"
  },
  {
    id: "postgres",
    binary: "psql",
    args: ["--version"],
    purpose: "Neon schema migration and read/write verification"
  },
  { id: "eas", binary: "eas", args: ["--version"], purpose: "Expo/EAS build and submit" },
  {
    id: "xcode",
    binary: "xcodebuild",
    args: ["-version"],
    purpose: "SwiftUI and local iOS validation"
  },
  {
    id: "codex",
    binary: "codex",
    args: ["--version"],
    purpose: "Optional noninteractive model/build host"
  }
];
function compactVersion(value, redactor) {
  const line = redactor.redactText(value).split(/\r?\n/).find((item) => item.trim().length > 0);
  return line ? line.trim().slice(0, 200) : null;
}
async function inspectCliPrerequisites(runner, options = {}) {
  const redactor = options.redactor ?? new Redactor();
  return Promise.all(
    (options.prerequisites ?? CLI_PREREQUISITES).map(async (prerequisite) => {
      try {
        const result2 = await runner.run({
          command: prerequisite.binary,
          args: prerequisite.args
        });
        if (result2.exitCode === 0) {
          return {
            id: prerequisite.id,
            binary: prerequisite.binary,
            purpose: prerequisite.purpose,
            status: "installed",
            version: compactVersion(result2.stdout || result2.stderr, redactor),
            nextAction: null
          };
        }
        return {
          id: prerequisite.id,
          binary: prerequisite.binary,
          purpose: prerequisite.purpose,
          status: "unavailable",
          version: null,
          nextAction: `${prerequisite.binary} exists but its version check exited ${result2.exitCode}; repair or reinstall it.`
        };
      } catch (error) {
        const missing = error.code === "ENOENT";
        return {
          id: prerequisite.id,
          binary: prerequisite.binary,
          purpose: prerequisite.purpose,
          status: missing ? "missing" : "unavailable",
          version: null,
          nextAction: missing ? `Install ${prerequisite.binary} before using ${prerequisite.purpose}.` : `${prerequisite.binary} could not be inspected: ${redactor.redactText(
            error instanceof Error ? error.message : String(error)
          )}`
        };
      }
    })
  );
}

// lib/credentials/cli-session.ts
import { spawn } from "node:child_process";
var CLI_AUTH_COMMANDS = {
  github: {
    login: { command: "gh", args: ["auth", "login"] },
    status: { command: "gh", args: ["auth", "status"] },
    logout: { command: "gh", args: ["auth", "logout", "--hostname", "github.com"] }
  },
  vercel: {
    login: { command: "vercel", args: ["login"] },
    status: { command: "vercel", args: ["whoami"] },
    logout: { command: "vercel", args: ["logout"] }
  },
  stripe: {
    login: { command: "stripe", args: ["login"] },
    status: { command: "stripe", args: ["balance", "retrieve"] },
    logout: { command: "stripe", args: ["logout"] }
  },
  eas: {
    login: { command: "eas", args: ["login"] },
    status: { command: "eas", args: ["whoami"] },
    logout: { command: "eas", args: ["logout"] }
  }
};
function supportsInteractiveCliAuth(provider) {
  return Object.hasOwn(CLI_AUTH_COMMANDS, provider);
}
function runInteractiveCliLogin(provider) {
  const spec = CLI_AUTH_COMMANDS[provider]?.login;
  if (!spec) throw new Error(`No official interactive CLI login is registered for ${provider}.`);
  return new Promise((resolve27, reject) => {
    const child = spawn(spec.command, spec.args, { shell: false, stdio: "inherit" });
    child.once("error", reject);
    child.once("close", (code) => {
      if (code === 0) resolve27();
      else reject(new Error(`${provider} login exited with ${code ?? "unknown"}`));
    });
  });
}
var CliSessionCredentialBackend = class {
  constructor(runner) {
    this.runner = runner;
  }
  runner;
  id = "cli_session";
  writable = false;
  async get(reference) {
    void reference;
    return null;
  }
  async set() {
    throw new Error("CLI sessions are created only through the provider's interactive login.");
  }
  async delete(reference) {
    const spec = CLI_AUTH_COMMANDS[reference.provider]?.logout;
    if (!spec) return false;
    const result2 = await this.runner.run({ command: spec.command, args: spec.args });
    return result2.exitCode === 0;
  }
  async inspect(reference) {
    const spec = CLI_AUTH_COMMANDS[reference.provider]?.status;
    if (!spec) {
      return {
        status: "unavailable",
        writable: false,
        message: `No CLI session status command is registered for ${reference.provider}`
      };
    }
    try {
      const result2 = await this.runner.run({ command: spec.command, args: spec.args });
      return {
        status: result2.exitCode === 0 ? "available" : "missing",
        writable: false,
        message: result2.exitCode === 0 ? `${reference.provider} CLI session passed an official read check` : `${reference.provider} CLI session check exited with ${result2.exitCode}`
      };
    } catch (error) {
      const missingBinary = error.code === "ENOENT";
      return {
        status: missingBinary ? "unavailable" : "missing",
        writable: false,
        message: missingBinary ? `${CLI_AUTH_COMMANDS[reference.provider]?.status.command ?? reference.provider} is not installed` : `CLI session check failed: ${error instanceof Error ? error.message : String(error)}`
      };
    }
  }
};

// lib/credentials/node-runner.ts
import { spawn as spawn2 } from "node:child_process";
var SHELL_BINARIES = /* @__PURE__ */ new Set([
  "sh",
  "bash",
  "zsh",
  "fish",
  "cmd",
  "cmd.exe",
  "powershell",
  "powershell.exe",
  "pwsh",
  "pwsh.exe"
]);
function executableName(command) {
  return command.replaceAll("\\", "/").split("/").at(-1)?.toLowerCase() ?? "";
}
function assertDirectCommand(command) {
  const trimmed = command.trim();
  if (trimmed.length === 0 || trimmed !== command || /\s/.test(trimmed)) {
    throw new Error(
      "Commands must name one executable directly; shell command strings are forbidden"
    );
  }
  if (SHELL_BINARIES.has(executableName(trimmed))) {
    throw new Error(`Shell executables are forbidden: ${executableName(trimmed)}`);
  }
}
var NodeCommandRunner = class {
  env;
  maxOutputBytes;
  constructor(options = {}) {
    this.env = options.env ?? process.env;
    this.maxOutputBytes = options.maxOutputBytes ?? 2 * 1024 * 1024;
  }
  async run(invocation2) {
    assertDirectCommand(invocation2.command);
    const environment = Object.fromEntries(
      Object.entries({ ...this.env, ...invocation2.env }).filter(
        (entry) => entry[1] !== void 0
      )
    );
    return new Promise((resolve27, reject) => {
      const child = spawn2(invocation2.command, [...invocation2.args], {
        cwd: invocation2.cwd,
        env: environment,
        shell: false,
        stdio: ["pipe", "pipe", "pipe"]
      });
      let stdout = "";
      let stderr = "";
      let outputBytes = 0;
      let exceededLimit = false;
      const abort = () => child.kill("SIGTERM");
      const collect = (stream, chunk) => {
        if (exceededLimit) return;
        outputBytes += chunk.byteLength;
        if (outputBytes > this.maxOutputBytes) {
          exceededLimit = true;
          child.kill("SIGTERM");
          return;
        }
        if (stream === "stdout") stdout += chunk.toString("utf8");
        else stderr += chunk.toString("utf8");
      };
      child.stdout.on("data", (chunk) => collect("stdout", chunk));
      child.stderr.on("data", (chunk) => collect("stderr", chunk));
      child.once("error", reject);
      child.once("close", (code, signal) => {
        invocation2.signal?.removeEventListener("abort", abort);
        if (exceededLimit) {
          reject(new Error(`Command output exceeded ${this.maxOutputBytes} bytes`));
          return;
        }
        resolve27({
          exitCode: code ?? (signal ? 1 : 0),
          stdout,
          stderr
        });
      });
      if (invocation2.signal?.aborted) abort();
      else invocation2.signal?.addEventListener("abort", abort, { once: true });
      if (invocation2.stdin !== void 0) child.stdin.end(invocation2.stdin);
      else child.stdin.end();
    });
  }
};

// lib/data/connectors.ts
var UnavailableDataConnector = class {
  constructor(id, source, reason, nextAction2) {
    this.id = id;
    this.source = source;
    this.reason = reason;
    this.nextAction = nextAction2;
  }
  id;
  source;
  reason;
  nextAction;
  rawExportsCommitted = false;
  transport = "provider_api";
  credentialRequired = false;
  async fetch() {
    throw new DataConnectorNotConfiguredError(this.reason, this.source, this.nextAction);
  }
};

// lib/data/neon-commercial-connector.ts
var aggregateRowSchema = external_exports.object({
  record_type: external_exports.enum(["commercial", "product"]),
  metric_id: external_exports.string().min(1).max(200),
  event_count: external_exports.number().int().nonnegative(),
  sample_size: external_exports.number().int().nonnegative(),
  qualified_count: external_exports.number().int().nonnegative(),
  price_contexts: external_exports.array(
    external_exports.object({
      plan_key: external_exports.string().max(200).nullable(),
      displayed_price: external_exports.string().min(1).max(200),
      billing_period: external_exports.string().max(100).nullable()
    }).strict()
  ),
  release_versions: external_exports.array(external_exports.string().min(1).max(200))
}).strict();
var NEON_COMMERCIAL_EVIDENCE_SQL = String.raw`
with bounds as (
  select
    (:'window_start')::timestamptz as window_start,
    (:'window_end')::timestamptz as window_end
),
commercial as (
  select
    'commercial'::text as record_type,
    event as metric_id,
    count(*)::bigint as event_count,
    count(distinct visitor_id)::bigint as sample_size,
    count(*) filter (where qualified is true)::bigint as qualified_count,
    coalesce(
      jsonb_agg(
        distinct jsonb_build_object(
          'plan_key', plan_key,
          'displayed_price', displayed_price,
          'billing_period', billing_period
        )
      ) filter (where displayed_price is not null),
      '[]'::jsonb
    ) as price_contexts,
    coalesce(
      jsonb_agg(distinct release_version) filter (where release_version is not null),
      '[]'::jsonb
    ) as release_versions
  from commercial_events, bounds
  where occurred_at >= bounds.window_start and occurred_at < bounds.window_end
  group by event
),
product as (
  select
    'product'::text as record_type,
    event as metric_id,
    count(*)::bigint as event_count,
    count(distinct visitor_id)::bigint as sample_size,
    0::bigint as qualified_count,
    '[]'::jsonb as price_contexts,
    coalesce(
      jsonb_agg(distinct release_version) filter (where release_version is not null),
      '[]'::jsonb
    ) as release_versions
  from product_events, bounds
  where occurred_at >= bounds.window_start and occurred_at < bounds.window_end
  group by event
)
select json_build_object(
  'record_type', record_type,
  'metric_id', metric_id,
  'event_count', event_count,
  'sample_size', sample_size,
  'qualified_count', qualified_count,
  'price_contexts', price_contexts,
  'release_versions', release_versions
)::text
from (
  select * from commercial
  union all
  select * from product
) evidence
order by record_type, metric_id;
`.trim();
function decoded(value, label) {
  try {
    return decodeURIComponent(value);
  } catch {
    throw new Error(`Neon connection string has invalid ${label} encoding.`);
  }
}
function postgresEnvironment(secret) {
  let url;
  try {
    url = new URL(secret);
  } catch {
    throw new Error("Neon credential must be a postgres:// or postgresql:// connection string.");
  }
  if (!["postgres:", "postgresql:"].includes(url.protocol) || !url.hostname) {
    throw new Error("Neon credential must be a postgres:// or postgresql:// connection string.");
  }
  const database = decoded(url.pathname.replace(/^\//, ""), "database");
  if (!database) throw new Error("Neon connection string must name a database.");
  const environment = {
    DATABASE_URL: void 0,
    PGSERVICE: void 0,
    PGPASSFILE: void 0,
    PGHOST: url.hostname,
    PGPORT: url.port || "5432",
    PGDATABASE: database,
    PGUSER: decoded(url.username, "username"),
    PGPASSWORD: decoded(url.password, "password"),
    PGAPPNAME: "venture-harness-data-sync",
    PGOPTIONS: ""
  };
  const sslMode = url.searchParams.get("sslmode");
  const channelBinding = url.searchParams.get("channel_binding");
  const connectTimeout = url.searchParams.get("connect_timeout");
  if (sslMode) environment.PGSSLMODE = sslMode;
  if (channelBinding) environment.PGCHANNELBINDING = channelBinding;
  if (connectTimeout) environment.PGCONNECT_TIMEOUT = connectTimeout;
  return environment;
}
function parseRows(stdout) {
  return stdout.split(/\r?\n/).filter((line) => line.trim().length > 0).map((line, index) => {
    let parsed;
    try {
      parsed = JSON.parse(line);
    } catch {
      throw new DataNormalizationError(
        `Neon aggregate row ${index + 1} is not valid JSON.`,
        "neon_commercial_evidence"
      );
    }
    const result2 = aggregateRowSchema.safeParse(parsed);
    if (!result2.success) {
      throw new DataNormalizationError(
        `Neon aggregate row ${index + 1} violates the allowlisted evidence shape: ${result2.error.issues.map((issue) => `${issue.path.join(".") || "row"}: ${issue.message}`).join("; ")}`,
        "neon_commercial_evidence"
      );
    }
    return result2.data;
  });
}
function reportingWindow(now, windowHours) {
  return {
    start: new Date(now.getTime() - windowHours * 36e5).toISOString(),
    end: now.toISOString()
  };
}
function createNeonCommercialEvidenceConnector(options) {
  const binary = options.binary ?? "psql";
  assertDirectCommand(binary);
  const windowHours = options.windowHours ?? 24 * 7;
  if (!Number.isFinite(windowHours) || windowHours <= 0) {
    throw new Error("Neon evidence windowHours must be a positive finite number.");
  }
  return {
    id: "neon-commercial-evidence",
    source: "neon_commercial_evidence",
    transport: "database",
    credentialRequired: true,
    rawExportsCommitted: false,
    async fetch(context2) {
      if (!context2.credentialRef) throw new Error("credential_ref required for Neon evidence");
      const window2 = reportingWindow(context2.now, windowHours);
      const result2 = await options.broker.withSecret(context2.credentialRef, async (secret) => {
        const env = postgresEnvironment(secret);
        for (const key of ["PGHOST", "PGDATABASE", "PGUSER", "PGPASSWORD"]) {
          const value = env[key];
          if (value) options.broker.redactor.addSecret(value);
        }
        const invocation2 = {
          command: binary,
          args: [
            "--no-psqlrc",
            "--no-align",
            "--tuples-only",
            "--quiet",
            "--set=ON_ERROR_STOP=1",
            `--set=window_start=${window2.start}`,
            `--set=window_end=${window2.end}`,
            "--command",
            NEON_COMMERCIAL_EVIDENCE_SQL
          ],
          env,
          sensitiveEnv: Object.keys(env),
          signal: context2.signal
        };
        return options.runner.run(invocation2);
      });
      if (result2.exitCode !== 0) {
        const detail = options.broker.redactor.redactText(result2.stderr || result2.stdout).trim().slice(0, 2e3);
        throw new Error(
          `Neon aggregate query exited ${result2.exitCode}${detail ? `: ${detail}` : ""}`
        );
      }
      return {
        source: "neon_commercial_evidence",
        sourceAccount: options.sourceAccount,
        fetchedAt: context2.now.toISOString(),
        reportingWindow: window2,
        timezone: options.timezone,
        dimensions: ["record_type", "metric_id", "price_contexts", "release_versions"],
        quality: "complete",
        limitations: [
          "Aggregate counts only; no visitor identifiers, submissions, attribution payloads, or free-form content were selected.",
          "Observed counts and release groupings are descriptive and do not establish causation."
        ],
        releaseVersion: options.releaseVersion ?? null,
        rows: parseRows(result2.stdout)
      };
    }
  };
}

// lib/data/official-http-connectors.ts
var TRANSIENT_HTTP_STATUS = /* @__PURE__ */ new Set([408, 425, 429, 500, 502, 503, 504]);
function providerWindow(now, windowHours) {
  if (!Number.isFinite(windowHours) || windowHours <= 0) {
    throw new Error("Direct-data windowHours must be a positive number.");
  }
  const start = new Date(now.getTime() - windowHours * 36e5);
  return {
    start,
    end: now,
    startIso: start.toISOString(),
    endIso: now.toISOString(),
    startDate: start.toISOString().slice(0, 10),
    endDate: now.toISOString().slice(0, 10)
  };
}
function object(value, source, label) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    throw new DataNormalizationError(`${source} returned an invalid ${label} object.`, source);
  }
  return value;
}
function optionalObject(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : null;
}
function array(value, source, label) {
  if (value === void 0) return [];
  if (!Array.isArray(value)) {
    throw new DataNormalizationError(`${source} returned an invalid ${label} array.`, source);
  }
  return value;
}
function text(value, source, label) {
  if (typeof value !== "string" || value.length === 0) {
    throw new DataNormalizationError(`${source} returned an invalid ${label}.`, source);
  }
  return value;
}
function finite(value, source, label) {
  const parsed = typeof value === "string" && value.trim() !== "" ? Number(value) : value;
  if (typeof parsed !== "number" || !Number.isFinite(parsed)) {
    throw new DataNormalizationError(`${source} returned a non-numeric ${label}.`, source);
  }
  return parsed;
}
function optionalFinite(value, source, label) {
  return value === void 0 || value === null ? null : finite(value, source, label);
}
function bearerRequest(method, url, secret, body, signal) {
  return {
    method,
    url,
    headers: {
      Authorization: `Bearer ${secret}`,
      ...body === void 0 ? {} : { "Content-Type": "application/json" }
    },
    ...body === void 0 ? {} : { body: JSON.stringify(body) },
    sensitiveHeaders: ["authorization"],
    sensitiveUrl: false,
    signal
  };
}
function assertCredentialProvider(reference, provider, source) {
  if (reference.provider !== provider) {
    throw new DataProviderFetchError(
      `${source} credential_ref belongs to ${reference.provider}, not ${provider}.`,
      source,
      false,
      `Register a least-privilege ${provider} read credential and set its cred:// reference for ${source}.`
    );
  }
}
function createProviderHttpConnector(options) {
  return {
    id: options.id,
    source: options.source,
    transport: "provider_api",
    credentialRequired: true,
    rawExportsCommitted: false,
    async fetch(context2) {
      if (!context2.credentialRef) {
        throw new DataProviderFetchError(
          `credential_ref required for ${options.source}`,
          options.source,
          false,
          `Register a least-privilege ${options.provider} read credential and rerun vh data sync.`
        );
      }
      const window2 = providerWindow(context2.now, options.windowHours);
      return options.broker.withSecret(context2.credentialRef, async (secret, reference) => {
        assertCredentialProvider(reference, options.provider, options.source);
        let response;
        try {
          response = await options.fetcher.fetch(options.request(secret, context2, window2));
        } catch (error) {
          const safe = options.broker.redactor.redactText(
            error instanceof Error ? error.message : String(error)
          );
          throw new DataProviderFetchError(
            `${options.source} official read failed: ${safe}`,
            options.source,
            true,
            `Check ${options.provider} status and the brokered read credential, then rerun vh data sync.`
          );
        }
        if (response.status < 200 || response.status >= 300) {
          const retryable = TRANSIENT_HTTP_STATUS.has(response.status);
          throw new DataProviderFetchError(
            `${options.source} official read returned HTTP ${response.status}; provider response content was not persisted.`,
            options.source,
            retryable,
            retryable ? `Honor Retry-After or provider status, then rerun vh data sync for ${options.source}.` : `Run vh doctor, verify the ${options.provider} credential scope and resource identifiers, then rerun vh data sync.`
          );
        }
        return options.map(response, context2, window2);
      });
    }
  };
}
function baseDataset(options, context2, window2) {
  return {
    sourceAccount: options.sourceAccount,
    fetchedAt: context2.now.toISOString(),
    reportingWindow: { start: window2.startIso, end: window2.endIso },
    timezone: options.timezone,
    releaseVersion: options.releaseVersion ?? null
  };
}
function createGoogleSearchConsoleConnector(options) {
  const rowLimit = 25e3;
  return createProviderHttpConnector({
    ...options,
    id: "gsc.search-analytics",
    provider: "google",
    source: "gsc",
    request: (secret, context2, window2) => bearerRequest(
      "POST",
      `https://searchconsole.googleapis.com/webmasters/v3/sites/${encodeURIComponent(options.siteUrl)}/searchAnalytics/query`,
      secret,
      {
        startDate: window2.startDate,
        endDate: window2.endDate,
        dimensions: ["date", "country", "device"],
        rowLimit,
        dataState: "final"
      },
      context2.signal
    ),
    map(response, context2, window2) {
      const body = object(response.body, "gsc", "response");
      const providerRows = array(body.rows, "gsc", "rows");
      const rows = providerRows.map((value, index) => {
        const row = object(value, "gsc", `rows[${index}]`);
        const keys = array(row.keys, "gsc", `rows[${index}].keys`);
        if (keys.length !== 3) {
          throw new DataNormalizationError(
            `gsc row ${index} did not match the requested aggregate dimensions.`,
            "gsc"
          );
        }
        return {
          date: text(keys[0], "gsc", `rows[${index}].date`),
          country: text(keys[1], "gsc", `rows[${index}].country`),
          device: text(keys[2], "gsc", `rows[${index}].device`),
          clicks: finite(row.clicks, "gsc", `rows[${index}].clicks`),
          impressions: finite(row.impressions, "gsc", `rows[${index}].impressions`),
          ctr: finite(row.ctr, "gsc", `rows[${index}].ctr`),
          position: finite(row.position, "gsc", `rows[${index}].position`)
        };
      });
      const truncated = rows.length >= rowLimit;
      return {
        source: "gsc",
        ...baseDataset(options, context2, window2),
        dimensions: ["date", "country", "device"],
        quality: truncated ? "partial" : "complete",
        limitations: [
          "Search Console returns aggregate, privacy-filtered data and can omit low-volume rows.",
          "Raw query text and page URLs are deliberately excluded from this learning dataset.",
          ...truncated ? [`The ${rowLimit}-row response may be truncated; pagination is not inferred.`] : []
        ],
        rows
      };
    }
  });
}
function ga4Date(value) {
  return /^\d{8}$/.test(value) ? `${value.slice(0, 4)}-${value.slice(4, 6)}-${value.slice(6, 8)}` : value;
}
function createGoogleAnalyticsConnector(options) {
  const propertyId = options.propertyId.replace(/^properties\//, "");
  if (!/^\d+$/.test(propertyId)) {
    throw new Error("GA4 propertyId must be the numeric property identifier.");
  }
  return createProviderHttpConnector({
    ...options,
    id: "ga4.run-report",
    provider: "google",
    source: "ga4",
    request: (secret, context2, window2) => bearerRequest(
      "POST",
      `https://analyticsdata.googleapis.com/v1beta/properties/${propertyId}:runReport`,
      secret,
      {
        dateRanges: [{ startDate: window2.startDate, endDate: window2.endDate }],
        dimensions: [
          { name: "date" },
          { name: "sessionDefaultChannelGroup" },
          { name: "deviceCategory" }
        ],
        metrics: [{ name: "sessions" }, { name: "totalUsers" }, { name: "eventCount" }],
        keepEmptyRows: false,
        limit: "100000"
      },
      context2.signal
    ),
    map(response, context2, window2) {
      const body = object(response.body, "ga4", "response");
      const providerRows = array(body.rows, "ga4", "rows");
      const rows = providerRows.map((value, index) => {
        const row = object(value, "ga4", `rows[${index}]`);
        const dimensions = array(row.dimensionValues, "ga4", `rows[${index}].dimensionValues`);
        const metrics = array(row.metricValues, "ga4", `rows[${index}].metricValues`);
        if (dimensions.length !== 3 || metrics.length !== 3) {
          throw new DataNormalizationError(
            `ga4 row ${index} did not match the requested aggregate dimensions and metrics.`,
            "ga4"
          );
        }
        const dimension = (position) => text(
          object(dimensions[position], "ga4", `rows[${index}].dimensionValues[${position}]`).value,
          "ga4",
          `rows[${index}].dimensionValues[${position}].value`
        );
        const metric = (position) => finite(
          object(metrics[position], "ga4", `rows[${index}].metricValues[${position}]`).value,
          "ga4",
          `rows[${index}].metricValues[${position}].value`
        );
        return {
          date: ga4Date(dimension(0)),
          channel_group: dimension(1),
          device_category: dimension(2),
          sessions: metric(0),
          total_users: metric(1),
          event_count: metric(2)
        };
      });
      const metadata = optionalObject(body.metadata);
      const sampling = Array.isArray(metadata?.samplingMetadatas) ? metadata.samplingMetadatas.length > 0 : false;
      const thresholded = metadata?.subjectToThresholding === true;
      const responseTimezone = typeof metadata?.timeZone === "string" && metadata.timeZone.length > 0 ? metadata.timeZone : options.timezone;
      return {
        source: "ga4",
        ...baseDataset({ ...options, timezone: responseTimezone }, context2, window2),
        dimensions: ["date", "channel_group", "device_category"],
        quality: thresholded ? "thresholded" : sampling ? "sampled" : "complete",
        limitations: [
          "GA4 contains only the consented population configured for the property.",
          "Provider thresholding, sampling, data retention, and current-day lag can limit comparisons.",
          ...sampling ? ["GA4 response metadata reported sampling."] : [],
          ...thresholded ? ["GA4 response metadata reported thresholding."] : []
        ],
        rows
      };
    }
  });
}
function bingDate(value) {
  if (typeof value === "string") {
    const dotNet = value.match(/^\/Date\((\d+)(?:[+-]\d+)?\)\/$/);
    if (dotNet) return new Date(Number(dotNet[1])).toISOString();
    if (Number.isFinite(Date.parse(value))) return new Date(value).toISOString();
  }
  throw new DataNormalizationError("bing_webmaster returned an invalid Date.", "bing_webmaster");
}
function createBingWebmasterConnector(options) {
  return createProviderHttpConnector({
    ...options,
    id: "bing.rank-and-traffic",
    provider: "bing",
    source: "bing_webmaster",
    request(secret, context2) {
      const url = new URL("https://ssl.bing.com/webmaster/api.svc/json/GetRankAndTrafficStats");
      url.searchParams.set("siteUrl", options.siteUrl);
      const headers = {};
      const sensitiveHeaders = [];
      if (options.authMode === "oauth") {
        headers.Authorization = `Bearer ${secret}`;
        sensitiveHeaders.push("authorization");
      } else {
        url.searchParams.set("apikey", secret);
      }
      return {
        method: "GET",
        url: url.toString(),
        headers,
        sensitiveHeaders,
        sensitiveUrl: options.authMode === "api_key",
        signal: context2.signal
      };
    },
    map(response, context2, window2) {
      const body = object(response.body, "bing_webmaster", "response");
      const providerRows = array(body.d, "bing_webmaster", "d");
      const rows = providerRows.map((value, index) => {
        const row = object(value, "bing_webmaster", `d[${index}]`);
        return {
          date: bingDate(row.Date),
          clicks: finite(row.Clicks, "bing_webmaster", `d[${index}].Clicks`),
          impressions: finite(row.Impressions, "bing_webmaster", `d[${index}].Impressions`)
        };
      });
      return {
        source: "bing_webmaster",
        ...baseDataset(options, context2, window2),
        dimensions: ["date"],
        quality: "partial",
        limitations: [
          "The legacy GetRankAndTrafficStats method chooses its own coverage; the configured reporting window records the sync observation window, not a server-side filter.",
          "Bing Webmaster API availability is legacy and must be live-doctored per account.",
          "This aggregate connector does not fetch query or page text."
        ],
        rows
      };
    }
  });
}
function createStripeBalanceConnector(options) {
  const limit = 100;
  return createProviderHttpConnector({
    ...options,
    id: "stripe.balance-transactions",
    provider: "stripe",
    source: "stripe",
    request(secret, context2, window2) {
      const basic = Buffer.from(`${secret}:`).toString("base64");
      options.broker.redactor.addSecret(basic);
      const url = new URL("https://api.stripe.com/v1/balance_transactions");
      url.searchParams.set("created[gte]", String(Math.floor(window2.start.getTime() / 1e3)));
      url.searchParams.set("created[lte]", String(Math.floor(window2.end.getTime() / 1e3)));
      url.searchParams.set("limit", String(limit));
      return {
        method: "GET",
        url: url.toString(),
        headers: { Authorization: `Basic ${basic}` },
        sensitiveHeaders: ["authorization"],
        sensitiveUrl: false,
        signal: context2.signal
      };
    },
    map(response, context2, window2) {
      const body = object(response.body, "stripe", "response");
      const providerRows = array(body.data, "stripe", "data");
      const rows = providerRows.map((value, index) => {
        const row = object(value, "stripe", `data[${index}]`);
        return {
          created_at: new Date(
            finite(row.created, "stripe", `data[${index}].created`) * 1e3
          ).toISOString(),
          available_at: optionalFinite(row.available_on, "stripe", `data[${index}].available_on`) === null ? null : new Date(Number(row.available_on) * 1e3).toISOString(),
          currency: text(row.currency, "stripe", `data[${index}].currency`),
          gross_minor: finite(row.amount, "stripe", `data[${index}].amount`),
          fee_minor: finite(row.fee, "stripe", `data[${index}].fee`),
          net_minor: finite(row.net, "stripe", `data[${index}].net`),
          type: text(row.type, "stripe", `data[${index}].type`),
          reporting_category: typeof row.reporting_category === "string" ? row.reporting_category : null,
          status: typeof row.status === "string" ? row.status : null,
          mode: options.mode
        };
      });
      const truncated = body.has_more === true;
      return {
        source: "stripe",
        ...baseDataset(options, context2, window2),
        dimensions: ["created_at", "currency", "type", "reporting_category", "mode"],
        quality: truncated ? "partial" : "complete",
        limitations: [
          `Stripe ${options.mode} mode is isolated from the other mode; values are integer minor currency units.`,
          "Customer, source, description, and payment payload fields are deliberately discarded before normalization.",
          ...truncated ? [
            `Stripe reported has_more after ${limit} balance transactions; this sync is partial.`
          ] : []
        ],
        rows
      };
    }
  });
}
function createBrevoAggregateConnector(options) {
  return createProviderHttpConnector({
    ...options,
    id: "brevo.transactional-aggregate",
    provider: "brevo",
    source: "brevo",
    request(secret, context2, window2) {
      const url = new URL("https://api.brevo.com/v3/smtp/statistics/aggregatedReport");
      url.searchParams.set("startDate", window2.startDate);
      url.searchParams.set("endDate", window2.endDate);
      return {
        method: "GET",
        url: url.toString(),
        headers: { "api-key": secret },
        sensitiveHeaders: ["api-key"],
        sensitiveUrl: false,
        signal: context2.signal
      };
    },
    map(response, context2, window2) {
      const body = object(response.body, "brevo", "response");
      const safeMetric = (key) => optionalFinite(body[key], "brevo", key);
      return {
        source: "brevo",
        ...baseDataset(options, context2, window2),
        dimensions: ["reporting_window"],
        quality: "complete",
        limitations: [
          "Brevo transactional delivery aggregates do not prove inbox placement or human reading.",
          "Recipient addresses, message bodies, event payloads, and campaign content are never requested.",
          "Date boundaries follow the configured venture timezone and must be compared with the Brevo account setting."
        ],
        rows: [
          {
            requests: safeMetric("requests"),
            delivered: safeMetric("delivered"),
            hard_bounces: safeMetric("hardBounces"),
            soft_bounces: safeMetric("softBounces"),
            blocked: safeMetric("blocked"),
            invalid: safeMetric("invalid"),
            spam_reports: safeMetric("spamReports"),
            unsubscribed: safeMetric("unsubscribed"),
            opens: safeMetric("opens"),
            unique_opens: safeMetric("uniqueOpens"),
            clicks: safeMetric("clicks"),
            unique_clicks: safeMetric("uniqueClicks")
          }
        ]
      };
    }
  });
}
function createRevenueCatMetricsConnector(options) {
  return createProviderHttpConnector({
    ...options,
    id: "revenuecat.overview-metrics",
    provider: "revenuecat",
    source: "revenuecat",
    request(secret, context2) {
      const url = new URL(
        `https://api.revenuecat.com/v2/projects/${encodeURIComponent(options.projectId)}/metrics/overview`
      );
      if (options.currency) url.searchParams.set("currency", options.currency.toUpperCase());
      return bearerRequest("GET", url.toString(), secret, void 0, context2.signal);
    },
    map(response, context2, window2) {
      const body = object(response.body, "revenuecat", "response");
      const providerRows = array(body.metrics, "revenuecat", "metrics");
      const currency = typeof body.currency === "string" ? body.currency : options.currency ?? null;
      const rows = providerRows.map((value, index) => {
        const row = object(value, "revenuecat", `metrics[${index}]`);
        return {
          metric_id: text(row.id, "revenuecat", `metrics[${index}].id`),
          unit: typeof row.unit === "string" ? row.unit : null,
          period: typeof row.period === "string" ? row.period : null,
          value: finite(row.value, "revenuecat", `metrics[${index}].value`),
          currency,
          last_updated_at: typeof row.last_updated_at_iso8601 === "string" ? row.last_updated_at_iso8601 : optionalFinite(
            row.last_updated_at,
            "revenuecat",
            `metrics[${index}].last_updated_at`
          ) === null ? null : new Date(Number(row.last_updated_at)).toISOString()
        };
      });
      return {
        source: "revenuecat",
        ...baseDataset(options, context2, window2),
        dimensions: ["metric_id", "period", "currency"],
        quality: "partial",
        limitations: [
          "RevenueCat overview metrics are point-in-time project summaries; each metric period and last_updated_at must be interpreted separately from the requested observation window.",
          "Customer and purchase-level APIs are deliberately not called, so no app-user identifiers enter the normalized dataset.",
          "Some overview metrics may be cached or delayed by the provider."
        ],
        rows
      };
    }
  });
}

// lib/data/release-log-connector.ts
import { existsSync as existsSync3, readFileSync as readFileSync4 } from "node:fs";
import { relative, resolve as resolve4, sep } from "node:path";
var releaseLogRowSchema = external_exports.object({
  release_id: external_exports.string().regex(/^[A-Za-z0-9][A-Za-z0-9._-]*$/),
  released_at: external_exports.string().datetime({ offset: true }),
  release_version: external_exports.string().min(1).max(200),
  environment: external_exports.enum(["local", "test", "preview", "production"]),
  status: external_exports.enum(["succeeded", "failed", "rolled_back"]),
  change_kind: external_exports.enum(["code", "config", "content", "metadata", "migration"]),
  journey_id: external_exports.string().regex(/^[A-Za-z0-9][A-Za-z0-9._-]*$/).nullable(),
  incident_count: external_exports.number().int().nonnegative().default(0)
}).strict();
function inside(root, candidate) {
  const absolute = resolve4(root, candidate);
  const rel = relative(root, absolute);
  if (rel === "" || !rel.startsWith(`..${sep}`) && rel !== ".." && !rel.startsWith(sep)) {
    return absolute;
  }
  throw new Error(`Release log path escapes the venture root: ${candidate}`);
}
function readRows(path) {
  return readFileSync4(path, "utf8").split(/\r?\n/).filter((line) => line.trim().length > 0).map((line, index) => {
    let value;
    try {
      value = JSON.parse(line);
    } catch {
      throw new DataNormalizationError(
        `Release log line ${index + 1} is not valid JSON.`,
        "release_log"
      );
    }
    const result2 = releaseLogRowSchema.safeParse(value);
    if (!result2.success) {
      throw new DataNormalizationError(
        `Release log line ${index + 1} contains prohibited or invalid fields: ${result2.error.issues.map((issue) => `${issue.path.join(".") || "row"}: ${issue.message}`).join("; ")}`,
        "release_log"
      );
    }
    return result2.data;
  });
}
function createReleaseLogConnector(options) {
  const root = resolve4(options.rootDir);
  const path = inside(root, options.path ?? "reports/releases/release-log.jsonl");
  const windowHours = options.windowHours ?? 24 * 30;
  if (!Number.isFinite(windowHours) || windowHours <= 0) {
    throw new Error("Release-log windowHours must be a positive finite number.");
  }
  return {
    id: "local-release-log",
    source: "release_log",
    transport: "local",
    credentialRequired: false,
    rawExportsCommitted: false,
    async fetch(context2) {
      if (!existsSync3(path)) {
        throw new Error(
          `Release log is missing at ${relative(root, path).split(sep).join("/")}; missing is not zero.`
        );
      }
      const start = new Date(context2.now.getTime() - windowHours * 36e5);
      const rows = readRows(path).filter((row) => {
        const releasedAt = Date.parse(row.released_at);
        return releasedAt >= start.getTime() && releasedAt < context2.now.getTime();
      });
      return {
        source: "release_log",
        sourceAccount: options.sourceAccount ?? "venture-release-log",
        fetchedAt: context2.now.toISOString(),
        reportingWindow: { start: start.toISOString(), end: context2.now.toISOString() },
        timezone: options.timezone,
        dimensions: ["environment", "status", "change_kind", "journey_id", "release_version"],
        quality: "complete",
        limitations: [
          "Only strict categorical release metadata is normalized; commit messages and free-form release notes are excluded.",
          "Temporal overlap between a release and a metric change does not establish causation."
        ],
        releaseVersion: rows.length === 1 ? rows[0].release_version : null,
        rows
      };
    }
  };
}

// lib/founder-launch/idea.ts
import { createHash as createHash5 } from "node:crypto";
import { parse as parseYaml } from "yaml";

// lib/launch/types.ts
var routingLevelSchema = external_exports.enum(["unknown", "low", "moderate", "high"]);
var founderBriefSchema = external_exports.object({
  id: external_exports.string().regex(/^[a-z][a-z0-9-]+$/),
  synthetic: external_exports.literal(true).optional(),
  name: external_exports.string().min(1),
  specific_user_or_audience: external_exports.string().min(1),
  problem_or_job: external_exports.string().min(1),
  intended_outcome: external_exports.string().min(1),
  smallest_core_journey: external_exports.string().min(1),
  primary_success_signal: external_exports.string().min(1),
  material_constraints: uniqueArray(external_exports.string().min(1)),
  known_truths: uniqueArray(external_exports.string().min(1)),
  assumptions: uniqueArray(external_exports.string().min(1)),
  app_kind: appKindSchema,
  requested_mobile_stack: mobileStackSchema,
  business_model: external_exports.enum([
    "unselected",
    "b2b",
    "b2c",
    "b2b2c",
    "marketplace",
    "internal",
    "nonprofit"
  ]),
  monetization_model: external_exports.enum([
    "none",
    "subscription",
    "one_time",
    "usage_based",
    "transaction_fee",
    "lead_generation",
    "services",
    "hybrid"
  ]),
  native_digital_goods: external_exports.boolean(),
  target_market: external_exports.string().min(1).nullable().optional(),
  domain: external_exports.string().regex(/^(?=.{1,253}$)(?:[A-Za-z0-9](?:[A-Za-z0-9-]{0,61}[A-Za-z0-9])?\.)+[A-Za-z]{2,63}$/).nullable().optional(),
  locale: external_exports.string().regex(/^[A-Za-z]{2,3}(?:-[A-Za-z0-9]{2,8})*$/).optional(),
  currency: external_exports.string().regex(/^[A-Z]{3}$/).optional(),
  timezone: external_exports.string().min(1).optional(),
  repository_visibility: external_exports.enum(["private", "public"]).optional(),
  bundle_identifier: external_exports.string().regex(/^[A-Za-z][A-Za-z0-9.-]+$/).nullable().optional(),
  app_scheme: external_exports.string().regex(/^[a-z][a-z0-9+.-]*$/).nullable().optional(),
  factors: external_exports.object({
    smallest_useful_build_cost: routingLevelSchema,
    smallest_useful_build_time: routingLevelSchema,
    reversibility: routingLevelSchema,
    regulatory_or_safety_risk: routingLevelSchema,
    real_usage_required: routingLevelSchema,
    marketplace_cold_start: routingLevelSchema,
    operational_burden: routingLevelSchema,
    founder_evidence: routingLevelSchema,
    concierge_delivery_fit: routingLevelSchema,
    app_store_required: routingLevelSchema,
    deep_native_requirements: routingLevelSchema,
    on_device_requirements: routingLevelSchema
  }).strict(),
  needs: external_exports.object({
    authenticated_product: external_exports.boolean(),
    database: external_exports.boolean(),
    file_storage: external_exports.boolean(),
    transactional_email: external_exports.boolean(),
    lifecycle_email: external_exports.boolean(),
    feedback: external_exports.boolean(),
    analytics: external_exports.boolean(),
    search_discovery: external_exports.boolean(),
    scheduled_learning: external_exports.boolean()
  }).strict(),
  preferred_dns_provider: external_exports.enum(["mijndomein", "manual", "provider_api", "delegated"]),
  deceptive_request: external_exports.boolean().default(false),
  unsafe_non_defaultable_choice: external_exports.string().min(1).nullable().default(null),
  indispensable_missing_credential: external_exports.string().min(1).nullable().default(null)
}).strict();
var LaunchBriefError = class extends Error {
  constructor(message, code) {
    super(message);
    this.code = code;
    this.name = "LaunchBriefError";
  }
  code;
};

// lib/launch/router.ts
var levels = { unknown: 0, low: 1, moderate: 2, high: 3 };
var modeOrder = ["thin_mvp", "product_first", "concierge_first", "validate_first"];
function reasonFor(mode) {
  switch (mode) {
    case "thin_mvp":
      return "The smallest useful product is reversible enough to build and learn from directly.";
    case "product_first":
      return "Real product usage or an installed app is required before value can be demonstrated honestly.";
    case "concierge_first":
      return "The outcome can be delivered honestly by a bounded concierge process before full automation.";
    case "validate_first":
      return "Build cost, risk, cold-start, or uncertainty makes a lower-cost demand test the safer first commitment.";
  }
}
function add(scores, mode, amount) {
  scores[mode] += amount;
}
function routeLaunchMode(input) {
  const brief = founderBriefSchema.parse(input);
  if (brief.deceptive_request) {
    throw new LaunchBriefError(
      "The requested product would be deceptive; revise the user promise and disclosures before building.",
      "deceptive_request"
    );
  }
  if (brief.unsafe_non_defaultable_choice) {
    throw new LaunchBriefError(
      `A material security, legal, or payment choice cannot be defaulted safely: ${brief.unsafe_non_defaultable_choice}`,
      "unsafe_choice"
    );
  }
  if (brief.indispensable_missing_credential) {
    throw new LaunchBriefError(
      `An indispensable provider credential or action is unavailable: ${brief.indispensable_missing_credential}`,
      "missing_credential"
    );
  }
  const f = brief.factors;
  const scores = {
    validate_first: 0,
    thin_mvp: 1,
    product_first: 0,
    concierge_first: 0
  };
  add(scores, "validate_first", levels[f.smallest_useful_build_cost]);
  add(scores, "validate_first", levels[f.smallest_useful_build_time]);
  add(scores, "thin_mvp", levels[f.reversibility]);
  add(scores, "product_first", Math.max(0, levels[f.reversibility] - 1));
  add(scores, "validate_first", levels[f.regulatory_or_safety_risk] * 2);
  add(scores, "product_first", levels[f.real_usage_required] * 2);
  add(scores, "thin_mvp", Math.max(0, levels[f.real_usage_required] - 1));
  add(scores, "validate_first", levels[f.marketplace_cold_start]);
  add(scores, "concierge_first", levels[f.marketplace_cold_start]);
  add(scores, "validate_first", levels[f.operational_burden]);
  add(scores, "thin_mvp", levels[f.founder_evidence]);
  add(scores, "product_first", levels[f.founder_evidence]);
  add(scores, "concierge_first", levels[f.concierge_delivery_fit] * 2);
  add(scores, "product_first", Math.max(0, levels[f.app_store_required] - 1) * 3);
  if (f.concierge_delivery_fit === "high" && f.regulatory_or_safety_risk !== "high") {
    add(scores, "concierge_first", 2);
  }
  if (f.smallest_useful_build_cost === "low" && f.smallest_useful_build_time === "low" && f.reversibility === "high") {
    add(scores, "thin_mvp", 4);
  }
  const ranked = modeOrder.map((mode) => ({ mode, score: scores[mode] })).sort((a, b) => b.score - a.score || modeOrder.indexOf(a.mode) - modeOrder.indexOf(b.mode));
  const winner = ranked[0];
  const margin = winner.score - ranked[1].score;
  const confidence = Number(Math.min(0.96, 0.55 + Math.max(0, margin) * 0.07).toFixed(2));
  return {
    selectedMode: winner.mode,
    confidence,
    rationale: reasonFor(winner.mode),
    scores,
    rejectedAlternatives: ranked.slice(1).map(({ mode }) => ({
      mode,
      reason: `${reasonFor(mode)} Its routing score (${scores[mode]}) was below ${winner.mode} (${winner.score}).`
    })),
    assumptions: [...brief.assumptions],
    evidenceThatCouldChangeChoice: [
      "A material change in smallest-useful build cost or reversibility.",
      "Evidence that real usage or an App Store artifact is indispensable.",
      "New regulatory, safety, cold-start, or operational constraints.",
      "Verified evidence that concierge delivery can or cannot produce the promised outcome honestly."
    ]
  };
}
function routeRail(brief) {
  let mobileStack = brief.requested_mobile_stack;
  if (brief.app_kind === "web") mobileStack = "none";
  else if (mobileStack === "auto") {
    mobileStack = brief.factors.deep_native_requirements === "high" || brief.factors.on_device_requirements === "high" ? "swiftui" : "expo_react_native";
  }
  const rationale = mobileStack === "swiftui" ? "SwiftUI is selected for Apple-first deep native or on-device requirements." : mobileStack === "expo_react_native" ? "Expo React Native is selected for fast iteration, common device APIs, and shared TypeScript logic." : "The venture uses the default server-rendered Next.js web rail.";
  return { appKind: brief.app_kind, mobileStack, rationale };
}
function routePayment(brief) {
  if (brief.monetization_model === "none" || brief.monetization_model === "lead_generation") {
    return {
      provider: "none",
      entitlementSource: "none",
      rationale: "No paid entitlement is active."
    };
  }
  const mobile = brief.app_kind !== "web";
  if (mobile && brief.native_digital_goods) {
    return {
      provider: "revenuecat",
      entitlementSource: "revenuecat",
      rationale: "RevenueCat is the single entitlement source for native digital purchases; Test Store is used in development."
    };
  }
  return {
    provider: "stripe",
    entitlementSource: "stripe",
    rationale: "Stripe is selected for the web or non-native payment flow and remains traceable to approved offer prices."
  };
}
function resolveCapabilities(brief, rail, payment) {
  const active = /* @__PURE__ */ new Set(["public_website"]);
  if (brief.needs.authenticated_product) active.add("authenticated_product");
  if (brief.needs.database) active.add("database");
  if (brief.needs.file_storage) active.add("file_storage");
  if (brief.needs.transactional_email) active.add("transactional_email");
  if (brief.needs.lifecycle_email) active.add("lifecycle_email");
  if (brief.needs.feedback) active.add("feedback_intake");
  if (brief.needs.analytics) {
    active.add("ga4");
    active.add("vercel_analytics");
  }
  if (brief.needs.search_discovery) {
    active.add("gsc");
    active.add("bing_webmaster");
    active.add("web_seo_aeo_geo");
  }
  if (brief.needs.scheduled_learning) active.add("scheduled_learning_loops");
  if (payment.provider === "stripe") active.add("stripe");
  if (payment.provider === "revenuecat") active.add("revenuecat");
  if (rail.appKind !== "web") {
    active.add("app_store_connect");
    active.add("ios_aso");
    if (rail.mobileStack === "expo_react_native") active.add("eas");
  }
  return [...active].sort();
}
function routeLaunch(raw) {
  const brief = founderBriefSchema.parse(raw);
  const mode = routeLaunchMode(brief);
  const rail = routeRail(brief);
  const payment = routePayment(brief);
  return {
    briefId: brief.id,
    mode,
    rail,
    payment,
    capabilities: resolveCapabilities(brief, rail, payment)
  };
}

// lib/launch/manual-evidence.ts
import { existsSync as existsSync4, lstatSync, readFileSync as readFileSync5, realpathSync } from "node:fs";
import { relative as relative2, resolve as resolve5, sep as sep2 } from "node:path";
import { isDeepStrictEqual } from "node:util";

// lib/providers/adapter.ts
import { createHash as createHash4 } from "node:crypto";

// lib/providers/retry.ts
var OUTAGE_CODES = /* @__PURE__ */ new Set([408, 425, 500, 502, 503, 504]);
function classifyProviderFailure(input) {
  if (input.networkError) {
    return { retryable: true, classification: "retryable_network" };
  }
  if (input.statusCode === 429) {
    const seconds = Number(input.retryAfter);
    return {
      retryable: true,
      classification: "retryable_rate_limit",
      suggestedDelayMs: Number.isFinite(seconds) && seconds >= 0 ? seconds * 1e3 : void 0
    };
  }
  if (input.statusCode !== void 0 && OUTAGE_CODES.has(input.statusCode)) {
    return { retryable: true, classification: "retryable_outage" };
  }
  if (input.statusCode === 401 || input.statusCode === 403) {
    return { retryable: false, classification: "terminal_auth" };
  }
  if (input.statusCode === 400 || input.statusCode === 404 || input.statusCode === 405 || input.statusCode === 422) {
    return { retryable: false, classification: "terminal_validation" };
  }
  if (input.statusCode === 409) {
    return { retryable: false, classification: "terminal_conflict" };
  }
  return { retryable: false, classification: "terminal_unknown" };
}

// lib/providers/transports.ts
function getPath(input, path) {
  if (path === "") return input;
  return path.split(".").reduce((value, part) => {
    if (Array.isArray(value) && !/^\d+$/.test(part)) {
      if (value.length !== 1) return void 0;
      value = value[0];
    }
    if (value === null || typeof value !== "object") return void 0;
    return value[part];
  }, input);
}
function interpolate(template, output) {
  if (template === "{result}" && ["string", "number", "boolean"].includes(typeof output)) {
    return String(output);
  }
  return template.replace(/\{result\.([a-zA-Z0-9_.-]+)\}/g, (_match, path) => {
    const value = getPath(output, path);
    return ["string", "number", "boolean"].includes(typeof value) ? String(value) : _match;
  });
}
var RESULT_PLACEHOLDER = /\{result(?:\.[a-zA-Z0-9_.-]+)?\}/;
function interpolateJson(value, output) {
  if (value === void 0) return void 0;
  if (typeof value === "string") {
    const exact = value.match(/^\{result(?:\.([a-zA-Z0-9_.-]+))?\}$/);
    if (exact) {
      const resolved = exact[1] ? getPath(output, exact[1]) : output;
      if (resolved === null || ["string", "number", "boolean"].includes(typeof resolved) || Array.isArray(resolved) || typeof resolved === "object" && resolved !== null) {
        return resolved;
      }
      return value;
    }
    return interpolate(value, output);
  }
  if (Array.isArray(value)) return value.map((item) => interpolateJson(item, output));
  if (value !== null && typeof value === "object") {
    return Object.fromEntries(
      Object.entries(value).map(([key, item]) => [key, interpolateJson(item, output)])
    );
  }
  return value;
}
function hasUnresolvedResult(value) {
  if (typeof value === "string") return RESULT_PLACEHOLDER.test(value);
  if (Array.isArray(value)) return value.some(hasUnresolvedResult);
  if (value !== null && typeof value === "object") {
    return Object.values(value).some(hasUnresolvedResult);
  }
  return false;
}
function resolvedAssertions(assertions, output) {
  return assertions?.map((assertion) => ({
    ...assertion,
    expected: interpolateJson(assertion.expected, output)
  }));
}
function deepContains(actual, expected) {
  if (Object.is(actual, expected)) return true;
  if (Array.isArray(actual)) {
    if (Array.isArray(expected)) {
      return expected.every(
        (expectedItem) => actual.some((actualItem) => deepContains(actualItem, expectedItem))
      );
    }
    return actual.some((item) => deepContains(item, expected));
  }
  if (actual !== null && expected !== null && typeof actual === "object" && typeof expected === "object" && !Array.isArray(expected)) {
    return Object.entries(expected).every(
      ([key, value]) => deepContains(actual[key], value)
    );
  }
  if (typeof actual === "string" && typeof expected === "string") {
    return actual.includes(expected);
  }
  return false;
}
function assertionsMatch(output, assertions) {
  if (!assertions || assertions.length === 0) return false;
  return assertions.every((assertion) => {
    const actual = assertion.path === "" ? output : getPath(output, assertion.path);
    if (assertion.operator === "exists") return actual !== void 0 && actual !== null;
    if (assertion.operator === "equals") return Object.is(actual, assertion.expected);
    return deepContains(actual, assertion.expected);
  });
}
function assertionReadBackStatus(output, assertions) {
  if (!assertions || assertions.length === 0) return "unavailable";
  return assertionsMatch(output, assertions) ? "matched" : "mismatched";
}
function parseOutput(value) {
  try {
    return JSON.parse(value);
  } catch {
    return value;
  }
}
function isDirectBinary(binary) {
  const trimmed = binary.trim();
  if (trimmed.length === 0 || /\s/.test(trimmed)) return false;
  const name = trimmed.split("/").at(-1)?.toLowerCase() ?? trimmed.toLowerCase();
  return !["sh", "bash", "zsh", "fish", "cmd", "powershell", "pwsh"].includes(name);
}
function commandErrorIsTransient(error) {
  const code = error && typeof error === "object" && "code" in error ? String(error.code) : "";
  return ["EAI_AGAIN", "ECONNRESET", "ENETDOWN", "ENETUNREACH", "ETIMEDOUT"].includes(code);
}
async function withCredential(context2, ref, use) {
  if (!context2.credentials) {
    throw new Error(`Credential broker is required for ${ref}`);
  }
  return context2.credentials.withSecret(ref, (secret) => use(secret));
}
var CommandProviderTransport = class {
  kind = "cli";
  runner;
  availability;
  constructor(options) {
    this.runner = options.runner;
    this.availability = options.available ?? (async () => ({ available: true }));
  }
  available() {
    return this.availability();
  }
  async execute(operation2, context2) {
    if (!operation2.command) {
      return {
        status: "failed",
        message: "CLI operation is missing a command specification",
        retryable: false,
        effectOutcome: "confirmed_no_write"
      };
    }
    return this.runSpec(operation2.command, operation2, context2);
  }
  async readBack(operation2, execution, context2) {
    const spec = operation2.readBack?.command;
    if (!spec) {
      return {
        operationId: operation2.id,
        status: execution.verified ? "matched" : "unavailable",
        message: execution.verified ? "The command response carried verification evidence" : "No CLI read-back command is declared"
      };
    }
    const resolved = {
      ...spec,
      args: spec.args.map((arg) => interpolate(arg, execution.output))
    };
    const assertions = resolvedAssertions(operation2.readBack?.assertions, execution.output);
    if (hasUnresolvedResult(resolved.args) || hasUnresolvedResult(assertions)) {
      return {
        operationId: operation2.id,
        status: "unavailable",
        message: "CLI read-back was not run because the apply result did not contain one unambiguous value for every {result...} placeholder"
      };
    }
    const result2 = await this.runSpec(resolved, operation2, context2);
    const status = result2.status === "succeeded" ? assertionReadBackStatus(result2.output, assertions) : "unavailable";
    return {
      operationId: operation2.id,
      status,
      message: status === "matched" ? operation2.readBack?.description ?? "CLI read-back succeeded" : result2.status === "succeeded" ? status === "mismatched" ? "CLI read-back contradicted a declared assertion for the requested state" : "CLI read-back succeeded but no declared assertion proved the requested state" : result2.message,
      evidence: result2.output
    };
  }
  async runSpec(spec, operation2, context2) {
    if (!isDirectBinary(spec.binary)) {
      return {
        status: "failed",
        providerCode: "shell_binary_forbidden",
        message: `Commands must invoke an executable directly: ${spec.binary}`,
        retryable: false,
        effectOutcome: "confirmed_no_write"
      };
    }
    const invoke = async (authSecret, stdinSecret) => {
      const env = spec.authEnvironment ? { [spec.authEnvironment.name]: authSecret } : void 0;
      const invocation2 = {
        command: spec.binary,
        args: [...spec.args],
        cwd: spec.cwd,
        env,
        sensitiveEnv: spec.authEnvironment ? [spec.authEnvironment.name] : void 0,
        stdin: stdinSecret,
        sensitiveStdin: stdinSecret !== void 0
      };
      try {
        const result2 = await this.runner.run(invocation2);
        if (result2.exitCode === 0) {
          const parsedOutput = parseOutput(result2.stdout);
          if (spec.captureCredential) {
            if (!context2.credentials) {
              return {
                status: "failed",
                providerCode: "terminal_validation",
                message: "Credential capture requires an injected credential broker",
                retryable: false
              };
            }
            const reference = context2.credentials.getReference(
              spec.captureCredential.credentialRef
            );
            if (!reference) {
              return {
                status: "failed",
                providerCode: "terminal_validation",
                message: `Credential capture target is not registered: ${spec.captureCredential.credentialRef}`,
                retryable: false
              };
            }
            const captured = getPath(parsedOutput, spec.captureCredential.outputPath);
            if (typeof captured !== "string" || captured.length === 0) {
              return {
                status: "failed",
                providerCode: "terminal_validation",
                message: `Command output did not contain a string at ${spec.captureCredential.outputPath}`,
                retryable: false
              };
            }
            try {
              await context2.credentials.store({ ...reference, value: captured });
            } catch (error) {
              return {
                status: "failed",
                providerCode: "terminal_validation",
                message: context2.redactor.redactText(
                  error instanceof Error ? error.message : String(error)
                ),
                retryable: false
              };
            }
          }
          const output = context2.redactor.redact(parsedOutput);
          return {
            status: "succeeded",
            message: `${operation2.action} command completed; read-back is still required`,
            output,
            effectOutcome: "confirmed_write"
          };
        }
        return {
          status: "failed",
          providerCode: `exit_${result2.exitCode}`,
          message: context2.redactor.redactText(
            result2.stderr || `${operation2.action} exited with ${result2.exitCode}`
          ),
          retryable: false,
          effectOutcome: "unknown"
        };
      } catch (error) {
        const decision = classifyProviderFailure({
          networkError: commandErrorIsTransient(error)
        });
        return {
          status: "failed",
          providerCode: decision.classification,
          message: context2.redactor.redactText(
            error instanceof Error ? error.message : String(error)
          ),
          retryable: decision.retryable,
          effectOutcome: "unknown"
        };
      }
    };
    const withStdin = (authSecret) => spec.stdinCredentialRef ? withCredential(context2, spec.stdinCredentialRef, (secret) => invoke(authSecret, secret)) : invoke(authSecret);
    return spec.authEnvironment ? withCredential(context2, spec.authEnvironment.credentialRef, withStdin) : withStdin();
  }
};
function flattenForm(value, prefix = "", entries = []) {
  if (value === null) {
    entries.push([prefix, ""]);
  } else if (Array.isArray(value)) {
    value.forEach((item, index) => flattenForm(item, `${prefix}[${index}]`, entries));
  } else if (typeof value === "object") {
    for (const [key, item] of Object.entries(value)) {
      flattenForm(item, prefix ? `${prefix}[${key}]` : key, entries);
    }
  } else {
    entries.push([prefix, String(value)]);
  }
  return entries;
}
function encodeBody(spec) {
  if (spec.body === void 0) return {};
  if (spec.encoding === "form") {
    return {
      body: new URLSearchParams(flattenForm(spec.body)).toString(),
      contentType: "application/x-www-form-urlencoded"
    };
  }
  return { body: JSON.stringify(spec.body), contentType: "application/json" };
}
var HttpProviderTransport = class {
  constructor(fetcher, availability = async () => ({ available: true }), jwtSigner) {
    this.fetcher = fetcher;
    this.availability = availability;
    this.jwtSigner = jwtSigner;
  }
  fetcher;
  availability;
  jwtSigner;
  kind = "http";
  available() {
    return this.availability();
  }
  async execute(operation2, context2) {
    if (!operation2.http) {
      return {
        status: "failed",
        message: "HTTP operation is missing a request specification",
        retryable: false,
        effectOutcome: "confirmed_no_write"
      };
    }
    return this.runSpec(operation2.http, operation2, context2);
  }
  async readBack(operation2, execution, context2) {
    const source = operation2.readBack?.http;
    if (!source) {
      return {
        operationId: operation2.id,
        status: execution.verified ? "matched" : "unavailable",
        message: execution.verified ? "The API response carried verification evidence" : "No HTTP read-back request is declared"
      };
    }
    const spec = {
      ...source,
      url: interpolate(source.url, execution.output)
    };
    const assertions = resolvedAssertions(operation2.readBack?.assertions, execution.output);
    if (hasUnresolvedResult(spec.url) || hasUnresolvedResult(assertions)) {
      return {
        operationId: operation2.id,
        status: "unavailable",
        message: "API read-back was not run because the apply result did not contain one unambiguous value for every {result...} placeholder"
      };
    }
    const result2 = await this.runSpec(spec, operation2, context2);
    const status = result2.status === "succeeded" ? assertionReadBackStatus(result2.output, assertions) : "unavailable";
    return {
      operationId: operation2.id,
      status,
      message: status === "matched" ? operation2.readBack?.description ?? "API read-back succeeded" : result2.status === "succeeded" ? status === "mismatched" ? "API read-back contradicted a declared assertion for the requested state" : "API read-back succeeded but no declared assertion proved the requested state" : result2.message,
      evidence: result2.output
    };
  }
  async runSpec(spec, operation2, context2) {
    const send = async (secret) => {
      let url = spec.url;
      const headers = { ...spec.headers ?? {} };
      const sensitiveHeaders = [];
      if (spec.auth && secret !== void 0) {
        if (spec.auth.scheme === "bearer" || spec.auth.scheme === "jwt") {
          headers.Authorization = `Bearer ${secret}`;
          sensitiveHeaders.push("authorization");
        } else if (spec.auth.scheme === "basic") {
          const encoded3 = Buffer.from(`${secret}:`).toString("base64");
          context2.redactor.addSecret(encoded3);
          headers.Authorization = `Basic ${encoded3}`;
          sensitiveHeaders.push("authorization");
        } else if (spec.auth.scheme === "api_key_header") {
          const name = spec.auth.name ?? "api-key";
          headers[name] = secret;
          sensitiveHeaders.push(name.toLowerCase());
        } else {
          const parsed = new URL(url);
          parsed.searchParams.set(spec.auth.name ?? "apikey", secret);
          url = parsed.toString();
        }
      }
      if (spec.nativeIdempotency) {
        headers["Idempotency-Key"] = operation2.idempotencyKey;
      }
      const encoded2 = encodeBody(spec);
      if (encoded2.contentType && !headers["Content-Type"]) {
        headers["Content-Type"] = encoded2.contentType;
      }
      const request2 = {
        method: spec.method,
        url,
        headers,
        body: encoded2.body,
        sensitiveHeaders,
        sensitiveUrl: spec.auth?.scheme === "api_key_query",
        signal: context2.signal
      };
      let response;
      try {
        response = await this.fetcher.fetch(request2);
      } catch (error) {
        const decision2 = classifyProviderFailure({ networkError: true });
        return {
          status: "failed",
          providerCode: decision2.classification,
          message: context2.redactor.redactText(
            error instanceof Error ? error.message : String(error)
          ),
          retryable: decision2.retryable,
          effectOutcome: "unknown"
        };
      }
      if (response.status >= 200 && response.status < 300) {
        if (spec.captureCredential) {
          if (!context2.credentials) {
            return {
              status: "failed",
              providerCode: "terminal_validation",
              message: "Credential capture requires an injected credential broker",
              retryable: false,
              effectOutcome: "unknown"
            };
          }
          const reference = context2.credentials.getReference(spec.captureCredential.credentialRef);
          if (!reference) {
            return {
              status: "failed",
              providerCode: "terminal_validation",
              message: `Credential capture target is not registered: ${spec.captureCredential.credentialRef}`,
              retryable: false,
              effectOutcome: "unknown"
            };
          }
          const captured = getPath(response.body, spec.captureCredential.outputPath);
          if (typeof captured !== "string" || captured.length === 0) {
            return {
              status: "failed",
              providerCode: "terminal_validation",
              message: `HTTP response did not contain a string at ${spec.captureCredential.outputPath}`,
              retryable: false,
              effectOutcome: "unknown"
            };
          }
          try {
            await context2.credentials.store({ ...reference, value: captured });
            context2.redactor.addSecret(captured);
          } catch (error) {
            return {
              status: "failed",
              providerCode: "terminal_validation",
              message: context2.redactor.redactText(
                error instanceof Error ? error.message : "Credential capture failed"
              ),
              retryable: false,
              effectOutcome: "unknown"
            };
          }
        }
        const output2 = context2.redactor.redact(response.body);
        return {
          status: "succeeded",
          statusCode: response.status,
          message: `${operation2.action} returned HTTP ${response.status}; read-back is still required`,
          output: output2,
          effectOutcome: "confirmed_write"
        };
      }
      const output = context2.redactor.redact(response.body);
      const retryAfter = response.headers?.["retry-after"];
      const decision = classifyProviderFailure({
        statusCode: response.status,
        retryAfter
      });
      return {
        status: "failed",
        statusCode: response.status,
        providerCode: decision.classification,
        message: `Provider returned HTTP ${response.status}`,
        output,
        retryable: decision.retryable,
        effectOutcome: ["retryable_rate_limit", "terminal_auth", "terminal_validation"].includes(
          decision.classification
        ) ? "confirmed_no_write" : "unknown"
      };
    };
    if (!spec.auth) return send();
    return withCredential(context2, spec.auth.credentialRef, async (secret) => {
      if (spec.auth?.scheme !== "jwt") return send(secret);
      if (!this.jwtSigner) {
        return {
          status: "failed",
          providerCode: "jwt_signer_missing",
          message: "A JWT signer must be injected for private-key authentication",
          retryable: false,
          effectOutcome: "confirmed_no_write"
        };
      }
      try {
        const token = await this.jwtSigner(secret, operation2);
        context2.redactor.addSecret(token);
        return send(token);
      } catch (error) {
        return {
          status: "failed",
          providerCode: "jwt_signing_failed",
          message: context2.redactor.redactText(
            error instanceof Error ? error.message : String(error)
          ),
          retryable: false,
          effectOutcome: "confirmed_no_write"
        };
      }
    });
  }
};
var ManualProviderTransport = class {
  kind = "manual";
  async available() {
    return { available: true, detail: "Human completion is required" };
  }
  async execute(operation2) {
    return {
      status: "waiting_manual",
      message: operation2.manual ? `Manual action required in ${operation2.manual.system}` : "Manual action is missing instructions",
      retryable: false,
      effectOutcome: "confirmed_no_write"
    };
  }
  async readBack(operation2) {
    return {
      operationId: operation2.id,
      status: "manual_required",
      message: "A human must provide the declared completion evidence"
    };
  }
};
function transportMap(...transports) {
  return Object.fromEntries(transports.map((transport) => [transport.kind, transport]));
}

// lib/providers/types.ts
var providerIds = [
  "github",
  "vercel",
  "neon",
  "stripe",
  "revenuecat",
  "brevo",
  "google",
  "bing",
  "dns",
  "mijndomein",
  "app_store_connect",
  "eas"
];
var ProviderPlanError = class extends Error {
  constructor(message, code) {
    super(message);
    this.code = code;
    this.name = "ProviderPlanError";
  }
  code;
};

// lib/providers/adapter.ts
function dryRunResult() {
  return {
    status: "skipped",
    message: "Dry run: no provider command or request was sent",
    retryable: false
  };
}
function canonicalJson(value) {
  if (value === null || typeof value !== "object") return JSON.stringify(value);
  if (Array.isArray(value)) return `[${value.map((item) => canonicalJson(item)).join(",")}]`;
  return `{${Object.entries(value).filter(([, item]) => item !== void 0).sort(([left], [right]) => left.localeCompare(right)).map(([key, item]) => `${JSON.stringify(key)}:${canonicalJson(item)}`).join(",")}}`;
}
function requestHash(operation2) {
  return createHash4("sha256").update(canonicalJson(operation2)).digest("hex");
}
function idempotencyFailure(providerCode, message, effectOutcome = "unknown", retryable = false) {
  return { status: "failed", providerCode, message, retryable, effectOutcome };
}
function requiresDurableIdempotency(operation2) {
  return ["reversible_external", "irreversible_external", "financial", "communication"].includes(
    operation2.effectClass
  );
}
var DEPENDENCY_RESULT = /\{dependency\.([a-z0-9_-]+)\.([a-zA-Z0-9_.-]+)\}/g;
var RESULT_REFERENCE = /\{result\.([a-zA-Z0-9_.-]+)\}/g;
function resultPath(input, path) {
  return path.split(".").reduce((value, part) => {
    if (Array.isArray(value) && !/^\d+$/.test(part)) {
      if (value.length !== 1) return void 0;
      value = value[0];
    }
    if (value === null || typeof value !== "object") return void 0;
    return value[part];
  }, input);
}
function interpolateDependencyString(value, operation2, planOperations, completed) {
  return value.replace(DEPENDENCY_RESULT, (placeholder, capability, path) => {
    const dependencies = planOperations.filter(
      (candidate) => operation2.dependsOn.includes(candidate.id) && candidate.capability === capability
    );
    if (dependencies.length !== 1) return placeholder;
    const resolved = resultPath(completed.get(dependencies[0].id)?.output, path);
    return ["string", "number", "boolean"].includes(typeof resolved) ? String(resolved) : placeholder;
  });
}
function interpolateDependencyValue(value, operation2, planOperations, completed) {
  if (typeof value === "string") {
    return interpolateDependencyString(value, operation2, planOperations, completed);
  }
  if (Array.isArray(value)) {
    return value.map(
      (item) => interpolateDependencyValue(item, operation2, planOperations, completed)
    );
  }
  if (value !== null && typeof value === "object") {
    return Object.fromEntries(
      Object.entries(value).map(([key, item]) => [
        key,
        interpolateDependencyValue(item, operation2, planOperations, completed)
      ])
    );
  }
  return value;
}
function materializeDependencies(operation2, planOperations, completed) {
  const materialized2 = interpolateDependencyValue(
    operation2,
    operation2,
    planOperations,
    completed
  );
  return JSON.stringify(materialized2).includes("{dependency.") ? null : materialized2;
}
function replayOutputPaths(operation2, planOperations) {
  const paths = /* @__PURE__ */ new Set();
  const readBack = JSON.stringify(operation2.readBack ?? null);
  for (const match of readBack.matchAll(RESULT_REFERENCE)) paths.add(match[1]);
  for (const dependent of planOperations) {
    if (!dependent.dependsOn.includes(operation2.id)) continue;
    for (const match of JSON.stringify(dependent).matchAll(DEPENDENCY_RESULT)) {
      if (match[1] === operation2.capability) paths.add(match[2]);
    }
  }
  return [...paths].sort();
}
function executionState(plan, operations) {
  if (plan.dryRun || operations.every(({ result: result2 }) => result2.status === "skipped")) {
    return "planned";
  }
  const statuses = operations.map(({ result: result2 }) => result2.status);
  const succeeded = statuses.some((status) => status === "succeeded");
  const failed = statuses.some((status) => status === "failed");
  const waiting = statuses.some((status) => status === "waiting_manual");
  const blocked = statuses.some((status) => status === "skipped");
  if (failed && succeeded) return "degraded";
  if (failed) return "failed";
  if (waiting) return "waiting_manual";
  if (blocked && succeeded) return "degraded";
  if (blocked) return "failed";
  return "applied";
}
function scopeMissing(actual, required) {
  return required.filter((scope) => !actual.includes(scope) && !actual.includes("*"));
}
var DeclarativeProviderAdapter = class {
  constructor(descriptor2, buildPlan2) {
    this.descriptor = descriptor2;
    this.buildPlan = buildPlan2;
  }
  descriptor;
  buildPlan;
  async doctor(request2, context2) {
    const issues = [];
    const requestedCapabilities = request2.requiredCapabilities ?? [];
    for (const capability of requestedCapabilities) {
      if (!this.descriptor.capabilities.includes(capability)) {
        issues.push({
          code: "capability_unknown",
          message: `${this.descriptor.displayName} does not declare capability ${capability}`,
          remediation: `Choose one of: ${this.descriptor.capabilities.join(", ")}`
        });
      }
    }
    const transportResults = await Promise.all(
      this.descriptor.transports.map(async (kind) => {
        const transport = context2.transports[kind] ?? (kind === "manual" ? new ManualProviderTransport() : void 0);
        if (!transport) {
          return { kind, available: false, detail: "No transport was injected" };
        }
        try {
          return { kind, ...await transport.available() };
        } catch (error) {
          return {
            kind,
            available: false,
            detail: context2.redactor.redactText(
              error instanceof Error ? error.message : String(error)
            )
          };
        }
      })
    );
    const nonManual = transportResults.filter(({ kind }) => kind !== "manual");
    if (nonManual.length > 0 && nonManual.every(({ available }) => !available)) {
      issues.push({
        code: "transport_missing",
        message: `No executable ${this.descriptor.displayName} transport is available`,
        remediation: `Inject one of: ${nonManual.map(({ kind }) => kind).join(", ")}`
      });
    }
    const credentialResults = [];
    const credentialInspections = [];
    const manualCapabilities = {
      revenuecat: ["project_bootstrap"],
      dns: ["record"],
      mijndomein: ["record", "domain_attachment"],
      app_store_connect: ["first_app_record"],
      eas: ["app_store_prerequisite"]
    };
    const requestIsManualOnly = requestedCapabilities.length > 0 && requestedCapabilities.every(
      (capability) => manualCapabilities[this.descriptor.id]?.includes(capability)
    );
    const needsCredential = !requestIsManualOnly && this.descriptor.authMethods.some(
      (method) => method !== "manual" && method !== "none" && method !== "cli_session"
    );
    const acceptsCliSession = !requestIsManualOnly && this.descriptor.authMethods.includes("cli_session");
    const capabilityCredentialRequirements = requestedCapabilities.length > 0 ? (this.descriptor.credentialRequirements ?? []).filter(
      (requirement) => requirement.capabilities.some(
        (capability) => requestedCapabilities.includes(capability)
      )
    ) : [];
    const refs = request2.credentialRefs ?? [];
    if (capabilityCredentialRequirements.length === 0 && (needsCredential || acceptsCliSession) && refs.length === 0) {
      issues.push({
        code: "auth_missing",
        message: `No credential reference was supplied for ${this.descriptor.displayName}`,
        remediation: `Register a ${this.descriptor.authMethods.filter((method) => method !== "manual" && method !== "none").join(" or ")} credential and pass its cred:// reference`
      });
    }
    for (const ref of refs) {
      if (!context2.credentials) {
        issues.push({
          code: "auth_missing",
          message: `Credential ${ref} cannot be inspected without a broker`,
          remediation: "Inject a CredentialBroker into the provider context"
        });
        continue;
      }
      try {
        const inspection = await context2.credentials.inspect(ref);
        credentialInspections.push(inspection);
        credentialResults.push({
          ref: inspection.ref,
          status: inspection.status,
          scopes: [...inspection.scopes],
          expiresAt: inspection.expiresAt
        });
        if (inspection.status !== "available") {
          issues.push({
            code: "auth_invalid",
            message: `Credential ${ref} is ${inspection.status}`,
            remediation: "Test or replace the credential before applying a plan"
          });
        }
        const missing = scopeMissing(inspection.scopes, this.descriptor.requiredScopes);
        if (inspection.status === "available" && missing.length > 0) {
          issues.push({
            code: "auth_invalid",
            message: `Credential ${ref} has not declared required scopes: ${missing.join(", ")}`,
            remediation: "Replace it with a least-privilege credential that includes those scopes"
          });
        }
      } catch (error) {
        issues.push({
          code: "auth_invalid",
          message: context2.redactor.redactText(
            error instanceof Error ? error.message : String(error)
          ),
          remediation: "Register and test the referenced credential"
        });
      }
    }
    for (const requirement of capabilityCredentialRequirements) {
      const compatible = credentialInspections.filter(
        ({ kind }) => requirement.acceptedKinds.includes(kind)
      );
      if (compatible.length === 0) {
        issues.push({
          code: "auth_missing",
          message: `No ${requirement.acceptedKinds.join(" or ")} credential reference was supplied for ${requirement.purpose}`,
          remediation: `Register the ${requirement.purpose} credential in the broker and pass its cred:// reference`
        });
      }
    }
    for (const limitation of this.descriptor.limitations) {
      issues.push({
        code: "provider_limitation",
        message: limitation,
        remediation: "Keep the limitation as an explicit manual or verification step"
      });
    }
    const isManualOnly = this.descriptor.transports.every((transport) => transport === "manual");
    const authIssue = issues.some(({ code }) => code === "auth_missing" || code === "auth_invalid");
    const unavailable = issues.some(({ code }) => code === "transport_missing");
    const capabilityIssue = issues.some(({ code }) => code === "capability_unknown");
    return {
      provider: this.descriptor.id,
      status: isManualOnly ? "manual_only" : unavailable ? "unavailable" : authIssue ? "auth_required" : capabilityIssue ? "degraded" : "ready",
      credentialRefs: credentialResults,
      transports: transportResults,
      issues
    };
  }
  plan(request2) {
    if (request2.capabilities.length === 0) {
      throw new ProviderPlanError(
        "At least one provider capability must be requested",
        "invalid_plan"
      );
    }
    const unknown = request2.capabilities.filter(
      (capability) => !this.descriptor.capabilities.includes(capability)
    );
    if (unknown.length > 0) {
      throw new ProviderPlanError(
        `${this.descriptor.id} does not support: ${unknown.join(", ")}`,
        "unknown_capability"
      );
    }
    const plan = this.buildPlan({ ...request2, dryRun: request2.dryRun ?? true });
    if (plan.provider !== this.descriptor.id) {
      throw new ProviderPlanError(
        `Plan provider ${plan.provider} does not match adapter ${this.descriptor.id}`,
        "invalid_plan"
      );
    }
    for (const operation2 of plan.operations) {
      if (operation2.provider !== this.descriptor.id) {
        throw new ProviderPlanError(
          `Operation ${operation2.id} belongs to ${operation2.provider}`,
          "invalid_plan"
        );
      }
      const specCount = [operation2.command, operation2.http, operation2.manual].filter(
        Boolean
      ).length;
      if (specCount !== 1) {
        throw new ProviderPlanError(
          `Operation ${operation2.id} must declare exactly one transport specification`,
          "invalid_plan"
        );
      }
    }
    return plan;
  }
  async apply(plan, context2) {
    if (plan.provider !== this.descriptor.id) {
      throw new ProviderPlanError(
        `Cannot apply ${plan.provider} plan with ${this.descriptor.id} adapter`,
        "invalid_plan"
      );
    }
    const results = [];
    const completed = /* @__PURE__ */ new Map();
    const dryRun = plan.dryRun || context2.authorization !== "approved";
    if (!dryRun && plan.operations.some(requiresDurableIdempotency) && (!context2.idempotencyLedger || context2.idempotencyLedger.durability !== "durable_atomic" && !context2.fixtureMode)) {
      throw new ProviderPlanError(
        "Approved external provider apply requires a durable idempotency ledger",
        "invalid_plan"
      );
    }
    if (!dryRun) {
      for (const operation2 of plan.operations) {
        const capture2 = operation2.command?.captureCredential;
        if (!capture2) continue;
        if (!context2.credentials) {
          throw new ProviderPlanError(
            `${operation2.id} requires a credential broker for captured output`,
            "invalid_plan"
          );
        }
        const reference = context2.credentials.getReference(capture2.credentialRef);
        if (!reference || reference.provider !== operation2.provider) {
          throw new ProviderPlanError(
            `${operation2.id} credential capture target is not registered for ${operation2.provider}`,
            "invalid_plan"
          );
        }
        const inspection = await context2.credentials.inspect(capture2.credentialRef);
        if (!inspection.writable) {
          throw new ProviderPlanError(
            `${operation2.id} credential capture target is not writable`,
            "invalid_plan"
          );
        }
      }
    }
    for (const plannedOperation of plan.operations) {
      if (dryRun) {
        const result3 = dryRunResult();
        results.push({ operation: plannedOperation, result: result3, reused: false });
        completed.set(plannedOperation.id, result3);
        continue;
      }
      const blockedBy = plannedOperation.dependsOn.filter(
        (dependency) => completed.get(dependency)?.status !== "succeeded"
      );
      if (blockedBy.length > 0) {
        const result3 = {
          status: "skipped",
          message: `Blocked by incomplete dependencies: ${blockedBy.join(", ")}`,
          retryable: false
        };
        results.push({ operation: plannedOperation, result: result3, reused: false });
        completed.set(plannedOperation.id, result3);
        continue;
      }
      const operation2 = materializeDependencies(plannedOperation, plan.operations, completed);
      if (!operation2) {
        const result3 = {
          status: "failed",
          providerCode: "terminal_validation",
          message: `Could not resolve every declared dependency output for ${plannedOperation.action}`,
          retryable: false
        };
        results.push({ operation: plannedOperation, result: result3, reused: false });
        completed.set(plannedOperation.id, result3);
        continue;
      }
      const operationRequestHash = requestHash(operation2);
      const replay = {
        outputPaths: replayOutputPaths(plannedOperation, plan.operations)
      };
      const claim = await context2.idempotencyLedger?.claim(
        operation2.idempotencyKey,
        operationRequestHash
      );
      if (claim?.status === "conflict") {
        const result3 = idempotencyFailure(
          "idempotency_conflict",
          "The idempotency key is already bound to a different provider request"
        );
        results.push({ operation: operation2, result: result3, reused: false });
        completed.set(operation2.id, result3);
        continue;
      }
      if (claim?.status === "replay" && (operation2.reconcileOnReplay !== true || context2.reuseSuccessfulOperations === true)) {
        const result3 = context2.redactor.redact(claim.result);
        results.push({ operation: operation2, result: result3, reused: true });
        completed.set(operation2.id, result3);
        continue;
      }
      let transport;
      try {
        transport = this.resolveTransport(operation2.transport, context2);
      } catch (error) {
        const result3 = {
          status: "failed",
          providerCode: "terminal_validation",
          message: context2.redactor.redactText(
            error instanceof Error ? error.message : String(error)
          ),
          retryable: false,
          effectOutcome: "confirmed_no_write"
        };
        await context2.idempotencyLedger?.settle(
          operation2.idempotencyKey,
          operationRequestHash,
          "definitive_no_write",
          result3,
          replay
        );
        results.push({ operation: operation2, result: result3, reused: false });
        completed.set(operation2.id, result3);
        continue;
      }
      if (claim?.status === "pending_reconciliation") {
        const result3 = await this.reconcileUnknownOutcome(
          operation2,
          transport,
          claim.result,
          context2
        );
        const state = result3.status === "succeeded" ? "succeeded" : result3.effectOutcome === "confirmed_no_write" ? "definitive_no_write" : "pending_reconciliation";
        await context2.idempotencyLedger?.settle(
          operation2.idempotencyKey,
          operationRequestHash,
          state,
          result3,
          replay
        );
        results.push({ operation: operation2, result: result3, reused: true });
        completed.set(operation2.id, result3);
        continue;
      }
      let result2;
      try {
        result2 = await transport.execute(operation2, {
          credentials: context2.credentials,
          redactor: context2.redactor,
          signal: context2.signal
        });
      } catch (error) {
        result2 = {
          status: "failed",
          providerCode: "transport_exception",
          message: context2.redactor.redactText(
            error instanceof Error ? error.message : String(error)
          ),
          retryable: false,
          effectOutcome: "unknown"
        };
      }
      result2 = context2.redactor.redact(result2);
      if (result2.status === "succeeded" && result2.effectOutcome === void 0) {
        result2 = { ...result2, effectOutcome: "confirmed_write" };
      }
      const settlement = result2.status === "succeeded" ? "succeeded" : result2.effectOutcome === "confirmed_no_write" || result2.status === "waiting_manual" || result2.status === "skipped" ? "definitive_no_write" : "pending_reconciliation";
      await context2.idempotencyLedger?.settle(
        operation2.idempotencyKey,
        operationRequestHash,
        settlement,
        result2,
        replay
      );
      results.push({ operation: operation2, result: result2, reused: false });
      completed.set(operation2.id, result2);
    }
    return {
      planId: plan.id,
      provider: plan.provider,
      state: executionState(plan, results),
      operations: results
    };
  }
  /**
   * Reconstructs request-bound operation state and performs only provider
   * reconciliation/read operations. This path deliberately never calls
   * ProviderTransport.execute; a confirmed no-write is released back to the
   * workflow so a later authorized handler attempt may apply it safely.
   */
  async reconcile(plan, context2) {
    if (plan.provider !== this.descriptor.id) {
      throw new ProviderPlanError(
        `Cannot reconcile ${plan.provider} plan with ${this.descriptor.id} adapter`,
        "invalid_plan"
      );
    }
    if (!context2.idempotencyLedger || context2.idempotencyLedger.durability !== "durable_atomic" && !context2.fixtureMode) {
      throw new ProviderPlanError(
        "Provider reconciliation requires a durable idempotency ledger",
        "invalid_plan"
      );
    }
    const results = [];
    const completed = /* @__PURE__ */ new Map();
    for (const plannedOperation of plan.operations) {
      const blockedBy = plannedOperation.dependsOn.filter(
        (dependency) => completed.get(dependency)?.status !== "succeeded"
      );
      if (blockedBy.length > 0) {
        const result3 = {
          status: "skipped",
          message: `Reconciliation blocked by incomplete dependencies: ${blockedBy.join(", ")}`,
          retryable: false,
          effectOutcome: "confirmed_no_write"
        };
        results.push({ operation: plannedOperation, result: result3, reused: true });
        completed.set(plannedOperation.id, result3);
        continue;
      }
      const operation2 = materializeDependencies(plannedOperation, plan.operations, completed);
      if (!operation2) {
        const result3 = {
          status: "failed",
          providerCode: "terminal_validation",
          message: `Could not reconstruct every declared dependency output for ${plannedOperation.action}`,
          retryable: false,
          effectOutcome: "unknown"
        };
        results.push({ operation: plannedOperation, result: result3, reused: true });
        completed.set(plannedOperation.id, result3);
        continue;
      }
      const operationRequestHash = requestHash(operation2);
      const replay = { outputPaths: replayOutputPaths(plannedOperation, plan.operations) };
      const claim = await context2.idempotencyLedger.claim(
        operation2.idempotencyKey,
        operationRequestHash
      );
      if (claim.status === "conflict") {
        const result3 = idempotencyFailure(
          "idempotency_conflict",
          "The idempotency key is bound to a different provider request; reconciliation stopped"
        );
        results.push({ operation: operation2, result: result3, reused: true });
        completed.set(operation2.id, result3);
        continue;
      }
      if (claim.status === "acquired") {
        const result3 = idempotencyFailure(
          "unknown_outcome_reconciliation_required",
          "No request-bound provider attempt was recorded; provider write was not invoked",
          "confirmed_no_write",
          true
        );
        await context2.idempotencyLedger.settle(
          operation2.idempotencyKey,
          operationRequestHash,
          "definitive_no_write",
          result3,
          replay
        );
        results.push({ operation: operation2, result: result3, reused: true });
        completed.set(operation2.id, result3);
        continue;
      }
      if (claim.status === "replay") {
        const result3 = context2.redactor.redact(claim.result);
        results.push({ operation: operation2, result: result3, reused: true });
        completed.set(operation2.id, result3);
        continue;
      }
      let result2;
      try {
        const transport = this.resolveTransport(operation2.transport, context2);
        result2 = await this.reconcileUnknownOutcome(operation2, transport, claim.result, context2);
      } catch (error) {
        result2 = idempotencyFailure(
          "unknown_outcome_reconciliation_required",
          context2.redactor.redactText(
            `Provider reconciliation is unavailable: ${error instanceof Error ? error.message : String(error)}`
          )
        );
      }
      const settlement = result2.status === "succeeded" ? "succeeded" : result2.effectOutcome === "confirmed_no_write" ? "definitive_no_write" : "pending_reconciliation";
      await context2.idempotencyLedger.settle(
        operation2.idempotencyKey,
        operationRequestHash,
        settlement,
        result2,
        replay
      );
      results.push({ operation: operation2, result: result2, reused: true });
      completed.set(operation2.id, result2);
    }
    return {
      planId: plan.id,
      provider: plan.provider,
      state: executionState(plan, results),
      operations: results
    };
  }
  async readBack(report, context2) {
    const checks = [];
    for (const execution of report.operations) {
      const { operation: operation2, result: result2 } = execution;
      if (result2.status === "waiting_manual") {
        checks.push({
          operationId: operation2.id,
          status: "manual_required",
          message: "Human completion evidence has not been supplied"
        });
        continue;
      }
      if (result2.status !== "succeeded") {
        checks.push({
          operationId: operation2.id,
          status: "unavailable",
          message: `Read-back skipped because execution was ${result2.status}`
        });
        continue;
      }
      const transport = this.resolveTransport(
        operation2.readBack?.transport ?? operation2.transport,
        context2
      );
      if (!transport.readBack) {
        checks.push({
          operationId: operation2.id,
          status: result2.verified ? "matched" : "unavailable",
          message: result2.verified ? "Execution result included verification evidence" : "The injected transport does not implement read-back"
        });
        continue;
      }
      try {
        checks.push(
          context2.redactor.redact(
            await transport.readBack(operation2, result2, {
              credentials: context2.credentials,
              redactor: context2.redactor,
              signal: context2.signal
            })
          )
        );
      } catch (error) {
        checks.push({
          operationId: operation2.id,
          status: "unavailable",
          message: context2.redactor.redactText(
            error instanceof Error ? error.message : String(error)
          )
        });
      }
    }
    return { planId: report.planId, provider: report.provider, results: checks };
  }
  verify(report, readBack) {
    if (report.planId !== readBack.planId || report.provider !== readBack.provider) {
      throw new ProviderPlanError(
        "Read-back report does not belong to this execution",
        "invalid_plan"
      );
    }
    const statuses = readBack.results.map(({ status }) => status);
    const state = statuses.some(
      (status) => status === "mismatched"
    ) ? "failed" : statuses.some((status) => status === "manual_required") ? "pending" : statuses.length > 0 && statuses.every((status) => status === "matched") ? "verified" : "unavailable";
    return {
      planId: report.planId,
      provider: report.provider,
      state,
      checks: readBack.results
    };
  }
  resolveTransport(kind, context2) {
    const transport = context2.transports[kind] ?? (kind === "manual" ? new ManualProviderTransport() : void 0);
    if (!transport) throw new Error(`No ${kind} transport was injected`);
    return transport;
  }
  async reconcileUnknownOutcome(operation2, transport, prior, context2) {
    try {
      if (transport.reconcile) {
        const reconciliation = context2.redactor.redact(
          await transport.reconcile(operation2, {
            credentials: context2.credentials,
            redactor: context2.redactor,
            signal: context2.signal
          })
        );
        if (reconciliation.status === "matched") {
          return {
            status: "succeeded",
            message: reconciliation.message,
            output: reconciliation.result?.output ?? reconciliation.evidence,
            statusCode: reconciliation.result?.statusCode,
            providerCode: reconciliation.result?.providerCode,
            retryable: false,
            verified: true,
            effectOutcome: "confirmed_write"
          };
        }
        if (reconciliation.status === "definitive_no_write") {
          return {
            status: "failed",
            providerCode: "unknown_outcome_reconciliation_required",
            message: reconciliation.message,
            retryable: true,
            verified: true,
            effectOutcome: "confirmed_no_write"
          };
        }
      } else if (operation2.readBack && transport.readBack) {
        const readBack = context2.redactor.redact(
          await transport.readBack(operation2, prior, {
            credentials: context2.credentials,
            redactor: context2.redactor,
            signal: context2.signal
          })
        );
        if (readBack.status === "matched") {
          return {
            status: "succeeded",
            message: `Unknown provider outcome reconciled: ${readBack.message}`,
            output: readBack.evidence,
            retryable: false,
            verified: true,
            effectOutcome: "confirmed_write"
          };
        }
      }
    } catch (error) {
      return idempotencyFailure(
        "unknown_outcome_reconciliation_required",
        context2.redactor.redactText(
          `Provider reconciliation failed: ${error instanceof Error ? error.message : String(error)}`
        )
      );
    }
    return idempotencyFailure(
      "unknown_outcome_reconciliation_required",
      "The prior provider write remains ambiguous; the write was not repeated"
    );
  }
};

// lib/providers/descriptors.ts
var retryableStatusCodes = [408, 425, 429, 500, 502, 503, 504];
var standardRedaction = [
  "Credential values and authorization headers",
  "Cookies, refresh tokens, private keys, and webhook signing secrets",
  "Connection strings and provider responses marked sensitive"
];
function descriptor(input) {
  return {
    ...input,
    environments: input.environments ?? ["preview", "production"],
    rateLimits: input.rateLimits ?? {
      source: "provider_headers",
      retryAfter: true,
      retryableStatusCodes,
      defaultMaxAttempts: 3,
      notes: "Respect provider retry headers and retry only classified transient failures."
    },
    redactionRules: input.redactionRules ?? standardRedaction
  };
}
var providerDescriptors = {
  github: descriptor({
    id: "github",
    displayName: "GitHub",
    capabilities: ["repository", "actions_secret", "repository_settings", "draft_pull_request"],
    authMethods: ["cli_session", "oauth", "restricted_api_key"],
    riskClass: "high",
    effectClasses: ["read", "reversible_external", "irreversible_external"],
    reversibility: "conditionally_reversible",
    requiredScopes: ["repo", "workflow"],
    idempotency: {
      mode: "client_ledger",
      notes: "The adapter derives stable keys and verifies repository state before any replay."
    },
    verification: {
      mode: "response_and_read_back",
      evidence: [
        "repository identity",
        "exact default-branch commit and source tree",
        "settings read-back",
        "pull request URL"
      ],
      notes: "Secret values cannot be read back; only secret metadata can be verified."
    },
    transports: ["cli"],
    limitations: [
      "GitHub never returns Actions secret values after write; verification is metadata-only.",
      "Existing repositories with a different default-branch tree are never overwritten by source publication."
    ]
  }),
  vercel: descriptor({
    id: "vercel",
    displayName: "Vercel",
    capabilities: ["project", "environment_variable", "deployment", "domain", "web_analytics"],
    authMethods: ["cli_session", "api_key"],
    riskClass: "high",
    effectClasses: ["read", "local_write", "reversible_external", "manual"],
    reversibility: "conditionally_reversible",
    requiredScopes: [],
    environments: ["local", "preview", "production"],
    idempotency: {
      mode: "client_ledger",
      notes: "Stable plan keys prevent blind duplicate project and domain operations."
    },
    verification: {
      mode: "response_and_read_back",
      evidence: ["project inspection", "deployment inspection", "domain inspection"],
      notes: "Environment variable values remain write-only and are verified by metadata."
    },
    transports: ["cli", "manual"],
    limitations: [
      "Vercel environment variable values are not returned in readable form after write.",
      "Vercel documents Web Analytics enablement in the dashboard; no undocumented write endpoint is assumed."
    ]
  }),
  neon: descriptor({
    id: "neon",
    displayName: "Neon",
    capabilities: [
      "project",
      "branch",
      "database",
      "role",
      "schema_migration",
      "read_write_health_check"
    ],
    authMethods: ["api_key", "connection_string"],
    credentialRequirements: [
      {
        capabilities: ["project", "branch", "database", "role"],
        acceptedKinds: ["api_key"],
        purpose: "Neon control-plane provisioning"
      },
      {
        capabilities: ["schema_migration", "read_write_health_check"],
        acceptedKinds: ["connection_string"],
        purpose: "PostgreSQL migration and health-check access"
      }
    ],
    riskClass: "critical",
    effectClasses: ["read", "reversible_external", "irreversible_external"],
    reversibility: "conditionally_reversible",
    requiredScopes: [],
    idempotency: {
      mode: "client_ledger",
      notes: "Create operations require stable names and read-back before replay."
    },
    verification: {
      mode: "response_and_read_back",
      evidence: [
        "project or branch id",
        "database metadata",
        "role metadata",
        "schema migration ledger",
        "disposable read/write probe"
      ],
      notes: "The API key provisions Neon resources. A separately brokered database connection verifies migrations and read/write access."
    },
    transports: ["cli"],
    limitations: [
      "Neon create responses may contain database credentials; only the declared connection URI field may be captured directly into an already-registered writable credential reference, and raw output must never be persisted.",
      "A missing, wrong-provider, or read-only capture target blocks before project creation."
    ]
  }),
  stripe: descriptor({
    id: "stripe",
    displayName: "Stripe",
    capabilities: ["product", "price", "webhook", "billing_portal"],
    authMethods: ["restricted_api_key", "api_key"],
    riskClass: "critical",
    effectClasses: ["read", "financial", "reversible_external"],
    reversibility: "conditionally_reversible",
    requiredScopes: [],
    environments: ["sandbox", "production"],
    idempotency: {
      mode: "native_and_client_ledger",
      keyPlacement: "header",
      notes: "Every POST sends Stripe's Idempotency-Key and is also recorded locally."
    },
    verification: {
      mode: "response_and_read_back",
      evidence: ["resource id", "livemode", "active state", "configured amount/currency"],
      notes: "A successful POST is not enough; GET the created object and compare it."
    },
    transports: ["http"],
    limitations: [
      "Price objects are immutable for amount and currency; corrections require a new price."
    ]
  }),
  revenuecat: descriptor({
    id: "revenuecat",
    displayName: "RevenueCat",
    capabilities: ["project_bootstrap", "app", "entitlement", "offering", "webhook"],
    authMethods: ["restricted_api_key", "manual"],
    riskClass: "critical",
    effectClasses: ["read", "financial", "reversible_external", "manual"],
    reversibility: "conditionally_reversible",
    requiredScopes: [
      "project_configuration:apps:read_write",
      "project_configuration:entitlements:read_write",
      "project_configuration:offerings:read_write",
      "project_configuration:integrations:read_write"
    ],
    environments: ["sandbox", "production"],
    idempotency: {
      mode: "client_ledger",
      notes: "RevenueCat v2 operations use deterministic resource identifiers where supported."
    },
    verification: {
      mode: "response_and_read_back",
      evidence: ["project id", "app id", "entitlement id", "offering state"],
      notes: "Project and key creation remain human-gated prerequisites."
    },
    transports: ["http", "manual"],
    limitations: [
      "Create the RevenueCat project and secret API key in the dashboard before v2 API setup."
    ]
  }),
  brevo: descriptor({
    id: "brevo",
    displayName: "Brevo",
    capabilities: [
      "sending_domain",
      "sending_domain_verification",
      "sender",
      "template",
      "webhook"
    ],
    authMethods: ["api_key"],
    riskClass: "high",
    effectClasses: ["read", "communication", "reversible_external"],
    reversibility: "conditionally_reversible",
    requiredScopes: [],
    idempotency: {
      mode: "client_ledger",
      notes: "Resource identity and read-back prevent duplicate sender and webhook setup."
    },
    verification: {
      mode: "response_and_read_back",
      evidence: ["domain authentication state", "sender id", "template id", "webhook id"],
      notes: "DNS authentication can stay pending after the API accepts a domain."
    },
    transports: ["http"],
    limitations: [
      "Sending-domain authentication is asynchronous and requires external DNS records."
    ]
  }),
  google: descriptor({
    id: "google",
    displayName: "Google",
    capabilities: [
      "analytics_property",
      "analytics_web_stream",
      "site_verification_token",
      "site_verification",
      "search_console_site",
      "search_console_sitemap"
    ],
    authMethods: ["oauth", "service_account"],
    riskClass: "high",
    effectClasses: ["read", "reversible_external"],
    reversibility: "conditionally_reversible",
    requiredScopes: [
      "https://www.googleapis.com/auth/analytics.edit",
      "https://www.googleapis.com/auth/siteverification",
      "https://www.googleapis.com/auth/webmasters"
    ],
    idempotency: {
      mode: "client_ledger",
      notes: "Google create calls are preceded by stable lookup and followed by read-back."
    },
    verification: {
      mode: "response_and_read_back",
      evidence: ["property name", "stream measurement id", "verified site", "sitemap status"],
      notes: "Search Console data can lag after verification and sitemap submission."
    },
    transports: ["http"],
    limitations: [
      "Site verification requires publishing the returned token through DNS or the website before insertion succeeds."
    ]
  }),
  bing: descriptor({
    id: "bing",
    displayName: "Bing Webmaster Tools",
    capabilities: ["site", "sitemap", "url_submission"],
    authMethods: ["oauth", "api_key"],
    riskClass: "medium",
    effectClasses: ["read", "reversible_external"],
    reversibility: "conditionally_reversible",
    requiredScopes: [],
    idempotency: {
      mode: "client_ledger",
      notes: "Site and feed identity are stored locally to avoid blind resubmission."
    },
    verification: {
      mode: "response_and_read_back",
      evidence: ["site list", "feed list", "submission response"],
      notes: "Indexing is asynchronous and is never inferred from request acceptance."
    },
    transports: ["http"],
    limitations: [
      "Bing Webmaster API surfaces are legacy and availability must be checked during doctor."
    ]
  }),
  dns: descriptor({
    id: "dns",
    displayName: "DNS provider",
    capabilities: ["record"],
    authMethods: ["manual"],
    riskClass: "critical",
    effectClasses: ["manual", "reversible_external", "irreversible_external"],
    reversibility: "manual",
    requiredScopes: [],
    idempotency: {
      mode: "manual",
      notes: "The human operator must check for an existing record before writing."
    },
    verification: {
      mode: "manual",
      evidence: ["authoritative DNS lookup", "record value", "TTL"],
      notes: "Propagation and authoritative read-back are required."
    },
    transports: ["manual"],
    limitations: [
      "No provider-neutral DNS write API is assumed; this adapter emits a manual record plan only."
    ]
  }),
  mijndomein: descriptor({
    id: "mijndomein",
    displayName: "MijnDomein",
    capabilities: ["record", "domain_attachment"],
    authMethods: ["manual"],
    riskClass: "critical",
    effectClasses: ["manual", "reversible_external", "irreversible_external"],
    reversibility: "manual",
    requiredScopes: [],
    idempotency: {
      mode: "manual",
      notes: "Inspect the MijnDomein control panel before adding or changing a record."
    },
    verification: {
      mode: "manual",
      evidence: ["control-panel screenshot", "authoritative DNS lookup"],
      notes: "The adapter does not claim an undocumented MijnDomein API."
    },
    transports: ["manual"],
    limitations: [
      "No supported MijnDomein provisioning API is assumed; all changes remain human-gated."
    ]
  }),
  app_store_connect: descriptor({
    id: "app_store_connect",
    displayName: "App Store Connect",
    capabilities: [
      "first_app_record",
      "build_processing",
      "testflight_group",
      "build_group_assignment",
      "build_metadata"
    ],
    authMethods: ["jwt_private_key", "manual"],
    riskClass: "critical",
    effectClasses: ["manual", "reversible_external", "irreversible_external"],
    reversibility: "conditionally_reversible",
    requiredScopes: [],
    environments: ["testflight", "production"],
    idempotency: {
      mode: "client_ledger",
      notes: "Use stable app and group identifiers; the first app record stays manual."
    },
    verification: {
      mode: "response_and_read_back",
      evidence: ["App Store Connect app id", "bundle id", "TestFlight group id"],
      notes: "A human must attest the first app record before subsequent API plans run."
    },
    transports: ["http", "manual"],
    limitations: [
      "The App Store Connect API cannot create the first app record; create it in the web interface."
    ]
  }),
  eas: descriptor({
    id: "eas",
    displayName: "Expo Application Services",
    capabilities: ["app_store_prerequisite", "app_store_connection", "ios_build", "ios_submit"],
    authMethods: ["cli_session", "api_key"],
    riskClass: "critical",
    effectClasses: ["manual", "reversible_external", "irreversible_external"],
    reversibility: "conditionally_reversible",
    requiredScopes: [],
    environments: ["preview", "testflight", "production"],
    idempotency: {
      mode: "client_ledger",
      notes: "Build and submission ids are recorded; retries must target an existing build."
    },
    verification: {
      mode: "response_and_read_back",
      evidence: ["EAS build id", "build status", "submission id", "submission status"],
      notes: "A completed build does not prove App Store submission acceptance."
    },
    transports: ["cli", "manual"],
    limitations: [
      "EAS cannot remove Apple's requirement to create the first App Store Connect app record manually."
    ]
  })
};

// lib/providers/github-source-publication.ts
var BOOTSTRAP_CONTENT = Buffer.from("venture-harness-source-bootstrap-v1\n", "utf8");
var MAX_SOURCE_BLOB_BYTES = 50 * 1024 * 1024;
var MAX_SOURCE_TOTAL_BYTES = 100 * 1024 * 1024;
var MAX_COMMAND_OUTPUT_BYTES = 128 * 1024 * 1024;

// lib/providers/plan-helpers.ts
function stableHash(value) {
  const normalized = JSON.stringify(value, (_key, item) => {
    if (item && typeof item === "object" && !Array.isArray(item)) {
      return Object.fromEntries(
        Object.entries(item).sort(([a], [b]) => a.localeCompare(b))
      );
    }
    return item;
  });
  let hash = 2166136261;
  for (let index = 0; index < normalized.length; index += 1) {
    hash ^= normalized.charCodeAt(index);
    hash = Math.imul(hash, 16777619);
  }
  return (hash >>> 0).toString(36);
}
function inputString(request2, key) {
  const value = request2.inputs[key];
  if (typeof value !== "string" || value.trim() === "") {
    throw new ProviderPlanError(`Missing string input: ${key}`, "missing_input");
  }
  return value;
}
function credentialInput(request2, key) {
  const value = key ? inputString(request2, key) : request2.credentialRef;
  if (typeof value !== "string" || !value.startsWith("cred://")) {
    throw new ProviderPlanError(
      `Missing credential reference${key ? `: ${key}` : ""}`,
      "missing_input"
    );
  }
  return value;
}
function optionalString(request2, key) {
  const value = request2.inputs[key];
  if (value === void 0) return void 0;
  if (typeof value !== "string" || value.trim() === "") {
    throw new ProviderPlanError(`Invalid string input: ${key}`, "invalid_input");
  }
  return value;
}
function inputNumber(request2, key) {
  const value = request2.inputs[key];
  if (typeof value !== "number" || !Number.isFinite(value)) {
    throw new ProviderPlanError(`Missing number input: ${key}`, "missing_input");
  }
  return value;
}
function optionalNumber(request2, key) {
  const value = request2.inputs[key];
  if (value === void 0) return void 0;
  if (typeof value !== "number" || !Number.isFinite(value)) {
    throw new ProviderPlanError(`Invalid number input: ${key}`, "invalid_input");
  }
  return value;
}
function inputBoolean(request2, key, fallback) {
  const value = request2.inputs[key];
  if (value === void 0) return fallback;
  if (typeof value !== "boolean") {
    throw new ProviderPlanError(`Invalid boolean input: ${key}`, "invalid_input");
  }
  return value;
}
function inputStrings(request2, key) {
  const value = request2.inputs[key];
  if (!Array.isArray(value) || !value.every((item) => typeof item === "string")) {
    throw new ProviderPlanError(`Missing string array input: ${key}`, "missing_input");
  }
  return [...value];
}
function hasCapability(request2, capability) {
  return request2.capabilities.includes(capability);
}
function operationId(provider, action, identity) {
  return `${provider}.${action}.${stableHash(identity)}`;
}
function idempotencyKey(provider, environment, action, payload) {
  return `${provider}:${environment}:${action}:${stableHash(payload)}`;
}
function createPlan(provider, request2, operations, limitations = []) {
  const identity = {
    provider,
    environment: request2.environment,
    capabilities: [...request2.capabilities].sort(),
    inputs: request2.inputs
  };
  return {
    id: `plan.${provider}.${stableHash(identity)}`,
    provider,
    environment: request2.environment,
    dryRun: request2.dryRun ?? true,
    createdAt: (/* @__PURE__ */ new Date()).toISOString(),
    operations,
    limitations
  };
}

// lib/providers/plans.ts
function operation(request2, input) {
  const id = operationId(input.provider, input.action, {
    environment: request2.environment,
    identity: input.identity
  });
  return {
    id,
    provider: input.provider,
    capability: input.capability,
    action: input.action,
    title: input.title,
    transport: input.command ? "cli" : input.http ? "http" : "manual",
    environment: request2.environment,
    riskClass: input.riskClass,
    effectClass: input.effectClass,
    reversibility: input.reversibility,
    credentialRef: input.credentialRef,
    reconcileOnReplay: input.reconcileOnReplay,
    idempotencyKey: idempotencyKey(input.provider, request2.environment, input.action, {
      identity: input.identity,
      command: input.command,
      http: input.http,
      manual: input.manual
    }),
    dependsOn: input.dependsOn ?? [],
    command: input.command,
    http: input.http,
    manual: input.manual,
    readBack: input.readBack,
    verification: input.verification,
    estimatedCost: input.estimatedCost,
    emailRecipientCount: input.emailRecipientCount
  };
}
function encoded(value) {
  return encodeURIComponent(value);
}
function buildGitHubPlan(request2) {
  const operations = [];
  if (hasCapability(request2, "repository")) {
    const repository = inputString(request2, "repository");
    const visibility = optionalString(request2, "visibility") ?? "private";
    const sourceDirectory = optionalString(request2, "sourceDirectory") ?? ".";
    if (!["private", "public", "internal"].includes(visibility)) {
      throw new Error(`Unsupported GitHub repository visibility: ${visibility}`);
    }
    operations.push(
      operation(request2, {
        provider: "github",
        capability: "repository",
        action: "repository.create_from_source",
        title: `Publish the verified local source tree to ${repository}`,
        identity: { repository, sourceDirectory, visibility },
        riskClass: "high",
        effectClass: "reversible_external",
        reversibility: "conditionally_reversible",
        credentialRef: request2.credentialRef,
        reconcileOnReplay: true,
        command: {
          binary: "node",
          args: [
            "--import",
            "tsx",
            "scripts/github-publish-source.ts",
            "apply",
            "--repository",
            repository,
            "--visibility",
            visibility
          ],
          cwd: sourceDirectory
        },
        readBack: {
          transport: "cli",
          command: {
            binary: "node",
            args: [
              "--import",
              "tsx",
              "scripts/github-publish-source.ts",
              "verify",
              "--repository",
              repository,
              "--visibility",
              visibility,
              "--branch",
              "{result.branch}",
              "--commit",
              "{result.commitOid}",
              "--tree",
              "{result.treeOid}"
            ],
            cwd: sourceDirectory
          },
          description: "GitHub returned the exact repository, visibility, default branch, commit, and source tree",
          assertions: [
            { path: "verified", operator: "equals", expected: true },
            { path: "repository", operator: "equals", expected: repository },
            { path: "visibility", operator: "equals", expected: visibility },
            { path: "branch", operator: "equals", expected: "{result.branch}" },
            { path: "commitOid", operator: "equals", expected: "{result.commitOid}" },
            { path: "treeOid", operator: "equals", expected: "{result.treeOid}" }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Read the remote default branch and exact commit/tree back after source publication"
        }
      })
    );
  }
  if (hasCapability(request2, "actions_secret")) {
    const repository = inputString(request2, "repository");
    const secretName = inputString(request2, "secretName");
    const secretCredentialRef = credentialInput(request2, "secretCredentialRef");
    operations.push(
      operation(request2, {
        provider: "github",
        capability: "actions_secret",
        action: "actions_secret.set",
        title: `Set Actions secret metadata ${secretName}`,
        identity: { repository, secretName },
        riskClass: "critical",
        effectClass: "reversible_external",
        reversibility: "conditionally_reversible",
        credentialRef: request2.credentialRef,
        command: {
          binary: "gh",
          args: ["secret", "set", secretName, "--repo", repository],
          stdinCredentialRef: secretCredentialRef
        },
        readBack: {
          transport: "cli",
          command: {
            binary: "gh",
            args: ["secret", "list", "--repo", repository, "--json", "name,updatedAt"]
          },
          description: "GitHub lists the secret name and timestamp; the value remains unreadable",
          assertions: [{ path: "", operator: "contains", expected: { name: secretName } }]
        },
        verification: {
          strategy: "read_back",
          description: "Verify write-only secret metadata, never its value"
        }
      })
    );
  }
  if (hasCapability(request2, "repository_settings")) {
    const repository = inputString(request2, "repository");
    const deleteBranchOnMerge = inputBoolean(request2, "deleteBranchOnMerge", true);
    operations.push(
      operation(request2, {
        provider: "github",
        capability: "repository_settings",
        action: "repository.settings.update",
        title: `Update merge settings for ${repository}`,
        identity: repository,
        riskClass: "medium",
        effectClass: "reversible_external",
        reversibility: "reversible",
        credentialRef: request2.credentialRef,
        command: {
          binary: "gh",
          args: [
            "api",
            `repos/${repository}`,
            "--method",
            "PATCH",
            "-F",
            `delete_branch_on_merge=${deleteBranchOnMerge}`
          ]
        },
        readBack: {
          transport: "cli",
          command: {
            binary: "gh",
            args: ["api", `repos/${repository}`]
          },
          description: "GitHub repository settings are readable after the update",
          assertions: [
            {
              path: "delete_branch_on_merge",
              operator: "equals",
              expected: deleteBranchOnMerge
            }
          ]
        },
        verification: {
          strategy: "read_back",
          description: "Compare delete_branch_on_merge with the requested value"
        }
      })
    );
  }
  if (hasCapability(request2, "draft_pull_request")) {
    const repository = inputString(request2, "repository");
    const base = optionalString(request2, "baseBranch") ?? "main";
    const head = inputString(request2, "headBranch");
    const title = inputString(request2, "pullRequestTitle");
    const body = inputString(request2, "pullRequestBody");
    operations.push(
      operation(request2, {
        provider: "github",
        capability: "draft_pull_request",
        action: "pull_request.create_draft",
        title: `Open draft pull request from ${head}`,
        identity: { repository, base, head },
        riskClass: "medium",
        effectClass: "reversible_external",
        reversibility: "reversible",
        credentialRef: request2.credentialRef,
        command: {
          binary: "gh",
          args: [
            "pr",
            "create",
            "--repo",
            repository,
            "--draft",
            "--base",
            base,
            "--head",
            head,
            "--title",
            title,
            "--body",
            body
          ]
        },
        readBack: {
          transport: "cli",
          command: {
            binary: "gh",
            args: [
              "pr",
              "view",
              head,
              "--repo",
              repository,
              "--json",
              "url,isDraft,baseRefName,headRefName,state"
            ]
          },
          description: "GitHub returned the draft pull request and branch metadata",
          assertions: [
            { path: "isDraft", operator: "equals", expected: true },
            { path: "baseRefName", operator: "equals", expected: base },
            { path: "headRefName", operator: "equals", expected: head }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Confirm the PR exists, remains a draft, and targets the right base"
        }
      })
    );
  }
  return createPlan("github", request2, operations, [
    "Actions secret verification is metadata-only."
  ]);
}
function buildVercelPlan(request2) {
  const operations = [];
  if (hasCapability(request2, "project")) {
    const project = inputString(request2, "project");
    const scope = optionalString(request2, "scope");
    const projectIntent = optionalString(request2, "projectIntent") ?? "use_verified";
    if (!["create", "use_verified"].includes(projectIntent)) {
      throw new Error(`Unsupported Vercel project intent: ${projectIntent}`);
    }
    let createOperationId;
    if (projectIntent === "create") {
      const createArgs = ["project", "add", project];
      if (scope) createArgs.push("--scope", scope);
      const createProject = operation(request2, {
        provider: "vercel",
        capability: "project",
        action: "project.create",
        title: `Create Vercel project ${project}`,
        identity: { project, scope },
        riskClass: "high",
        effectClass: "reversible_external",
        reversibility: "conditionally_reversible",
        credentialRef: request2.credentialRef,
        command: { binary: "vercel", args: createArgs },
        readBack: {
          transport: "cli",
          command: {
            binary: "vercel",
            args: ["project", "inspect", project, ...scope ? ["--scope", scope] : []]
          },
          description: "Vercel returned the newly created project metadata",
          assertions: [{ path: "", operator: "contains", expected: project }]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Create the named project, then inspect it in the explicit account scope"
        }
      });
      createOperationId = createProject.id;
      operations.push(createProject);
    }
    const args = ["link", "--yes", "--project", project];
    if (scope) args.push("--scope", scope);
    operations.push(
      operation(request2, {
        provider: "vercel",
        capability: "project",
        action: "project.link",
        title: `Link the workspace to Vercel project ${project}`,
        identity: { project, scope, projectIntent },
        riskClass: "high",
        effectClass: "local_write",
        reversibility: "reversible",
        credentialRef: request2.credentialRef,
        dependsOn: createOperationId ? [createOperationId] : [],
        command: { binary: "vercel", args },
        readBack: {
          transport: "cli",
          command: {
            binary: "vercel",
            args: ["project", "inspect", project, ...scope ? ["--scope", scope] : []]
          },
          description: "Vercel returned the linked project metadata",
          assertions: [{ path: "", operator: "contains", expected: project }]
        },
        verification: {
          strategy: "read_back",
          description: "Inspect the project and compare its name and scope"
        }
      })
    );
  }
  if (hasCapability(request2, "environment_variable")) {
    const project = inputString(request2, "project");
    const name = inputString(request2, "environmentVariableName");
    const target = optionalString(request2, "environmentTarget") ?? "production";
    const valueRef = credentialInput(request2, "environmentValueCredentialRef");
    operations.push(
      operation(request2, {
        provider: "vercel",
        capability: "environment_variable",
        action: "environment_variable.set",
        title: `Set Vercel environment variable ${name}`,
        identity: { project, name, target },
        riskClass: "critical",
        effectClass: "reversible_external",
        reversibility: "conditionally_reversible",
        credentialRef: request2.credentialRef,
        command: {
          binary: "vercel",
          args: ["env", "add", name, target, "--force"],
          stdinCredentialRef: valueRef
        },
        readBack: {
          transport: "cli",
          command: { binary: "vercel", args: ["env", "ls", target] },
          description: "Vercel lists the variable name and target; its value remains unreadable",
          assertions: [{ path: "", operator: "contains", expected: name }]
        },
        verification: {
          strategy: "read_back",
          description: "Verify variable metadata without exposing its value"
        }
      })
    );
  }
  if (hasCapability(request2, "deployment")) {
    const production = request2.environment === "production";
    const project = optionalString(request2, "project");
    const scope = optionalString(request2, "scope");
    const args = ["deploy", "--yes", "--format=json"];
    if (project) args.push("--project", project);
    if (scope) args.push("--scope", scope);
    if (production) args.push("--prod");
    const projectDependency = [...operations].reverse().find(({ capability }) => capability === "project")?.id;
    operations.push(
      operation(request2, {
        provider: "vercel",
        capability: "deployment",
        action: production ? "deployment.production" : "deployment.preview",
        title: `Create a ${production ? "production" : "preview"} deployment`,
        identity: { project, scope, production },
        riskClass: production ? "critical" : "high",
        effectClass: "reversible_external",
        reversibility: "conditionally_reversible",
        credentialRef: request2.credentialRef,
        dependsOn: projectDependency ? [projectDependency] : [],
        command: { binary: "vercel", args },
        readBack: {
          transport: "cli",
          command: {
            binary: "vercel",
            args: [
              "inspect",
              "{result.url}",
              "--wait",
              "--format=json",
              ...scope ? ["--scope", scope] : []
            ]
          },
          description: "Vercel inspection reached a terminal deployment state",
          assertions: [
            { path: "id", operator: "equals", expected: "{result.id}" },
            { path: "readyState", operator: "equals", expected: "READY" }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Inspect the returned deployment URL and require a ready state"
        }
      })
    );
  }
  if (hasCapability(request2, "domain")) {
    const domain = inputString(request2, "domain");
    const project = inputString(request2, "project");
    operations.push(
      operation(request2, {
        provider: "vercel",
        capability: "domain",
        action: "domain.add",
        title: `Attach ${domain} to ${project}`,
        identity: { domain, project },
        riskClass: "critical",
        effectClass: "reversible_external",
        reversibility: "conditionally_reversible",
        credentialRef: request2.credentialRef,
        command: {
          binary: "vercel",
          args: ["domains", "add", domain, project]
        },
        readBack: {
          transport: "cli",
          command: { binary: "vercel", args: ["domains", "inspect", domain] },
          description: "Vercel returned domain ownership and configuration state",
          assertions: [{ path: "", operator: "contains", expected: domain }]
        },
        verification: {
          strategy: "read_back",
          description: "Inspect the domain and keep DNS configuration pending if needed"
        }
      })
    );
  }
  if (hasCapability(request2, "web_analytics")) {
    const project = inputString(request2, "project");
    const scope = optionalString(request2, "scope");
    operations.push(
      operation(request2, {
        provider: "vercel",
        capability: "web_analytics",
        action: "web_analytics.enable_manual",
        title: `Enable Web Analytics for ${project}`,
        identity: { project, scope },
        riskClass: "medium",
        effectClass: "manual",
        reversibility: "manual",
        manual: {
          system: "Vercel dashboard",
          url: `https://vercel.com/${scope ?? "dashboard"}/${project}/analytics`,
          instructions: [
            `Open Web Analytics for the exact project ${project} in scope ${scope ?? "the reviewed account"}.`,
            "Enable Web Analytics in the dashboard; do not enable a paid Analytics Plus add-on unless separately authorized.",
            "Deploy the repository only after its consent-aware analytics integration has passed local checks.",
            "Visit the verified deployment and confirm the analytics script request succeeds without private form or user-content fields.",
            "Read the project Analytics page back and keep collection readiness distinct from observed traffic data."
          ],
          requiredFields: { project, ...scope ? { scope } : {} },
          completionEvidence: [
            "Vercel project Analytics page showing Web Analytics enabled",
            "Verified deployment URL and successful analytics script request",
            "Consent and analytics-PII quality evidence for the deployed release"
          ]
        },
        verification: {
          strategy: "manual",
          description: "Require dashboard and deployed-script read-back; enabling a control is not evidence that traffic was collected"
        }
      })
    );
  }
  return createPlan("vercel", request2, operations, [
    "Deployment and domain readiness require read-back; command acceptance is not success.",
    "Web Analytics enablement stays a declared manual action because the documented setup uses the Vercel dashboard."
  ]);
}
function neonAuth(request2) {
  return {
    name: "NEON_API_KEY",
    credentialRef: credentialInput(request2)
  };
}
var neonMigrationVersion = "001_core_evidence";
var neonMigrationPath = "migrations/sql/001_core_evidence.up.sql";
var neonManagedTables = [
  "vh_schema_migrations",
  "experiment_events",
  "commercial_events",
  "submissions",
  "consent_events",
  "product_events",
  "provider_webhook_events",
  "analytics_sync_runs"
];
var neonManagedConstraints = [
  "commercial_events_attribution_object",
  "submissions_payload_object",
  "product_events_props_object",
  "provider_webhook_status",
  "analytics_sync_window",
  "analytics_sync_row_count",
  "analytics_sync_quality",
  "analytics_sync_dimensions_array",
  "analytics_sync_limitations_array"
];
var neonSchemaEvidence = [
  `migration:${neonMigrationVersion}`,
  ...neonManagedTables.map((table) => `table:${table}`),
  ...neonManagedConstraints.map((constraint) => `constraint:${constraint}`)
];
var neonSchemaReadBackSql = [
  `select 'migration:' || version from vh_schema_migrations where version = '${neonMigrationVersion}'`,
  `union all select 'table:' || table_name from information_schema.tables where table_schema = 'public' and table_name in (${neonManagedTables.map((table) => `'${table}'`).join(", ")})`,
  `union all select 'constraint:' || conname from pg_constraint where connamespace = 'public'::regnamespace and conname in (${neonManagedConstraints.map((constraint) => `'${constraint}'`).join(", ")});`
].join(" ");
var neonHealthEvidence = "vh_read_write_ok";
var neonReadWriteHealthSql = [
  "begin;",
  "create temporary table vh_provider_health_check (probe text primary key) on commit drop;",
  `insert into vh_provider_health_check (probe) values ('${neonHealthEvidence}');`,
  `select probe from vh_provider_health_check where probe = '${neonHealthEvidence}';`,
  "rollback;"
].join(" ");
function neonDatabaseAuth(databaseCredentialRef) {
  return {
    name: "PGDATABASE",
    credentialRef: databaseCredentialRef
  };
}
function buildNeonPlan(request2) {
  const operations = [];
  const workingDirectory = optionalString(request2, "workingDirectory");
  if (hasCapability(request2, "project")) {
    const organizationId = inputString(request2, "organizationId");
    const name = inputString(request2, "projectName");
    const region = inputString(request2, "regionId");
    const databaseCredentialRef = request2.inputs.databaseCredentialRef ? credentialInput(request2, "databaseCredentialRef") : void 0;
    operations.push(
      operation(request2, {
        provider: "neon",
        capability: "project",
        action: "project.create",
        title: `Create Neon project ${name}`,
        identity: { organizationId, name, region },
        riskClass: "critical",
        effectClass: "reversible_external",
        reversibility: "conditionally_reversible",
        credentialRef: request2.credentialRef,
        command: {
          binary: "neonctl",
          args: [
            "projects",
            "create",
            "--org-id",
            organizationId,
            "--name",
            name,
            "--region-id",
            region,
            "--output",
            "json"
          ],
          authEnvironment: neonAuth(request2),
          ...databaseCredentialRef ? {
            captureCredential: {
              credentialRef: databaseCredentialRef,
              outputPath: "connection_uris.0.connection_uri"
            }
          } : {}
        },
        readBack: {
          transport: "cli",
          command: {
            binary: "neonctl",
            args: ["projects", "get", "{result.project.id}", "--output", "json"],
            authEnvironment: neonAuth(request2)
          },
          description: "Neon returned the new project identity and region",
          assertions: [
            { path: "org_id", operator: "equals", expected: organizationId },
            { path: "name", operator: "equals", expected: name },
            { path: "region_id", operator: "equals", expected: region }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Read the project by returned id and compare name and region"
        }
      })
    );
  }
  if (hasCapability(request2, "branch")) {
    const projectId = inputString(request2, "projectId");
    const name = inputString(request2, "branchName");
    operations.push(
      operation(request2, {
        provider: "neon",
        capability: "branch",
        action: "branch.create",
        title: `Create Neon branch ${name}`,
        identity: { projectId, name },
        riskClass: "high",
        effectClass: "reversible_external",
        reversibility: "reversible",
        credentialRef: request2.credentialRef,
        command: {
          binary: "neonctl",
          args: [
            "branches",
            "create",
            "--project-id",
            projectId,
            "--name",
            name,
            "--output",
            "json"
          ],
          authEnvironment: neonAuth(request2)
        },
        readBack: {
          transport: "cli",
          command: {
            binary: "neonctl",
            args: [
              "branches",
              "get",
              "{result.branch.id}",
              "--project-id",
              projectId,
              "--output",
              "json"
            ],
            authEnvironment: neonAuth(request2)
          },
          description: "Neon returned the branch by id",
          assertions: [{ path: "name", operator: "equals", expected: name }]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Read the branch and compare its project and name"
        }
      })
    );
  }
  if (hasCapability(request2, "database")) {
    const projectId = inputString(request2, "projectId");
    const branchId = inputString(request2, "branchId");
    const name = inputString(request2, "databaseName");
    operations.push(
      operation(request2, {
        provider: "neon",
        capability: "database",
        action: "database.create",
        title: `Create Neon database ${name}`,
        identity: { projectId, branchId, name },
        riskClass: "critical",
        effectClass: "reversible_external",
        reversibility: "conditionally_reversible",
        credentialRef: request2.credentialRef,
        command: {
          binary: "neonctl",
          args: [
            "databases",
            "create",
            "--project-id",
            projectId,
            "--branch-id",
            branchId,
            "--name",
            name,
            "--output",
            "json"
          ],
          authEnvironment: neonAuth(request2)
        },
        readBack: {
          transport: "cli",
          command: {
            binary: "neonctl",
            args: [
              "databases",
              "list",
              "--project-id",
              projectId,
              "--branch-id",
              branchId,
              "--output",
              "json"
            ],
            authEnvironment: neonAuth(request2)
          },
          description: "Neon returned database metadata without a connection string",
          assertions: [{ path: "", operator: "contains", expected: { name } }]
        },
        verification: {
          strategy: "read_back",
          description: "Read database identity by project, branch, and name"
        }
      })
    );
  }
  if (hasCapability(request2, "role")) {
    const projectId = inputString(request2, "projectId");
    const branchId = inputString(request2, "branchId");
    const name = inputString(request2, "roleName");
    operations.push(
      operation(request2, {
        provider: "neon",
        capability: "role",
        action: "role.create",
        title: `Create Neon role ${name}`,
        identity: { projectId, branchId, name },
        riskClass: "critical",
        effectClass: "reversible_external",
        reversibility: "conditionally_reversible",
        credentialRef: request2.credentialRef,
        command: {
          binary: "neonctl",
          args: [
            "roles",
            "create",
            "--project-id",
            projectId,
            "--branch-id",
            branchId,
            "--name",
            name,
            "--output",
            "json"
          ],
          authEnvironment: neonAuth(request2)
        },
        readBack: {
          transport: "cli",
          command: {
            binary: "neonctl",
            args: [
              "roles",
              "list",
              "--project-id",
              projectId,
              "--branch-id",
              branchId,
              "--output",
              "json"
            ],
            authEnvironment: neonAuth(request2)
          },
          description: "Neon returned role metadata; password material stays redacted",
          assertions: [{ path: "", operator: "contains", expected: { name } }]
        },
        verification: {
          strategy: "read_back",
          description: "Read role metadata without emitting generated credentials"
        }
      })
    );
  }
  const databaseOperationId = operations.find(({ capability }) => capability === "database")?.id;
  const projectOperationId = operations.find(({ capability }) => capability === "project")?.id;
  let migrationOperationId;
  if (hasCapability(request2, "schema_migration")) {
    const databaseCredentialRef = credentialInput(request2, "databaseCredentialRef");
    const schemaMigration = operation(request2, {
      provider: "neon",
      capability: "schema_migration",
      action: "schema.migrate",
      title: `Apply database migration ${neonMigrationVersion}`,
      identity: { version: neonMigrationVersion, path: neonMigrationPath },
      riskClass: "critical",
      effectClass: "reversible_external",
      reversibility: "conditionally_reversible",
      credentialRef: databaseCredentialRef,
      dependsOn: databaseOperationId ? [databaseOperationId] : projectOperationId ? [projectOperationId] : [],
      command: {
        binary: "psql",
        args: ["--no-psqlrc", "--set=ON_ERROR_STOP=1", "--file", neonMigrationPath],
        authEnvironment: neonDatabaseAuth(databaseCredentialRef),
        ...workingDirectory ? { cwd: workingDirectory } : {}
      },
      readBack: {
        transport: "cli",
        command: {
          binary: "psql",
          args: [
            "--no-psqlrc",
            "--set=ON_ERROR_STOP=1",
            "--tuples-only",
            "--no-align",
            "--command",
            neonSchemaReadBackSql
          ],
          authEnvironment: neonDatabaseAuth(databaseCredentialRef),
          ...workingDirectory ? { cwd: workingDirectory } : {}
        },
        description: "PostgreSQL returned the migration ledger entry, managed tables, and named constraints",
        assertions: neonSchemaEvidence.map((expected) => ({
          path: "",
          operator: "contains",
          expected
        }))
      },
      verification: {
        strategy: "read_back",
        description: "Read back the versioned migration ledger, every managed table, and every named invariant constraint"
      }
    });
    migrationOperationId = schemaMigration.id;
    operations.push(schemaMigration);
  }
  if (hasCapability(request2, "read_write_health_check")) {
    const databaseCredentialRef = credentialInput(request2, "databaseCredentialRef");
    const dependency = migrationOperationId ?? databaseOperationId ?? projectOperationId;
    operations.push(
      operation(request2, {
        provider: "neon",
        capability: "read_write_health_check",
        action: "database.read_write_health_check",
        title: "Verify disposable database read/write access",
        identity: neonHealthEvidence,
        riskClass: "high",
        effectClass: "reversible_external",
        reversibility: "reversible",
        credentialRef: databaseCredentialRef,
        dependsOn: dependency ? [dependency] : [],
        command: {
          binary: "psql",
          args: [
            "--no-psqlrc",
            "--set=ON_ERROR_STOP=1",
            "--tuples-only",
            "--no-align",
            "--command",
            neonReadWriteHealthSql
          ],
          authEnvironment: neonDatabaseAuth(databaseCredentialRef),
          ...workingDirectory ? { cwd: workingDirectory } : {}
        },
        readBack: {
          transport: "cli",
          command: {
            binary: "psql",
            args: [
              "--no-psqlrc",
              "--set=ON_ERROR_STOP=1",
              "--tuples-only",
              "--no-align",
              "--command",
              neonReadWriteHealthSql
            ],
            authEnvironment: neonDatabaseAuth(databaseCredentialRef),
            ...workingDirectory ? { cwd: workingDirectory } : {}
          },
          description: "A fresh transactional write/read probe returned the expected sentinel",
          assertions: [{ path: "", operator: "contains", expected: neonHealthEvidence }]
        },
        verification: {
          strategy: "read_back",
          description: "Repeat a temporary-table write/read probe and roll it back without retaining probe data"
        }
      })
    );
  }
  return createPlan("neon", request2, operations, [
    "Neon project creation is bound to inputs.organizationId and passes it explicitly to neonctl as --org-id; project read-back verifies the returned org_id.",
    "Neon resource provisioning uses only the API-key credential reference.",
    "Database connections are accepted only through inputs.databaseCredentialRef and are injected into psql through PGDATABASE, never argv or plan artifacts.",
    "For project creation, the command transport captures connection_uris[0].connection_uri directly into an already-registered writable databaseCredentialRef and redacts it before returning output.",
    "Generated role passwords in command output are redacted and are not stored as databaseCredentialRef."
  ]);
}
function stripeAuth(request2) {
  return { scheme: "basic", credentialRef: credentialInput(request2) };
}
function buildStripePlan(request2) {
  const operations = [];
  let productOperationId;
  if (hasCapability(request2, "product")) {
    const name = inputString(request2, "productName");
    const description = optionalString(request2, "productDescription");
    const body = { name };
    if (description) body.description = description;
    const product = operation(request2, {
      provider: "stripe",
      capability: "product",
      action: "product.create",
      title: `Create Stripe product ${name}`,
      identity: name,
      riskClass: "high",
      effectClass: "financial",
      reversibility: "conditionally_reversible",
      credentialRef: request2.credentialRef,
      http: {
        method: "POST",
        url: "https://api.stripe.com/v1/products",
        body,
        encoding: "form",
        auth: stripeAuth(request2),
        nativeIdempotency: true
      },
      readBack: {
        transport: "http",
        http: {
          method: "GET",
          url: "https://api.stripe.com/v1/products/{result.id}",
          auth: stripeAuth(request2)
        },
        description: "Stripe returned the created product by id",
        assertions: [
          { path: "name", operator: "equals", expected: name },
          { path: "id", operator: "exists" }
        ]
      },
      verification: {
        strategy: "response_then_read_back",
        description: "Compare product name, livemode, and active state"
      }
    });
    productOperationId = product.id;
    operations.push(product);
  }
  if (hasCapability(request2, "price")) {
    const productId = inputString(request2, "productId");
    const currency = inputString(request2, "currency").toLowerCase();
    const unitAmount = inputNumber(request2, "unitAmount");
    if (!Number.isInteger(unitAmount) || unitAmount < 0) {
      throw new Error("Stripe unitAmount must be a non-negative integer in minor units");
    }
    const interval = optionalString(request2, "recurringInterval");
    const body = {
      product: productId,
      currency,
      unit_amount: unitAmount
    };
    if (interval) body.recurring = { interval };
    operations.push(
      operation(request2, {
        provider: "stripe",
        capability: "price",
        action: "price.create",
        title: `Create ${currency.toUpperCase()} ${unitAmount} Stripe price`,
        identity: { productId, currency, unitAmount, interval },
        riskClass: "critical",
        effectClass: "financial",
        reversibility: "irreversible",
        credentialRef: request2.credentialRef,
        dependsOn: productOperationId ? [productOperationId] : [],
        http: {
          method: "POST",
          url: "https://api.stripe.com/v1/prices",
          body,
          encoding: "form",
          auth: stripeAuth(request2),
          nativeIdempotency: true
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: "https://api.stripe.com/v1/prices/{result.id}",
            auth: stripeAuth(request2)
          },
          description: "Stripe returned the immutable price by id",
          assertions: [
            { path: "product", operator: "equals", expected: productId },
            { path: "currency", operator: "equals", expected: currency },
            { path: "unit_amount", operator: "equals", expected: unitAmount }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Compare exact minor-unit amount, currency, product, and interval"
        }
      })
    );
  }
  if (hasCapability(request2, "webhook")) {
    const url = inputString(request2, "webhookUrl");
    const enabledEvents = inputStrings(request2, "enabledEvents");
    const webhookSecretCredentialRef = credentialInput(request2, "webhookSecretCredentialRef");
    operations.push(
      operation(request2, {
        provider: "stripe",
        capability: "webhook",
        action: "webhook_endpoint.create",
        title: `Create Stripe webhook endpoint ${url}`,
        identity: { url, enabledEvents: [...enabledEvents].sort() },
        riskClass: "critical",
        effectClass: "communication",
        reversibility: "reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "POST",
          url: "https://api.stripe.com/v1/webhook_endpoints",
          body: { url, enabled_events: enabledEvents },
          encoding: "form",
          auth: stripeAuth(request2),
          nativeIdempotency: true,
          captureCredential: {
            credentialRef: webhookSecretCredentialRef,
            outputPath: "secret"
          }
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: "https://api.stripe.com/v1/webhook_endpoints/{result.id}",
            auth: stripeAuth(request2)
          },
          description: "Stripe returned the webhook endpoint metadata",
          assertions: [
            { path: "url", operator: "equals", expected: url },
            {
              path: "enabled_events",
              operator: "contains",
              expected: enabledEvents
            }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Compare URL, status, livemode, and enabled events; redact secret"
        }
      })
    );
  }
  if (hasCapability(request2, "billing_portal")) {
    const headline = optionalString(request2, "headline");
    const body = {};
    if (headline) body.business_profile = { headline };
    operations.push(
      operation(request2, {
        provider: "stripe",
        capability: "billing_portal",
        action: "billing_portal.configuration.create",
        title: "Create Stripe billing portal configuration",
        identity: body,
        riskClass: "high",
        effectClass: "financial",
        reversibility: "conditionally_reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "POST",
          url: "https://api.stripe.com/v1/billing_portal/configurations",
          body,
          encoding: "form",
          auth: stripeAuth(request2),
          nativeIdempotency: true
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: "https://api.stripe.com/v1/billing_portal/configurations/{result.id}",
            auth: stripeAuth(request2)
          },
          description: "Stripe returned the portal configuration by id",
          assertions: headline ? [
            {
              path: "business_profile.headline",
              operator: "equals",
              expected: headline
            }
          ] : [{ path: "id", operator: "exists" }]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Compare portal configuration and active/default status"
        }
      })
    );
  }
  return createPlan("stripe", request2, operations, [
    "Prices cannot be corrected in place; create a new price when exact values differ."
  ]);
}
function revenueCatAuth(request2) {
  return { scheme: "bearer", credentialRef: credentialInput(request2) };
}
function buildRevenueCatPlan(request2) {
  const operations = [];
  if (hasCapability(request2, "project_bootstrap")) {
    const projectName = inputString(request2, "projectName");
    operations.push(
      operation(request2, {
        provider: "revenuecat",
        capability: "project_bootstrap",
        action: "project.bootstrap_manual",
        title: `Create RevenueCat project ${projectName} and restricted key`,
        identity: projectName,
        riskClass: "critical",
        effectClass: "manual",
        reversibility: "manual",
        manual: {
          system: "RevenueCat dashboard",
          url: "https://app.revenuecat.com/",
          instructions: [
            "Create the project in the RevenueCat dashboard.",
            "Create a secret v2 API key with only the configuration permissions required by this plan.",
            "Store the key in a credential backend and record only its cred:// reference.",
            "Record the returned project id for subsequent API operations."
          ],
          requiredFields: { projectName },
          completionEvidence: [
            "RevenueCat project id",
            "Credential reference (never the key value)",
            "Declared key permissions"
          ]
        },
        verification: {
          strategy: "manual",
          description: "A human records the project id and tested credential reference"
        }
      })
    );
  }
  if (hasCapability(request2, "app")) {
    const projectId = inputString(request2, "projectId");
    const name = inputString(request2, "appName");
    const type = inputString(request2, "appType");
    const bundleId = inputString(request2, "bundleId");
    operations.push(
      operation(request2, {
        provider: "revenuecat",
        capability: "app",
        action: "app.create",
        title: `Create RevenueCat app ${name}`,
        identity: { projectId, name, type, bundleId },
        riskClass: "critical",
        effectClass: "financial",
        reversibility: "conditionally_reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "POST",
          url: `https://api.revenuecat.com/v2/projects/${encoded(projectId)}/apps`,
          body: { name, type, app_store: { bundle_id: bundleId } },
          auth: revenueCatAuth(request2)
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: `https://api.revenuecat.com/v2/projects/${encoded(projectId)}/apps/{result.id}`,
            auth: revenueCatAuth(request2)
          },
          description: "RevenueCat returned the app by project and id",
          assertions: [
            { path: "name", operator: "equals", expected: name },
            { path: "type", operator: "equals", expected: type },
            {
              path: "app_store.bundle_id",
              operator: "equals",
              expected: bundleId
            }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Compare app name, platform type, and bundle identifier"
        }
      })
    );
  }
  if (hasCapability(request2, "entitlement")) {
    const projectId = inputString(request2, "projectId");
    const lookupKey = inputString(request2, "entitlementLookupKey");
    const displayName = inputString(request2, "entitlementDisplayName");
    operations.push(
      operation(request2, {
        provider: "revenuecat",
        capability: "entitlement",
        action: "entitlement.create",
        title: `Create RevenueCat entitlement ${lookupKey}`,
        identity: { projectId, lookupKey },
        riskClass: "critical",
        effectClass: "financial",
        reversibility: "conditionally_reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "POST",
          url: `https://api.revenuecat.com/v2/projects/${encoded(projectId)}/entitlements`,
          body: { lookup_key: lookupKey, display_name: displayName },
          auth: revenueCatAuth(request2)
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: `https://api.revenuecat.com/v2/projects/${encoded(projectId)}/entitlements/{result.id}`,
            auth: revenueCatAuth(request2)
          },
          description: "RevenueCat returned the entitlement by id",
          assertions: [
            { path: "lookup_key", operator: "equals", expected: lookupKey },
            { path: "display_name", operator: "equals", expected: displayName }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Compare lookup key and display name"
        }
      })
    );
  }
  if (hasCapability(request2, "offering")) {
    const projectId = inputString(request2, "projectId");
    const lookupKey = inputString(request2, "offeringLookupKey");
    const displayName = inputString(request2, "offeringDisplayName");
    operations.push(
      operation(request2, {
        provider: "revenuecat",
        capability: "offering",
        action: "offering.create",
        title: `Create RevenueCat offering ${lookupKey}`,
        identity: { projectId, lookupKey },
        riskClass: "critical",
        effectClass: "financial",
        reversibility: "conditionally_reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "POST",
          url: `https://api.revenuecat.com/v2/projects/${encoded(projectId)}/offerings`,
          body: { lookup_key: lookupKey, display_name: displayName },
          auth: revenueCatAuth(request2)
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: `https://api.revenuecat.com/v2/projects/${encoded(projectId)}/offerings/{result.id}`,
            auth: revenueCatAuth(request2)
          },
          description: "RevenueCat returned the offering by id",
          assertions: [
            { path: "lookup_key", operator: "equals", expected: lookupKey },
            { path: "display_name", operator: "equals", expected: displayName }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Compare offering lookup key, display name, and current state"
        }
      })
    );
  }
  if (hasCapability(request2, "webhook")) {
    const projectId = inputString(request2, "projectId");
    const name = inputString(request2, "webhookName");
    const url = inputString(request2, "webhookUrl");
    operations.push(
      operation(request2, {
        provider: "revenuecat",
        capability: "webhook",
        action: "webhook.create",
        title: `Create RevenueCat webhook ${name}`,
        identity: { projectId, name, url },
        riskClass: "critical",
        effectClass: "communication",
        reversibility: "reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "POST",
          url: `https://api.revenuecat.com/v2/projects/${encoded(projectId)}/integrations/webhooks`,
          body: { name, url },
          auth: revenueCatAuth(request2)
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: `https://api.revenuecat.com/v2/projects/${encoded(projectId)}/integrations/webhooks/{result.id}`,
            auth: revenueCatAuth(request2)
          },
          description: "RevenueCat returned webhook metadata by id",
          assertions: [
            { path: "name", operator: "equals", expected: name },
            { path: "url", operator: "equals", expected: url }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Compare webhook name, URL, and active state"
        }
      })
    );
  }
  return createPlan("revenuecat", request2, operations, [
    "Project and secret-key creation are dashboard-only prerequisites."
  ]);
}
function brevoAuth(request2) {
  return {
    scheme: "api_key_header",
    credentialRef: credentialInput(request2),
    name: "api-key"
  };
}
function buildBrevoPlan(request2) {
  const operations = [];
  if (hasCapability(request2, "sending_domain")) {
    const domainName = inputString(request2, "domainName");
    operations.push(
      operation(request2, {
        provider: "brevo",
        capability: "sending_domain",
        action: "sending_domain.create",
        title: `Add Brevo sending domain ${domainName}`,
        identity: domainName,
        riskClass: "high",
        effectClass: "communication",
        reversibility: "reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "POST",
          url: "https://api.brevo.com/v3/senders/domains",
          body: { name: domainName },
          auth: brevoAuth(request2)
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: "https://api.brevo.com/v3/senders/domains",
            auth: brevoAuth(request2)
          },
          description: "Brevo returned the domain and authentication state",
          assertions: [
            {
              path: "domains",
              operator: "contains",
              expected: { domain_name: domainName }
            }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Require authenticated DNS state before treating sending as ready"
        }
      })
    );
  }
  if (hasCapability(request2, "sending_domain_verification")) {
    const domainName = inputString(request2, "domainName");
    operations.push(
      operation(request2, {
        provider: "brevo",
        capability: "sending_domain_verification",
        action: "sending_domain.authenticate",
        title: `Authenticate Brevo sending domain ${domainName}`,
        identity: domainName,
        riskClass: "high",
        effectClass: "communication",
        reversibility: "reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "PUT",
          url: `https://api.brevo.com/v3/senders/domains/${encoded(domainName)}/authenticate`,
          auth: brevoAuth(request2)
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: `https://api.brevo.com/v3/senders/domains/${encoded(domainName)}`,
            auth: brevoAuth(request2)
          },
          description: "Brevo returned a verified and authenticated domain configuration",
          assertions: [
            { path: "domain", operator: "equals", expected: domainName },
            { path: "verified", operator: "equals", expected: true },
            { path: "authenticated", operator: "equals", expected: true }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Require verified=true and authenticated=true after the DNS checkpoint"
        }
      })
    );
  }
  if (hasCapability(request2, "sender")) {
    const name = inputString(request2, "senderName");
    const email = inputString(request2, "senderEmail");
    operations.push(
      operation(request2, {
        provider: "brevo",
        capability: "sender",
        action: "sender.create",
        title: `Create Brevo sender ${name}`,
        identity: { name, email },
        riskClass: "high",
        effectClass: "communication",
        reversibility: "reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "POST",
          url: "https://api.brevo.com/v3/senders",
          body: { name, email },
          auth: brevoAuth(request2)
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: "https://api.brevo.com/v3/senders",
            auth: brevoAuth(request2)
          },
          description: "Brevo sender list contains the requested sender",
          assertions: [
            {
              path: "senders",
              operator: "contains",
              expected: { name, email }
            }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Compare sender name, address, and active state"
        }
      })
    );
  }
  if (hasCapability(request2, "template")) {
    const name = inputString(request2, "templateName");
    const subject = inputString(request2, "templateSubject");
    const htmlContent = inputString(request2, "templateHtml");
    operations.push(
      operation(request2, {
        provider: "brevo",
        capability: "template",
        action: "template.create",
        title: `Create Brevo template ${name}`,
        identity: { name, subject, contentHash: htmlContent.length },
        riskClass: "high",
        effectClass: "communication",
        reversibility: "reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "POST",
          url: "https://api.brevo.com/v3/smtp/templates",
          body: { name, subject, htmlContent, isActive: false },
          auth: brevoAuth(request2)
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: "https://api.brevo.com/v3/smtp/templates/{result.id}",
            auth: brevoAuth(request2)
          },
          description: "Brevo returned the inactive template by id",
          assertions: [
            { path: "name", operator: "equals", expected: name },
            { path: "subject", operator: "equals", expected: subject },
            { path: "isActive", operator: "equals", expected: false }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Compare template name and subject; keep it inactive until review"
        }
      })
    );
  }
  if (hasCapability(request2, "webhook")) {
    const url = inputString(request2, "webhookUrl");
    const events = inputStrings(request2, "webhookEvents");
    operations.push(
      operation(request2, {
        provider: "brevo",
        capability: "webhook",
        action: "webhook.create",
        title: `Create Brevo webhook ${url}`,
        identity: { url, events: [...events].sort() },
        riskClass: "critical",
        effectClass: "communication",
        reversibility: "reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "POST",
          url: "https://api.brevo.com/v3/webhooks",
          body: { url, events, type: "transactional" },
          auth: brevoAuth(request2)
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: "https://api.brevo.com/v3/webhooks/{result.id}",
            auth: brevoAuth(request2)
          },
          description: "Brevo returned the webhook by id",
          assertions: [
            { path: "url", operator: "equals", expected: url },
            { path: "events", operator: "contains", expected: events }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Compare endpoint URL, type, and event set"
        }
      })
    );
  }
  return createPlan("brevo", request2, operations, [
    "Domain setup remains pending until DNS authentication reads back as valid."
  ]);
}
function googleAuth(request2) {
  return { scheme: "bearer", credentialRef: credentialInput(request2) };
}
function buildGooglePlan(request2) {
  const operations = [];
  if (hasCapability(request2, "analytics_property")) {
    const accountId = inputString(request2, "analyticsAccountId");
    const displayName = inputString(request2, "propertyDisplayName");
    const timeZone = inputString(request2, "reportingTimeZone");
    const currencyCode2 = inputString(request2, "currencyCode");
    operations.push(
      operation(request2, {
        provider: "google",
        capability: "analytics_property",
        action: "analytics_property.create",
        title: `Create Google Analytics property ${displayName}`,
        identity: { accountId, displayName },
        riskClass: "high",
        effectClass: "reversible_external",
        reversibility: "conditionally_reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "POST",
          url: `https://analyticsadmin.googleapis.com/v1beta/properties?parent=accounts/${encoded(accountId)}`,
          body: { displayName, timeZone, currencyCode: currencyCode2 },
          auth: googleAuth(request2)
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: "https://analyticsadmin.googleapis.com/v1beta/{result.name}",
            auth: googleAuth(request2)
          },
          description: "Google Analytics returned the property resource",
          assertions: [
            { path: "displayName", operator: "equals", expected: displayName },
            { path: "timeZone", operator: "equals", expected: timeZone },
            { path: "currencyCode", operator: "equals", expected: currencyCode2 }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Compare display name, time zone, currency, and account parent"
        }
      })
    );
  }
  if (hasCapability(request2, "analytics_web_stream")) {
    const propertyId = inputString(request2, "analyticsPropertyId");
    const displayName = inputString(request2, "streamDisplayName");
    const defaultUri = inputString(request2, "defaultUri");
    const measurementIdCredentialRef = credentialInput(request2, "measurementIdCredentialRef");
    operations.push(
      operation(request2, {
        provider: "google",
        capability: "analytics_web_stream",
        action: "analytics_web_stream.create",
        title: `Create Analytics web stream ${displayName}`,
        identity: { propertyId, defaultUri },
        riskClass: "high",
        effectClass: "reversible_external",
        reversibility: "conditionally_reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "POST",
          url: `https://analyticsadmin.googleapis.com/v1beta/properties/${encoded(propertyId)}/dataStreams`,
          body: {
            type: "WEB_DATA_STREAM",
            displayName,
            webStreamData: { defaultUri }
          },
          auth: googleAuth(request2),
          captureCredential: {
            credentialRef: measurementIdCredentialRef,
            outputPath: "webStreamData.measurementId"
          }
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: "https://analyticsadmin.googleapis.com/v1beta/{result.name}",
            auth: googleAuth(request2)
          },
          description: "Google Analytics returned the web data stream",
          assertions: [
            { path: "displayName", operator: "equals", expected: displayName },
            {
              path: "webStreamData.defaultUri",
              operator: "equals",
              expected: defaultUri
            },
            { path: "webStreamData.measurementId", operator: "exists" }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Compare property, URI, display name, and measurement id"
        }
      })
    );
  }
  if (hasCapability(request2, "site_verification_token")) {
    const identifier4 = inputString(request2, "siteIdentifier");
    const siteType = optionalString(request2, "siteType") ?? "INET_DOMAIN";
    const method = optionalString(request2, "verificationMethod") ?? "DNS_TXT";
    operations.push(
      operation(request2, {
        provider: "google",
        capability: "site_verification_token",
        action: "site_verification.token.get",
        title: `Request Google verification token for ${identifier4}`,
        identity: { identifier: identifier4, siteType, method },
        riskClass: "medium",
        effectClass: "read",
        reversibility: "reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "POST",
          url: "https://www.googleapis.com/siteVerification/v1/token",
          body: {
            site: { identifier: identifier4, type: siteType },
            verificationMethod: method
          },
          auth: googleAuth(request2)
        },
        readBack: {
          transport: "http",
          http: {
            method: "POST",
            url: "https://www.googleapis.com/siteVerification/v1/token",
            body: {
              site: { identifier: identifier4, type: siteType },
              verificationMethod: method
            },
            auth: googleAuth(request2)
          },
          description: "Google returned the same DNS verification token for the same site and method",
          assertions: [
            { path: "method", operator: "equals", expected: method },
            { path: "token", operator: "equals", expected: "{result.token}" }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Treat the token as pending until it is published and verified"
        }
      })
    );
  }
  if (hasCapability(request2, "site_verification")) {
    const identifier4 = inputString(request2, "siteIdentifier");
    const siteType = optionalString(request2, "siteType") ?? "INET_DOMAIN";
    const method = optionalString(request2, "verificationMethod") ?? "DNS_TXT";
    operations.push(
      operation(request2, {
        provider: "google",
        capability: "site_verification",
        action: "site_verification.web_resource.insert",
        title: `Verify Google ownership of ${identifier4}`,
        identity: { identifier: identifier4, siteType, method },
        riskClass: "high",
        effectClass: "reversible_external",
        reversibility: "reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "POST",
          url: `https://www.googleapis.com/siteVerification/v1/webResource?verificationMethod=${encoded(method)}`,
          body: { site: { identifier: identifier4, type: siteType } },
          auth: googleAuth(request2)
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: "https://www.googleapis.com/siteVerification/v1/webResource",
            auth: googleAuth(request2)
          },
          description: "Google Site Verification returned the owned domain",
          assertions: [
            {
              path: "items",
              operator: "contains",
              expected: { site: { identifier: identifier4, type: siteType } }
            }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Require the exact domain in the authenticated user's verified resources"
        }
      })
    );
  }
  if (hasCapability(request2, "search_console_site")) {
    const siteUrl = inputString(request2, "siteUrl");
    operations.push(
      operation(request2, {
        provider: "google",
        capability: "search_console_site",
        action: "search_console.site.add",
        title: `Add ${siteUrl} to Search Console`,
        identity: siteUrl,
        riskClass: "high",
        effectClass: "reversible_external",
        reversibility: "reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "PUT",
          url: `https://searchconsole.googleapis.com/webmasters/v3/sites/${encoded(siteUrl)}`,
          auth: googleAuth(request2)
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: "https://searchconsole.googleapis.com/webmasters/v3/sites",
            auth: googleAuth(request2)
          },
          description: "Search Console site list contains the requested property",
          assertions: [
            {
              path: "siteEntry",
              operator: "contains",
              expected: { siteUrl }
            }
          ]
        },
        verification: {
          strategy: "read_back",
          description: "Require the expected permission level in the site list"
        }
      })
    );
  }
  if (hasCapability(request2, "search_console_sitemap")) {
    const siteUrl = inputString(request2, "siteUrl");
    const sitemapUrl = inputString(request2, "sitemapUrl");
    operations.push(
      operation(request2, {
        provider: "google",
        capability: "search_console_sitemap",
        action: "search_console.sitemap.submit",
        title: `Submit sitemap ${sitemapUrl}`,
        identity: { siteUrl, sitemapUrl },
        riskClass: "medium",
        effectClass: "reversible_external",
        reversibility: "reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "PUT",
          url: `https://searchconsole.googleapis.com/webmasters/v3/sites/${encoded(siteUrl)}/sitemaps/${encoded(sitemapUrl)}`,
          auth: googleAuth(request2)
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: `https://searchconsole.googleapis.com/webmasters/v3/sites/${encoded(siteUrl)}/sitemaps/${encoded(sitemapUrl)}`,
            auth: googleAuth(request2)
          },
          description: "Search Console returned the submitted sitemap state",
          assertions: [{ path: "path", operator: "equals", expected: sitemapUrl }]
        },
        verification: {
          strategy: "read_back",
          description: "Record submission state and errors; never infer indexing"
        }
      })
    );
  }
  return createPlan("google", request2, operations, [
    "Search and Analytics data can lag; resource existence is distinct from data availability."
  ]);
}
function bingAuth(request2) {
  const mode = optionalString(request2, "authMode") ?? "api_key";
  if (mode === "oauth") {
    return { scheme: "bearer", credentialRef: credentialInput(request2) };
  }
  return {
    scheme: "api_key_query",
    credentialRef: credentialInput(request2),
    name: "apikey"
  };
}
var bingBase = "https://ssl.bing.com/webmaster/api.svc/json";
function buildBingPlan(request2) {
  const operations = [];
  if (hasCapability(request2, "site")) {
    const siteUrl = inputString(request2, "siteUrl");
    operations.push(
      operation(request2, {
        provider: "bing",
        capability: "site",
        action: "site.add",
        title: `Add ${siteUrl} to Bing Webmaster Tools`,
        identity: siteUrl,
        riskClass: "medium",
        effectClass: "reversible_external",
        reversibility: "reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "POST",
          url: `${bingBase}/AddSite`,
          body: { siteUrl },
          auth: bingAuth(request2)
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: `${bingBase}/GetUserSites`,
            auth: bingAuth(request2)
          },
          description: "Bing site list contains the requested site URL",
          assertions: [{ path: "d", operator: "contains", expected: { Url: siteUrl } }]
        },
        verification: {
          strategy: "read_back",
          description: "Read the site list and keep ownership verification distinct"
        }
      })
    );
  }
  if (hasCapability(request2, "sitemap")) {
    const siteUrl = inputString(request2, "siteUrl");
    const sitemapUrl = inputString(request2, "sitemapUrl");
    operations.push(
      operation(request2, {
        provider: "bing",
        capability: "sitemap",
        action: "feed.add",
        title: `Submit ${sitemapUrl} to Bing`,
        identity: { siteUrl, sitemapUrl },
        riskClass: "medium",
        effectClass: "reversible_external",
        reversibility: "reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "POST",
          url: `${bingBase}/SubmitFeed`,
          body: { siteUrl, feedUrl: sitemapUrl },
          auth: bingAuth(request2)
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: `${bingBase}/GetFeeds?siteUrl=${encoded(siteUrl)}`,
            auth: bingAuth(request2)
          },
          description: "Bing feed list contains the submitted sitemap",
          assertions: [{ path: "d", operator: "contains", expected: { Url: sitemapUrl } }]
        },
        verification: {
          strategy: "read_back",
          description: "Read feed state and errors; request acceptance is not indexing"
        }
      })
    );
  }
  if (hasCapability(request2, "url_submission")) {
    const siteUrl = inputString(request2, "siteUrl");
    const url = inputString(request2, "submissionUrl");
    operations.push(
      operation(request2, {
        provider: "bing",
        capability: "url_submission",
        action: "url.submit",
        title: `Submit ${url} to Bing`,
        identity: { siteUrl, url },
        riskClass: "medium",
        effectClass: "reversible_external",
        reversibility: "conditionally_reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "POST",
          url: `${bingBase}/SubmitUrl`,
          body: { siteUrl, url },
          auth: bingAuth(request2)
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Record only API acceptance; indexing remains unknown"
        }
      })
    );
  }
  return createPlan("bing", request2, operations, [
    "The legacy Webmaster API may be unavailable; run doctor before any request."
  ]);
}
function buildManualDnsPlan(provider, request2) {
  const operations = [];
  if (hasCapability(request2, "record")) {
    const zone = inputString(request2, "zone");
    const recordType2 = inputString(request2, "recordType").toUpperCase();
    const recordName = inputString(request2, "recordName");
    const recordValue = inputString(request2, "recordValue");
    const ttl = optionalNumber(request2, "ttl") ?? 3600;
    operations.push(
      operation(request2, {
        provider,
        capability: "record",
        action: "dns_record.change_manual",
        title: `Set ${recordType2} ${recordName} in ${zone}`,
        identity: { zone, recordType: recordType2, recordName, recordValue, ttl },
        riskClass: "critical",
        effectClass: "manual",
        reversibility: "manual",
        manual: {
          system: provider === "mijndomein" ? "MijnDomein control panel" : "DNS control panel",
          url: provider === "mijndomein" ? "https://mijn.mijndomein.nl/" : void 0,
          instructions: [
            `Open the authoritative DNS zone for ${zone}.`,
            `Check for an existing ${recordType2} record named ${recordName}; do not create a duplicate.`,
            `Set the value exactly to ${recordValue} with TTL ${ttl}.`,
            "Save the change, then query the authoritative nameservers until the exact record reads back."
          ],
          requiredFields: {
            zone,
            recordType: recordType2,
            recordName,
            recordValue,
            ttl
          },
          completionEvidence: [
            "Control-panel change receipt or screenshot",
            "Authoritative DNS response with exact name, type, value, and TTL"
          ]
        },
        verification: {
          strategy: "manual",
          description: "Require authoritative DNS read-back; propagation alone is not proof"
        }
      })
    );
  }
  if (provider === "mijndomein" && hasCapability(request2, "domain_attachment")) {
    const domain = inputString(request2, "domain");
    const target = inputString(request2, "targetService");
    operations.push(
      operation(request2, {
        provider,
        capability: "domain_attachment",
        action: "domain.attach_manual",
        title: `Attach ${domain} to ${target}`,
        identity: { domain, target },
        riskClass: "critical",
        effectClass: "manual",
        reversibility: "manual",
        manual: {
          system: "MijnDomein control panel",
          url: "https://mijn.mijndomein.nl/",
          instructions: [
            `Inspect all existing records for ${domain} before changing anything.`,
            `Use the exact DNS records supplied by ${target}; do not infer addresses or verification values.`,
            "Preserve unrelated mail and verification records.",
            "Verify the target service and authoritative DNS after saving."
          ],
          requiredFields: { domain, targetService: target },
          completionEvidence: [
            "Before-and-after DNS record list",
            "Authoritative DNS response",
            `${target} domain verification state`
          ]
        },
        verification: {
          strategy: "manual",
          description: "Require both authoritative DNS and target-provider verification"
        }
      })
    );
  }
  return createPlan(provider, request2, operations, [
    "No undocumented DNS write API is called; a human owns each control-panel change."
  ]);
}
function buildDnsPlan(request2) {
  return buildManualDnsPlan("dns", request2);
}
function buildMijnDomeinPlan(request2) {
  return buildManualDnsPlan("mijndomein", request2);
}
function appStoreAuth(request2) {
  return { scheme: "jwt", credentialRef: credentialInput(request2) };
}
function buildAppStoreConnectPlan(request2) {
  const operations = [];
  let buildProcessingOperationId;
  let testflightGroupOperationId;
  if (hasCapability(request2, "first_app_record")) {
    const name = inputString(request2, "appName");
    const bundleId = inputString(request2, "bundleId");
    const sku = inputString(request2, "sku");
    const primaryLanguage = optionalString(request2, "primaryLanguage") ?? "en-US";
    operations.push(
      operation(request2, {
        provider: "app_store_connect",
        capability: "first_app_record",
        action: "app_record.create_manual",
        title: `Create first App Store Connect record for ${name}`,
        identity: { name, bundleId, sku },
        riskClass: "critical",
        effectClass: "manual",
        reversibility: "manual",
        manual: {
          system: "App Store Connect web interface",
          url: "https://appstoreconnect.apple.com/apps",
          instructions: [
            "Open My Apps and choose New App.",
            `Select the registered bundle identifier ${bundleId}.`,
            `Enter name ${name}, SKU ${sku}, and primary language ${primaryLanguage}.`,
            "Create the record and copy the numeric Apple app id.",
            "Do not claim the app is submitted or approved; this step creates only the record."
          ],
          requiredFields: { name, bundleId, sku, primaryLanguage },
          completionEvidence: [
            "Numeric App Store Connect app id",
            "Bundle identifier shown on the app record",
            "App record URL"
          ]
        },
        verification: {
          strategy: "manual",
          description: "A human records the app id and verifies the exact bundle identifier"
        }
      })
    );
  }
  if (hasCapability(request2, "build_processing")) {
    const appId = inputString(request2, "appStoreAppId");
    const appVersion = inputString(request2, "appVersion");
    const buildNumber = inputString(request2, "buildNumber");
    const query = [
      `filter[app]=${encoded(appId)}`,
      `filter[preReleaseVersion.version]=${encoded(appVersion)}`,
      `filter[version]=${encoded(buildNumber)}`,
      "filter[processingState]=VALID",
      "include=betaGroups",
      "limit=2"
    ].join("&");
    const build = operation(request2, {
      provider: "app_store_connect",
      capability: "build_processing",
      action: "processing.status.get",
      title: `Read TestFlight processing for ${appVersion} (${buildNumber})`,
      identity: { appId, appVersion, buildNumber },
      riskClass: "medium",
      effectClass: "read",
      reversibility: "reversible",
      credentialRef: request2.credentialRef,
      http: {
        method: "GET",
        url: `https://api.appstoreconnect.apple.com/v1/builds?${query}`,
        auth: appStoreAuth(request2)
      },
      readBack: {
        transport: "http",
        http: {
          method: "GET",
          url: `https://api.appstoreconnect.apple.com/v1/builds?${query}`,
          auth: appStoreAuth(request2)
        },
        description: "App Store Connect returned one exact VALID processed build",
        assertions: [
          {
            path: "data",
            operator: "contains",
            expected: {
              type: "builds",
              attributes: { version: buildNumber, processingState: "VALID" }
            }
          }
        ]
      },
      verification: {
        strategy: "read_back",
        description: "Require the exact app, marketing version, build number, and VALID processing state; do not infer publication"
      }
    });
    buildProcessingOperationId = build.id;
    operations.push(build);
  }
  if (hasCapability(request2, "testflight_group")) {
    const appId = inputString(request2, "appStoreAppId");
    const name = inputString(request2, "betaGroupName");
    const isInternalGroup = inputBoolean(request2, "isInternalGroup", true);
    const group = operation(request2, {
      provider: "app_store_connect",
      capability: "testflight_group",
      action: "testflight_group.create",
      title: `Create TestFlight group ${name}`,
      identity: { appId, name, isInternalGroup },
      riskClass: "high",
      effectClass: "reversible_external",
      reversibility: "reversible",
      credentialRef: request2.credentialRef,
      dependsOn: buildProcessingOperationId ? [buildProcessingOperationId] : [],
      http: {
        method: "POST",
        url: "https://api.appstoreconnect.apple.com/v1/betaGroups",
        body: {
          data: {
            type: "betaGroups",
            attributes: { name, isInternalGroup },
            relationships: {
              app: { data: { type: "apps", id: appId } }
            }
          }
        },
        auth: appStoreAuth(request2)
      },
      readBack: {
        transport: "http",
        http: {
          method: "GET",
          url: "https://api.appstoreconnect.apple.com/v1/betaGroups/{result.data.id}",
          auth: appStoreAuth(request2)
        },
        description: "App Store Connect returned the TestFlight group by id",
        assertions: [
          { path: "data.attributes.name", operator: "equals", expected: name },
          {
            path: "data.attributes.isInternalGroup",
            operator: "equals",
            expected: isInternalGroup
          }
        ]
      },
      verification: {
        strategy: "response_then_read_back",
        description: "Compare group name, internal flag, and related app id"
      }
    });
    testflightGroupOperationId = group.id;
    operations.push(group);
  }
  if (hasCapability(request2, "build_group_assignment")) {
    const buildId = inputString(request2, "appStoreBuildId");
    const groupId = inputString(request2, "betaGroupId");
    operations.push(
      operation(request2, {
        provider: "app_store_connect",
        capability: "build_group_assignment",
        action: "testflight_group.build.add",
        title: "Assign the processed build to the verified TestFlight group",
        identity: { buildId, groupId },
        riskClass: "high",
        effectClass: "reversible_external",
        reversibility: "reversible",
        credentialRef: request2.credentialRef,
        dependsOn: [
          ...buildProcessingOperationId ? [buildProcessingOperationId] : [],
          ...testflightGroupOperationId ? [testflightGroupOperationId] : []
        ],
        http: {
          method: "POST",
          url: `https://api.appstoreconnect.apple.com/v1/betaGroups/${encoded(groupId)}/relationships/builds`,
          body: { data: [{ type: "builds", id: buildId }] },
          auth: appStoreAuth(request2)
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: `https://api.appstoreconnect.apple.com/v1/betaGroups/${encoded(groupId)}/relationships/builds`,
            auth: appStoreAuth(request2)
          },
          description: "App Store Connect returned the processed build in the TestFlight group",
          assertions: [
            { path: "data", operator: "contains", expected: { type: "builds", id: buildId } }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Require the exact processed build in the exact group; TestFlight access is not App Store publication"
        }
      })
    );
  }
  if (hasCapability(request2, "build_metadata")) {
    const detailId = inputString(request2, "betaAppReviewDetailId");
    const contactFirstName = inputString(request2, "contactFirstName");
    const contactLastName = inputString(request2, "contactLastName");
    const contactEmail = inputString(request2, "contactEmail");
    const contactPhone = inputString(request2, "contactPhone");
    const notes = inputString(request2, "reviewNotes");
    operations.push(
      operation(request2, {
        provider: "app_store_connect",
        capability: "build_metadata",
        action: "beta_review_details.update",
        title: "Update TestFlight beta review contact details",
        identity: detailId,
        riskClass: "critical",
        effectClass: "communication",
        reversibility: "reversible",
        credentialRef: request2.credentialRef,
        http: {
          method: "PATCH",
          url: `https://api.appstoreconnect.apple.com/v1/betaAppReviewDetails/${encoded(detailId)}`,
          body: {
            data: {
              type: "betaAppReviewDetails",
              id: detailId,
              attributes: {
                contactFirstName,
                contactLastName,
                contactEmail,
                contactPhone,
                notes
              }
            }
          },
          auth: appStoreAuth(request2)
        },
        readBack: {
          transport: "http",
          http: {
            method: "GET",
            url: `https://api.appstoreconnect.apple.com/v1/betaAppReviewDetails/${encoded(detailId)}`,
            auth: appStoreAuth(request2)
          },
          description: "App Store Connect returned the beta review details",
          assertions: [
            {
              path: "data.attributes.contactEmail",
              operator: "equals",
              expected: contactEmail
            },
            {
              path: "data.attributes.notes",
              operator: "equals",
              expected: notes
            }
          ]
        },
        verification: {
          strategy: "read_back",
          description: "Compare the exact review contact fields and notes"
        }
      })
    );
  }
  return createPlan("app_store_connect", request2, operations, [
    "The API plan starts only after a human creates and verifies the first app record."
  ]);
}
function easAuthEnvironment(request2) {
  return request2.credentialRef ? { name: "EXPO_TOKEN", credentialRef: credentialInput(request2) } : void 0;
}
function buildEasPlan(request2) {
  const operations = [];
  let buildOperationId;
  let appStoreConnectionOperationId;
  const projectDirectory = optionalString(request2, "projectDirectory");
  if (hasCapability(request2, "app_store_prerequisite")) {
    const name = inputString(request2, "appName");
    const bundleId = inputString(request2, "bundleId");
    const sku = inputString(request2, "sku");
    operations.push(
      operation(request2, {
        provider: "eas",
        capability: "app_store_prerequisite",
        action: "app_store_record.confirm_manual",
        title: `Create or confirm App Store record for ${name}`,
        identity: { name, bundleId, sku },
        riskClass: "critical",
        effectClass: "manual",
        reversibility: "manual",
        manual: {
          system: "App Store Connect web interface",
          url: "https://appstoreconnect.apple.com/apps",
          instructions: [
            "Search My Apps for the exact bundle identifier before creating anything.",
            `If absent, create the first app record with name ${name}, bundle id ${bundleId}, and SKU ${sku}.`,
            "Record the numeric Apple app id in the EAS submit profile.",
            "Do not start EAS submission until the record is visible and the bundle id matches."
          ],
          requiredFields: { name, bundleId, sku },
          completionEvidence: [
            "Numeric Apple app id",
            "Matching bundle identifier",
            "EAS submit profile reference"
          ]
        },
        verification: {
          strategy: "manual",
          description: "Require a human-attested Apple app id before submission"
        }
      })
    );
  }
  if (hasCapability(request2, "app_store_connection")) {
    const appStoreAppId = inputString(request2, "appStoreAppId");
    const bundleId = inputString(request2, "bundleId");
    const connection = operation(request2, {
      provider: "eas",
      capability: "app_store_connection",
      action: "app_store_connection.connect",
      title: `Connect EAS to App Store Connect app ${appStoreAppId}`,
      identity: { appStoreAppId, bundleId },
      riskClass: "critical",
      effectClass: "reversible_external",
      reversibility: "reversible",
      credentialRef: request2.credentialRef,
      command: {
        binary: "eas",
        args: [
          "integrations:asc:connect",
          "--asc-app-id",
          appStoreAppId,
          "--bundle-id",
          bundleId,
          "--json",
          "--non-interactive"
        ],
        ...projectDirectory ? { cwd: projectDirectory } : {},
        authEnvironment: easAuthEnvironment(request2)
      },
      readBack: {
        transport: "cli",
        command: {
          binary: "eas",
          args: ["integrations:asc:status", "--json", "--non-interactive"],
          ...projectDirectory ? { cwd: projectDirectory } : {},
          authEnvironment: easAuthEnvironment(request2)
        },
        description: "EAS returned the linked App Store Connect app and bundle identifier",
        assertions: [
          { path: "", operator: "contains", expected: appStoreAppId },
          { path: "", operator: "contains", expected: bundleId }
        ]
      },
      verification: {
        strategy: "response_then_read_back",
        description: "Require the same-run Apple app id and bundle id from EAS link status"
      }
    });
    appStoreConnectionOperationId = connection.id;
    operations.push(connection);
  }
  if (hasCapability(request2, "ios_build")) {
    const profile2 = optionalString(request2, "buildProfile") ?? "production";
    const build = operation(request2, {
      provider: "eas",
      capability: "ios_build",
      action: "ios.build",
      title: `Start EAS iOS build with ${profile2} profile`,
      identity: { profile: profile2, environment: request2.environment },
      riskClass: "critical",
      effectClass: "reversible_external",
      reversibility: "conditionally_reversible",
      credentialRef: request2.credentialRef,
      command: {
        binary: "eas",
        args: ["build", "--platform", "ios", "--profile", profile2, "--non-interactive", "--json"],
        ...projectDirectory ? { cwd: projectDirectory } : {},
        authEnvironment: easAuthEnvironment(request2)
      },
      readBack: {
        transport: "cli",
        command: {
          binary: "eas",
          args: ["build:view", "{result.id}", "--json"],
          ...projectDirectory ? { cwd: projectDirectory } : {},
          authEnvironment: easAuthEnvironment(request2)
        },
        description: "EAS returned a terminal build record by id",
        assertions: [
          { path: "id", operator: "equals", expected: "{result.id}" },
          { path: "platform", operator: "equals", expected: "IOS" },
          { path: "status", operator: "equals", expected: "FINISHED" },
          { path: "buildProfile", operator: "equals", expected: profile2 }
        ]
      },
      verification: {
        strategy: "response_then_read_back",
        description: "Require a finished build status and matching profile/platform"
      }
    });
    buildOperationId = build.id;
    operations.push(build);
  }
  if (hasCapability(request2, "ios_submit")) {
    const buildId = inputString(request2, "easBuildId");
    const profile2 = optionalString(request2, "submitProfile") ?? "production";
    operations.push(
      operation(request2, {
        provider: "eas",
        capability: "ios_submit",
        action: "ios.submit",
        title: `Submit EAS build ${buildId} to App Store Connect`,
        identity: { buildId, profile: profile2 },
        riskClass: "critical",
        effectClass: "irreversible_external",
        reversibility: "conditionally_reversible",
        credentialRef: request2.credentialRef,
        dependsOn: [
          ...buildOperationId ? [buildOperationId] : [],
          ...appStoreConnectionOperationId ? [appStoreConnectionOperationId] : []
        ],
        command: {
          binary: "eas",
          args: [
            "submit",
            "--platform",
            "ios",
            "--profile",
            profile2,
            "--id",
            buildId,
            "--non-interactive"
          ],
          ...projectDirectory ? { cwd: projectDirectory } : {},
          authEnvironment: easAuthEnvironment(request2)
        },
        readBack: {
          transport: "cli",
          command: {
            binary: "eas",
            args: [
              "submit:list",
              "--platform",
              "ios",
              "--status",
              "finished",
              "--limit",
              "10",
              "--json",
              "--non-interactive"
            ],
            ...projectDirectory ? { cwd: projectDirectory } : {},
            authEnvironment: easAuthEnvironment(request2)
          },
          description: "EAS returned the App Store submission state by id",
          assertions: [
            {
              path: "",
              operator: "contains",
              expected: {
                platform: "IOS",
                status: "FINISHED",
                submittedBuild: { id: buildId }
              }
            }
          ]
        },
        verification: {
          strategy: "response_then_read_back",
          description: "Require a finished submission state; do not infer Apple approval"
        }
      })
    );
  }
  return createPlan("eas", request2, operations, [
    "A completed EAS submission does not prove TestFlight processing or App Review approval."
  ]);
}

// lib/providers/public-evidence.ts
var publicDnsRecordSchema = external_exports.object({
  source_provider: external_exports.enum(["vercel", "google", "brevo"]),
  type: external_exports.enum(["A", "AAAA", "CNAME", "TXT", "MX", "CAA"]),
  name: external_exports.string().min(1).max(253),
  value: external_exports.string().min(1).max(4096),
  ttl: external_exports.number().int().min(60).max(86400),
  priority: external_exports.number().int().min(0).max(65535).optional(),
  reason: external_exports.string().min(1).max(500)
}).strict();
var publicProviderIdentifierSchema = external_exports.object({
  type: external_exports.enum([
    "property_id",
    "stream_id",
    "measurement_id",
    "build_id",
    "submission_id",
    "app_id",
    "apple_build_id",
    "testflight_group_id",
    "app_version",
    "build_number"
  ]),
  value: external_exports.string().min(1).max(500)
}).strict();
var providerPublicOutputsSchema = external_exports.object({
  dnsRecords: external_exports.array(publicDnsRecordSchema).max(100).default([]),
  identifiers: external_exports.array(publicProviderIdentifierSchema).max(100).default([])
}).strict();
var providerOrder = { vercel: 0, google: 1, brevo: 2 };
function orderPublicDnsRecords(records) {
  const unique2 = /* @__PURE__ */ new Map();
  for (const candidate of records) {
    const record3 = publicDnsRecordSchema.parse(candidate);
    unique2.set(
      [
        record3.source_provider,
        record3.type,
        record3.name,
        record3.value,
        record3.ttl,
        record3.priority ?? ""
      ].join("\0"),
      record3
    );
  }
  return [...unique2.values()].sort(
    (left, right) => providerOrder[left.source_provider] - providerOrder[right.source_provider] || left.type.localeCompare(right.type) || left.name.localeCompare(right.name) || left.value.localeCompare(right.value) || left.ttl - right.ttl || (left.priority ?? -1) - (right.priority ?? -1)
  );
}
function objects(value, depth = 0) {
  if (depth > 8 || value === null || typeof value !== "object") return [];
  if (Array.isArray(value)) return value.flatMap((item) => objects(item, depth + 1));
  const record3 = value;
  return [record3, ...Object.values(record3).flatMap((item) => objects(item, depth + 1))];
}
function scalar2(value) {
  if (typeof value !== "string" && typeof value !== "number") return void 0;
  const text2 = String(value).trim();
  if (text2.length === 0 || text2.length > 4096 || text2.startsWith("cred://") || /[\u0000-\u001f\u007f]/.test(text2)) {
    return void 0;
  }
  return text2;
}
function dnsType(value) {
  const parsed = external_exports.enum(["A", "AAAA", "CNAME", "TXT", "MX", "CAA"]).safeParse(typeof value === "string" ? value.toUpperCase() : value);
  return parsed.success ? parsed.data : void 0;
}
function recordsFromBrevo(value) {
  const records = [];
  for (const object2 of objects(value)) {
    const dns = object2.dns_records;
    if (!dns || Array.isArray(dns) || typeof dns !== "object") continue;
    for (const candidate of Object.values(dns)) {
      if (!candidate || Array.isArray(candidate) || typeof candidate !== "object") continue;
      const item = candidate;
      const type = dnsType(item.type);
      const value2 = scalar2(item.value);
      const name = scalar2(item.host_name) ?? "@";
      if (!type || !value2) continue;
      records.push({
        source_provider: "brevo",
        type,
        name: name === "" ? "@" : name.replace(/\.$/, "") || "@",
        value: value2,
        ttl: typeof item.ttl === "number" ? item.ttl : 3600,
        ...typeof item.priority === "number" ? { priority: item.priority } : {},
        reason: "Authenticate the configured Brevo sending domain."
      });
    }
  }
  return records;
}
function recordsFromGoogle(value, inputs) {
  const records = [];
  for (const object2 of objects(value)) {
    const method = scalar2(object2.method);
    const token = scalar2(object2.token);
    if (method !== "DNS_TXT" || !token) continue;
    records.push({
      source_provider: "google",
      type: "TXT",
      name: "@",
      value: token,
      ttl: typeof inputs.dnsTtl === "number" ? inputs.dnsTtl : 3600,
      reason: `Verify ${scalar2(inputs.siteIdentifier) ?? "the configured domain"} with Google Site Verification.`
    });
  }
  return records;
}
function recordsFromVercel(value) {
  const records = [];
  for (const object2 of objects(value)) {
    for (const key of ["verification", "dnsRecords", "dns_records"]) {
      const candidates = object2[key];
      if (!Array.isArray(candidates)) continue;
      for (const candidate of candidates) {
        if (!candidate || Array.isArray(candidate) || typeof candidate !== "object") continue;
        const item = candidate;
        const type = dnsType(item.type);
        const name = scalar2(item.domain) ?? scalar2(item.name) ?? scalar2(item.host);
        const value2 = scalar2(item.value) ?? scalar2(item.target);
        if (!type || !name || !value2) continue;
        records.push({
          source_provider: "vercel",
          type,
          name,
          value: value2,
          ttl: typeof item.ttl === "number" ? item.ttl : 300,
          ...typeof item.priority === "number" ? { priority: item.priority } : {},
          reason: scalar2(item.reason) ?? "Attach and verify the configured Vercel domain."
        });
      }
    }
  }
  return records;
}
function identifier(identifiers, type, value, transform = (text2) => text2) {
  const safe = scalar2(value);
  if (!safe || safe.length > 500) return;
  const parsed = publicProviderIdentifierSchema.safeParse({ type, value: transform(safe) });
  if (parsed.success) identifiers.set(`${type}\0${parsed.data.value}`, parsed.data);
}
function collectProviderPublicOutputs(input) {
  const successful = input.report.operations.filter(({ result: result2 }) => result2.status === "succeeded");
  const raw = [
    ...successful.map(({ result: result2 }) => result2.output),
    ...input.verification.checks.map(({ evidence }) => evidence)
  ];
  const dnsRecords = input.provider === "brevo" ? raw.flatMap(recordsFromBrevo) : input.provider === "google" ? raw.flatMap((value) => recordsFromGoogle(value, input.requestInputs)) : input.provider === "vercel" ? raw.flatMap(recordsFromVercel) : [];
  const identifiers = /* @__PURE__ */ new Map();
  for (const operation2 of successful) {
    for (const object2 of objects(operation2.result.output)) {
      if (operation2.operation.capability === "analytics_property") {
        const name = scalar2(object2.name);
        if (name?.startsWith("properties/")) {
          identifier(
            identifiers,
            "property_id",
            name,
            (value) => value.slice("properties/".length)
          );
        }
      }
      if (operation2.operation.capability === "analytics_web_stream") {
        const name = scalar2(object2.name);
        if (name?.includes("/dataStreams/")) {
          identifier(identifiers, "stream_id", name, (value) => value.split("/").at(-1));
        }
        identifier(identifiers, "measurement_id", object2.measurementId);
      }
      if (operation2.operation.capability === "ios_build") {
        identifier(identifiers, "build_id", object2.id);
        identifier(identifiers, "app_version", object2.appVersion);
        identifier(identifiers, "build_number", object2.appBuildVersion ?? object2.buildNumber);
      }
      if (operation2.operation.capability === "ios_submit") {
        identifier(identifiers, "submission_id", object2.id);
      }
      if (operation2.operation.capability === "build_processing") {
        identifier(identifiers, "apple_build_id", object2.id);
      }
      if (operation2.operation.capability === "testflight_group") {
        identifier(identifiers, "testflight_group_id", object2.id);
      }
    }
  }
  return providerPublicOutputsSchema.parse({
    dnsRecords: orderPublicDnsRecords(dnsRecords),
    identifiers: [...identifiers.values()].sort(
      (left, right) => left.type.localeCompare(right.type) || left.value.localeCompare(right.value)
    )
  });
}
function publicIdentifier(output, type) {
  if (!output || Array.isArray(output) || typeof output !== "object") return void 0;
  const parsed = providerPublicOutputsSchema.safeParse(
    output.publicOutputs
  );
  if (!parsed.success) return void 0;
  const values = parsed.data.identifiers.filter((item) => item.type === type).map(({ value }) => value);
  return new Set(values).size === 1 ? values[0] : void 0;
}

// lib/providers/registry.ts
var adapters = {
  github: new DeclarativeProviderAdapter(providerDescriptors.github, buildGitHubPlan),
  vercel: new DeclarativeProviderAdapter(providerDescriptors.vercel, buildVercelPlan),
  neon: new DeclarativeProviderAdapter(providerDescriptors.neon, buildNeonPlan),
  stripe: new DeclarativeProviderAdapter(providerDescriptors.stripe, buildStripePlan),
  revenuecat: new DeclarativeProviderAdapter(providerDescriptors.revenuecat, buildRevenueCatPlan),
  brevo: new DeclarativeProviderAdapter(providerDescriptors.brevo, buildBrevoPlan),
  google: new DeclarativeProviderAdapter(providerDescriptors.google, buildGooglePlan),
  bing: new DeclarativeProviderAdapter(providerDescriptors.bing, buildBingPlan),
  dns: new DeclarativeProviderAdapter(providerDescriptors.dns, buildDnsPlan),
  mijndomein: new DeclarativeProviderAdapter(providerDescriptors.mijndomein, buildMijnDomeinPlan),
  app_store_connect: new DeclarativeProviderAdapter(
    providerDescriptors.app_store_connect,
    buildAppStoreConnectPlan
  ),
  eas: new DeclarativeProviderAdapter(providerDescriptors.eas, buildEasPlan)
};
var ProviderRegistry = class {
  providers = /* @__PURE__ */ new Map();
  constructor(initial = Object.values(adapters)) {
    for (const adapter of initial) this.register(adapter);
  }
  register(adapter) {
    if (this.providers.has(adapter.descriptor.id)) {
      throw new Error(`Provider adapter already registered: ${adapter.descriptor.id}`);
    }
    this.providers.set(adapter.descriptor.id, adapter);
  }
  get(id) {
    const adapter = this.providers.get(id);
    if (!adapter) throw new Error(`Provider adapter is not registered: ${id}`);
    return adapter;
  }
  has(id) {
    return this.providers.has(id);
  }
  list() {
    return [...this.providers.values()];
  }
};
var providerRegistry = new ProviderRegistry();
function getProviderAdapter(id) {
  return providerRegistry.get(id);
}

// lib/providers/stack-profiles.ts
var stackCapabilityContracts = {
  "source.repository.create": "repository",
  "hosting.web.deploy": "deployment",
  "database.postgres.provision": "project",
  "database.migrations.apply": "schema_migration",
  "commerce.web_subscription": "product",
  "commerce.native_subscription": "entitlement",
  "email.transactional": "template",
  "analytics.web.configure": "analytics_property",
  "search.google.configure": "search_console_site",
  "search.bing.configure": "site",
  "dns.record": "record",
  "mobile.ios.build": "ios_build"
};
var founderDefaultBindings = {
  "source.repository.create": {
    providerId: "github",
    capability: "repository",
    rationale: "Founder-default source publication uses the registered GitHub adapter."
  },
  "hosting.web.deploy": {
    providerId: "vercel",
    capability: "deployment",
    rationale: "Founder-default web deployment uses the registered Vercel adapter."
  },
  "database.postgres.provision": {
    providerId: "neon",
    capability: "project",
    rationale: "Founder-default PostgreSQL provisioning starts with the registered Neon project contract."
  },
  "database.migrations.apply": {
    providerId: "neon",
    capability: "schema_migration",
    rationale: "Founder-default migrations use the registered Neon PostgreSQL migration contract."
  },
  "commerce.web_subscription": {
    providerId: "stripe",
    capability: "product",
    rationale: "Founder-default web subscription catalog setup starts with Stripe product provisioning."
  },
  "commerce.native_subscription": {
    providerId: "revenuecat",
    capability: "entitlement",
    rationale: "Founder-default native subscription access uses RevenueCat entitlements."
  },
  "email.transactional": {
    providerId: "brevo",
    capability: "template",
    rationale: "Founder-default transactional email preparation uses inactive Brevo templates."
  },
  "analytics.web.configure": {
    providerId: "google",
    capability: "analytics_property",
    rationale: "Founder-default web analytics configuration uses Google Analytics properties."
  },
  "search.google.configure": {
    providerId: "google",
    capability: "search_console_site",
    rationale: "Founder-default Google discovery setup uses the Search Console site contract."
  },
  "search.bing.configure": {
    providerId: "bing",
    capability: "site",
    rationale: "Founder-default Bing discovery setup uses the Webmaster site contract."
  },
  "dns.record": {
    providerId: "mijndomein",
    capability: "record",
    rationale: "The founder-default registrar is MijnDomein and DNS changes remain manual."
  },
  "mobile.ios.build": {
    providerId: "eas",
    capability: "ios_build",
    rationale: "Founder-default cross-platform iOS builds use the registered EAS adapter."
  }
};
var genericDnsStackProfile = {
  profileId: "founder-default-generic-dns",
  version: "0.2.0",
  label: "Founder default with provider-neutral DNS",
  verification: "local_contract_only",
  bindings: {
    ...founderDefaultBindings,
    "dns.record": {
      providerId: "dns",
      capability: "record",
      rationale: "Use the registered provider-neutral manual DNS adapter for a non-MijnDomein zone."
    }
  }
};
var stackCapabilityRoles = Object.keys(stackCapabilityContracts);
var knownStackCapabilityRoles = new Set(stackCapabilityRoles);

// lib/launch/manual-evidence.ts
var propagationCheckSchema = external_exports.object({
  resolver: external_exports.string().min(1).max(253),
  checked_at: external_exports.string().datetime({ offset: true }),
  status: external_exports.literal("matched")
}).strict();
var manualDnsOutputSchema = external_exports.object({
  mode: external_exports.literal("manual_dns"),
  records: external_exports.array(publicDnsRecordSchema).min(1).max(100),
  preserved_existing_mail_records: external_exports.literal(true),
  preserved_nameservers: external_exports.literal(true),
  propagation_checks: external_exports.array(propagationCheckSchema).min(2).max(10)
}).strict().superRefine(rejectCredentialMaterial);
var appleFirstAppRecordOutputSchema = external_exports.object({
  app_name: external_exports.string().min(1).max(255),
  bundle_identifier: external_exports.string().regex(/^[A-Za-z0-9]+(?:[.-][A-Za-z0-9-]+)+$/, "expected a reverse-DNS bundle ID"),
  sku: external_exports.string().min(1).max(255),
  primary_language: external_exports.string().regex(/^[a-z]{2}(?:-[A-Z]{2})?$/),
  apple_app_id: external_exports.string().regex(/^\d{6,20}$/),
  team_id: external_exports.string().regex(/^[A-Z0-9]{10}$/),
  provider_state: external_exports.literal("configured"),
  verified_at: external_exports.string().datetime({ offset: true })
}).strict().superRefine(rejectCredentialMaterial);
var manualOutputSchemas = {
  "dns-records": manualDnsOutputSchema,
  "apple-first-app-record": appleFirstAppRecordOutputSchema
};
var manualEvidenceArtifactSchema = external_exports.object({
  schema_version: external_exports.literal(1),
  kind: external_exports.literal("manual_action_evidence"),
  run_id: external_exports.string().min(1).max(120),
  node_id: external_exports.string().min(1).max(120),
  status: external_exports.literal("verified"),
  approved_by: external_exports.string().min(1).max(300),
  verified_at: external_exports.string().datetime({ offset: true }),
  output: external_exports.unknown(),
  verification: external_exports.array(external_exports.string().min(1).max(1e3)).min(1).max(20),
  limitations: external_exports.array(external_exports.string().min(1).max(1e3)).max(20).default([])
}).strict().superRefine(rejectCredentialMaterial);
var launchManualActionContracts = {
  "dns-records": {
    nodeId: "dns-records",
    requiredFields: [
      "mode=manual_dns",
      "records[]: source_provider, type, name, value, ttl, optional priority, reason",
      "preserved_existing_mail_records=true",
      "preserved_nameservers=true",
      "propagation_checks[] from at least two resolvers"
    ],
    effect: "Add only the declared DNS records; do not replace nameservers or remove mail records.",
    risk: "high",
    rollback: "Remove only records created by this run after confirming no dependent service uses them.",
    completionEvidence: [
      "typed output matching the ordered DNS plan",
      "matched DNS read-back from at least two named resolvers",
      "explicit confirmation that existing MX, SPF, DKIM, and DMARC records were preserved"
    ],
    evidencePath: "reports/launch/<run-id>/manual/dns-records.json"
  },
  "apple-first-app-record": {
    nodeId: "apple-first-app-record",
    requiredFields: [
      "app_name",
      "bundle_identifier",
      "sku",
      "primary_language",
      "apple_app_id",
      "team_id",
      "provider_state=configured",
      "verified_at"
    ],
    effect: "Create the first App Store Connect app record; do not submit or publish a release.",
    risk: "medium",
    rollback: "No automatic deletion is attempted; correct the app record in App Store Connect.",
    completionEvidence: [
      "typed identifiers copied from the App Store Connect record",
      "provider_state=configured and a verification timestamp",
      "no claim of TestFlight processing or App Store publication"
    ],
    evidencePath: "reports/launch/<run-id>/manual/apple-first-app-record.json"
  }
};
function expectedDnsRecordsFromDependencies(dependencyOutputs) {
  const records = [];
  for (const output of Object.values(dependencyOutputs)) {
    if (!output || Array.isArray(output) || typeof output !== "object") continue;
    const parsed = providerPublicOutputsSchema.safeParse(output.publicOutputs);
    if (parsed.success) records.push(...parsed.data.dnsRecords);
  }
  return orderPublicDnsRecords(records);
}
function inside2(rootDir, artifact) {
  const root = realpathSync(rootDir);
  const target = resolve5(root, artifact);
  const rel = relative2(root, target);
  if (rel === "" || rel === ".." || rel.startsWith(`..${sep2}`) || rel.startsWith(sep2)) {
    throw new Error(`Evidence artifact escapes the venture root: ${artifact}`);
  }
  return target;
}
function validatorFor(nodeId) {
  return manualOutputSchemas[nodeId];
}
function validateLaunchManualOutput(nodeId, output) {
  const schema = validatorFor(nodeId);
  if (!schema) return { ok: false, message: `No manual output contract exists for ${nodeId}.` };
  const result2 = schema.safeParse(output);
  return result2.success ? { ok: true } : {
    ok: false,
    message: `${nodeId} output is invalid: ${result2.error.issues.map((issue) => `${issue.path.join(".") || "output"}: ${issue.message}`).join("; ")}`
  };
}
function launchManualOutputValidatorName(nodeId) {
  return `launch.manual.${nodeId}`;
}
function createLaunchManualBindings() {
  return {
    validators: Object.fromEntries(
      Object.keys(manualOutputSchemas).map((nodeId) => [
        launchManualOutputValidatorName(nodeId),
        ({ output, dependencyOutputs }) => {
          const validation = validateLaunchManualOutput(nodeId, output);
          if (!validation.ok || nodeId !== "dns-records") return validation;
          const expected = expectedDnsRecordsFromDependencies(dependencyOutputs);
          if (Object.keys(dependencyOutputs).length === 0) return { ok: true };
          if (expected.length === 0) {
            return {
              ok: false,
              message: "dns-records has no typed upstream provider record plan; do not infer a DNS value or complete a partial action."
            };
          }
          const actual = manualDnsOutputSchema.parse(output).records;
          return isDeepStrictEqual(actual, expected) ? { ok: true } : {
            ok: false,
            message: "dns-records output does not exactly match the ordered Vercel, Google, and Brevo record plan from this run."
          };
        }
      ])
    )
  };
}
function createRepositoryInterruptEvidenceVerifier(options) {
  const redactor = options.redactor ?? new Redactor();
  const maxBytes = options.maxBytes ?? 1e6;
  return (context2) => {
    const parsedReference = artifactReferenceSchema.safeParse(context2.evidenceArtifact);
    if (!parsedReference.success) {
      return { ok: false, message: parsedReference.error.issues[0]?.message };
    }
    const expected = `reports/launch/${context2.runId}/manual/${context2.node.id}.json`;
    if (context2.evidenceArtifact !== expected) {
      return {
        ok: false,
        message: `Evidence for ${context2.node.id} must use ${expected}.`
      };
    }
    let path;
    try {
      path = inside2(options.rootDir, context2.evidenceArtifact);
    } catch (error) {
      return { ok: false, message: error instanceof Error ? error.message : String(error) };
    }
    if (!existsSync4(path))
      return { ok: false, message: `Evidence file does not exist: ${expected}.` };
    const stat2 = lstatSync(path);
    if (stat2.isSymbolicLink() || !stat2.isFile()) {
      return { ok: false, message: `Evidence must be a regular non-symlink file: ${expected}.` };
    }
    if (stat2.size === 0 || stat2.size > maxBytes) {
      return {
        ok: false,
        message: `Evidence size must be between 1 and ${maxBytes} bytes: ${expected}.`
      };
    }
    let raw;
    let artifact;
    try {
      raw = readFileSync5(path, "utf8");
      artifact = manualEvidenceArtifactSchema.parse(JSON.parse(raw));
    } catch (error) {
      return {
        ok: false,
        message: `Evidence file is not valid typed JSON: ${error instanceof Error ? error.message : String(error)}`
      };
    }
    if (redactor.redactText(raw) !== raw) {
      return { ok: false, message: "Evidence contains registered credential material." };
    }
    if (artifact.run_id !== context2.runId || artifact.node_id !== context2.node.id || artifact.approved_by !== context2.approvedBy) {
      return {
        ok: false,
        message: "Evidence run_id, node_id, or approved_by does not match the active interrupt."
      };
    }
    const outputValidation = validateLaunchManualOutput(context2.node.id, artifact.output);
    if (!outputValidation.ok) return outputValidation;
    if (!isDeepStrictEqual(artifact.output, context2.output)) {
      return { ok: false, message: "Evidence output does not match the submitted manual output." };
    }
    return { ok: true };
  };
}

// lib/launch/compiler.ts
var capabilityProviders = {
  public_website: { provider: "vercel", resource: "project and deployments" },
  database: {
    provider: "neon",
    resource: "verified project, branch, database, migration, and read/write health"
  },
  transactional_email: { provider: "brevo", resource: "sender, domain, and templates" },
  lifecycle_email: { provider: "brevo", resource: "lifecycle policy and templates" },
  stripe: { provider: "stripe", resource: "product, exact prices, checkout, portal, and webhook" },
  revenuecat: {
    provider: "revenuecat",
    resource: "app, products, entitlement, offering, packages, and webhook"
  },
  ga4: { provider: "google", resource: "GA4 property and data stream" },
  gsc: { provider: "google", resource: "Search Console property and sitemap" },
  bing_webmaster: { provider: "bing", resource: "site and sitemap" },
  vercel_analytics: { provider: "vercel", resource: "verified analytics setting" },
  app_store_connect: {
    provider: "app_store_connect",
    resource: "app record, metadata, builds, and TestFlight groups"
  },
  eas: { provider: "eas", resource: "project, build profiles, build, and submit workflow" }
};
var launchProviderCapabilitiesByNode = {
  "github-repository": [
    "repository",
    "actions_secret",
    "repository_settings",
    "draft_pull_request"
  ],
  "neon-database": [
    "project",
    "branch",
    "database",
    "role",
    "schema_migration",
    "read_write_health_check"
  ],
  "brevo-sending-domain": ["sending_domain"],
  "brevo-domain-verification": ["sending_domain_verification"],
  "brevo-email": ["sender", "template", "webhook"],
  "stripe-commerce": ["product", "price", "webhook", "billing_portal"],
  "google-analytics-property": ["analytics_property"],
  "google-analytics-stream": ["analytics_web_stream"],
  "google-site-dns-record": ["site_verification_token"],
  "google-site-verification": ["site_verification"],
  "google-search-console": ["search_console_site", "search_console_sitemap"],
  "bing-discovery": ["site", "sitemap", "url_submission"],
  "vercel-project": ["project", "environment_variable", "deployment", "domain", "web_analytics"],
  "vercel-database-environment": ["environment_variable"],
  "vercel-stripe-environment": ["environment_variable"],
  "vercel-stripe-webhook-environment": ["environment_variable"],
  "vercel-brevo-environment": ["environment_variable"],
  "vercel-ga-environment": ["environment_variable"],
  "dns-records": ["record"],
  "revenuecat-entitlements": ["project_bootstrap", "app", "entitlement", "offering", "webhook"],
  "eas-build": ["ios_build"],
  "eas-submit": ["app_store_connection", "ios_submit"],
  "testflight-state": ["build_processing", "testflight_group", "build_group_assignment"],
  "production-deploy": ["deployment"]
};
var launchProviderByNode = {
  "github-repository": "github",
  "neon-database": "neon",
  "brevo-sending-domain": "brevo",
  "brevo-domain-verification": "brevo",
  "brevo-email": "brevo",
  "stripe-commerce": "stripe",
  "google-analytics-property": "google",
  "google-analytics-stream": "google",
  "google-site-dns-record": "google",
  "google-site-verification": "google",
  "google-search-console": "google",
  "bing-discovery": "bing",
  "vercel-project": "vercel",
  "vercel-database-environment": "vercel",
  "vercel-stripe-environment": "vercel",
  "vercel-stripe-webhook-environment": "vercel",
  "vercel-brevo-environment": "vercel",
  "vercel-ga-environment": "vercel",
  "production-deploy": "vercel",
  "dns-records": "dns",
  "revenuecat-entitlements": "revenuecat",
  "apple-first-app-record": "app_store_connect",
  "eas-build": "eas",
  "eas-submit": "eas",
  "testflight-state": "app_store_connect"
};
function launchProviderOperationCeiling(definition2) {
  return definition2.nodes.filter((node) => node.kind === "provider").reduce((total, node) => total + Math.max(1, node.authorization.scopes.length), 0);
}
var launchBuildAgentHandlers = /* @__PURE__ */ new Set([
  "launch.prepareRepository",
  "launch.designDirection",
  "launch.buildCoreJourney",
  "launch.configureEventPack",
  "launch.defineValidationGate",
  "launch.prepareConciergeOperations",
  "launch.defineUsageProof"
]);
function launchBuildAgentTaskCount(definition2) {
  return definition2.nodes.filter(
    (node) => node.handler && launchBuildAgentHandlers.has(node.handler)
  ).length;
}
function providerNode(id, purpose, capability, dependencies, transport = "cli", authorizationProfile = "standard_launch") {
  return workflowNode(id, {
    purpose,
    kind: "provider",
    capability,
    dependencies,
    transport,
    handler: `provider.${id}`,
    effect: "external_reversible",
    risk: "medium",
    authorization: {
      required: true,
      profile: authorizationProfile,
      scopes: [...launchProviderCapabilitiesByNode[id] ?? [capability]]
    },
    idempotencyKey: `launch:${id}`,
    timeoutMs: 12e4,
    retry: {
      maxAttempts: 3,
      retryableCodes: ["rate_limited", "provider_unavailable", "timeout"],
      backoff: { strategy: "exponential", initialMs: 500, maxMs: 8e3, multiplier: 2 }
    },
    concurrencyGroup: capability,
    cost: { amount: 0, unit: "unknown" },
    budgetCategory: capability,
    evidence: { required: true, artifact: `reports/launch/provider-${id}.json` },
    completion: { description: `${purpose}; provider state read back and verified` }
  });
}
function manualNode(id, purpose, capability, dependencies, authorizationProfile = "standard_launch") {
  return workflowNode(id, {
    purpose,
    kind: "manual_action",
    capability,
    dependencies,
    transport: "manual",
    handler: void 0,
    output: { validator: launchManualOutputValidatorName(id) },
    effect: "external_reversible",
    risk: "medium",
    authorization: { required: true, profile: authorizationProfile, scopes: [capability] },
    idempotencyKey: `manual:${id}`,
    concurrencyGroup: "manual",
    evidence: { required: true, artifact: `reports/launch/manual-${id}.json` },
    completion: {
      description: `${purpose}; submitted fields and verification evidence validated`,
      validator: launchManualOutputValidatorName(id)
    }
  });
}
function compileLaunchGraph(briefInput, decisionInput) {
  const brief = founderBriefSchema.parse(briefInput);
  const decision = decisionInput ?? routeLaunch(brief);
  if (decision.capabilities.includes("file_storage")) {
    throw new Error(
      "file_storage was requested, but Venture Harness v0.2 has no selected storage provider, typed provider adapter, or manual evidence contract. Next: select and implement an explicit storage capability before compiling the launch graph; storage cannot be silently omitted."
    );
  }
  const activeEventPacks = resolveActiveEventPacks({
    capabilities: decision.capabilities,
    appKind: decision.rail.appKind,
    monetizationModel: brief.monetization_model,
    leadJourney: ["lead_generation", "services"].includes(brief.monetization_model)
  });
  const repositoryReadyNodeId = decision.rail.appKind === "web" ? "finalize-dependencies" : "prepare-repository";
  const modePreparationNodes = [];
  if (decision.mode.selectedMode === "validate_first") {
    modePreparationNodes.push(
      workflowNode("define-validation-gate", {
        purpose: "Define the smallest honest demand test, its primary signal, decision threshold, stop rule, and optional 30/60/90-day gates before product scope expands.",
        kind: "model",
        capability: "validation.strategy",
        dependencies: [repositoryReadyNodeId],
        transport: "model",
        handler: "launch.defineValidationGate",
        model: { tier: "capable" },
        effect: "local_write",
        idempotencyKey: `launch:${brief.id}:validation-gate`,
        concurrencyGroup: "strategy",
        evidence: {
          required: true,
          artifact: "reports/launch/validation-gate.json"
        },
        completion: {
          description: "The validation hypothesis, threshold, stop rule, assumptions, and optional timed gates are explicit."
        }
      })
    );
  }
  if (decision.mode.selectedMode === "concierge_first") {
    modePreparationNodes.push(
      workflowNode("prepare-concierge-operations", {
        purpose: "Define the bounded human-delivery workflow, service limits, disclosures, evidence capture, and handoff before automating the outcome.",
        kind: "model",
        capability: "concierge.operations",
        dependencies: [repositoryReadyNodeId],
        transport: "model",
        handler: "launch.prepareConciergeOperations",
        model: { tier: "capable" },
        effect: "local_write",
        idempotencyKey: `launch:${brief.id}:concierge-operations`,
        concurrencyGroup: "strategy",
        evidence: {
          required: true,
          artifact: "reports/launch/concierge-operations.json"
        },
        completion: {
          description: "The concierge journey is deliverable without deception and has capacity, privacy, escalation, and evidence limits."
        }
      })
    );
  }
  const designDependencies = modePreparationNodes.length > 0 ? modePreparationNodes.map(({ id }) => id) : [repositoryReadyNodeId];
  const postBuildModeNodes = decision.mode.selectedMode === "product_first" ? [
    workflowNode("define-usage-proof", {
      purpose: "Define how real product use will demonstrate the promised value, including activation, retention, quality, and deletion or failure signals.",
      kind: "model",
      capability: "product.usage_proof",
      dependencies: ["build-core-journey", "configure-event-pack"],
      transport: "model",
      handler: "launch.defineUsageProof",
      model: { tier: "capable" },
      effect: "local_write",
      idempotencyKey: `launch:${brief.id}:usage-proof`,
      concurrencyGroup: "strategy",
      evidence: { required: true, artifact: "reports/launch/usage-proof.json" },
      completion: {
        description: "Activation, usage, retention, failure, and deletion evidence are connected to the core journey without collecting private content."
      }
    })
  ] : [];
  const dependencyBootstrapNodes = decision.rail.appKind === "web" ? [
    workflowNode("install-dependencies", {
      purpose: "Install the ordinary child repository's exact lockfile before any product build or quality command runs.",
      capability: "dependencies.install",
      handler: "launch.installDependencies",
      effect: "local_write",
      idempotencyKey: `launch:${brief.id}:install-dependencies`,
      timeoutMs: 3e5,
      retry: {
        maxAttempts: 2,
        retryableCodes: ["DEPENDENCY_INSTALL_FAILED"],
        backoff: { strategy: "none", initialMs: 0, maxMs: 0, multiplier: 1 }
      },
      reconciliation: {
        handler: "launch.installDependencies",
        pollIntervalMs: 0,
        maxPollAttempts: 1
      },
      concurrencyGroup: "local",
      evidence: {
        required: true,
        artifact: "reports/quality/dependency-install.json"
      },
      completion: {
        description: "The exact child lockfile installed successfully through a direct package-manager argv without joining the parent workspace or executing third-party lifecycle scripts."
      }
    })
  ] : [];
  const dependencyFinalizationNodes = decision.rail.appKind === "web" ? [
    workflowNode("finalize-dependencies", {
      purpose: "Install and checkpoint the final exact child lockfile after repository and dependency planning; reject any later package or lock mutation.",
      capability: "dependencies.install",
      dependencies: ["prepare-repository"],
      handler: "launch.installDependencies",
      effect: "local_write",
      idempotencyKey: `launch:${brief.id}:finalize-dependencies`,
      timeoutMs: 3e5,
      retry: {
        maxAttempts: 2,
        retryableCodes: ["DEPENDENCY_INSTALL_FAILED"],
        backoff: { strategy: "none", initialMs: 0, maxMs: 0, multiplier: 1 }
      },
      reconciliation: {
        handler: "launch.installDependencies",
        pollIntervalMs: 0,
        maxPollAttempts: 1
      },
      concurrencyGroup: "local",
      evidence: {
        required: true,
        artifact: "reports/quality/dependency-finalization.json"
      },
      completion: {
        description: "The final package manifest and lockfile are installed, read back, and frozen before product, provider, source publication, or deployment work proceeds."
      }
    })
  ] : [];
  const nodes = [
    ...dependencyBootstrapNodes,
    workflowNode("prepare-repository", {
      purpose: "Create the venture-owned local scaffold and managed-file manifest.",
      capability: "harness.create",
      dependencies: dependencyBootstrapNodes.map(({ id }) => id),
      effect: "local_write",
      handler: "launch.prepareRepository",
      idempotencyKey: `launch:${brief.id}:prepare`,
      concurrencyGroup: "local",
      evidence: { required: true, artifact: "reports/launch/local-scaffold.json" }
    }),
    ...dependencyFinalizationNodes,
    ...modePreparationNodes,
    workflowNode("design-direction", {
      purpose: "Create a distinct, accessible visual direction and responsive composition for the smallest core journey.",
      kind: "model",
      capability: "design.system",
      dependencies: designDependencies,
      transport: "model",
      handler: "launch.designDirection",
      model: { tier: "capable" },
      effect: "local_write",
      idempotencyKey: `launch:${brief.id}:design-direction`,
      concurrencyGroup: "product-build",
      evidence: { required: true, artifact: "reports/launch/design-direction.json" },
      completion: {
        description: "Design thesis, tokens, responsive composition, accessibility constraints, and anti-template audit are recorded."
      }
    }),
    workflowNode("build-core-journey", {
      purpose: `Build and test the smallest useful ${decision.rail.appKind} journey: ${brief.smallest_core_journey}`,
      kind: "model",
      capability: decision.rail.appKind === "web" ? "product.web" : decision.rail.mobileStack === "swiftui" ? "product.swiftui" : "product.expo",
      dependencies: ["design-direction"],
      transport: "model",
      handler: "launch.buildCoreJourney",
      model: { tier: "capable" },
      effect: "local_write",
      idempotencyKey: `launch:${brief.id}:build-core-journey`,
      timeoutMs: 9e5,
      concurrencyGroup: "product-build",
      evidence: { required: true, artifact: "reports/launch/core-journey.json" },
      completion: {
        description: "The declared core journey exists, uses labeled sample data where needed, and has affected tests."
      }
    }),
    workflowNode("configure-event-pack", {
      purpose: "Enable the minimum capability-driven analytics event pack without personal or free-form data.",
      capability: "analytics.event_pack",
      dependencies: ["build-core-journey"],
      handler: "launch.configureEventPack",
      effect: "local_write",
      idempotencyKey: `launch:${brief.id}:event-pack`,
      concurrencyGroup: "analytics",
      evidence: { required: true, artifact: "reports/launch/event-pack.json" }
    }),
    ...postBuildModeNodes,
    workflowNode("verify-local", {
      purpose: "Run affected local tests, schemas, secrets, PII, truth, and core journey checks.",
      capability: "quality.fast",
      dependencies: [
        "build-core-journey",
        "configure-event-pack",
        ...postBuildModeNodes.map(({ id }) => id)
      ],
      handler: "launch.verifyLocal",
      idempotencyKey: `launch:${brief.id}:verify-local`,
      concurrencyGroup: "quality",
      evidence: { required: true, artifact: "reports/quality/launch-fast.json" }
    }),
    providerNode(
      "github-repository",
      "Publish the verified local source tree to the child GitHub repository and read the exact remote commit back.",
      "github_repository",
      ["verify-local"]
    )
  ];
  const completionDependencies = /* @__PURE__ */ new Set(["verify-local", "github-repository"]);
  const preDeployDependencies = /* @__PURE__ */ new Set(["verify-local", "github-repository"]);
  const dnsDependencies = [];
  const has = (capability) => decision.capabilities.includes(capability);
  const needsBrevo = has("transactional_email") || has("lifecycle_email");
  const needsGa4 = has("ga4");
  const needsGsc = has("gsc");
  if (has("database")) {
    nodes.push(
      providerNode(
        "neon-database",
        "Create or use an explicitly identified Neon database, capture a generated connection URI only behind a writable credential reference, apply the executable schema migration, and verify read/write health.",
        "database",
        [repositoryReadyNodeId]
      )
    );
    completionDependencies.add("neon-database");
    preDeployDependencies.add("neon-database");
  }
  if (needsBrevo) {
    nodes.push(
      providerNode(
        "brevo-sending-domain",
        "Create or locate the Brevo sending domain and read back its exact public DNS authentication record plan.",
        "transactional_email",
        [repositoryReadyNodeId],
        "api"
      )
    );
    dnsDependencies.push("brevo-sending-domain");
  }
  if (has("stripe")) {
    nodes.push(
      providerNode(
        "stripe-commerce",
        "Create and verify test-mode product, exact prices, checkout, portal, and webhook.",
        "stripe",
        [repositoryReadyNodeId],
        "api"
      )
    );
    completionDependencies.add("stripe-commerce");
    preDeployDependencies.add("stripe-commerce");
  }
  if (needsGa4) {
    nodes.push(
      providerNode(
        "google-analytics-property",
        "Create or locate the GA4 property and read the exact property identifier back.",
        "google_discovery",
        [repositoryReadyNodeId],
        "api"
      ),
      providerNode(
        "google-analytics-stream",
        "Create or locate the GA4 web stream from the same-run property identifier and read its measurement identifier back.",
        "google_discovery",
        ["google-analytics-property"],
        "api"
      )
    );
    completionDependencies.add("google-analytics-stream");
    preDeployDependencies.add("google-analytics-stream");
  }
  if (needsGsc) {
    nodes.push(
      providerNode(
        "google-site-dns-record",
        "Request and read back the exact Google DNS verification token without claiming site ownership.",
        "google_discovery",
        [repositoryReadyNodeId],
        "api"
      )
    );
    dnsDependencies.push("google-site-dns-record");
  }
  if (has("bing_webmaster")) {
    nodes.push(
      providerNode(
        "bing-discovery",
        "Add or locate the exact Bing site after production is reachable, submit its sitemap, and verify data access.",
        "bing_webmaster",
        ["production-deploy"],
        "api"
      )
    );
    completionDependencies.add("bing-discovery");
  }
  nodes.push(
    providerNode(
      "vercel-project",
      "Create or link the explicitly scoped Vercel project, configure its domain when declared, deploy preview, and read project and deployment state back.",
      "public_website",
      [...preDeployDependencies].sort()
    )
  );
  completionDependencies.add("vercel-project");
  dnsDependencies.push("vercel-project");
  const environmentNodes = [];
  if (has("database")) {
    environmentNodes.push(
      providerNode(
        "vercel-database-environment",
        "Bind the brokered Neon connection URI to the Vercel production DATABASE_URL variable and verify only its name and target by read-back.",
        "public_website",
        ["neon-database", "vercel-project"]
      )
    );
  }
  if (has("stripe")) {
    environmentNodes.push(
      providerNode(
        "vercel-stripe-environment",
        "Bind the restricted Stripe credential reference to STRIPE_SECRET_KEY in Vercel production without reading its value back.",
        "public_website",
        ["stripe-commerce", "vercel-project"]
      ),
      providerNode(
        "vercel-stripe-webhook-environment",
        "Bind the captured Stripe webhook signing secret reference to STRIPE_WEBHOOK_SECRET in Vercel production without persisting the value.",
        "public_website",
        ["stripe-commerce", "vercel-project"]
      )
    );
  }
  if (needsBrevo) {
    environmentNodes.push(
      providerNode(
        "vercel-brevo-environment",
        "Bind the restricted Brevo credential reference to BREVO_API_KEY in Vercel production without reading its value back.",
        "public_website",
        ["brevo-sending-domain", "vercel-project"]
      )
    );
  }
  if (needsGa4) {
    environmentNodes.push(
      providerNode(
        "vercel-ga-environment",
        "Bind the read-back GA4 measurement identifier reference to NEXT_PUBLIC_GA_MEASUREMENT_ID in Vercel production.",
        "public_website",
        ["google-analytics-stream", "vercel-project"]
      )
    );
  }
  nodes.push(...environmentNodes);
  for (const node of environmentNodes) {
    completionDependencies.add(node.id);
    preDeployDependencies.add(node.id);
  }
  const needsDnsRecords = Boolean(brief.domain) || has("stripe") || needsBrevo || needsGa4 || needsGsc;
  if (needsDnsRecords) {
    if (brief.preferred_dns_provider === "mijndomein" || brief.preferred_dns_provider === "manual") {
      nodes.push(
        manualNode(
          "dns-records",
          "Apply one ordered DNS task preserving existing MX, SPF, DKIM, and DMARC records, then verify propagation.",
          "dns",
          [...new Set(dnsDependencies)].sort()
        )
      );
    } else {
      nodes.push(
        providerNode(
          "dns-records",
          "Apply additive DNS records idempotently and verify propagation without replacing nameservers.",
          "dns",
          [...new Set(dnsDependencies)].sort(),
          "api"
        )
      );
    }
    completionDependencies.add("dns-records");
  }
  if (needsBrevo) {
    nodes.push(
      providerNode(
        "brevo-domain-verification",
        "Authenticate the Brevo sending domain only after the planned DNS records are verified, then require authenticated=true by read-back.",
        "transactional_email",
        ["dns-records", "brevo-sending-domain"],
        "api"
      ),
      providerNode(
        "brevo-email",
        "Create and read back the reviewed sender, inactive template, and optional webhook without sending email.",
        "transactional_email",
        ["brevo-domain-verification"],
        "api"
      )
    );
    completionDependencies.add("brevo-email");
  }
  if (needsGsc) {
    nodes.push(
      providerNode(
        "google-site-verification",
        "Ask Google Site Verification to verify the already-published DNS token and read ownership back.",
        "google_discovery",
        ["dns-records", "google-site-dns-record"],
        "api"
      ),
      providerNode(
        "google-search-console",
        "Add and read back the Search Console property and sitemap after Google ownership verification; do not infer indexing.",
        "google_discovery",
        ["google-site-verification"],
        "api"
      )
    );
    completionDependencies.add("google-search-console");
  }
  if (decision.rail.appKind !== "web") {
    nodes.push(
      manualNode(
        "apple-first-app-record",
        "Create the first App Store Connect app record and return app name, bundle ID, SKU, language, Apple app ID, and team ID.",
        "app_store_connect",
        [repositoryReadyNodeId],
        "mobile_testflight"
      )
    );
    if (has("revenuecat")) {
      nodes.push(
        providerNode(
          "revenuecat-entitlements",
          "Configure Test Store app, products, entitlement, offering, packages, and webhook; keep Apple products pending until verified.",
          "revenuecat",
          [repositoryReadyNodeId],
          "api"
        )
      );
      completionDependencies.add("revenuecat-entitlements");
      preDeployDependencies.add("revenuecat-entitlements");
    }
    if (has("eas")) {
      nodes.push(
        providerNode(
          "eas-build",
          "Configure EAS project/build profiles and produce a reproducible iOS build.",
          "eas",
          ["build-core-journey", "verify-local"],
          "cli",
          "mobile_testflight"
        ),
        providerNode(
          "eas-submit",
          "Connect the exact same-run Apple app record, submit the verified EAS build, and read submission state back without claiming TestFlight processing.",
          "eas",
          ["apple-first-app-record", "eas-build"],
          "cli",
          "mobile_testflight"
        ),
        providerNode(
          "testflight-state",
          "Read the submitted build from App Store Connect by app/version/build number, require VALID processing, and verify its TestFlight group assignment without claiming publication.",
          "app_store_connect",
          ["apple-first-app-record", "eas-build", "eas-submit"],
          "api",
          "mobile_testflight"
        )
      );
      completionDependencies.add("testflight-state");
      preDeployDependencies.add("testflight-state");
    }
  }
  nodes.push(
    workflowNode("verify-launch", {
      purpose: "Verify the built application, critical journey, and every provider prerequisite required before production deployment.",
      capability: "quality.mvp",
      dependencies: [...preDeployDependencies, "vercel-project"].sort(),
      handler: "launch.verifyMvp",
      idempotencyKey: `launch:${brief.id}:verify-mvp`,
      concurrencyGroup: "quality",
      evidence: { required: true, artifact: "reports/quality/launch-mvp.json" }
    }),
    providerNode(
      "production-deploy",
      "Deploy the verified web surface to production and read the immutable deployment and domain state back.",
      "public_website",
      ["verify-launch"]
    ),
    workflowNode("verify-production", {
      purpose: "Run read-only HTTPS smoke plus desktop and mobile critical-surface journeys against the exact production URL returned by deployment read-back.",
      capability: "quality.post_deploy",
      dependencies: ["production-deploy"],
      handler: "launch.verifyProduction",
      effect: "read",
      idempotencyKey: `launch:${brief.id}:verify-production`,
      concurrencyGroup: "quality",
      evidence: { required: true, artifact: "reports/quality/post-deploy.json" }
    }),
    workflowNode("launch-report", {
      purpose: "Write sanitized human and JSON launch reports with only genuine remaining actions.",
      capability: "launch.report",
      dependencies: [.../* @__PURE__ */ new Set(["verify-production", ...completionDependencies])].sort(),
      handler: "launch.report",
      idempotencyKey: `launch:${brief.id}:report`,
      concurrencyGroup: "report",
      evidence: { required: true, artifact: "reports/launch/final.json" }
    })
  );
  const budgets = Object.fromEntries(
    [...new Set(nodes.map((node) => node.budgetCategory))].map((category) => [category, 0])
  );
  return defineWorkflow({
    id: `launch-${brief.id}`,
    name: `Launch ${brief.name}`,
    version: "0.2.0",
    nodes,
    maxParallel: 4,
    // This is a scheduler safety bound, not a product-loop budget. A launch
    // graph can legitimately need several parallel batches plus resumptions.
    maxIterations: Math.max(50, nodes.length * 4),
    budgets,
    metadata: {
      synthetic: brief.synthetic ?? false,
      launchMode: decision.mode.selectedMode,
      appKind: decision.rail.appKind,
      paymentProvider: decision.payment.provider,
      activeEventPacks
    }
  });
}
function parallelLayers(graph) {
  const layer = /* @__PURE__ */ new Map();
  for (const id of topologicalOrder(graph)) {
    const node = graph.nodes.find((candidate) => candidate.id === id);
    layer.set(
      id,
      node.dependencies.length === 0 ? 0 : 1 + Math.max(...node.dependencies.map((dep) => layer.get(dep) ?? 0))
    );
  }
  const out = [];
  for (const [id, index] of layer) (out[index] ??= []).push(id);
  return out.map((ids) => ids.sort());
}
function criticalPath(graph) {
  const best = /* @__PURE__ */ new Map();
  for (const id of topologicalOrder(graph)) {
    const node = graph.nodes.find((candidate) => candidate.id === id);
    const prefix = node.dependencies.map((dependency) => best.get(dependency) ?? []).sort((a, b) => b.length - a.length || a.join().localeCompare(b.join()))[0] ?? [];
    best.set(id, [...prefix, id]);
  }
  return [...best.values()].sort((a, b) => b.length - a.length || a.join().localeCompare(b.join()))[0] ?? [];
}
function compileLaunchDryRun(briefInput) {
  const brief = founderBriefSchema.parse(briefInput);
  const decision = routeLaunch(brief);
  const graph = compileLaunchGraph(brief, decision);
  const eventPacks = resolveActiveEventPacks({
    capabilities: decision.capabilities,
    appKind: decision.rail.appKind,
    monetizationModel: brief.monetization_model,
    leadJourney: ["lead_generation", "services"].includes(brief.monetization_model)
  });
  const resources = [];
  for (const capability of decision.capabilities) {
    const mapped = capabilityProviders[capability];
    if (!mapped) continue;
    resources.push({
      provider: mapped.provider,
      resource: mapped.resource,
      environment: mapped.provider === "stripe" ? "test" : mapped.provider === "revenuecat" ? "test" : "preview",
      estimatedCost: mapped.provider === "eas" ? "unknown" : 0,
      directChargeBasis: mapped.provider === "eas" ? null : "reviewed_known_zero_direct_charge",
      ongoingAccountPlanUsageCovered: false
    });
  }
  return {
    synthetic: brief.synthetic ?? false,
    decision,
    eventPacks,
    graph,
    resources,
    manualActions: graph.nodes.filter((node) => node.kind === "manual_action").map((node) => {
      const contract = launchManualActionContracts[node.id];
      if (!contract) throw new Error(`No manual action contract exists for ${node.id}.`);
      return { purpose: node.purpose, ...contract };
    }),
    criticalPath: criticalPath(graph),
    parallelLayers: parallelLayers(graph),
    authorizationRequirements: [
      ...new Set(
        graph.nodes.filter((node) => node.authorization.required).flatMap((node) => node.authorization.scopes)
      )
    ].sort(),
    verificationCommands: ["pnpm verify:fast", "pnpm verify:mvp", "pnpm test:fixtures"]
  };
}

// lib/launch/authorization-scope.ts
var PREVIEW_PROVIDER_NODES = /* @__PURE__ */ new Set(["github-repository", "vercel-project"]);
function canonicalProfile2(profile2) {
  const canonical = profile2.replaceAll("-", "_");
  if (![
    "read_only",
    "build_local",
    "preview_launch",
    "standard_launch",
    "live_commerce_launch",
    "mobile_testflight",
    "autofix_low_risk"
  ].includes(canonical)) {
    throw new Error(`Unknown authorization profile ${profile2}.`);
  }
  return canonical;
}
function isMobile(definition2) {
  return definition2.metadata?.appKind !== "web";
}
function scopeLaunchGraphForAuthorization(definition2, requestedProfile) {
  const profile2 = canonicalProfile2(requestedProfile);
  if (profile2 === "read_only" || profile2 === "autofix_low_risk") {
    throw new Error(
      `${profile2} cannot apply a launch. Use vh plan or vh launch --dry-run, or choose build-local, preview-launch, standard-launch, live-commerce-launch, or mobile-testflight.`
    );
  }
  const mobile = isMobile(definition2);
  if (mobile && profile2 !== "mobile_testflight" && profile2 !== "build_local") {
    throw new Error(
      `${profile2} does not authorize the mobile rail. Use build-local for repository-only work or mobile-testflight for the compiled iOS/TestFlight graph.`
    );
  }
  if (!mobile && profile2 === "mobile_testflight") {
    throw new Error(
      "mobile_testflight does not match a web-only rail. Use build-local, preview-launch, standard-launch, or live-commerce-launch."
    );
  }
  const keep = (node) => {
    if (profile2 === "build_local") {
      return node.id === "launch-report" || node.kind !== "provider" && node.kind !== "manual_action" && node.id !== "verify-launch" && node.id !== "verify-production";
    }
    if (profile2 === "preview_launch") {
      return node.id === "launch-report" || node.id === "verify-launch" || (node.kind === "code" || node.kind === "model") && node.id !== "verify-production" || PREVIEW_PROVIDER_NODES.has(node.id);
    }
    return true;
  };
  const selected = definition2.nodes.filter(keep);
  const selectedIds = new Set(selected.map(({ id }) => id));
  const nodes = selected.map((node) => {
    const dependencies = node.dependencies.filter((dependency) => selectedIds.has(dependency));
    if (node.id === "launch-report") {
      return {
        ...node,
        dependencies: profile2 === "build_local" ? ["verify-local"] : profile2 === "preview_launch" ? ["verify-launch"] : dependencies
      };
    }
    return { ...node, dependencies };
  });
  return defineWorkflow({
    ...definition2,
    nodes,
    metadata: {
      ...definition2.metadata ?? {},
      authorizationProfile: profile2,
      launchScope: profile2 === "build_local" ? "local" : profile2 === "preview_launch" ? "preview" : "full"
    }
  });
}
function nodeEnvironment(nodeId) {
  if (["stripe-commerce", "revenuecat-entitlements"].includes(nodeId)) return "test";
  if ([
    "dns-records",
    "apple-first-app-record",
    "eas-build",
    "eas-submit",
    "testflight-state",
    "production-deploy"
  ].includes(nodeId)) {
    return "production";
  }
  return "preview";
}
function requiredEnvironmentsForLaunch(definition2) {
  const environments2 = /* @__PURE__ */ new Set();
  for (const node of definition2.nodes) {
    if (node.kind === "provider" || node.kind === "manual_action") {
      environments2.add(nodeEnvironment(node.id));
    } else {
      environments2.add("local");
    }
  }
  const order = ["local", "test", "preview", "production"];
  return order.filter((environment) => environments2.has(environment));
}
function requiredCapabilitiesForLaunch(definition2) {
  return [
    ...new Set(
      definition2.nodes.filter((node) => node.kind === "provider").flatMap((node) => node.authorization.scopes)
    )
  ].sort();
}

// lib/founder-launch/idea.ts
var DEFAULT_ASSUMPTIONS = Object.freeze({
  audience: "The initial user is the narrow early-adopter group described by the founder.",
  problem: "The first release addresses the single highest-friction job described in the idea.",
  outcome: "The first release proves one useful outcome rather than a broad product suite.",
  journey: "The smallest journey is discover, start, complete the core outcome, and verify it.",
  signal: "The primary signal is one verified completion of the smallest core journey."
});
function slug(value) {
  const normalized = value.normalize("NFKC").toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "").slice(0, 63);
  return /^[a-z][a-z0-9-]+$/.test(normalized) ? normalized : `venture-${createHash5("sha256").update(value).digest("hex").slice(0, 12)}`;
}
function textLine(source, labels) {
  for (const label of labels) {
    const match = source.match(new RegExp(`^\\s*(?:[-*]\\s*)?${label}\\s*:\\s*(.+?)\\s*$`, "im"));
    if (match?.[1]) return match[1].trim();
  }
  return void 0;
}
function heading(source) {
  return source.match(/^\s*#\s+(.+?)\s*$/m)?.[1]?.trim();
}
function booleanLine(source, labels, fallback) {
  const raw = textLine(source, labels)?.toLowerCase();
  if (!raw) return fallback;
  if (["yes", "true", "required", "on"].includes(raw)) return true;
  if (["no", "false", "none", "off"].includes(raw)) return false;
  return fallback;
}
function moneyLine(source, labels) {
  const raw = textLine(source, labels);
  if (!raw) return null;
  const normalized = raw.replace(",", ".");
  const match = normalized.match(/(?:^|\s)(\d+(?:\.\d{1,2})?)(?:\s|$|\/)/u);
  if (!match?.[1]) return null;
  const amount = Number(match[1]);
  if (!Number.isFinite(amount) || amount <= 0 || !Number.isSafeInteger(amount * 100)) {
    throw new Error(`${labels[0]} must be a positive amount with at most two decimal places`);
  }
  return amount;
}
function commercialTerms(source, brief) {
  const monthlyPrice = moneyLine(source, ["Monthly price", "Price per month"]);
  const annualPrice = moneyLine(source, ["Annual price", "Price per year"]);
  const generalPrice = moneyLine(source, ["Price"]);
  const generalInterval = textLine(source, ["Billing", "Billing interval"])?.toLowerCase();
  return Object.freeze({
    currency: brief.currency ?? "EUR",
    monthlyPrice: monthlyPrice ?? (generalPrice !== null && generalInterval !== "annual" && generalInterval !== "yearly" ? generalPrice : null),
    annualPrice: annualPrice ?? (generalPrice !== null && (generalInterval === "annual" || generalInterval === "yearly") ? generalPrice : null)
  });
}
function frontMatter(source) {
  const match = source.match(/^---\s*\r?\n([\s\S]*?)\r?\n---(?:\s*\r?\n|$)/);
  return match?.[1] ? parseYaml(match[1]) : void 0;
}
function structuredBrief(source) {
  const candidates = [
    frontMatter(source),
    (() => {
      try {
        return parseYaml(source);
      } catch {
        return void 0;
      }
    })()
  ];
  for (const candidate of candidates) {
    const result2 = founderBriefSchema.safeParse(candidate);
    if (result2.success) return result2.data;
  }
  return void 0;
}
function assertSafeIdea(source) {
  const credential = findCredentialMaterial(source);
  if (credential) {
    throw new Error(
      `Founder idea contains forbidden credential-like material (${credential.kind}); use cred:// references only`
    );
  }
  if (/^\s*(?:[-*]\s*)?(?:[^:\n]{0,80}\b)?(?:api[ _-]?key|token|password|secret|credential|authorization(?:\s+header)?)\s*:/imu.test(
    source
  )) {
    throw new Error(
      "Founder idea contains a credential-labeled field; remove the value and use a cred:// reference outside the idea"
    );
  }
  if (source.trim().length < 12)
    throw new Error("Founder idea must contain at least 12 non-whitespace characters");
  if (source.length > 1e5)
    throw new Error("Founder idea exceeds the 100000-character launch limit");
}
function markdownBrief(source) {
  const title = textLine(source, ["Name", "Product", "Venture"]) ?? heading(source) ?? "Founder Venture";
  const explicitAudience = textLine(source, ["Audience", "User", "Initial user", "ICP"]);
  const explicitProblem = textLine(source, ["Problem", "Job", "Problem or job"]);
  const explicitOutcome = textLine(source, ["Outcome", "Core outcome", "Intended outcome"]);
  const explicitJourney = textLine(source, ["Journey", "Core journey", "Smallest journey"]);
  const explicitSignal = textLine(source, ["Success", "Success signal", "Primary signal"]);
  const assumptionsAdded = [];
  const assume = (value, fallback, label) => {
    if (value) return value;
    assumptionsAdded.push(`${label}: ${fallback}`);
    return fallback;
  };
  const commerce = (textLine(source, ["Commerce", "Monetization", "Revenue model"]) ?? "subscription").toLowerCase();
  const appKindRaw = (textLine(source, ["Rail", "App", "App kind"]) ?? "web").toLowerCase();
  const appKind = appKindRaw.includes("hybrid") ? "hybrid" : appKindRaw.includes("ios") || appKindRaw.includes("mobile") ? "ios" : "web";
  const monetizationModel = commerce.includes("none") || commerce.includes("free") ? "none" : commerce.includes("one") ? "one_time" : commerce.includes("usage") ? "usage_based" : commerce.includes("service") ? "services" : "subscription";
  const domain = textLine(source, ["Domain"]);
  const repositoryVisibility = (textLine(source, ["Repository visibility", "Visibility"]) ?? "private").toLowerCase();
  const brief = founderBriefSchema.parse({
    id: slug(textLine(source, ["Slug", "Id"]) ?? title),
    name: title.slice(0, 100),
    specific_user_or_audience: assume(
      explicitAudience,
      DEFAULT_ASSUMPTIONS.audience,
      "Audience assumption"
    ),
    problem_or_job: assume(explicitProblem, DEFAULT_ASSUMPTIONS.problem, "Problem assumption"),
    intended_outcome: assume(explicitOutcome, DEFAULT_ASSUMPTIONS.outcome, "Outcome assumption"),
    smallest_core_journey: assume(
      explicitJourney,
      DEFAULT_ASSUMPTIONS.journey,
      "Journey assumption"
    ),
    primary_success_signal: slug(
      assume(explicitSignal, DEFAULT_ASSUMPTIONS.signal, "Signal assumption")
    ).replaceAll("-", "_"),
    material_constraints: [
      "Do not fabricate provider, customer, demand, or verification state",
      "Keep credentials outside the generated repository"
    ],
    known_truths: [
      "The founder supplied this idea as the input for the initial launch compilation.",
      ...explicitAudience ? ["The initial audience was explicitly supplied."] : [],
      ...explicitProblem ? ["The initial problem or job was explicitly supplied."] : [],
      ...explicitOutcome ? ["The intended outcome was explicitly supplied."] : [],
      ...explicitJourney ? ["The smallest core journey was explicitly supplied."] : [],
      ...explicitSignal ? ["The primary success signal was explicitly supplied."] : []
    ],
    assumptions: assumptionsAdded,
    app_kind: appKind,
    requested_mobile_stack: appKind === "web" ? "none" : "auto",
    business_model: "b2b",
    monetization_model: monetizationModel,
    native_digital_goods: appKind !== "web" && commerce.includes("native"),
    target_market: textLine(source, ["Market", "Target market"]) ?? null,
    domain: domain ?? null,
    locale: textLine(source, ["Locale"]) ?? "en-US",
    currency: (textLine(source, ["Currency"]) ?? "EUR").toUpperCase(),
    timezone: textLine(source, ["Timezone"]) ?? "Europe/Amsterdam",
    repository_visibility: repositoryVisibility === "public" ? "public" : "private",
    bundle_identifier: null,
    app_scheme: null,
    factors: {
      smallest_useful_build_cost: "low",
      smallest_useful_build_time: "low",
      reversibility: "high",
      regulatory_or_safety_risk: "low",
      real_usage_required: "high",
      marketplace_cold_start: "low",
      operational_burden: "moderate",
      founder_evidence: "low",
      concierge_delivery_fit: "low",
      app_store_required: appKind === "web" ? "low" : "high",
      deep_native_requirements: "low",
      on_device_requirements: "low"
    },
    needs: {
      authenticated_product: booleanLine(source, ["Auth", "Authentication"], true),
      database: booleanLine(source, ["Database"], true),
      file_storage: booleanLine(source, ["File storage"], false),
      transactional_email: booleanLine(source, ["Email", "Transactional email"], true),
      lifecycle_email: booleanLine(source, ["Lifecycle email"], false),
      feedback: booleanLine(source, ["Feedback"], true),
      analytics: booleanLine(source, ["Analytics"], true),
      search_discovery: booleanLine(source, ["Search", "SEO", "Discovery"], true),
      scheduled_learning: booleanLine(source, ["Learning", "Scheduled learning"], true)
    },
    preferred_dns_provider: (textLine(source, ["DNS", "DNS provider"]) ?? "manual").toLowerCase() === "mijndomein" ? "mijndomein" : "manual",
    ...booleanLine(source, ["Synthetic", "Fixture"], false) ? { synthetic: true } : {},
    deceptive_request: false,
    unsafe_non_defaultable_choice: null,
    indispensable_missing_credential: null
  });
  return { brief, assumptionsAdded };
}
function compileFounderIdea(source) {
  assertSafeIdea(source);
  const hash = createHash5("sha256").update(source).digest("hex");
  const structured = structuredBrief(source);
  if (structured) {
    return Object.freeze({
      brief: structured,
      sourceHash: hash,
      sourceKind: "structured_brief",
      assumptionsAdded: Object.freeze([]),
      commercialTerms: commercialTerms(source, structured)
    });
  }
  const compiled = markdownBrief(source);
  return Object.freeze({
    brief: compiled.brief,
    sourceHash: hash,
    sourceKind: "markdown_idea",
    assumptionsAdded: Object.freeze([...compiled.assumptionsAdded]),
    commercialTerms: commercialTerms(source, compiled.brief)
  });
}

// lib/founder-launch/orchestrator.ts
import { createHash as createHash9 } from "node:crypto";
import { execFileSync } from "node:child_process";
import { lstatSync as lstatSync3, readFileSync as readFileSync7, realpathSync as realpathSync3 } from "node:fs";
import { isAbsolute as isAbsolute2, relative as relative5, resolve as resolve8, sep as sep5 } from "node:path";

// lib/materialization/grant.ts
import { createHash as createHash6 } from "node:crypto";
var semver = external_exports.string().regex(/^\d+\.\d+\.\d+$/);
var slug2 = external_exports.string().regex(/^[a-z][a-z0-9-]{1,62}$/);
var effectSchema = external_exports.enum([
  "repository.create",
  "company_stack.provision",
  "source.push",
  "preview.deploy",
  "production.deploy",
  "domain.configure",
  "commerce.configure",
  "loops.schedule"
]);
var providerDestinationSchema = external_exports.object({
  capability: external_exports.string().regex(/^[a-z][a-z0-9_.-]+$/),
  provider: external_exports.string().regex(/^[a-z][a-z0-9-]+$/),
  externalAccountId: external_exports.string().min(1),
  ownerOrganizationId: external_exports.string().min(1),
  stackClass: external_exports.literal("company"),
  ownership: external_exports.enum(["company_owned", "company_owned_dedicated_account"])
}).strict();
var launchGrantBodySchema = external_exports.object({
  ownerOrganizationId: external_exports.string().min(1),
  ventureName: external_exports.string().min(1).max(100),
  ventureSlug: slug2,
  ideaDigest: external_exports.string().regex(/^[a-f0-9]{64}$/),
  seed: external_exports.object({
    id: external_exports.enum(["agentic-web-saas", "agentic-ios-subscription", "hybrid-agentic-service"]),
    version: semver
  }).strict(),
  stackProfile: external_exports.object({ id: slug2, version: semver }).strict(),
  repository: external_exports.object({
    owner: external_exports.string().min(1),
    name: slug2,
    visibility: external_exports.enum(["private", "public"])
  }).strict(),
  providerAccounts: external_exports.array(providerDestinationSchema),
  autonomyProfile: external_exports.enum(["plan_only", "owner_preview", "owner_live_launch"]),
  allowedExternalEffects: external_exports.array(effectSchema).min(1),
  modelBudget: external_exports.object({
    maxTokens: external_exports.number().int().nonnegative(),
    maxMinorUnits: external_exports.number().int().nonnegative(),
    currency: external_exports.string().regex(/^[A-Z]{3}$/)
  }).strict().optional(),
  externalResourceBudget: external_exports.object({
    maxResources: external_exports.number().int().positive(),
    maxMinorUnits: external_exports.number().int().nonnegative(),
    currency: external_exports.string().regex(/^[A-Z]{3}$/)
  }).strict().optional(),
  modelExecutionPolicy: external_exports.discriminatedUnion("mode", [
    external_exports.object({
      mode: external_exports.literal("chatgpt_subscription_non_metered"),
      maxBuildAgentTasks: external_exports.number().int().positive(),
      attestation: external_exports.literal("codex_login_status_chatgpt_subscription"),
      usageAccounting: external_exports.literal("observational")
    }).strict(),
    external_exports.object({
      mode: external_exports.literal("fixture_no_model_execution"),
      maxBuildAgentTasks: external_exports.number().int().positive(),
      attestation: external_exports.literal("fixture_build_host"),
      usageAccounting: external_exports.literal("none")
    }).strict()
  ]).optional(),
  providerOperationBudget: external_exports.object({
    maxOperations: external_exports.number().int().positive(),
    maxDirectChargeMinorUnits: external_exports.number().int().nonnegative(),
    currency: external_exports.string().regex(/^[A-Z]{3}$/),
    estimateBasis: external_exports.literal("reviewed_known_zero_direct_charge"),
    ongoingAccountPlanUsageCovered: external_exports.literal(false)
  }).strict().optional(),
  permissions: external_exports.object({
    productionDeployment: external_exports.boolean(),
    domainConfiguration: external_exports.boolean(),
    liveCommerceConfiguration: external_exports.boolean()
  }).strict(),
  createdAt: external_exports.string().datetime({ offset: true }),
  expiresAt: external_exports.string().datetime({ offset: true }),
  grantedBy: external_exports.object({
    actorId: external_exports.string().min(1),
    actorType: external_exports.enum(["founder", "organization_owner"])
  }).strict(),
  approvalRef: external_exports.string().min(1),
  revokedAt: external_exports.string().datetime({ offset: true }).nullable().default(null)
}).strict().superRefine((value, context2) => {
  if (new Date(value.expiresAt) <= new Date(value.createdAt)) {
    context2.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["expiresAt"],
      message: "Launch Grant expiry must be after creation"
    });
  }
  if (value.providerAccounts.some(
    (account) => account.ownerOrganizationId !== value.ownerOrganizationId
  )) {
    context2.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["providerAccounts"],
      message: "CompanyStack accounts must be owned by the grant owner organization"
    });
  }
  const effects = new Set(value.allowedExternalEffects);
  if (effects.size !== value.allowedExternalEffects.length) {
    context2.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["allowedExternalEffects"],
      message: "Launch Grant effects must be unique"
    });
  }
  if (effects.has("production.deploy") && !value.permissions.productionDeployment) {
    context2.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["permissions", "productionDeployment"],
      message: "production.deploy requires explicit production deployment permission"
    });
  }
  if (effects.has("domain.configure") && !value.permissions.domainConfiguration) {
    context2.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["permissions", "domainConfiguration"],
      message: "domain.configure requires explicit domain permission"
    });
  }
  if (effects.has("commerce.configure") && !value.permissions.liveCommerceConfiguration) {
    context2.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["permissions", "liveCommerceConfiguration"],
      message: "commerce.configure requires explicit live-commerce permission"
    });
  }
  if (!value.modelExecutionPolicy && !value.modelBudget) {
    context2.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["modelBudget"],
      message: "Launch Grant requires either a canonical model execution policy or the legacy hard-metered model budget"
    });
  }
  if (!value.providerOperationBudget && !value.externalResourceBudget) {
    context2.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["externalResourceBudget"],
      message: "Launch Grant requires either a canonical provider-operation budget or the legacy hard-metered external budget"
    });
  }
  if (value.providerOperationBudget && value.externalResourceBudget && (value.providerOperationBudget.currency !== value.externalResourceBudget.currency || value.providerOperationBudget.maxOperations !== value.externalResourceBudget.maxResources || value.providerOperationBudget.maxDirectChargeMinorUnits !== value.externalResourceBudget.maxMinorUnits)) {
    context2.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["providerOperationBudget"],
      message: "Canonical provider-operation budget must match the legacy compatibility fields"
    });
  }
});
var launchGrantSchema = external_exports.object({
  grantId: external_exports.string().regex(/^lg_[a-f0-9]{26}$/),
  schemaVersion: external_exports.literal(1)
}).merge(launchGrantBodySchema.innerType()).strict();
function stable2(value) {
  if (Array.isArray(value)) return `[${value.map(stable2).join(",")}]`;
  if (value !== null && typeof value === "object") {
    return `{${Object.entries(value).sort(([left], [right]) => left.localeCompare(right)).map(([key, item]) => `${JSON.stringify(key)}:${stable2(item)}`).join(",")}}`;
  }
  return JSON.stringify(value);
}
function freeze(value) {
  if (value !== null && typeof value === "object") {
    for (const child of Object.values(value)) freeze(child);
    Object.freeze(value);
  }
  return value;
}
function immutableGrantBody(body) {
  return Object.fromEntries(Object.entries(body).filter(([key]) => key !== "revokedAt"));
}
function createLaunchGrant(input) {
  const body = launchGrantBodySchema.parse(input);
  const grantId = `lg_${createHash6("sha256").update(stable2(immutableGrantBody(body))).digest("hex").slice(0, 26)}`;
  return freeze(launchGrantSchema.parse({ grantId, schemaVersion: 1, ...body }));
}
function parseLaunchGrant(input) {
  const parsed = launchGrantSchema.parse(input);
  const grantId = parsed.grantId;
  const candidateBody = Object.fromEntries(
    Object.entries(parsed).filter(([key]) => key !== "grantId" && key !== "schemaVersion")
  );
  const body = launchGrantBodySchema.parse(candidateBody);
  const expectedId = `lg_${createHash6("sha256").update(stable2(immutableGrantBody(body))).digest("hex").slice(0, 26)}`;
  if (grantId !== expectedId)
    throw new Error("Launch Grant content does not match its immutable ID");
  return freeze({ grantId, schemaVersion: 1, ...body });
}
function assertLaunchGrantActive(grant, at) {
  parseLaunchGrant(grant);
  if (grant.revokedAt) throw new Error("Launch Grant is revoked");
  if (at < new Date(grant.createdAt)) throw new Error("Launch Grant is not active yet");
  if (at >= new Date(grant.expiresAt)) throw new Error("Launch Grant is expired");
}
function assertLaunchEffectAuthorized(grant, effect, at) {
  assertLaunchGrantActive(grant, at);
  if (!grant.allowedExternalEffects.includes(effect)) {
    throw new Error(`Launch Grant does not authorize ${effect}`);
  }
}

// lib/materialization/materializer.ts
import { createHash as createHash8 } from "node:crypto";
import { lstat, mkdir as mkdir2, readdir, unlink as unlink2, writeFile } from "node:fs/promises";
import { dirname as dirname3, relative as relative3, resolve as resolve6, sep as sep3 } from "node:path";
import { stringify as stringify2 } from "yaml";

// lib/runtime/file-idempotency-ledger.ts
import { createHash as createHash7, randomBytes as randomBytes2 } from "node:crypto";
import { mkdir, open, readFile, rename, unlink } from "node:fs/promises";
import { dirname as dirname2 } from "node:path";
function digest(key) {
  return createHash7("sha256").update(key).digest("hex");
}
var SAFE_PROVIDER_CODES = /* @__PURE__ */ new Set([
  "retryable_rate_limit",
  "retryable_outage",
  "retryable_network",
  "terminal_auth",
  "terminal_validation",
  "terminal_conflict",
  "terminal_unknown",
  "transport_exception",
  "shell_binary_forbidden",
  "jwt_signer_missing",
  "jwt_signing_failed",
  "idempotency_conflict",
  "unknown_outcome_reconciliation_required"
]);
function safeProviderCode(code) {
  if (!code) return void 0;
  return SAFE_PROVIDER_CODES.has(code) || /^exit_\d{1,3}$/.test(code) ? code : void 0;
}
var LEDGER_ID = /^ledger_[a-f0-9]{64}$/u;
function newLedgerId() {
  return `ledger_${randomBytes2(32).toString("hex")}`;
}
function emptyDocument(ledgerId) {
  return { version: 4, ledgerId, entries: {} };
}
var REPLAY_PATH = /^(?:[A-Za-z][A-Za-z0-9_-]*|\d+)(?:\.(?:[A-Za-z][A-Za-z0-9_-]*|\d+))*$/u;
var SENSITIVE_REPLAY_PATH = /(?:^|\.)(?:access[_-]?token|api[_-]?key|connection[_-]?(?:string|uri)|credential|password|private[_-]?key|refresh[_-]?token|secret|token)(?:$|\.)/iu;
var PUBLIC_IDENTIFIER = /^[A-Za-z0-9][A-Za-z0-9._:/+-]{0,499}$/u;
function safeReplayPath(path) {
  return path.length <= 200 && REPLAY_PATH.test(path) && !SENSITIVE_REPLAY_PATH.test(path);
}
function safeReplayScalar(value) {
  if (typeof value === "number") return Number.isFinite(value) ? value : void 0;
  if (typeof value === "boolean") return value;
  if (typeof value !== "string" || value !== value.trim()) return void 0;
  if (value.length === 0 || value.length > 500 || /[\u0000-\u001f\u007f]/u.test(value) || value.startsWith("cred://") || value.includes("[REDACTED]") || looksLikeCredentialValue(value) || /\b(?:Bearer|Basic)\s+/iu.test(value)) {
    return void 0;
  }
  try {
    const url = new URL(value);
    if (!["http:", "https:"].includes(url.protocol) || url.username !== "" || url.password !== "" || url.search !== "" || url.hash !== "") {
      return void 0;
    }
    return value;
  } catch {
    return PUBLIC_IDENTIFIER.test(value) ? value : void 0;
  }
}
function valueAtPath(input, path) {
  return path.split(".").reduce((value, part) => {
    if (Array.isArray(value)) {
      if (/^\d+$/u.test(part)) return value[Number(part)];
      if (value.length !== 1) return void 0;
      value = value[0];
    }
    if (!value || typeof value !== "object") return void 0;
    return value[part];
  }, input);
}
function selectedReplayValues(output, selection) {
  if (!selection || !output || typeof output !== "object") return void 0;
  const selected = {};
  for (const path of [...new Set(selection.outputPaths)].sort().slice(0, 32)) {
    if (!safeReplayPath(path)) continue;
    const value = safeReplayScalar(valueAtPath(output, path));
    if (value !== void 0) selected[path] = value;
  }
  return Object.keys(selected).length > 0 ? selected : void 0;
}
function normalizedReplayValues(value) {
  if (!value || Array.isArray(value) || typeof value !== "object") return void 0;
  const entries = Object.entries(value);
  if (entries.length === 0 || entries.length > 32) return void 0;
  const normalized = {};
  for (const [path, candidate] of entries) {
    const scalar3 = safeReplayScalar(candidate);
    if (!safeReplayPath(path) || scalar3 === void 0) return void 0;
    normalized[path] = scalar3;
  }
  return normalized;
}
function replayOutput(values) {
  if (!values) return void 0;
  const output = {};
  for (const [path, value] of Object.entries(values).sort(
    ([left], [right]) => left.localeCompare(right)
  )) {
    const parts = path.split(".");
    let cursor = output;
    for (const part of parts.slice(0, -1)) {
      if (!cursor[part] || typeof cursor[part] !== "object" || Array.isArray(cursor[part])) {
        cursor[part] = {};
      }
      cursor = cursor[part];
    }
    cursor[parts.at(-1)] = value;
  }
  return output;
}
function parsedEntry(value, legacy = false) {
  if (!value || Array.isArray(value) || typeof value !== "object") {
    throw new Error("Provider idempotency ledger contains a non-object entry");
  }
  const candidate = value;
  const allowed = /* @__PURE__ */ new Set([
    "providerCode",
    "recordedAt",
    "replayValues",
    "requestHash",
    "retryable",
    "state",
    "status",
    "statusCode",
    "verified"
  ]);
  if (Object.keys(candidate).some((key) => !allowed.has(key))) {
    throw new Error("Provider idempotency ledger entry contains an unknown field");
  }
  const states = [
    "succeeded",
    "definitive_no_write",
    "pending_reconciliation"
  ];
  if (!legacy && !states.includes(candidate.state)) {
    throw new Error("Provider idempotency ledger entry has an invalid durable state");
  }
  if (!["succeeded", "failed", "waiting_manual", "skipped"].includes(candidate.status ?? "")) {
    throw new Error("Provider idempotency ledger entry has an invalid status");
  }
  if (candidate.statusCode !== void 0 && !Number.isInteger(candidate.statusCode)) {
    throw new Error("Provider idempotency ledger entry has an invalid status code");
  }
  if (candidate.providerCode !== void 0 && !safeProviderCode(candidate.providerCode)) {
    throw new Error("Provider idempotency ledger entry has an invalid provider code");
  }
  if (typeof candidate.retryable !== "boolean" || typeof candidate.verified !== "boolean") {
    throw new Error("Provider idempotency ledger entry has invalid boolean state");
  }
  if (typeof candidate.recordedAt !== "string" || Number.isNaN(Date.parse(candidate.recordedAt))) {
    throw new Error("Provider idempotency ledger entry has an invalid timestamp");
  }
  const replayValues = normalizedReplayValues(candidate.replayValues);
  if (candidate.replayValues !== void 0 && replayValues === void 0) {
    throw new Error("Provider idempotency ledger entry has invalid replay values");
  }
  if (candidate.requestHash !== void 0 && (typeof candidate.requestHash !== "string" || !/^[a-f0-9]{64}$/u.test(candidate.requestHash))) {
    throw new Error("Provider idempotency ledger entry has an invalid request hash");
  }
  if (!legacy && (candidate.state === "succeeded" && candidate.status !== "succeeded" || candidate.state !== "succeeded" && candidate.status === "succeeded")) {
    throw new Error("Provider idempotency ledger entry has inconsistent durable state");
  }
  return {
    state: !legacy ? candidate.state : candidate.status === "succeeded" ? "succeeded" : "pending_reconciliation",
    requestHash: typeof candidate.requestHash === "string" ? candidate.requestHash : void 0,
    status: candidate.status,
    statusCode: typeof candidate.statusCode === "number" && Number.isInteger(candidate.statusCode) ? candidate.statusCode : void 0,
    providerCode: safeProviderCode(candidate.providerCode),
    retryable: candidate.retryable === true,
    verified: candidate.verified === true,
    replayValues: legacy ? void 0 : replayValues,
    recordedAt: new Date(candidate.recordedAt).toISOString()
  };
}
var FileProviderIdempotencyLedger = class {
  constructor(path, options = {}) {
    this.path = path;
    this.now = options.now ?? (() => /* @__PURE__ */ new Date());
  }
  path;
  durability = "durable_atomic";
  queue = Promise.resolve();
  now;
  knownLedgerId;
  async identity() {
    if (this.knownLedgerId) return this.knownLedgerId;
    return this.enqueue(
      () => this.withLock(async () => {
        const document2 = await this.readOrInitialize();
        this.knownLedgerId = document2.ledgerId;
        return document2.ledgerId;
      })
    );
  }
  async get(key) {
    await this.identity();
    await this.queue;
    const document2 = await this.read();
    const entry = document2.entries[digest(key)];
    if (!entry) return null;
    return this.result(entry);
  }
  async put(key, result2) {
    await this.identity();
    const update = async () => {
      await this.withLock(async () => {
        const document2 = await this.read();
        const existing = document2.entries[digest(key)];
        if (!existing?.requestHash) {
          throw new Error("Provider idempotency promotion does not match a claimed request");
        }
        document2.entries[digest(key)] = this.entry(
          result2.status === "succeeded" ? "succeeded" : "pending_reconciliation",
          result2,
          existing?.requestHash,
          void 0,
          existing?.replayValues
        );
        await this.write(document2);
      });
    };
    const pending = this.queue.then(update, update);
    this.queue = pending.catch(() => void 0);
    return pending;
  }
  async claim(key, requestHash2) {
    if (!/^[a-f0-9]{64}$/.test(requestHash2)) {
      throw new Error("Provider idempotency request hash must be SHA-256 hex");
    }
    await this.identity();
    return this.enqueue(
      () => this.withLock(async () => {
        const document2 = await this.read();
        const hashedKey = digest(key);
        const existing = document2.entries[hashedKey];
        if (!existing) {
          document2.entries[hashedKey] = this.entry(
            "pending_reconciliation",
            unknownAttempt("Provider operation was claimed before execution"),
            requestHash2
          );
          await this.write(document2);
          return { status: "acquired" };
        }
        if (!existing.requestHash || existing.requestHash !== requestHash2) {
          return { status: "conflict" };
        }
        if (existing.state === "succeeded") {
          return { status: "replay", result: this.result(existing) };
        }
        if (existing.state === "pending_reconciliation") {
          return {
            status: "pending_reconciliation",
            result: this.result(existing)
          };
        }
        document2.entries[hashedKey] = this.entry(
          "pending_reconciliation",
          unknownAttempt("Provider operation was claimed before retry"),
          requestHash2
        );
        await this.write(document2);
        return { status: "acquired" };
      })
    );
  }
  async settle(key, requestHash2, state, result2, replay) {
    await this.identity();
    await this.enqueue(
      () => this.withLock(async () => {
        const document2 = await this.read();
        const hashedKey = digest(key);
        const existing = document2.entries[hashedKey];
        if (!existing || existing.requestHash !== requestHash2) {
          throw new Error("Provider idempotency settlement does not match the claimed request");
        }
        document2.entries[hashedKey] = this.entry(state, result2, requestHash2, replay);
        await this.write(document2);
      })
    );
  }
  async read() {
    let raw;
    try {
      raw = await readFile(this.path, "utf8");
    } catch (error) {
      if (error.code === "ENOENT") {
        throw new Error(`Provider idempotency ledger is missing: ${this.path}`);
      }
      throw error;
    }
    let parsed;
    try {
      parsed = JSON.parse(raw);
    } catch {
      throw new Error(`Provider idempotency ledger is corrupt JSON: ${this.path}`);
    }
    if (!parsed || Array.isArray(parsed) || typeof parsed !== "object") {
      throw new Error(`Provider idempotency ledger is not an object: ${this.path}`);
    }
    const candidate = parsed;
    if (candidate.version !== 4 || typeof candidate.ledgerId !== "string" || !LEDGER_ID.test(candidate.ledgerId) || !candidate.entries || Array.isArray(candidate.entries) || typeof candidate.entries !== "object" || Object.keys(candidate).sort().join(",") !== "entries,ledgerId,version") {
      throw new Error(`Unsupported or corrupt provider idempotency ledger: ${this.path}`);
    }
    if (this.knownLedgerId && candidate.ledgerId !== this.knownLedgerId) {
      throw new Error(`Provider idempotency ledger identity changed: ${this.path}`);
    }
    const entries = {};
    for (const [key, value] of Object.entries(candidate.entries)) {
      if (!/^[a-f0-9]{64}$/u.test(key)) {
        throw new Error(`Provider idempotency ledger contains an invalid entry key: ${this.path}`);
      }
      entries[key] = parsedEntry(value);
    }
    return { version: 4, ledgerId: candidate.ledgerId, entries };
  }
  async readOrInitialize() {
    try {
      return await this.read();
    } catch (error) {
      if (!String(error instanceof Error ? error.message : error).startsWith(
        "Provider idempotency ledger is missing:"
      )) {
        return this.migrateLegacy();
      }
      const document2 = emptyDocument(newLedgerId());
      await this.write(document2);
      return document2;
    }
  }
  async migrateLegacy() {
    let parsed;
    try {
      parsed = JSON.parse(await readFile(this.path, "utf8"));
    } catch {
      throw new Error(`Provider idempotency ledger is corrupt JSON: ${this.path}`);
    }
    if (!parsed || Array.isArray(parsed) || typeof parsed !== "object") {
      throw new Error(`Provider idempotency ledger is not an object: ${this.path}`);
    }
    const candidate = parsed;
    if (![1, 2, 3].includes(Number(candidate.version)) || !candidate.entries || Array.isArray(candidate.entries) || typeof candidate.entries !== "object" || Object.keys(candidate).sort().join(",") !== "entries,version") {
      throw new Error(`Unsupported or corrupt provider idempotency ledger: ${this.path}`);
    }
    const entries = {};
    for (const [key, value] of Object.entries(candidate.entries)) {
      if (!/^[a-f0-9]{64}$/u.test(key)) {
        throw new Error(`Provider idempotency ledger contains an invalid entry key: ${this.path}`);
      }
      entries[key] = parsedEntry(value, candidate.version !== 3);
    }
    const document2 = { version: 4, ledgerId: newLedgerId(), entries };
    await this.write(document2);
    return document2;
  }
  async write(document2) {
    const directory = dirname2(this.path);
    await mkdir(directory, { recursive: true, mode: 448 });
    const temporary = `${this.path}.${process.pid}.${randomBytes2(6).toString("hex")}.tmp`;
    const handle = await open(temporary, "wx", 384);
    try {
      await handle.writeFile(`${JSON.stringify(document2, null, 2)}
`, "utf8");
      await handle.sync();
    } finally {
      await handle.close();
    }
    await rename(temporary, this.path);
    const directoryHandle = await open(directory, "r");
    try {
      await directoryHandle.sync();
    } finally {
      await directoryHandle.close();
    }
  }
  entry(state, result2, requestHash2, replay, preservedReplayValues) {
    const replayValues = state === "succeeded" && result2.status === "succeeded" ? selectedReplayValues(result2.output, replay) ?? preservedReplayValues : void 0;
    return {
      state,
      requestHash: requestHash2,
      status: result2.status,
      statusCode: typeof result2.statusCode === "number" && Number.isInteger(result2.statusCode) ? result2.statusCode : void 0,
      providerCode: safeProviderCode(result2.providerCode),
      retryable: result2.retryable === true,
      verified: result2.verified === true,
      replayValues,
      recordedAt: this.now().toISOString()
    };
  }
  result(entry) {
    const output = replayOutput(entry.replayValues);
    return {
      status: entry.status,
      statusCode: entry.statusCode,
      providerCode: entry.providerCode,
      message: entry.state === "succeeded" ? "A request-bound provider apply is recorded; read-back verification is required" : entry.state === "definitive_no_write" ? "Provider evidence confirms the prior attempt did not write" : "The prior provider attempt has an unknown outcome and requires reconciliation",
      retryable: entry.retryable,
      verified: entry.verified,
      ...output === void 0 ? {} : { output },
      effectOutcome: entry.state === "succeeded" ? "confirmed_write" : entry.state === "definitive_no_write" ? "confirmed_no_write" : "unknown"
    };
  }
  enqueue(work) {
    const pending = this.queue.then(work, work);
    this.queue = pending.then(
      () => void 0,
      () => void 0
    );
    return pending;
  }
  async withLock(work) {
    await mkdir(dirname2(this.path), { recursive: true, mode: 448 });
    const lockPath = `${this.path}.lock`;
    let handle;
    for (let attempt = 0; attempt < 200; attempt += 1) {
      try {
        handle = await open(lockPath, "wx", 384);
        break;
      } catch (error) {
        if (error.code !== "EEXIST") throw error;
        await new Promise((resolve27) => setTimeout(resolve27, 10));
      }
    }
    if (!handle) {
      throw new Error(`Provider idempotency ledger is locked: ${this.path}`);
    }
    try {
      await handle.writeFile(`${process.pid}
`, "utf8");
      await handle.sync();
      return await work();
    } finally {
      await handle.close();
      await unlink(lockPath).catch(() => void 0);
    }
  }
};
function unknownAttempt(message) {
  return {
    status: "failed",
    providerCode: "unknown_outcome_reconciliation_required",
    message,
    retryable: false,
    effectOutcome: "unknown"
  };
}

// lib/materialization/provider-effects.ts
var MINOR_UNIT_EXPONENTS = Object.freeze({
  AED: 2,
  AUD: 2,
  BGN: 2,
  BHD: 3,
  BIF: 0,
  BRL: 2,
  CAD: 2,
  CHF: 2,
  CLP: 0,
  CNY: 2,
  CZK: 2,
  DJF: 0,
  DKK: 2,
  EUR: 2,
  GBP: 2,
  GNF: 0,
  HKD: 2,
  HUF: 2,
  ILS: 2,
  INR: 2,
  IQD: 3,
  ISK: 0,
  JOD: 3,
  JPY: 0,
  KMF: 0,
  KRW: 0,
  KWD: 3,
  LYD: 3,
  MXN: 2,
  NOK: 2,
  NZD: 2,
  OMR: 3,
  PLN: 2,
  PYG: 0,
  QAR: 2,
  RON: 2,
  RWF: 0,
  SAR: 2,
  SEK: 2,
  SGD: 2,
  TND: 3,
  TRY: 2,
  UGX: 0,
  USD: 2,
  VND: 0,
  VUV: 0,
  XAF: 0,
  XOF: 0,
  XPF: 0,
  ZAR: 2
});

// lib/materialization/seeds.ts
import { stringify } from "yaml";

// lib/materialization/web-seed-lock.ts
var AGENTIC_WEB_PNPM_LOCK = String.raw`lockfileVersion: '9.0'

settings:
  autoInstallPeers: true
  excludeLinksFromLockfile: false

importers:

  .:
    dependencies:
      next:
        specifier: 15.5.21
        version: 15.5.21(@playwright/test@1.62.1)(react-dom@19.2.7(react@19.2.7))(react@19.2.7)
      react:
        specifier: 19.2.7
        version: 19.2.7
      react-dom:
        specifier: 19.2.7
        version: 19.2.7(react@19.2.7)
    devDependencies:
      '@playwright/test':
        specifier: 1.62.1
        version: 1.62.1
      '@types/node':
        specifier: 22.20.1
        version: 22.20.1
      '@types/react':
        specifier: 19.2.17
        version: 19.2.17
      '@types/react-dom':
        specifier: 19.2.3
        version: 19.2.3(@types/react@19.2.17)
      tsx:
        specifier: 4.23.1
        version: 4.23.1
      typescript:
        specifier: 5.9.3
        version: 5.9.3

packages:

  '@emnapi/runtime@1.11.3':
    resolution: {integrity: sha512-Xz4Tpyki7XyrpbUK1jR1AhdAdaXyhhY4lZ3neLodmhpuWfy2PAQN5B46sAiU4liOXGLkHypn/qU+jvfWSCYYLA==}

  '@esbuild/aix-ppc64@0.28.2':
    resolution: {integrity: sha512-XExcO+dvLKvVtNTibSTBej1NCAbaGhWn9Ww1ZPx80qsahhPFe/8jgWP0IchNe0F3HwkU7n8ejhH8bjonqht8mQ==}
    engines: {node: '>=18'}
    cpu: [ppc64]
    os: [aix]

  '@esbuild/android-arm64@0.28.2':
    resolution: {integrity: sha512-5YfKeeI8qWfBZIX+u2xZC3Zlb3Os/gLS2sbEKM+I4ZOcsWmHS2WLysCcQZDAFRslDUU5Oiq44gf6PYN1vGwG5A==}
    engines: {node: '>=18'}
    cpu: [arm64]
    os: [android]

  '@esbuild/android-arm@0.28.2':
    resolution: {integrity: sha512-kXXoiPVVGQcnIYGOeaovwOURpniDBpSq4A03qkQ+BMQqtGG6HYap3xne9C1O1yo4TR3qxlCX5IqqmX6fFo2Lqg==}
    engines: {node: '>=18'}
    cpu: [arm]
    os: [android]

  '@esbuild/android-x64@0.28.2':
    resolution: {integrity: sha512-O387ite7SzUyCcy3JQX4P4bLtEA7bLLkx+esve5JHnyYfNTxcVpXZo9jhdB0lTKN44gztELTdU7nS8Nr16Fs1Q==}
    engines: {node: '>=18'}
    cpu: [x64]
    os: [android]

  '@esbuild/darwin-arm64@0.28.2':
    resolution: {integrity: sha512-n4KqkOQrraxHJcgjM1RvwbigfQKIKJVpM7xp+KsxiyUSrRdIXnt73VhrPAx0fV44hgfmIVKjxMN9J1t5jySVkw==}
    engines: {node: '>=18'}
    cpu: [arm64]
    os: [darwin]

  '@esbuild/darwin-x64@0.28.2':
    resolution: {integrity: sha512-uq6suIWYP37qzGddBKPw5QEQPi6HiLGsO7UmkpfyaYNQ3D+rN6w6WfwH+nuqcGXWvawGwxOEroO4YGnFh95azw==}
    engines: {node: '>=18'}
    cpu: [x64]
    os: [darwin]

  '@esbuild/freebsd-arm64@0.28.2':
    resolution: {integrity: sha512-n+I0BTSRIoy+d6RPKnEVwql5UwBJolytvY4mAOIEJorKlqgPII8ix6slVVrfZ5Tnj7glIZvloylbB/EJPMWEXw==}
    engines: {node: '>=18'}
    cpu: [arm64]
    os: [freebsd]

  '@esbuild/freebsd-x64@0.28.2':
    resolution: {integrity: sha512-78XJTJkvPs0kz2w61301PJjXl4g7q3JqiYMZ/M/yVI73EHBrCRTgkhu9oqG7vPqq+a/yadEW8aD+agKlk5xrmg==}
    engines: {node: '>=18'}
    cpu: [x64]
    os: [freebsd]

  '@esbuild/linux-arm64@0.28.2':
    resolution: {integrity: sha512-pW4AC0P3it8c7do9MVM4p51FzHzdM/TZrerurgRcHJ2WTa1VQ1CIq18xncfpBJw4ojkiZZrKW2yIBWBP92j6Ug==}
    engines: {node: '>=18'}
    cpu: [arm64]
    os: [linux]

  '@esbuild/linux-arm@0.28.2':
    resolution: {integrity: sha512-XlDnu2q5yoqems+xay6wSAcg9DDD7K9RLKZEBOMZm3ckNpJBvOX20tSfby8KfrrhINDyv9V2YVZKY/SpoGJI8w==}
    engines: {node: '>=18'}
    cpu: [arm]
    os: [linux]

  '@esbuild/linux-ia32@0.28.2':
    resolution: {integrity: sha512-CYbnj78HsIeA+DhgUKgFCfvNsTHFhMMrinUrMZpDXJXKN8T3XViTZ/+wtHeVxEWY8ewSzTFN+nRmSwO2tZaLUQ==}
    engines: {node: '>=18'}
    cpu: [ia32]
    os: [linux]

  '@esbuild/linux-loong64@0.28.2':
    resolution: {integrity: sha512-buwkd8nsph4R+ajRvw0qM5Hja/TXQow3ptzWO2EbG/cqcIkHloRrdlBtQlshyYGTNFvfkfJ5tpPLVkY4DtsPfQ==}
    engines: {node: '>=18'}
    cpu: [loong64]
    os: [linux]

  '@esbuild/linux-mips64el@0.28.2':
    resolution: {integrity: sha512-ZVykbDyk7519VwiNb9Lcj9m8XM6v5V9uKPvrEMkkEedVewf+0itkhahp4HDpgERXhwLRpWFypsGbG/J8s0QjJA==}
    engines: {node: '>=18'}
    cpu: [mips64el]
    os: [linux]

  '@esbuild/linux-ppc64@0.28.2':
    resolution: {integrity: sha512-CAXl+Dtd9UUuJd8pKKdwh6MLm3MUMiqMPmhZ3tTSXPqfyQ3vDl6R5hZdZ/kYojK4ofXtdfSv1tFq8XzWx3heNQ==}
    engines: {node: '>=18'}
    cpu: [ppc64]
    os: [linux]

  '@esbuild/linux-riscv64@0.28.2':
    resolution: {integrity: sha512-GeXCej4IQtU1B+QlDV8W/RRvbzI3O/Stss+/bCXv4lZls5WGRtu2a+3JkA3i4qIUlMXpcHebWpF8AkJhATowuA==}
    engines: {node: '>=18'}
    cpu: [riscv64]
    os: [linux]

  '@esbuild/linux-s390x@0.28.2':
    resolution: {integrity: sha512-3H1weTYZPxt/WOhByszQZybS9w5lKzUn1FDMsgEChbHWQwHYQQRfBxgCcZvPhjHfKyJjIievvMmEUawJrdY9Dg==}
    engines: {node: '>=18'}
    cpu: [s390x]
    os: [linux]

  '@esbuild/linux-x64@0.28.2':
    resolution: {integrity: sha512-4xTZr1FUmSoQW4XIWmit3tzQrUTZM+N3P0XV8xROKYF50XfI7xeO90+1bZvNwxIufQ9hDQVRJH5YhgPVF8A/HQ==}
    engines: {node: '>=18'}
    cpu: [x64]
    os: [linux]

  '@esbuild/netbsd-arm64@0.28.2':
    resolution: {integrity: sha512-sSATRjPeDBg3pdgHoQfoYBob11Kk1FGa9lui5RIHZCoCkJa9QKlvl3/vKz2usCmYYjs7ymJR/2Nnsqe+Hjt5nw==}
    engines: {node: '>=18'}
    cpu: [arm64]
    os: [netbsd]

  '@esbuild/netbsd-x64@0.28.2':
    resolution: {integrity: sha512-lqnzCV+mM0gIADaKihiCg6ifgfU2L3h5E33rNQBN1Y4MaVGnzryzmvvf7UHxprpQdE8hpqLolJ9Rl+SkIRDpyw==}
    engines: {node: '>=18'}
    cpu: [x64]
    os: [netbsd]

  '@esbuild/openbsd-arm64@0.28.2':
    resolution: {integrity: sha512-AL2qJILH7lNjrDmCQDvdxMfAUIv8KMNZOvrwAQ8i8//ntL9FflhOyMJ8OZSMBb8/AWXe3/5v5S20y3zCoZWKoQ==}
    engines: {node: '>=18'}
    cpu: [arm64]
    os: [openbsd]

  '@esbuild/openbsd-x64@0.28.2':
    resolution: {integrity: sha512-QtiuPytchRyC4rwUKhexJdQKvDuZ6hWloi3igqPQNUJCS1/v9EiO3UTOXR6A3FoMo4fnAKbWJdqaIwhOzh8qEw==}
    engines: {node: '>=18'}
    cpu: [x64]
    os: [openbsd]

  '@esbuild/openharmony-arm64@0.28.2':
    resolution: {integrity: sha512-WkhYDmpTjLvGlScA1rwjRUmhl4k8oXR3cIbtqWmELgU/dFeHHlEllxDvdWcNJV9rbzCexB5vz8gtNewWLgCT7Q==}
    engines: {node: '>=18'}
    cpu: [arm64]
    os: [openharmony]

  '@esbuild/sunos-x64@0.28.2':
    resolution: {integrity: sha512-GPMSkTOtMnv2U2F8gxe4Io6qmVs+YKyp832Etqqxr0hFngmXQ3rzwytelm3GIn7T4VviRUlf3sOgBOiTdvaf7g==}
    engines: {node: '>=18'}
    cpu: [x64]
    os: [sunos]

  '@esbuild/win32-arm64@0.28.2':
    resolution: {integrity: sha512-PIhhEkE9uPBleRBrQEJpUn7MBnibZzbGzYWPmY3x+YoVg/95zbjB4CxPPOQ8l5tYYM4mMaCthF8/1DIfBQQyWQ==}
    engines: {node: '>=18'}
    cpu: [arm64]
    os: [win32]

  '@esbuild/win32-ia32@0.28.2':
    resolution: {integrity: sha512-YmJbfTlvU7Sdn9BB+4PRES4oB6pxgS37MAONj+hBr/cpXS1aBPKXxNnDbu+QCWPj0o9dgyxeq79g6c5P8KeuYA==}
    engines: {node: '>=18'}
    cpu: [ia32]
    os: [win32]

  '@esbuild/win32-x64@0.28.2':
    resolution: {integrity: sha512-5ebpxr3nWMzrL/rnUI755Jkuee0bHL/Gq0WTF9lvcpv73wAp5eu8MfBUgWK9bhWvZjj7yX8etf/8tI8Ney695g==}
    engines: {node: '>=18'}
    cpu: [x64]
    os: [win32]

  '@img/colour@1.1.0':
    resolution: {integrity: sha512-Td76q7j57o/tLVdgS746cYARfSyxk8iEfRxewL9h4OMzYhbW4TAcppl0mT4eyqXddh6L/jwoM75mo7ixa/pCeQ==}
    engines: {node: '>=18'}

  '@img/sharp-darwin-arm64@0.34.5':
    resolution: {integrity: sha512-imtQ3WMJXbMY4fxb/Ndp6HBTNVtWCUI0WdobyheGf5+ad6xX8VIDO8u2xE4qc/fr08CKG/7dDseFtn6M6g/r3w==}
    engines: {node: ^18.17.0 || ^20.3.0 || >=21.0.0}
    cpu: [arm64]
    os: [darwin]

  '@img/sharp-darwin-x64@0.34.5':
    resolution: {integrity: sha512-YNEFAF/4KQ/PeW0N+r+aVVsoIY0/qxxikF2SWdp+NRkmMB7y9LBZAVqQ4yhGCm/H3H270OSykqmQMKLBhBJDEw==}
    engines: {node: ^18.17.0 || ^20.3.0 || >=21.0.0}
    cpu: [x64]
    os: [darwin]

  '@img/sharp-libvips-darwin-arm64@1.2.4':
    resolution: {integrity: sha512-zqjjo7RatFfFoP0MkQ51jfuFZBnVE2pRiaydKJ1G/rHZvnsrHAOcQALIi9sA5co5xenQdTugCvtb1cuf78Vf4g==}
    cpu: [arm64]
    os: [darwin]

  '@img/sharp-libvips-darwin-x64@1.2.4':
    resolution: {integrity: sha512-1IOd5xfVhlGwX+zXv2N93k0yMONvUlANylbJw1eTah8K/Jtpi15KC+WSiaX/nBmbm2HxRM1gZ0nSdjSsrZbGKg==}
    cpu: [x64]
    os: [darwin]

  '@img/sharp-libvips-linux-arm64@1.2.4':
    resolution: {integrity: sha512-excjX8DfsIcJ10x1Kzr4RcWe1edC9PquDRRPx3YVCvQv+U5p7Yin2s32ftzikXojb1PIFc/9Mt28/y+iRklkrw==}
    cpu: [arm64]
    os: [linux]

  '@img/sharp-libvips-linux-arm@1.2.4':
    resolution: {integrity: sha512-bFI7xcKFELdiNCVov8e44Ia4u2byA+l3XtsAj+Q8tfCwO6BQ8iDojYdvoPMqsKDkuoOo+X6HZA0s0q11ANMQ8A==}
    cpu: [arm]
    os: [linux]

  '@img/sharp-libvips-linux-ppc64@1.2.4':
    resolution: {integrity: sha512-FMuvGijLDYG6lW+b/UvyilUWu5Ayu+3r2d1S8notiGCIyYU/76eig1UfMmkZ7vwgOrzKzlQbFSuQfgm7GYUPpA==}
    cpu: [ppc64]
    os: [linux]

  '@img/sharp-libvips-linux-riscv64@1.2.4':
    resolution: {integrity: sha512-oVDbcR4zUC0ce82teubSm+x6ETixtKZBh/qbREIOcI3cULzDyb18Sr/Wcyx7NRQeQzOiHTNbZFF1UwPS2scyGA==}
    cpu: [riscv64]
    os: [linux]

  '@img/sharp-libvips-linux-s390x@1.2.4':
    resolution: {integrity: sha512-qmp9VrzgPgMoGZyPvrQHqk02uyjA0/QrTO26Tqk6l4ZV0MPWIW6LTkqOIov+J1yEu7MbFQaDpwdwJKhbJvuRxQ==}
    cpu: [s390x]
    os: [linux]

  '@img/sharp-libvips-linux-x64@1.2.4':
    resolution: {integrity: sha512-tJxiiLsmHc9Ax1bz3oaOYBURTXGIRDODBqhveVHonrHJ9/+k89qbLl0bcJns+e4t4rvaNBxaEZsFtSfAdquPrw==}
    cpu: [x64]
    os: [linux]

  '@img/sharp-libvips-linuxmusl-arm64@1.2.4':
    resolution: {integrity: sha512-FVQHuwx1IIuNow9QAbYUzJ+En8KcVm9Lk5+uGUQJHaZmMECZmOlix9HnH7n1TRkXMS0pGxIJokIVB9SuqZGGXw==}
    cpu: [arm64]
    os: [linux]

  '@img/sharp-libvips-linuxmusl-x64@1.2.4':
    resolution: {integrity: sha512-+LpyBk7L44ZIXwz/VYfglaX/okxezESc6UxDSoyo2Ks6Jxc4Y7sGjpgU9s4PMgqgjj1gZCylTieNamqA1MF7Dg==}
    cpu: [x64]
    os: [linux]

  '@img/sharp-linux-arm64@0.34.5':
    resolution: {integrity: sha512-bKQzaJRY/bkPOXyKx5EVup7qkaojECG6NLYswgktOZjaXecSAeCWiZwwiFf3/Y+O1HrauiE3FVsGxFg8c24rZg==}
    engines: {node: ^18.17.0 || ^20.3.0 || >=21.0.0}
    cpu: [arm64]
    os: [linux]

  '@img/sharp-linux-arm@0.34.5':
    resolution: {integrity: sha512-9dLqsvwtg1uuXBGZKsxem9595+ujv0sJ6Vi8wcTANSFpwV/GONat5eCkzQo/1O6zRIkh0m/8+5BjrRr7jDUSZw==}
    engines: {node: ^18.17.0 || ^20.3.0 || >=21.0.0}
    cpu: [arm]
    os: [linux]

  '@img/sharp-linux-ppc64@0.34.5':
    resolution: {integrity: sha512-7zznwNaqW6YtsfrGGDA6BRkISKAAE1Jo0QdpNYXNMHu2+0dTrPflTLNkpc8l7MUP5M16ZJcUvysVWWrMefZquA==}
    engines: {node: ^18.17.0 || ^20.3.0 || >=21.0.0}
    cpu: [ppc64]
    os: [linux]

  '@img/sharp-linux-riscv64@0.34.5':
    resolution: {integrity: sha512-51gJuLPTKa7piYPaVs8GmByo7/U7/7TZOq+cnXJIHZKavIRHAP77e3N2HEl3dgiqdD/w0yUfiJnII77PuDDFdw==}
    engines: {node: ^18.17.0 || ^20.3.0 || >=21.0.0}
    cpu: [riscv64]
    os: [linux]

  '@img/sharp-linux-s390x@0.34.5':
    resolution: {integrity: sha512-nQtCk0PdKfho3eC5MrbQoigJ2gd1CgddUMkabUj+rBevs8tZ2cULOx46E7oyX+04WGfABgIwmMC0VqieTiR4jg==}
    engines: {node: ^18.17.0 || ^20.3.0 || >=21.0.0}
    cpu: [s390x]
    os: [linux]

  '@img/sharp-linux-x64@0.34.5':
    resolution: {integrity: sha512-MEzd8HPKxVxVenwAa+JRPwEC7QFjoPWuS5NZnBt6B3pu7EG2Ge0id1oLHZpPJdn3OQK+BQDiw9zStiHBTJQQQQ==}
    engines: {node: ^18.17.0 || ^20.3.0 || >=21.0.0}
    cpu: [x64]
    os: [linux]

  '@img/sharp-linuxmusl-arm64@0.34.5':
    resolution: {integrity: sha512-fprJR6GtRsMt6Kyfq44IsChVZeGN97gTD331weR1ex1c1rypDEABN6Tm2xa1wE6lYb5DdEnk03NZPqA7Id21yg==}
    engines: {node: ^18.17.0 || ^20.3.0 || >=21.0.0}
    cpu: [arm64]
    os: [linux]

  '@img/sharp-linuxmusl-x64@0.34.5':
    resolution: {integrity: sha512-Jg8wNT1MUzIvhBFxViqrEhWDGzqymo3sV7z7ZsaWbZNDLXRJZoRGrjulp60YYtV4wfY8VIKcWidjojlLcWrd8Q==}
    engines: {node: ^18.17.0 || ^20.3.0 || >=21.0.0}
    cpu: [x64]
    os: [linux]

  '@img/sharp-wasm32@0.34.5':
    resolution: {integrity: sha512-OdWTEiVkY2PHwqkbBI8frFxQQFekHaSSkUIJkwzclWZe64O1X4UlUjqqqLaPbUpMOQk6FBu/HtlGXNblIs0huw==}
    engines: {node: ^18.17.0 || ^20.3.0 || >=21.0.0}
    cpu: [wasm32]

  '@img/sharp-win32-arm64@0.34.5':
    resolution: {integrity: sha512-WQ3AgWCWYSb2yt+IG8mnC6Jdk9Whs7O0gxphblsLvdhSpSTtmu69ZG1Gkb6NuvxsNACwiPV6cNSZNzt0KPsw7g==}
    engines: {node: ^18.17.0 || ^20.3.0 || >=21.0.0}
    cpu: [arm64]
    os: [win32]

  '@img/sharp-win32-ia32@0.34.5':
    resolution: {integrity: sha512-FV9m/7NmeCmSHDD5j4+4pNI8Cp3aW+JvLoXcTUo0IqyjSfAZJ8dIUmijx1qaJsIiU+Hosw6xM5KijAWRJCSgNg==}
    engines: {node: ^18.17.0 || ^20.3.0 || >=21.0.0}
    cpu: [ia32]
    os: [win32]

  '@img/sharp-win32-x64@0.34.5':
    resolution: {integrity: sha512-+29YMsqY2/9eFEiW93eqWnuLcWcufowXewwSNIT6UwZdUUCrM3oFjMWH/Z6/TMmb4hlFenmfAVbpWeup2jryCw==}
    engines: {node: ^18.17.0 || ^20.3.0 || >=21.0.0}
    cpu: [x64]
    os: [win32]

  '@next/env@15.5.21':
    resolution: {integrity: sha512-hjJI/GfrjWHgNguRIBzItjRRu0m3Nrz17GhxsjuHfjIvg9hyg3239REd2dpI+bpMTFuVrVprHzEQ19m++cDtbw==}

  '@next/swc-darwin-arm64@15.5.21':
    resolution: {integrity: sha512-ZfjqPEdi6TRC/fWx7UDbwb1fbVgyh2uD5tVTRKIDZDlYM+UNuE/LafDG2fwuAoZilADpABh46OY/F5qf9JjqLQ==}
    engines: {node: '>= 10'}
    cpu: [arm64]
    os: [darwin]

  '@next/swc-darwin-x64@15.5.21':
    resolution: {integrity: sha512-TlCf1NpxgQLzTrexuev75xwmNCJMd1/qkJpTVP1GRRcih93hlIBn1P72hkh8T0gnRFr6BmWksQtbyG3jT6jnww==}
    engines: {node: '>= 10'}
    cpu: [x64]
    os: [darwin]

  '@next/swc-linux-arm64-gnu@15.5.21':
    resolution: {integrity: sha512-LXRsq1p+HvHSi7ygwNcSEEcK0zuo5jS75ZlqFHtOH+LF7qntXAJVJxah+1Pi/GyBm7EpkwU7m4EgbvIKrMqm9A==}
    engines: {node: '>= 10'}
    cpu: [arm64]
    os: [linux]

  '@next/swc-linux-arm64-musl@15.5.21':
    resolution: {integrity: sha512-hyGixhFxpDKjqoev6l4KlcRBlt9AXWrGhDZwmwg49sMJM5tnKQPSi+SEj9+e5n+l/bthRGZUdh59GKIs6lQPRw==}
    engines: {node: '>= 10'}
    cpu: [arm64]
    os: [linux]

  '@next/swc-linux-x64-gnu@15.5.21':
    resolution: {integrity: sha512-qfE+YfOba6S2+13e8qn1/UozDVNZ2clBlrs8UtDoax4s8ediu6sq93z66OEHUYlb69Tffh5JTNkgtsAKiSuugg==}
    engines: {node: '>= 10'}
    cpu: [x64]
    os: [linux]

  '@next/swc-linux-x64-musl@15.5.21':
    resolution: {integrity: sha512-BXLGG+EvIwp/Rrgl6HY8sqvD6BOUOIRz8/naDbeLNX7mlA5H2XRcL6MW/0IGnJISfj5BA9gNhFyJj5yOoiIDJQ==}
    engines: {node: '>= 10'}
    cpu: [x64]
    os: [linux]

  '@next/swc-win32-arm64-msvc@15.5.21':
    resolution: {integrity: sha512-tNGNOlT0Wn7E4IMsSnufjXN/l2L2/AGdLLpa2vzS89SYCBuihgLn3ngLsIrvndAnWo9nAkus+4gZHTI/Ijx9HA==}
    engines: {node: '>= 10'}
    cpu: [arm64]
    os: [win32]

  '@next/swc-win32-x64-msvc@15.5.21':
    resolution: {integrity: sha512-DmIdWmC9p4rdNIiQqo8ap0+Cnj6kKtTZnuSCxoYydSc8sgpDgAg9wFhxplunak9imLV0pTvc5WVCOHwm5eHLtQ==}
    engines: {node: '>= 10'}
    cpu: [x64]
    os: [win32]

  '@playwright/test@1.62.1':
    resolution: {integrity: sha512-DTcUc8qii+cpHvtOwggMtBRMjKZHXYWdw8syRYu2vtzuq4Wxphqq4NfCs5Zt44L6mA8rfDfj+PHnxFc/FeK6mQ==}
    engines: {node: '>=20'}
    hasBin: true

  '@swc/helpers@0.5.15':
    resolution: {integrity: sha512-JQ5TuMi45Owi4/BIMAJBoSQoOJu12oOk/gADqlcUL9JEdHB8vyjUSsxqeNXnmXHjYKMi2WcYtezGEEhqUI/E2g==}

  '@types/node@22.20.1':
    resolution: {integrity: sha512-EANqOCF9QFyra+4pfxUcX9STKJpCLjMbObVzljIJomAWSnuSIEAvyzEU53GaajbXJEgdh0iEcPL+DGvpUd4k1Q==}

  '@types/react-dom@19.2.3':
    resolution: {integrity: sha512-jp2L/eY6fn+KgVVQAOqYItbF0VY/YApe5Mz2F0aykSO8gx31bYCZyvSeYxCHKvzHG5eZjc+zyaS5BrBWya2+kQ==}
    peerDependencies:
      '@types/react': ^19.2.0

  '@types/react@19.2.17':
    resolution: {integrity: sha512-MXfmqaVPEVgkBT/aY0aGCkRWWtByiYQXo3xdQ8r5RzuFrPiRn8Gar2tQdXSUQ2GKV3bkXckek89V8wQBY2Q/Aw==}

  caniuse-lite@1.0.30001809:
    resolution: {integrity: sha512-xxWVywk6a6Arlk+hymeycyn/VgqEfLDxupvhH/xiY5SJ/18kmi9o6MiO320DCUzypORHLtvh0I4i04tUhCNHNQ==}

  client-only@0.0.1:
    resolution: {integrity: sha512-IV3Ou0jSMzZrd3pZ48nLkT9DA7Ag1pnPzaiQhpW7c3RbcqqzvzzVu+L8gfqMp/8IM2MQtSiqaCxrrcfu8I8rMA==}

  csstype@3.2.3:
    resolution: {integrity: sha512-z1HGKcYy2xA8AGQfwrn0PAy+PB7X/GSj3UVJW9qKyn43xWa+gl5nXmU4qqLMRzWVLFC8KusUX8T/0kCiOYpAIQ==}

  detect-libc@2.1.2:
    resolution: {integrity: sha512-Btj2BOOO83o3WyH59e8MgXsxEQVcarkUOpEYrubB0urwnN10yQ364rsiByU11nZlqWYZm05i/of7io4mzihBtQ==}
    engines: {node: '>=8'}

  esbuild@0.28.2:
    resolution: {integrity: sha512-HKVLS8dvII+xoKW9kmqxbRKrnWEXfJJr/FZhhJmiqIB0e053QNYFqOBouTMO/k5sID4MvCiUCvv8b9M4h32wIA==}
    engines: {node: '>=18'}
    hasBin: true

  fsevents@2.3.2:
    resolution: {integrity: sha512-xiqMQR4xAeHTuB9uWm+fFRcIOgKBMiOBP+eXiyT7jsgVCq1bkVygt00oASowB7EdtpOHaaPgKt812P9ab+DDKA==}
    engines: {node: ^8.16.0 || ^10.6.0 || >=11.0.0}
    os: [darwin]

  fsevents@2.3.3:
    resolution: {integrity: sha512-5xoDfX+fL7faATnagmWPpbFtwh/R77WmMMqqHGS65C3vvB0YHrgF+B1YmZ3441tMj5n63k0212XNoJwzlhffQw==}
    engines: {node: ^8.16.0 || ^10.6.0 || >=11.0.0}
    os: [darwin]

  nanoid@3.3.18:
    resolution: {integrity: sha512-DTg4MJbGMWkfi6VZFdNt2/caMbQy4Ou+Op/hJQvGEWcnVfoA1QA+xzRKAzw9jD6+GVOOeYr/mIcuDSdug6F6+w==}
    engines: {node: ^10 || ^12 || ^13.7 || ^14 || >=15.0.1}
    hasBin: true

  next@15.5.21:
    resolution: {integrity: sha512-/TsdBtkWLhkl+NVL3Uqws2UphNd6IPzOtzSk1fHaf+0P7GQKLZDUytyhns/Ykbzdy9+YRjwG7ONvrHaaTDdFqQ==}
    engines: {node: ^18.18.0 || ^19.8.0 || >= 20.0.0}
    hasBin: true
    peerDependencies:
      '@opentelemetry/api': ^1.1.0
      '@playwright/test': ^1.51.1
      babel-plugin-react-compiler: '*'
      react: ^18.2.0 || 19.0.0-rc-de68d2f4-20241204 || ^19.0.0
      react-dom: ^18.2.0 || 19.0.0-rc-de68d2f4-20241204 || ^19.0.0
      sass: ^1.3.0
    peerDependenciesMeta:
      '@opentelemetry/api':
        optional: true
      '@playwright/test':
        optional: true
      babel-plugin-react-compiler:
        optional: true
      sass:
        optional: true

  picocolors@1.1.1:
    resolution: {integrity: sha512-xceH2snhtb5M9liqDsmEw56le376mTZkEX/jEb/RxNFyegNul7eNslCXP9FDj/Lcu0X8KEyMceP2ntpaHrDEVA==}

  playwright-core@1.62.1:
    resolution: {integrity: sha512-wPYSwEBJY9GHraISXqyqtx0na0LpO3XEX7jNDhntbex7tzUS7kLnZsOlFruFJB4Hi/rhDMjXGqHewDZ68nYZVw==}
    engines: {node: '>=20'}
    hasBin: true

  playwright@1.62.1:
    resolution: {integrity: sha512-0M+L3LAD8/nm554LOla9Ayx0j0tmFZ0FBcoQ7F1VuVHpM/XpiC8RcDzBQB8W5+hA8L22THxELzeF+2WcUzvcLg==}
    engines: {node: '>=20'}
    hasBin: true

  postcss@8.4.31:
    resolution: {integrity: sha512-PS08Iboia9mts/2ygV3eLpY5ghnUcfLV/EXTOW1E2qYxJKGGBUtNjN76FYHnMs36RmARn41bC0AZmn+rR0OVpQ==}
    engines: {node: ^10 || ^12 || >=14}

  react-dom@19.2.7:
    resolution: {integrity: sha512-t0BRVXvbiE/o20Hfw669rLbMCDWtYZLvmJigy2f0MxsXF+71pxhR3xOkspmsO8h3ZlNzyibAmtCa3l4lYKk6gQ==}
    peerDependencies:
      react: ^19.2.7

  react@19.2.7:
    resolution: {integrity: sha512-HNe9WslTbXmFK8o8cmwgAeJFSBvt1bPdHCVKtaaV+WlAN36mpT4hcRpwbf3fY56ar2oIXzsBpOAiIRHAdY0OlQ==}
    engines: {node: '>=0.10.0'}

  scheduler@0.27.0:
    resolution: {integrity: sha512-eNv+WrVbKu1f3vbYJT/xtiF5syA5HPIMtf9IgY/nKg0sWqzAUEvqY/xm7OcZc/qafLx/iO9FgOmeSAp4v5ti/Q==}

  semver@7.8.5:
    resolution: {integrity: sha512-Y7/KDsb8LjooZpwaqGyulO6DQlksgCncchHGk+sZIY4SBvUocMBEFH5Ur1fI4dV+Jvl0w6cjvucaIi40puRioA==}
    engines: {node: '>=10'}
    hasBin: true

  sharp@0.34.5:
    resolution: {integrity: sha512-Ou9I5Ft9WNcCbXrU9cMgPBcCK8LiwLqcbywW3t4oDV37n1pzpuNLsYiAV8eODnjbtQlSDwZ2cUEeQz4E54Hltg==}
    engines: {node: ^18.17.0 || ^20.3.0 || >=21.0.0}

  source-map-js@1.2.1:
    resolution: {integrity: sha512-UXWMKhLOwVKb728IUtQPXxfYU+usdybtUrK/8uGE8CQMvrhOpwvzDBwj0QhSL7MQc7vIsISBG8VQ8+IDQxpfQA==}
    engines: {node: '>=0.10.0'}

  styled-jsx@5.1.6:
    resolution: {integrity: sha512-qSVyDTeMotdvQYoHWLNGwRFJHC+i+ZvdBRYosOFgC+Wg1vx4frN2/RG/NA7SYqqvKNLf39P2LSRA2pu6n0XYZA==}
    engines: {node: '>= 12.0.0'}
    peerDependencies:
      '@babel/core': '*'
      babel-plugin-macros: '*'
      react: '>= 16.8.0 || 17.x.x || ^18.0.0-0 || ^19.0.0-0'
    peerDependenciesMeta:
      '@babel/core':
        optional: true
      babel-plugin-macros:
        optional: true

  tslib@2.8.1:
    resolution: {integrity: sha512-oJFu94HQb+KVduSUQL7wnpmqnfmLsOA/nAh6b6EH0wCEoK0/mPeXU6c3wKDV83MkOuHPRHtSXKKU99IBazS/2w==}

  tsx@4.23.1:
    resolution: {integrity: sha512-GQHnkIfxyx1wYCOS/wonik5MVRZU9hi1TEZmzGZSCJB1y9YgoZ8H6itNE/u4suE+yLmOzuE4E5S4TZ/ZX2wcWQ==}
    engines: {node: '>=18.0.0'}
    hasBin: true

  typescript@5.9.3:
    resolution: {integrity: sha512-jl1vZzPDinLr9eUt3J/t7V6FgNEw9QjvBPdysz9KfQDD41fQrC2Y4vKQdiaUpFT4bXlb1RHhLpp8wtm6M5TgSw==}
    engines: {node: '>=14.17'}
    hasBin: true

  undici-types@6.21.0:
    resolution: {integrity: sha512-iwDZqg0QAGrg9Rav5H4n0M64c3mkR59cJ6wQp+7C4nI0gsmExaedaYLNO44eT4AtBBwjbTiGPMlt2Md0T9H9JQ==}

snapshots:

  '@emnapi/runtime@1.11.3':
    dependencies:
      tslib: 2.8.1
    optional: true

  '@esbuild/aix-ppc64@0.28.2':
    optional: true

  '@esbuild/android-arm64@0.28.2':
    optional: true

  '@esbuild/android-arm@0.28.2':
    optional: true

  '@esbuild/android-x64@0.28.2':
    optional: true

  '@esbuild/darwin-arm64@0.28.2':
    optional: true

  '@esbuild/darwin-x64@0.28.2':
    optional: true

  '@esbuild/freebsd-arm64@0.28.2':
    optional: true

  '@esbuild/freebsd-x64@0.28.2':
    optional: true

  '@esbuild/linux-arm64@0.28.2':
    optional: true

  '@esbuild/linux-arm@0.28.2':
    optional: true

  '@esbuild/linux-ia32@0.28.2':
    optional: true

  '@esbuild/linux-loong64@0.28.2':
    optional: true

  '@esbuild/linux-mips64el@0.28.2':
    optional: true

  '@esbuild/linux-ppc64@0.28.2':
    optional: true

  '@esbuild/linux-riscv64@0.28.2':
    optional: true

  '@esbuild/linux-s390x@0.28.2':
    optional: true

  '@esbuild/linux-x64@0.28.2':
    optional: true

  '@esbuild/netbsd-arm64@0.28.2':
    optional: true

  '@esbuild/netbsd-x64@0.28.2':
    optional: true

  '@esbuild/openbsd-arm64@0.28.2':
    optional: true

  '@esbuild/openbsd-x64@0.28.2':
    optional: true

  '@esbuild/openharmony-arm64@0.28.2':
    optional: true

  '@esbuild/sunos-x64@0.28.2':
    optional: true

  '@esbuild/win32-arm64@0.28.2':
    optional: true

  '@esbuild/win32-ia32@0.28.2':
    optional: true

  '@esbuild/win32-x64@0.28.2':
    optional: true

  '@img/colour@1.1.0':
    optional: true

  '@img/sharp-darwin-arm64@0.34.5':
    optionalDependencies:
      '@img/sharp-libvips-darwin-arm64': 1.2.4
    optional: true

  '@img/sharp-darwin-x64@0.34.5':
    optionalDependencies:
      '@img/sharp-libvips-darwin-x64': 1.2.4
    optional: true

  '@img/sharp-libvips-darwin-arm64@1.2.4':
    optional: true

  '@img/sharp-libvips-darwin-x64@1.2.4':
    optional: true

  '@img/sharp-libvips-linux-arm64@1.2.4':
    optional: true

  '@img/sharp-libvips-linux-arm@1.2.4':
    optional: true

  '@img/sharp-libvips-linux-ppc64@1.2.4':
    optional: true

  '@img/sharp-libvips-linux-riscv64@1.2.4':
    optional: true

  '@img/sharp-libvips-linux-s390x@1.2.4':
    optional: true

  '@img/sharp-libvips-linux-x64@1.2.4':
    optional: true

  '@img/sharp-libvips-linuxmusl-arm64@1.2.4':
    optional: true

  '@img/sharp-libvips-linuxmusl-x64@1.2.4':
    optional: true

  '@img/sharp-linux-arm64@0.34.5':
    optionalDependencies:
      '@img/sharp-libvips-linux-arm64': 1.2.4
    optional: true

  '@img/sharp-linux-arm@0.34.5':
    optionalDependencies:
      '@img/sharp-libvips-linux-arm': 1.2.4
    optional: true

  '@img/sharp-linux-ppc64@0.34.5':
    optionalDependencies:
      '@img/sharp-libvips-linux-ppc64': 1.2.4
    optional: true

  '@img/sharp-linux-riscv64@0.34.5':
    optionalDependencies:
      '@img/sharp-libvips-linux-riscv64': 1.2.4
    optional: true

  '@img/sharp-linux-s390x@0.34.5':
    optionalDependencies:
      '@img/sharp-libvips-linux-s390x': 1.2.4
    optional: true

  '@img/sharp-linux-x64@0.34.5':
    optionalDependencies:
      '@img/sharp-libvips-linux-x64': 1.2.4
    optional: true

  '@img/sharp-linuxmusl-arm64@0.34.5':
    optionalDependencies:
      '@img/sharp-libvips-linuxmusl-arm64': 1.2.4
    optional: true

  '@img/sharp-linuxmusl-x64@0.34.5':
    optionalDependencies:
      '@img/sharp-libvips-linuxmusl-x64': 1.2.4
    optional: true

  '@img/sharp-wasm32@0.34.5':
    dependencies:
      '@emnapi/runtime': 1.11.3
    optional: true

  '@img/sharp-win32-arm64@0.34.5':
    optional: true

  '@img/sharp-win32-ia32@0.34.5':
    optional: true

  '@img/sharp-win32-x64@0.34.5':
    optional: true

  '@next/env@15.5.21': {}

  '@next/swc-darwin-arm64@15.5.21':
    optional: true

  '@next/swc-darwin-x64@15.5.21':
    optional: true

  '@next/swc-linux-arm64-gnu@15.5.21':
    optional: true

  '@next/swc-linux-arm64-musl@15.5.21':
    optional: true

  '@next/swc-linux-x64-gnu@15.5.21':
    optional: true

  '@next/swc-linux-x64-musl@15.5.21':
    optional: true

  '@next/swc-win32-arm64-msvc@15.5.21':
    optional: true

  '@next/swc-win32-x64-msvc@15.5.21':
    optional: true

  '@playwright/test@1.62.1':
    dependencies:
      playwright: 1.62.1

  '@swc/helpers@0.5.15':
    dependencies:
      tslib: 2.8.1

  '@types/node@22.20.1':
    dependencies:
      undici-types: 6.21.0

  '@types/react-dom@19.2.3(@types/react@19.2.17)':
    dependencies:
      '@types/react': 19.2.17

  '@types/react@19.2.17':
    dependencies:
      csstype: 3.2.3

  caniuse-lite@1.0.30001809: {}

  client-only@0.0.1: {}

  csstype@3.2.3: {}

  detect-libc@2.1.2:
    optional: true

  esbuild@0.28.2:
    optionalDependencies:
      '@esbuild/aix-ppc64': 0.28.2
      '@esbuild/android-arm': 0.28.2
      '@esbuild/android-arm64': 0.28.2
      '@esbuild/android-x64': 0.28.2
      '@esbuild/darwin-arm64': 0.28.2
      '@esbuild/darwin-x64': 0.28.2
      '@esbuild/freebsd-arm64': 0.28.2
      '@esbuild/freebsd-x64': 0.28.2
      '@esbuild/linux-arm': 0.28.2
      '@esbuild/linux-arm64': 0.28.2
      '@esbuild/linux-ia32': 0.28.2
      '@esbuild/linux-loong64': 0.28.2
      '@esbuild/linux-mips64el': 0.28.2
      '@esbuild/linux-ppc64': 0.28.2
      '@esbuild/linux-riscv64': 0.28.2
      '@esbuild/linux-s390x': 0.28.2
      '@esbuild/linux-x64': 0.28.2
      '@esbuild/netbsd-arm64': 0.28.2
      '@esbuild/netbsd-x64': 0.28.2
      '@esbuild/openbsd-arm64': 0.28.2
      '@esbuild/openbsd-x64': 0.28.2
      '@esbuild/openharmony-arm64': 0.28.2
      '@esbuild/sunos-x64': 0.28.2
      '@esbuild/win32-arm64': 0.28.2
      '@esbuild/win32-ia32': 0.28.2
      '@esbuild/win32-x64': 0.28.2

  fsevents@2.3.2:
    optional: true

  fsevents@2.3.3:
    optional: true

  nanoid@3.3.18: {}

  next@15.5.21(@playwright/test@1.62.1)(react-dom@19.2.7(react@19.2.7))(react@19.2.7):
    dependencies:
      '@next/env': 15.5.21
      '@swc/helpers': 0.5.15
      caniuse-lite: 1.0.30001809
      postcss: 8.4.31
      react: 19.2.7
      react-dom: 19.2.7(react@19.2.7)
      styled-jsx: 5.1.6(react@19.2.7)
    optionalDependencies:
      '@next/swc-darwin-arm64': 15.5.21
      '@next/swc-darwin-x64': 15.5.21
      '@next/swc-linux-arm64-gnu': 15.5.21
      '@next/swc-linux-arm64-musl': 15.5.21
      '@next/swc-linux-x64-gnu': 15.5.21
      '@next/swc-linux-x64-musl': 15.5.21
      '@next/swc-win32-arm64-msvc': 15.5.21
      '@next/swc-win32-x64-msvc': 15.5.21
      '@playwright/test': 1.62.1
      sharp: 0.34.5
    transitivePeerDependencies:
      - '@babel/core'
      - babel-plugin-macros

  picocolors@1.1.1: {}

  playwright-core@1.62.1: {}

  playwright@1.62.1:
    dependencies:
      playwright-core: 1.62.1
    optionalDependencies:
      fsevents: 2.3.2

  postcss@8.4.31:
    dependencies:
      nanoid: 3.3.18
      picocolors: 1.1.1
      source-map-js: 1.2.1

  react-dom@19.2.7(react@19.2.7):
    dependencies:
      react: 19.2.7
      scheduler: 0.27.0

  react@19.2.7: {}

  scheduler@0.27.0: {}

  semver@7.8.5:
    optional: true

  sharp@0.34.5:
    dependencies:
      '@img/colour': 1.1.0
      detect-libc: 2.1.2
      semver: 7.8.5
    optionalDependencies:
      '@img/sharp-darwin-arm64': 0.34.5
      '@img/sharp-darwin-x64': 0.34.5
      '@img/sharp-libvips-darwin-arm64': 1.2.4
      '@img/sharp-libvips-darwin-x64': 1.2.4
      '@img/sharp-libvips-linux-arm': 1.2.4
      '@img/sharp-libvips-linux-arm64': 1.2.4
      '@img/sharp-libvips-linux-ppc64': 1.2.4
      '@img/sharp-libvips-linux-riscv64': 1.2.4
      '@img/sharp-libvips-linux-s390x': 1.2.4
      '@img/sharp-libvips-linux-x64': 1.2.4
      '@img/sharp-libvips-linuxmusl-arm64': 1.2.4
      '@img/sharp-libvips-linuxmusl-x64': 1.2.4
      '@img/sharp-linux-arm': 0.34.5
      '@img/sharp-linux-arm64': 0.34.5
      '@img/sharp-linux-ppc64': 0.34.5
      '@img/sharp-linux-riscv64': 0.34.5
      '@img/sharp-linux-s390x': 0.34.5
      '@img/sharp-linux-x64': 0.34.5
      '@img/sharp-linuxmusl-arm64': 0.34.5
      '@img/sharp-linuxmusl-x64': 0.34.5
      '@img/sharp-wasm32': 0.34.5
      '@img/sharp-win32-arm64': 0.34.5
      '@img/sharp-win32-ia32': 0.34.5
      '@img/sharp-win32-x64': 0.34.5
    optional: true

  source-map-js@1.2.1: {}

  styled-jsx@5.1.6(react@19.2.7):
    dependencies:
      client-only: 0.0.1
      react: 19.2.7

  tslib@2.8.1: {}

  tsx@4.23.1:
    dependencies:
      esbuild: 0.28.2
    optionalDependencies:
      fsevents: 2.3.3

  typescript@5.9.3: {}

  undici-types@6.21.0: {}
`;

// lib/materialization/seeds.ts
var CORE_VERSION = "0.2.0";
function file(path, ownership, content) {
  return Object.freeze({ path, ownership, content });
}
function yaml(value) {
  return stringify(value, { lineWidth: 100, sortMapEntries: false });
}
var repositoryFiles = [
  file(
    ".github/workflows/venture-core.yml",
    "core_owned",
    "name: Venture Core\non:\n  pull_request:\n  workflow_dispatch:\njobs:\n  verify:\n    uses: meestierolff/venture-harness/.github/workflows/venture-verify.yml@{{workflowRefSha}}\n"
  ),
  file(
    "src/app-shell.ts",
    "merge_managed",
    'export const ventureShell = { venture: "{{ventureSlug}}", rail: "{{rail}}", runtime: "0.2" } as const;\n'
  ),
  file(
    "src/product/identity.json",
    "venture_owned",
    '{\n  "name": "{{ventureName}}",\n  "slug": "{{ventureSlug}}",\n  "designStatus": "venture-specific-scaffold",\n  "paletteSeed": "{{accentHue}}:{{secondaryHue}}:{{motifStep}}"\n}\n'
  ),
  file(
    "src/design/theme.css",
    "venture_owned",
    ':root {\n  --venture-name: "{{ventureName}}";\n  --accent: oklch(42% 0.13 {{accentHue}});\n  --accent-soft: oklch(93% 0.035 {{accentHue}});\n  --accent-secondary: oklch(52% 0.11 {{secondaryHue}});\n  --motif-step: {{motifStep}}px;\n}\n'
  ),
  file(
    "config/venture-policy.json",
    "venture_owned",
    '{\n  "ventureId": "{{ventureSlug}}",\n  "productionEffectsRequireGrant": true,\n  "advertisingSpendRequiresSeparateSpendGrant": true\n}\n'
  ),
  file(
    "README.md",
    "venture_owned",
    "# {{ventureName}}\n\nIndependent venture repository materialized from `{{seedId}}@{{seedVersion}}`.\n\nThe initial web surface is an honest, noindex-by-default product scaffold. Run `pnpm verify` locally, complete the venture brief, and verify provider state before any production launch.\n"
  )
];
var recursiveServiceFiles = [
  file(
    "runtime/bootstrap.ts",
    "core_owned",
    'import { createVentureRuntime, defineRecursiveServiceCommand, defineRecursiveServiceReconcileCommand, type VentureRuntimeOptions } from "@venture-harness/agent-runtime";\nimport type { OrganizationMembership } from "@venture-harness/organizations";\n\nexport const VENTURE_ID = "{{ventureSlug}}";\nexport const PRIMARY_SERVICE_COMMAND = defineRecursiveServiceCommand({\n  id: "{{ventureSlug}}.execute",\n  title: "Execute {{ventureName}} service",\n  description: "Execute the primary customer Service Blueprint for {{ventureName}}.",\n});\nexport const PRIMARY_SERVICE_RECONCILE_COMMAND = defineRecursiveServiceReconcileCommand({\n  id: "{{ventureSlug}}.reconcile",\n  executionCommandId: PRIMARY_SERVICE_COMMAND.id,\n  title: "Reconcile {{ventureName}} service",\n  description: "Read back and settle the primary customer Service Blueprint for {{ventureName}} without repeating it.",\n});\n\nexport function createRuntime(\n  memberships: readonly OrganizationMembership[],\n  options: Omit<VentureRuntimeOptions, "memberships" | "recursiveCommands" | "recursiveReconcileCommands">,\n) {\n  return createVentureRuntime({ ...options, memberships, recursiveCommands: [PRIMARY_SERVICE_COMMAND], recursiveReconcileCommands: [PRIMARY_SERVICE_RECONCILE_COMMAND] });\n}\n'
  ),
  file(
    "service-blueprints/primary.json",
    "venture_owned",
    '{\n  "schemaVersion": 1,\n  "id": "{{ventureSlug}}.primary",\n  "version": 1,\n  "commandId": "{{ventureSlug}}.execute",\n  "outcome": "Deliver the venture-specific primary outcome",\n  "requiredCapabilities": [],\n  "usageUnit": "completed_outcome",\n  "billingUnit": "completed_outcome",\n  "completionCriteria": ["verified outcome evidence"]\n}\n'
  )
];
var routeFactor = (rationale) => ({ level: "unknown", rationale });
var ventureConfig = {
  venture: {
    name: "{{ventureName}}",
    legal_name: null,
    domain: null,
    market: null,
    target_market: null,
    language: "en",
    locale: "en",
    currency: "EUR",
    timezone: "UTC",
    stage: "build",
    repository_visibility: "{{repositoryVisibility}}",
    production_status: "none",
    harness_version: CORE_VERSION,
    app_kind: "web",
    launch_mode: "thin_mvp",
    business_model: "unselected",
    monetization_model: "unselected",
    risk_profile: "unassessed",
    privacy_profile: "minimal",
    mobile_stack: "none",
    outcomes: {
      primary: { statement: null, success_signal: null },
      secondary: []
    },
    capabilities: {
      active: ["public_website", "web_seo_aeo_geo"],
      open: []
    },
    extensions: {}
  },
  validation: {
    stage: "build",
    minimum_days: 30,
    target_days: 60,
    maximum_days: 90,
    launch_date: null,
    decision_date: null,
    primary_conversion: null,
    qualification_rule: null,
    build_threshold: null,
    iterate_threshold: null,
    stop_threshold: null
  },
  infrastructure: {
    domain_registered: false,
    vercel_project_created: false,
    neon_database_created: false,
    ga4_property_created: false,
    vercel_analytics_enabled: false,
    google_search_console_verified: false,
    bing_webmaster_verified: false
  },
  extensions: {}
};
var launchConfig = {
  contract_version: 1,
  launch: {
    selected_mode: "thin_mvp",
    confidence: 0,
    rationale: "Seed default only; reroute from the founder brief before launch.",
    rejected_alternatives: [],
    assumptions: [
      "The smallest useful web journey is unresolved until the founder brief compiles."
    ],
    evidence_that_could_change_choice: [
      "A complete founder brief and smallest-useful-product assessment."
    ],
    rail: {
      app_kind: "web",
      mobile_stack: "none",
      rationale: "This seed is an independently buildable ordinary web product."
    },
    routing_factors: {
      smallest_useful_build_cost: routeFactor("Assess from the founder brief."),
      smallest_useful_build_time: routeFactor("Assess from the founder brief."),
      reversibility: routeFactor("Assess the smallest useful implementation."),
      regulatory_or_safety_risk: routeFactor("No venture risk assessment exists yet."),
      real_usage_required: routeFactor("Determine whether real usage is needed to prove value."),
      marketplace_cold_start: routeFactor("Determine whether the venture is a marketplace."),
      operational_burden: routeFactor("Assess delivery and support burden."),
      founder_evidence: routeFactor("No founder evidence is compiled yet."),
      concierge_delivery_fit: routeFactor("Assess whether concierge delivery can be honest."),
      app_store_required: routeFactor("The selected seed has no app-store rail.")
    },
    progressive_commitment: {
      specific_user_or_audience: null,
      problem_or_job: null,
      intended_outcome: null,
      smallest_core_journey: null,
      primary_success_signal: null,
      material_constraints: [],
      known_truths: [],
      unresolved_assumptions: [],
      blocking_issues: []
    },
    extensions: {}
  },
  extensions: {}
};
var mobileConfig = {
  contract_version: 1,
  mobile: {
    stack: "none",
    rationale: "The ordinary web seed does not include a mobile rail.",
    bundle_identifier: null,
    app_scheme: null,
    app_store_connect: {
      team_id: null,
      app_id: null,
      sku: null,
      primary_language: "en",
      credential_ref: null,
      first_app_record: { state: "not_required", manual_action_ref: null }
    },
    eas: {
      project_id: null,
      credential_ref: null,
      build_profiles: ["development", "preview", "production"],
      submit_enabled: false
    },
    signing: { credential_ref: null, certificates_in_git_forbidden: true },
    metadata_artifact_ref: null,
    screenshot_flow_artifact_ref: null,
    extensions: {}
  },
  extensions: {}
};
var analyticsConfig = {
  providers: {
    vercel: {
      layer: 1,
      purpose: "Aggregate traffic only after consent and provider verification."
    },
    ga4: {
      layer: 2,
      purpose: "Consented acquisition and journey aggregates after provider verification."
    },
    neon: {
      layer: 3,
      purpose: "First-party commercial evidence after a database is configured."
    }
  },
  consent: {
    default_mode: "strict",
    google_analytics: "opt_in",
    vercel_analytics: "opt_in",
    allow_withdrawal: true,
    settings_link_required: true
  },
  collection: {
    track_material_behaviour: true,
    collect_keystrokes: false,
    collect_mouse_movement: false,
    session_replay: false,
    send_form_values_to_analytics: false,
    send_search_text_to_third_parties: false,
    send_email_to_analytics: false,
    enable_advertising_features: false
  },
  prohibited_properties: [
    "email",
    "name",
    "message",
    "search_text",
    "form_value",
    "free_text",
    "user_content"
  ],
  events: {
    page_view: {
      purpose: "Measure consented aggregate route usage without private content.",
      trigger: "A public route becomes visible after analytics consent.",
      destinations: ["vercel", "ga4"],
      consent: "analytics",
      props: ["route_id", "release_version"],
      neon: false,
      experiment: false
    },
    core_journey_started: {
      purpose: "Record that the typed primary journey started without submitted values.",
      trigger: "The allowlisted primary action is invoked after analytics consent.",
      destinations: ["neon"],
      consent: "analytics",
      props: ["journey_id", "surface_id", "release_version"],
      neon: true,
      experiment: false
    },
    core_journey_completed: {
      purpose: "Record a verified primary outcome without personal or free-form data.",
      trigger: "The product verifies the allowlisted primary outcome after analytics consent.",
      destinations: ["neon"],
      consent: "analytics",
      props: ["journey_id", "outcome_id", "release_version"],
      neon: true,
      experiment: false
    }
  },
  event_packs: { active: ["core_product", "web_acquisition"] },
  core_journeys: {
    core_product: {
      active: true,
      required_packs: ["core_product"],
      start_events: ["core_journey_started"],
      outcome_events: ["core_journey_completed"],
      authoritative_destination: "neon"
    }
  }
};
function disabledLoop(cadence, expression, destination) {
  return {
    cadence,
    enabled: false,
    trigger: { kind: "cron", expression },
    inputs: [],
    primary_metrics: [],
    guardrail_metrics: [],
    metric_definitions: [],
    candidate_rules: [],
    decision_rules: [],
    maximum_actions: cadence === "weekly" ? 3 : 1,
    maximum_iterations: 1,
    autonomy: "propose",
    authorized_effect_types: ["none", "local_write", "git_write"],
    output_destination: destination,
    next_run_at: null,
    stop_condition: "Stop when required data is missing or stale; report the exact missing source.",
    extensions: {}
  };
}
var loopsConfig = {
  contract_version: 1,
  loops: {
    daily_early_signal: disabledLoop("daily", "0 6 * * *", "reports/learning/daily"),
    weekly_growth: disabledLoop("weekly", "0 6 * * 1", "reports/learning/weekly"),
    biweekly_product: disabledLoop("biweekly", "0 7 1,15 * *", "reports/learning/biweekly"),
    monthly_strategy: disabledLoop("monthly", "0 8 1 * *", "reports/learning/monthly")
  },
  extensions: {}
};
function unconfiguredProvider(capabilityIds, selectedTransport = "none") {
  return {
    state: "unconfigured",
    capability_ids: capabilityIds,
    external_resource_ids: {},
    account_id: null,
    team_id: null,
    region: null,
    selected_transport: selectedTransport,
    credential_ref: null,
    required_scopes: [],
    last_verified_at: null,
    evidence_artifact_ref: null,
    error_code: null,
    error_message: null,
    retryable: null,
    next_action: null,
    manual_action_ref: null,
    extensions: {}
  };
}
var providersConfig = {
  contract_version: 1,
  providers: {
    github: unconfiguredProvider([]),
    vercel: unconfiguredProvider(["public_website", "vercel_analytics"]),
    neon: unconfiguredProvider(["database"]),
    stripe: unconfiguredProvider(["stripe"]),
    revenuecat: unconfiguredProvider(["revenuecat"]),
    brevo: unconfiguredProvider(["transactional_email", "lifecycle_email"]),
    google: unconfiguredProvider(["ga4", "gsc"]),
    bing: unconfiguredProvider(["bing_webmaster"]),
    dns: unconfiguredProvider(["public_website"], "manual"),
    mijndomein: unconfiguredProvider(["public_website"], "manual"),
    app_store_connect: unconfiguredProvider(["app_store_connect", "ios_aso"]),
    eas: unconfiguredProvider(["eas"])
  },
  extensions: {}
};
function policyProfile(overrides = {}) {
  return {
    allowed_capabilities: ["*"],
    allowed_side_effect_classes: ["none"],
    allowed_risk_classes: ["low"],
    allowed_environments: ["local"],
    max_estimated_spend: { amount: 0, currency: "EUR" },
    unknown_external_costs_allowed: false,
    max_email_recipients: 0,
    production_deploy_allowed: false,
    live_products_and_prices_allowed: false,
    actual_charges_allowed: false,
    transactional_test_email_allowed: false,
    dns_additions_allowed: false,
    nameserver_changes_allowed: false,
    app_store_submission_allowed: false,
    extensions: {},
    ...overrides
  };
}
var productionSideEffects = [
  "none",
  "local_write",
  "git_write",
  "external_read",
  "reversible_external_write",
  "preview_deploy",
  "production_deploy",
  "transactional_email",
  "dns_addition"
];
var policiesConfig = {
  contract_version: 1,
  authorization: {
    default_profile: "read_only",
    profiles: {
      read_only: policyProfile({
        allowed_side_effect_classes: ["none", "external_read"],
        allowed_environments: ["local", "test", "preview", "production"]
      }),
      build_local: policyProfile({
        allowed_side_effect_classes: ["none", "local_write", "git_write"],
        allowed_risk_classes: ["low", "moderate"],
        allowed_environments: ["local", "test"]
      }),
      preview_launch: policyProfile({
        allowed_side_effect_classes: [
          "none",
          "local_write",
          "git_write",
          "external_read",
          "reversible_external_write",
          "preview_deploy"
        ],
        allowed_risk_classes: ["low", "moderate", "high"],
        allowed_environments: ["local", "test", "preview"],
        unknown_external_costs_allowed: true
      }),
      standard_launch: policyProfile({
        allowed_side_effect_classes: productionSideEffects,
        allowed_risk_classes: ["low", "moderate", "high", "critical"],
        allowed_environments: ["local", "test", "preview", "production"],
        max_email_recipients: 1,
        unknown_external_costs_allowed: true,
        production_deploy_allowed: true,
        transactional_test_email_allowed: true,
        dns_additions_allowed: true
      }),
      live_commerce_launch: policyProfile({
        allowed_side_effect_classes: [
          ...productionSideEffects,
          "live_commerce_config",
          "customer_charge"
        ],
        allowed_risk_classes: ["low", "moderate", "high", "critical"],
        allowed_environments: ["local", "test", "preview", "production"],
        max_email_recipients: 1,
        unknown_external_costs_allowed: true,
        production_deploy_allowed: true,
        live_products_and_prices_allowed: true,
        actual_charges_allowed: true,
        transactional_test_email_allowed: true,
        dns_additions_allowed: true
      }),
      mobile_testflight: policyProfile({
        allowed_side_effect_classes: [
          "none",
          "local_write",
          "git_write",
          "external_read",
          "reversible_external_write",
          "preview_deploy",
          "production_deploy",
          "dns_addition",
          "testflight_upload"
        ],
        allowed_risk_classes: ["low", "moderate", "high", "critical"],
        allowed_environments: ["local", "test", "preview", "production"],
        unknown_external_costs_allowed: true,
        production_deploy_allowed: true,
        dns_additions_allowed: true
      }),
      autofix_low_risk: policyProfile({
        allowed_side_effect_classes: ["none", "local_write", "git_write"],
        allowed_environments: ["local", "test"]
      })
    },
    active_envelopes: [],
    always_require_distinct_checkpoint_for: [
      "external_delete",
      "destructive_data_change",
      "nameserver_change",
      "bulk_communication",
      "customer_charge",
      "app_store_publication"
    ],
    extensions: {}
  },
  extensions: {}
};
var offerConfig = {
  icp: { description: null, disqualifiers: [] },
  offer: { sentence: null, day_one_win: null, guarantee: null },
  pricing: {
    currency: "EUR",
    monthly_price: null,
    annual_price: null,
    implementation_fee: null,
    annual_waives_implementation_fee: false,
    usage_price: null,
    upsell: null
  },
  economics: {
    cac_assumption: null,
    delivery_cost_monthly: null,
    onboarding_cost: null,
    target_contribution_margin: 0.7,
    payback_target_days: 30
  }
};
var coreEvidenceMigration = `-- Venture Harness v0.2 core evidence schema.
-- Additive and idempotent: safe to apply repeatedly in one venture database.
begin;

create table if not exists vh_schema_migrations (
  version text primary key,
  applied_at timestamptz not null default now()
);

create table if not exists experiment_events (
  id bigint generated always as identity primary key,
  occurred_at timestamptz not null default now(),
  event text not null,
  experiment_id text not null,
  variant_key text,
  visitor_id text not null,
  route text,
  displayed_offer text,
  displayed_price text,
  metric text,
  release_version text,
  event_id text unique
);

create table if not exists commercial_events (
  id bigint generated always as identity primary key,
  occurred_at timestamptz not null default now(),
  event text not null,
  visitor_id text not null,
  plan_key text,
  displayed_price text,
  billing_period text,
  experiment_id text,
  variant_key text,
  qualified boolean,
  qualification_tier text,
  attribution jsonb,
  provider text,
  release_version text,
  event_id text unique,
  constraint commercial_events_attribution_object
    check (attribution is null or jsonb_typeof(attribution) = 'object')
);

-- Submitted private values belong only in this first-party table. Analytics
-- call sites must never copy payload fields into product_events or providers.
create table if not exists submissions (
  id bigint generated always as identity primary key,
  occurred_at timestamptz not null default now(),
  form_id text not null,
  visitor_id text not null,
  payload jsonb not null,
  qualified boolean not null,
  qualification_tier text,
  event_id text unique,
  constraint submissions_payload_object check (jsonb_typeof(payload) = 'object')
);

create table if not exists consent_events (
  id bigint generated always as identity primary key,
  occurred_at timestamptz not null default now(),
  event text not null,
  visitor_id text not null,
  from_state text,
  to_state text,
  event_id text unique
);

create table if not exists product_events (
  id bigint generated always as identity primary key,
  occurred_at timestamptz not null default now(),
  event text not null,
  visitor_id text,
  journey_id text,
  release_version text,
  props jsonb not null default '{}'::jsonb,
  event_id text unique,
  constraint product_events_props_object check (jsonb_typeof(props) = 'object')
);

create table if not exists provider_webhook_events (
  id bigint generated always as identity primary key,
  provider text not null,
  external_event_id text not null,
  event_type text not null,
  received_at timestamptz not null default now(),
  processed_at timestamptz,
  status text not null default 'received',
  payload_hash text not null,
  error_code text,
  unique (provider, external_event_id),
  constraint provider_webhook_status
    check (status in ('received', 'processed', 'failed', 'ignored'))
);

create table if not exists analytics_sync_runs (
  id bigint generated always as identity primary key,
  dataset_id text not null unique,
  source text not null,
  source_account text not null,
  fetched_at timestamptz not null,
  window_start timestamptz not null,
  window_end timestamptz not null,
  timezone text not null,
  quality_status text not null,
  dimensions jsonb not null default '[]'::jsonb,
  limitations jsonb not null default '[]'::jsonb,
  release_version text,
  row_count integer not null,
  constraint analytics_sync_window check (window_end >= window_start),
  constraint analytics_sync_row_count check (row_count >= 0),
  constraint analytics_sync_quality
    check (quality_status in ('complete', 'partial', 'sampled', 'thresholded', 'stale', 'unavailable')),
  constraint analytics_sync_dimensions_array check (jsonb_typeof(dimensions) = 'array'),
  constraint analytics_sync_limitations_array check (jsonb_typeof(limitations) = 'array')
);

create index if not exists experiment_events_experiment_occurred_idx
  on experiment_events (experiment_id, occurred_at);
create index if not exists commercial_events_event_occurred_idx
  on commercial_events (event, occurred_at);
create index if not exists submissions_form_occurred_idx
  on submissions (form_id, occurred_at);
create index if not exists consent_events_visitor_occurred_idx
  on consent_events (visitor_id, occurred_at);
create index if not exists product_events_journey_occurred_idx
  on product_events (journey_id, occurred_at);
create index if not exists provider_webhook_events_status_received_idx
  on provider_webhook_events (status, received_at);
create index if not exists analytics_sync_runs_source_fetched_idx
  on analytics_sync_runs (source, fetched_at desc);

insert into vh_schema_migrations (version)
values ('001_core_evidence')
on conflict (version) do nothing;

commit;
`;
var coreEvidenceRollback = `-- Safe rollback for 001_core_evidence.
-- It refuses to drop evidence when any managed table contains data.
begin;

do $$
declare
  table_name text;
  has_rows boolean;
begin
  foreach table_name in array array[
    'experiment_events',
    'commercial_events',
    'submissions',
    'consent_events',
    'product_events',
    'provider_webhook_events',
    'analytics_sync_runs'
  ]
  loop
    if to_regclass('public.' || table_name) is not null then
      execute format('select exists (select 1 from %I limit 1)', table_name) into has_rows;
      if has_rows then
        raise exception using
          errcode = '55000',
          message = format('safe rollback refused: %s contains evidence', table_name),
          hint = 'Export and explicitly archive/delete the data, then rerun this rollback.';
      end if;
    end if;
  end loop;
end
$$;

drop table if exists analytics_sync_runs;
drop table if exists provider_webhook_events;
drop table if exists product_events;
drop table if exists consent_events;
drop table if exists submissions;
drop table if exists commercial_events;
drop table if exists experiment_events;

do $$
begin
  if to_regclass('public.vh_schema_migrations') is not null then
    delete from vh_schema_migrations where version = '001_core_evidence';
  end if;
end
$$;

do $$
begin
  if to_regclass('public.vh_schema_migrations') is not null
     and not exists (select 1 from vh_schema_migrations) then
    drop table vh_schema_migrations;
  end if;
end
$$;

commit;
`;
var ordinaryWebFiles = [
  file("pnpm-lock.yaml", "merge_managed", AGENTIC_WEB_PNPM_LOCK),
  file(
    "app/layout.tsx",
    "merge_managed",
    `import type { Metadata } from "next";
import type { ReactNode } from "react";
import "../src/design/theme.css";
import "./globals.css";
import { INDEXING_ENABLED, SITE, SITE_URL } from "../src/config/site";

export const metadata: Metadata = {
  metadataBase: SITE_URL,
  title: { default: SITE.name, template: "%s | " + SITE.name },
  description: SITE.description,
  alternates: { canonical: "/" },
  robots: { index: INDEXING_ENABLED, follow: INDEXING_ENABLED },
};

export default function RootLayout({ children }: Readonly<{ children: ReactNode }>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
`
  ),
  file(
    "app/page.tsx",
    "venture_owned",
    `import Link from "next/link";

export default function Home() {
  return (
    <main className="shell">
      <header className="masthead" aria-labelledby="venture-title">
        <p className="eyebrow">Independent web product scaffold</p>
        <h1 id="venture-title">{{ventureName}}</h1>
        <p className="lede">
          A focused foundation for the venture-specific primary journey. Capabilities remain
          unverified until the launch flow builds and tests them.
        </p>
        <Link className="primaryAction" href="/status">
          Review launch status
        </Link>
      </header>
      <section className="statusStrip" aria-labelledby="current-state">
        <h2 id="current-state">Current state</h2>
        <p>Buildable scaffold \xB7 provider effects unconfigured \xB7 indexing disabled by default</p>
      </section>
    </main>
  );
}
`
  ),
  file(
    "app/status/page.tsx",
    "venture_owned",
    `import Link from "next/link";

export default function StatusPage() {
  return (
    <main className="shell compact">
      <p className="eyebrow">Product truth</p>
      <h1>{{ventureName}} is not launched yet.</h1>
      <p className="lede">
        This repository contains a working web foundation. Provider accounts, customer outcomes,
        commerce, and production state require separate verified evidence.
      </p>
      <Link className="textLink" href="/">Return home</Link>
    </main>
  );
}
`
  ),
  file(
    "app/api/health/route.ts",
    "core_owned",
    `export function GET() {
  return Response.json({
    status: "ok",
    venture: "{{ventureSlug}}",
    evidence: "local_build_shape",
  });
}
`
  ),
  file(
    "app/robots.ts",
    "merge_managed",
    `import type { MetadataRoute } from "next";
import { INDEXING_ENABLED, SITE_URL } from "../src/config/site";

export default function robots(): MetadataRoute.Robots {
  return {
    rules: { userAgent: "*", allow: INDEXING_ENABLED ? "/" : undefined, disallow: INDEXING_ENABLED ? undefined : "/" },
    sitemap: new URL("/sitemap.xml", SITE_URL).toString(),
  };
}
`
  ),
  file(
    "app/sitemap.ts",
    "merge_managed",
    `import type { MetadataRoute } from "next";
import { SITE_URL } from "../src/config/site";

export default function sitemap(): MetadataRoute.Sitemap {
  return [
    { url: new URL("/", SITE_URL).toString(), changeFrequency: "weekly", priority: 1 },
    { url: new URL("/status", SITE_URL).toString(), changeFrequency: "monthly", priority: 0.2 },
  ];
}
`
  ),
  file(
    "app/globals.css",
    "venture_owned",
    `* { box-sizing: border-box; }
html { background: #f5f2e9; color: #151b1c; }
body { margin: 0; min-height: 100vh; font-family: Inter, ui-sans-serif, system-ui, sans-serif; }
a { color: inherit; }
a:focus-visible { outline: 3px solid var(--accent-secondary); outline-offset: 4px; }
.shell { min-height: 100vh; padding: clamp(1.5rem, 5vw, 5rem); display: grid; align-content: space-between; gap: 4rem; }
.shell::before { content: ""; width: min(14rem, 42vw); height: var(--motif-step); background: var(--accent); display: block; }
.masthead, .compact { max-width: 58rem; }
.eyebrow { color: var(--accent); font-size: .78rem; font-weight: 800; letter-spacing: .12em; text-transform: uppercase; }
h1, h2 { font-family: Georgia, "Times New Roman", serif; font-weight: 500; letter-spacing: -.035em; margin: 0; text-wrap: balance; }
h1 { font-size: clamp(3rem, 10vw, 7.5rem); line-height: .92; max-width: 11ch; }
h2 { font-size: clamp(1.5rem, 4vw, 2.25rem); }
.lede { max-width: 46rem; font-size: clamp(1.1rem, 2vw, 1.35rem); line-height: 1.65; margin: 1.75rem 0; }
.primaryAction { display: inline-block; background: var(--accent); color: white; padding: .9rem 1.15rem; border: 2px solid var(--accent); font-weight: 750; text-decoration: none; }
.primaryAction:hover { background: #151b1c; border-color: #151b1c; }
.statusStrip { border-top: 1px solid #9c9a91; padding-top: 1.25rem; display: grid; grid-template-columns: minmax(10rem, .35fr) 1fr; gap: 1.5rem; }
.statusStrip p { margin: .35rem 0 0; line-height: 1.6; }
.compact { align-content: start; }
.textLink { color: var(--accent); font-weight: 750; text-underline-offset: .25em; }
@media (max-width: 42rem) {
  .shell { align-content: start; gap: 3rem; }
  .statusStrip { grid-template-columns: 1fr; }
}
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after { scroll-behavior: auto !important; transition: none !important; }
}
`
  ),
  file(
    "src/config/site.ts",
    "merge_managed",
    `function exactHttpOrigin(value: string | undefined): string | null {
  if (!value) return null;
  const candidate = value.includes("://") ? value : \`https://\${value}\`;
  const parsed = new URL(candidate);
  if (
    !["http:", "https:"].includes(parsed.protocol) ||
    parsed.username ||
    parsed.password ||
    parsed.pathname !== "/" ||
    parsed.search ||
    parsed.hash
  ) {
    throw new Error("The configured site URL must be one exact HTTP(S) origin");
  }
  return parsed.origin;
}

const explicitSiteOrigin = exactHttpOrigin(process.env.NEXT_PUBLIC_SITE_URL);
const vercelProductionOrigin = exactHttpOrigin(process.env.VERCEL_PROJECT_PRODUCTION_URL);
const configuredSiteUrl =
  explicitSiteOrigin ?? vercelProductionOrigin ?? "http://localhost:3000";
const verifiedProductionEnvironment =
  process.env.VERCEL === "1" && process.env.VERCEL_ENV === "production";

export const SITE_URL = new URL(configuredSiteUrl);
export const INDEXING_ENABLED =
  verifiedProductionEnvironment &&
  Boolean(explicitSiteOrigin ?? vercelProductionOrigin) &&
  process.env.NEXT_PUBLIC_INDEXING_ENABLED !== "false";
export const SITE = Object.freeze({
  name: "{{ventureName}}",
  slug: "{{ventureSlug}}",
  description:
    "An independently buildable product scaffold; capabilities require launch-time verification.",
});
`
  ),
  file(
    "src/analytics/events.ts",
    "merge_managed",
    `export const ANALYTICS_EVENT_NAMES = [
  "page_view",
  "core_journey_started",
  "core_journey_completed",
] as const;

export type AnalyticsEventName = (typeof ANALYTICS_EVENT_NAMES)[number];
export interface SafeAnalyticsProperties {
  route_id?: string;
  journey_id?: string;
  surface_id?: string;
  outcome_id?: string;
  release_version?: string;
}

export function analyticsEvent(name: AnalyticsEventName, properties: SafeAnalyticsProperties) {
  return Object.freeze({ name, properties: Object.freeze({ ...properties }) });
}
`
  ),
  file(
    "scripts/github-publish-source.ts",
    "core_owned",
    String.raw`import { spawnSync } from "node:child_process";
import { createHash } from "node:crypto";
import { mkdtempSync, realpathSync, rmSync } from "node:fs";
import { join } from "node:path";
import { tmpdir } from "node:os";

const MAX_ENTRIES = 10_000;
const MAX_BLOB_BYTES = 50 * 1024 * 1024;
const MAX_TOTAL_BYTES = 100 * 1024 * 1024;
const MAX_OUTPUT_BYTES = 128 * 1024 * 1024;
const COMMIT_MESSAGE = "chore: publish verified venture source";
const BOOTSTRAP_PATH = ".venture-harness-bootstrap";
const BOOTSTRAP_CONTENT = Buffer.from("venture-harness-source-bootstrap-v1\n", "utf8");

type Visibility = "private" | "public" | "internal";
type TreeMode = "100644" | "100755" | "120000";

interface CommandResult {
  exitCode: number;
  stdout: Buffer;
  stderr: string;
}

interface SourceEntry {
  path: string;
  mode: TreeMode;
  oid: string;
  content: Buffer;
}

interface SourceSnapshot {
  treeOid: string;
  entries: SourceEntry[];
}

interface RepositoryState {
  visibility: Visibility;
  archived: boolean;
  defaultBranch: string | null;
}

interface BranchState {
  commitOid: string;
  treeOid: string;
}

function run(
  command: string,
  args: string[],
  options: { cwd: string; env?: Record<string, string>; input?: Buffer } = { cwd: process.cwd() },
): CommandResult {
  if (!command || /\s/.test(command) || ["sh", "bash", "zsh", "fish"].includes(command)) {
    throw new Error("Refusing an unsafe command binary");
  }
  const result = spawnSync(command, args, {
    cwd: options.cwd,
    env: { ...process.env, ...options.env },
    input: options.input,
    encoding: null,
    shell: false,
    maxBuffer: MAX_OUTPUT_BYTES,
  });
  if (result.error) {
    const code = "code" in result.error ? String(result.error.code) : "spawn_error";
    throw new Error("Direct " + command + " invocation failed (" + code + ")");
  }
  return {
    exitCode: result.status ?? 1,
    stdout: result.stdout ?? Buffer.alloc(0),
    stderr: (result.stderr ?? Buffer.alloc(0)).toString("utf8"),
  };
}

function success(result: CommandResult, label: string): Buffer {
  if (result.exitCode !== 0) {
    throw new Error(label + " failed with exit code " + result.exitCode + "; no remote state was inferred");
  }
  return result.stdout;
}

function isMissing(result: CommandResult): boolean {
  return result.exitCode !== 0 && /(?:HTTP\s+(?:404|409)|not found|repository is empty)/i.test(result.stderr);
}

function json(stdout: Buffer, label: string): unknown {
  try {
    return JSON.parse(stdout.toString("utf8")) as unknown;
  } catch {
    throw new Error(label + " did not return valid JSON");
  }
}

function record(value: unknown, label: string): Record<string, unknown> {
  if (value === null || typeof value !== "object" || Array.isArray(value)) {
    throw new Error(label + " did not return one JSON object");
  }
  return value as Record<string, unknown>;
}

function text(value: unknown, label: string): string {
  if (typeof value !== "string" || value.length === 0) {
    throw new Error(label + " did not return one non-empty string");
  }
  return value;
}

function assertRepository(repository: string): void {
  if (
    !/^[A-Za-z0-9](?:[A-Za-z0-9-]{0,38})\/[A-Za-z0-9._-]{1,100}$/.test(repository) ||
    repository.endsWith(".git")
  ) {
    throw new Error("GitHub repository must be an exact owner/name target");
  }
}

function assertVisibility(value: string): asserts value is Visibility {
  if (value !== "private" && value !== "public" && value !== "internal") {
    throw new Error("Unsupported GitHub repository visibility");
  }
}

function assertBranch(branch: string): void {
  if (
    !/^[A-Za-z0-9][A-Za-z0-9._/-]{0,199}$/.test(branch) ||
    branch.includes("..") ||
    branch.includes("//") ||
    branch.endsWith(".lock")
  ) {
    throw new Error("GitHub default branch is not a safe exact ref");
  }
}

function assertOid(oid: string, label: string): void {
  if (!/^[0-9a-f]{40}$/.test(oid)) throw new Error(label + " is not an exact SHA-1 object id");
}

function assertSourcePath(path: string): void {
  if (
    !path ||
    path.startsWith("/") ||
    path.includes("\0") ||
    path.split("/").some((part) => !part || part === "." || part === "..") ||
    path === BOOTSTRAP_PATH ||
    path === ".venture" ||
    path.startsWith(".venture/")
  ) {
    throw new Error("Local source contains an unsafe or private runtime path");
  }
  const name = path.split("/").at(-1) ?? path;
  const environmentFile = /^\.env(?:\..+)?$/.test(name);
  const reviewedExample = name === ".env.example" || /^\.env(?:\..+)?\.example$/.test(name);
  if (environmentFile && !reviewedExample) {
    throw new Error("Local source contains an unreviewed environment file");
  }
}

function loadSnapshot(root: string): SourceSnapshot {
  const sourceRoot = realpathSync(root);
  const temporaryRoot = mkdtempSync(join(tmpdir(), "vh-child-source-"));
  const gitDirectory = join(temporaryRoot, "source.git");
  const gitEnvironment = { GIT_DIR: gitDirectory, GIT_WORK_TREE: sourceRoot };
  try {
    success(
      run("git", ["init", "--bare", "--object-format=sha1", gitDirectory], { cwd: sourceRoot }),
      "Initialize isolated source index",
    );
    success(
      run(
        "git",
        [
          "add",
          "-A",
          "--",
          ".",
          ":(exclude).venture",
          ":(exclude).venture/**",
          ":(exclude)reports",
          ":(exclude)reports/**",
          ":(exclude)*.tsbuildinfo",
          ":(exclude)**/*.tsbuildinfo",
        ],
        { cwd: sourceRoot, env: gitEnvironment },
      ),
      "Snapshot local venture source",
    );
    const treeOid = success(run("git", ["write-tree"], { cwd: sourceRoot, env: gitEnvironment }), "Write local source tree")
      .toString("utf8")
      .trim();
    assertOid(treeOid, "Local source tree id");
    const listing = success(
      run("git", ["ls-tree", "-r", "-z", "--full-tree", treeOid], {
        cwd: sourceRoot,
        env: gitEnvironment,
      }),
      "List local source tree",
    )
      .toString("utf8")
      .split("\0")
      .filter(Boolean);
    if (listing.length === 0) throw new Error("Refusing to publish an empty source tree");
    if (listing.length > MAX_ENTRIES) throw new Error("Local source exceeds the entry safety limit");

    const entries: SourceEntry[] = [];
    let totalBytes = 0;
    for (const row of listing) {
      const separator = row.indexOf("\t");
      if (separator < 0) throw new Error("Local source tree returned a malformed entry");
      const [mode, type, oid] = row.slice(0, separator).split(" ");
      const path = row.slice(separator + 1);
      if (type !== "blob" || !["100644", "100755", "120000"].includes(mode ?? "")) {
        throw new Error("Local source contains an unsupported tree entry");
      }
      assertSourcePath(path);
      assertOid(oid ?? "", "Local source blob id");
      const content = success(
        run("git", ["cat-file", "blob", oid!], { cwd: sourceRoot, env: gitEnvironment }),
        "Read local source blob",
      );
      if (content.byteLength > MAX_BLOB_BYTES) throw new Error("Local source blob exceeds the safety limit");
      totalBytes += content.byteLength;
      if (totalBytes > MAX_TOTAL_BYTES) throw new Error("Local source exceeds the aggregate safety limit");
      entries.push({ path, mode: mode as TreeMode, oid: oid!, content });
    }
    return { treeOid, entries };
  } finally {
    rmSync(temporaryRoot, { recursive: true, force: true });
  }
}

function gh(path: string, options: { method?: string; body?: unknown } = {}): CommandResult {
  const args = ["api", path];
  if (options.method) args.push("--method", options.method);
  if (options.body !== undefined) args.push("--input", "-");
  return run("gh", args, {
    cwd: process.cwd(),
    ...(options.body === undefined
      ? {}
      : { input: Buffer.from(JSON.stringify(options.body), "utf8") }),
  });
}

function readRepository(repository: string): RepositoryState | null {
  const result = gh("repos/" + repository);
  if (isMissing(result)) return null;
  const response = record(json(success(result, "Read GitHub repository"), "GitHub repository"), "GitHub repository");
  if (text(response.full_name, "GitHub repository name").toLowerCase() !== repository.toLowerCase()) {
    throw new Error("GitHub repository read-back returned a different target");
  }
  const visibility = text(response.visibility, "GitHub repository visibility");
  assertVisibility(visibility);
  if (typeof response.archived !== "boolean") throw new Error("GitHub repository archived state is missing");
  const defaultBranch = response.default_branch == null ? null : text(response.default_branch, "GitHub default branch");
  if (defaultBranch) assertBranch(defaultBranch);
  return { visibility, archived: response.archived, defaultBranch };
}

function assertRepositoryState(state: RepositoryState, visibility: Visibility): void {
  if (state.visibility !== visibility) {
    throw new Error("GitHub repository visibility differs from the authorized target");
  }
  if (state.archived) throw new Error("GitHub repository is archived");
}

function readBranch(repository: string, branch: string): BranchState | null {
  const reference = gh("repos/" + repository + "/git/ref/heads/" + encodeURIComponent(branch));
  if (isMissing(reference)) return null;
  const ref = record(json(success(reference, "Read GitHub branch"), "GitHub branch"), "GitHub branch");
  const commitOid = text(record(ref.object, "GitHub branch object").sha, "GitHub branch commit id");
  assertOid(commitOid, "GitHub branch commit id");
  const commitResult = gh("repos/" + repository + "/git/commits/" + commitOid);
  const commit = record(json(success(commitResult, "Read GitHub commit"), "GitHub commit"), "GitHub commit");
  const treeOid = text(record(commit.tree, "GitHub commit tree").sha, "GitHub commit tree id");
  assertOid(treeOid, "GitHub commit tree id");
  return { commitOid, treeOid };
}

function createRepository(repository: string, visibility: Visibility): void {
  success(
    run("gh", ["repo", "create", repository, "--" + visibility], { cwd: process.cwd() }),
    "Create GitHub repository",
  );
}

function gitBlobOid(content: Buffer): string {
  return createHash("sha1")
    .update(Buffer.from("blob " + content.byteLength + "\0", "utf8"))
    .update(content)
    .digest("hex");
}

function bootstrapRepository(repository: string): void {
  const result = gh("repos/" + repository + "/contents/" + BOOTSTRAP_PATH, {
    method: "PUT",
    body: {
      message: "chore: initialize source publication",
      content: BOOTSTRAP_CONTENT.toString("base64"),
    },
  });
  const response = record(
    json(success(result, "Initialize empty GitHub repository"), "GitHub bootstrap response"),
    "GitHub bootstrap response",
  );
  const content = record(response.content, "GitHub bootstrap content");
  if (text(content.sha, "GitHub bootstrap blob id") !== gitBlobOid(BOOTSTRAP_CONTENT)) {
    throw new Error("GitHub bootstrap blob did not hash back to the trusted marker");
  }
}

function isTrustedBootstrapTree(repository: string, treeOid: string): boolean {
  const result = gh("repos/" + repository + "/git/trees/" + treeOid + "?recursive=1");
  const response = record(
    json(success(result, "Read GitHub bootstrap tree"), "GitHub bootstrap tree"),
    "GitHub bootstrap tree",
  );
  if (response.truncated !== false || !Array.isArray(response.tree) || response.tree.length !== 1) {
    return false;
  }
  const entry = record(response.tree[0], "GitHub bootstrap tree entry");
  return (
    entry.path === BOOTSTRAP_PATH &&
    entry.mode === "100644" &&
    entry.type === "blob" &&
    entry.sha === gitBlobOid(BOOTSTRAP_CONTENT)
  );
}

function uploadSnapshot(
  repository: string,
  branch: string,
  parentCommitOid: string,
  snapshot: SourceSnapshot,
): string {
  const uploaded = new Set<string>();
  for (const entry of snapshot.entries) {
    if (uploaded.has(entry.oid)) continue;
    const blobResult = gh("repos/" + repository + "/git/blobs", {
      method: "POST",
      body: { content: entry.content.toString("base64"), encoding: "base64" },
    });
    const blob = record(json(success(blobResult, "Create GitHub source blob"), "GitHub source blob"), "GitHub source blob");
    if (text(blob.sha, "GitHub source blob id") !== entry.oid) {
      throw new Error("GitHub blob read-back did not match the local source blob");
    }
    uploaded.add(entry.oid);
  }
  const treeResult = gh("repos/" + repository + "/git/trees", {
    method: "POST",
    body: {
      tree: snapshot.entries.map((entry) => ({
        path: entry.path,
        mode: entry.mode,
        type: "blob",
        sha: entry.oid,
      })),
    },
  });
  const tree = record(json(success(treeResult, "Create GitHub source tree"), "GitHub source tree"), "GitHub source tree");
  if (text(tree.sha, "GitHub source tree id") !== snapshot.treeOid) {
    throw new Error("GitHub tree read-back did not match the exact local source tree");
  }
  const commitResult = gh("repos/" + repository + "/git/commits", {
    method: "POST",
    body: { message: COMMIT_MESSAGE, tree: snapshot.treeOid, parents: [parentCommitOid] },
  });
  const commit = record(json(success(commitResult, "Create GitHub source commit"), "GitHub source commit"), "GitHub source commit");
  const commitOid = text(commit.sha, "GitHub source commit id");
  assertOid(commitOid, "GitHub source commit id");
  const commitTree = text(record(commit.tree, "GitHub source commit tree").sha, "GitHub source commit tree id");
  if (commitTree !== snapshot.treeOid) throw new Error("GitHub commit did not retain the exact source tree");
  success(
    gh("repos/" + repository + "/git/refs/heads/" + encodeURIComponent(branch), {
      method: "PATCH",
      body: { sha: commitOid, force: false },
    }),
    "Advance GitHub default branch",
  );
  return commitOid;
}

async function verify(
  repository: string,
  visibility: Visibility,
  branch: string,
  commitOid: string,
  treeOid: string,
): Promise<{ repository: string; visibility: Visibility; branch: string; commitOid: string; treeOid: string; verified: true }> {
  for (let attempt = 0; attempt < 20; attempt += 1) {
    const state = readRepository(repository);
    if (state) {
      assertRepositoryState(state, visibility);
      if (state.defaultBranch !== null && state.defaultBranch !== branch) {
        throw new Error("GitHub default branch read-back differs from the published branch");
      }
      if (state.defaultBranch === branch) {
        const remote = readBranch(repository, branch);
        if (remote) {
          if (remote.commitOid !== commitOid) throw new Error("GitHub branch points to a different commit");
          if (remote.treeOid !== treeOid) throw new Error("GitHub commit points to a different source tree");
          return { repository, visibility, branch, commitOid, treeOid, verified: true };
        }
      }
    }
    await new Promise((resolveWait) => setTimeout(resolveWait, 250));
  }
  throw new Error("GitHub exact source read-back remained unavailable");
}

function options(args: string[], required: string[]): Record<string, string> {
  if (args.length !== required.length * 2) throw new Error("Unexpected source publication arguments");
  const allowed = new Set(required);
  const parsed: Record<string, string> = {};
  for (let index = 0; index < args.length; index += 2) {
    const flag = args[index]!;
    const value = args[index + 1]!;
    if (!allowed.has(flag) || flag in parsed || !value || value.startsWith("--")) {
      throw new Error("Invalid source publication arguments");
    }
    parsed[flag] = value;
  }
  for (const flag of required) if (!(flag in parsed)) throw new Error("Missing required " + flag + " value");
  return parsed;
}

async function main(args: string[]): Promise<unknown> {
  const [command, ...rest] = args;
  if (command !== "apply" && command !== "verify") {
    throw new Error("Expected apply or verify; no provider operation was attempted");
  }
  const required = command === "apply"
    ? ["--repository", "--visibility"]
    : ["--repository", "--visibility", "--branch", "--commit", "--tree"];
  const parsed = options(rest, required);
  const repository = parsed["--repository"]!;
  const visibility = parsed["--visibility"]!;
  assertRepository(repository);
  assertVisibility(visibility);

  if (command === "verify") {
    const branch = parsed["--branch"]!;
    const commitOid = parsed["--commit"]!;
    const treeOid = parsed["--tree"]!;
    assertBranch(branch);
    assertOid(commitOid, "Expected GitHub commit id");
    assertOid(treeOid, "Expected GitHub tree id");
    return verify(repository, visibility, branch, commitOid, treeOid);
  }

  const snapshot = loadSnapshot(process.cwd());
  let repositoryState = readRepository(repository);
  let created = false;
  if (!repositoryState) {
    createRepository(repository, visibility);
    created = true;
    repositoryState = readRepository(repository);
    if (!repositoryState) throw new Error("GitHub accepted creation but repository read-back is unavailable");
  }
  assertRepositoryState(repositoryState, visibility);
  let branch = repositoryState.defaultBranch;
  let remote = branch ? readBranch(repository, branch) : null;
  if (!remote) {
    bootstrapRepository(repository);
    repositoryState = readRepository(repository);
    if (!repositoryState) throw new Error("GitHub bootstrap repository read-back is unavailable");
    assertRepositoryState(repositoryState, visibility);
    branch = repositoryState.defaultBranch;
    if (!branch) throw new Error("GitHub bootstrap did not create a default branch");
    remote = readBranch(repository, branch);
    if (!remote) throw new Error("GitHub bootstrap branch read-back is unavailable");
  }
  if (!branch || !remote) throw new Error("GitHub default branch state is incomplete");
  const targetBranch = branch;
  const baseRemote = remote;
  assertBranch(targetBranch);
  if (baseRemote.treeOid === snapshot.treeOid) {
    const verified = await verify(
      repository,
      visibility,
      targetBranch,
      baseRemote.commitOid,
      snapshot.treeOid,
    );
    return { ...verified, created, source: "local_git_tree" as const };
  }
  if (!isTrustedBootstrapTree(repository, baseRemote.treeOid)) {
    throw new Error("GitHub repository already contains a different tree; refusing to overwrite it");
  }
  const commitOid = uploadSnapshot(
    repository,
    targetBranch,
    baseRemote.commitOid,
    snapshot,
  );
  const verified = await verify(
    repository,
    visibility,
    targetBranch,
    commitOid,
    snapshot.treeOid,
  );
  return { ...verified, created, source: "local_git_tree" as const };
}

try {
  const result = await main(process.argv.slice(2));
  process.stdout.write(JSON.stringify(result) + "\n");
} catch (error) {
  process.stderr.write(
    (error instanceof Error ? error.message : "GitHub source publication failed without a safe diagnostic") + "\n",
  );
  process.exitCode = 1;
}
`
  ),
  file(
    "playwright.config.ts",
    "core_owned",
    `import { defineConfig, devices } from "@playwright/test";

const configuredBaseURL = process.env.PLAYWRIGHT_BASE_URL?.trim();
const baseURL = configuredBaseURL || "http://127.0.0.1:43127";
const parsedBaseURL = new URL(baseURL);

if (!["http:", "https:"].includes(parsedBaseURL.protocol) || parsedBaseURL.username || parsedBaseURL.password) {
  throw new Error("PLAYWRIGHT_BASE_URL must be an HTTP(S) origin without embedded credentials");
}

export default defineConfig({
  testDir: "./tests/e2e",
  timeout: 30_000,
  expect: { timeout: 7_500 },
  forbidOnly: Boolean(process.env.CI),
  retries: process.env.CI ? 1 : 0,
  workers: 1,
  reporter: "line",
  outputDir: ".venture/private/test-results",
  use: {
    baseURL: parsedBaseURL.origin,
    trace: "retain-on-failure",
    screenshot: "only-on-failure",
  },
  webServer: configuredBaseURL
    ? undefined
    : {
        command: "pnpm exec next start --hostname 127.0.0.1 --port 43127",
        url: parsedBaseURL.origin,
        reuseExistingServer: false,
        timeout: 120_000,
      },
  projects: [
    {
      name: "desktop-chromium",
      use: { ...devices["Desktop Chrome"], viewport: { width: 1440, height: 900 } },
    },
    {
      name: "mobile-chromium",
      use: { ...devices["iPhone 13"], browserName: "chromium" },
    },
  ],
});
`
  ),
  file(
    "tests/e2e/post-deploy-readonly.spec.ts",
    "venture_owned",
    String.raw`import { expect, test } from "@playwright/test";

test("deployed primary public journey remains readable without mutation", async ({ page, request }) => {
  const runtimeErrors: string[] = [];
  page.on("pageerror", (error) => runtimeErrors.push(error.message));
  page.on("console", (message) => {
    if (message.type() === "error") runtimeErrors.push(message.text());
  });
  await page.route("**/*", async (route) => {
    const method = route.request().method();
    if (method === "GET" || method === "HEAD") await route.continue();
    else await route.abort("blockedbyclient");
  });

  const smoke = await request.get("/", { failOnStatusCode: false });
  expect(smoke.status()).toBeGreaterThanOrEqual(200);
  expect(smoke.status()).toBeLessThan(400);

  const response = await page.goto("/", { waitUntil: "domcontentloaded" });
  expect(response).not.toBeNull();
  expect(response!.status()).toBeLessThan(400);
  await expect(page.locator("main")).toBeVisible();
  await expect(page.getByRole("heading", { level: 1, name: "{{ventureName}}" })).toBeVisible();

  const canonical = await page.locator('link[rel="canonical"]').getAttribute("href");
  expect(canonical).not.toBeNull();
  const canonicalOrigin = new URL(canonical!).origin;
  expect(canonicalOrigin).not.toMatch(/^https?:\/\/(?:localhost|127\.0\.0\.1)(?::|$)/u);
  expect(new URL(canonical!).protocol).toBe("https:");
  if (process.env.EXPECTED_PUBLIC_ORIGIN) {
    expect(canonicalOrigin).toBe(new URL(process.env.EXPECTED_PUBLIC_ORIGIN).origin);
  }
  await expect(page.locator('meta[name="robots"]')).toHaveAttribute(
    "content",
    expect.stringMatching(/index.*follow/i),
  );

  const robots = await request.get("/robots.txt", { failOnStatusCode: false });
  expect(robots.status()).toBe(200);
  const robotsText = await robots.text();
  expect(robotsText).toContain("Allow: /");
  expect(robotsText).not.toContain("Disallow: /");
  expect(robotsText).toContain("Sitemap: " + canonicalOrigin + "/sitemap.xml");
  const sitemap = await request.get("/sitemap.xml", { failOnStatusCode: false });
  expect(sitemap.status()).toBe(200);
  const sitemapText = await sitemap.text();
  expect(sitemapText).toContain("<loc>" + canonicalOrigin + "/</loc>");
  expect(sitemapText).toContain("<loc>" + canonicalOrigin + "/status</loc>");

  const primaryAction = page.getByRole("link", { name: "Review launch status" });
  await expect(primaryAction).toHaveAttribute("href", "/status");
  await primaryAction.click();
  await expect(page).toHaveURL(/\/status$/);
  await expect(page.getByRole("heading", { level: 1 })).toContainText("not launched yet");
  expect(runtimeErrors).toEqual([]);
});
`
  ),
  file(
    "next.config.mjs",
    "core_owned",
    `/** @type {import("next").NextConfig} */
const nextConfig = {
  output: "standalone",
  poweredByHeader: false,
  reactStrictMode: true,
};

export default nextConfig;
`
  ),
  file(
    "tsconfig.json",
    "core_owned",
    `{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "es2022"],
    "allowJs": false,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": { "@/*": ["./*"] }
  },
  "include": ["next-env.d.ts", ".next/types/**/*.ts", "**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules"]
}
`
  ),
  file(
    "next-env.d.ts",
    "core_owned",
    `/// <reference types="next" />
/// <reference types="next/image-types/global" />

// Generated by the Venture Harness web seed. Next.js may maintain this file.
`
  ),
  file(
    ".env.example",
    "merge_managed",
    "NEXT_PUBLIC_SITE_URL=http://localhost:3000\nNEXT_PUBLIC_INDEXING_ENABLED=false\n"
  ),
  file(
    "docs/product/PRODUCT_TRUTH.md",
    "venture_owned",
    `# {{ventureName}} product truth

This register describes only the generated seed state.

| Claim | Status | Evidence | Public boundary |
| --- | --- | --- | --- |
| The repository contains an independently buildable Next.js web scaffold. | local implementation | package.json, app/, next.config.mjs | Do not call it launched until production read-back and the primary journey pass. |
| Analytics uses an allowlisted, free-form-free event baseline. | local contract | config/analytics.yaml, src/analytics/events.ts | No provider is configured and no event delivery is claimed. |
| Provider configuration is credential-reference-only. | local contract | config/providers.yaml, config/connectors.json | Every provider starts unconfigured. |

No customer, revenue, outcome, provider connection, deployment, or market evidence is claimed.
`
  ),
  file("config/venture.yaml", "venture_owned", yaml(ventureConfig)),
  file("config/launch.yaml", "venture_owned", yaml(launchConfig)),
  file("config/mobile.yaml", "venture_owned", yaml(mobileConfig)),
  file("config/analytics.yaml", "venture_owned", yaml(analyticsConfig)),
  file("config/loops.yaml", "venture_owned", yaml(loopsConfig)),
  file("config/providers.yaml", "venture_owned", yaml(providersConfig)),
  file("config/policies.yaml", "venture_owned", yaml(policiesConfig)),
  file("config/offer.yaml", "venture_owned", yaml(offerConfig)),
  file("migrations/sql/001_core_evidence.up.sql", "core_owned", coreEvidenceMigration),
  file("migrations/sql/001_core_evidence.down.sql", "core_owned", coreEvidenceRollback),
  file(
    "tests/seed-contract.test.mjs",
    "core_owned",
    `import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
import test from "node:test";

const manifest = JSON.parse(readFileSync("venture.manifest.json", "utf8"));
const connectors = readFileSync("config/connectors.json", "utf8");
const providers = readFileSync("config/providers.yaml", "utf8");

test("ordinary web seed stays an ordinary app", () => {
  assert.equal(manifest.rail, "web");
  assert.equal("serviceBlueprints" in manifest, false);
  assert.equal("agentSurface" in manifest, false);
});

test("tracked connector metadata is credential-free", () => {
  assert.equal(connectors.includes("externalAccountId"), false);
  assert.equal(connectors.includes("credential"), false);
  const credentialReferenceLines = providers
    .split(/\\r?\\n/u)
    .filter((line) => /(?:^|_)credential_ref:/u.test(line));
  for (const line of credentialReferenceLines) {
    const value = line.slice(line.indexOf(":") + 1).trim().replace(/^['"]|['"]$/gu, "");
    assert.equal(value === "null" || /^cred:\\/\\/[a-z0-9][a-z0-9._/-]*$/iu.test(value), true);
  }
  assert.equal(
    /(?:sk_(?:live|test)_[A-Za-z0-9]{12,}|whsec_[A-Za-z0-9]{12,}|xkeysib-[A-Za-z0-9_-]{12,}|gh[pousr]_[A-Za-z0-9]{20,}|AKIA[A-Z0-9]{16}|Bearer\\s+[A-Za-z0-9._~-]{12,})/u.test(
      providers,
    ),
    false,
  );
});
`
  )
];
var recursiveRuntimePackages = Object.freeze({
  "@venture-harness/core": CORE_VERSION,
  "@venture-harness/agent-runtime": CORE_VERSION,
  "@venture-harness/command-bus": CORE_VERSION,
  "@venture-harness/credentials": CORE_VERSION,
  "@venture-harness/connections": CORE_VERSION,
  "@venture-harness/entitlements": CORE_VERSION,
  "@venture-harness/audit": CORE_VERSION,
  "@venture-harness/events": CORE_VERSION,
  "@venture-harness/telemetry": CORE_VERSION,
  "@venture-harness/organizations": CORE_VERSION,
  "@venture-harness/agent-gateway": CORE_VERSION,
  "@venture-harness/api-generator": CORE_VERSION,
  "@venture-harness/cli-generator": CORE_VERSION,
  "@venture-harness/mcp-generator": CORE_VERSION,
  "@venture-harness/sdk-generator": CORE_VERSION,
  "@venture-harness/ui": CORE_VERSION
});
var recursiveGeneratorVersions = Object.freeze({
  api: CORE_VERSION,
  cli: CORE_VERSION,
  mcp: CORE_VERSION,
  sdk: CORE_VERSION,
  ui: CORE_VERSION
});
function definition(id, rail, options) {
  return Object.freeze({
    id,
    version: CORE_VERSION,
    rail,
    serviceRuntime: options.serviceRuntime,
    coreCompatibility: "^0.2.0",
    runtimePackages: Object.freeze({ ...options.runtimePackages }),
    developmentPackages: Object.freeze({ ...options.developmentPackages ?? {} }),
    packageScripts: Object.freeze({
      ...options.packageScripts ?? { verify: "pnpm typecheck && pnpm test" }
    }),
    generatorVersions: Object.freeze({
      ...options.generatorVersions ?? recursiveGeneratorVersions
    }),
    files: Object.freeze([...repositoryFiles, ...options.files ?? []])
  });
}
var VENTURE_SEEDS = Object.freeze({
  "agentic-web-saas": definition("agentic-web-saas", "web", {
    serviceRuntime: "none",
    runtimePackages: {
      next: "15.5.21",
      react: "19.2.7",
      "react-dom": "19.2.7"
    },
    developmentPackages: {
      "@playwright/test": "1.62.1",
      "@types/node": "22.20.1",
      "@types/react": "19.2.17",
      "@types/react-dom": "19.2.3",
      tsx: "4.23.1",
      typescript: "5.9.3"
    },
    packageScripts: {
      dev: "next dev",
      build: "next build",
      start: "next start",
      typecheck: "tsc --noEmit",
      test: "node --test tests/*.test.mjs",
      "test:e2e:readonly": "playwright test tests/e2e/post-deploy-readonly.spec.ts",
      verify: "pnpm typecheck && pnpm test && pnpm build",
      "verify:fast": "pnpm typecheck && pnpm test",
      "verify:mvp": "pnpm verify:fast && pnpm build && pnpm test:e2e:readonly"
    },
    generatorVersions: { ui: CORE_VERSION },
    files: ordinaryWebFiles
  }),
  "agentic-ios-subscription": definition("agentic-ios-subscription", "ios", {
    serviceRuntime: "recursive",
    runtimePackages: {
      ...recursiveRuntimePackages,
      "@venture-harness/billing": CORE_VERSION,
      "@venture-harness/provider-sdk": CORE_VERSION
    },
    files: recursiveServiceFiles
  }),
  "hybrid-agentic-service": definition("hybrid-agentic-service", "hybrid", {
    serviceRuntime: "recursive",
    runtimePackages: {
      ...recursiveRuntimePackages,
      "@venture-harness/billing": CORE_VERSION,
      "@venture-harness/loops": CORE_VERSION,
      "@venture-harness/provider-sdk": CORE_VERSION
    },
    files: recursiveServiceFiles
  })
});
function ventureSeed(id, version) {
  const seed = VENTURE_SEEDS[id];
  if (seed.version !== version) {
    throw new Error(`Unsupported seed version ${id}@${version}`);
  }
  return seed;
}

// lib/materialization/materializer.ts
function sha256(value) {
  return createHash8("sha256").update(value).digest("hex");
}
function stable3(value) {
  if (Array.isArray(value)) return `[${value.map(stable3).join(",")}]`;
  if (value !== null && typeof value === "object") {
    return `{${Object.entries(value).sort(([left], [right]) => left.localeCompare(right)).map(([key, item]) => `${JSON.stringify(key)}:${stable3(item)}`).join(",")}}`;
  }
  return JSON.stringify(value);
}
function safeRelativePath(path) {
  if (!path || path.startsWith("/") || path.split("/").includes("..") || path.includes("\\")) {
    throw new Error(`Unsafe materialization path: ${path}`);
  }
  return path;
}
function render(template, values) {
  return template.replace(/\{\{([A-Za-z][A-Za-z0-9]*)\}\}/g, (_match, key) => {
    const value = values[key];
    if (value === void 0) throw new Error(`Unknown seed template value: ${key}`);
    return value;
  });
}
var NodeMaterializationFileSystem = class {
  #root;
  constructor(root) {
    this.#root = resolve6(root);
  }
  #inside(path) {
    const target = resolve6(this.#root, safeRelativePath(path));
    const child = relative3(this.#root, target);
    if (!child || child === ".." || child.startsWith(`..${sep3}`)) {
      throw new Error(`Materialization path escapes workspace: ${path}`);
    }
    return target;
  }
  async prepareEmpty() {
    try {
      const stat2 = await lstat(this.#root);
      if (stat2.isSymbolicLink() || !stat2.isDirectory()) {
        throw new Error("Venture workspace must be a real directory");
      }
      if ((await readdir(this.#root)).length > 0) {
        throw new Error("Venture workspace must be empty");
      }
    } catch (error) {
      if (error.code !== "ENOENT") throw error;
      await mkdir2(this.#root, { recursive: true });
    }
  }
  async writeExclusive(path, content) {
    const target = this.#inside(path);
    await mkdir2(dirname3(target), { recursive: true });
    await writeFile(target, content, { encoding: "utf8", flag: "wx", mode: 384 });
  }
  async removeCreated(path) {
    await unlink2(this.#inside(path)).catch((error) => {
      if (error.code !== "ENOENT") throw error;
    });
  }
};
function accountProviders(grant) {
  return Object.fromEntries(
    [...new Set(grant.providerAccounts.map((account) => account.provider))].sort().map((provider) => [provider, "0.2.0"])
  );
}
function materialized(path, ownership, content) {
  return Object.freeze({
    path: safeRelativePath(path),
    ownership,
    content,
    sha256: sha256(content)
  });
}
function compileVentureMaterialization(input) {
  const grant = parseLaunchGrant(input.grant);
  assertLaunchGrantActive(grant, input.at);
  if (!/^\d+\.\d+\.\d+$/.test(input.coreVersion)) throw new Error("Core version must be exact");
  if (!/^[a-f0-9]{40}$/.test(input.workflowRefSha)) {
    throw new Error("Reusable workflow reference must be an immutable 40-character SHA");
  }
  const seed = ventureSeed(grant.seed.id, grant.seed.version);
  if (!seed.coreCompatibility.startsWith(`^${input.coreVersion.split(".")[0]}.`)) {
    throw new Error(`${seed.id}@${seed.version} is incompatible with Core ${input.coreVersion}`);
  }
  const effects = Object.freeze([...input.effects ?? grant.allowedExternalEffects]);
  for (const effect of effects) assertLaunchEffectAuthorized(grant, effect, input.at);
  const manifest = Object.freeze({
    schemaVersion: 1,
    ventureId: `venture_${sha256(`${grant.ownerOrganizationId}\0${grant.ventureSlug}`).slice(0, 20)}`,
    ventureName: grant.ventureName,
    ventureSlug: grant.ventureSlug,
    ownerOrganizationId: grant.ownerOrganizationId,
    repository: grant.repository,
    seed: grant.seed,
    stackProfile: grant.stackProfile,
    rail: seed.rail,
    coreVersion: input.coreVersion,
    ...seed.serviceRuntime === "recursive" ? {
      serviceBlueprints: Object.freeze([`${grant.ventureSlug}.primary`]),
      agentSurface: Object.freeze({
        cli: grant.ventureSlug,
        mcpPrefix: grant.ventureSlug.replaceAll("-", "_"),
        sdkPackage: `@${grant.ventureSlug}/sdk`,
        restPrefix: "/v1"
      })
    } : {},
    connectorManifest: "config/connectors.json",
    companyResourcesOwnedBy: grant.ownerOrganizationId,
    advertisingSpendAuthorized: false
  });
  const accentHue = String(Number.parseInt(sha256(grant.ventureSlug).slice(0, 4), 16) % 360);
  const secondaryHue = String((Number(accentHue) + 137) % 360);
  const motifStep = String(6 + Number.parseInt(sha256(grant.ventureSlug).slice(4, 6), 16) % 7);
  const values = {
    ventureName: grant.ventureName,
    ventureSlug: grant.ventureSlug,
    seedId: seed.id,
    seedVersion: seed.version,
    rail: seed.rail,
    workflowRefSha: input.workflowRefSha,
    accentHue,
    secondaryHue,
    motifStep,
    repositoryVisibility: grant.repository.visibility
  };
  const files = seed.files.map(
    (file2) => materialized(file2.path, file2.ownership, render(file2.content, values))
  );
  files.push(
    materialized(
      "venture.manifest.json",
      "venture_owned",
      `${JSON.stringify(manifest, null, 2)}
`
    ),
    materialized(
      "config/connectors.json",
      "venture_owned",
      `${JSON.stringify(
        {
          schemaVersion: 1,
          ventureId: manifest.ventureId,
          providers: grant.providerAccounts.map(({ capability, provider, ownership }) => ({
            capability,
            provider,
            ownership,
            accountSelection: "runtime_connection_required"
          }))
        },
        null,
        2
      )}
`
    ),
    materialized(
      "package.json",
      "merge_managed",
      `${JSON.stringify(
        {
          name: grant.ventureSlug,
          version: "0.1.0",
          private: true,
          type: "module",
          packageManager: "pnpm@9.15.9",
          engines: { node: ">=22.5.0" },
          scripts: seed.packageScripts,
          dependencies: seed.runtimePackages,
          ...Object.keys(seed.developmentPackages).length > 0 ? { devDependencies: seed.developmentPackages } : {}
        },
        null,
        2
      )}
`
    ),
    materialized(
      ".gitignore",
      "core_owned",
      "node_modules/\n.next/\ndist/\n.env*\n!.env.example\n.venture/private/\n"
    ),
    materialized(
      ".venture/launch-grant.receipt.json",
      "core_owned",
      `${JSON.stringify(
        {
          schemaVersion: 1,
          grantId: grant.grantId,
          ideaDigest: grant.ideaDigest,
          allowedExternalEffects: grant.allowedExternalEffects,
          ...grant.modelExecutionPolicy ? { modelExecutionPolicy: grant.modelExecutionPolicy } : {},
          ...grant.providerOperationBudget ? { providerOperationBudget: grant.providerOperationBudget } : {},
          createdAt: grant.createdAt,
          expiresAt: grant.expiresAt,
          advertisingSpendAuthorized: false
        },
        null,
        2
      )}
`
    )
  );
  files.sort((left, right) => left.path.localeCompare(right.path));
  const lock = createVentureHarnessLock({
    harness_version: input.coreVersion,
    core_version: input.coreVersion,
    config_contract_version: 2,
    source: { kind: "seed", ref: `${seed.id}@${seed.version}` },
    seed: { id: seed.id, version: seed.version },
    runtime_packages: seed.runtimePackages,
    provider_adapters: accountProviders(grant),
    generators: seed.generatorVersions,
    managed_files: files.map(({ path, ownership, sha256: hash }) => ({
      path,
      ownership,
      sha256: hash,
      ...ownership === "merge_managed" ? { base_sha256: hash } : {}
    })),
    applied_migrations: [],
    migration_state: [],
    update_channel: "stable",
    workflow_ref_sha: input.workflowRefSha,
    last_verified_upgrade: null,
    extensions: { venture_manifest: "venture.manifest.json" }
  });
  const lockContent = stringify2(lock, { lineWidth: 100, sortMapEntries: false });
  files.push(materialized("harness.lock", "core_owned", lockContent));
  files.sort((left, right) => left.path.localeCompare(right.path));
  const planDigest = sha256(
    stable3({
      grantId: grant.grantId,
      manifest,
      effects,
      files: files.map(({ path, ownership, sha256: hash }) => ({ path, ownership, sha256: hash }))
    })
  );
  return Object.freeze({ grant, seed, manifest, files, lock, effects, planDigest });
}
async function materializeVenture(plan, fileSystem, at = /* @__PURE__ */ new Date()) {
  assertLaunchGrantActive(plan.grant, at);
  await fileSystem.prepareEmpty();
  const created = [];
  try {
    for (const file2 of plan.files) {
      await fileSystem.writeExclusive(file2.path, file2.content);
      created.push(file2.path);
    }
    return Object.freeze({
      status: "materialized",
      files: Object.freeze(created),
      planDigest: plan.planDigest
    });
  } catch (error) {
    for (const path of [...created].reverse()) await fileSystem.removeCreated(path);
    throw error;
  }
}

// lib/materialization/packs.ts
function pack(id, input) {
  return Object.freeze({
    id,
    version: "0.2.0",
    coreCompatibility: "^0.2.0",
    capabilities: Object.freeze(input.capabilities ?? []),
    serviceBlueprints: Object.freeze(input.serviceBlueprints ?? []),
    commands: Object.freeze(input.commands ?? []),
    events: Object.freeze(input.events ?? []),
    migrations: Object.freeze(input.migrations ?? []),
    providers: Object.freeze(input.providers ?? []),
    evaluations: Object.freeze(input.evaluations ?? []),
    loops: Object.freeze(input.loops ?? []),
    uiContributions: Object.freeze(input.uiContributions ?? []),
    uninstall: id === "winner-loop" ? "preserve_evidence" : "reversible_if_unused"
  });
}
var PACKS = Object.freeze({
  "validate-first": pack("validate-first", {
    capabilities: ["experiments.manage", "analytics.events.write"],
    serviceBlueprints: ["validation.run"],
    commands: ["validation.plan", "validation.evaluate"],
    events: ["validation.hypothesis.created", "validation.decision.recorded"],
    migrations: ["pack-validate-first-001"],
    evaluations: ["validation-thresholds"],
    loops: ["early-signals"],
    uiContributions: ["validation-dashboard"]
  }),
  "ship-to-users": pack("ship-to-users", {
    capabilities: ["hosting.web.deploy", "source.repository.write"],
    serviceBlueprints: ["launch.execute"],
    commands: ["launch.execute"],
    events: ["launch.verified"],
    migrations: ["pack-ship-to-users-001"],
    providers: ["github", "vercel", "neon"],
    evaluations: ["primary-journey"],
    loops: ["launch-verification"],
    uiContributions: ["launch-status"]
  }),
  "distribution-pr": pack("distribution-pr", {
    capabilities: ["distribution.content.publish", "distribution.content.metrics.read"],
    serviceBlueprints: ["distribution.campaign"],
    commands: ["campaigns.launch", "learnings.ingest"],
    events: ["distribution.learning.received"],
    migrations: ["pack-distribution-pr-001"],
    providers: ["postiz", "zernio"],
    evaluations: ["distribution-evidence"],
    loops: ["weekly-distribution"],
    uiContributions: ["campaign-evidence"]
  }),
  "winner-loop": pack("winner-loop", {
    capabilities: [
      "distribution.content.publish",
      "distribution.content.metrics.read",
      "ads.organic_post.boost",
      "ads.insights.read",
      "subscription.lifecycle.read"
    ],
    serviceBlueprints: ["winner-loop.evaluate", "winner-loop.paid-test"],
    commands: ["winner-loop.publish", "winner-loop.evaluate", "winner-loop.propose-paid-test"],
    events: [
      "winner-loop.creative.published",
      "winner-loop.metrics.observed",
      "winner-loop.recommendation.created"
    ],
    migrations: ["pack-winner-loop-001"],
    providers: ["tiktok", "revenuecat", "attribution"],
    evaluations: ["baseline-adjusted-winner", "optimization-readiness"],
    loops: ["winner-metric-snapshots", "creative-fatigue"],
    uiContributions: ["creative-lineage", "spend-approval"]
  }),
  "web-saas": pack("web-saas", {
    capabilities: ["commerce.web_subscription", "email.transactional"],
    serviceBlueprints: ["subscription.web"],
    commands: ["subscriptions.manage"],
    events: ["subscription.changed"],
    migrations: ["pack-web-saas-001"],
    providers: ["stripe", "brevo"],
    loops: ["subscription-reconciliation"],
    uiContributions: ["billing-portal"]
  }),
  "ios-subscription": pack("ios-subscription", {
    capabilities: ["commerce.native_subscription", "subscription.lifecycle.read"],
    serviceBlueprints: ["subscription.ios"],
    commands: ["entitlements.refresh"],
    events: ["subscription.lifecycle.received"],
    migrations: ["pack-ios-subscription-001"],
    providers: ["revenuecat", "app-store-connect"],
    evaluations: ["subscriber-cohorts"],
    loops: ["subscription-reconciliation"],
    uiContributions: ["entitlement-status"]
  })
});

// lib/founder-launch/stack.ts
import {
  closeSync as closeSync2,
  constants,
  existsSync as existsSync5,
  fstatSync,
  fsyncSync as fsyncSync2,
  lstatSync as lstatSync2,
  mkdirSync as mkdirSync3,
  openSync as openSync2,
  readFileSync as readFileSync6,
  realpathSync as realpathSync2,
  renameSync as renameSync3,
  unlinkSync as unlinkSync2,
  writeFileSync as writeFileSync3
} from "node:fs";
import { dirname as dirname4, isAbsolute, join as join3, relative as relative4, resolve as resolve7, sep as sep4 } from "node:path";
var FOUNDER_STACK_PROFILE_ID = "founder-default";
var FOUNDER_STACK_SCHEMA_VERSION = 1;
var founderStackRoleDefinitions = {
  "source.repository": {
    providerId: "github",
    capabilities: ["repository"]
  },
  "hosting.web": {
    providerId: "vercel",
    capabilities: ["deployment"]
  },
  "database.postgres": {
    providerId: "neon",
    capabilities: ["project"]
  },
  "commerce.web": {
    providerId: "stripe",
    capabilities: ["product"]
  },
  "commerce.native": {
    providerId: "revenuecat",
    capabilities: ["entitlement"]
  },
  "email.transactional": {
    providerId: "brevo",
    capabilities: ["template"]
  },
  "growth.google": {
    providerId: "google",
    capabilities: ["analytics_property", "search_console_site"]
  },
  "search.bing": {
    providerId: "bing",
    capabilities: ["site"]
  },
  "dns.records": {
    providerId: "dns",
    capabilities: ["record"]
  }
};
var canonicalIdentifierSchema = external_exports.string().min(1).max(200).regex(/^[A-Za-z0-9_][A-Za-z0-9._-]*$/u, "expected a canonical organization identifier");
var safeMetadataSchema = external_exports.string().min(1).max(300).refine((value) => value.trim() === value, "metadata must not have surrounding whitespace").refine((value) => !/[\u0000-\u001f\u007f]/u.test(value), "metadata contains control characters");
var safeScopeSchema = external_exports.string().min(1).max(300).refine((value) => value.trim() === value, "scope must not have surrounding whitespace").refine((value) => !/[\u0000-\u001f\u007f]/u.test(value), "scope contains control characters");
var founderStackVerificationSchema = external_exports.discriminatedUnion("status", [
  external_exports.object({ status: external_exports.literal("unverified") }).strict(),
  external_exports.object({
    status: external_exports.literal("verified"),
    verifiedAt: external_exports.string().datetime({ offset: true }),
    source: external_exports.enum(["official_cli", "official_api", "manual_read_back"])
  }).strict()
]);
var founderStackRoleConnectionSchema = external_exports.object({
  credentialRef: credentialReferenceSchema.optional(),
  accountId: safeMetadataSchema.optional(),
  teamId: safeMetadataSchema.optional(),
  organizationId: safeMetadataSchema.optional(),
  scopes: external_exports.array(safeScopeSchema).max(100).refine((values) => new Set(values).size === values.length),
  expiresAt: external_exports.string().datetime({ offset: true }).optional(),
  verification: founderStackVerificationSchema
}).strict();
var founderStackRolesSchema = external_exports.object({
  "source.repository": founderStackRoleConnectionSchema,
  "hosting.web": founderStackRoleConnectionSchema,
  "database.postgres": founderStackRoleConnectionSchema,
  "commerce.web": founderStackRoleConnectionSchema,
  "commerce.native": founderStackRoleConnectionSchema,
  "email.transactional": founderStackRoleConnectionSchema,
  "growth.google": founderStackRoleConnectionSchema,
  "search.bing": founderStackRoleConnectionSchema,
  "dns.records": founderStackRoleConnectionSchema
}).strict();
var durableWritableBackendSchema = external_exports.enum(["macos_keychain", "onepassword"]);
var writableCredentialBackendSchema = external_exports.discriminatedUnion("mode", [
  external_exports.object({
    mode: external_exports.literal("shared"),
    backend: durableWritableBackendSchema
  }).strict(),
  external_exports.object({
    mode: external_exports.literal("per_template"),
    backends: external_exports.object({
      neonDatabaseUri: durableWritableBackendSchema,
      stripeWebhookSigning: durableWritableBackendSchema,
      googleAnalyticsMeasurementId: durableWritableBackendSchema
    }).strict()
  }).strict(),
  external_exports.object({
    mode: external_exports.literal("fixture"),
    backend: external_exports.literal("memory"),
    fixtureLabel: canonicalIdentifierSchema
  }).strict()
]);
var founderStackLaunchDefaultsSchema = external_exports.object({
  neon: external_exports.object({ region: safeMetadataSchema.nullable() }).strict(),
  stripe: external_exports.object({ mode: external_exports.literal("test") }).strict(),
  brevo: external_exports.object({
    senderName: safeMetadataSchema.nullable(),
    senderEmail: external_exports.string().email().max(300).nullable(),
    templateName: safeMetadataSchema.nullable(),
    templateSubject: safeMetadataSchema.nullable(),
    templateHtml: safeMetadataSchema.nullable()
  }).strict(),
  google: external_exports.object({ analyticsAccountId: safeMetadataSchema.nullable() }).strict(),
  bing: external_exports.object({ authMode: external_exports.enum(["api_key", "oauth"]).nullable() }).strict(),
  dns: external_exports.object({
    providerId: external_exports.literal("dns"),
    adapter: external_exports.enum(["manual_generic", "mijndomein_manual"]).nullable(),
    registrarAccountId: safeMetadataSchema.nullable(),
    zoneId: safeMetadataSchema.nullable()
  }).strict()
}).strict();
var founderStackConnectionSchema = external_exports.object({
  schemaVersion: external_exports.literal(FOUNDER_STACK_SCHEMA_VERSION),
  profileId: external_exports.literal(FOUNDER_STACK_PROFILE_ID),
  ownerOrganizationId: canonicalIdentifierSchema,
  roles: founderStackRolesSchema,
  writableCredentialBackend: writableCredentialBackendSchema,
  launchDefaults: founderStackLaunchDefaultsSchema,
  writableRefs: external_exports.object({
    neonDatabaseUriTemplate: external_exports.literal("cred://neon/{ventureSlug}-database"),
    stripeWebhookSigningTemplate: external_exports.literal("cred://stripe/{ventureSlug}-webhook"),
    googleAnalyticsMeasurementIdTemplate: external_exports.literal(
      "cred://google/{ventureSlug}-measurement-id"
    )
  }).strict()
}).strict().superRefine((value, context2) => {
  for (const [role, definition2] of Object.entries(founderStackRoleDefinitions)) {
    const credentialRef2 = value.roles[role].credentialRef;
    if (credentialRef2 && !credentialRef2.startsWith(`cred://${definition2.providerId}/`)) {
      context2.addIssue({
        code: external_exports.ZodIssueCode.custom,
        path: ["roles", role, "credentialRef"],
        message: `${role} requires a cred://${definition2.providerId}/ reference`
      });
    }
    if (definition2.providerId === "dns" && credentialRef2) {
      context2.addIssue({
        code: external_exports.ZodIssueCode.custom,
        path: ["roles", role, "credentialRef"],
        message: "the provider-neutral DNS role is manual-only and must not store a credential reference"
      });
    }
    if (definition2.providerId !== "dns" && value.roles[role].verification.status === "verified" && !credentialRef2) {
      context2.addIssue({
        code: external_exports.ZodIssueCode.custom,
        path: ["roles", role, "verification"],
        message: `${role} cannot declare credential verification without a credential reference`
      });
    }
  }
});
function founderStackDnsDestinationId(connection) {
  const selected = connection?.roles["dns.records"];
  return connection?.launchDefaults.dns.registrarAccountId ?? selected?.accountId ?? selected?.organizationId ?? null;
}
function credentialFinding(value) {
  const { writableCredentialBackend, ...credentialSafeRecord } = value;
  return findCredentialMaterial(credentialSafeRecord, {
    allowedCredentialReferenceKeys: ["credentialRef"]
  }) ?? findCredentialMaterial(writableCredentialBackend);
}
function parseFounderStackConnection(value) {
  const parsed = founderStackConnectionSchema.parse(value);
  const finding = credentialFinding(parsed);
  if (finding) {
    throw new Error(`Founder Stack connection contains forbidden material at ${finding.path}`);
  }
  return parsed;
}
var MAX_CONNECTION_FILE_BYTES = 1e6;
function checkedProfileId(profileId) {
  if (profileId !== FOUNDER_STACK_PROFILE_ID) {
    throw new Error(`Unsupported Founder Stack profile: ${profileId}`);
  }
  return profileId;
}
function readRegularFile(path, label) {
  const noFollow = "O_NOFOLLOW" in constants ? constants.O_NOFOLLOW : 0;
  let descriptor2;
  try {
    descriptor2 = openSync2(path, constants.O_RDONLY | noFollow);
    const metadata = fstatSync(descriptor2);
    if (!metadata.isFile()) throw new Error(`${label} must be a regular non-symlink file`);
    if (metadata.size > MAX_CONNECTION_FILE_BYTES) {
      throw new Error(`${label} exceeds the ${MAX_CONNECTION_FILE_BYTES}-byte limit`);
    }
    return readFileSync6(descriptor2, "utf8");
  } catch (error) {
    if (error instanceof Error && error.message.startsWith(label)) throw error;
    throw new Error(`${label} must be a readable regular non-symlink file`);
  } finally {
    if (descriptor2 !== void 0) closeSync2(descriptor2);
  }
}
function containedInputPath(baseDir, file2) {
  if (!file2 || isAbsolute(file2)) {
    throw new Error("Founder Stack --file must be a project-relative path");
  }
  const root = resolve7(baseDir);
  const path = resolve7(root, file2);
  const rel = relative4(root, path);
  if (rel === "" || rel === ".." || rel.startsWith(`..${sep4}`) || isAbsolute(rel)) {
    throw new Error("Founder Stack --file escapes the project root");
  }
  return path;
}
function assertRealInputContainment(baseDir, path) {
  const root = realpathSync2(resolve7(baseDir));
  const target = realpathSync2(path);
  const rel = relative4(root, target);
  if (rel === "" || rel === ".." || rel.startsWith(`..${sep4}`) || isAbsolute(rel)) {
    throw new Error("Founder Stack --file resolves outside the project root");
  }
}
function loadFounderStackConnectionFile(file2, options = {}) {
  const path = containedInputPath(options.baseDir ?? process.cwd(), file2);
  if (!existsSync5(path)) throw new Error(`Founder Stack connection file does not exist: ${file2}`);
  assertRealInputContainment(options.baseDir ?? process.cwd(), path);
  const content = readRegularFile(path, "Founder Stack connection file");
  let value;
  try {
    value = JSON.parse(content);
  } catch {
    throw new Error("Founder Stack connection file must contain canonical JSON");
  }
  return parseFounderStackConnection(value);
}
function defaultFounderStackStateRoot() {
  return join3(dirname4(defaultCredentialCatalogPath()), "founder-stacks");
}
function ensureStateRoot(path) {
  mkdirSync3(path, { recursive: true, mode: 448 });
  const metadata = lstatSync2(path);
  if (metadata.isSymbolicLink() || !metadata.isDirectory()) {
    throw new Error("Founder Stack state root must be a regular non-symlink directory");
  }
}
function syncDirectory(path) {
  const descriptor2 = openSync2(path, constants.O_RDONLY);
  try {
    fsyncSync2(descriptor2);
  } finally {
    closeSync2(descriptor2);
  }
}
var FileFounderStackStore = class {
  rootDir;
  constructor(rootDir = defaultFounderStackStateRoot()) {
    this.rootDir = resolve7(rootDir);
  }
  pathFor(profileId) {
    return join3(this.rootDir, `${checkedProfileId(profileId)}.v1.json`);
  }
  load(profileId) {
    ensureStateRoot(this.rootDir);
    const path = this.pathFor(profileId);
    if (!existsSync5(path)) return null;
    const content = readRegularFile(path, "Founder Stack state file");
    let value;
    try {
      value = JSON.parse(content);
    } catch {
      throw new Error(
        "Founder Stack state file is not valid JSON; restore it from trusted metadata"
      );
    }
    return parseFounderStackConnection(value);
  }
  save(connection) {
    const parsed = parseFounderStackConnection(connection);
    ensureStateRoot(this.rootDir);
    const path = this.pathFor(parsed.profileId);
    if (existsSync5(path)) {
      const current = this.load(parsed.profileId);
      if (current && current.ownerOrganizationId !== parsed.ownerOrganizationId) {
        throw new Error("Founder Stack profile belongs to another organization");
      }
    }
    const temporary = join3(this.rootDir, `.${parsed.profileId}.${process.pid}.${Date.now()}.next`);
    const noFollow = "O_NOFOLLOW" in constants ? constants.O_NOFOLLOW : 0;
    let descriptor2;
    try {
      descriptor2 = openSync2(
        temporary,
        constants.O_WRONLY | constants.O_CREAT | constants.O_EXCL | noFollow,
        384
      );
      writeFileSync3(descriptor2, `${JSON.stringify(parsed, null, 2)}
`, "utf8");
      fsyncSync2(descriptor2);
      closeSync2(descriptor2);
      descriptor2 = void 0;
      renameSync3(temporary, path);
      syncDirectory(this.rootDir);
    } catch (error) {
      if (descriptor2 !== void 0) closeSync2(descriptor2);
      if (existsSync5(temporary)) unlinkSync2(temporary);
      throw error;
    }
    return parsed;
  }
};
function defaultRoleConnection() {
  return { scopes: [], verification: { status: "unverified" } };
}
function loginCommand(providerId) {
  return `vh auth login ${providerId} --ref cred://${providerId}/founder-default`;
}
function includesScopes(actual, required) {
  return required.every((scope) => actual.includes(scope) || actual.includes("*"));
}
var writableCaptureRoles = /* @__PURE__ */ new Set([
  "database.postgres",
  "commerce.web",
  "growth.google"
]);
function missingLaunchDefaults(connection, role) {
  if (role === "database.postgres" && !connection?.launchDefaults.neon.region) {
    return ["launchDefaults.neon.region"];
  }
  if (role === "email.transactional") {
    const brevo = connection?.launchDefaults.brevo;
    return [
      ["senderName", brevo?.senderName],
      ["senderEmail", brevo?.senderEmail],
      ["templateName", brevo?.templateName],
      ["templateSubject", brevo?.templateSubject],
      ["templateHtml", brevo?.templateHtml]
    ].filter(([, value]) => !value).map(([field]) => `launchDefaults.brevo.${field}`);
  }
  if (role === "growth.google" && !connection?.launchDefaults.google.analyticsAccountId) {
    return ["launchDefaults.google.analyticsAccountId"];
  }
  if (role === "search.bing" && !connection?.launchDefaults.bing.authMode) {
    return ["launchDefaults.bing.authMode"];
  }
  if (role === "dns.records") {
    return [
      ...!connection?.launchDefaults.dns.adapter ? ["launchDefaults.dns.adapter"] : [],
      ...!founderStackDnsDestinationId(connection) ? ["launchDefaults.dns.registrarAccountId or roles.dns.records.accountId/organizationId"] : []
    ];
  }
  return [];
}
async function doctorFounderStackConnection(options) {
  const registry = options.registry ?? providerRegistry;
  const now = options.now ?? (() => /* @__PURE__ */ new Date());
  const context2 = { ...options.context, authorization: "dry_run" };
  let writableCredentialTargets;
  if (!options.connection || !context2.credentials) {
    writableCredentialTargets = {
      status: "unconfigured",
      fixtureOnly: options.connection?.writableCredentialBackend.mode === "fixture",
      targets: [],
      nextCommand: "vh stack create founder-default --file <connection.json>"
    };
  } else {
    try {
      const registered = await registerFounderStackWritableCredentialRefs(
        options.connection,
        { ventureSlug: "founder-stack-doctor" },
        context2.credentials
      );
      writableCredentialTargets = {
        status: "ready",
        fixtureOnly: options.connection.writableCredentialBackend.mode === "fixture",
        targets: registered.inspections.map(({ purpose, ref, backend, status }) => ({
          purpose,
          ref,
          backend,
          status
        })),
        nextCommand: "vh launch --dry-run"
      };
    } catch {
      writableCredentialTargets = {
        status: "unconfigured",
        fixtureOnly: options.connection.writableCredentialBackend.mode === "fixture",
        targets: [],
        nextCommand: "vh stack create founder-default --file <connection.json>"
      };
    }
  }
  const roles = await Promise.all(
    Object.keys(founderStackRoleDefinitions).map(async (role) => {
      const definition2 = founderStackRoleDefinitions[role];
      const connection = options.connection?.roles[role] ?? defaultRoleConnection();
      const missingDefaults = missingLaunchDefaults(options.connection, role);
      const adapter = registry.get(definition2.providerId);
      const doctor = await adapter.doctor(
        {
          credentialRefs: connection.credentialRef ? [connection.credentialRef] : [],
          requiredCapabilities: [...definition2.capabilities]
        },
        context2
      );
      const manualOnly = adapter.descriptor.transports.every((transport) => transport === "manual");
      let authenticated2 = false;
      let metadataMatches = true;
      if (connection.credentialRef && context2.credentials) {
        try {
          const inspection = await context2.credentials.inspect(connection.credentialRef);
          const reference = context2.credentials.getReference(connection.credentialRef);
          authenticated2 = inspection.status === "available" && (inspection.kind === "cli_session" && inspection.backend === "cli_session" || inspection.testStatus === "passed" && inspection.testedAt !== void 0);
          metadataMatches = reference?.provider === definition2.providerId && (!connection.accountId || !reference.accountId || connection.accountId === reference.accountId) && includesScopes(reference.scopes, connection.scopes) && (!connection.expiresAt || Date.parse(connection.expiresAt) > now().getTime());
        } catch {
          authenticated2 = false;
          metadataMatches = false;
        }
      }
      let status = manualOnly ? "manual_only" : !connection.credentialRef ? "unconfigured" : doctor.status === "ready" && authenticated2 && metadataMatches ? "ready" : doctor.status === "unavailable" || doctor.status === "degraded" ? "unconfigured" : "auth_required";
      if (status === "ready" && writableCaptureRoles.has(role) && writableCredentialTargets.status !== "ready") {
        status = "unconfigured";
      }
      if (missingDefaults.length > 0) status = "unconfigured";
      const nextCommand = status === "ready" || status === "manual_only" ? "vh launch --dry-run" : status === "unconfigured" && missingDefaults.length > 0 ? "vh stack create founder-default --file <connection.json>" : status === "unconfigured" && writableCaptureRoles.has(role) ? writableCredentialTargets.nextCommand : status === "unconfigured" ? loginCommand(definition2.providerId) : `vh auth test ${definition2.providerId} --ref ${connection.credentialRef}`;
      return {
        role,
        providerId: definition2.providerId,
        status,
        credentialRef: connection.credentialRef ?? null,
        accountId: connection.accountId ?? null,
        teamId: connection.teamId ?? null,
        organizationId: connection.organizationId ?? null,
        scopes: [...connection.scopes],
        expiresAt: connection.expiresAt ?? null,
        declaredVerification: connection.verification,
        providerDoctorStatus: doctor.status,
        issueCodes: doctor.issues.map(({ code }) => code),
        missingLaunchDefaults: missingDefaults,
        nextCommand,
        liveProviderState: "not_checked"
      };
    })
  );
  return {
    schemaVersion: 1,
    profileId: FOUNDER_STACK_PROFILE_ID,
    ownerOrganizationId: options.connection?.ownerOrganizationId ?? null,
    status: roles.every(({ status }) => status === "ready" || status === "manual_only") ? "ready" : "attention_required",
    roles,
    writableCredentialTargets,
    externalEffects: false,
    launchGrantRequired: false,
    verificationScope: "credential_and_transport_readiness_only",
    liveProviderState: "not_checked"
  };
}
var ventureSlugSchema = external_exports.string().min(1).max(63).regex(/^[a-z][a-z0-9]*(?:-[a-z0-9]+)*$/u, "expected a canonical venture slug");
var domainSchema = external_exports.string().min(1).max(253).regex(
  /^(?:[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?\.)+[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?$/u,
  "expected a lowercase domain without a scheme or path"
);
function writableBackendFor(connection, template) {
  const selection = connection.writableCredentialBackend;
  if (selection.mode === "fixture" || selection.mode === "shared") return selection.backend;
  return selection.backends[template];
}
function renderFounderStackProviderConfigOverrides(connection, input) {
  const parsed = parseFounderStackConnection(connection);
  const ventureSlug = ventureSlugSchema.parse(input.ventureSlug);
  const domain = input.domain === void 0 || input.domain === null ? null : domainSchema.parse(input.domain);
  const roleForProvider = /* @__PURE__ */ new Map();
  for (const [role, definition2] of Object.entries(founderStackRoleDefinitions)) {
    roleForProvider.set(definition2.providerId, parsed.roles[role]);
  }
  const neonDatabaseUri = parsed.writableRefs.neonDatabaseUriTemplate.replace(
    "{ventureSlug}",
    ventureSlug
  );
  const stripeWebhookSigning = parsed.writableRefs.stripeWebhookSigningTemplate.replace(
    "{ventureSlug}",
    ventureSlug
  );
  const googleAnalyticsMeasurementId = parsed.writableRefs.googleAnalyticsMeasurementIdTemplate.replace("{ventureSlug}", ventureSlug);
  const writableCredentialRegistrations = [
    {
      purpose: "neon_database_uri",
      ref: neonDatabaseUri,
      provider: "neon",
      kind: "connection_string",
      backend: writableBackendFor(parsed, "neonDatabaseUri"),
      label: `${ventureSlug} Neon database URI capture`,
      scopes: []
    },
    {
      purpose: "stripe_webhook_signing",
      ref: stripeWebhookSigning,
      provider: "stripe",
      kind: "ci_secret",
      backend: writableBackendFor(parsed, "stripeWebhookSigning"),
      label: `${ventureSlug} Stripe webhook signing capture`,
      scopes: []
    },
    {
      purpose: "google_analytics_measurement_id",
      ref: googleAnalyticsMeasurementId,
      provider: "google",
      kind: "ci_secret",
      backend: writableBackendFor(parsed, "googleAnalyticsMeasurementId"),
      label: `${ventureSlug} Google Analytics measurement ID capture`,
      scopes: []
    }
  ];
  const overrideFor = (providerId) => {
    const role = roleForProvider.get(providerId);
    const externalResourceIds = {};
    if (role.organizationId) externalResourceIds.organization_id = role.organizationId;
    if (providerId === "stripe") externalResourceIds.mode = parsed.launchDefaults.stripe.mode;
    if (providerId === "brevo") {
      const defaults = parsed.launchDefaults.brevo;
      if (defaults.senderName) externalResourceIds.sender_name = defaults.senderName;
      if (defaults.senderEmail) externalResourceIds.sender_email = defaults.senderEmail;
      if (defaults.templateName) externalResourceIds.template_name = defaults.templateName;
      if (defaults.templateSubject) externalResourceIds.template_subject = defaults.templateSubject;
      if (defaults.templateHtml) externalResourceIds.template_html = defaults.templateHtml;
    }
    if (providerId === "google" && parsed.launchDefaults.google.analyticsAccountId) {
      externalResourceIds.analytics_account_id = parsed.launchDefaults.google.analyticsAccountId;
    }
    if (providerId === "bing" && parsed.launchDefaults.bing.authMode) {
      externalResourceIds.auth_mode = parsed.launchDefaults.bing.authMode;
    }
    if (providerId === "dns") {
      const defaults = parsed.launchDefaults.dns;
      externalResourceIds.provider_id = defaults.providerId;
      if (defaults.adapter) externalResourceIds.adapter = defaults.adapter;
      if (defaults.registrarAccountId) {
        externalResourceIds.registrar_account_id = defaults.registrarAccountId;
      }
      if (defaults.zoneId) externalResourceIds.zone_id = defaults.zoneId;
    }
    if (providerId === "neon") externalResourceIds.database_credential_ref = neonDatabaseUri;
    if (providerId === "stripe") {
      externalResourceIds.webhook_secret_credential_ref = stripeWebhookSigning;
    }
    if (providerId === "google") {
      externalResourceIds.measurement_id_credential_ref = googleAnalyticsMeasurementId;
    }
    if (domain && providerId === "vercel") externalResourceIds.domain = domain;
    if (domain && providerId === "google") {
      externalResourceIds.site_url = `sc-domain:${domain}`;
      externalResourceIds.sitemap_url = `https://${domain}/sitemap.xml`;
    }
    if (domain && providerId === "bing") externalResourceIds.site_url = `https://${domain}/`;
    if (domain && providerId === "dns") externalResourceIds.domain = domain;
    return {
      state: role.credentialRef ? "auth_required" : "unconfigured",
      credential_ref: role.credentialRef ?? null,
      account_id: role.accountId ?? null,
      team_id: role.teamId ?? null,
      region: providerId === "neon" ? parsed.launchDefaults.neon.region : null,
      required_scopes: [...role.scopes],
      last_verified_at: null,
      external_resource_ids: externalResourceIds,
      ...providerId === "dns" ? { selected_transport: "manual" } : {}
    };
  };
  const providers = {
    github: overrideFor("github"),
    vercel: overrideFor("vercel"),
    neon: overrideFor("neon"),
    stripe: overrideFor("stripe"),
    revenuecat: overrideFor("revenuecat"),
    brevo: overrideFor("brevo"),
    google: overrideFor("google"),
    bing: overrideFor("bing"),
    dns: overrideFor("dns")
  };
  return {
    profileId: parsed.profileId,
    ownerOrganizationId: parsed.ownerOrganizationId,
    ventureSlug,
    domain,
    writableCredentialRefs: {
      neonDatabaseUri,
      stripeWebhookSigning,
      googleAnalyticsMeasurementId
    },
    writableCredentialRegistrations,
    providers
  };
}
async function registerFounderStackWritableCredentialRefs(connection, input, broker) {
  const rendered2 = renderFounderStackProviderConfigOverrides(connection, input);
  const inspections = [];
  for (const registration of rendered2.writableCredentialRegistrations) {
    const existing = broker.getReference(registration.ref);
    if (existing && (existing.provider !== registration.provider || existing.kind !== registration.kind || existing.backend !== registration.backend)) {
      throw new Error(`Writable credential target metadata conflicts at ${registration.ref}`);
    }
    if (!existing) {
      broker.register({
        ref: registration.ref,
        provider: registration.provider,
        kind: registration.kind,
        backend: registration.backend,
        label: registration.label,
        scopes: registration.scopes
      });
    }
    const inspection = await broker.inspect(registration.ref);
    const backendReady = inspection.writable && inspection.status !== "unavailable" && (registration.backend !== "onepassword" || inspection.status === "available");
    if (!backendReady) {
      throw new Error(`Writable credential backend is unavailable for ${registration.purpose}`);
    }
    inspections.push({ ...registration, status: inspection.status, writable: inspection.writable });
  }
  return { rendered: rendered2, inspections };
}

// lib/founder-launch/orchestrator.ts
var CORE_VERSION2 = "0.2.0";
var STACK_VERSION = "0.2.0";
var MAX_IDEA_BYTES = 1e5;
var ROLE_BY_PROVIDER = {
  github: "source.repository",
  vercel: "hosting.web",
  neon: "database.postgres",
  stripe: "commerce.web",
  revenuecat: "commerce.native",
  brevo: "email.transactional",
  google: "growth.google",
  bing: "search.bing",
  dns: "dns.records"
};
var ENVIRONMENT_BINDINGS = {
  "database.postgres": ["DATABASE_URL"],
  "commerce.web": ["STRIPE_SECRET_KEY", "STRIPE_WEBHOOK_SECRET"],
  "email.transactional": ["BREVO_API_KEY"],
  "growth.google": ["NEXT_PUBLIC_GA_MEASUREMENT_ID"]
};
function sha2562(value) {
  return createHash9("sha256").update(value).digest("hex");
}
function seedFor(brief) {
  if (brief.app_kind === "web") return "agentic-web-saas";
  if (brief.app_kind === "mobile_ios" || brief.app_kind === "mobile_cross_platform") {
    return "agentic-ios-subscription";
  }
  return "hybrid-agentic-service";
}
function requiredRoles(brief) {
  const roles = /* @__PURE__ */ new Set(["source.repository"]);
  if (brief.app_kind === "web" || brief.app_kind === "hybrid") roles.add("hosting.web");
  if (brief.needs.database) roles.add("database.postgres");
  if (brief.monetization_model !== "none") {
    roles.add(brief.native_digital_goods ? "commerce.native" : "commerce.web");
  }
  if (brief.needs.transactional_email) roles.add("email.transactional");
  if (brief.needs.analytics || brief.needs.search_discovery) roles.add("growth.google");
  if (brief.needs.search_discovery) roles.add("search.bing");
  if (brief.domain) roles.add("dns.records");
  return [...roles];
}
function accountIdFor(connection, role) {
  const selected = connection.roles[role];
  if (role === "source.repository" || role === "hosting.web") {
    return selected.teamId ?? selected.accountId ?? selected.organizationId ?? null;
  }
  if (role === "database.postgres" || role === "commerce.native") {
    return selected.organizationId ?? selected.accountId ?? selected.teamId ?? null;
  }
  if (role === "growth.google") {
    return connection.launchDefaults.google.analyticsAccountId ?? selected.accountId ?? selected.organizationId ?? null;
  }
  if (role === "dns.records") {
    return founderStackDnsDestinationId(connection);
  }
  return selected.accountId ?? selected.teamId ?? selected.organizationId ?? null;
}
function providerAccounts(connection, graph) {
  const accounts = [];
  const seen = /* @__PURE__ */ new Set();
  for (const node of graph.nodes.filter(
    ({ id, kind }) => kind === "provider" || id === "dns-records"
  )) {
    const provider = launchProviderByNode[node.id];
    const role = provider ? ROLE_BY_PROVIDER[provider] : void 0;
    if (!provider || !role) continue;
    for (const capability of node.authorization.scopes) {
      const selected = connection.roles[role];
      const externalAccountId = provider === "google" && !capability.startsWith("analytics_") ? selected.accountId ?? selected.organizationId ?? null : accountIdFor(connection, role);
      if (!externalAccountId) continue;
      const key = `${provider}\0${capability}\0${externalAccountId}`;
      if (seen.has(key)) continue;
      seen.add(key);
      accounts.push({
        capability,
        provider,
        externalAccountId,
        ownerOrganizationId: connection.ownerOrganizationId,
        stackClass: "company",
        ownership: "company_owned"
      });
    }
  }
  return accounts;
}
function effectsFor(brief) {
  const effects = /* @__PURE__ */ new Set([
    "repository.create",
    "company_stack.provision",
    "source.push",
    "preview.deploy",
    "production.deploy"
  ]);
  if (brief.domain) effects.add("domain.configure");
  if (brief.monetization_model !== "none") effects.add("commerce.configure");
  if (brief.needs.scheduled_learning) effects.add("loops.schedule");
  return [...effects];
}
function containedOutput(baseDir, requested, slug4) {
  const root = resolve8(baseDir);
  const relativeOutput = requested ?? `ventures/${slug4}`;
  if (!relativeOutput || isAbsolute2(relativeOutput)) {
    throw new Error("Founder launch --output must be a non-empty project-relative path");
  }
  const target = resolve8(root, relativeOutput);
  const child = relative5(root, target);
  if (child === "" || child === ".." || child.startsWith(`..${sep5}`) || isAbsolute2(child)) {
    throw new Error("Founder launch --output escapes the selected workspace");
  }
  return target;
}
function loadFounderIdeaFile(file2, baseDir = process.cwd()) {
  if (!file2) throw new Error("Founder launch --idea requires a file path");
  const root = realpathSync3(resolve8(baseDir));
  const target = resolve8(root, file2);
  const child = relative5(root, target);
  if (child === "" || child === ".." || child.startsWith(`..${sep5}`) || isAbsolute2(child)) {
    throw new Error("Founder launch --idea escapes the selected workspace");
  }
  const metadata = lstatSync3(target);
  if (metadata.isSymbolicLink() || !metadata.isFile()) {
    throw new Error("Founder launch --idea must be a regular non-symlink file");
  }
  const canonicalTarget = realpathSync3(target);
  const canonicalChild = relative5(root, canonicalTarget);
  if (canonicalChild === "" || canonicalChild === ".." || canonicalChild.startsWith(`..${sep5}`) || isAbsolute2(canonicalChild)) {
    throw new Error("Founder launch --idea resolves outside the selected workspace");
  }
  if (metadata.size > MAX_IDEA_BYTES) {
    throw new Error(`Founder launch --idea exceeds the ${MAX_IDEA_BYTES}-byte limit`);
  }
  return readFileSync7(canonicalTarget, "utf8");
}
function resolveFounderWorkflowRefSha(baseDir, explicit) {
  const candidate = explicit ?? process.env.VH_WORKFLOW_REF_SHA;
  if (candidate) {
    if (!/^[a-f0-9]{40}$/u.test(candidate)) {
      throw new Error("VH_WORKFLOW_REF_SHA must be an immutable lowercase 40-character Git SHA");
    }
    return candidate;
  }
  try {
    const resolved = execFileSync("git", ["rev-parse", "HEAD"], {
      cwd: resolve8(baseDir),
      encoding: "utf8",
      stdio: ["ignore", "pipe", "ignore"]
    }).trim();
    if (/^[a-f0-9]{40}$/u.test(resolved)) return resolved;
  } catch {
  }
  throw new Error(
    "Founder launch cannot resolve an immutable workflow revision. Set VH_WORKFLOW_REF_SHA to the reviewed Venture Harness commit SHA."
  );
}
function quoteCli(value) {
  return /^[A-Za-z0-9_./-]+$/u.test(value) ? value : `'${value.replaceAll("'", "'\\''")}'`;
}
function blockersFor(compiled, connection, roles, doctor, allowFixtureStack) {
  const blockers = [];
  if (doctor?.writableCredentialTargets.fixtureOnly && !allowFixtureStack) {
    blockers.push({
      code: "stack_role_not_ready",
      message: "The Founder Stack uses fixture-only credential storage and cannot authorize production.",
      nextAction: "Create founder-default with a writable macos_keychain or onepassword backend, then rerun stack doctor."
    });
  }
  const repositoryOwner = accountIdFor(connection, "source.repository");
  if (!repositoryOwner) {
    blockers.push({
      code: "repository_owner_missing",
      role: "source.repository",
      provider: "github",
      message: "The Founder Stack does not identify the exact GitHub owner for the new repository.",
      nextAction: "Update founder-default source.repository with an accountId, teamId, or organizationId, then rerun stack doctor."
    });
  }
  if (compiled.brief.monetization_model !== "none" && !compiled.brief.native_digital_goods && Number(compiled.commercialTerms.monthlyPrice !== null) + Number(compiled.commercialTerms.annualPrice !== null) !== 1) {
    blockers.push({
      code: "exact_price_missing",
      role: "commerce.web",
      provider: "stripe",
      message: "Stripe planning requires exactly one reviewed monthly or annual displayed price.",
      nextAction: "Add exactly one of `Monthly price:` or `Annual price:` to the founder idea and rerun dry-run."
    });
  }
  for (const role of roles) {
    const definitionProvider = role === "source.repository" ? "github" : role === "hosting.web" ? "vercel" : role === "database.postgres" ? "neon" : role === "commerce.web" ? "stripe" : role === "commerce.native" ? "revenuecat" : role === "email.transactional" ? "brevo" : role === "growth.google" ? "google" : role === "search.bing" ? "bing" : "dns";
    const accountId = accountIdFor(connection, role);
    const dnsAdapterReady = role !== "dns.records" || connection.launchDefaults.dns.adapter !== null;
    if (!accountId) {
      blockers.push({
        code: "provider_account_missing",
        role,
        provider: definitionProvider,
        message: `The Founder Stack does not bind ${role} to an exact external account.`,
        nextAction: `Update founder-default ${role} account metadata and rerun stack doctor.`
      });
    }
    if (!dnsAdapterReady) {
      blockers.push({
        code: "stack_role_not_ready",
        role,
        provider: definitionProvider,
        message: "The Founder Stack does not select an exact supported manual DNS adapter for this domain.",
        nextAction: "Set founder-default launchDefaults.dns.adapter to manual_generic or mijndomein_manual, then rerun stack doctor."
      });
    }
    const diagnostic = doctor?.roles.find((candidate) => candidate.role === role);
    if (!diagnostic) {
      blockers.push({
        code: "stack_role_not_ready",
        role,
        provider: definitionProvider,
        message: `Founder Stack role ${role} has not been checked by the read-only doctor.`,
        nextAction: "Run vh stack doctor founder-default, then rerun the launch dry-run."
      });
    } else if (accountId && dnsAdapterReady && diagnostic.status !== "ready" && !(role === "dns.records" && diagnostic.status === "manual_only")) {
      blockers.push({
        code: "stack_role_not_ready",
        role,
        provider: definitionProvider,
        message: `Founder Stack role ${role} is ${diagnostic.status}; no provider effect is authorized.`,
        nextAction: diagnostic.nextCommand
      });
    }
  }
  return blockers.filter(
    (item, index, all) => all.findIndex(
      (candidate) => candidate.code === item.code && candidate.role === item.role && candidate.message === item.message
    ) === index
  );
}
function compileFounderLaunchPreparation(input) {
  if (input.executionMode === "apply" && (!input.production || !input.nonInteractive)) {
    throw new Error("Founder launch apply requires --production and --non-interactive");
  }
  if (!/^[a-f0-9]{40}$/u.test(input.workflowRefSha)) {
    throw new Error("Founder launch requires an immutable 40-character workflow reference SHA");
  }
  const connection = parseFounderStackConnection(input.stack);
  const idea = compileFounderIdea(input.ideaSource);
  const roles = requiredRoles(idea.brief);
  const dryRun = compileLaunchDryRun(idea.brief);
  const outputDirectory = containedOutput(input.baseDir, input.output, idea.brief.id);
  const repositoryOwner = accountIdFor(connection, "source.repository") ?? connection.ownerOrganizationId;
  const allowedExternalEffects = effectsFor(idea.brief);
  const maxBuildAgentTasks = launchBuildAgentTaskCount(dryRun.graph);
  const maxProviderOperations = Math.max(1, launchProviderOperationCeiling(dryRun.graph));
  const grant = createLaunchGrant({
    ownerOrganizationId: connection.ownerOrganizationId,
    ventureName: idea.brief.name,
    ventureSlug: idea.brief.id,
    ideaDigest: idea.sourceHash,
    seed: { id: seedFor(idea.brief), version: CORE_VERSION2 },
    stackProfile: { id: connection.profileId, version: STACK_VERSION },
    repository: {
      owner: repositoryOwner,
      name: idea.brief.id,
      visibility: idea.brief.repository_visibility ?? "private"
    },
    providerAccounts: providerAccounts(connection, dryRun.graph),
    autonomyProfile: input.executionMode === "apply" ? "owner_live_launch" : "plan_only",
    allowedExternalEffects: [...allowedExternalEffects],
    modelExecutionPolicy: idea.brief.synthetic ? {
      mode: "fixture_no_model_execution",
      maxBuildAgentTasks,
      attestation: "fixture_build_host",
      usageAccounting: "none"
    } : {
      mode: "chatgpt_subscription_non_metered",
      maxBuildAgentTasks,
      attestation: "codex_login_status_chatgpt_subscription",
      usageAccounting: "observational"
    },
    providerOperationBudget: {
      maxOperations: maxProviderOperations,
      maxDirectChargeMinorUnits: 0,
      currency: idea.commercialTerms.currency,
      estimateBasis: "reviewed_known_zero_direct_charge",
      ongoingAccountPlanUsageCovered: false
    },
    permissions: {
      productionDeployment: input.production,
      domainConfiguration: input.production && Boolean(idea.brief.domain),
      liveCommerceConfiguration: input.production && idea.brief.monetization_model !== "none"
    },
    createdAt: input.at.toISOString(),
    expiresAt: new Date(input.at.getTime() + 24 * 60 * 60 * 1e3).toISOString(),
    grantedBy: { actorId: input.actorId ?? "local-founder", actorType: "founder" },
    approvalRef: `cli:founder-one-prompt:${sha2562(`${idea.sourceHash}\0${connection.profileId}`).slice(0, 20)}`,
    revokedAt: null
  });
  const materialization = compileVentureMaterialization({
    grant,
    at: input.at,
    coreVersion: CORE_VERSION2,
    workflowRefSha: input.workflowRefSha,
    effects: []
  });
  const stackOverrides = renderFounderStackProviderConfigOverrides(connection, {
    ventureSlug: idea.brief.id,
    domain: idea.brief.domain
  });
  const blockers = blockersFor(
    idea,
    connection,
    roles,
    input.stackDoctor,
    input.allowFixtureStack ?? false
  );
  const selectedProviders = roles.map((role) => {
    const selected = connection.roles[role];
    const renderedProvider = Object.entries(stackOverrides.providers).find(
      ([, candidate]) => candidate.credential_ref === selected.credentialRef
    )?.[0];
    return {
      role,
      provider: renderedProvider ?? (role === "source.repository" ? "github" : role === "hosting.web" ? "vercel" : role === "database.postgres" ? "neon" : role === "commerce.web" ? "stripe" : role === "commerce.native" ? "revenuecat" : role === "email.transactional" ? "brevo" : role === "growth.google" ? "google" : role === "search.bing" ? "bing" : "dns"),
      accountId: selected.accountId ?? null,
      teamId: selected.teamId ?? null,
      organizationId: selected.organizationId ?? null,
      credentialRef: selected.credentialRef ?? null,
      verification: selected.verification.status
    };
  });
  const environmentVariables = roles.flatMap(
    (role) => (ENVIRONMENT_BINDINGS[role] ?? []).map((name) => ({
      name,
      sourceRole: role,
      valuePersisted: false
    }))
  );
  const migrations = materialization.files.filter(({ path }) => path.startsWith("migrations/") && path.endsWith(".up.sql")).map(({ path }) => path);
  const ideaArgument = quoteCli(input.ideaPath);
  const domainMode = !idea.brief.domain ? "none" : connection.roles["dns.records"].credentialRef ? "automatic_if_adapter_available" : "manual_consolidated_action";
  const setup = {
    analytics: idea.brief.needs.analytics ? "google_analytics" : "not_requested",
    search: idea.brief.needs.search_discovery ? "google_search_console_and_bing" : "not_requested",
    email: idea.brief.needs.transactional_email ? "brevo_transactional" : "not_requested",
    commerce: dryRun.decision.payment.provider === "stripe" ? "stripe_test_mode" : dryRun.decision.payment.provider === "revenuecat" ? "revenuecat_test_mode" : "none"
  };
  return Object.freeze({
    schemaVersion: 1,
    status: blockers.length === 0 ? "ready" : "blocked",
    executionMode: input.executionMode,
    production: input.production,
    nonInteractive: input.nonInteractive,
    idea,
    selectedVentureType: {
      mode: dryRun.decision.mode.selectedMode,
      rail: dryRun.decision.rail.appKind,
      seed: materialization.grant.seed,
      commerce: dryRun.decision.payment.provider
    },
    stackProfile: connection.profileId,
    selectedProviders,
    repository: {
      owner: repositoryOwner,
      name: idea.brief.id,
      visibility: idea.brief.repository_visibility ?? "private",
      localDirectory: outputDirectory
    },
    resources: dryRun.resources,
    environmentVariables,
    migrations,
    domain: {
      requested: idea.brief.domain ?? null,
      mode: domainMode,
      expectedRecords: idea.brief.domain ? ["Vercel attachment records", "Google site-verification TXT", "Brevo mail records"] : [],
      canonicalOrigin: idea.brief.domain ? "custom_domain_after_readback" : "provider_production_url",
      pendingAction: idea.brief.domain ? domainMode === "automatic_if_adapter_available" ? "Attach the domain through the installed DNS adapter, then read authoritative DNS back before treating it as canonical." : "Apply the consolidated DNS action for this domain, then read authoritative DNS back before treating it as canonical." : null
    },
    setup,
    estimatedExternalEffects: allowedExternalEffects,
    launchGrant: grant,
    grantDisposition: blockers.length > 0 ? "withheld_blocked" : input.executionMode === "apply" ? "issued_for_apply" : "proposed_not_issued",
    materialization,
    graphDryRun: dryRun,
    blockers,
    exactFinalCommand: `vh launch --idea ${ideaArgument} --stack founder-default --production --apply --non-interactive`
  });
}
async function materializeFounderVenture(preparation, targetDirectory = preparation.repository.localDirectory) {
  if (preparation.executionMode !== "apply") {
    throw new Error("A founder dry-run cannot materialize a venture directory");
  }
  if (preparation.status !== "ready" || preparation.grantDisposition !== "issued_for_apply") {
    throw new Error("A blocked founder launch cannot materialize or issue its Launch Grant");
  }
  return materializeVenture(
    preparation.materialization,
    new NodeMaterializationFileSystem(targetDirectory),
    new Date(preparation.launchGrant.createdAt)
  );
}

// lib/founder-launch/founder-config.ts
import {
  existsSync as existsSync6,
  lstatSync as lstatSync4,
  mkdirSync as mkdirSync4,
  readFileSync as readFileSync8,
  realpathSync as realpathSync4,
  writeFileSync as writeFileSync4
} from "node:fs";
import { homedir as homedir2 } from "node:os";
import { dirname as dirname5, isAbsolute as isAbsolute3, join as join4, relative as relative6, resolve as resolve9, sep as sep6 } from "node:path";
var FOUNDER_CONFIG_KEYS = ["ventures-root"];
function defaultFounderConfigPath(options = {}) {
  const configRoot = options.xdgConfigHome ? resolve9(options.xdgConfigHome) : join4(resolve9(options.homeDirectory ?? homedir2()), ".config");
  return join4(configRoot, "venture-harness", "founder.json");
}
function loadFounderConfig(path = defaultFounderConfigPath()) {
  const absolute = resolve9(path);
  if (!existsSync6(absolute)) return { schemaVersion: 1 };
  const value = JSON.parse(readFileSync8(absolute, "utf8"));
  if (value.schemaVersion !== 1) {
    throw new Error("unsupported founder config; expected schemaVersion 1");
  }
  if (value.venturesRoot !== void 0 && typeof value.venturesRoot !== "string") {
    throw new Error("founder config venturesRoot must be a string");
  }
  return {
    schemaVersion: 1,
    ...typeof value.venturesRoot === "string" ? { venturesRoot: value.venturesRoot } : {}
  };
}
function saveFounderConfig(config, path = defaultFounderConfigPath()) {
  const absolute = resolve9(path);
  mkdirSync4(dirname5(absolute), { recursive: true, mode: 448 });
  writeFileSync4(absolute, `${JSON.stringify(config, null, 2)}
`, { mode: 384 });
}
function contains(parent, child) {
  const relation = relative6(parent, child);
  return relation === "" || !relation.startsWith("..") && !isAbsolute3(relation);
}
function resolveVenturesRoot(candidate, options) {
  if (!candidate.trim()) {
    throw new Error("ventures-root must be a non-empty absolute path");
  }
  const expanded = candidate.startsWith("~/") ? join4(homedir2(), candidate.slice(2)) : candidate;
  if (!isAbsolute3(expanded)) {
    throw new Error(
      `ventures-root must be an absolute path; received ${candidate}. Next: pass an absolute directory such as ~/Projects/ventures.`
    );
  }
  const target = resolve9(expanded);
  if (existsSync6(target)) {
    const metadata = lstatSync4(target);
    if (metadata.isSymbolicLink()) {
      throw new Error(
        `ventures-root must not be a symbolic link; received ${target}. Next: point ventures-root at the real directory.`
      );
    }
    if (!metadata.isDirectory()) {
      throw new Error(`ventures-root must be a directory; received ${target}`);
    }
  } else {
    mkdirSync4(target, { recursive: true, mode: 448 });
  }
  const canonicalTarget = realpathSync4(target);
  const canonicalCore = realpathSync4(resolve9(options.coreRoot));
  if (contains(canonicalCore, canonicalTarget)) {
    throw new Error(
      `ventures-root must not be inside the Venture Harness repository (${canonicalCore}). Ventures are independent products with their own Git history. Next: choose a sibling directory such as ~/Projects/ventures.`
    );
  }
  if (contains(canonicalTarget, canonicalCore)) {
    throw new Error(
      `ventures-root must not contain the Venture Harness repository (${canonicalCore}). Next: choose a directory that holds only ventures, such as ~/Projects/ventures.`
    );
  }
  return canonicalTarget;
}
var VENTURES_ROOT_UNSET_MESSAGE = "No ventures root is configured, so there is no safe directory to materialize an independent venture into.\nNext: run vh config set ventures-root <absolute-path> (for example ~/Projects/ventures), or run vh stack connect founder-default.";
function configuredVenturesRoot(options) {
  const config = loadFounderConfig(options.configPath ?? defaultFounderConfigPath());
  if (!config.venturesRoot) return void 0;
  return resolveVenturesRoot(config.venturesRoot, { coreRoot: options.coreRoot });
}

// lib/learning/engine.ts
var FORBIDDEN_AUTOFIX_EFFECTS = /* @__PURE__ */ new Set([
  "price_change",
  "material_claim_change",
  "bulk_communication",
  "cold_outreach",
  "spend_increase",
  "privacy_expansion",
  "destructive_data_change",
  "app_store_publication",
  "nameserver_change"
]);
function actionSort(a, b) {
  const kindPriority = (candidate) => candidate.kind === "verified_bug_fix" ? 0 : candidate.kind === "config_drift" ? 1 : 2;
  return kindPriority(a) - kindPriority(b) || b.confidence - a.confidence || a.id.localeCompare(b.id);
}
function disposition(candidate, definition2) {
  if (definition2.autonomy !== "autofix_low_risk") return definition2.autonomy;
  const eligibleKind = ["verified_bug_fix", "metadata_repair", "config_drift"].includes(
    candidate.kind
  );
  const forbidden = candidate.effectTypes.some((effect) => FORBIDDEN_AUTOFIX_EFFECTS.has(effect));
  const unauthorized = candidate.effectTypes.some(
    (effect) => !definition2.authorizedEffectTypes.includes(effect)
  );
  return eligibleKind && candidate.risk === "low" && !forbidden && !unauthorized ? "autofix_low_risk" : "propose";
}
function runLearningLoop(args) {
  const { definition: definition2, freshness, metrics } = args;
  const now = args.now ?? /* @__PURE__ */ new Date();
  const freshnessBySource = new Map(freshness.map((entry) => [entry.source, entry]));
  const requiredGaps = definition2.requiredSources.flatMap((requirement) => {
    const entry = freshnessBySource.get(requirement.source);
    if (!entry || entry.status !== "fresh") {
      return [
        `Required source ${requirement.source} is ${entry?.status ?? "missing"}; missing is not zero.`
      ];
    }
    return [];
  });
  const invalidMetrics = metrics.flatMap((metric) => {
    if (metric.value !== null && !Number.isFinite(metric.value))
      return [`Metric ${metric.id} is non-finite.`];
    if (metric.sampleSize !== null && metric.sampleSize < 0)
      return [`Metric ${metric.id} has a negative sample size.`];
    return [];
  });
  const missingPrimary = definition2.primaryMetrics.filter(
    (id) => !metrics.some((metric) => metric.id === id && metric.value !== null)
  );
  const limitations = [
    ...requiredGaps,
    ...invalidMetrics,
    ...missingPrimary.map((id) => `Primary metric ${id} is unavailable.`),
    ...metrics.flatMap(
      (metric) => metric.limitation ? [`${metric.id}: ${metric.limitation}`] : []
    )
  ];
  if (requiredGaps.length > 0 || invalidMetrics.length > 0 || missingPrimary.length > 0) {
    return {
      loopId: definition2.id,
      cadence: definition2.cadence,
      generatedAt: now.toISOString(),
      status: "insufficient_evidence",
      freshness,
      metrics,
      protectedWinners: [],
      actions: [],
      rejectedActions: args.candidates.map((candidate) => ({
        id: candidate.id,
        reason: "Required decision evidence is missing, stale, or invalid."
      })),
      limitations,
      nextRunAt: definition2.nextRunAt,
      stopCondition: definition2.stopCondition
    };
  }
  const actions = [];
  const rejectedActions = [];
  const conceptualJourneys = /* @__PURE__ */ new Set();
  let boundedActions = 0;
  for (const candidate of [...args.candidates].sort(actionSort)) {
    if (candidate.confidence < 0.7) {
      rejectedActions.push({
        id: candidate.id,
        reason: "Confidence is below the 0.70 action threshold."
      });
      continue;
    }
    if (candidate.protectsWinner && candidate.kind === "conceptual_hypothesis") {
      rejectedActions.push({
        id: candidate.id,
        reason: "The proposal risks a protected winner without stronger evidence."
      });
      continue;
    }
    if (candidate.kind === "conceptual_hypothesis" && conceptualJourneys.has(candidate.journey)) {
      rejectedActions.push({
        id: candidate.id,
        reason: "Only one conceptual hypothesis may be active per journey."
      });
      continue;
    }
    const unboundedBugFix = candidate.kind === "verified_bug_fix";
    if (!unboundedBugFix && boundedActions >= definition2.maximumActions) {
      rejectedActions.push({
        id: candidate.id,
        reason: `Loop action cap ${definition2.maximumActions} reached.`
      });
      continue;
    }
    if (candidate.kind === "conceptual_hypothesis") conceptualJourneys.add(candidate.journey);
    if (!unboundedBugFix) boundedActions += 1;
    actions.push({ ...candidate, disposition: disposition(candidate, definition2) });
  }
  return {
    loopId: definition2.id,
    cadence: definition2.cadence,
    generatedAt: now.toISOString(),
    status: "complete",
    freshness,
    metrics,
    protectedWinners: args.candidates.filter((candidate) => candidate.protectsWinner).map((candidate) => candidate.journey).filter((value, index, all) => all.indexOf(value) === index).sort(),
    actions,
    rejectedActions,
    limitations,
    nextRunAt: definition2.nextRunAt,
    stopCondition: definition2.stopCondition
  };
}
function buildOperatingCadence(args) {
  const next = (cadence) => args.loops.filter((loop2) => loop2.cadence === cadence && loop2.nextRunAt).map((loop2) => loop2.nextRunAt).sort()[0] ?? null;
  return {
    generatedAt: (args.now ?? /* @__PURE__ */ new Date()).toISOString(),
    nextDailyReview: next("daily"),
    nextWeeklyReview: next("weekly"),
    nextBiweeklyReview: next("biweekly"),
    nextMonthlyReview: next("monthly"),
    missingDataSources: args.freshness.filter((entry) => entry.required && entry.status !== "fresh").map((entry) => entry.source).sort(),
    activeHypotheses: [...new Set(args.activeHypotheses ?? [])].sort(),
    activeExperiments: [...new Set(args.activeExperiments ?? [])].sort(),
    activeBlockers: [...new Set(args.activeBlockers ?? [])].sort()
  };
}

// lib/learning/derive.ts
function latestDataset(datasets, source) {
  return datasets.filter((dataset) => dataset.provenance.source === source).sort(
    (left, right) => Date.parse(right.provenance.fetchedAt) - Date.parse(left.provenance.fetchedAt)
  )[0];
}
function matches(row, filter) {
  return Object.entries(filter ?? {}).every(([key, value]) => Object.is(row[key], value));
}
function numericSum(rows, field) {
  const values = rows.map((row) => row[field]);
  return values.every((value) => typeof value === "number" && Number.isFinite(value)) ? values.reduce((sum, value) => sum + value, 0) : null;
}
function uniqueDefinitions(definitions) {
  return definitions.filter(
    (definition2, index, all) => all.findIndex(({ id }) => id === definition2.id) === index
  );
}
function inferObservedMetricDefinitions(datasets) {
  const definitions = [];
  const neon = latestDataset(datasets, "neon_commercial_evidence");
  for (const row of neon?.rows ?? []) {
    if (typeof row.record_type !== "string" || typeof row.metric_id !== "string") continue;
    const base = `${row.record_type}.${row.metric_id}`;
    definitions.push({
      id: `${base}.count`,
      source: "neon_commercial_evidence",
      filter: { record_type: row.record_type, metric_id: row.metric_id },
      value: { operation: "sum", field: "event_count" },
      sampleSizeField: "sample_size"
    });
    if (row.record_type === "commercial") {
      definitions.push({
        id: `${base}.qualified_count`,
        source: "neon_commercial_evidence",
        filter: { record_type: row.record_type, metric_id: row.metric_id },
        value: { operation: "sum", field: "qualified_count" },
        sampleSizeField: "sample_size"
      });
    }
  }
  if (latestDataset(datasets, "release_log")) {
    definitions.push(
      {
        id: "release.total",
        source: "release_log",
        value: { operation: "count_rows" }
      },
      {
        id: "release.failed",
        source: "release_log",
        filter: { status: "failed" },
        value: { operation: "count_rows" }
      },
      {
        id: "release.rolled_back",
        source: "release_log",
        filter: { status: "rolled_back" },
        value: { operation: "count_rows" }
      },
      {
        id: "release.incident_count",
        source: "release_log",
        value: { operation: "sum", field: "incident_count" }
      }
    );
  }
  return uniqueDefinitions(definitions);
}
function deriveLearningMetrics(datasets, definitions = inferObservedMetricDefinitions(datasets)) {
  return uniqueDefinitions(definitions).flatMap((definition2) => {
    const dataset = latestDataset(datasets, definition2.source);
    if (!dataset) return [];
    const rows = dataset.rows.filter((row) => matches(row, definition2.filter));
    const value = definition2.value.operation === "count_rows" ? rows.length : numericSum(rows, definition2.value.field);
    const sampleSize = definition2.sampleSizeField ? numericSum(rows, definition2.sampleSizeField) : rows.length;
    const invalidValue = value === null ? `Field ${definition2.value.operation === "sum" ? definition2.value.field : "rows"} was not consistently numeric.` : null;
    const limitations = [
      ...dataset.provenance.limitations,
      ...definition2.limitation ? [definition2.limitation] : [],
      ...rows.length === 0 ? [
        "The source query completed for this window and returned no matching rows; zero is observed, not imputed from missing data."
      ] : [],
      ...invalidValue ? [invalidValue] : []
    ];
    return [
      {
        id: definition2.id,
        value,
        sampleSize,
        source: definition2.source,
        window: { ...dataset.provenance.reportingWindow },
        limitation: limitations.length > 0 ? [...new Set(limitations)].join(" ") : void 0,
        evidenceRefs: [dataset.id]
      }
    ];
  });
}
function thresholdMatches(value, comparator, threshold) {
  if (comparator === "lt") return value < threshold;
  if (comparator === "lte") return value <= threshold;
  if (comparator === "gt") return value > threshold;
  if (comparator === "gte") return value >= threshold;
  return value === threshold;
}
function deriveDescriptiveCandidates(args) {
  const maximum = args.maximumCandidates ?? 3;
  if (!Number.isInteger(maximum) || maximum < 0) {
    throw new Error("maximumCandidates must be a non-negative integer.");
  }
  for (const rule of args.rules) {
    if (!Number.isFinite(rule.threshold)) {
      throw new Error(`Candidate rule ${rule.id} has a non-finite threshold.`);
    }
    if (!Number.isInteger(rule.minimumSampleSize) || rule.minimumSampleSize < 0) {
      throw new Error(`Candidate rule ${rule.id} has an invalid minimumSampleSize.`);
    }
    if (!Number.isFinite(rule.confidence) || rule.confidence < 0 || rule.confidence > 1) {
      throw new Error(`Candidate rule ${rule.id} confidence must be between 0 and 1.`);
    }
  }
  return args.rules.flatMap((rule) => {
    const metric = args.metrics.find(({ id }) => id === rule.metricId);
    if (!metric || metric.value === null || !Number.isFinite(metric.value) || metric.sampleSize === null || metric.sampleSize < rule.minimumSampleSize || !thresholdMatches(metric.value, rule.comparator, rule.threshold)) {
      return [];
    }
    const comparator = {
      lt: "<",
      lte: "<=",
      gt: ">",
      gte: ">=",
      eq: "="
    }[rule.comparator];
    return [
      {
        id: rule.id,
        journey: rule.journey,
        kind: "conceptual_hypothesis",
        title: rule.title,
        rationale: `Observed ${rule.metricId}=${metric.value} with sample size ${metric.sampleSize} for ${metric.window.start}..${metric.window.end}; the predeclared threshold was ${comparator} ${rule.threshold}. This descriptive signal does not establish cause.`,
        evidenceRefs: [...metric.evidenceRefs ?? []],
        confidence: rule.confidence,
        risk: rule.risk,
        effectTypes: [...rule.effectTypes ?? ["local_write"]],
        protectsWinner: rule.protectsWinner ?? false
      }
    ];
  }).sort((left, right) => right.confidence - left.confidence || left.id.localeCompare(right.id)).slice(0, maximum);
}

// lib/learning/default-data-runtime.ts
function createDefaultDataLearningRuntime(options) {
  const connectors = [];
  const requirementsBySource = /* @__PURE__ */ new Map();
  const credentialRefs = {};
  if (options.neon) {
    connectors.push(
      createNeonCommercialEvidenceConnector({
        broker: options.broker,
        runner: options.commandRunner,
        sourceAccount: options.neon.sourceAccount,
        timezone: options.timezone,
        windowHours: options.neon.windowHours,
        releaseVersion: options.neon.releaseVersion
      })
    );
    requirementsBySource.set("neon_commercial_evidence", {
      source: "neon_commercial_evidence",
      required: options.neon.required ?? true,
      freshnessHours: options.neon.windowHours ?? 24 * 7
    });
    credentialRefs.neon_commercial_evidence = options.neon.credentialRef;
  }
  if (options.releaseLog !== false) {
    const releaseLog = options.releaseLog ?? {};
    connectors.push(
      createReleaseLogConnector({
        rootDir: options.rootDir,
        path: releaseLog.path,
        sourceAccount: releaseLog.sourceAccount,
        timezone: options.timezone,
        windowHours: releaseLog.windowHours
      })
    );
    requirementsBySource.set("release_log", {
      source: "release_log",
      required: releaseLog.required ?? false,
      freshnessHours: releaseLog.windowHours ?? 24 * 30
    });
  }
  for (const entry of options.directConnectors ?? []) {
    connectors.push(entry.connector);
    if (entry.credentialRef) credentialRefs[entry.connector.source] = entry.credentialRef;
  }
  for (const requirement of options.requirements ?? []) {
    requirementsBySource.set(requirement.source, { ...requirement });
  }
  const requirements = [...requirementsBySource.values()].sort(
    (left, right) => left.source.localeCompare(right.source)
  );
  const derive = (datasets) => {
    const metrics = deriveLearningMetrics(datasets, options.metricDefinitions);
    const candidates = deriveDescriptiveCandidates({
      metrics,
      rules: options.candidateRules ?? [],
      maximumCandidates: options.maximumCandidates
    });
    return { metrics, candidates };
  };
  return {
    connectors,
    requirements,
    credentialRefs,
    sync: ({ now, signal } = {}) => syncDataSources(connectors, requirements, {
      now,
      signal,
      credentialRefs
    }),
    derive,
    learn({ definition: definition2, syncResult, now }) {
      const inputs = derive(syncResult.datasets);
      return runLearningLoop({
        definition: definition2,
        freshness: syncResult.freshness,
        metrics: inputs.metrics,
        candidates: inputs.candidates,
        now
      });
    }
  };
}

// lib/learning/report.ts
import { mkdirSync as mkdirSync5, renameSync as renameSync4, writeFileSync as writeFileSync5 } from "node:fs";
import { dirname as dirname6, relative as relative7, resolve as resolve10, sep as sep7 } from "node:path";
function inside3(root, candidate) {
  const absolute = resolve10(root, candidate);
  const rel = relative7(root, absolute);
  if (rel === "" || !rel.startsWith(`..${sep7}`) && rel !== ".." && !rel.startsWith(sep7)) {
    return absolute;
  }
  throw new Error(`Learning report destination escapes the venture root: ${candidate}`);
}
function repositoryPath(root, absolute) {
  return relative7(root, absolute).split(sep7).join("/");
}
function writeAtomic(path, content) {
  mkdirSync5(dirname6(path), { recursive: true, mode: 448 });
  const temporary = `${path}.next-${process.pid}-${Date.now()}`;
  writeFileSync5(temporary, content, { encoding: "utf8", mode: 384 });
  renameSync4(temporary, path);
}
function singleLine(value) {
  return value.replace(/[\u0000-\u001f\u007f]+/g, " ").trim();
}
function renderLearningReportMarkdown(report) {
  const limitations = report.limitations.length ? report.limitations.map((item) => `- ${singleLine(item)}`) : ["- None recorded."];
  const actions = report.actions.length ? report.actions.map(
    (action) => `- **${singleLine(action.title)}** (${action.disposition}; ${action.confidence.toFixed(2)} confidence) \u2014 ${singleLine(action.rationale)}`
  ) : ["- None. No action is inferred from missing evidence."];
  const freshness = report.freshness.length ? report.freshness.map(
    (entry) => `- ${entry.source}: ${entry.status}; fetched ${entry.fetchedAt ?? "never"}; age ${entry.ageHours ?? "unknown"}h`
  ) : ["- No normalized data-source evidence was available."];
  return [
    `# ${singleLine(report.cadence)} learning report`,
    "",
    `- Loop: \`${singleLine(report.loopId)}\``,
    `- Generated: ${report.generatedAt}`,
    `- Status: **${report.status}**`,
    `- Next run: ${report.nextRunAt ?? "not scheduled"}`,
    "",
    "## Freshness",
    "",
    ...freshness,
    "",
    "## Actions",
    "",
    ...actions,
    "",
    "## Limitations",
    "",
    ...limitations,
    "",
    "## Stop condition",
    "",
    singleLine(report.stopCondition),
    ""
  ].join("\n");
}
function persistLearningReport(args) {
  const root = resolve10(args.rootDir);
  const destination = inside3(root, args.definition.outputDestination);
  const stamp = args.report.generatedAt.replace(/[:.]/g, "-");
  const base = `${stamp}-${args.report.loopId.replace(/[^a-z0-9_-]+/gi, "-")}`;
  const jsonPath = inside3(destination, `${base}.json`);
  const markdownPath = inside3(destination, `${base}.md`);
  const latestJsonPath = inside3(destination, "latest.json");
  const latestMarkdownPath = inside3(destination, "latest.md");
  const json2 = `${JSON.stringify(args.report, null, 2)}
`;
  const markdown = renderLearningReportMarkdown(args.report);
  for (const path of [jsonPath, latestJsonPath]) writeAtomic(path, json2);
  for (const path of [markdownPath, latestMarkdownPath]) writeAtomic(path, markdown);
  return {
    json: repositoryPath(root, jsonPath),
    markdown: repositoryPath(root, markdownPath),
    latestJson: repositoryPath(root, latestJsonPath),
    latestMarkdown: repositoryPath(root, latestMarkdownPath)
  };
}
function renderOperatingCadenceMarkdown(cadence) {
  const items = (values, empty) => values.length > 0 ? values.map((value) => `- ${singleLine(value)}`) : [`- ${empty}`];
  return [
    "# Operating cadence",
    "",
    `- Generated: ${cadence.generatedAt}`,
    `- Next daily review: ${cadence.nextDailyReview ?? "not scheduled"}`,
    `- Next weekly review: ${cadence.nextWeeklyReview ?? "not scheduled"}`,
    `- Next biweekly review: ${cadence.nextBiweeklyReview ?? "not scheduled"}`,
    `- Next monthly review: ${cadence.nextMonthlyReview ?? "not scheduled"}`,
    "",
    "## Missing data sources",
    "",
    ...items(cadence.missingDataSources, "None in the latest direct-sync artifact."),
    "",
    "## Active hypotheses",
    "",
    ...items(cadence.activeHypotheses, "None recorded."),
    "",
    "## Active experiments",
    "",
    ...items(cadence.activeExperiments, "None recorded."),
    "",
    "## Active blockers",
    "",
    ...items(cadence.activeBlockers, "None recorded."),
    ""
  ].join("\n");
}
function persistOperatingCadence(args) {
  const root = resolve10(args.rootDir);
  const jsonPath = inside3(root, "reports/learning/operating-cadence.json");
  const markdownPath = inside3(root, "reports/learning/operating-cadence.md");
  writeAtomic(jsonPath, `${JSON.stringify(args.cadence, null, 2)}
`);
  writeAtomic(markdownPath, renderOperatingCadenceMarkdown(args.cadence));
  return {
    json: repositoryPath(root, jsonPath),
    markdown: repositoryPath(root, markdownPath)
  };
}

// lib/learning/schedule.ts
var FIELD_LIMITS = [
  { name: "minute", minimum: 0, maximum: 59 },
  { name: "hour", minimum: 0, maximum: 23 },
  { name: "day of month", minimum: 1, maximum: 31 },
  { name: "month", minimum: 1, maximum: 12 },
  { name: "day of week", minimum: 0, maximum: 6 }
];
function parseField(value, index) {
  const limit = FIELD_LIMITS[index];
  if (value === "*") return { any: true, values: /* @__PURE__ */ new Set() };
  const values = value.split(",").map((part) => {
    if (!/^\d+$/.test(part)) {
      throw new Error(
        `Unsupported ${limit.name} cron field "${value}"; use * or comma-separated integers.`
      );
    }
    const parsed = Number(part);
    if (parsed < limit.minimum || parsed > limit.maximum) {
      throw new Error(
        `${limit.name} cron value ${parsed} is outside ${limit.minimum}-${limit.maximum}.`
      );
    }
    return parsed;
  });
  return { any: false, values: new Set(values) };
}
function dayMatches(dayOfMonth, dayOfWeek, date) {
  if (dayOfMonth.any && dayOfWeek.any) return true;
  const monthDayMatches = dayOfMonth.values.has(date.getUTCDate());
  const weekDayMatches = dayOfWeek.values.has(date.getUTCDay());
  if (dayOfMonth.any) return weekDayMatches;
  if (dayOfWeek.any) return monthDayMatches;
  return monthDayMatches || weekDayMatches;
}
function nextCronOccurrence(expression, after) {
  if (Number.isNaN(after.getTime())) throw new Error("Cannot schedule from an invalid date.");
  const parts = expression.trim().split(/\s+/);
  if (parts.length !== 5) {
    throw new Error(`Unsupported cron expression "${expression}"; expected five fields.`);
  }
  const [minute, hour, dayOfMonth, month, dayOfWeek] = parts.map(parseField);
  const candidate = new Date(after);
  candidate.setUTCSeconds(0, 0);
  candidate.setUTCMinutes(candidate.getUTCMinutes() + 1);
  const maximum = candidate.getTime() + 366 * 2 * 24 * 60 * 60 * 1e3;
  while (candidate.getTime() <= maximum) {
    if ((month.any || month.values.has(candidate.getUTCMonth() + 1)) && (hour.any || hour.values.has(candidate.getUTCHours())) && (minute.any || minute.values.has(candidate.getUTCMinutes())) && dayMatches(dayOfMonth, dayOfWeek, candidate)) {
      return candidate.toISOString();
    }
    candidate.setUTCMinutes(candidate.getUTCMinutes() + 1);
  }
  throw new Error(`Cron expression "${expression}" has no occurrence within two years.`);
}

// lib/migrations/file-system.ts
import { mkdir as mkdir3, readFile as readFile2, rename as rename2, rm, writeFile as writeFile2 } from "node:fs/promises";
import { dirname as dirname7, relative as relative8, resolve as resolve11, sep as sep8 } from "node:path";
function insideRoot(root, path) {
  const absolute = resolve11(root, path);
  const rel = relative8(root, absolute);
  if (rel === "" || !rel.startsWith(`..${sep8}`) && rel !== ".." && !rel.startsWith(sep8)) {
    return absolute;
  }
  throw new Error(`migration path escapes repository root: ${path}`);
}
function createNodeMigrationFileSystem(root = process.cwd()) {
  let sequence = 0;
  return {
    async readText(path) {
      try {
        return await readFile2(insideRoot(root, path), "utf8");
      } catch (error) {
        if (error.code === "ENOENT") return null;
        throw error;
      }
    },
    async writeAtomic(path, content) {
      const destination = insideRoot(root, path);
      await mkdir3(dirname7(destination), { recursive: true });
      const temporary = `${destination}.vh-next-${process.pid}-${sequence++}`;
      try {
        await writeFile2(temporary, content, { encoding: "utf8", flag: "wx" });
        await rename2(temporary, destination);
      } finally {
        await rm(temporary, { force: true }).catch(() => void 0);
      }
    },
    async remove(path) {
      await rm(insideRoot(root, path), { force: true });
    }
  };
}

// lib/migrations/v0-1-to-v0-2.ts
import { parse as parse2, stringify as stringify3 } from "yaml";

// lib/migrations/types.ts
var migrationManifestSchema = external_exports.object({
  id: external_exports.string().regex(/^\d{3}-[a-z0-9-]+$/),
  from_version: semverSchema,
  to_version: semverSchema,
  reversible: external_exports.literal(true),
  lock_update_last: external_exports.literal(true),
  writes: external_exports.array(external_exports.string().min(1)).min(1)
}).strict();
var migrationChangeReportSchema = external_exports.object({
  path: external_exports.string(),
  operation: external_exports.enum(["create", "update", "unchanged"]),
  status: external_exports.enum(["planned", "applied", "unchanged", "rolled_back", "rollback_failed"])
}).strict();
var migrationReportSchema = external_exports.object({
  migration_id: external_exports.string(),
  from_version: semverSchema,
  to_version: semverSchema,
  status: external_exports.enum(["planned", "applied", "already_current", "failed"]),
  dry_run: external_exports.boolean(),
  started_at: external_exports.string().datetime({ offset: true }),
  completed_at: external_exports.string().datetime({ offset: true }),
  changes: external_exports.array(migrationChangeReportSchema),
  warnings: external_exports.array(external_exports.string()),
  lock_updated: external_exports.boolean(),
  rolled_back: external_exports.boolean(),
  error: external_exports.object({
    code: external_exports.string(),
    message: external_exports.string(),
    next_action: external_exports.string()
  }).strict().nullable(),
  rollback_errors: external_exports.array(external_exports.string())
}).strict();

// lib/migrations/runner.ts
function safeErrorMessage(error) {
  if (!(error instanceof Error)) return "unknown migration failure";
  return error.message.replace(
    /(gh[pousr]_|sk_(?:live|test)_|xox[baprs]-)[A-Za-z0-9_-]+/g,
    "$1[REDACTED]"
  );
}
function validatePlan(plan) {
  const paths = plan.changes.map((change) => change.path);
  if (new Set(paths).size !== paths.length)
    throw new Error("migration plan contains duplicate paths");
  for (const path of paths) {
    if (path.startsWith("/") || path.split("/").includes("..")) {
      throw new Error(`migration path must be repository-relative: ${path}`);
    }
  }
  const lockChanges = plan.changes.filter((change) => change.path === "harness.lock");
  if (lockChanges.length !== 1) {
    throw new Error("migration plan must contain exactly one harness.lock change");
  }
  return [...plan.changes].sort((a, b) => {
    if (a.path === "harness.lock") return 1;
    if (b.path === "harness.lock") return -1;
    return a.path.localeCompare(b.path);
  });
}
async function applyMigrationPlan(plan, options) {
  const clock = options.clock ?? (() => /* @__PURE__ */ new Date());
  const startedAt = clock().toISOString();
  let ordered;
  try {
    ordered = validatePlan(plan);
  } catch (error) {
    return migrationReportSchema.parse({
      migration_id: plan.id,
      from_version: plan.fromVersion,
      to_version: plan.toVersion,
      status: "failed",
      dry_run: options.dryRun ?? false,
      started_at: startedAt,
      completed_at: clock().toISOString(),
      changes: [],
      warnings: plan.warnings,
      lock_updated: false,
      rolled_back: false,
      error: {
        code: "invalid_migration_plan",
        message: safeErrorMessage(error),
        next_action: "Fix the migration plan; no files were changed."
      },
      rollback_errors: []
    });
  }
  const originals = /* @__PURE__ */ new Map();
  const changes = [];
  try {
    for (const change of ordered) {
      const original = await options.fileSystem.readText(change.path);
      originals.set(change.path, original);
      const operation2 = original === null ? "create" : original === change.content ? "unchanged" : "update";
      changes.push({
        path: change.path,
        operation: operation2,
        status: operation2 === "unchanged" ? "unchanged" : "planned"
      });
    }
  } catch (error) {
    return migrationReportSchema.parse({
      migration_id: plan.id,
      from_version: plan.fromVersion,
      to_version: plan.toVersion,
      status: "failed",
      dry_run: options.dryRun ?? false,
      started_at: startedAt,
      completed_at: clock().toISOString(),
      changes,
      warnings: plan.warnings,
      lock_updated: false,
      rolled_back: false,
      error: {
        code: "migration_read_failed",
        message: safeErrorMessage(error),
        next_action: "Restore readable repository files and rerun the migration."
      },
      rollback_errors: []
    });
  }
  const pending = changes.filter((change) => change.operation !== "unchanged");
  if (pending.length === 0) {
    return migrationReportSchema.parse({
      migration_id: plan.id,
      from_version: plan.fromVersion,
      to_version: plan.toVersion,
      status: "already_current",
      dry_run: options.dryRun ?? false,
      started_at: startedAt,
      completed_at: clock().toISOString(),
      changes,
      warnings: plan.warnings,
      lock_updated: false,
      rolled_back: false,
      error: null,
      rollback_errors: []
    });
  }
  if (options.dryRun) {
    return migrationReportSchema.parse({
      migration_id: plan.id,
      from_version: plan.fromVersion,
      to_version: plan.toVersion,
      status: "planned",
      dry_run: true,
      started_at: startedAt,
      completed_at: clock().toISOString(),
      changes,
      warnings: plan.warnings,
      lock_updated: false,
      rolled_back: false,
      error: null,
      rollback_errors: []
    });
  }
  const attempted = [];
  try {
    for (const change of ordered) {
      const report = changes.find((entry) => entry.path === change.path);
      if (report.operation === "unchanged") continue;
      attempted.push(change.path);
      await options.fileSystem.writeAtomic(change.path, change.content);
      report.status = "applied";
    }
    return migrationReportSchema.parse({
      migration_id: plan.id,
      from_version: plan.fromVersion,
      to_version: plan.toVersion,
      status: "applied",
      dry_run: false,
      started_at: startedAt,
      completed_at: clock().toISOString(),
      changes,
      warnings: plan.warnings,
      lock_updated: changes.some(
        (change) => change.path === "harness.lock" && change.status === "applied"
      ),
      rolled_back: false,
      error: null,
      rollback_errors: []
    });
  } catch (error) {
    const rollbackErrors = [];
    for (const path of [...attempted].reverse()) {
      const report = changes.find((entry) => entry.path === path);
      try {
        const original = originals.get(path);
        if (original === null) await options.fileSystem.remove(path);
        else await options.fileSystem.writeAtomic(path, original);
        report.status = "rolled_back";
      } catch (rollbackError) {
        report.status = "rollback_failed";
        rollbackErrors.push(`${path}: ${safeErrorMessage(rollbackError)}`);
      }
    }
    return migrationReportSchema.parse({
      migration_id: plan.id,
      from_version: plan.fromVersion,
      to_version: plan.toVersion,
      status: "failed",
      dry_run: false,
      started_at: startedAt,
      completed_at: clock().toISOString(),
      changes,
      warnings: plan.warnings,
      lock_updated: false,
      rolled_back: attempted.length > 0 && rollbackErrors.length === 0,
      error: {
        code: "migration_write_failed",
        message: safeErrorMessage(error),
        next_action: rollbackErrors.length === 0 ? "The original files were restored; resolve the write failure and rerun." : "Rollback was incomplete; restore the listed files from version control before rerunning."
      },
      rollback_errors: rollbackErrors
    });
  }
}

// lib/migrations/v0-1-to-v0-2.ts
var V01_TO_V02_MIGRATION_ID = "001-v0-1-to-v0-2";
var V01_VERSION = "0.1.0";
var V02_VERSION = "0.2.0";
function yaml2(value) {
  return stringify3(value, { lineWidth: 100, sortMapEntries: false });
}
function preparationFailure(message, code, nextAction2, clock, warnings = []) {
  const started = clock().toISOString();
  return migrationReportSchema.parse({
    migration_id: V01_TO_V02_MIGRATION_ID,
    from_version: V01_VERSION,
    to_version: V02_VERSION,
    status: "failed",
    dry_run: false,
    started_at: started,
    completed_at: clock().toISOString(),
    changes: [],
    warnings,
    lock_updated: false,
    rolled_back: false,
    error: { code, message, next_action: nextAction2 },
    rollback_errors: []
  });
}
function nullableString(value) {
  return typeof value === "string" && value.length > 0 ? value : null;
}
function valueOr(value, allowed, fallback) {
  return typeof value === "string" && allowed.includes(value) ? value : fallback;
}
function migrateVenture(legacyInput) {
  const legacy = legacyVentureSchema.parse(legacyInput);
  const old = legacy.venture;
  const validation = legacy.validation;
  const targetMarket = nullableString(old.target_market) ?? nullableString(old.market);
  const locale = nullableString(old.locale) ?? old.language;
  return ventureV02Schema.parse({
    ...legacy,
    venture: {
      ...old,
      name: old.name,
      legal_name: nullableString(old.legal_name),
      domain: old.domain,
      market: nullableString(old.market),
      target_market: targetMarket,
      language: old.language,
      locale,
      currency: old.currency.toUpperCase(),
      timezone: old.timezone,
      stage: old.stage,
      repository_visibility: valueOr(old.repository_visibility, ["private", "public"], "private"),
      production_status: valueOr(
        old.production_status,
        ["none", "preview_live", "validation_site_live", "product_live", "testflight_live"],
        "none"
      ),
      harness_version: V02_VERSION,
      app_kind: valueOr(
        old.app_kind,
        ["web", "mobile_ios", "mobile_cross_platform", "hybrid"],
        "web"
      ),
      launch_mode: valueOr(
        old.launch_mode,
        ["validate_first", "thin_mvp", "product_first", "concierge_first"],
        "validate_first"
      ),
      business_model: valueOr(
        old.business_model,
        ["unselected", "b2b", "b2c", "b2b2c", "marketplace", "internal", "nonprofit"],
        "unselected"
      ),
      monetization_model: valueOr(
        old.monetization_model,
        [
          "unselected",
          "none",
          "subscription",
          "one_time",
          "usage_based",
          "transaction_fee",
          "lead_generation",
          "services",
          "hybrid"
        ],
        "unselected"
      ),
      risk_profile: valueOr(
        old.risk_profile,
        ["unassessed", "low", "moderate", "high", "regulated"],
        "unassessed"
      ),
      privacy_profile: valueOr(
        old.privacy_profile,
        ["unassessed", "minimal", "standard", "sensitive", "special_category"],
        "unassessed"
      ),
      mobile_stack: valueOr(
        old.mobile_stack,
        ["none", "expo_react_native", "swiftui", "auto"],
        "none"
      ),
      outcomes: {
        primary: {
          statement: null,
          success_signal: validation.primary_conversion
        },
        secondary: []
      },
      capabilities: {
        active: [
          "public_website",
          "database",
          "ga4",
          "gsc",
          "bing_webmaster",
          "vercel_analytics",
          "web_seo_aeo_geo",
          "feedback_intake",
          "scheduled_learning_loops"
        ],
        open: []
      },
      extensions: old.extensions && typeof old.extensions === "object" && !Array.isArray(old.extensions) ? old.extensions : {}
    },
    validation,
    infrastructure: legacy.infrastructure,
    extensions: legacy.extensions && typeof legacy.extensions === "object" && !Array.isArray(legacy.extensions) ? legacy.extensions : {}
  });
}
var BOOLEAN_PROVIDER_MAP = {
  domain_registered: "dns",
  vercel_project_created: "vercel",
  neon_database_created: "neon",
  ga4_property_created: "google",
  vercel_analytics_enabled: "vercel",
  google_search_console_verified: "google",
  bing_webmaster_verified: "bing"
};
function migrateProviders(infrastructure, warnings) {
  const config = createDefaultProvidersConfig();
  for (const [legacyKey, providerId] of Object.entries(BOOLEAN_PROVIDER_MAP)) {
    if (infrastructure[legacyKey] !== true) continue;
    const provider = config.providers[providerId];
    provider.state = "configured";
    provider.next_action = "Read provider state back and attach verification evidence.";
    warnings.push(
      `${legacyKey}=true mapped to ${providerId}: configured, not verified; v0.1 stored no evidence artifact.`
    );
  }
  return providersSchema.parse(config);
}
async function preserveOrDefault(fileSystem, path, schema, fallback, warnings) {
  const existing = await fileSystem.readText(path);
  if (existing === null) return yaml2(fallback);
  schema.parse(parse2(existing));
  warnings.push(`${path} already existed and was valid; preserved without rewriting.`);
  return existing;
}
async function planV01ToV02Migration(options) {
  const { fileSystem, clock } = options;
  const [frameworkText, ventureText] = await Promise.all([
    fileSystem.readText(HARNESS_FRAMEWORK_CONFIG_PATH),
    fileSystem.readText("config/venture.yaml")
  ]);
  if (frameworkText === null || ventureText === null) {
    throw new Error("config/framework.yaml and config/venture.yaml are required");
  }
  return buildPlan(fileSystem, frameworkText, ventureText, clock);
}
async function buildPlan(fileSystem, frameworkText, ventureText, clock) {
  const warnings = [
    "Launch mode defaults to validate_first with zero confidence until the launch router evaluates the brief.",
    "Legacy infrastructure booleans remain in config/venture.yaml as a deprecated compatibility view."
  ];
  const framework = parse2(frameworkText);
  if (!framework.framework) throw new Error("config/framework.yaml is missing framework");
  framework.framework.version = V02_VERSION;
  const migratedFramework = frameworkSchema.parse(framework);
  const migratedFrameworkContent = yaml2(migratedFramework);
  const migratedVenture = migrateVenture(parse2(ventureText));
  const launch = createDefaultLaunchConfig();
  launch.launch.progressive_commitment.specific_user_or_audience = migratedVenture.venture.target_market;
  launch.launch.progressive_commitment.primary_success_signal = migratedVenture.venture.outcomes.primary.success_signal;
  const mobile = createDefaultMobileConfig();
  mobile.mobile.app_store_connect.primary_language = migratedVenture.venture.locale;
  const providersPath = "config/providers.yaml";
  const existingProviders = await fileSystem.readText(providersPath);
  const providers = existingProviders === null ? yaml2(migrateProviders(migratedVenture.infrastructure, warnings)) : await preserveOrDefault(
    fileSystem,
    providersPath,
    providersSchema,
    createDefaultProvidersConfig(),
    warnings
  );
  const appliedAt = clock().toISOString();
  const lock = createHarnessLock({
    source: { kind: "release", ref: "v0.2.0" },
    managed_files: [
      createManagedFileLockEntry({
        path: HARNESS_FRAMEWORK_CONFIG_PATH,
        ownership: "harness",
        content: migratedFrameworkContent
      })
    ],
    applied_migrations: [
      {
        id: V01_TO_V02_MIGRATION_ID,
        from_version: V01_VERSION,
        to_version: V02_VERSION,
        applied_at: appliedAt
      }
    ]
  });
  return {
    id: V01_TO_V02_MIGRATION_ID,
    fromVersion: V01_VERSION,
    toVersion: V02_VERSION,
    warnings,
    changes: [
      { path: HARNESS_FRAMEWORK_CONFIG_PATH, content: migratedFrameworkContent },
      { path: "config/venture.yaml", content: yaml2(migratedVenture) },
      {
        path: "config/launch.yaml",
        content: await preserveOrDefault(
          fileSystem,
          "config/launch.yaml",
          launchSchema,
          launch,
          warnings
        )
      },
      { path: providersPath, content: providers },
      {
        path: "config/policies.yaml",
        content: await preserveOrDefault(
          fileSystem,
          "config/policies.yaml",
          policiesSchema,
          createDefaultPoliciesConfig(),
          warnings
        )
      },
      {
        path: "config/loops.yaml",
        content: await preserveOrDefault(
          fileSystem,
          "config/loops.yaml",
          loopsSchema,
          createDefaultLoopsConfig(),
          warnings
        )
      },
      {
        path: "config/mobile.yaml",
        content: await preserveOrDefault(
          fileSystem,
          "config/mobile.yaml",
          mobileSchema,
          mobile,
          warnings
        )
      },
      { path: "harness.lock", content: yaml2(lock) }
    ]
  };
}
async function migrateV01ToV02(options) {
  const clock = options.clock ?? (() => /* @__PURE__ */ new Date());
  let lockText;
  try {
    lockText = await options.fileSystem.readText("harness.lock");
  } catch (error) {
    return preparationFailure(
      error instanceof Error ? error.message : "could not read harness.lock",
      "lock_read_failed",
      "Restore a readable harness.lock or remove the corrupt file, then rerun.",
      clock
    );
  }
  if (lockText !== null) {
    try {
      const lock = parseHarnessLock(lockText);
      if (lock.harness_version === V02_VERSION) {
        return applyMigrationPlan(
          {
            id: V01_TO_V02_MIGRATION_ID,
            fromVersion: V01_VERSION,
            toVersion: V02_VERSION,
            warnings: [],
            changes: [{ path: "harness.lock", content: lockText }]
          },
          options
        );
      }
      if (lock.harness_version !== V01_VERSION) {
        return preparationFailure(
          `unsupported harness version ${lock.harness_version}`,
          "unsupported_source_version",
          "Run the migration chain for the locked source version instead.",
          clock
        );
      }
    } catch (error) {
      return preparationFailure(
        error instanceof Error ? error.message : "invalid harness.lock",
        "invalid_harness_lock",
        "Repair harness.lock from version control before migrating; no files were changed.",
        clock
      );
    }
  }
  try {
    const plan = await planV01ToV02Migration({ fileSystem: options.fileSystem, clock });
    return applyMigrationPlan(plan, options);
  } catch (error) {
    return preparationFailure(
      error instanceof Error ? error.message : "could not prepare migration",
      "migration_preparation_failed",
      "Fix the reported legacy or v0.2 config validation error and rerun; no files were changed.",
      clock
    );
  }
}

// lib/migrations/registry.ts
var PlanningFileSystem = class {
  constructor(base) {
    this.base = base;
  }
  base;
  staged = /* @__PURE__ */ new Map();
  async readText(path) {
    if (this.staged.has(path)) return this.staged.get(path) ?? null;
    return this.base.readText(path);
  }
  async writeAtomic(path, content) {
    this.staged.set(path, content);
  }
  async remove(path) {
    this.staged.set(path, null);
  }
};
function assertDefinition(definition2) {
  if (definition2.fromVersion === definition2.toVersion) {
    throw new Error(`migration ${definition2.id} cannot target its source version`);
  }
}
function assertPlannedChanges(definition2, plan) {
  const paths = plan.changes.map((change) => change.path);
  if (new Set(paths).size !== paths.length) {
    throw new Error(`migration ${definition2.id} returned duplicate paths`);
  }
  for (const path of paths) {
    if (path.startsWith("/") || path.split("/").includes("..")) {
      throw new Error(`migration ${definition2.id} returned an escaping path: ${path}`);
    }
  }
  if (paths.filter((path) => path === "harness.lock").length !== 1) {
    throw new Error(`migration ${definition2.id} must return exactly one harness.lock change`);
  }
}
var MigrationRegistry = class {
  definitions;
  constructor(definitions) {
    this.definitions = [...definitions];
    const identities = /* @__PURE__ */ new Set();
    for (const definition2 of this.definitions) {
      assertDefinition(definition2);
      if (identities.has(definition2.id)) {
        throw new Error(`duplicate migration id: ${definition2.id}`);
      }
      identities.add(definition2.id);
    }
  }
  resolveChain(fromVersion, toVersion) {
    if (fromVersion === toVersion) return [];
    const queue = [
      { version: fromVersion, chain: [] }
    ];
    const shortestDepth = /* @__PURE__ */ new Map([[fromVersion, 0]]);
    const matches2 = [];
    let matchDepth = null;
    while (queue.length > 0) {
      const current = queue.shift();
      if (matchDepth !== null && current.chain.length >= matchDepth) continue;
      const outgoing = this.definitions.filter((definition2) => definition2.fromVersion === current.version).sort((left, right) => left.id.localeCompare(right.id));
      for (const definition2 of outgoing) {
        const chain = [...current.chain, definition2];
        if (definition2.toVersion === toVersion) {
          matchDepth = chain.length;
          matches2.push(chain);
          continue;
        }
        const previousDepth = shortestDepth.get(definition2.toVersion);
        if (previousDepth !== void 0 && previousDepth < chain.length) continue;
        shortestDepth.set(definition2.toVersion, chain.length);
        queue.push({ version: definition2.toVersion, chain });
      }
    }
    const shortest = matches2.filter((chain) => chain.length === matchDepth);
    if (shortest.length === 0) {
      throw new Error(`no registered migration chain from ${fromVersion} to ${toVersion}`);
    }
    if (shortest.length > 1) {
      throw new Error(
        `ambiguous migration chain from ${fromVersion} to ${toVersion}: ${shortest.map((chain) => chain.map((migration) => migration.id).join(" -> ")).join(" | ")}`
      );
    }
    return shortest[0];
  }
  async planChain(options) {
    const migrations = this.resolveChain(options.fromVersion, options.toVersion);
    const stagedFileSystem = new PlanningFileSystem(options.fileSystem);
    const clock = options.clock ?? (() => /* @__PURE__ */ new Date());
    const plans = [];
    for (const migration of migrations) {
      const plan = await migration.plan({ fileSystem: stagedFileSystem, clock });
      if (plan.id !== migration.id || plan.fromVersion !== migration.fromVersion || plan.toVersion !== migration.toVersion) {
        throw new Error(`migration ${migration.id} returned mismatched plan metadata`);
      }
      assertPlannedChanges(migration, plan);
      plans.push(plan);
      for (const change of plan.changes) {
        await stagedFileSystem.writeAtomic(change.path, change.content);
      }
    }
    return {
      fromVersion: options.fromVersion,
      toVersion: options.toVersion,
      migrations,
      plans,
      stagedFileSystem
    };
  }
};
var V01_TO_V02_MIGRATION = {
  id: V01_TO_V02_MIGRATION_ID,
  fromVersion: V01_VERSION,
  toVersion: V02_VERSION,
  plan: planV01ToV02Migration
};
var defaultMigrationRegistry = new MigrationRegistry([V01_TO_V02_MIGRATION]);

// packages/core/dist/index.js
var CREDENTIAL_VALUE_PATTERNS2 = [
  /\bwhsec_[a-z0-9_-]{8,}/iu,
  /\b(?:sk|rk|pk|atk)_(?:live|test)?_?[a-z0-9_-]{8,}/iu,
  /\bsk-[a-z0-9_-]{16,}/iu,
  /\bxkeysib-[a-z0-9_-]{12,}/iu,
  /\bAIza[a-z0-9_-]{30,}/iu,
  /\b(?:gh[pousr]_[a-z0-9]{20,}|github_pat_[a-z0-9_]{20,})\b/iu,
  /\bxox[baprs]-[a-z0-9-]{10,}/iu,
  /\b(?:AKIA|ASIA)[A-Z0-9]{16}\b/u,
  /\bbearer\s+[a-z0-9._~+/=-]{8,}/iu,
  /-----BEGIN [A-Z ]*PRIVATE KEY-----/u,
  /\beyJ[a-z0-9_-]{8,}\.[a-z0-9_-]{8,}\.[a-z0-9_-]{8,}\b/iu,
  /[?&](?:access_token|api_key|token|secret)=[^&\s]{6,}/iu,
  /\b(?:(?:vh|credential)[_-])canary[_-][a-z0-9_-]{6,}/iu
];
function credentialFieldWords2(field) {
  return field.replace(/([a-z0-9])([A-Z])/gu, "$1_$2").toLowerCase().split(/[^a-z0-9]+/u).filter(Boolean);
}
function secretBearingField2(field) {
  const words = credentialFieldWords2(field);
  if (["secret", "password", "token", "credential", "authorization"].some((word) => words.includes(word))) {
    return true;
  }
  const joined = words.join("");
  return ["apikey", "privatekey", "signingkey"].some((marker) => joined.includes(marker));
}
function findCredentialMaterial2(value, options = {}) {
  const canaries = [...options.canaries ?? []].filter(Boolean);
  const referenceKeys = new Set(options.allowedCredentialReferenceKeys ?? []);
  const visited = /* @__PURE__ */ new WeakSet();
  const inspect = (candidate, path) => {
    if (typeof candidate === "string") {
      if (canaries.some((canary) => candidate.includes(canary))) {
        return { kind: "registered_canary", path };
      }
      if (CREDENTIAL_VALUE_PATTERNS2.some((pattern) => pattern.test(candidate))) {
        return { kind: "credential_pattern", path };
      }
      return null;
    }
    if (candidate === null || typeof candidate === "boolean")
      return null;
    if (typeof candidate === "number") {
      return Number.isFinite(candidate) ? null : { kind: "non_json_value", path };
    }
    if (typeof candidate !== "object")
      return { kind: "non_json_value", path };
    if (visited.has(candidate))
      return { kind: "non_json_value", path };
    visited.add(candidate);
    if (Array.isArray(candidate)) {
      for (const [index, entry] of candidate.entries()) {
        const finding = inspect(entry, `${path}[${index}]`);
        if (finding)
          return finding;
      }
      return null;
    }
    const prototype = Object.getPrototypeOf(candidate);
    if (prototype !== Object.prototype && prototype !== null) {
      return { kind: "non_json_value", path };
    }
    for (const [field, entry] of Object.entries(candidate)) {
      const childPath = `${path}.${field}`;
      if (referenceKeys.has(field)) {
        if (typeof entry !== "string" || !/^cred:\/\/[A-Za-z0-9][A-Za-z0-9/_:.-]*$/u.test(entry)) {
          return { kind: "invalid_credential_reference", path: childPath };
        }
      } else if (secretBearingField2(field)) {
        return { kind: "secret_bearing_field", path: childPath };
      }
      const finding = inspect(entry, childPath);
      if (finding)
        return finding;
    }
    return null;
  };
  return inspect(value, "$");
}
function assertNonEmpty(value, field) {
  const normalized = value.trim();
  if (!normalized)
    throw new Error(`${field} must not be empty`);
  return normalized;
}
function tenantKey(tenant) {
  const organizationId = assertNonEmpty(tenant.organizationId, "organizationId");
  const ventureId2 = assertNonEmpty(tenant.ventureId, "ventureId");
  if (organizationId !== tenant.organizationId) {
    throw new Error("organizationId must not contain leading or trailing whitespace");
  }
  if (ventureId2 !== tenant.ventureId) {
    throw new Error("ventureId must not contain leading or trailing whitespace");
  }
  const tenantIdPattern = /^[A-Za-z0-9_][A-Za-z0-9._-]*$/;
  if (!tenantIdPattern.test(organizationId)) {
    throw new Error("organizationId must be a canonical tenant identifier");
  }
  if (!tenantIdPattern.test(ventureId2)) {
    throw new Error("ventureId must be a canonical tenant identifier");
  }
  return `${organizationId}:${ventureId2}`;
}
function canonicalCommandId(value) {
  const commandId = value.trim().toLowerCase();
  if (!/^[a-z][a-z0-9-]*(?:\.[a-z][a-z0-9-]*)+$/.test(commandId)) {
    throw new Error(`Invalid command id: ${value}`);
  }
  return commandId;
}
function sortJson(value) {
  if (Array.isArray(value))
    return value.map(sortJson);
  if (value && typeof value === "object") {
    return Object.fromEntries(Object.entries(value).sort(([left], [right]) => left.localeCompare(right)).map(([key, child]) => [key, sortJson(child)]));
  }
  return value;
}
function stableJson(value) {
  return JSON.stringify(sortJson(value));
}
function assertCredentialFree(value, path = "value", canaries = []) {
  const finding = findCredentialMaterial2(value, { canaries });
  if (finding)
    throw new Error(`credential-like material is forbidden at ${path}${finding.path.slice(1)}`);
}

// lib/runtime/build-agent-host.ts
var BuildAgentHostError = class extends Error {
  constructor(code, message) {
    super(message);
    this.code = code;
    this.name = "BuildAgentHostError";
  }
  code;
};

// lib/runtime/codex-cli-build-agent.ts
import { resolve as resolve12 } from "node:path";
var buildAgentCheckSchema = external_exports.object({
  command: external_exports.string().min(1).max(500),
  status: external_exports.enum(["passed", "failed", "skipped"]),
  evidence: external_exports.string().max(2e3).nullable()
}).strict();
var buildAgentArtifactRoleSchema = external_exports.enum([
  "repository_scaffold",
  "managed_manifest",
  "design_record",
  "design_implementation",
  "core_journey",
  "affected_test",
  "event_contract",
  "event_instrumentation",
  "validation_record",
  "concierge_operations",
  "usage_proof"
]);
var buildAgentCompletionSchema = external_exports.object({
  outcome: external_exports.enum(["changed", "already_compliant"]),
  artifacts: external_exports.array(
    external_exports.object({
      path: artifactReferenceSchema,
      role: buildAgentArtifactRoleSchema
    }).strict()
  ).min(1).max(100).refine(
    (artifacts) => new Set(artifacts.map(({ path, role }) => `${role}:${path}`)).size === artifacts.length,
    "completion artifacts must be unique by role and path"
  ),
  validator: external_exports.object({
    check_command: external_exports.string().min(1).max(500)
  }).strict()
}).strict();
var buildAgentFinalResultSchema = external_exports.object({
  status: external_exports.enum(["completed", "blocked"]),
  summary: external_exports.string().min(1).max(4e3),
  changed_files: external_exports.array(artifactReferenceSchema).max(500),
  checks: external_exports.array(buildAgentCheckSchema).max(100),
  limitations: external_exports.array(external_exports.string().min(1).max(2e3)).max(100),
  completion: buildAgentCompletionSchema.nullable()
}).strict().superRefine((result2, ctx) => {
  if (result2.status === "blocked") return;
  if (!result2.completion) {
    ctx.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["completion"],
      message: "completed tasks require typed completion evidence"
    });
    return;
  }
  if (result2.completion.outcome === "changed" && result2.changed_files.length === 0) {
    ctx.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["changed_files"],
      message: "changed completion requires at least one reported file"
    });
  }
  if (result2.completion.outcome === "already_compliant" && result2.changed_files.length > 0) {
    ctx.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["changed_files"],
      message: "already_compliant completion cannot report changed files"
    });
  }
  const validator = result2.checks.find(
    ({ command }) => command === result2.completion?.validator.check_command
  );
  if (validator?.status !== "passed" || !validator.evidence?.trim()) {
    ctx.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["completion", "validator", "check_command"],
      message: "completion validator must reference a passed check with non-empty evidence"
    });
  }
});
var FORBIDDEN_CONTEXT_KEY = /^(access_token|refresh_token|id_token|api_key|secret|client_secret|private_key|password|credential_value)$/i;
var CODEX_EXEC_ARGS = [
  "exec",
  "--sandbox",
  "workspace-write",
  "--ephemeral",
  "--ignore-user-config",
  "--json"
];
var CODEX_ENVIRONMENT_KEYS = [
  "NODE_ENV",
  "PATH",
  "HOME",
  "USERPROFILE",
  "CODEX_HOME",
  "XDG_CONFIG_HOME",
  "TMPDIR",
  "TMP",
  "TEMP",
  "LANG",
  "LC_ALL",
  "TERM",
  "NO_COLOR",
  "CI",
  "SystemRoot",
  "PATHEXT"
];
var PRODUCT_COMMAND_ENVIRONMENT_KEYS = [
  "PATH",
  "TMPDIR",
  "TMP",
  "TEMP",
  "LANG",
  "LC_ALL",
  "TERM",
  "NO_COLOR",
  "CI",
  "SystemRoot",
  "PATHEXT"
];
function codexBuildAgentEnvironment(source) {
  return {
    NODE_ENV: source.NODE_ENV ?? "production",
    ...Object.fromEntries(
      CODEX_ENVIRONMENT_KEYS.flatMap(
        (key) => source[key] === void 0 ? [] : [[key, source[key]]]
      )
    )
  };
}
function productCommandEnvironment(source, isolatedHome) {
  const home = resolve12(isolatedHome);
  return {
    NODE_ENV: source.NODE_ENV ?? "production",
    ...Object.fromEntries(
      PRODUCT_COMMAND_ENVIRONMENT_KEYS.flatMap(
        (key) => source[key] === void 0 ? [] : [[key, source[key]]]
      )
    ),
    HOME: home,
    USERPROFILE: home,
    XDG_CONFIG_HOME: resolve12(home, ".config"),
    npm_config_userconfig: resolve12(home, ".npmrc"),
    NPM_CONFIG_USERCONFIG: resolve12(home, ".npmrc")
  };
}
function assertCredentialFree2(value, path = "context") {
  if (typeof value === "string") {
    if (looksLikeCredentialValue(value)) {
      throw new BuildAgentHostError(
        "credential_material",
        `${path} contains credential material; the build-agent boundary accepts no secret values.`
      );
    }
    return;
  }
  if (Array.isArray(value)) {
    value.forEach((entry, index) => assertCredentialFree2(entry, `${path}[${index}]`));
    return;
  }
  if (!value || typeof value !== "object") return;
  for (const [key, entry] of Object.entries(value)) {
    if (FORBIDDEN_CONTEXT_KEY.test(key)) {
      throw new BuildAgentHostError(
        "credential_material",
        `${path}.${key} is a forbidden credential field; pass no secret values to a build agent.`
      );
    }
    assertCredentialFree2(entry, `${path}.${key}`);
  }
}
function promptFor(request2, redactor) {
  assertCredentialFree2(request2.context);
  const context2 = redactor.redact(request2.context);
  return [
    "Execute one bounded Venture Harness product-build task in the current repository.",
    "Read AGENTS.md, PROJECT.md, docs/product/PRODUCT_TRUTH.md, the typed config, and only the skill/docs needed for this task.",
    "Work only inside the repository. Do not deploy, publish, send, charge, change DNS, create provider resources, commit, push, or expose credentials.",
    "Use deterministic code and existing scripts when they are sufficient. Preserve venture-owned work and label samples, prototypes, and unverified state honestly.",
    "Never read or print credential values. Repository config may contain only cred:// references.",
    `Run ID: ${request2.runId}`,
    `Node ID: ${request2.nodeId}`,
    `Purpose: ${request2.purpose}`,
    "Task instructions:",
    request2.instructions,
    "Bounded JSON context:",
    JSON.stringify(context2, null, 2),
    "When finished, return exactly one JSON object as the final response, with no Markdown fence or surrounding prose:",
    '{"status":"completed|blocked","summary":"concise factual result","changed_files":["repo/relative/path"],"checks":[{"command":"literal direct command","status":"passed|failed|skipped","evidence":"concise observed evidence or null"}],"limitations":["genuine limitation"],"completion":{"outcome":"changed|already_compliant","artifacts":[{"path":"repo/relative/path","role":"repository_scaffold|managed_manifest|design_record|design_implementation|core_journey|affected_test|event_contract|event_instrumentation|validation_record|concierge_operations|usage_proof"}],"validator":{"check_command":"exact command from checks"}}}',
    "Use status=blocked and completion=null when the task is not actually complete. Use outcome=changed only for files whose content changed during this task. Use outcome=already_compliant only when no repository file changed and the named direct validator proves the requested completion condition. List only repository-relative files and artifacts that exist at the end. Do not include raw file contents, secrets, tokens, personal data, or provider response bodies."
  ].join("\n\n");
}
function asRecord(value) {
  return value !== null && typeof value === "object" && !Array.isArray(value) ? value : null;
}
function assistantText(event) {
  const record3 = asRecord(event);
  if (!record3) return null;
  if (record3.type === "item.completed") {
    const item = asRecord(record3.item);
    if (item?.type === "agent_message" && typeof item.text === "string") return item.text;
  }
  if (record3.type === "turn.completed" && typeof record3.final_output === "string") {
    return record3.final_output;
  }
  if (record3.type === "result" && typeof record3.result === "string") return record3.result;
  return null;
}
function usageFrom(event) {
  const record3 = asRecord(event);
  if (record3?.type !== "turn.completed") return void 0;
  const usage = asRecord(record3.usage);
  if (!usage) return void 0;
  const inputTokens = usage.input_tokens;
  const cachedInputTokens = usage.cached_input_tokens;
  const outputTokens = usage.output_tokens;
  if (typeof inputTokens !== "number" || typeof cachedInputTokens !== "number" || typeof outputTokens !== "number") {
    return void 0;
  }
  return { inputTokens, cachedInputTokens, outputTokens };
}
function parseJsonLines(stdout) {
  const lines = stdout.split(/\r?\n/).filter((line) => line.trim().length > 0);
  const finalTexts = [];
  const eventTypes = /* @__PURE__ */ new Set();
  let usage;
  for (const [index, line] of lines.entries()) {
    let event;
    try {
      event = JSON.parse(line);
    } catch {
      throw new BuildAgentHostError(
        "invalid_jsonl",
        `Codex JSONL line ${index + 1} was not valid JSON; no result was accepted.`
      );
    }
    const type = asRecord(event)?.type;
    if (typeof type === "string") eventTypes.add(type);
    const text2 = assistantText(event);
    if (text2) finalTexts.push(text2);
    usage = usageFrom(event) ?? usage;
  }
  const finalText = finalTexts.at(-1);
  if (!finalText) {
    throw new BuildAgentHostError(
      "missing_final_result",
      "Codex JSONL contained no final structured agent result; no task result was accepted."
    );
  }
  return { finalText, eventTypes: [...eventTypes].sort(), usage };
}
function compactVersion2(value, redactor) {
  const line = redactor.redactText(value).split(/\r?\n/).find((entry) => entry.trim().length > 0);
  return line ? line.trim().slice(0, 200) : null;
}
var CodexCliBuildAgentHost = class {
  id = "codex_cli";
  rootDir;
  runner;
  redactor;
  binary;
  inspection;
  constructor(options) {
    this.rootDir = resolve12(options.rootDir);
    this.runner = options.runner;
    this.redactor = options.redactor ?? new Redactor();
    this.binary = options.binary ?? "codex";
  }
  inspect() {
    this.inspection ??= this.inspectOnce();
    return this.inspection;
  }
  async inspectOnce() {
    try {
      const result2 = await this.runner.run({
        command: this.binary,
        args: ["--version"],
        cwd: this.rootDir
      });
      if (result2.exitCode === 0) {
        const login = await this.runner.run({
          command: this.binary,
          args: ["login", "status"],
          cwd: this.rootDir
        });
        const loginStatus = this.redactor.redactText(`${login.stdout}
${login.stderr}`);
        const billingMode = login.exitCode !== 0 ? "unknown" : /logged in using chatgpt|chatgpt (?:account|subscription)/iu.test(loginStatus) ? "chatgpt_subscription" : /api[ -]?key|openai_api_key/iu.test(loginStatus) ? "api_key_metered" : "unknown";
        return {
          host: this.id,
          status: "available",
          version: compactVersion2(result2.stdout || result2.stderr, this.redactor),
          billingMode,
          billingEvidence: "codex_login_status",
          nextAction: billingMode === "chatgpt_subscription" ? null : "Run codex login with a ChatGPT subscription account; API-key or unknown billing cannot satisfy the founder non-metered build policy."
        };
      }
      return {
        host: this.id,
        status: "unavailable",
        version: null,
        billingMode: "unknown",
        billingEvidence: null,
        nextAction: `codex exists but its version check exited ${result2.exitCode}; authenticate or repair the Codex CLI before launch apply.`
      };
    } catch (error) {
      const missing = error.code === "ENOENT";
      return {
        host: this.id,
        status: missing ? "missing" : "unavailable",
        version: null,
        billingMode: "unknown",
        billingEvidence: null,
        nextAction: missing ? "Install and authenticate the Codex CLI, then rerun the same launch command." : `Codex CLI inspection failed: ${this.redactor.redactText(
          error instanceof Error ? error.message : String(error)
        )}`
      };
    }
  }
  async run(request2) {
    const inspection = await this.inspect();
    if (inspection.status !== "available") {
      throw new BuildAgentHostError(
        "host_unavailable",
        inspection.nextAction ?? "Codex CLI is unavailable; no product task was run."
      );
    }
    const prompt = promptFor(request2, this.redactor);
    const result2 = await this.runner.run({
      command: this.binary,
      args: [...CODEX_EXEC_ARGS, "-C", this.rootDir, "-"],
      cwd: this.rootDir,
      stdin: prompt,
      sensitiveStdin: true,
      signal: request2.signal
    });
    if (result2.exitCode !== 0) {
      const detail = this.redactor.redactText(result2.stderr || result2.stdout).trim().slice(0, 2e3);
      throw new BuildAgentHostError(
        "process_failed",
        `Codex build task exited ${result2.exitCode}${detail ? `: ${detail}` : ""}`
      );
    }
    const parsedJsonLines = parseJsonLines(result2.stdout);
    let finalValue;
    try {
      finalValue = JSON.parse(parsedJsonLines.finalText);
    } catch {
      throw new BuildAgentHostError(
        "invalid_final_result",
        "Codex final agent message was not the required JSON object; no task result was accepted."
      );
    }
    const parsed = buildAgentFinalResultSchema.safeParse(this.redactor.redact(finalValue));
    if (!parsed.success) {
      throw new BuildAgentHostError(
        "invalid_final_result",
        `Codex final result did not match the build-agent contract: ${parsed.error.issues.map((issue) => `${issue.path.join(".") || "result"}: ${issue.message}`).join("; ")}`
      );
    }
    return {
      status: parsed.data.status,
      summary: parsed.data.summary,
      changedFiles: parsed.data.changed_files,
      checks: parsed.data.checks,
      limitations: parsed.data.limitations,
      eventTypes: parsedJsonLines.eventTypes,
      completion: parsed.data.completion ? {
        outcome: parsed.data.completion.outcome,
        artifacts: parsed.data.completion.artifacts,
        validator: {
          checkCommand: parsed.data.completion.validator.check_command
        }
      } : null,
      usage: parsedJsonLines.usage
    };
  }
};

// lib/runtime/checkpoint-evidence.ts
import { existsSync as existsSync7, lstatSync as lstatSync5, readFileSync as readFileSync9, realpathSync as realpathSync5 } from "node:fs";
import { relative as relative9, resolve as resolve13, sep as sep9 } from "node:path";
function inside4(rootDir, artifact) {
  const root = realpathSync5(rootDir);
  const target = resolve13(root, artifact);
  const lexicalRelative = relative9(root, target);
  if (lexicalRelative === "" || lexicalRelative === ".." || lexicalRelative.startsWith(`..${sep9}`) || lexicalRelative.startsWith(sep9)) {
    throw new Error(`Checkpoint evidence escapes the venture root: ${artifact}`);
  }
  const realTarget = realpathSync5(target);
  const realRelative = relative9(root, realTarget);
  if (realRelative === "" || realRelative === ".." || realRelative.startsWith(`..${sep9}`) || realRelative.startsWith(sep9)) {
    throw new Error(`Checkpoint evidence resolves outside the venture root: ${artifact}`);
  }
  return target;
}
function loadRepositoryCheckpointEvidence(options) {
  const reference = artifactReferenceSchema.parse(options.evidenceArtifact);
  const prefix = `reports/launch/${options.runId}/checkpoints/`;
  if (!reference.startsWith(prefix) || !reference.endsWith(".json")) {
    throw new Error(`Checkpoint evidence must be a JSON file under ${prefix}.`);
  }
  const lexicalTarget = resolve13(options.rootDir, reference);
  if (!existsSync7(lexicalTarget)) {
    throw new Error(`Checkpoint evidence file does not exist: ${reference}.`);
  }
  const target = inside4(options.rootDir, reference);
  const stat2 = lstatSync5(target);
  const maxBytes = options.maxBytes ?? 1e6;
  if (stat2.isSymbolicLink() || !stat2.isFile()) {
    throw new Error(`Checkpoint evidence must be a regular non-symlink file: ${reference}.`);
  }
  if (stat2.size === 0 || stat2.size > maxBytes) {
    throw new Error(
      `Checkpoint evidence size must be between 1 and ${maxBytes} bytes: ${reference}.`
    );
  }
  const raw = readFileSync9(target, "utf8");
  const redactor = options.redactor ?? new Redactor();
  if (redactor.redactText(raw) !== raw) {
    throw new Error("Checkpoint evidence contains registered credential material.");
  }
  let decoded2;
  try {
    decoded2 = JSON.parse(raw);
  } catch (error) {
    throw new Error(
      `Checkpoint evidence is not valid JSON: ${error instanceof Error ? error.message : String(error)}`
    );
  }
  return authorizationCheckpointEvidenceSchema.parse(decoded2);
}
function createRepositoryCheckpointEvidenceVerifier(options) {
  return (context2) => {
    let artifact;
    try {
      artifact = loadRepositoryCheckpointEvidence({
        ...options,
        evidenceArtifact: context2.evidenceArtifact,
        runId: context2.runId
      });
    } catch (error) {
      return { ok: false, message: error instanceof Error ? error.message : String(error) };
    }
    if (artifact.run_id !== context2.runId || artifact.node_id !== context2.node.id || artifact.effect !== context2.effect || artifact.operation_id !== context2.operationId || artifact.approved_by !== context2.approvedBy || artifact.approved_at !== context2.approvedAt) {
      return {
        ok: false,
        message: "Checkpoint evidence run, node, effect, operation, approver, or approval time does not match the requested grant."
      };
    }
    return { ok: true };
  };
}

// lib/runtime/launch-report.ts
import { randomBytes as randomBytes3 } from "node:crypto";
import { mkdir as mkdir4, open as open2, readFile as readFile3, rename as rename3 } from "node:fs/promises";
import { join as join5, resolve as resolve14 } from "node:path";
var EMAIL = /\b[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}\b/gi;
var PHONE = /(?<![\w])\+\d(?:[\d .()-]{7,}\d)/g;
var PRIVATE_KEY = /-----BEGIN [^-\n]*PRIVATE KEY-----[\s\S]*?-----END [^-\n]*PRIVATE KEY-----/g;
var QUERY_VALUE = /([?&][a-z0-9_.~-]+)=([^&\s#]+)/gi;
function sanitizeText(value, redactor) {
  return redactor.redactText(value).replace(PRIVATE_KEY, "[REDACTED]").replace(EMAIL, "[REDACTED PII]").replace(PHONE, "[REDACTED PII]").replace(QUERY_VALUE, "$1=[REDACTED]");
}
function outcomeLine(outcome) {
  return `${outcome.id}: ${outcome.state}; evidence ${outcome.evidenceRef ?? "not recorded"}`;
}
function objectValue(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : null;
}
function stringArray(value) {
  return Array.isArray(value) ? value.filter((item) => typeof item === "string") : [];
}
function providerFacts(record3) {
  const output = objectValue(record3.output);
  if (output?.state !== "verified") {
    return { environments: [], capabilities: [], resourceRefs: [] };
  }
  const resourceRefs = stringArray(output.resourceRefs);
  const valueFor = (type) => resourceRefs.find((reference) => reference.startsWith(`${type}=`))?.slice(type.length + 1);
  return {
    environments: stringArray(output.environments),
    capabilities: stringArray(output.capabilities),
    resourceRefs,
    accountId: valueFor("account_id"),
    teamId: valueFor("team_id"),
    region: valueFor("region")
  };
}
function productOutcomeLine(record3) {
  const output = objectValue(record3.output);
  const summary = typeof output?.summary === "string" ? output.summary : null;
  const changedFiles = stringArray(output?.changedFiles);
  return summary ? `${record3.definition.id}: ${summary}${changedFiles.length > 0 ? `; files ${changedFiles.join(", ")}` : ""}` : outcomeLine({
    id: record3.definition.id,
    capability: record3.definition.capability,
    state: record3.state,
    evidenceRef: record3.evidenceArtifact,
    effectVerified: record3.effectVerified
  });
}
function qualityOutcomeLine(record3) {
  const output = objectValue(record3.output);
  const command = stringArray(output?.command);
  const exitCode = typeof output?.exitCode === "number" ? output.exitCode : null;
  return command.length > 0 && exitCode !== null ? `${command.join(" ")}: ${exitCode === 0 ? "PASS" : `FAIL (${exitCode})`}; evidence ${record3.evidenceArtifact ?? "not recorded"}` : outcomeLine({
    id: record3.definition.id,
    capability: record3.definition.capability,
    state: record3.state,
    evidenceRef: record3.evidenceArtifact,
    effectVerified: record3.effectVerified
  });
}
function providerOutcomeLine(outcome) {
  const resources = outcome.resourceRefs?.length ? `; resources ${outcome.resourceRefs.join(", ")}` : "; resource identifiers not returned by the verified read-back";
  const scope = [
    outcome.accountId ? `account ${outcome.accountId}` : null,
    outcome.teamId ? `team ${outcome.teamId}` : null,
    outcome.region ? `region ${outcome.region}` : null
  ].filter(Boolean).join("; ");
  return `${outcome.provider} / ${outcome.capability}: ${outcome.lifecycleState}; verified ${outcome.verified ? "yes" : "no"}; ${scope || "account/team/region not recorded"}${resources}; evidence ${outcome.evidenceRef ?? "not recorded"}`;
}
function createLaunchReportInputFromRun(input) {
  const providerByNode = input.providerByNode ?? {};
  const nodes = Object.values(input.state.nodes).map((record3) => ({
    id: record3.definition.id,
    capability: record3.definition.capability,
    state: record3.state,
    provider: providerByNode[record3.definition.id],
    evidenceRef: record3.evidenceArtifact,
    effectVerified: record3.effectVerified,
    errorCode: record3.error?.code
  }));
  const providers = nodes.filter((node) => input.state.nodes[node.id].definition.kind === "provider").map((node) => {
    const facts = providerFacts(input.state.nodes[node.id]);
    const metadata = input.providerMetadata?.[node.provider ?? ""];
    return {
      provider: node.provider ?? "unmapped-provider",
      capability: facts.capabilities.length > 0 ? facts.capabilities.join(", ") : node.capability,
      lifecycleState: node.state === "succeeded" && node.effectVerified ? "verified" : node.state,
      environment: facts.environments.join(", ") || void 0,
      accountId: metadata?.accountId ?? facts.accountId,
      teamId: metadata?.teamId ?? facts.teamId,
      region: metadata?.region ?? facts.region,
      resourceRefs: facts.resourceRefs,
      evidenceRef: node.evidenceRef,
      verified: node.state === "succeeded" && node.effectVerified
    };
  });
  const manualActions = Object.values(input.state.nodes).filter(
    (record3) => record3.definition.kind === "manual_action" || record3.error?.code === "provider_manual_action_required" || record3.state === "waiting_for_auth" || record3.state === "waiting_for_external_action"
  ).map((record3) => {
    const contract = launchManualActionContracts[record3.definition.id];
    const evidencePath = contract?.evidencePath.replace("<run-id>", input.state.runId);
    return {
      nodeId: record3.definition.id,
      resolved: record3.state === "succeeded" || record3.state === "compensated",
      action: contract?.effect ?? record3.waiting?.reason ?? record3.definition.purpose,
      requiredFields: contract?.requiredFields ?? [],
      risk: contract?.risk ?? record3.definition.risk,
      evidenceNeeded: contract?.completionEvidence ?? [record3.definition.completion.description],
      resumeCommand: record3.definition.kind === "manual_action" ? `vh resume ${input.state.runId} --manual ${record3.definition.id} --evidence ${evidencePath ?? "<repository-relative-artifact>"}` : `vh explain ${input.state.runId} ${record3.definition.id}`
    };
  });
  const unverifiedProviders = providers.filter(({ verified }) => !verified).map(
    ({ provider, capability, lifecycleState }) => `${provider} / ${capability} remains ${lifecycleState}; no verified provider state was inferred.`
  );
  const waiting = manualActions.filter(({ resolved }) => !resolved);
  const failedNodes = nodes.filter(
    ({ state }) => state === "failed_terminal" || state === "failed_retryable"
  );
  const generatedNextCommands = waiting.length > 0 ? waiting.map(({ resumeCommand }) => resumeCommand) : failedNodes.length > 0 ? failedNodes.map(({ id }) => `vh explain ${input.state.runId} ${id}`) : [`vh status ${input.state.runId}`];
  const records = input.state.nodes;
  const productIds = [
    "prepare-repository",
    "define-validation-gate",
    "prepare-concierge-operations",
    "design-direction",
    "build-core-journey",
    "configure-event-pack",
    "define-usage-proof"
  ];
  const generatedSections = {
    whatBuilt: productIds.filter((id) => records[id]).map((id) => productOutcomeLine(records[id])),
    repository: providers.filter(({ provider }) => provider === "github").map(providerOutcomeLine),
    deploymentsAndBuilds: providers.filter(({ provider }) => ["vercel", "eas", "app_store_connect"].includes(provider)).map(providerOutcomeLine),
    commerce: [
      `Payment provider ${input.launch.paymentProvider ?? "not recorded"}; entitlement source ${input.launch.entitlementSource ?? "not recorded"}.`,
      ...providers.filter(({ provider }) => ["stripe", "revenuecat"].includes(provider)).map(providerOutcomeLine)
    ],
    email: providers.filter(({ provider }) => provider === "brevo").map(providerOutcomeLine),
    analyticsAndSearch: [
      `Event packs ${input.launch.activeEventPacks?.join(", ") || "not recorded"}; consent ${input.launch.consentMode ?? "not recorded"}.`,
      ...records["configure-event-pack"] ? [productOutcomeLine(records["configure-event-pack"])] : [],
      ...providers.filter(({ provider }) => ["google", "bing"].includes(provider)).map(providerOutcomeLine)
    ],
    asoAndTestflight: providers.filter(({ provider }) => ["app_store_connect", "eas", "revenuecat"].includes(provider)).map(providerOutcomeLine),
    checksRun: ["verify-local", "verify-launch"].filter((id) => records[id]).map((id) => qualityOutcomeLine(records[id])),
    scheduledLoops: input.brief.scheduledLearning ? [
      "Scheduled learning was requested; no external schedule is inferred by this launch report."
    ] : [],
    nextReviews: []
  };
  const sections = Object.fromEntries(
    Object.entries(generatedSections).map(([key, values]) => [
      key,
      [...values ?? [], ...input.sections?.[key] ?? []]
    ])
  );
  return {
    generatedAt: input.generatedAt,
    run: { id: input.state.runId, status: input.state.status },
    brief: input.brief,
    launch: input.launch,
    authorization: input.authorization,
    nodes,
    providers,
    manualActions,
    credentialReferences: input.credentialReferences,
    limitations: [...unverifiedProviders, ...input.limitations ?? []],
    nextCommands: [...generatedNextCommands, ...input.nextCommands ?? []],
    sections
  };
}
function sortedUnique(values, redactor) {
  return [...new Set(values.map((value) => sanitizeText(value, redactor)))].sort();
}
function overallState(input) {
  if (input.run.status === "failed" || input.nodes.some(({ state }) => state === "failed_terminal")) {
    return "failed";
  }
  if (input.run.status === "waiting" || input.nodes.some(
    ({ state }) => state === "waiting_for_approval" || state === "waiting_for_manual_action" || state === "waiting_for_auth" || state === "waiting_for_external_action"
  )) {
    return "waiting";
  }
  if (input.run.status === "succeeded" && input.nodes.every(({ state }) => ["succeeded", "skipped", "compensated"].includes(state))) {
    return "succeeded";
  }
  return "degraded";
}
function isGenuinelyUnresolved(action, nodes) {
  if (action.resolved) return false;
  const node = nodes.get(action.nodeId);
  if (!node) return false;
  return node.state === "waiting_for_approval" || node.state === "waiting_for_manual_action" || node.state === "waiting_for_auth" || node.state === "waiting_for_external_action" || node.errorCode === "provider_manual_action_required";
}
function normalizeSections(sections, redactor) {
  const normalize = (values) => sortedUnique(values ?? [], redactor);
  return {
    whatBuilt: normalize(sections?.whatBuilt),
    repository: normalize(sections?.repository),
    deploymentsAndBuilds: normalize(sections?.deploymentsAndBuilds),
    commerce: normalize(sections?.commerce),
    email: normalize(sections?.email),
    analyticsAndSearch: normalize(sections?.analyticsAndSearch),
    asoAndTestflight: normalize(sections?.asoAndTestflight),
    checksRun: normalize(sections?.checksRun),
    scheduledLoops: normalize(sections?.scheduledLoops),
    nextReviews: normalize(sections?.nextReviews)
  };
}
function cleanCredentialReference(input, redactor) {
  assertCredentialRef(input.ref);
  return {
    ref: input.ref,
    provider: sanitizeText(input.provider, redactor),
    status: sanitizeText(input.status, redactor),
    scopes: sortedUnique(input.scopes, redactor),
    expiresAt: input.expiresAt,
    accountId: input.accountId ? sanitizeText(input.accountId, redactor) : void 0
  };
}
function lineItems(values, empty) {
  return values.length > 0 ? values.map((value) => `- ${value}`).join("\n") : `_${empty}_`;
}
function renderMarkdown(document2) {
  const authorization = document2.authorization ? `${document2.authorization.profile}; approval ${document2.authorization.approvalRef}; expires ${document2.authorization.expiresAt}; ${document2.authorization.spendScope === "reviewed_direct_provider_operations_only" ? "reviewed direct-operation ceiling" : "ceiling"} ${document2.authorization.spendCeiling.amount} ${document2.authorization.spendCeiling.currency}${document2.authorization.spendScope === "reviewed_direct_provider_operations_only" ? "; ongoing account-plan usage excluded" : ""}` : "No session authorization metadata was supplied";
  const nodeLines = document2.nodes.map(
    (node) => `${node.id}: ${node.state}; capability ${node.capability}; evidence ${node.evidenceRef ?? "not recorded"}`
  );
  const providerLines = document2.providers.map(
    (provider) => `${provider.provider} / ${provider.capability}: ${provider.lifecycleState}; verified ${provider.verified ? "yes" : "no"}; account ${provider.accountId ?? "not recorded"}; team ${provider.teamId ?? "not recorded"}; region ${provider.region ?? "not recorded"}; evidence ${provider.evidenceRef ?? "not recorded"}`
  );
  const credentialLines = document2.credentialReferences.map(
    (credential) => `${credential.ref}: ${credential.status}; scopes ${credential.scopes.join(", ") || "not recorded"}; expiry ${credential.expiresAt ?? "not recorded"}`
  );
  const manualLines = document2.remainingManualActions.map(
    (action) => `${action.nodeId}: ${action.action}; fields ${action.requiredFields.join(", ") || "none"}; risk ${action.risk}; evidence ${action.evidenceNeeded.join(", ") || "not recorded"}; resume \`${action.resumeCommand}\``
  );
  const sections = document2.sections;
  return `# Launch report: ${document2.brief.name} / ${document2.run.id}

- Generated: ${document2.generatedAt}
- Launch mode / rail: ${document2.launch.mode} / ${document2.launch.rail}
- Payment / entitlement source: ${document2.launch.paymentProvider ?? "none recorded"} / ${document2.launch.entitlementSource ?? "none recorded"}
- Event packs / consent: ${document2.launch.activeEventPacks?.join(", ") || "none recorded"} / ${document2.launch.consentMode ?? "not recorded"}
- Authorization: ${authorization}
- Overall state: ${document2.overallState}
- Brief: ${document2.brief.id}${document2.brief.synthetic ? " (synthetic fixture)" : ""}

## What was built

${lineItems(sections.whatBuilt, "No verified build summary was supplied.")}

## Repository

${lineItems(sections.repository, "No verified repository state was supplied.")}

## Deployments and builds

${lineItems(sections.deploymentsAndBuilds, "No verified deployment or build state was supplied.")}

## Provider resources

${lineItems(providerLines, "No verified provider resource state was supplied.")}

## Commerce

${lineItems(sections.commerce, "No commerce state was supplied.")}

## Email

${lineItems(sections.email, "No email state was supplied.")}

## Analytics and search

${lineItems(sections.analyticsAndSearch, "No analytics or search state was supplied.")}

## ASO and TestFlight

${lineItems(sections.asoAndTestflight, "No ASO or TestFlight state was supplied.")}

## Checks run

${lineItems(sections.checksRun, "No quality check state was supplied.")}

## Node outcomes

${lineItems(nodeLines, "No node outcomes were supplied.")}

## Active credential references

${lineItems(credentialLines, "No active credential references were supplied.")}

## Remaining manual actions

${lineItems(manualLines, "No genuinely unresolved manual action remains.")}

## Known limitations

${lineItems(document2.limitations, "No known limitation was supplied.")}

## Scheduled loops

${lineItems(sections.scheduledLoops, "No scheduled loop was supplied.")}

## Next reviews

${lineItems(sections.nextReviews, "No review date was supplied.")}

## Next commands

${lineItems(
    document2.nextCommands.map((command) => `\`${command}\``),
    "No next command is required."
  )}
`;
}
function renderLaunchReport(input, options = {}) {
  const redactor = options.redactor ?? new Redactor();
  const nodeMap = new Map(input.nodes.map((node) => [node.id, node]));
  const nodes = input.nodes.map((node) => ({
    ...node,
    id: sanitizeText(node.id, redactor),
    capability: sanitizeText(node.capability, redactor),
    provider: node.provider ? sanitizeText(node.provider, redactor) : void 0,
    evidenceRef: node.evidenceRef ? sanitizeText(node.evidenceRef, redactor) : void 0,
    errorCode: node.errorCode ? sanitizeText(node.errorCode, redactor) : void 0
  })).sort((left, right) => left.id.localeCompare(right.id));
  const providers = input.providers.map((provider) => ({
    ...provider,
    provider: sanitizeText(provider.provider, redactor),
    capability: sanitizeText(provider.capability, redactor),
    lifecycleState: sanitizeText(provider.lifecycleState, redactor),
    environment: provider.environment ? sanitizeText(provider.environment, redactor) : void 0,
    accountId: provider.accountId ? sanitizeText(provider.accountId, redactor) : void 0,
    teamId: provider.teamId ? sanitizeText(provider.teamId, redactor) : void 0,
    region: provider.region ? sanitizeText(provider.region, redactor) : void 0,
    resourceRefs: sortedUnique(provider.resourceRefs ?? [], redactor),
    evidenceRef: provider.evidenceRef ? sanitizeText(provider.evidenceRef, redactor) : void 0
  })).sort(
    (left, right) => `${left.provider}:${left.capability}`.localeCompare(`${right.provider}:${right.capability}`)
  );
  const remainingManualActions = input.manualActions.filter((action) => isGenuinelyUnresolved(action, nodeMap)).map((action) => ({
    ...action,
    nodeId: sanitizeText(action.nodeId, redactor),
    action: sanitizeText(action.action, redactor),
    requiredFields: sortedUnique(action.requiredFields, redactor),
    risk: sanitizeText(action.risk, redactor),
    evidenceNeeded: sortedUnique(action.evidenceNeeded, redactor),
    resumeCommand: sanitizeText(action.resumeCommand, redactor)
  })).sort((left, right) => left.nodeId.localeCompare(right.nodeId));
  const authorization = input.authorization ? {
    ...input.authorization,
    profile: sanitizeText(input.authorization.profile, redactor),
    approvalRef: sanitizeText(input.authorization.approvalRef, redactor)
  } : null;
  const document2 = {
    schemaVersion: 1,
    generatedAt: input.generatedAt,
    run: { id: sanitizeText(input.run.id, redactor), status: input.run.status },
    brief: {
      id: sanitizeText(input.brief.id, redactor),
      name: sanitizeText(input.brief.name, redactor),
      synthetic: input.brief.synthetic
    },
    launch: {
      mode: sanitizeText(input.launch.mode, redactor),
      rail: sanitizeText(input.launch.rail, redactor),
      paymentProvider: input.launch.paymentProvider ? sanitizeText(input.launch.paymentProvider, redactor) : void 0,
      entitlementSource: input.launch.entitlementSource ? sanitizeText(input.launch.entitlementSource, redactor) : void 0,
      activeEventPacks: sortedUnique(input.launch.activeEventPacks ?? [], redactor),
      consentMode: input.launch.consentMode ? sanitizeText(input.launch.consentMode, redactor) : void 0
    },
    authorization,
    overallState: overallState(input),
    nodes,
    providers,
    evidenceRefs: sortedUnique(
      [
        ...nodes.map(({ evidenceRef }) => evidenceRef),
        ...providers.map(({ evidenceRef }) => evidenceRef)
      ].filter((value) => value !== void 0),
      redactor
    ),
    remainingManualActions,
    credentialReferences: (input.credentialReferences ?? []).map((credential) => cleanCredentialReference(credential, redactor)).sort((left, right) => left.ref.localeCompare(right.ref)),
    limitations: sortedUnique(input.limitations, redactor),
    nextCommands: sortedUnique(input.nextCommands, redactor),
    sections: normalizeSections(input.sections, redactor)
  };
  const json2 = `${JSON.stringify(document2, null, 2)}
`;
  return { document: document2, json: json2, markdown: renderMarkdown(document2) };
}
async function atomicWrite(path, contents) {
  const temporary = `${path}.${process.pid}.${randomBytes3(6).toString("hex")}.tmp`;
  const handle = await open2(temporary, "wx", 384);
  try {
    await handle.writeFile(contents, "utf8");
    await handle.sync();
  } finally {
    await handle.close();
  }
  await rename3(temporary, path);
}
async function persistLaunchReport(report, outputDirectory = "reports/launch") {
  const directory = resolve14(outputDirectory);
  await mkdir4(directory, { recursive: true, mode: 448 });
  const jsonPath = join5(directory, "final.json");
  const markdownPath = join5(directory, "final.md");
  await atomicWrite(jsonPath, report.json);
  await atomicWrite(markdownPath, report.markdown);
  const [storedJson, storedMarkdown] = await Promise.all([
    readFile3(jsonPath, "utf8"),
    readFile3(markdownPath, "utf8")
  ]);
  if (storedJson !== report.json || storedMarkdown !== report.markdown) {
    throw new Error("Launch report read-back did not match the rendered artifacts");
  }
  return { ...report, jsonPath, markdownPath };
}
function createLaunchReportWorkflowBinding(options) {
  const handler = options.handler ?? "launch.report";
  return {
    handlers: {
      [handler]: async (context2) => {
        const persisted = await persistLaunchReport(
          renderLaunchReport(await options.input(context2), { redactor: options.redactor }),
          options.outputDirectory
        );
        return {
          output: {
            schemaVersion: 1,
            overallState: persisted.document.overallState,
            jsonPath: options.artifactReferences?.json ?? persisted.jsonPath,
            markdownPath: options.artifactReferences?.markdown ?? persisted.markdownPath,
            remainingManualActions: persisted.document.remainingManualActions.length
          },
          effectVerified: true,
          evidenceArtifact: options.artifactReferences?.json ?? persisted.jsonPath
        };
      }
    }
  };
}

// lib/runtime/launch-product-bindings.ts
import { createHash as createHash12 } from "node:crypto";
import {
  existsSync as existsSync9,
  lstatSync as lstatSync7,
  mkdirSync as mkdirSync7,
  readdirSync as readdirSync3,
  readFileSync as readFileSync11,
  renameSync as renameSync5,
  writeFileSync as writeFileSync7
} from "node:fs";
import { dirname as dirname9, isAbsolute as isAbsolute4, relative as relative11, resolve as resolve16, sep as sep11 } from "node:path";
import { parse as parse3 } from "yaml";

// lib/mobile/scaffold.ts
import { createHash as createHash11 } from "node:crypto";
import {
  closeSync as closeSync3,
  existsSync as existsSync8,
  lstatSync as lstatSync6,
  mkdirSync as mkdirSync6,
  openSync as openSync3,
  readFileSync as readFileSync10,
  readdirSync as readdirSync2,
  realpathSync as realpathSync6,
  writeFileSync as writeFileSync6
} from "node:fs";
import { dirname as dirname8, join as join6, relative as relative10, resolve as resolve15, sep as sep10 } from "node:path";

// lib/mobile/templates.ts
import { createHash as createHash10 } from "node:crypto";
var EXPO_TEMPLATE_TOOLCHAIN = {
  expo: "53.0.0",
  react: "19.0.0",
  reactNative: "0.79.2",
  typescript: "5.7.3"
};
function json(value) {
  return `${JSON.stringify(value, null, 2)}
`;
}
function swiftString(value) {
  return value.replaceAll("\\", "\\\\").replaceAll('"', '\\"').replaceAll("\n", "\\n").replaceAll("\r", "\\r").replaceAll("	", "\\t");
}
function pbxString(value) {
  return `"${value.replaceAll("\\", "\\\\").replaceAll('"', '\\"')}"`;
}
function moduleNameFor(ventureId2) {
  return ventureId2.split("-").filter(Boolean).map((segment) => `${segment[0].toUpperCase()}${segment.slice(1)}`).join("");
}
function pbxId(ventureId2, label) {
  return createHash10("sha256").update(`venture-harness:${ventureId2}:${label}`).digest("hex").slice(0, 24).toUpperCase();
}
function expoTemplates(input, bundleIdentifier, appScheme) {
  const moduleName = moduleNameFor(input.ventureId);
  const packageName = `${input.ventureId}-mobile`;
  const displayNameLiteral = JSON.stringify(input.displayName);
  return {
    moduleName,
    limitations: [
      "The scaffold pins an offline-reviewed Expo SDK compatibility set but does not install packages or prove a native build.",
      "EAS credentials, signing, submission, store metadata, purchases, and provider read-back remain unconfigured."
    ],
    files: [
      {
        relativePath: "package.json",
        content: json({
          name: packageName,
          version: "0.1.0",
          private: true,
          main: "node_modules/expo/AppEntry.js",
          scripts: {
            start: "expo start",
            ios: "expo start --ios",
            android: "expo start --android",
            typecheck: "tsc --noEmit"
          },
          dependencies: {
            expo: EXPO_TEMPLATE_TOOLCHAIN.expo,
            react: EXPO_TEMPLATE_TOOLCHAIN.react,
            "react-native": EXPO_TEMPLATE_TOOLCHAIN.reactNative
          },
          devDependencies: {
            "@types/react": "19.0.10",
            typescript: EXPO_TEMPLATE_TOOLCHAIN.typescript
          }
        })
      },
      {
        relativePath: "app.json",
        content: json({
          expo: {
            name: input.displayName,
            slug: input.ventureId,
            version: "0.1.0",
            orientation: "portrait",
            scheme: appScheme,
            userInterfaceStyle: "automatic",
            ios: {
              bundleIdentifier,
              supportsTablet: true
            },
            extra: {
              ventureHarness: {
                scaffoldStatus: "local_prototype",
                submissionConfigured: false
              }
            }
          }
        })
      },
      {
        relativePath: "eas.json",
        content: json({
          cli: { appVersionSource: "local" },
          build: {
            development: { distribution: "internal" },
            preview: { distribution: "internal" },
            production: { autoIncrement: false }
          }
        })
      },
      {
        relativePath: "tsconfig.json",
        content: json({
          extends: "expo/tsconfig.base",
          compilerOptions: {
            strict: true,
            noEmit: true
          },
          include: ["**/*.ts", "**/*.tsx", ".expo/types/**/*.ts", "expo-env.d.ts"]
        })
      },
      {
        relativePath: "expo-env.d.ts",
        content: '/// <reference types="expo/types" />\n'
      },
      {
        relativePath: "App.tsx",
        content: `import { ScaffoldScreen } from "./src/ScaffoldScreen";

export default function App() {
  return <ScaffoldScreen appName={${displayNameLiteral}} />;
}
`
      },
      {
        relativePath: "src/ScaffoldScreen.tsx",
        content: `import { SafeAreaView, StyleSheet, Text, View } from "react-native";

export function ScaffoldScreen({ appName }: { appName: string }) {
  return (
    <SafeAreaView style={styles.safeArea}>
      <View style={styles.content}>
        <Text accessibilityRole="header" style={styles.eyebrow}>
          Local prototype scaffold
        </Text>
        <Text style={styles.title}>{appName}</Text>
        <Text style={styles.body}>
          Replace this bounded placeholder with the reviewed smallest core journey.
        </Text>
      </View>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  safeArea: { flex: 1, backgroundColor: "#F5F3EE" },
  content: { flex: 1, justifyContent: "center", padding: 24, gap: 12 },
  eyebrow: { color: "#425466", fontSize: 14, fontWeight: "600" },
  title: { color: "#17212B", fontSize: 32, fontWeight: "700" },
  body: { color: "#425466", fontSize: 17, lineHeight: 25 },
});
`
      },
      {
        relativePath: ".gitignore",
        content: ".expo/\nnode_modules/\ndist/\nweb-build/\n"
      },
      {
        relativePath: "README.md",
        content: `# Generated Expo scaffold

This create-only scaffold is a local prototype for \`${input.ventureId}\`. It preserves the
founder brief's display name and records identifiers in \`.venture-scaffold.json\`.

No package installation, signing, EAS submission, store record, purchase, or live provider
state is implied. Review the core journey and dependency versions before installing or building.
`
      }
    ]
  };
}
function swiftProject(input, moduleName, bundleIdentifier) {
  const id = (label) => pbxId(input.ventureId, label);
  const project = id("project");
  const mainGroup = id("main-group");
  const productGroup = id("product-group");
  const sourceGroup = id("source-group");
  const target = id("app-target");
  const product = id("app-product");
  const appFile = id("app-file");
  const viewFile = id("view-file");
  const appBuildFile = id("app-build-file");
  const viewBuildFile = id("view-build-file");
  const sources = id("sources-phase");
  const resources = id("resources-phase");
  const frameworks = id("frameworks-phase");
  const projectConfigurations = id("project-config-list");
  const targetConfigurations = id("target-config-list");
  const projectDebug = id("project-debug");
  const projectRelease = id("project-release");
  const targetDebug = id("target-debug");
  const targetRelease = id("target-release");
  const quotedName = pbxString(input.displayName);
  return `// !$*UTF8*$!
{
	archiveVersion = 1;
	classes = {};
	objectVersion = 56;
	objects = {

/* Begin PBXBuildFile section */
		${appBuildFile} /* ${moduleName}App.swift in Sources */ = {isa = PBXBuildFile; fileRef = ${appFile} /* ${moduleName}App.swift */; };
		${viewBuildFile} /* ScaffoldView.swift in Sources */ = {isa = PBXBuildFile; fileRef = ${viewFile} /* ScaffoldView.swift */; };
/* End PBXBuildFile section */

/* Begin PBXFileReference section */
		${appFile} /* ${moduleName}App.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = ${moduleName}App.swift; sourceTree = "<group>"; };
		${viewFile} /* ScaffoldView.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = ScaffoldView.swift; sourceTree = "<group>"; };
		${product} /* ${moduleName}.app */ = {isa = PBXFileReference; explicitFileType = wrapper.application; includeInIndex = 0; path = ${moduleName}.app; sourceTree = BUILT_PRODUCTS_DIR; };
/* End PBXFileReference section */

/* Begin PBXFrameworksBuildPhase section */
		${frameworks} /* Frameworks */ = {isa = PBXFrameworksBuildPhase; buildActionMask = 2147483647; files = (); runOnlyForDeploymentPostprocessing = 0; };
/* End PBXFrameworksBuildPhase section */

/* Begin PBXGroup section */
		${mainGroup} = {isa = PBXGroup; children = (${sourceGroup} /* ${moduleName} */, ${productGroup} /* Products */); sourceTree = "<group>"; };
		${productGroup} /* Products */ = {isa = PBXGroup; children = (${product} /* ${moduleName}.app */); name = Products; sourceTree = "<group>"; };
		${sourceGroup} /* ${moduleName} */ = {isa = PBXGroup; children = (${appFile} /* ${moduleName}App.swift */, ${viewFile} /* ScaffoldView.swift */); path = ${moduleName}; sourceTree = "<group>"; };
/* End PBXGroup section */

/* Begin PBXNativeTarget section */
		${target} /* ${moduleName} */ = {isa = PBXNativeTarget; buildConfigurationList = ${targetConfigurations} /* Build configuration list for PBXNativeTarget "${moduleName}" */; buildPhases = (${sources} /* Sources */, ${frameworks} /* Frameworks */, ${resources} /* Resources */); buildRules = (); dependencies = (); name = ${moduleName}; productName = ${moduleName}; productReference = ${product} /* ${moduleName}.app */; productType = "com.apple.product-type.application"; };
/* End PBXNativeTarget section */

/* Begin PBXProject section */
		${project} /* Project object */ = {isa = PBXProject; attributes = {BuildIndependentTargetsInParallel = YES; LastSwiftUpdateCheck = 1500; LastUpgradeCheck = 1500; TargetAttributes = {${target} = {CreatedOnToolsVersion = 15.0; }; }; }; buildConfigurationList = ${projectConfigurations} /* Build configuration list for PBXProject "${moduleName}" */; compatibilityVersion = "Xcode 14.0"; developmentRegion = en; hasScannedForEncodings = 0; knownRegions = (en, Base); mainGroup = ${mainGroup}; productRefGroup = ${productGroup} /* Products */; projectDirPath = ""; projectRoot = ""; targets = (${target} /* ${moduleName} */); };
/* End PBXProject section */

/* Begin PBXResourcesBuildPhase section */
		${resources} /* Resources */ = {isa = PBXResourcesBuildPhase; buildActionMask = 2147483647; files = (); runOnlyForDeploymentPostprocessing = 0; };
/* End PBXResourcesBuildPhase section */

/* Begin PBXSourcesBuildPhase section */
		${sources} /* Sources */ = {isa = PBXSourcesBuildPhase; buildActionMask = 2147483647; files = (${appBuildFile} /* ${moduleName}App.swift in Sources */, ${viewBuildFile} /* ScaffoldView.swift in Sources */); runOnlyForDeploymentPostprocessing = 0; };
/* End PBXSourcesBuildPhase section */

/* Begin XCBuildConfiguration section */
		${projectDebug} /* Debug */ = {isa = XCBuildConfiguration; buildSettings = {ALWAYS_SEARCH_USER_PATHS = NO; CLANG_ENABLE_MODULES = YES; SWIFT_OPTIMIZATION_LEVEL = "-Onone"; }; name = Debug; };
		${projectRelease} /* Release */ = {isa = XCBuildConfiguration; buildSettings = {ALWAYS_SEARCH_USER_PATHS = NO; CLANG_ENABLE_MODULES = YES; SWIFT_COMPILATION_MODE = wholemodule; }; name = Release; };
		${targetDebug} /* Debug */ = {isa = XCBuildConfiguration; buildSettings = {CODE_SIGN_STYLE = Automatic; CURRENT_PROJECT_VERSION = 1; GENERATE_INFOPLIST_FILE = YES; INFOPLIST_KEY_CFBundleDisplayName = ${quotedName}; IPHONEOS_DEPLOYMENT_TARGET = 16.0; MARKETING_VERSION = 0.1.0; PRODUCT_BUNDLE_IDENTIFIER = ${bundleIdentifier}; PRODUCT_NAME = "$(TARGET_NAME)"; SDKROOT = iphoneos; SUPPORTED_PLATFORMS = "iphoneos iphonesimulator"; SWIFT_VERSION = 5.0; TARGETED_DEVICE_FAMILY = "1,2"; }; name = Debug; };
		${targetRelease} /* Release */ = {isa = XCBuildConfiguration; buildSettings = {CODE_SIGN_STYLE = Automatic; CURRENT_PROJECT_VERSION = 1; GENERATE_INFOPLIST_FILE = YES; INFOPLIST_KEY_CFBundleDisplayName = ${quotedName}; IPHONEOS_DEPLOYMENT_TARGET = 16.0; MARKETING_VERSION = 0.1.0; PRODUCT_BUNDLE_IDENTIFIER = ${bundleIdentifier}; PRODUCT_NAME = "$(TARGET_NAME)"; SDKROOT = iphoneos; SUPPORTED_PLATFORMS = "iphoneos iphonesimulator"; SWIFT_VERSION = 5.0; TARGETED_DEVICE_FAMILY = "1,2"; }; name = Release; };
/* End XCBuildConfiguration section */

/* Begin XCConfigurationList section */
		${projectConfigurations} /* Build configuration list for PBXProject "${moduleName}" */ = {isa = XCConfigurationList; buildConfigurations = (${projectDebug} /* Debug */, ${projectRelease} /* Release */); defaultConfigurationIsVisible = 0; defaultConfigurationName = Release; };
		${targetConfigurations} /* Build configuration list for PBXNativeTarget "${moduleName}" */ = {isa = XCConfigurationList; buildConfigurations = (${targetDebug} /* Debug */, ${targetRelease} /* Release */); defaultConfigurationIsVisible = 0; defaultConfigurationName = Release; };
/* End XCConfigurationList section */
	};
	rootObject = ${project} /* Project object */;
}
`;
}
function swiftUiTemplates(input, bundleIdentifier, appScheme) {
  const moduleName = moduleNameFor(input.ventureId);
  const displayName = swiftString(input.displayName);
  return {
    moduleName,
    limitations: [
      "The native project is unsigned and local-only until Apple identifiers and signing references are reviewed.",
      "A parsed project or simulator compile does not prove archive, upload, TestFlight processing, store metadata, purchases, or publication."
    ],
    files: [
      {
        relativePath: `${moduleName}/${moduleName}App.swift`,
        content: `import SwiftUI

@main
struct ${moduleName}App: App {
    var body: some Scene {
        WindowGroup {
            ScaffoldView(appName: "${displayName}")
        }
    }
}
`
      },
      {
        relativePath: `${moduleName}/ScaffoldView.swift`,
        content: `import SwiftUI

struct ScaffoldView: View {
    let appName: String

    var body: some View {
        VStack(alignment: .leading, spacing: 12) {
            Text("Local prototype scaffold")
                .font(.subheadline.weight(.semibold))
                .foregroundStyle(.secondary)
            Text(appName)
                .font(.largeTitle.bold())
            Text("Replace this bounded placeholder with the reviewed smallest core journey.")
                .font(.body)
                .foregroundStyle(.secondary)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .leading)
        .padding(24)
        .background(Color(red: 0.96, green: 0.95, blue: 0.93))
        .accessibilityElement(children: .contain)
    }
}
`
      },
      {
        relativePath: `${moduleName}.xcodeproj/project.pbxproj`,
        content: swiftProject(input, moduleName, bundleIdentifier)
      },
      {
        relativePath: `${moduleName}.xcodeproj/xcshareddata/xcschemes/${moduleName}.xcscheme`,
        content: `<?xml version="1.0" encoding="UTF-8"?>
<Scheme LastUpgradeVersion="1500" version="1.7">
  <BuildAction parallelizeBuildables="YES" buildImplicitDependencies="YES">
    <BuildActionEntries>
      <BuildActionEntry buildForTesting="YES" buildForRunning="YES" buildForProfiling="YES" buildForArchiving="YES" buildForAnalyzing="YES">
        <BuildableReference BuildableIdentifier="primary" BlueprintIdentifier="${pbxId(input.ventureId, "app-target")}" BuildableName="${moduleName}.app" BlueprintName="${moduleName}" ReferencedContainer="container:${moduleName}.xcodeproj" />
      </BuildActionEntry>
    </BuildActionEntries>
  </BuildAction>
  <TestAction buildConfiguration="Debug" shouldUseLaunchSchemeArgsEnv="YES" />
  <LaunchAction buildConfiguration="Debug" useCustomWorkingDirectory="NO" ignoresPersistentStateOnLaunch="NO" debugDocumentVersioning="YES" debugServiceExtension="internal" allowLocationSimulation="YES">
    <BuildableProductRunnable runnableDebuggingMode="0">
      <BuildableReference BuildableIdentifier="primary" BlueprintIdentifier="${pbxId(input.ventureId, "app-target")}" BuildableName="${moduleName}.app" BlueprintName="${moduleName}" ReferencedContainer="container:${moduleName}.xcodeproj" />
    </BuildableProductRunnable>
  </LaunchAction>
  <ProfileAction buildConfiguration="Release" shouldUseLaunchSchemeArgsEnv="YES" savedToolIdentifier="" useCustomWorkingDirectory="NO" debugDocumentVersioning="YES" />
  <AnalyzeAction buildConfiguration="Debug" />
  <ArchiveAction buildConfiguration="Release" revealArchiveInOrganizer="YES" />
</Scheme>
`
      },
      {
        relativePath: ".gitignore",
        content: "DerivedData/\n*.xcuserstate\nxcuserdata/\n"
      },
      {
        relativePath: "README.md",
        content: `# Generated SwiftUI scaffold

This create-only scaffold is a local prototype for \`${input.ventureId}\`. The shared
\`${moduleName}\` scheme and project are deterministic; the URL scheme reserved for later reviewed
configuration is \`${appScheme}\`.

No signing, archive, upload, App Store record, purchase, or live provider state is implied.
`
      }
    ]
  };
}
function renderMobileTemplates(input, bundleIdentifier, appScheme) {
  const renderer = {
    expo_react_native: expoTemplates,
    swiftui: swiftUiTemplates
  };
  return renderer[input.stack](input, bundleIdentifier, appScheme);
}

// lib/mobile/types.ts
var mobileScaffoldStackSchema = external_exports.enum(["expo_react_native", "swiftui"]);
var boundedPlainText = (label, maximum) => external_exports.string().min(1).max(maximum).refine((value) => value === value.trim(), `${label} must not have surrounding whitespace`).refine((value) => !/[\u0000-\u001f\u007f]/.test(value), `${label} must not contain controls`);
var mobileBundleIdentifierSchema = external_exports.string().min(3).max(155).regex(
  /^[A-Za-z][A-Za-z0-9-]*(?:\.[A-Za-z0-9][A-Za-z0-9-]*)+$/,
  "expected a reverse-domain bundle identifier"
);
var mobileAppSchemeSchema = external_exports.string().min(1).max(64).regex(/^[a-z][a-z0-9+.-]*$/, "expected a lowercase application URL scheme");
var mobileScaffoldDirectorySchema = artifactReferenceSchema.refine(
  (value) => /^[A-Za-z0-9][A-Za-z0-9._-]*(?:\/[A-Za-z0-9][A-Za-z0-9._-]*)*$/.test(value),
  "scaffold directory must contain only safe repository path segments"
).refine((value) => value !== ".", "scaffold directory cannot be the repository root");
var mobileScaffoldRequestSchema = external_exports.object({
  stack: mobileScaffoldStackSchema,
  ventureId: external_exports.string().min(1).max(63).regex(/^[a-z][a-z0-9-]*$/),
  displayName: boundedPlainText("display name", 80),
  bundleIdentifier: mobileBundleIdentifierSchema.nullable().optional(),
  appScheme: mobileAppSchemeSchema.nullable().optional(),
  outputDirectory: mobileScaffoldDirectorySchema.optional()
}).strict().superRefine((value, context2) => rejectCredentialMaterial(value, context2));
var mobileScaffoldFileSchema = external_exports.object({
  path: artifactReferenceSchema,
  sha256: external_exports.string().regex(/^[a-f0-9]{64}$/)
}).strict();
var mobileScaffoldManifestSchema = external_exports.object({
  schemaVersion: external_exports.literal(1),
  generator: external_exports.literal("venture-harness/mobile-scaffold"),
  templateVersion: external_exports.literal("1.0.0"),
  stack: mobileScaffoldStackSchema,
  ventureId: external_exports.string().min(1),
  displayName: external_exports.string().min(1),
  outputDirectory: mobileScaffoldDirectorySchema,
  identity: external_exports.object({
    bundleIdentifier: mobileBundleIdentifierSchema,
    bundleIdentifierState: external_exports.enum(["configured", "local_placeholder"]),
    appScheme: mobileAppSchemeSchema,
    appSchemeState: external_exports.enum(["configured", "derived"])
  }).strict(),
  files: external_exports.array(mobileScaffoldFileSchema),
  safeguards: external_exports.object({
    noOverwrite: external_exports.literal(true),
    credentialsPersisted: external_exports.literal(false),
    signingMaterialPersisted: external_exports.literal(false),
    submissionConfigured: external_exports.literal(false)
  }).strict(),
  limitations: external_exports.array(external_exports.string().min(1))
}).strict();
var mobileScaffoldResultSchema = external_exports.object({
  manifest: mobileScaffoldManifestSchema,
  manifestPath: artifactReferenceSchema,
  createdFiles: external_exports.array(artifactReferenceSchema),
  unchangedFiles: external_exports.array(artifactReferenceSchema)
}).strict();
var MobileScaffoldError = class extends Error {
  constructor(code, message) {
    super(message);
    this.code = code;
    this.name = "MobileScaffoldError";
  }
  code;
};

// lib/mobile/scaffold.ts
var MANIFEST_NAME = ".venture-scaffold.json";
function sha2563(content) {
  return createHash11("sha256").update(content).digest("hex");
}
function defaultMobileScaffoldDirectory(stack) {
  return stack === "expo_react_native" ? "mobile/expo" : "mobile/ios";
}
function placeholderBundleIdentifier(ventureId2) {
  return `com.example.${ventureId2}`;
}
function relativeReference(root, absolute) {
  return relative10(root, absolute).split(sep10).join("/");
}
function pathInside(root, reference) {
  const absolute = resolve15(root, reference);
  const rel = relative10(root, absolute);
  if (rel !== "" && !rel.startsWith(`..${sep10}`) && rel !== ".." && !rel.startsWith(sep10)) {
    return absolute;
  }
  throw new MobileScaffoldError("unsafe_path", `Path escapes repository root: ${reference}`);
}
function assertNoSymlinkBetween(root, absolute) {
  const rel = relative10(root, absolute);
  let current = root;
  for (const segment of rel.split(sep10).filter(Boolean)) {
    current = join6(current, segment);
    if (!existsSync8(current)) continue;
    const status = lstatSync6(current);
    if (status.isSymbolicLink()) {
      throw new MobileScaffoldError(
        "unsafe_path",
        `Scaffold path traverses a symbolic link: ${relativeReference(root, current)}`
      );
    }
  }
}
function ensureDirectory(root, absolute) {
  const rel = relative10(root, absolute);
  let current = root;
  for (const segment of rel.split(sep10).filter(Boolean)) {
    current = join6(current, segment);
    if (existsSync8(current)) {
      const status = lstatSync6(current);
      if (status.isSymbolicLink() || !status.isDirectory()) {
        throw new MobileScaffoldError(
          "unsafe_path",
          `Expected a real directory at ${relativeReference(root, current)}.`
        );
      }
      continue;
    }
    mkdirSync6(current, { mode: 493 });
  }
}
function readExactFile(root, path, expected) {
  if (!existsSync8(path)) return false;
  const status = lstatSync6(path);
  if (status.isSymbolicLink() || !status.isFile()) {
    throw new MobileScaffoldError(
      "output_conflict",
      `Refusing to replace non-file scaffold target ${relativeReference(root, path)}.`
    );
  }
  if (readFileSync10(path, "utf8") !== expected) {
    throw new MobileScaffoldError(
      "output_conflict",
      `Refusing to overwrite existing content at ${relativeReference(root, path)}.`
    );
  }
  return true;
}
function writeCreateOnly(root, path, content) {
  assertNoSymlinkBetween(root, path);
  ensureDirectory(root, dirname8(path));
  if (readExactFile(root, path, content)) return "unchanged";
  let descriptor2;
  try {
    descriptor2 = openSync3(path, "wx", 420);
    writeFileSync6(descriptor2, content, { encoding: "utf8" });
    closeSync3(descriptor2);
    descriptor2 = void 0;
    return "created";
  } catch (error) {
    if (descriptor2 !== void 0) closeSync3(descriptor2);
    if (error.code === "EEXIST") {
      if (readExactFile(root, path, content)) return "unchanged";
      throw new MobileScaffoldError(
        "io_failure",
        `A concurrent writer removed ${relativeReference(root, path)} before read-back.`
      );
    }
    throw new MobileScaffoldError(
      "io_failure",
      `Could not create ${relativeReference(root, path)}: ${error instanceof Error ? error.message : String(error)}`
    );
  }
}
function generateMobileScaffold(rootDirectory, requestInput) {
  const request2 = mobileScaffoldRequestSchema.parse(requestInput);
  const root = realpathSync6(resolve15(rootDirectory));
  if (!lstatSync6(root).isDirectory()) {
    throw new MobileScaffoldError("unsafe_path", `Repository root is not a directory: ${root}`);
  }
  const outputDirectory = request2.outputDirectory ?? defaultMobileScaffoldDirectory(request2.stack);
  const output = pathInside(root, outputDirectory);
  assertNoSymlinkBetween(root, output);
  const bundleIdentifier = request2.bundleIdentifier ?? placeholderBundleIdentifier(request2.ventureId);
  const appScheme = request2.appScheme ?? request2.ventureId;
  const rendered2 = renderMobileTemplates(request2, bundleIdentifier, appScheme);
  const templateFiles = [...rendered2.files].sort(
    (left, right) => left.relativePath.localeCompare(right.relativePath)
  );
  const limitations = [
    ...rendered2.limitations,
    ...request2.bundleIdentifier ? [] : [
      `The bundle identifier ${bundleIdentifier} is a local placeholder and must be replaced by a reviewed identifier before signing or provider setup.`
    ]
  ];
  const manifest = mobileScaffoldManifestSchema.parse({
    schemaVersion: 1,
    generator: "venture-harness/mobile-scaffold",
    templateVersion: "1.0.0",
    stack: request2.stack,
    ventureId: request2.ventureId,
    displayName: request2.displayName,
    outputDirectory,
    identity: {
      bundleIdentifier,
      bundleIdentifierState: request2.bundleIdentifier ? "configured" : "local_placeholder",
      appScheme,
      appSchemeState: request2.appScheme ? "configured" : "derived"
    },
    files: templateFiles.map((file2) => ({
      path: `${outputDirectory}/${file2.relativePath}`,
      sha256: sha2563(file2.content)
    })),
    safeguards: {
      noOverwrite: true,
      credentialsPersisted: false,
      signingMaterialPersisted: false,
      submissionConfigured: false
    },
    limitations
  });
  const manifestContent = `${JSON.stringify(manifest, null, 2)}
`;
  const manifestPath = `${outputDirectory}/${MANIFEST_NAME}`;
  const absoluteManifestPath = pathInside(root, manifestPath);
  if (existsSync8(output)) {
    const outputStatus = lstatSync6(output);
    if (outputStatus.isSymbolicLink() || !outputStatus.isDirectory()) {
      throw new MobileScaffoldError(
        "output_conflict",
        `Refusing to replace existing scaffold output ${outputDirectory}.`
      );
    }
    const entries = readdirSync2(output);
    if (entries.length > 0 && !existsSync8(absoluteManifestPath)) {
      throw new MobileScaffoldError(
        "output_conflict",
        `Refusing to add a scaffold inside existing unowned directory ${outputDirectory}.`
      );
    }
  }
  if (existsSync8(absoluteManifestPath)) {
    readExactFile(root, absoluteManifestPath, manifestContent);
  }
  for (const file2 of templateFiles) {
    const absolute = pathInside(root, `${outputDirectory}/${file2.relativePath}`);
    if (existsSync8(absolute)) readExactFile(root, absolute, file2.content);
  }
  const createdFiles = [];
  const unchangedFiles = [];
  const write = (path, content) => {
    const absolute = pathInside(root, path);
    const bucket = writeCreateOnly(root, absolute, content);
    (bucket === "created" ? createdFiles : unchangedFiles).push(path);
  };
  write(manifestPath, manifestContent);
  for (const file2 of templateFiles) write(`${outputDirectory}/${file2.relativePath}`, file2.content);
  readExactFile(root, absoluteManifestPath, manifestContent);
  for (const file2 of templateFiles) {
    const absolute = pathInside(root, `${outputDirectory}/${file2.relativePath}`);
    readExactFile(root, absolute, file2.content);
    if (sha2563(readFileSync10(absolute, "utf8")) !== sha2563(file2.content)) {
      throw new MobileScaffoldError(
        "io_failure",
        `Hash read-back failed for ${relativeReference(root, absolute)}.`
      );
    }
  }
  return mobileScaffoldResultSchema.parse({
    manifest,
    manifestPath,
    createdFiles,
    unchangedFiles
  });
}

// lib/runtime/launch-product-bindings.ts
var AGENT_TASKS = {
  "launch.prepareRepository": "Inspect the selected rail and existing repository, then create or adapt only the smallest venture-owned scaffold needed for the brief. Resolve every package needed by the planned product into package.json and the exact child lockfile now; dependency inputs are finalized immediately after this task and later tasks may not mutate them. Preserve managed contracts and existing project-owned work. Record assumptions instead of inventing non-critical product detail.",
  "launch.designDirection": "Create and implement an original, accessible visual direction for the smallest core journey. Record the design thesis, tokens, responsive composition, accessibility constraints, and anti-template audit. Do not copy a reference identity or fabricate product proof.",
  "launch.buildCoreJourney": "Implement the smallest useful core journey declared by the brief on the selected rail. Add affected tests, label sample or synthetic data, keep public claims within PRODUCT_TRUTH, and avoid unrelated features.",
  "launch.configureEventPack": "Resolve and wire only the capability-driven analytics event packs needed by this core journey. Keep private form/search/user content out of analytics, require consent for third-party analytics, and include exact displayed prices on price-bearing evidence events.",
  "launch.defineValidationGate": "Define one bounded demand hypothesis, its primary signal, threshold, stop rule, assumptions, and evidence that would change the decision. Treat 30/60/90-day gates as optional for validate-first only. Do not require a pricing experiment without adequate traffic and decision value.",
  "launch.prepareConciergeOperations": "Define the smallest honest concierge delivery workflow. Specify capacity, privacy boundaries, service limits, failure escalation, human handoffs, evidence capture, and which work remains intentionally manual. Do not imply automation that does not exist.",
  "launch.defineUsageProof": "Connect the real core journey to bounded activation, usage, retention, quality, failure, and deletion evidence. Prefer first-party behavioral evidence, keep private content out of analytics, and do not infer causation from release timing alone."
};
var QUALITY_COMMANDS = {
  "launch.verifyLocal": ["verify:fast"],
  "launch.verifyMvp": ["verify:mvp"]
};
var CHILD_DEPENDENCY_INSTALL_ARGS = [
  "install",
  "--frozen-lockfile",
  "--ignore-workspace",
  "--ignore-scripts",
  "--prod=false"
];
var DEPENDENCY_INSTALL_CHECKPOINT_SCHEMA_VERSION = 1;
var POST_DEPLOY_TEST_ARGS = [
  "exec",
  "playwright",
  "test",
  "tests/e2e/post-deploy-readonly.spec.ts"
];
var COMPLETION_POLICIES = {
  "launch.prepareRepository": {
    requiredArtifactRoles: ["repository_scaffold", "managed_manifest"],
    relevantValidator: /(?:scaffold|manifest|harness[-:]?lock|validate:configs|verify:(?:fast|mvp)|test[^\n]*(?:config|scaffold))/i
  },
  "launch.designDirection": {
    requiredArtifactRoles: ["design_record", "design_implementation"],
    relevantValidator: /(?:design|accessib|responsive|visual|contrast|playwright|verify:mvp|validate:claims)/i
  },
  "launch.buildCoreJourney": {
    requiredArtifactRoles: ["core_journey", "affected_test"],
    relevantValidator: /(?:journey|e2e|playwright|\btest\b|verify:(?:fast|mvp)|\bbuild\b)/i
  },
  "launch.configureEventPack": {
    requiredArtifactRoles: ["event_contract", "event_instrumentation"],
    relevantValidator: /(?:analytics|event|consent|taxonomy|telemetry|instrument|pii|verify:fast)/i
  }
};
var SNAPSHOT_IGNORED_DIRECTORIES = /* @__PURE__ */ new Set([
  ".git",
  ".next",
  ".turbo",
  ".venture",
  "coverage",
  "node_modules",
  "reports"
]);
function inside5(root, path) {
  const absolute = resolve16(root, path);
  const rel = relative11(root, absolute);
  if (rel === "" || !rel.startsWith(`..${sep11}`) && rel !== ".." && !rel.startsWith(sep11)) {
    return absolute;
  }
  throw new WorkflowExecutionError("UNSAFE_ARTIFACT_PATH", `Path escapes venture root: ${path}`);
}
function repositoryReference(root, path) {
  if (isAbsolute4(path) || path.includes("\\") || path.split("/").some((segment) => segment === "" || segment === "." || segment === "..")) {
    throw new WorkflowExecutionError(
      "BUILD_AGENT_EVIDENCE_INVALID",
      `Build-agent path must be a canonical repository-relative reference: ${path}`
    );
  }
  const absolute = inside5(root, path);
  const reference = relative11(root, absolute).split(sep11).join("/");
  if (reference !== path) {
    throw new WorkflowExecutionError(
      "BUILD_AGENT_EVIDENCE_INVALID",
      `Build-agent path is not canonical: ${path}`
    );
  }
  return { absolute, reference };
}
function sha2564(path) {
  return createHash12("sha256").update(readFileSync11(path)).digest("hex");
}
function dependencyInstallCheckpoint(value) {
  if (!value || Array.isArray(value) || typeof value !== "object") return null;
  const record3 = value;
  if (record3.schemaVersion !== DEPENDENCY_INSTALL_CHECKPOINT_SCHEMA_VERSION || record3.packageManifest !== "package.json" || record3.lockfile !== "pnpm-lock.yaml" || typeof record3.packageManifestSha256 !== "string" || !/^[a-f0-9]{64}$/u.test(record3.packageManifestSha256) || typeof record3.lockfileSha256 !== "string" || !/^[a-f0-9]{64}$/u.test(record3.lockfileSha256)) {
    return null;
  }
  return {
    schemaVersion: DEPENDENCY_INSTALL_CHECKPOINT_SCHEMA_VERSION,
    packageManifest: "package.json",
    packageManifestSha256: record3.packageManifestSha256,
    lockfile: "pnpm-lock.yaml",
    lockfileSha256: record3.lockfileSha256
  };
}
function readDependencyInstallState(root, expected) {
  const packagePath = inside5(root, expected.packageManifest);
  const lockPath = inside5(root, expected.lockfile);
  if (!existsSync9(packagePath) || !lstatSync7(packagePath).isFile() || !existsSync9(lockPath) || !lstatSync7(lockPath).isFile()) {
    return {
      ...expected,
      state: "input_mismatch",
      installedModulesReadBack: false,
      installedLockfileReadBack: false,
      requiredToolingReadBack: false,
      message: "The checkpointed package manifest or lockfile is missing or not a regular file."
    };
  }
  const currentPackageSha256 = sha2564(packagePath);
  const currentLockSha256 = sha2564(lockPath);
  if (currentPackageSha256 !== expected.packageManifestSha256 || currentLockSha256 !== expected.lockfileSha256) {
    return {
      ...expected,
      state: "input_mismatch",
      installedModulesReadBack: false,
      installedLockfileReadBack: false,
      requiredToolingReadBack: false,
      message: "package.json or pnpm-lock.yaml changed after the dependency operation was checkpointed."
    };
  }
  const modulesPath = inside5(root, "node_modules");
  const installedModulesReadBack = existsSync9(modulesPath) && lstatSync7(modulesPath).isDirectory();
  const installedLockPath = inside5(root, "node_modules/.pnpm/lock.yaml");
  const installedLockfileReadBack = installedModulesReadBack && existsSync9(installedLockPath) && lstatSync7(installedLockPath).isFile() && sha2564(installedLockPath) === expected.lockfileSha256;
  const binaryDirectory = inside5(root, "node_modules/.bin");
  const commandInstalled = (name) => [name, `${name}.cmd`, `${name}.ps1`].some(
    (candidate) => existsSync9(resolve16(binaryDirectory, candidate))
  );
  const requiredToolingReadBack = installedModulesReadBack && commandInstalled("tsc") && commandInstalled("playwright");
  return {
    ...expected,
    state: installedModulesReadBack && installedLockfileReadBack && requiredToolingReadBack ? "verified" : "not_applied",
    installedModulesReadBack,
    installedLockfileReadBack,
    requiredToolingReadBack,
    message: installedModulesReadBack && installedLockfileReadBack && requiredToolingReadBack ? null : "The exact child dependency installation is absent or incomplete on read-back."
  };
}
function repositorySnapshot(root) {
  const snapshot = /* @__PURE__ */ new Map();
  const visit = (directory) => {
    for (const entry of readdirSync3(directory, { withFileTypes: true }).sort(
      (left, right) => left.name.localeCompare(right.name)
    )) {
      if (entry.isDirectory() && SNAPSHOT_IGNORED_DIRECTORIES.has(entry.name)) continue;
      const absolute = resolve16(directory, entry.name);
      if (entry.isDirectory()) {
        visit(absolute);
        continue;
      }
      if (!entry.isFile()) continue;
      const reference = relative11(root, absolute).split(sep11).join("/");
      snapshot.set(reference, sha2564(absolute));
    }
  };
  visit(root);
  return snapshot;
}
function repositoryChanges(before, after) {
  return [.../* @__PURE__ */ new Set([...before.keys(), ...after.keys()])].sort().flatMap((path) => {
    const beforeSha256 = before.get(path) ?? null;
    const afterSha256 = after.get(path) ?? null;
    return beforeSha256 === afterSha256 ? [] : [{ path, beforeSha256, afterSha256 }];
  });
}
function artifactRoleAllowsPath(role, path) {
  const productSource = /^(?:app|components|mobile|pages|public|src)\//.test(path);
  const codeOrProductSource = productSource || /^lib\//.test(path);
  switch (role) {
    case "repository_scaffold":
      return productSource || /^(?:package\.json|pnpm-lock\.yaml|tsconfig\.json|next\.config\.[^/]+|vite\.config\.[^/]+)$/.test(
        path
      );
    case "managed_manifest":
      return path === "harness.lock";
    case "design_record":
      return path === "docs/brand/DESIGN.md" || /^docs\/brand\/[^/]*DESIGN[^/]*\.md$/i.test(path);
    case "design_implementation":
      return productSource || /(?:^|\/)(?:design|styles?|theme|tokens?|ui)(?:\/|\.)/i.test(path) || /\.(?:css|less|sass|scss)$/.test(path);
    case "core_journey":
      return codeOrProductSource;
    case "affected_test":
      return /^(?:tests?|e2e)\//.test(path) || /(?:^|\/)[^/]+\.(?:spec|test)\.[^/]+$/.test(path);
    case "event_contract":
      return path === "config/analytics.yaml" || /^lib\/analytics\//.test(path) || /(?:^|\/)(?:analytics|events?|taxonomy|telemetry)(?:\/|\.)/i.test(path);
    case "event_instrumentation":
      return productSource || /^lib\//.test(path) && /(?:analytics|consent|events?|instrument|telemetry|track)/i.test(path);
    case "validation_record":
      return /^(?:config|docs)\//.test(path) && /validat|experiment|hypoth/i.test(path);
    case "concierge_operations":
      return /^(?:config|docs|lib)\//.test(path) && /concierge|operation|handoff/i.test(path);
    case "usage_proof":
      return /^(?:config|docs|lib|tests)\//.test(path) && /usage|retention|activation|evidence/i.test(path);
  }
}
function directCheck(command) {
  return /^(?:(?:pnpm|npm|yarn|bun|npx|node|deno|swiftc|xcodebuild|cargo|gradle)(?:\s|$)|\.\/scripts\/|scripts\/)/.test(
    command.trim()
  ) && !/[;&|`$<>]/.test(command);
}
function taskInstructions(handler, instructions) {
  const policy = COMPLETION_POLICIES[handler];
  if (!policy) return instructions;
  return [
    instructions,
    `Completion evidence must include these artifact roles: ${policy.requiredArtifactRoles.join(", ")}.`,
    "For outcome=changed, report every repository file whose content changed and name at least one changed completion artifact. For outcome=already_compliant, change no repository content and cite unchanged artifacts that already satisfy every required role.",
    "The completion validator must name one directly executed, relevant check from checks; it must pass and include observed evidence."
  ].join("\n\n");
}
function safeSegment(value, label) {
  if (!/^[A-Za-z0-9][A-Za-z0-9._-]*$/.test(value)) {
    throw new WorkflowExecutionError(
      "UNSAFE_ARTIFACT_PATH",
      `${label} cannot be used in a launch evidence path: ${value}`
    );
  }
  return value;
}
function writeJsonAtomic(path, value) {
  mkdirSync7(dirname9(path), { recursive: true, mode: 448 });
  const temporary = `${path}.next-${process.pid}-${Date.now()}`;
  writeFileSync7(temporary, `${JSON.stringify(value, null, 2)}
`, {
    encoding: "utf8",
    mode: 384
  });
  renameSync5(temporary, path);
}
function artifactPaths(root, context2) {
  const runId2 = safeSegment(context2.runId, "run ID");
  const nodeId = safeSegment(context2.node.id, "node ID");
  const reference = `reports/launch/${runId2}/product/${nodeId}.json`;
  return { reference, absolute: inside5(root, reference) };
}
function dependencyInstallEvidencePaths(root, runIdInput, nodeIdInput = "install-dependencies") {
  const runId2 = safeSegment(runIdInput, "run ID");
  const nodeId = safeSegment(nodeIdInput, "dependency install node ID");
  const reference = `reports/launch/${runId2}/product/${nodeId}.json`;
  return { reference, absolute: inside5(root, reference) };
}
function dependencyReconciliationEvidencePaths(root, runIdInput, nodeIdInput) {
  const runId2 = safeSegment(runIdInput, "run ID");
  const nodeId = safeSegment(nodeIdInput, "dependency install node ID");
  const reference = `reports/launch/${runId2}/product/${nodeId}.reconcile.json`;
  return { reference, absolute: inside5(root, reference) };
}
function checkpointFromInstallEvidence(root, runId2, nodeId) {
  const paths = dependencyInstallEvidencePaths(root, runId2, nodeId);
  if (!existsSync9(paths.absolute) || !lstatSync7(paths.absolute).isFile()) return null;
  let parsed;
  try {
    parsed = JSON.parse(readFileSync11(paths.absolute, "utf8"));
  } catch {
    return null;
  }
  if (!parsed || Array.isArray(parsed) || typeof parsed !== "object") return null;
  const record3 = parsed;
  if (record3.runId !== runId2 || record3.nodeId !== nodeId) return null;
  return dependencyInstallCheckpoint({
    schemaVersion: DEPENDENCY_INSTALL_CHECKPOINT_SCHEMA_VERSION,
    packageManifest: record3.packageManifest,
    packageManifestSha256: record3.packageManifestSha256,
    lockfile: record3.lockfile,
    lockfileSha256: record3.lockfileSha256
  });
}
function verifiedChangedFiles(root, changedFiles) {
  const seen = /* @__PURE__ */ new Set();
  return changedFiles.map((path) => {
    const { absolute, reference } = repositoryReference(root, path);
    if (seen.has(reference)) {
      throw new WorkflowExecutionError(
        "BUILD_AGENT_EVIDENCE_INVALID",
        `Build agent reported changed file ${path} more than once.`
      );
    }
    seen.add(reference);
    if (!existsSync9(absolute) || !lstatSync7(absolute).isFile()) {
      throw new WorkflowExecutionError(
        "BUILD_AGENT_EVIDENCE_INVALID",
        `Build agent reported changed file ${path}, but it does not exist after the task.`
      );
    }
    return reference;
  });
}
function validateAgentCompletion(root, handler, result2, before, after) {
  if (!result2.completion) {
    throw new WorkflowExecutionError(
      "BUILD_AGENT_EVIDENCE_INVALID",
      `Build agent completed ${handler} without typed completion evidence.`
    );
  }
  const changedFiles = verifiedChangedFiles(root, result2.changedFiles);
  const changes = repositoryChanges(before, after);
  const changedPaths = new Set(changes.map(({ path }) => path));
  const reportedPaths = new Set(changedFiles);
  const deletedPaths = changes.filter(({ afterSha256 }) => afterSha256 === null).map(({ path }) => path);
  if (deletedPaths.length > 0) {
    throw new WorkflowExecutionError(
      "BUILD_AGENT_EVIDENCE_INVALID",
      `Build agent deleted repository files without a typed deletion contract: ${deletedPaths.join(", ")}.`
    );
  }
  const unreported = changes.map(({ path }) => path).filter((path) => !reportedPaths.has(path));
  const unchanged = changedFiles.filter((path) => !changedPaths.has(path));
  if (unreported.length > 0 || unchanged.length > 0) {
    const details = [
      ...unreported.length > 0 ? [`unreported changes: ${unreported.join(", ")}`] : [],
      ...unchanged.length > 0 ? [`unchanged reported files: ${unchanged.join(", ")}`] : []
    ].join("; ");
    throw new WorkflowExecutionError(
      "BUILD_AGENT_EVIDENCE_INVALID",
      `Build agent before/after hash validation failed for ${handler}: ${details}.`
    );
  }
  if (result2.completion.outcome === "changed") {
    if (changedFiles.length === 0 || changes.length === 0) {
      throw new WorkflowExecutionError(
        "BUILD_AGENT_EVIDENCE_INVALID",
        `Build agent declared changed completion for ${handler}, but no repository content changed.`
      );
    }
  } else if (changedFiles.length > 0 || changes.length > 0) {
    throw new WorkflowExecutionError(
      "BUILD_AGENT_EVIDENCE_INVALID",
      `Build agent declared already_compliant for ${handler}, but repository content changed.`
    );
  }
  const artifacts = result2.completion.artifacts.map(({ path, role }) => {
    const { absolute, reference } = repositoryReference(root, path);
    if (!existsSync9(absolute) || !lstatSync7(absolute).isFile()) {
      throw new WorkflowExecutionError(
        "BUILD_AGENT_EVIDENCE_INVALID",
        `Build agent completion artifact ${path} does not exist as a regular file.`
      );
    }
    if (!artifactRoleAllowsPath(role, reference)) {
      throw new WorkflowExecutionError(
        "BUILD_AGENT_EVIDENCE_INVALID",
        `Build agent artifact ${path} cannot satisfy role ${role}.`
      );
    }
    return { path: reference, role };
  });
  const policy = COMPLETION_POLICIES[handler];
  for (const role of policy?.requiredArtifactRoles ?? []) {
    if (!artifacts.some((artifact) => artifact.role === role)) {
      throw new WorkflowExecutionError(
        "BUILD_AGENT_EVIDENCE_INVALID",
        `Build agent completion for ${handler} is missing required artifact role ${role}.`
      );
    }
  }
  if (result2.completion.outcome === "changed" && !artifacts.some(({ path }) => reportedPaths.has(path))) {
    throw new WorkflowExecutionError(
      "BUILD_AGENT_EVIDENCE_INVALID",
      `Build agent changed files for ${handler}, but none of its completion artifacts changed.`
    );
  }
  if (result2.completion.outcome === "already_compliant" && artifacts.some(
    ({ path }) => before.get(path) === void 0 || before.get(path) !== after.get(path)
  )) {
    throw new WorkflowExecutionError(
      "BUILD_AGENT_EVIDENCE_INVALID",
      `Build agent already_compliant artifacts for ${handler} were not present unchanged before the task.`
    );
  }
  const checkCommand = result2.completion.validator.checkCommand;
  const validator = result2.checks.find(({ command }) => command === checkCommand);
  if (!directCheck(checkCommand) || validator?.status !== "passed" || !validator.evidence?.trim() || policy && !policy.relevantValidator.test(checkCommand)) {
    throw new WorkflowExecutionError(
      "BUILD_AGENT_EVIDENCE_INVALID",
      `Build agent completion validator for ${handler} must be a relevant direct passed check with non-empty evidence.`
    );
  }
  return {
    changedFiles,
    artifacts,
    repositoryChanges: changes,
    validator: { checkCommand, evidence: validator.evidence }
  };
}
function persistEvidence(root, context2, evidence, redactor) {
  const paths = artifactPaths(root, context2);
  writeJsonAtomic(paths.absolute, redactor.redact(evidence));
  const readBack = JSON.parse(readFileSync11(paths.absolute, "utf8"));
  if (readBack.runId !== context2.runId || readBack.nodeId !== context2.node.id) {
    throw new WorkflowExecutionError(
      "BUILD_AGENT_EVIDENCE_INVALID",
      `Launch evidence read-back failed for ${context2.node.id}.`
    );
  }
  return paths.reference;
}
function hostOutput(result2) {
  return {
    status: result2.status,
    summary: result2.summary,
    changedFiles: result2.changedFiles,
    checks: result2.checks,
    limitations: result2.limitations,
    completion: result2.completion,
    host: "build_agent",
    usage: result2.usage ?? null
  };
}
function configuredMobileScaffold(root, brief, explicit) {
  const configPath = inside5(root, "config/mobile.yaml");
  const configured = existsSync9(configPath) ? mobileSchema.parse(parse3(readFileSync11(configPath, "utf8"))).mobile : void 0;
  return {
    bundleIdentifier: explicit?.bundleIdentifier ?? configured?.bundle_identifier ?? brief.bundle_identifier ?? void 0,
    appScheme: explicit?.appScheme ?? configured?.app_scheme ?? brief.app_scheme ?? brief.id,
    outputDirectory: explicit?.outputDirectory
  };
}
async function runMobileScaffoldTask(options, context2) {
  const startedAt = options.now().toISOString();
  const rail = routeRail(options.brief);
  if (rail.mobileStack === "none" || rail.mobileStack === "auto") {
    throw new WorkflowExecutionError(
      "MOBILE_SCAFFOLD_ROUTE_INVALID",
      `The mobile scaffold handler requires a concrete mobile stack; received ${rail.mobileStack}.`
    );
  }
  let result2;
  try {
    const scaffold = configuredMobileScaffold(
      options.rootDir,
      options.brief,
      options.mobileScaffold
    );
    result2 = generateMobileScaffold(options.rootDir, {
      stack: rail.mobileStack,
      ventureId: options.brief.id,
      displayName: options.brief.name,
      bundleIdentifier: scaffold?.bundleIdentifier,
      appScheme: scaffold?.appScheme,
      outputDirectory: scaffold?.outputDirectory
    });
  } catch (error) {
    const evidenceArtifact2 = persistEvidence(
      options.rootDir,
      context2,
      {
        schemaVersion: 1,
        runId: context2.runId,
        nodeId: context2.node.id,
        handler: context2.node.handler,
        host: "repo_native_mobile_scaffold",
        startedAt,
        finishedAt: options.now().toISOString(),
        status: "failed",
        stack: rail.mobileStack,
        error: options.redactor.redactText(error instanceof Error ? error.message : String(error))
      },
      options.redactor
    );
    throw new WorkflowExecutionError(
      "MOBILE_SCAFFOLD_FAILED",
      `Mobile scaffold generation failed; inspect ${evidenceArtifact2}.`
    );
  }
  const verifiedFiles = [result2.manifestPath, ...result2.manifest.files.map(({ path }) => path)];
  verifiedChangedFiles(options.rootDir, verifiedFiles);
  const output = {
    status: "completed",
    summary: result2.createdFiles.length > 0 ? `Created the ${result2.manifest.stack} local prototype scaffold without overwriting existing files.` : `Verified the existing ${result2.manifest.stack} local prototype scaffold without changing files.`,
    changedFiles: result2.createdFiles,
    unchangedFiles: result2.unchangedFiles,
    checks: [
      {
        command: "repo-native mobile scaffold schema, create-only preflight, and hash read-back",
        status: "passed",
        evidence: result2.manifestPath
      }
    ],
    limitations: result2.manifest.limitations,
    host: "repo_native_mobile_scaffold",
    scaffold: {
      stack: result2.manifest.stack,
      outputDirectory: result2.manifest.outputDirectory,
      manifestPath: result2.manifestPath,
      identity: result2.manifest.identity,
      safeguards: result2.manifest.safeguards
    }
  };
  const evidenceArtifact = persistEvidence(
    options.rootDir,
    context2,
    {
      schemaVersion: 1,
      runId: context2.runId,
      nodeId: context2.node.id,
      handler: context2.node.handler,
      host: "repo_native_mobile_scaffold",
      startedAt,
      finishedAt: options.now().toISOString(),
      result: output,
      verifiedChangedFiles: verifiedFiles,
      rawPromptPersisted: false,
      rawJsonlPersisted: false
    },
    options.redactor
  );
  context2.trace({
    host: "repo_native_mobile_scaffold",
    evidenceArtifact,
    stack: result2.manifest.stack
  });
  return { output, effectVerified: true, evidenceArtifact };
}
async function runAgentTask(options, instructions, context2) {
  const startedAt = options.now().toISOString();
  const handler = context2.node.handler ?? context2.node.id;
  const policy = COMPLETION_POLICIES[handler];
  const before = repositorySnapshot(options.rootDir);
  let result2;
  try {
    result2 = options.redactor.redact(
      await options.agentHost.run({
        runId: context2.runId,
        nodeId: context2.node.id,
        purpose: context2.node.purpose,
        instructions: taskInstructions(handler, instructions),
        context: {
          brief: options.brief,
          node: {
            id: context2.node.id,
            capability: context2.node.capability,
            effect: context2.node.effect,
            completion: context2.node.completion.description
          },
          completionEvidence: policy ? {
            outcomes: ["changed", "already_compliant"],
            requiredArtifactRoles: policy.requiredArtifactRoles,
            validator: "one relevant direct passed check with observed evidence"
          } : null,
          dependencyOutputs: context2.dependencyOutputs
        },
        signal: context2.signal
      })
    );
  } catch (error) {
    const changes = repositoryChanges(before, repositorySnapshot(options.rootDir));
    const evidenceArtifact2 = persistEvidence(
      options.rootDir,
      context2,
      {
        schemaVersion: 1,
        runId: context2.runId,
        nodeId: context2.node.id,
        handler: context2.node.handler,
        host: options.agentHost.id,
        startedAt,
        finishedAt: options.now().toISOString(),
        status: "failed",
        repositoryChanges: changes,
        error: options.redactor.redactText(error instanceof Error ? error.message : String(error)),
        rawPromptPersisted: false,
        rawJsonlPersisted: false
      },
      options.redactor
    );
    throw new WorkflowExecutionError(
      "BUILD_AGENT_FAILED",
      `Build agent failed ${context2.node.id}; inspect ${evidenceArtifact2}.`
    );
  }
  const after = repositorySnapshot(options.rootDir);
  const observedChanges = repositoryChanges(before, after);
  if (result2.status === "blocked" || result2.checks.some((check) => check.status === "failed")) {
    const evidenceArtifact2 = persistEvidence(
      options.rootDir,
      context2,
      {
        schemaVersion: 1,
        runId: context2.runId,
        nodeId: context2.node.id,
        handler: context2.node.handler,
        host: options.agentHost.id,
        startedAt,
        finishedAt: options.now().toISOString(),
        status: "blocked",
        result: hostOutput(result2),
        repositoryChanges: observedChanges,
        eventTypes: result2.eventTypes,
        rawPromptPersisted: false,
        rawJsonlPersisted: false
      },
      options.redactor
    );
    context2.trace({ host: options.agentHost.id, evidenceArtifact: evidenceArtifact2, status: result2.status });
    throw new WorkflowExecutionError(
      "BUILD_AGENT_BLOCKED",
      `Build agent did not complete ${context2.node.id}; inspect ${evidenceArtifact2}.`
    );
  }
  let validation;
  try {
    validation = validateAgentCompletion(options.rootDir, handler, result2, before, after);
  } catch (error) {
    const evidenceArtifact2 = persistEvidence(
      options.rootDir,
      context2,
      {
        schemaVersion: 1,
        runId: context2.runId,
        nodeId: context2.node.id,
        handler: context2.node.handler,
        host: options.agentHost.id,
        startedAt,
        finishedAt: options.now().toISOString(),
        status: "invalid_evidence",
        result: result2,
        repositoryChanges: observedChanges,
        error: options.redactor.redactText(error instanceof Error ? error.message : String(error)),
        rawPromptPersisted: false,
        rawJsonlPersisted: false
      },
      options.redactor
    );
    throw new WorkflowExecutionError(
      "BUILD_AGENT_EVIDENCE_INVALID",
      `Build agent evidence for ${context2.node.id} was invalid; inspect ${evidenceArtifact2}.`
    );
  }
  const normalizedResult = {
    ...result2,
    changedFiles: validation.changedFiles,
    completion: result2.completion ? { ...result2.completion, artifacts: validation.artifacts } : null
  };
  const output = hostOutput(normalizedResult);
  const evidenceArtifact = persistEvidence(
    options.rootDir,
    context2,
    {
      schemaVersion: 1,
      runId: context2.runId,
      nodeId: context2.node.id,
      handler: context2.node.handler,
      host: options.agentHost.id,
      startedAt,
      finishedAt: options.now().toISOString(),
      result: output,
      eventTypes: result2.eventTypes,
      verifiedChangedFiles: validation.repositoryChanges,
      verifiedCompletionArtifacts: validation.artifacts,
      completionValidator: validation.validator,
      rawPromptPersisted: false,
      rawJsonlPersisted: false
    },
    options.redactor
  );
  context2.trace({ host: options.agentHost.id, evidenceArtifact, status: result2.status });
  const meteredTokens = result2.usage ? result2.usage.inputTokens + result2.usage.outputTokens : 0;
  return {
    output,
    effectVerified: true,
    evidenceArtifact,
    ...context2.node.cost.unit === "tokens" && meteredTokens > 0 ? {
      costs: [
        {
          kind: "model",
          category: context2.node.budgetCategory,
          amount: meteredTokens,
          unit: "tokens",
          inputTokens: result2.usage.inputTokens,
          outputTokens: result2.usage.outputTokens,
          tool: options.agentHost.id,
          metadata: { cachedInputTokens: result2.usage.cachedInputTokens }
        }
      ]
    } : {}
  };
}
async function runQualityCommand(options, args, context2) {
  const startedAt = options.now().toISOString();
  const result2 = await options.commandRunner.run({
    command: "pnpm",
    args,
    cwd: options.rootDir,
    signal: context2.signal
  });
  const evidenceArtifact = persistEvidence(
    options.rootDir,
    context2,
    {
      schemaVersion: 1,
      runId: context2.runId,
      nodeId: context2.node.id,
      handler: context2.node.handler,
      startedAt,
      finishedAt: options.now().toISOString(),
      command: ["pnpm", ...args],
      exitCode: result2.exitCode,
      stdoutExcerpt: options.redactor.redactText(result2.stdout).slice(-4e3),
      stderrExcerpt: options.redactor.redactText(result2.stderr).slice(-4e3)
    },
    options.redactor
  );
  context2.trace({ command: ["pnpm", ...args], exitCode: result2.exitCode, evidenceArtifact });
  if (result2.exitCode !== 0) {
    throw new WorkflowExecutionError(
      "QUALITY_CHECK_FAILED",
      `${["pnpm", ...args].join(" ")} exited ${result2.exitCode}; inspect ${evidenceArtifact}.`
    );
  }
  return {
    output: { command: ["pnpm", ...args], exitCode: result2.exitCode },
    evidenceArtifact
  };
}
async function runDependencyInstall(options, context2) {
  const startedAt = options.now().toISOString();
  const packagePath = inside5(options.rootDir, "package.json");
  const lockPath = inside5(options.rootDir, "pnpm-lock.yaml");
  let result2 = null;
  let invocationError = null;
  let packageManifestSha256 = null;
  let lockfileSha256 = null;
  let installedModulesReadBack = false;
  let installedLockfileReadBack = false;
  let requiredToolingReadBack = false;
  let checkpoint = null;
  try {
    if (!existsSync9(packagePath) || !lstatSync7(packagePath).isFile()) {
      throw new Error("package.json is missing or is not a regular file");
    }
    if (!existsSync9(lockPath) || !lstatSync7(lockPath).isFile()) {
      throw new Error("pnpm-lock.yaml is missing or is not a regular file");
    }
    packageManifestSha256 = sha2564(packagePath);
    lockfileSha256 = sha2564(lockPath);
    checkpoint = {
      schemaVersion: DEPENDENCY_INSTALL_CHECKPOINT_SCHEMA_VERSION,
      packageManifest: "package.json",
      packageManifestSha256,
      lockfile: "pnpm-lock.yaml",
      lockfileSha256
    };
    if (options.checkpointOperation) {
      context2.checkpointOperation?.(checkpoint);
    }
    result2 = await options.commandRunner.run({
      command: "pnpm",
      args: CHILD_DEPENDENCY_INSTALL_ARGS,
      cwd: options.rootDir,
      signal: context2.signal
    });
    if (result2.exitCode === 0) {
      const readBack = readDependencyInstallState(options.rootDir, checkpoint);
      installedModulesReadBack = readBack.installedModulesReadBack;
      installedLockfileReadBack = readBack.installedLockfileReadBack;
      requiredToolingReadBack = readBack.requiredToolingReadBack;
      if (readBack.state === "input_mismatch") {
        throw new Error(readBack.message ?? "the frozen dependency inputs changed");
      }
      if (readBack.state !== "verified") {
        throw new Error(
          "pnpm exited successfully but the exact locked modules and required development tools were absent on read-back"
        );
      }
    }
  } catch (error) {
    invocationError = options.redactor.redactText(
      error instanceof Error ? error.message : String(error)
    );
  }
  const evidenceArtifact = persistEvidence(
    options.rootDir,
    context2,
    {
      schemaVersion: 1,
      runId: context2.runId,
      nodeId: context2.node.id,
      handler: context2.node.handler,
      startedAt,
      finishedAt: options.now().toISOString(),
      command: ["pnpm", ...CHILD_DEPENDENCY_INSTALL_ARGS],
      packageManifest: "package.json",
      packageManifestSha256,
      lockfile: "pnpm-lock.yaml",
      lockfileSha256,
      frozenLockfile: true,
      parentWorkspaceIgnored: true,
      lifecycleScriptsDisabled: true,
      installedModulesReadBack,
      installedLockfileReadBack,
      requiredToolingReadBack,
      exitCode: result2?.exitCode ?? null,
      stdoutExcerpt: options.redactor.redactText(result2?.stdout ?? "").slice(-4e3),
      stderrExcerpt: options.redactor.redactText(result2?.stderr ?? "").slice(-4e3),
      invocationError
    },
    options.redactor
  );
  context2.trace({
    command: ["pnpm", ...CHILD_DEPENDENCY_INSTALL_ARGS],
    exitCode: result2?.exitCode ?? null,
    evidenceArtifact
  });
  if (!result2 || result2.exitCode !== 0 || invocationError) {
    const reason = invocationError ?? `pnpm exited ${result2?.exitCode ?? "without a result"}`;
    if (checkpoint && options.waitOnFailure && context2.checkpointOperation) {
      return {
        wait: {
          kind: "external",
          reason: `Frozen child dependency install failed (${reason}); repair local package-manager or registry availability, then resume the same run. Inspect ${evidenceArtifact}.`
        },
        evidenceArtifact
      };
    }
    throw new WorkflowExecutionError(
      "DEPENDENCY_INSTALL_FAILED",
      `Frozen child dependency install failed (${reason}); inspect ${evidenceArtifact}. No source publish or deployment node can start from this failed dependency.`
    );
  }
  return {
    output: {
      command: ["pnpm", ...CHILD_DEPENDENCY_INSTALL_ARGS],
      exitCode: 0,
      packageManifest: "package.json",
      packageManifestSha256,
      lockfile: "pnpm-lock.yaml",
      lockfileSha256,
      frozenLockfile: true,
      installedModulesReadBack: true,
      installedLockfileReadBack: true,
      requiredToolingReadBack: true,
      lifecycleScriptsDisabled: true
    },
    effectVerified: true,
    evidenceArtifact
  };
}
function persistDependencyReconciliationEvidence(root, context2, readBack, redactor, now) {
  const paths = dependencyReconciliationEvidencePaths(root, context2.runId, context2.node.id);
  writeJsonAtomic(
    paths.absolute,
    redactor.redact({
      runId: context2.runId,
      nodeId: context2.node.id,
      handler: context2.node.handler,
      reason: context2.reason,
      observedAt: now().toISOString(),
      ...readBack
    })
  );
  const persisted = JSON.parse(readFileSync11(paths.absolute, "utf8"));
  if (persisted.runId !== context2.runId || persisted.nodeId !== context2.node.id || persisted.state !== readBack.state) {
    throw new WorkflowExecutionError(
      "DEPENDENCY_RECONCILIATION_EVIDENCE_INVALID",
      "Dependency reconciliation evidence failed atomic read-back."
    );
  }
  return paths.reference;
}
function reconcileDependencyInstall(options, context2) {
  const checkpoint = dependencyInstallCheckpoint(context2.operation.checkpoint);
  if (!checkpoint) {
    return {
      status: "failed",
      code: "DEPENDENCY_CHECKPOINT_INVALID",
      message: "The dependency operation has no valid immutable package/lock checkpoint; start a deliberate new launch instead of guessing at mutable local state."
    };
  }
  const readBack = readDependencyInstallState(options.rootDir, checkpoint);
  const evidenceArtifact = persistDependencyReconciliationEvidence(
    options.rootDir,
    context2,
    readBack,
    options.redactor,
    options.now
  );
  context2.trace({
    dependencyReconciliation: readBack.state,
    evidenceArtifact
  });
  if (readBack.state === "input_mismatch") {
    return {
      status: "failed",
      code: "DEPENDENCY_INPUT_CHANGED",
      message: readBack.message ?? "The package manifest or lockfile no longer matches the checkpointed install inputs."
    };
  }
  if (readBack.state === "not_applied") return { status: "not_applied" };
  return {
    status: "verified",
    output: {
      command: ["pnpm", ...CHILD_DEPENDENCY_INSTALL_ARGS],
      exitCode: 0,
      packageManifest: checkpoint.packageManifest,
      packageManifestSha256: checkpoint.packageManifestSha256,
      lockfile: checkpoint.lockfile,
      lockfileSha256: checkpoint.lockfileSha256,
      frozenLockfile: true,
      installedModulesReadBack: true,
      installedLockfileReadBack: true,
      requiredToolingReadBack: true,
      lifecycleScriptsDisabled: true,
      reconciled: true
    },
    evidenceArtifact
  };
}
function dependencyCheckpointForHandler(root, context2) {
  for (const nodeId of ["finalize-dependencies", "install-dependencies"]) {
    const direct = context2.dependencyOutputs[nodeId];
    if (direct && !Array.isArray(direct) && typeof direct === "object") {
      const checkpoint = dependencyInstallCheckpoint({
        schemaVersion: DEPENDENCY_INSTALL_CHECKPOINT_SCHEMA_VERSION,
        packageManifest: direct.packageManifest,
        packageManifestSha256: direct.packageManifestSha256,
        lockfile: direct.lockfile,
        lockfileSha256: direct.lockfileSha256
      });
      return { checkpoint, expected: true, nodeId };
    }
  }
  for (const nodeId of ["finalize-dependencies", "install-dependencies"]) {
    const paths = dependencyInstallEvidencePaths(root, context2.runId, nodeId);
    if (existsSync9(paths.absolute)) {
      return {
        checkpoint: checkpointFromInstallEvidence(root, context2.runId, nodeId),
        expected: true,
        nodeId
      };
    }
  }
  return { checkpoint: null, expected: false, nodeId: "install-dependencies" };
}
async function ensureCurrentDependencyInstall(options, context2) {
  const selected = dependencyCheckpointForHandler(options.rootDir, context2);
  if (!selected.expected) return;
  if (!selected.checkpoint) {
    throw new WorkflowExecutionError(
      "DEPENDENCY_CHECKPOINT_INVALID",
      "The prior dependency-install evidence is missing or malformed; no product or quality command was started.",
      { details: { effectOutcome: "confirmed_no_write" } }
    );
  }
  const readBack = readDependencyInstallState(options.rootDir, selected.checkpoint);
  if (readBack.state === "input_mismatch") {
    throw new WorkflowExecutionError(
      "DEPENDENCY_INPUT_CHANGED",
      `${readBack.message ?? "Dependency inputs changed."} No product or quality command was started.`,
      { details: { effectOutcome: "confirmed_no_write" } }
    );
  }
  if (readBack.state === "verified") return;
  const installContext = {
    ...context2,
    node: {
      ...context2.node,
      id: selected.nodeId,
      handler: "launch.installDependencies",
      effect: "local_write"
    },
    dependencyOutputs: {},
    idempotencyKey: `${context2.runId}:${selected.nodeId}:repair`
  };
  try {
    await runDependencyInstall(
      {
        ...options,
        checkpointOperation: false,
        waitOnFailure: false
      },
      installContext
    );
  } catch (error) {
    throw new WorkflowExecutionError(
      "DEPENDENCY_REPAIR_FAILED",
      `The checkpointed child dependencies were absent and the bounded reinstall failed; no product or quality command was started. ${error instanceof Error ? error.message : String(error)}`,
      { details: { effectOutcome: "confirmed_no_write" } }
    );
  }
}
function productionDeploymentUrl(context2) {
  const dependency = context2.dependencyOutputs["production-deploy"];
  if (!dependency || typeof dependency !== "object" || Array.isArray(dependency)) {
    throw new WorkflowExecutionError(
      "POST_DEPLOY_URL_MISSING",
      "Production verification requires the verified production-deploy output."
    );
  }
  const resourceRefs = dependency.resourceRefs;
  if (!Array.isArray(resourceRefs)) {
    throw new WorkflowExecutionError(
      "POST_DEPLOY_URL_MISSING",
      "Production deployment read-back did not expose an allowlisted URL resource reference."
    );
  }
  const origins = /* @__PURE__ */ new Set();
  for (const reference of resourceRefs) {
    if (typeof reference !== "string" || !reference.startsWith("url=")) continue;
    try {
      const url = new URL(reference.slice("url=".length));
      const privateIpv4 = /^(?:10\.|127\.|169\.254\.|192\.168\.|172\.(?:1[6-9]|2\d|3[01])\.)/.test(
        url.hostname
      );
      if (url.protocol !== "https:" || url.username || url.password || url.port || privateIpv4 || url.hostname === "localhost" || url.hostname === "::1" || url.hostname.endsWith(".local")) {
        continue;
      }
      origins.add(url.origin);
    } catch {
    }
  }
  if (origins.size !== 1) {
    throw new WorkflowExecutionError(
      "POST_DEPLOY_URL_AMBIGUOUS",
      `Production deployment read-back must provide one exact safe HTTPS origin; found ${origins.size}.`
    );
  }
  return [...origins][0];
}
async function runPostDeployVerification(options, context2) {
  const startedAt = options.now().toISOString();
  const deploymentUrl = productionDeploymentUrl(context2);
  const result2 = await options.commandRunner.run({
    command: "pnpm",
    args: POST_DEPLOY_TEST_ARGS,
    cwd: options.rootDir,
    env: {
      PLAYWRIGHT_BASE_URL: deploymentUrl,
      ...options.brief.domain ? { EXPECTED_PUBLIC_ORIGIN: `https://${options.brief.domain}` } : {}
    },
    signal: context2.signal
  });
  const evidenceArtifact = persistEvidence(
    options.rootDir,
    context2,
    {
      schemaVersion: 1,
      runId: context2.runId,
      nodeId: context2.node.id,
      handler: context2.node.handler,
      startedAt,
      finishedAt: options.now().toISOString(),
      deploymentUrl,
      command: ["pnpm", ...POST_DEPLOY_TEST_ARGS],
      checks: ["HTTPS response", "desktop read-only journey", "mobile read-only journey"],
      exitCode: result2.exitCode,
      stdoutExcerpt: options.redactor.redactText(result2.stdout).slice(-4e3),
      stderrExcerpt: options.redactor.redactText(result2.stderr).slice(-4e3),
      limitations: [
        "This is a read-only post-deploy smoke and critical-surface check; it does not prove provider uptime or conversion behavior."
      ]
    },
    options.redactor
  );
  context2.trace({
    command: ["pnpm", ...POST_DEPLOY_TEST_ARGS],
    deploymentUrl,
    exitCode: result2.exitCode,
    evidenceArtifact
  });
  if (result2.exitCode !== 0) {
    throw new WorkflowExecutionError(
      "POST_DEPLOY_VERIFICATION_FAILED",
      `Read-only production journey checks exited ${result2.exitCode}; inspect ${evidenceArtifact}.`
    );
  }
  return {
    output: {
      deploymentUrl,
      command: ["pnpm", ...POST_DEPLOY_TEST_ARGS],
      exitCode: 0,
      verified: true
    },
    evidenceArtifact
  };
}
async function assertBuildAgentHostAvailable(host) {
  const inspection = await host.inspect();
  if (inspection.status !== "available") {
    throw new WorkflowExecutionError(
      "BUILD_AGENT_UNAVAILABLE",
      `${inspection.nextAction ?? `${inspection.host} is unavailable.`} No run or external action was created.`
    );
  }
}
function createLaunchProductBindings(options) {
  const rootDir = resolve16(options.rootDir);
  const redactor = options.redactor ?? new Redactor();
  const now = options.now ?? (() => /* @__PURE__ */ new Date());
  const handlers = {};
  const reconcilers = {};
  const rail = routeRail(options.brief);
  for (const [handler, instructions] of Object.entries(AGENT_TASKS)) {
    handlers[handler] = async (context2) => {
      if (rail.appKind === "web") {
        await ensureCurrentDependencyInstall(
          { rootDir, commandRunner: options.commandRunner, redactor, now },
          context2
        );
      }
      return runAgentTask(
        { rootDir, brief: options.brief, agentHost: options.agentHost, redactor, now },
        instructions,
        context2
      );
    };
  }
  handlers["launch.installDependencies"] = (context2) => runDependencyInstall(
    {
      rootDir,
      commandRunner: options.commandRunner,
      redactor,
      now,
      checkpointOperation: true,
      waitOnFailure: true
    },
    context2
  );
  reconcilers["launch.installDependencies"] = (context2) => reconcileDependencyInstall({ rootDir, redactor, now }, context2);
  if (rail.mobileStack !== "none") {
    handlers["launch.prepareRepository"] = (context2) => runMobileScaffoldTask(
      {
        rootDir,
        brief: options.brief,
        redactor,
        now,
        mobileScaffold: options.mobileScaffold
      },
      context2
    );
  }
  for (const [handler, args] of Object.entries(QUALITY_COMMANDS)) {
    handlers[handler] = async (context2) => {
      if (rail.appKind === "web") {
        await ensureCurrentDependencyInstall(
          { rootDir, commandRunner: options.commandRunner, redactor, now },
          context2
        );
      }
      return runQualityCommand(
        { rootDir, commandRunner: options.commandRunner, redactor, now },
        args,
        context2
      );
    };
  }
  handlers["launch.verifyProduction"] = async (context2) => {
    if (rail.appKind === "web") {
      await ensureCurrentDependencyInstall(
        { rootDir, commandRunner: options.commandRunner, redactor, now },
        context2
      );
    }
    return runPostDeployVerification(
      { rootDir, commandRunner: options.commandRunner, brief: options.brief, redactor, now },
      context2
    );
  };
  return { handlers, reconcilers };
}

// lib/runtime/native-http-fetcher.ts
import { request as httpsRequest } from "node:https";

// lib/security/outbound-http.ts
import { lookup } from "node:dns/promises";
import { isIP } from "node:net";
var OFFICIAL_PROVIDER_HTTP_HOSTS = Object.freeze([
  "analyticsadmin.googleapis.com",
  "analyticsdata.googleapis.com",
  "api.appstoreconnect.apple.com",
  "api.brevo.com",
  "api.revenuecat.com",
  "api.stripe.com",
  "app.revenuecat.com",
  "appstoreconnect.apple.com",
  "mijn.mijndomein.nl",
  "searchconsole.googleapis.com",
  "ssl.bing.com",
  "vercel.com",
  "www.googleapis.com"
]);
function parseIpv4(address) {
  const parts = address.split(".");
  if (parts.length !== 4 || parts.some((part) => !/^\d{1,3}$/.test(part))) return null;
  const octets = parts.map(Number);
  return octets.every((part) => part >= 0 && part <= 255) ? octets : null;
}
function parseIpv6(address) {
  let candidate = address.toLowerCase().replace(/^\[|\]$/g, "");
  if (candidate.includes("%") || (candidate.match(/::/g) ?? []).length > 1) return null;
  const ipv4Tail = candidate.match(/(?:^|:)(\d{1,3}(?:\.\d{1,3}){3})$/)?.[1];
  if (ipv4Tail) {
    const octets = parseIpv4(ipv4Tail);
    if (!octets) return null;
    candidate = candidate.slice(0, -ipv4Tail.length) + `${(octets[0] << 8 | octets[1]).toString(16)}:${(octets[2] << 8 | octets[3]).toString(16)}`;
  }
  const [leftRaw, rightRaw] = candidate.split("::");
  const left = leftRaw ? leftRaw.split(":") : [];
  const right = rightRaw ? rightRaw.split(":") : [];
  const missing = candidate.includes("::") ? 8 - left.length - right.length : 0;
  const groups = [...left, ...Array.from({ length: missing }, () => "0"), ...right];
  if (groups.length !== 8 || missing < 0 || groups.some((group) => !/^[a-f0-9]{1,4}$/.test(group))) {
    return null;
  }
  return groups.map((group) => Number.parseInt(group, 16));
}
function isPublicInternetAddress(address) {
  const ipv4 = parseIpv4(address);
  if (ipv4) {
    const [a, b, c] = ipv4;
    return !(a === 0 || a === 10 || a === 127 || a === 100 && b >= 64 && b <= 127 || a === 169 && b === 254 || a === 172 && b >= 16 && b <= 31 || a === 192 && b === 0 && (c === 0 || c === 2) || a === 192 && b === 88 && c === 99 || a === 192 && b === 168 || a === 198 && (b === 18 || b === 19) || a === 198 && b === 51 && c === 100 || a === 203 && b === 0 && c === 113 || a >= 224);
  }
  const ipv6 = parseIpv6(address);
  if (!ipv6) return false;
  const [first = 0, second = 0] = ipv6;
  if (ipv6.every((group) => group === 0)) return false;
  if (ipv6.slice(0, 7).every((group) => group === 0) && ipv6[7] === 1) return false;
  if ((first & 65024) === 64512) return false;
  if ((first & 65472) === 65152) return false;
  if ((first & 65280) === 65280) return false;
  if (first === 256 && second === 0) return false;
  if (first === 100 && second === 65435) return false;
  if (first === 8194 || first === 16383) return false;
  if (first === 8193 && (second === 0 || second === 3512)) return false;
  if (first === 8193 && second >= 32 && second <= 47) return false;
  if (ipv6.slice(0, 5).every((group) => group === 0) && [0, 65535].includes(ipv6[5])) {
    const embedded = [ipv6[6] >> 8, ipv6[6] & 255, ipv6[7] >> 8, ipv6[7] & 255].join(".");
    return isPublicInternetAddress(embedded);
  }
  return true;
}
var systemResolver = async (hostname) => (await lookup(hostname, { all: true, verbatim: true })).map(({ address }) => address);
async function validateProviderUrl(raw, policy = {}) {
  let url;
  try {
    url = new URL(raw);
  } catch {
    throw new Error("Provider URL must be an absolute HTTPS URL");
  }
  if (url.protocol !== "https:") throw new Error("Provider URL must use HTTPS");
  if (url.username || url.password) throw new Error("Provider URL must not contain credentials");
  if (url.hash) throw new Error("Provider URL must not contain a fragment");
  if (url.port && url.port !== "443") throw new Error("Provider URL must use the HTTPS port");
  const hostname = url.hostname.toLowerCase();
  const allowed = new Set(
    (policy.allowedHosts ?? OFFICIAL_PROVIDER_HTTP_HOSTS).map((host) => host.toLowerCase())
  );
  if (!allowed.has(hostname)) throw new Error("Provider URL host is not allowlisted");
  if (isIP(hostname)) throw new Error("Provider URL must use an allowlisted DNS hostname");
  const resolved = await (policy.resolveHost ?? systemResolver)(hostname);
  if (resolved.length === 0) throw new Error("Provider URL resolved to no addresses");
  const unique2 = [...new Set(resolved)];
  const addresses = unique2.map((address) => {
    const family = isIP(address);
    if (family !== 4 && family !== 6 || !isPublicInternetAddress(address)) {
      throw new Error("Provider URL resolved to a non-public address");
    }
    return { address, family };
  });
  return Object.freeze({ url, addresses: Object.freeze(addresses) });
}

// lib/security/oauth.ts
var MAX_TRANSACTION_AGE_MS = 10 * 60 * 1e3;

// lib/security/uploads.ts
var DEFAULT_UPLOAD_LIMIT_BYTES = 10 * 1024 * 1024;
var MIME_SIGNATURES = Object.freeze({
  "image/png": (bytes) => [137, 80, 78, 71, 13, 10, 26, 10].every(
    (value, index) => bytes[index] === value
  ),
  "image/jpeg": (bytes) => bytes[0] === 255 && bytes[1] === 216 && bytes[2] === 255,
  "image/gif": (bytes) => ["GIF87a", "GIF89a"].includes(Buffer.from(bytes.slice(0, 6)).toString("ascii")),
  "image/webp": (bytes) => Buffer.from(bytes.slice(0, 4)).toString("ascii") === "RIFF" && Buffer.from(bytes.slice(8, 12)).toString("ascii") === "WEBP",
  "application/pdf": (bytes) => Buffer.from(bytes.slice(0, 5)).toString("ascii") === "%PDF-",
  "video/mp4": (bytes) => Buffer.from(bytes.slice(4, 8)).toString("ascii") === "ftyp",
  "application/json": (bytes) => {
    try {
      JSON.parse(new TextDecoder("utf-8", { fatal: true }).decode(bytes));
      return true;
    } catch {
      return false;
    }
  },
  "text/plain": (bytes) => {
    try {
      const text2 = new TextDecoder("utf-8", { fatal: true }).decode(bytes);
      return !text2.includes("\0");
    } catch {
      return false;
    }
  }
});

// lib/runtime/native-http-fetcher.ts
var ALWAYS_SENSITIVE_HEADERS = /* @__PURE__ */ new Set([
  "authorization",
  "cookie",
  "proxy-authorization",
  "set-cookie",
  "x-api-key"
]);
function safeUrl(url, sensitive, redactor) {
  if (sensitive) {
    try {
      const parsed = new URL(url);
      if (parsed.username) parsed.username = "[REDACTED]";
      if (parsed.password) parsed.password = "[REDACTED]";
      for (const key of [...parsed.searchParams.keys()]) {
        parsed.searchParams.set(key, "[REDACTED]");
      }
      return parsed.toString();
    } catch {
      return "[REDACTED URL]";
    }
  }
  return redactor.redactText(url);
}
function safeHeaders(headers, sensitiveHeaders, redactor) {
  const sensitive = new Set(sensitiveHeaders.map((name) => name.toLowerCase()));
  return Object.fromEntries(
    Object.entries(headers).map(([name, value]) => [
      name,
      sensitive.has(name.toLowerCase()) || ALWAYS_SENSITIVE_HEADERS.has(name.toLowerCase()) ? "[REDACTED]" : redactor.redactText(value)
    ])
  );
}
function responseHeaders(headers) {
  const safe = {};
  for (const name of [
    "content-type",
    "retry-after",
    "x-ratelimit-limit",
    "x-ratelimit-remaining",
    "x-ratelimit-reset"
  ]) {
    const value = headers.get(name);
    if (value !== null) safe[name] = value;
  }
  return safe;
}
function withoutCrossHostSecrets(headers, sensitiveHeaders) {
  const sensitive = /* @__PURE__ */ new Set([
    ...ALWAYS_SENSITIVE_HEADERS,
    ...sensitiveHeaders.map((name) => name.toLowerCase())
  ]);
  return Object.fromEntries(
    Object.entries(headers).filter(([name]) => !sensitive.has(name.toLowerCase()))
  );
}
var NativeHttpFetcher = class {
  fetchImpl;
  redactor;
  onRequest;
  maxResponseBytes;
  maxRedirects;
  timeoutMs;
  allowedHosts;
  resolveHost;
  constructor(options = {}) {
    this.fetchImpl = options.fetch;
    this.redactor = options.redactor ?? new Redactor();
    this.onRequest = options.onRequest;
    this.maxResponseBytes = options.maxResponseBytes ?? 2 * 1024 * 1024;
    this.maxRedirects = options.maxRedirects ?? 3;
    this.timeoutMs = options.timeoutMs ?? 3e4;
    this.allowedHosts = options.allowedHosts;
    this.resolveHost = options.resolveHost;
    if (!Number.isSafeInteger(this.maxResponseBytes) || this.maxResponseBytes < 1) {
      throw new Error("Native HTTP response limit is invalid");
    }
    if (!Number.isSafeInteger(this.maxRedirects) || this.maxRedirects < 0) {
      throw new Error("Native HTTP redirect limit is invalid");
    }
    if (!Number.isSafeInteger(this.timeoutMs) || this.timeoutMs < 1) {
      throw new Error("Native HTTP timeout is invalid");
    }
  }
  async requestOnce(request2, validated, headers, body) {
    if (this.fetchImpl) {
      const response = await this.fetchImpl(validated.url, {
        method: request2.method,
        headers,
        body,
        signal: request2.signal,
        redirect: "manual"
      });
      const bytes = new Uint8Array(await response.arrayBuffer());
      if (bytes.byteLength > this.maxResponseBytes) {
        throw new Error(`Provider response exceeded ${this.maxResponseBytes} bytes`);
      }
      return { status: response.status, headers: response.headers, bytes };
    }
    const pinned = validated.addresses[0];
    return new Promise((resolve27, reject) => {
      const nativeRequest = httpsRequest(
        validated.url,
        {
          method: request2.method,
          headers,
          signal: request2.signal,
          lookup: (_hostname, _options, callback) => {
            callback(null, pinned.address, pinned.family);
          }
        },
        (response) => {
          const chunks = [];
          let size = 0;
          response.on("data", (chunk) => {
            size += chunk.byteLength;
            if (size > this.maxResponseBytes) {
              response.destroy(
                new Error(`Provider response exceeded ${this.maxResponseBytes} bytes`)
              );
              return;
            }
            chunks.push(Uint8Array.from(chunk));
          });
          response.once("error", reject);
          response.once("end", () => {
            const responseHeaderBag = new Headers();
            for (const [name, value] of Object.entries(response.headers)) {
              if (Array.isArray(value))
                value.forEach((entry) => responseHeaderBag.append(name, entry));
              else if (value !== void 0) responseHeaderBag.set(name, String(value));
            }
            resolve27({
              status: response.statusCode ?? 0,
              headers: responseHeaderBag,
              bytes: Uint8Array.from(Buffer.concat(chunks.map((chunk) => Buffer.from(chunk))))
            });
          });
        }
      );
      nativeRequest.setTimeout(this.timeoutMs, () => {
        nativeRequest.destroy(new Error(`Provider request exceeded ${this.timeoutMs}ms`));
      });
      nativeRequest.once("error", reject);
      if (body !== void 0) nativeRequest.write(body);
      nativeRequest.end();
    });
  }
  async fetch(request2) {
    let validated = await validateProviderUrl(request2.url, {
      allowedHosts: this.allowedHosts,
      resolveHost: this.resolveHost
    });
    const metadata = {
      method: request2.method,
      url: safeUrl(validated.url.href, request2.sensitiveUrl, this.redactor),
      headers: safeHeaders(request2.headers, request2.sensitiveHeaders, this.redactor),
      hasBody: request2.body !== void 0
    };
    this.onRequest?.(metadata);
    let headers = request2.headers;
    let requestBody = request2.body;
    let response = null;
    try {
      for (let redirect = 0; redirect <= this.maxRedirects; redirect++) {
        response = await this.requestOnce(request2, validated, headers, requestBody);
        if (![301, 302, 303, 307, 308].includes(response.status)) break;
        if (redirect === this.maxRedirects) throw new Error("Provider redirect limit exceeded");
        if (request2.method !== "GET") {
          throw new Error("Provider write redirects are forbidden");
        }
        const location = response.headers.get("location");
        if (!location) throw new Error("Provider redirect omitted its target");
        const next = await validateProviderUrl(new URL(location, validated.url), {
          allowedHosts: this.allowedHosts,
          resolveHost: this.resolveHost
        });
        if (next.url.hostname !== validated.url.hostname) {
          headers = withoutCrossHostSecrets(headers, request2.sensitiveHeaders);
        }
        validated = next;
        requestBody = void 0;
      }
    } catch (error) {
      const reason = this.redactor.redactText(
        error instanceof Error ? error.message : String(error)
      );
      throw new Error(`${request2.method} ${metadata.url} failed: ${reason}`);
    }
    if (!response) throw new Error(`${request2.method} ${metadata.url} failed without a response`);
    const text2 = new TextDecoder().decode(response.bytes);
    let parsedBody;
    if (text2.length > 0) {
      try {
        parsedBody = JSON.parse(text2);
      } catch {
        parsedBody = text2;
      }
    }
    return {
      status: response.status,
      headers: responseHeaders(response.headers),
      body: parsedBody
    };
  }
};

// lib/runtime/provider-bindings.ts
import { createHash as createHash13 } from "node:crypto";
var ProviderPlanFactoryPrerequisiteError = class extends Error {
  code = "provider_factory_prerequisite";
  constructor(message) {
    super(message);
    this.name = "ProviderPlanFactoryPrerequisiteError";
  }
};
function createOfficialProviderTransports(options) {
  const transports = [
    options.manualTransport ?? new ManualProviderTransport(),
    ...options.additional ?? []
  ];
  if (options.commandRunner) {
    transports.push(
      new CommandProviderTransport({
        runner: options.commandRunner,
        available: options.commandAvailable
      })
    );
  }
  if (options.httpFetcher) {
    transports.push(
      new HttpProviderTransport(options.httpFetcher, options.httpAvailable, options.jwtSigner)
    );
  }
  return transportMap(...transports);
}
function createOfficialProviderContext(options) {
  return {
    credentials: options.credentials,
    redactor: options.redactor ?? options.credentials?.redactor ?? new Redactor(),
    idempotencyLedger: options.idempotencyLedger,
    transports: createOfficialProviderTransports(options)
  };
}
function mergeContext(base, override) {
  return {
    ...base,
    ...override,
    transports: { ...base.transports, ...override?.transports },
    redactor: override?.redactor ?? base.redactor
  };
}
var SAFE_PROVIDER_FAILURE_CODES = /* @__PURE__ */ new Set([
  "retryable_rate_limit",
  "retryable_network",
  "retryable_outage",
  "terminal_auth",
  "terminal_validation",
  "terminal_conflict",
  "terminal_unknown",
  "transport_exception",
  "shell_binary_forbidden",
  "jwt_signer_missing",
  "jwt_signing_failed",
  "timeout"
]);
function safeFailureCode(code) {
  if (!code) return void 0;
  return SAFE_PROVIDER_FAILURE_CODES.has(code) || /^exit_\d{1,3}$/.test(code) ? code : void 0;
}
function mappedRetryCode(result2) {
  switch (result2.providerCode) {
    case "retryable_rate_limit":
      return "rate_limited";
    case "retryable_network":
    case "retryable_outage":
      return "provider_unavailable";
    default:
      return safeFailureCode(result2.providerCode) ?? "provider_failed";
  }
}
function minimalFailureDetails(provider, results) {
  return {
    provider,
    outcomes: results.map((result2) => ({
      status: result2.status,
      statusCode: result2.statusCode ?? null,
      providerCode: safeFailureCode(result2.providerCode) ?? null,
      retryable: result2.retryable === true,
      effectOutcome: result2.effectOutcome ?? "unknown"
    }))
  };
}
function executionFailure(provider, results) {
  if (results.some(({ status }) => status === "waiting_manual")) {
    return new WorkflowExecutionError(
      "provider_manual_action_required",
      `${provider} requires a declared manual action and cannot be marked successful`,
      { details: minimalFailureDetails(provider, results) }
    );
  }
  const failed = results.filter(({ status }) => status === "failed");
  const retryable = failed.find((result2) => result2.retryable === true);
  if (retryable) {
    return new WorkflowExecutionError(
      mappedRetryCode(retryable),
      `${provider} apply failed with a retryable provider error`,
      { retryable: true, details: minimalFailureDetails(provider, results) }
    );
  }
  return new WorkflowExecutionError(
    "provider_failed",
    `${provider} apply did not complete every operation`,
    { details: minimalFailureDetails(provider, results) }
  );
}
function verificationFailure(provider, verification) {
  const statuses = verification.checks.map(({ status }) => status);
  if (verification.state === "pending" || statuses.includes("manual_required")) {
    return new WorkflowExecutionError(
      "provider_manual_action_required",
      `${provider} verification is pending a declared manual action`,
      { details: { provider, verification: verification.state, statuses } }
    );
  }
  if (verification.state === "unavailable" || statuses.includes("unavailable")) {
    return new WorkflowExecutionError(
      "provider_pending",
      `${provider} apply is awaiting read-back proof`,
      {
        retryable: false,
        details: { provider, verification: verification.state, statuses }
      }
    );
  }
  return new WorkflowExecutionError(
    "provider_verification_failed",
    `${provider} read-back did not match the requested state`,
    { details: { provider, verification: verification.state, statuses } }
  );
}
async function resolveValue(value, ...args) {
  return typeof value === "function" ? value(...args) : value;
}
async function promoteVerifiedOperations(ledger, operations) {
  await Promise.all(
    operations.filter(({ result: result2 }) => result2.status === "succeeded").map(
      ({ operation: operation2, result: result2 }) => ledger.put(operation2.idempotencyKey, {
        status: "succeeded",
        statusCode: result2.statusCode,
        providerCode: result2.providerCode,
        message: "Provider state was verified by read-back",
        retryable: false,
        verified: true
      })
    )
  );
}
var SAFE_RESOURCE_KEYS = {
  accountId: "account_id",
  account_id: "account_id",
  appId: "app_id",
  app_id: "app_id",
  appStoreAppId: "app_id",
  appVersion: "app_version",
  appBuildVersion: "build_number",
  branchId: "branch_id",
  branch_id: "branch_id",
  buildId: "build_id",
  build_id: "build_id",
  buildNumber: "build_number",
  build_number: "build_number",
  bundleId: "bundle_id",
  bundle_id: "bundle_id",
  databaseId: "database_id",
  database_id: "database_id",
  databaseName: "database_name",
  database_name: "database_name",
  deploymentId: "deployment_id",
  deployment_id: "deployment_id",
  domain: "domain",
  entitlementId: "entitlement_id",
  entitlement_id: "entitlement_id",
  measurementId: "measurement_id",
  measurement_id: "measurement_id",
  html_url: "url",
  nameWithOwner: "repository",
  offeringId: "offering_id",
  offering_id: "offering_id",
  priceId: "price_id",
  price_id: "price_id",
  productId: "product_id",
  product_id: "product_id",
  project: "project",
  projectId: "project_id",
  project_id: "project_id",
  projectName: "project_name",
  project_name: "project_name",
  propertyId: "property_id",
  property_id: "property_id",
  repository: "repository",
  repositoryId: "repository_id",
  repository_id: "repository_id",
  region: "region",
  regionId: "region",
  region_id: "region",
  siteUrl: "site_url",
  site_url: "site_url",
  streamId: "stream_id",
  stream_id: "stream_id",
  submissionId: "submission_id",
  submission_id: "submission_id",
  teamId: "team_id",
  team_id: "team_id",
  testflightGroupId: "testflight_group_id",
  testflight_group_id: "testflight_group_id",
  url: "url",
  webhookId: "webhook_id",
  webhook_id: "webhook_id"
};
function safeResourceValue(value, redactor) {
  if (typeof value !== "string" && typeof value !== "number") return null;
  let text2 = String(value).trim();
  if (text2.length === 0 || text2.length > 500 || text2.startsWith("cred://") || text2.includes("[REDACTED]") || looksLikeCredentialValue(text2) || redactor.redactText(text2) !== text2) {
    return null;
  }
  try {
    const url = new URL(text2);
    url.username = "";
    url.password = "";
    url.search = "";
    url.hash = "";
    text2 = url.toString();
  } catch {
  }
  return text2;
}
function collectSafeResourceRefs(value, redactor, refs = /* @__PURE__ */ new Map()) {
  if (Array.isArray(value)) {
    value.forEach((item) => collectSafeResourceRefs(item, redactor, refs));
    return [...refs.values()].sort(
      (left, right) => left.type.localeCompare(right.type) || left.value.localeCompare(right.value)
    );
  }
  if (!value || typeof value !== "object") return [...refs.values()];
  for (const [key, item] of Object.entries(value)) {
    const type = SAFE_RESOURCE_KEYS[key];
    if (type) {
      const safe = safeResourceValue(item, redactor);
      if (safe) refs.set(`${type}\0${safe}`, { type, value: safe });
    }
    if (typeof item === "object" && item !== null) {
      collectSafeResourceRefs(item, redactor, refs);
    }
  }
  return [...refs.values()].sort(
    (left, right) => left.type.localeCompare(right.type) || left.value.localeCompare(right.value)
  ).slice(0, 100);
}
function canonicalJson2(value) {
  if (value === null || typeof value !== "object") return JSON.stringify(value) ?? "null";
  if (Array.isArray(value)) return `[${value.map((item) => canonicalJson2(item)).join(",")}]`;
  return `{${Object.entries(value).filter(([, item]) => item !== void 0).sort(([left], [right]) => left.localeCompare(right)).map(([key, item]) => `${JSON.stringify(key)}:${canonicalJson2(item)}`).join(",")}}`;
}
function providerPlanSnapshot(target, plan) {
  return JSON.parse(
    canonicalJson2({
      target: {
        provider: target.provider,
        request: target.request,
        operationBudget: target.operationBudget
      },
      plan: {
        id: plan.id,
        provider: plan.provider,
        environment: plan.environment,
        dryRun: plan.dryRun,
        operations: plan.operations
      }
    })
  );
}
function snapshotDigest(snapshot) {
  return createHash13("sha256").update(canonicalJson2(snapshot)).digest("hex");
}
function ledgerBinding(identity) {
  return createHash13("sha256").update(identity).digest("hex");
}
function providerPlanCheckpoint(target, plan, ledgerIdentity) {
  const snapshot = providerPlanSnapshot(target, plan);
  return {
    schemaVersion: 2,
    kind: "provider_plan",
    provider: target.provider,
    digest: snapshotDigest(snapshot),
    ledgerBinding: ledgerBinding(ledgerIdentity),
    snapshot
  };
}
function parseProviderPlanCheckpoint(value) {
  if (!value || Array.isArray(value) || typeof value !== "object") return null;
  const candidate = value;
  if (Object.keys(candidate).sort().join(",") !== "digest,kind,ledgerBinding,provider,schemaVersion,snapshot" || candidate.schemaVersion !== 2 || candidate.kind !== "provider_plan" || typeof candidate.provider !== "string" || typeof candidate.digest !== "string" || !/^[a-f0-9]{64}$/u.test(candidate.digest) || typeof candidate.ledgerBinding !== "string" || !/^[a-f0-9]{64}$/u.test(candidate.ledgerBinding) || !candidate.snapshot || Array.isArray(candidate.snapshot) || typeof candidate.snapshot !== "object") {
    return null;
  }
  const snapshot = candidate.snapshot;
  if (!snapshot.target || !snapshot.plan || snapshot.target.provider !== candidate.provider || snapshot.plan.provider !== candidate.provider || !snapshot.target.request || typeof snapshot.target.request !== "object" || typeof snapshot.plan.id !== "string" || typeof snapshot.plan.environment !== "string" || snapshot.plan.dryRun !== false || !Array.isArray(snapshot.plan.operations) || snapshot.plan.operations.length === 0 || snapshot.plan.operations.some(
    (operation2) => !operation2 || typeof operation2 !== "object" || operation2.provider !== candidate.provider || typeof operation2.idempotencyKey !== "string"
  ) || snapshotDigest(snapshot) !== candidate.digest) {
    return null;
  }
  return candidate;
}
function checkpointSnapshot(checkpoint) {
  return checkpoint.snapshot;
}
function checkpointTarget(checkpoint) {
  const { target } = checkpointSnapshot(checkpoint);
  return {
    provider: target.provider,
    request: target.request,
    ...target.operationBudget ? { operationBudget: target.operationBudget } : {}
  };
}
async function prepareProviderPlan(workflow, target, options) {
  const adapter = target.adapter ?? (options.resolveAdapter ?? getProviderAdapter)(target.provider);
  if (adapter.descriptor.id !== target.provider) {
    throw new WorkflowExecutionError(
      "provider_adapter_mismatch",
      `Resolved ${adapter.descriptor.id} adapter for ${target.provider}`
    );
  }
  const base = await resolveValue(options.context, workflow, target);
  const context2 = mergeContext(base, target.context);
  let plan;
  try {
    plan = adapter.plan(target.request);
  } catch (error) {
    throw new WorkflowExecutionError(
      "provider_plan_failed",
      context2.redactor.redactText(error instanceof Error ? error.message : String(error))
    );
  }
  if (target.operationBudget) {
    const { maxOperations, missingCostClassification } = target.operationBudget;
    if (!Number.isInteger(maxOperations) || maxOperations < 1 || missingCostClassification !== void 0 && (!/^[A-Z]{3}$/u.test(missingCostClassification.currency) || missingCostClassification.basis !== "reviewed_known_zero_direct_charge" || missingCostClassification.ongoingAccountPlanUsageCovered !== false || missingCostClassification.allowedOperations.length === 0)) {
      throw new WorkflowExecutionError(
        "provider_budget_binding_invalid",
        `${target.provider} received an invalid immutable operation-budget binding`
      );
    }
    if (plan.operations.length > maxOperations) {
      throw new WorkflowExecutionError(
        "provider_operation_limit_exceeded",
        `${target.provider} planned ${plan.operations.length} operations, exceeding this launch node ceiling of ${maxOperations}`,
        { details: { provider: target.provider, effectOutcome: "confirmed_no_write" } }
      );
    }
    const requestedCapabilities = new Set(target.request.capabilities);
    const operations = plan.operations.map((operation2) => {
      if (operation2.provider !== target.provider || !requestedCapabilities.has(operation2.capability)) {
        throw new WorkflowExecutionError(
          "provider_plan_scope_mismatch",
          `${target.provider} produced an operation outside its exact requested capability scope`,
          { details: { provider: target.provider, effectOutcome: "confirmed_no_write" } }
        );
      }
      if (operation2.estimatedCost) return operation2;
      const reviewed = missingCostClassification?.allowedOperations.some(
        (candidate) => candidate.provider === operation2.provider && candidate.capability === operation2.capability && candidate.action === operation2.action
      );
      if (!reviewed || !missingCostClassification) {
        throw new WorkflowExecutionError(
          "provider_cost_estimate_missing",
          `${target.provider} operation ${operation2.action} has no explicit estimate or reviewed known-zero direct-charge classification`,
          { details: { provider: target.provider, effectOutcome: "confirmed_no_write" } }
        );
      }
      return {
        ...operation2,
        estimatedCost: { amount: 0, currency: missingCostClassification.currency }
      };
    });
    plan = { ...plan, operations };
  }
  if (plan.dryRun) {
    throw new WorkflowExecutionError(
      "provider_plan_only",
      `${target.provider} plan is a dry run; explicit apply input is required`
    );
  }
  if (plan.operations.length === 0) {
    throw new WorkflowExecutionError(
      "provider_plan_empty",
      `${target.provider} plan contains no complete provider operation`
    );
  }
  return { adapter, context: context2, plan };
}
async function durableLedgerIdentity(context2, provider) {
  if (!context2.idempotencyLedger || context2.idempotencyLedger.durability !== "durable_atomic") {
    throw new WorkflowExecutionError(
      "provider_reconciliation_ledger_required",
      `${provider} requires a durable atomic provider idempotency ledger`,
      { details: { provider, effectOutcome: "confirmed_no_write" } }
    );
  }
  try {
    return await context2.idempotencyLedger.identity();
  } catch {
    throw new WorkflowExecutionError(
      "provider_idempotency_ledger_unavailable",
      `${provider} durable idempotency ledger is missing, corrupt, or unavailable`,
      { details: { provider, effectOutcome: "confirmed_no_write" } }
    );
  }
}
async function prepareCheckpointProviderPlan(workflow, checkpoint, options) {
  const target = checkpointTarget(checkpoint);
  const prepared = await prepareProviderPlan(workflow, target, options);
  const reconstructed = providerPlanSnapshot(target, prepared.plan);
  if (snapshotDigest(reconstructed) !== checkpoint.digest) {
    throw new WorkflowExecutionError(
      "provider_reconciliation_target_mismatch",
      `${target.provider} immutable provider-plan snapshot cannot be rebuilt by the trusted adapter`
    );
  }
  return { target, ...prepared };
}
async function lifecycleProvesPlan(plan, options) {
  if (!options.lifecycleStore) return false;
  const scopes = new Map(
    plan.operations.map((operation2) => [
      `${operation2.provider}\0${operation2.environment}\0${operation2.capability}`,
      operation2
    ])
  );
  let records;
  try {
    records = await options.lifecycleStore.list();
  } catch {
    throw new WorkflowExecutionError(
      "provider_lifecycle_state_unavailable",
      `${plan.provider} lifecycle state is unavailable during reconciliation`
    );
  }
  return [...scopes.values()].every(
    (operation2) => records.some(
      (record3) => record3.provider === operation2.provider && record3.environment === operation2.environment && record3.capability === operation2.capability && record3.planId === plan.id
    )
  );
}
async function prepareProviderReconciliation(handler, factory, workflow, checkpoint, options) {
  let currentFailure;
  try {
    const target = await resolveProviderTarget(handler, factory, workflow);
    const prepared = await prepareProviderPlan(workflow, target, options);
    const identity2 = await durableLedgerIdentity(prepared.context, target.provider);
    if (ledgerBinding(identity2) !== checkpoint.ledgerBinding) {
      throw new WorkflowExecutionError(
        "provider_reconciliation_ledger_mismatch",
        `${target.provider} reconciliation resolved a different durable ledger generation`
      );
    }
    const reconstructed = providerPlanCheckpoint(target, prepared.plan, identity2);
    if (reconstructed.provider === checkpoint.provider && reconstructed.digest === checkpoint.digest) {
      return { ...prepared, target, ledgerIdentity: identity2 };
    }
  } catch (error) {
    currentFailure = error;
  }
  const stored = await prepareCheckpointProviderPlan(workflow, checkpoint, options);
  if (!await lifecycleProvesPlan(stored.plan, options)) {
    if (currentFailure) throw currentFailure;
    throw new WorkflowExecutionError(
      "provider_reconciliation_target_mismatch",
      `${checkpoint.provider} reconciliation target does not match the durable immutable plan`
    );
  }
  const identity = await durableLedgerIdentity(stored.context, stored.target.provider);
  if (ledgerBinding(identity) !== checkpoint.ledgerBinding) {
    throw new WorkflowExecutionError(
      "provider_reconciliation_ledger_mismatch",
      `${stored.target.provider} reconciliation resolved a different durable ledger generation`
    );
  }
  return { ...stored, ledgerIdentity: identity };
}
async function verifiedProviderResult(workflow, target, options, context2, plan, report, verification) {
  if (!context2.idempotencyLedger) {
    throw new WorkflowExecutionError(
      "idempotency_ledger_required",
      `${target.provider} verification requires a provider idempotency ledger`
    );
  }
  await promoteVerifiedOperations(context2.idempotencyLedger, report.operations);
  const resourceRefs = collectSafeResourceRefs(
    [
      target.request.inputs,
      report.operations.map(({ result: result2 }) => result2.output),
      verification.checks
    ],
    context2.redactor
  );
  const publicOutputs = collectProviderPublicOutputs({
    provider: target.provider,
    requestInputs: target.request.inputs,
    report,
    verification
  });
  for (const reference of publicOutputs.identifiers) {
    const value = safeResourceValue(reference.value, context2.redactor);
    if (value !== null) resourceRefs.push({ ...reference, value });
  }
  const uniqueResourceRefs = [
    ...new Map(resourceRefs.map((item) => [`${item.type}\0${item.value}`, item])).values()
  ].sort(
    (left, right) => left.type.localeCompare(right.type) || left.value.localeCompare(right.value)
  );
  const evidence = {
    provider: target.provider,
    planId: plan.id,
    state: "verified",
    environments: [...new Set(plan.operations.map(({ environment }) => environment))].sort(),
    capabilities: [...new Set(plan.operations.map(({ capability }) => capability))].sort(),
    operations: plan.operations.map((operation2) => ({
      id: operation2.id,
      action: operation2.action,
      capability: operation2.capability,
      environment: operation2.environment,
      status: "matched"
    })),
    resourceRefs: uniqueResourceRefs.map(({ type, value }) => `${type}=${value}`),
    publicOutputs,
    checks: verification.checks.map(({ operationId: operationId2, status }) => ({ operationId: operationId2, status }))
  };
  const evidenceArtifact = options.recordEvidence ? await options.recordEvidence({ evidence, workflow }) : `provider-readback://${target.provider}/${plan.id}`;
  if (options.lifecycleStore) {
    const verifiedAt = (options.now?.() ?? /* @__PURE__ */ new Date()).toISOString();
    const scopes = /* @__PURE__ */ new Map();
    for (const operation2 of plan.operations) {
      const record3 = {
        provider: target.provider,
        environment: operation2.environment,
        capability: operation2.capability,
        state: "verified",
        planId: plan.id,
        verifiedAt,
        resourceRefs: uniqueResourceRefs
      };
      scopes.set(`${record3.provider}\0${record3.environment}\0${record3.capability}`, record3);
    }
    try {
      await options.lifecycleStore.recordVerified([...scopes.values()]);
    } catch {
      throw new WorkflowExecutionError(
        "provider_lifecycle_persistence_failed",
        `${target.provider} was verified, but its safe lifecycle state could not be persisted; repair the lifecycle store and resume for read-back`,
        {
          details: {
            provider: target.provider,
            environments: evidence.environments,
            capabilities: evidence.capabilities
          }
        }
      );
    }
  }
  return {
    output: sanitizeJson(evidence),
    effectVerified: true,
    evidenceArtifact
  };
}
async function executeProviderPlan(workflow, target, options) {
  const { adapter, context: context2, plan } = await prepareProviderPlan(workflow, target, options);
  const providerLedgerIdentity = await durableLedgerIdentity(context2, target.provider);
  if (!workflow.checkpointOperation) {
    throw new WorkflowExecutionError(
      "provider_operation_checkpoint_required",
      `${target.provider} apply requires a durable immutable-target checkpoint`,
      { details: { provider: target.provider, effectOutcome: "confirmed_no_write" } }
    );
  }
  const immutableCheckpoint = providerPlanCheckpoint(target, plan, providerLedgerIdentity);
  workflow.checkpointOperation(immutableCheckpoint);
  const envelope = await resolveValue(options.authorization, workflow, target);
  if (envelope.run_id !== workflow.runId) {
    throw new WorkflowExecutionError(
      "authorization_rejected",
      `Authorization envelope belongs to ${envelope.run_id}, not ${workflow.runId}`
    );
  }
  const authorizationNow = options.now?.() ?? /* @__PURE__ */ new Date();
  let inspections;
  try {
    inspections = plan.operations.map(
      (operation2) => inspectOperationAuthorization(envelope, operation2, options.policies, authorizationNow)
    );
  } catch (error) {
    if (error instanceof AuthorizationError) {
      throw new WorkflowExecutionError("authorization_rejected", error.message, {
        details: { provider: target.provider, reason: error.code }
      });
    }
    throw error;
  }
  const requirements = inspections.flatMap(
    (inspection, index) => inspection.checkpointRequired ? [
      {
        effect: inspection.effect,
        operationId: plan.operations[index].id,
        provider: plan.operations[index].provider,
        action: plan.operations[index].action
      }
    ] : []
  );
  const grants = requirements.length === 0 ? [] : workflow.claimAuthorizationCheckpoints?.(requirements) ?? (() => {
    throw new WorkflowExecutionError(
      "authorization_checkpoint_required",
      `${target.provider} requires one-shot human authorization before provider execution`,
      { details: { requirements } }
    );
  })();
  const executionAuthorizationNow = options.now?.() ?? /* @__PURE__ */ new Date();
  try {
    for (const [index, operation2] of plan.operations.entries()) {
      const inspection = inspections[index];
      const checkpointGrant = inspection.checkpointRequired ? grants.find(
        (grant) => grant.effect === inspection.effect && grant.operationId === operation2.id
      ) : void 0;
      assertOperationAuthorized(envelope, operation2, options.policies, executionAuthorizationNow, {
        nodeId: workflow.node.id,
        checkpointGrant
      });
    }
  } catch (error) {
    if (error instanceof AuthorizationError) {
      throw new WorkflowExecutionError("authorization_rejected", error.message, {
        details: { provider: target.provider, reason: error.code }
      });
    }
    throw error;
  }
  const estimatedOperations = plan.operations.flatMap(
    (operation2) => operation2.estimatedCost ? [{ operationId: operation2.id, amount: operation2.estimatedCost.amount }] : []
  );
  if (estimatedOperations.length > 0) {
    if (!workflow.reserveAuthorizationSpend) {
      throw new WorkflowExecutionError(
        "authorization_spend_ledger_required",
        `${target.provider} has estimated provider cost but no durable run spend ledger`,
        { details: { provider: target.provider, effectOutcome: "confirmed_no_write" } }
      );
    }
    try {
      workflow.reserveAuthorizationSpend({
        reservationId: `${workflow.node.id}:${immutableCheckpoint.digest}`,
        currency: envelope.max_estimated_spend.currency,
        maxAmount: envelope.max_estimated_spend.amount,
        operations: estimatedOperations
      });
    } catch (error) {
      if (error instanceof WorkflowExecutionError) {
        const prior = error.details && typeof error.details === "object" && !Array.isArray(error.details) ? error.details : {};
        throw new WorkflowExecutionError(error.code, error.message, {
          retryable: error.retryable,
          details: { ...prior, effectOutcome: "confirmed_no_write" }
        });
      }
      throw error;
    }
  }
  const executionContext = {
    ...context2,
    authorization: "approved",
    reuseSuccessfulOperations: workflow.attempt > 1,
    signal: workflow.signal
  };
  const report = await adapter.apply(plan, executionContext);
  if (report.state !== "applied") {
    throw executionFailure(
      target.provider,
      report.operations.map(({ result: result2 }) => result2)
    );
  }
  const readBack = await adapter.readBack(report, executionContext);
  const verification = adapter.verify(report, readBack);
  if (verification.state !== "verified") {
    throw verificationFailure(target.provider, verification);
  }
  return verifiedProviderResult(workflow, target, options, context2, plan, report, verification);
}
async function resolveProviderTarget(handler, factory, workflow) {
  try {
    return await factory(workflow);
  } catch (error) {
    throw new WorkflowExecutionError(
      "provider_plan_factory_failed",
      error instanceof ProviderPlanFactoryPrerequisiteError ? error.message : `Provider plan factory "${handler}" failed without producing a safe request`
    );
  }
}
function reconciliationWorkflowContext(reconciliation) {
  return {
    runId: reconciliation.runId,
    node: reconciliation.node,
    attempt: reconciliation.attempt,
    input: reconciliation.input,
    dependencyOutputs: reconciliation.dependencyOutputs,
    idempotencyKey: reconciliation.idempotencyKey,
    signal: reconciliation.signal,
    trace: reconciliation.trace
  };
}
function reconciliationFailure(error) {
  if (error instanceof WorkflowExecutionError) {
    return {
      status: "failed",
      code: error.code,
      message: error.message,
      retryable: error.retryable,
      effectState: "unknown"
    };
  }
  return {
    status: "failed",
    code: "provider_reconciliation_failed",
    message: "Provider reconciliation failed without a safe verified outcome",
    retryable: false,
    effectState: "unknown"
  };
}
async function assertReconciliationAuthorized(workflow, target, plan, options) {
  const envelope = await resolveValue(options.authorization, workflow, target);
  if (envelope.run_id !== workflow.runId) {
    throw new WorkflowExecutionError(
      "provider_reconciliation_authorization_rejected",
      `Authorization envelope belongs to ${envelope.run_id}, not ${workflow.runId}`
    );
  }
  const authorizationNow = options.now?.() ?? /* @__PURE__ */ new Date();
  try {
    for (const operation2 of plan.operations) {
      inspectOperationAuthorization(envelope, operation2, options.policies, authorizationNow);
    }
  } catch (error) {
    if (error instanceof AuthorizationError) {
      throw new WorkflowExecutionError(
        "provider_reconciliation_authorization_rejected",
        error.message
      );
    }
    throw error;
  }
}
async function reconcileProviderPlan(workflow, prepared, options) {
  const { adapter, context: context2, plan, target } = prepared;
  await assertReconciliationAuthorized(workflow, target, plan, options);
  if (!adapter.reconcile) {
    return {
      status: "failed",
      code: "provider_reconciliation_unavailable",
      message: `${target.provider} adapter does not implement no-write reconciliation`,
      retryable: false,
      effectState: "unknown"
    };
  }
  const executionContext = {
    ...context2,
    authorization: "dry_run",
    signal: workflow.signal
  };
  const report = await adapter.reconcile(plan, executionContext);
  const results = report.operations.map(({ result: result2 }) => result2);
  if (results.some(({ providerCode }) => providerCode === "idempotency_conflict")) {
    return {
      status: "failed",
      code: "provider_idempotency_conflict",
      message: `${target.provider} reconciliation target does not match the durable request`,
      retryable: false,
      effectState: "unknown"
    };
  }
  if (results.some(
    ({ status, effectOutcome }) => effectOutcome === "unknown" || status === "failed" && effectOutcome === void 0
  )) {
    return {
      status: "unknown",
      message: `${target.provider} outcome remains unknown after bounded read-back reconciliation`
    };
  }
  const everyOperationConfirmedNoWrite = results.length > 0 && results.every(({ effectOutcome }) => effectOutcome === "confirmed_no_write");
  if (everyOperationConfirmedNoWrite) {
    return { status: "not_applied" };
  }
  const hasConfirmedWrite = results.some(
    ({ status, effectOutcome }) => status === "succeeded" || effectOutcome === "confirmed_write"
  );
  const hasIncompleteOperation = results.some(({ status }) => status !== "succeeded");
  if (hasConfirmedWrite && hasIncompleteOperation) {
    return {
      status: "partially_applied",
      message: `${target.provider} has a request-bound partial apply; only missing operations may be retried`
    };
  }
  if (report.state !== "applied") {
    return {
      status: "unknown",
      message: `${target.provider} reconciliation did not establish one safe plan outcome`
    };
  }
  const readBack = await adapter.readBack(report, executionContext);
  const verification = adapter.verify(report, readBack);
  if (verification.state === "failed") {
    return {
      status: "failed",
      code: "provider_verification_failed",
      message: `${target.provider} read-back conflicts with the immutable requested state`,
      retryable: false,
      effectState: "confirmed_write"
    };
  }
  if (verification.state !== "verified") {
    return { status: "pending" };
  }
  let verified;
  try {
    verified = await verifiedProviderResult(
      workflow,
      target,
      options,
      context2,
      plan,
      report,
      verification
    );
  } catch (error) {
    return {
      status: "failed",
      code: error instanceof WorkflowExecutionError ? error.code : "provider_reconciliation_completion_failed",
      message: error instanceof WorkflowExecutionError ? error.message : `${target.provider} was read-back verified but completion state could not be persisted`,
      retryable: false,
      effectState: "confirmed_write"
    };
  }
  return {
    status: "verified",
    output: verified.output,
    evidenceArtifact: verified.evidenceArtifact
  };
}
function createProviderWorkflowBindings(options) {
  return {
    handlers: Object.fromEntries(
      Object.entries(options.planFactories).map(([handler, factory]) => [
        handler,
        async (workflow) => {
          const target = await resolveProviderTarget(handler, factory, workflow);
          return executeProviderPlan(workflow, target, options);
        }
      ])
    ),
    reconcilers: Object.fromEntries(
      Object.entries(options.planFactories).map(([handler, factory]) => [
        handler,
        async (reconciliation) => {
          const workflow = reconciliationWorkflowContext(reconciliation);
          try {
            const checkpoint = parseProviderPlanCheckpoint(reconciliation.operation.checkpoint);
            if (!checkpoint) {
              return {
                status: "failed",
                code: "provider_reconciliation_target_missing",
                message: `Provider plan handler "${handler}" has no valid durable immutable-target checkpoint`,
                retryable: false,
                effectState: "unknown"
              };
            }
            const prepared = await prepareProviderReconciliation(
              handler,
              factory,
              workflow,
              checkpoint,
              options
            );
            return await reconcileProviderPlan(workflow, prepared, options);
          } catch (error) {
            return reconciliationFailure(error);
          }
        }
      ])
    )
  };
}

// lib/runtime/provider-lifecycle-store.ts
import { randomBytes as randomBytes4 } from "node:crypto";
import { mkdir as mkdir5, open as open3, readFile as readFile4, rename as rename4 } from "node:fs/promises";
import { dirname as dirname10, resolve as resolve17 } from "node:path";
var providerResourceTypes = [
  "account_id",
  "app_id",
  "app_version",
  "apple_build_id",
  "branch_id",
  "build_id",
  "build_number",
  "bundle_id",
  "database_id",
  "database_name",
  "deployment_id",
  "domain",
  "entitlement_id",
  "measurement_id",
  "offering_id",
  "price_id",
  "product_id",
  "project",
  "project_id",
  "project_name",
  "property_id",
  "repository",
  "repository_id",
  "region",
  "site_url",
  "stream_id",
  "submission_id",
  "team_id",
  "testflight_group_id",
  "url",
  "webhook_id"
];
var ProviderLifecycleStoreError = class extends Error {
  code = "provider_lifecycle_store_invalid";
  constructor(message) {
    super(message);
    this.name = "ProviderLifecycleStoreError";
  }
};
var environments = ["local", "preview", "sandbox", "production", "testflight"];
var capabilitySchema = external_exports.string().regex(/^[a-z][a-z0-9._-]{0,199}$/);
function safeResourceValue2(value) {
  if (value.length === 0 || value.length > 500 || /[\u0000-\u001f\u007f]/.test(value) || value.startsWith("cred://") || value.includes("[REDACTED]") || looksLikeCredentialValue(value)) {
    return false;
  }
  try {
    const url = new URL(value);
    return url.username === "" && url.password === "" && url.search === "" && url.hash === "";
  } catch {
    return true;
  }
}
var resourceReferenceSchema = external_exports.object({
  type: external_exports.enum(providerResourceTypes),
  value: external_exports.string()
}).strict().refine(({ value }) => safeResourceValue2(value), {
  message: "resource value is not a safe provider identifier"
});
var lifecycleRecordSchema = external_exports.object({
  provider: external_exports.enum(providerIds),
  environment: external_exports.enum(environments),
  capability: capabilitySchema,
  state: external_exports.literal("verified"),
  planId: external_exports.string().regex(/^plan\.[a-z][a-z0-9_-]*\.[a-z0-9]+$/).max(200),
  verifiedAt: external_exports.string().datetime({ offset: true }),
  resourceRefs: external_exports.array(resourceReferenceSchema).max(100)
}).strict();
var lifecycleDocumentSchema = external_exports.object({
  schemaVersion: external_exports.literal(1),
  records: external_exports.array(lifecycleRecordSchema).max(1e4)
}).strict();
function scopeKey(scope) {
  return `${scope.provider}\0${scope.environment}\0${scope.capability}`;
}
function compareRecords(left, right) {
  return left.provider.localeCompare(right.provider) || left.environment.localeCompare(right.environment) || left.capability.localeCompare(right.capability);
}
function normalizedRecord(input) {
  const parsed = lifecycleRecordSchema.safeParse(input);
  if (!parsed.success) {
    throw new ProviderLifecycleStoreError(
      `Provider lifecycle record is invalid: ${parsed.error.issues.map((issue) => `${issue.path.join(".") || "record"}: ${issue.message}`).join("; ")}`
    );
  }
  const refs = [...parsed.data.resourceRefs].sort(
    (left, right) => left.type.localeCompare(right.type) || left.value.localeCompare(right.value)
  ).filter(
    (reference, index, values) => index === 0 || reference.type !== values[index - 1].type || reference.value !== values[index - 1].value
  );
  return { ...parsed.data, resourceRefs: refs };
}
function parseProviderLifecycleDocument(input) {
  const parsed = lifecycleDocumentSchema.safeParse(input);
  if (!parsed.success) {
    throw new ProviderLifecycleStoreError(
      `Provider lifecycle state is invalid: ${parsed.error.issues.map((issue) => `${issue.path.join(".") || "document"}: ${issue.message}`).join("; ")}`
    );
  }
  const records = parsed.data.records.map(normalizedRecord);
  if (new Set(records.map(scopeKey)).size !== records.length) {
    throw new ProviderLifecycleStoreError(
      "Provider lifecycle state contains duplicate provider/environment/capability scopes"
    );
  }
  return records.sort(compareRecords);
}
function cloneRecord(record3) {
  return {
    ...record3,
    resourceRefs: record3.resourceRefs.map((reference) => ({ ...reference }))
  };
}
var FileProviderLifecycleStore = class {
  queue = Promise.resolve();
  path;
  constructor(path) {
    this.path = resolve17(path);
  }
  async list() {
    await this.queue;
    return (await this.read()).map(cloneRecord);
  }
  async get(scope) {
    await this.queue;
    const record3 = (await this.read()).find((candidate) => scopeKey(candidate) === scopeKey(scope));
    return record3 ? cloneRecord(record3) : null;
  }
  async recordVerified(records) {
    if (records.length === 0) {
      throw new ProviderLifecycleStoreError("At least one verified lifecycle record is required");
    }
    const normalized = records.map(normalizedRecord);
    if (new Set(normalized.map(scopeKey)).size !== normalized.length) {
      throw new ProviderLifecycleStoreError(
        "A verified lifecycle update contains duplicate provider/environment/capability scopes"
      );
    }
    const update = async () => {
      const current = new Map((await this.read()).map((record3) => [scopeKey(record3), record3]));
      for (const record3 of normalized) current.set(scopeKey(record3), record3);
      await this.write([...current.values()].sort(compareRecords));
    };
    const pending = this.queue.then(update, update);
    this.queue = pending.catch(() => void 0);
    return pending;
  }
  async read() {
    let raw;
    try {
      raw = await readFile4(this.path, "utf8");
    } catch (error) {
      if (error.code === "ENOENT") return [];
      throw new ProviderLifecycleStoreError(`Provider lifecycle state could not be read`);
    }
    let value;
    try {
      value = JSON.parse(raw);
    } catch {
      throw new ProviderLifecycleStoreError(
        "Provider lifecycle state is corrupt JSON; restore it from verified evidence before resuming"
      );
    }
    return parseProviderLifecycleDocument(value);
  }
  async write(records) {
    const directory = dirname10(this.path);
    await mkdir5(directory, { recursive: true, mode: 448 });
    const temporary = `${this.path}.${process.pid}.${randomBytes4(6).toString("hex")}.tmp`;
    const handle = await open3(temporary, "wx", 384);
    try {
      await handle.writeFile(`${JSON.stringify({ schemaVersion: 1, records }, null, 2)}
`, "utf8");
      await handle.sync();
    } finally {
      await handle.close();
    }
    await rename4(temporary, this.path);
  }
};

// lib/upgrade/engine.ts
import { createHash as createHash14 } from "node:crypto";
import { stringify as stringify4 } from "yaml";

// lib/upgrade/types.ts
var upgradeOwnershipSchema = external_exports.enum([
  "harness",
  "project",
  "generated",
  "core_owned",
  "merge_managed",
  "venture_owned"
]);
var relativePathSchema = external_exports.string().min(1).refine((value) => !value.startsWith("/") && !value.split("/").includes(".."), {
  message: "upgrade paths must be repository-relative"
});
var managedReleaseFileSchema = external_exports.object({
  path: relativePathSchema,
  ownership: upgradeOwnershipSchema,
  content: external_exports.string(),
  baseContent: external_exports.string().optional()
}).strict();
var harnessReleaseSchema = external_exports.object({
  version: external_exports.string().regex(/^\d+\.\d+\.\d+$/),
  configContractVersion: external_exports.number().int().positive().optional(),
  source: external_exports.object({
    kind: external_exports.enum(["template", "release", "local"]),
    ref: external_exports.string().min(1).nullable()
  }).strict(),
  files: external_exports.array(managedReleaseFileSchema)
}).strict().superRefine((release, context2) => {
  const seen = /* @__PURE__ */ new Set();
  for (const [index, file2] of release.files.entries()) {
    if (seen.has(file2.path)) {
      context2.addIssue({
        code: external_exports.ZodIssueCode.custom,
        path: ["files", index, "path"],
        message: `duplicate managed path: ${file2.path}`
      });
    }
    seen.add(file2.path);
  }
});

// lib/upgrade/engine.ts
function sha2565(content) {
  return createHash14("sha256").update(content).digest("hex");
}
function mergeLines(base, current, next) {
  const baseLines = base.split("\n");
  const currentLines = current.split("\n");
  const nextLines = next.split("\n");
  const length = Math.max(baseLines.length, currentLines.length, nextLines.length);
  const merged = [];
  for (let index = 0; index < length; index += 1) {
    const before = baseLines[index];
    const local = currentLines[index];
    const incoming = nextLines[index];
    if (local === incoming) merged.push(local ?? "");
    else if (local === before) merged.push(incoming ?? "");
    else if (incoming === before) merged.push(local ?? "");
    else return null;
  }
  return merged.join("\n");
}
async function planUpgrade(options) {
  const release = harnessReleaseSchema.parse(options.release);
  const previous = new Map(options.currentLock.managed_files.map((file2) => [file2.path, file2]));
  const files = [];
  for (const file2 of release.files) {
    const currentContent = await options.fileSystem.readText(file2.path);
    const currentHash = currentContent === null ? null : sha2565(currentContent);
    const previousEntry = previous.get(file2.path);
    const previousHash = previousEntry?.sha256 ?? null;
    const previousBaseHash = previousEntry?.base_sha256 ?? previousHash;
    const nextHash = sha2565(file2.content);
    let action;
    let reason;
    let resultContent = file2.content;
    if (file2.ownership === "project" || file2.ownership === "venture_owned") {
      action = "preserve";
      reason = "venture-owned files are never replaced by a Core upgrade";
      resultContent = currentContent ?? file2.content;
    } else if (currentHash === nextHash) {
      action = "unchanged";
      reason = "working tree already matches the target release";
    } else if (currentContent === null) {
      action = "create";
      reason = "managed file does not exist in the child venture";
    } else if (previousEntry && previousEntry.ownership !== file2.ownership) {
      action = "conflict";
      reason = `ownership changed from ${previousEntry.ownership} to ${file2.ownership}`;
    } else if (file2.ownership === "merge_managed" && currentHash !== previousBaseHash) {
      if (previousBaseHash === null || file2.baseContent === void 0 || sha2565(file2.baseContent) !== previousBaseHash) {
        action = "conflict";
        reason = "merge-managed file has no matching trusted three-way base";
      } else {
        const merged = mergeLines(file2.baseContent, currentContent, file2.content);
        if (merged === null) {
          action = "conflict";
          reason = "merge-managed file has overlapping local and Core edits";
        } else {
          action = "merge";
          reason = "non-overlapping local and Core edits merged against the trusted base";
          resultContent = merged;
        }
      }
    } else if (previousHash === null || currentHash !== previousHash) {
      action = "conflict";
      reason = previousHash === null ? "the lock has no trusted baseline hash for this existing file" : "the child venture changed this Core-owned file after the previous release";
    } else {
      action = "update";
      reason = "the file still matches its trusted baseline and can be replaced safely";
    }
    files.push({
      path: file2.path,
      ownership: file2.ownership,
      action,
      reason,
      previousHash,
      currentHash,
      nextHash,
      resultHash: sha2565(resultContent),
      ...action === "merge" ? { resultContent } : {}
    });
  }
  const releasePaths = new Set(release.files.map((file2) => file2.path));
  const managedFiles = [
    ...release.files.map((file2) => {
      const planned = files.find((candidate) => candidate.path === file2.path);
      return {
        path: file2.path,
        ownership: file2.ownership,
        sha256: file2.ownership === "project" || file2.ownership === "venture_owned" ? planned.currentHash ?? previous.get(file2.path)?.sha256 ?? null : planned.resultHash,
        ...file2.ownership === "merge_managed" ? { base_sha256: planned.nextHash } : {}
      };
    }),
    ...options.currentLock.managed_files.filter((file2) => !releasePaths.has(file2.path))
  ].sort((left, right) => left.path.localeCompare(right.path));
  const nextLock = harnessLockSchema.parse({
    ...options.currentLock,
    harness_version: release.version,
    ...options.currentLock.lock_version === 2 ? { core_version: release.version } : {},
    config_contract_version: release.configContractVersion ?? options.currentLock.config_contract_version,
    source: release.source,
    managed_files: managedFiles
  });
  return {
    fromVersion: options.currentLock.harness_version,
    toVersion: release.version,
    release,
    files,
    conflicts: files.filter((file2) => file2.action === "conflict"),
    nextLock
  };
}

// lib/upgrade/local-release.ts
import { createHash as createHash15 } from "node:crypto";
import { lstat as lstat2, readFile as readFile5, realpath, stat } from "node:fs/promises";
import { isAbsolute as isAbsolute5, relative as relative12, resolve as resolve18, sep as sep12 } from "node:path";
function sha2566(content) {
  return createHash15("sha256").update(content).digest("hex");
}
function isInside(root, candidate) {
  const rel = relative12(root, candidate);
  return rel === "" || !rel.startsWith(`..${sep12}`) && rel !== ".." && !isAbsolute5(rel);
}
function rejectRemoteLocator(locator) {
  if (/^[a-z][a-z0-9+.-]*:\/\//i.test(locator)) {
    throw new Error(
      "Release locators must be local filesystem paths; URL fetching and remote execution are forbidden"
    );
  }
}
async function locateLocalHarnessRelease(options) {
  rejectRemoteLocator(options.locator);
  const requestedRoot = resolve18(options.baseDir ?? process.cwd(), options.locator);
  const requestedStat = await stat(requestedRoot).catch(() => null);
  if (!requestedStat?.isDirectory()) {
    throw new Error(`Local release directory does not exist: ${options.locator}`);
  }
  const releaseRoot = await realpath(requestedRoot);
  const lockPath = resolve18(releaseRoot, "harness.lock");
  const lockRealPath = await realpath(lockPath).catch(() => null);
  if (lockRealPath === null || !isInside(releaseRoot, lockRealPath)) {
    throw new Error("Local release must contain a non-escaping harness.lock");
  }
  const lock = parseHarnessLock(await readFile5(lockRealPath, "utf8"));
  if (lock.source.kind !== "release" || lock.source.ref !== `v${lock.harness_version}`) {
    throw new Error(
      `Local release lock must declare source release/v${lock.harness_version}; found ${lock.source.kind}/${lock.source.ref ?? "null"}`
    );
  }
  if (lock.managed_files.length === 0) {
    throw new Error("Local release lock has no managed-file baseline");
  }
  const files = [];
  for (const entry of lock.managed_files) {
    if (entry.sha256 === null) {
      if (entry.ownership !== "project" && entry.ownership !== "venture_owned") {
        throw new Error(`Local release managed file has no trusted hash: ${entry.path}`);
      }
      files.push({ path: entry.path, ownership: entry.ownership, content: "" });
      continue;
    }
    const candidate = resolve18(releaseRoot, entry.path);
    const fileStat = await lstat2(candidate).catch(() => null);
    if (fileStat === null || !fileStat.isFile() || fileStat.isSymbolicLink()) {
      throw new Error(`Local release managed file is missing or not a regular file: ${entry.path}`);
    }
    const realCandidate = await realpath(candidate);
    if (!isInside(releaseRoot, realCandidate)) {
      throw new Error(`Local release managed file escapes its root: ${entry.path}`);
    }
    const content = await readFile5(realCandidate, "utf8");
    if (sha2566(content) !== entry.sha256) {
      throw new Error(`Local release managed hash mismatch: ${entry.path}`);
    }
    files.push({ path: entry.path, ownership: entry.ownership, content });
  }
  return harnessReleaseSchema.parse({
    version: lock.harness_version,
    configContractVersion: lock.config_contract_version,
    source: lock.source,
    files
  });
}

// lib/upgrade/operational.ts
import { createHash as createHash16 } from "node:crypto";
import { stringify as stringify5, parse as parse4 } from "yaml";
function sha2567(content) {
  return createHash16("sha256").update(content).digest("hex");
}
function safeMessage(error) {
  const message = error instanceof Error ? error.message : String(error);
  return message.replace(/(gh[pousr]_|sk_(?:live|test)_|xox[baprs]-)[A-Za-z0-9_-]+/g, "$1[REDACTED]").slice(0, 500);
}
var OPERATIONAL_UPGRADE_STEPS = [
  { id: "agent_adapter_sync", command: "pnpm", args: ["agents:sync"] },
  { id: "agent_adapter_parity", command: "pnpm", args: ["agents:check"] },
  { id: "typecheck", command: "pnpm", args: ["typecheck"] },
  { id: "migration_tests", command: "pnpm", args: ["test:migrations"] }
];
var ORDINARY_WEB_UPGRADE_STEPS = [
  { id: "child_verify", command: "pnpm", args: ["verify"] }
];
var ORDINARY_WEB_MIGRATION_STEP = {
  id: "child_migration_tests",
  command: "pnpm",
  args: ["test:migrations"]
};
var UpgradePreparationError = class extends Error {
  constructor(code, message, nextAction2) {
    super(message);
    this.code = code;
    this.nextAction = nextAction2;
    this.name = "UpgradePreparationError";
  }
  code;
  nextAction;
};
function preparationError(message) {
  return new UpgradePreparationError(
    "untrusted_upgrade_verification_profile",
    message,
    "Restore the child harness.lock, venture.manifest.json, and managed package.json to a trusted state, then rerun vh upgrade --release <local-release-root> --dry-run."
  );
}
function record(value) {
  return value !== null && typeof value === "object" && !Array.isArray(value) ? value : null;
}
function packageScripts(text2) {
  let parsed;
  try {
    parsed = JSON.parse(text2);
  } catch {
    throw preparationError("The managed child package.json is not valid JSON");
  }
  const scripts = record(record(parsed)?.scripts);
  if (!scripts) throw preparationError("The managed child package.json has no scripts object");
  for (const [name, command] of Object.entries(scripts)) {
    if (typeof command !== "string") {
      throw preparationError(`The managed child package.json script ${name} is not a string`);
    }
  }
  return scripts;
}
async function resolveOperationalVerificationProfile(options) {
  if (options.lock.lock_version === 1) {
    const frameworkText = await options.fileSystem.readText("config/framework.yaml");
    const frameworkDocument = frameworkText === null ? null : record(parse4(frameworkText));
    const framework = record(frameworkDocument?.framework);
    const verification = record(frameworkDocument?.verification);
    if (framework?.name !== "venture-harness" || frameworkDocument?.package_manager !== "pnpm" || verification?.primary !== "pnpm verify") {
      throw preparationError(
        "The v1 lock is not backed by the canonical Venture Harness Core framework contract"
      );
    }
    return { steps: OPERATIONAL_UPGRADE_STEPS, packageScripts: null };
  }
  if (options.lock.seed.id !== "agentic-web-saas") {
    throw preparationError(
      `No fixed operational verification profile is registered for v2 seed ${options.lock.seed.id}`
    );
  }
  if (options.lock.extensions.venture_manifest !== "venture.manifest.json") {
    throw preparationError("The v2 child lock does not select the canonical venture.manifest.json");
  }
  const manifestEntry = options.lock.managed_files.find(
    (entry) => entry.path === "venture.manifest.json"
  );
  const manifestText = await options.fileSystem.readText("venture.manifest.json");
  if (manifestEntry?.ownership !== "venture_owned" || manifestEntry.sha256 === null || manifestText === null || sha2567(manifestText) !== manifestEntry.sha256) {
    throw preparationError(
      "The ordinary web child venture.manifest.json does not match its trusted lock hash"
    );
  }
  let manifest;
  try {
    manifest = record(JSON.parse(manifestText));
  } catch {
    manifest = null;
  }
  const manifestSeed = record(manifest?.seed);
  if (manifest?.schemaVersion !== 1 || manifest?.rail !== "web" || manifestSeed?.id !== options.lock.seed.id || manifestSeed?.version !== options.lock.seed.version) {
    throw preparationError(
      "The ordinary web child manifest does not bind the seed and rail selected by harness.lock"
    );
  }
  const packageEntry = options.lock.managed_files.find((entry) => entry.path === "package.json");
  const packageText = await options.fileSystem.readText("package.json");
  if (packageEntry?.ownership !== "merge_managed" || packageText === null) {
    throw preparationError(
      "The ordinary web child requires package.json to remain a merge-managed seed file"
    );
  }
  const scripts = packageScripts(packageText);
  let registeredScripts;
  try {
    registeredScripts = ventureSeed(options.lock.seed.id, options.lock.seed.version).packageScripts;
  } catch {
    throw preparationError(
      `No registered script contract exists for ${options.lock.seed.id}@${options.lock.seed.version}`
    );
  }
  for (const [name, expected] of Object.entries(registeredScripts)) {
    if (scripts[name] !== expected) {
      throw preparationError(
        `The ordinary web child ${name} script does not match ${options.lock.seed.id}@${options.lock.seed.version}`
      );
    }
  }
  if (registeredScripts.verify !== "pnpm typecheck && pnpm test && pnpm build") {
    throw preparationError(
      `The registered ${options.lock.seed.id}@${options.lock.seed.version} verify command is not allowlisted for operational upgrades`
    );
  }
  const steps = [...ORDINARY_WEB_UPGRADE_STEPS];
  const currentMigrationScript = scripts["test:migrations"];
  const registeredMigrationScript = registeredScripts["test:migrations"];
  if (currentMigrationScript !== void 0) {
    if (registeredMigrationScript === void 0 || currentMigrationScript !== registeredMigrationScript) {
      throw preparationError(
        `The ordinary web child test:migrations script is not registered for ${options.lock.seed.id}@${options.lock.seed.version}`
      );
    }
    steps.push(ORDINARY_WEB_MIGRATION_STEP);
  }
  return { steps, packageScripts: scripts };
}
function assertVerificationScriptsPreserved(options) {
  if (options.profile.packageScripts === null) return;
  const packagePlan = options.files.find((file2) => file2.path === "package.json");
  const releasePackage = options.release.files.find((file2) => file2.path === "package.json");
  const candidateText = packagePlan && releasePackage ? packagePlan.action === "merge" ? packagePlan.resultContent ?? releasePackage.content : packagePlan.action === "create" || packagePlan.action === "update" || packagePlan.action === "unchanged" ? releasePackage.content : options.currentPackageText : options.currentPackageText;
  if (candidateText === null) {
    throw preparationError("The staged ordinary web child has no managed package.json");
  }
  const candidateScripts = packageScripts(candidateText);
  const scriptNames = [
    .../* @__PURE__ */ new Set([...Object.keys(options.profile.packageScripts), ...Object.keys(candidateScripts)])
  ].sort();
  const changedScript = scriptNames.find(
    (script) => candidateScripts[script] !== options.profile.packageScripts?.[script]
  );
  if (changedScript) {
    throw preparationError(
      `The release changes the trusted child ${changedScript} script; review that change separately before upgrading`
    );
  }
}
function verificationReport(steps, status) {
  return steps.map((step) => ({
    id: step.id,
    command: step.command,
    args: [...step.args],
    status,
    exitCode: null
  }));
}
async function detectSourceVersion(options) {
  if (options.currentLock) return options.currentLock.harness_version;
  const frameworkText = await options.fileSystem.readText("config/framework.yaml");
  if (frameworkText === null) {
    throw new Error("Cannot infer the unlocked source version: config/framework.yaml is missing");
  }
  const framework = parse4(frameworkText);
  const version = framework.framework?.version;
  if (typeof version !== "string" || !/^\d+\.\d+\.\d+$/.test(version)) {
    throw new Error("Cannot infer the unlocked source version from framework.version");
  }
  return version;
}
function summarizeMigrations(plans) {
  return plans.map((plan) => ({
    id: plan.id,
    fromVersion: plan.fromVersion,
    toVersion: plan.toVersion,
    paths: plan.changes.map((change) => change.path),
    warnings: plan.warnings
  }));
}
function withAppliedMigrations(options) {
  const seen = new Set(options.lock.applied_migrations.map((migration) => migration.id));
  const additions = options.plans.filter((plan) => !seen.has(plan.id)).map((plan) => ({
    id: plan.id,
    from_version: plan.fromVersion,
    to_version: plan.toVersion,
    applied_at: options.clock().toISOString()
  }));
  return harnessLockSchema.parse({
    ...options.lock,
    applied_migrations: [...options.lock.applied_migrations, ...additions]
  });
}
async function rollback(fileSystem, paths, originals) {
  const failures = [];
  for (const path of [...paths].reverse()) {
    try {
      const original = originals.get(path);
      if (original === null) await fileSystem.remove(path);
      else if (original !== void 0) await fileSystem.writeAtomic(path, original);
    } catch (error) {
      failures.push(`${path}: ${safeMessage(error)}`);
    }
  }
  return failures;
}
function failureReport(options) {
  return {
    status: options.conflicts?.length ? "blocked" : "failed",
    dryRun: options.dryRun,
    fromVersion: options.fromVersion,
    toVersion: options.toVersion,
    migrations: options.migrations ?? [],
    files: options.files ?? [],
    conflicts: options.conflicts ?? [],
    verification: options.verification ?? verificationReport(options.verificationSteps ?? [], "not_run"),
    lockUpdated: false,
    rolledBack: options.rolledBack ?? false,
    error: {
      code: options.code,
      message: options.message,
      nextAction: options.nextAction
    }
  };
}
async function applyOperationalUpgrade(options) {
  const dryRun = options.dryRun ?? false;
  const clock = options.clock ?? (() => /* @__PURE__ */ new Date());
  let fromVersion = options.currentLock?.harness_version ?? "0.0.0";
  let migrationPlans = [];
  let files = [];
  let verificationSteps = options.currentLock?.lock_version === 2 ? [] : OPERATIONAL_UPGRADE_STEPS;
  try {
    fromVersion = await detectSourceVersion(options);
    const chain = await (options.migrationRegistry ?? defaultMigrationRegistry).planChain({
      fromVersion,
      toVersion: options.release.version,
      fileSystem: options.fileSystem,
      clock
    });
    migrationPlans = chain.plans;
    const planningLock = options.currentLock ? options.currentLock : await chain.stagedFileSystem.readText("harness.lock").then((text2) => {
      if (text2 === null) {
        throw new Error("The migration chain did not produce a trusted harness.lock baseline");
      }
      const lock = parseHarnessLock(text2);
      if (lock.harness_version !== options.release.version) {
        throw new Error(
          `The staged migration lock targets ${lock.harness_version}, not release ${options.release.version}`
        );
      }
      return lock;
    });
    const verificationProfile = await resolveOperationalVerificationProfile({
      lock: planningLock,
      fileSystem: chain.stagedFileSystem
    });
    verificationSteps = verificationProfile.steps;
    const currentPackageText = verificationProfile.packageScripts === null ? null : await chain.stagedFileSystem.readText("package.json");
    const upgradePlan = await planUpgrade({
      fileSystem: chain.stagedFileSystem,
      currentLock: planningLock,
      release: options.release
    });
    files = upgradePlan.files;
    const migrations = summarizeMigrations(migrationPlans);
    if (upgradePlan.conflicts.length > 0) {
      return failureReport({
        dryRun,
        fromVersion,
        toVersion: options.release.version,
        migrations,
        files,
        conflicts: upgradePlan.conflicts,
        verificationSteps,
        code: "managed_file_conflict",
        message: `${upgradePlan.conflicts.length} managed file conflict(s) require review.`,
        nextAction: "Preserve the child change as project-owned or reconcile it against the trusted local release, then rerun vh upgrade --release <local-release-root> --dry-run."
      });
    }
    assertVerificationScriptsPreserved({
      profile: verificationProfile,
      files,
      release: options.release,
      currentPackageText
    });
    const migrationChanges = migrationPlans.flatMap(
      (plan) => plan.changes.filter((change) => change.path !== "harness.lock")
    );
    const releaseWrites = files.filter(
      (file2) => file2.action === "create" || file2.action === "update" || file2.action === "merge"
    );
    const candidateLock = withAppliedMigrations({
      lock: upgradePlan.nextLock,
      plans: migrationPlans,
      clock
    });
    const currentLockText = await options.fileSystem.readText("harness.lock");
    const candidateLockText = stringify5(candidateLock, {
      lineWidth: 100,
      sortMapEntries: false
    });
    const hasLockChange = currentLockText !== candidateLockText;
    const hasWrites = migrationChanges.length > 0 || releaseWrites.length > 0 || hasLockChange;
    if (!hasWrites) {
      return {
        status: "already_current",
        dryRun,
        fromVersion,
        toVersion: options.release.version,
        migrations,
        files,
        conflicts: [],
        verification: verificationReport(verificationSteps, "not_run"),
        lockUpdated: false,
        rolledBack: false,
        error: null
      };
    }
    if (dryRun) {
      return {
        status: "planned",
        dryRun: true,
        fromVersion,
        toVersion: options.release.version,
        migrations,
        files,
        conflicts: [],
        verification: verificationReport(verificationSteps, "planned"),
        lockUpdated: false,
        rolledBack: false,
        error: null
      };
    }
    const transactionPaths = [
      .../* @__PURE__ */ new Set([
        ...migrationChanges.map((change) => change.path),
        ...options.release.files.map((file2) => file2.path),
        ...planningLock.managed_files.map((file2) => file2.path),
        "harness.lock"
      ])
    ].sort();
    const originals = /* @__PURE__ */ new Map();
    for (const path of transactionPaths) {
      originals.set(path, await options.fileSystem.readText(path));
    }
    const verification = verificationReport(verificationSteps, "not_run");
    const attempted = [];
    try {
      for (const change of migrationChanges) {
        const current = await options.fileSystem.readText(change.path);
        if (current === change.content) continue;
        attempted.push(change.path);
        await options.fileSystem.writeAtomic(change.path, change.content);
      }
      for (const item of releaseWrites) {
        const releaseFile = options.release.files.find((file2) => file2.path === item.path);
        attempted.push(item.path);
        await options.fileSystem.writeAtomic(item.path, item.resultContent ?? releaseFile.content);
      }
      for (const [index, step] of verificationSteps.entries()) {
        let result2;
        try {
          result2 = await options.commandRunner.run({
            command: step.command,
            args: step.args,
            cwd: options.rootDir
          });
        } catch (error) {
          verification[index] = {
            ...verification[index],
            status: "failed",
            exitCode: null
          };
          throw error;
        }
        verification[index] = {
          ...verification[index],
          status: result2.exitCode === 0 ? "passed" : "failed",
          exitCode: result2.exitCode
        };
        if (result2.exitCode !== 0) {
          throw new Error(`verification ${step.id} exited ${result2.exitCode}`);
        }
      }
      for (const managedFile of candidateLock.managed_files) {
        if (managedFile.ownership === "project" || managedFile.ownership === "venture_owned") {
          continue;
        }
        const actual = await options.fileSystem.readText(managedFile.path);
        if (managedFile.sha256 === null || actual === null || sha2567(actual) !== managedFile.sha256) {
          throw new Error(`verified stage diverged from managed baseline: ${managedFile.path}`);
        }
      }
      attempted.push("harness.lock");
      await options.fileSystem.writeAtomic("harness.lock", candidateLockText);
      return {
        status: "applied",
        dryRun: false,
        fromVersion,
        toVersion: options.release.version,
        migrations,
        files,
        conflicts: [],
        verification,
        lockUpdated: true,
        rolledBack: false,
        error: null
      };
    } catch (error) {
      const rollbackFailures = await rollback(options.fileSystem, transactionPaths, originals);
      const validationFailed = verification.some((result2) => result2.status === "failed");
      return failureReport({
        dryRun: false,
        fromVersion,
        toVersion: options.release.version,
        migrations,
        files,
        verification,
        rolledBack: (attempted.length > 0 || verification.some((result2) => result2.status !== "not_run")) && rollbackFailures.length === 0,
        code: validationFailed ? "upgrade_validation_failed" : "upgrade_stage_failed",
        message: safeMessage(error),
        nextAction: rollbackFailures.length > 0 ? `Rollback was incomplete (${rollbackFailures.join("; ")}); restore those files from version control before retrying.` : "The previous files were restored; fix the reported stage or verification failure and rerun the dry run."
      });
    }
  } catch (error) {
    return failureReport({
      dryRun,
      fromVersion,
      toVersion: options.release.version,
      migrations: summarizeMigrations(migrationPlans),
      files,
      verificationSteps,
      code: error instanceof UpgradePreparationError ? error.code : "upgrade_preparation_failed",
      message: safeMessage(error),
      nextAction: error instanceof UpgradePreparationError ? error.nextAction : "Use a trusted local release with a registered migration chain, repair the reported input, and rerun vh upgrade --dry-run."
    });
  }
}

// lib/cli/default-credential-testers.ts
function record2(value) {
  return value !== null && typeof value === "object" && !Array.isArray(value) ? value : null;
}
function stringField(value, key) {
  const field = record2(value)?.[key];
  return typeof field === "string" || typeof field === "number" ? String(field) : null;
}
function targetFor(connection, provider) {
  if (!connection) return null;
  if (provider === "neon") {
    const role = connection.roles["database.postgres"];
    return role.organizationId ?? role.accountId ?? null;
  }
  if (provider === "stripe") return connection.roles["commerce.web"].accountId ?? null;
  if (provider === "revenuecat") {
    const role = connection.roles["commerce.native"];
    return role.organizationId ?? role.accountId ?? null;
  }
  if (provider === "brevo") return connection.roles["email.transactional"].accountId ?? null;
  if (provider === "google") {
    return connection.launchDefaults.google.analyticsAccountId ?? connection.roles["growth.google"].accountId ?? null;
  }
  return null;
}
function bearer(secret) {
  return { Authorization: `Bearer ${secret}` };
}
function probeFor(provider, secret, connection) {
  const target = targetFor(connection, provider);
  if (provider === "neon") {
    return target ? {
      url: `https://console.neon.tech/api/v2/projects?limit=1&org_id=${encodeURIComponent(target)}`,
      headers: bearer(secret),
      sensitiveHeaders: ["Authorization"],
      accountId: target
    } : {
      url: "https://console.neon.tech/api/v2/users/me",
      headers: bearer(secret),
      sensitiveHeaders: ["Authorization"]
    };
  }
  if (provider === "stripe") {
    return {
      url: "https://api.stripe.com/v1/account",
      headers: { Authorization: `Basic ${Buffer.from(`${secret}:`).toString("base64")}` },
      sensitiveHeaders: ["Authorization"],
      ...target ? { accountId: target, validate: (body) => stringField(body, "id") === target } : {}
    };
  }
  if (provider === "revenuecat") {
    return {
      url: target ? `https://api.revenuecat.com/v2/projects/${encodeURIComponent(target)}/apps?limit=1` : "https://api.revenuecat.com/v2/projects?limit=1",
      headers: bearer(secret),
      sensitiveHeaders: ["Authorization"],
      ...target ? { accountId: target } : {}
    };
  }
  if (provider === "brevo") {
    return {
      url: "https://api.brevo.com/v3/account",
      headers: { "api-key": secret },
      sensitiveHeaders: ["api-key"],
      ...target ? {
        accountId: target,
        validate: (body) => [stringField(body, "organization_id"), stringField(body, "user_id")].includes(target)
      } : {}
    };
  }
  if (provider === "google") {
    return {
      url: "https://analyticsadmin.googleapis.com/v1beta/accountSummaries?pageSize=200",
      headers: bearer(secret),
      sensitiveHeaders: ["Authorization"],
      ...target ? {
        accountId: target,
        validate: (body) => {
          const summaries = record2(body)?.accountSummaries;
          return Array.isArray(summaries) && summaries.some(
            (summary) => stringField(summary, "account") === `accounts/${target}`
          );
        }
      } : {}
    };
  }
  if (provider === "bing") {
    return {
      url: `https://ssl.bing.com/webmaster/api.svc/json/GetUserSites?apikey=${encodeURIComponent(secret)}`,
      headers: {},
      sensitiveHeaders: [],
      sensitiveUrl: true
    };
  }
  return null;
}
function createDefaultFounderCredentialTesters(options) {
  const testers = {};
  for (const provider of ["neon", "stripe", "revenuecat", "brevo", "google", "bing"]) {
    testers[provider] = async (secret, reference) => {
      const probe = probeFor(provider, secret, options.connection ?? null);
      if (!probe || reference.provider !== provider) {
        return { ok: false, message: `${provider} credential metadata does not match the probe` };
      }
      const request2 = {
        method: "GET",
        url: probe.url,
        headers: probe.headers,
        sensitiveHeaders: probe.sensitiveHeaders,
        sensitiveUrl: probe.sensitiveUrl ?? false
      };
      try {
        const response = await options.fetcher.fetch(request2);
        const responseAccepted = response.status >= 200 && response.status < 300;
        const identityAccepted = !probe.validate || probe.validate(response.body);
        if (!responseAccepted || !identityAccepted) {
          return {
            ok: false,
            message: responseAccepted ? `${provider} credential cannot access the exact Founder Stack account` : `${provider} credential probe returned HTTP ${response.status}`
          };
        }
        return {
          ok: true,
          // Organization/project/property targets prove access to the selected
          // Founder Stack destination, but they are not necessarily the
          // credential account identity stored on the reference. Preserve the
          // account identity recorded at login instead of overwriting it with
          // a differently scoped provider resource identifier.
          ...(provider === "stripe" || provider === "brevo") && probe.accountId ? { accountId: probe.accountId } : {},
          message: `${provider} credential passed a read-only official API probe`
        };
      } catch {
        return { ok: false, message: `${provider} credential read-only probe was unavailable` };
      }
    };
  }
  return testers;
}

// lib/cli/default-provider-runtime.ts
import { existsSync as existsSync10, readFileSync as readFileSync12 } from "node:fs";
import { resolve as resolve19 } from "node:path";
import { parse as parse5 } from "yaml";
var ProviderFactoryPrerequisiteError = class extends ProviderPlanFactoryPrerequisiteError {
  constructor(handler, message) {
    super(`${handler}: ${message}; no partial plan was returned and no provider request was made.`);
    this.handler = handler;
    this.name = "ProviderFactoryPrerequisiteError";
  }
  handler;
};
var DEFAULT_PROVIDER_TARGETS = {
  "provider.github-repository": { provider: "github", environment: "preview" },
  "provider.neon-database": { provider: "neon", environment: "preview" },
  "provider.brevo-sending-domain": { provider: "brevo", environment: "preview" },
  "provider.brevo-domain-verification": { provider: "brevo", environment: "preview" },
  "provider.brevo-email": { provider: "brevo", environment: "preview" },
  "provider.stripe-commerce": { provider: "stripe", environment: "sandbox" },
  "provider.google-analytics-property": { provider: "google", environment: "preview" },
  "provider.google-analytics-stream": { provider: "google", environment: "preview" },
  "provider.google-site-dns-record": { provider: "google", environment: "preview" },
  "provider.google-site-verification": { provider: "google", environment: "preview" },
  "provider.google-search-console": { provider: "google", environment: "preview" },
  "provider.bing-discovery": { provider: "bing", environment: "preview" },
  "provider.vercel-project": { provider: "vercel", environment: "preview" },
  "provider.vercel-database-environment": { provider: "vercel", environment: "preview" },
  "provider.vercel-stripe-environment": { provider: "vercel", environment: "preview" },
  "provider.vercel-stripe-webhook-environment": { provider: "vercel", environment: "preview" },
  "provider.vercel-brevo-environment": { provider: "vercel", environment: "preview" },
  "provider.vercel-ga-environment": { provider: "vercel", environment: "preview" },
  "provider.dns-records": { provider: "dns", environment: "production" },
  "provider.revenuecat-entitlements": { provider: "revenuecat", environment: "sandbox" },
  "provider.eas-build": { provider: "eas", environment: "testflight" },
  "provider.eas-submit": { provider: "eas", environment: "testflight" },
  "provider.testflight-state": {
    provider: "app_store_connect",
    environment: "testflight"
  },
  "provider.production-deploy": { provider: "vercel", environment: "production" }
};
var ADAPTER_CAPABILITIES = {
  vercel: {
    public_website: ["project", "deployment", "domain"],
    vercel_analytics: ["web_analytics"]
  },
  neon: {
    database: ["project", "database", "schema_migration", "read_write_health_check"]
  },
  stripe: { stripe: ["product", "price", "webhook", "billing_portal"] },
  revenuecat: { revenuecat: ["app", "entitlement", "offering", "webhook"] },
  brevo: {
    transactional_email: ["sending_domain", "sending_domain_verification", "sender", "template"],
    lifecycle_email: ["sending_domain", "sending_domain_verification", "sender", "template"]
  },
  google: {
    ga4: ["analytics_property", "analytics_web_stream"],
    gsc: [
      "site_verification_token",
      "site_verification",
      "search_console_site",
      "search_console_sitemap"
    ]
  },
  bing: { bing_webmaster: ["site", "sitemap"] },
  dns: { public_website: ["record"] },
  mijndomein: { public_website: ["record", "domain_attachment"] },
  app_store_connect: {
    app_store_connect: [
      "first_app_record",
      "build_processing",
      "testflight_group",
      "build_group_assignment"
    ],
    ios_aso: ["build_metadata"]
  },
  eas: { eas: ["app_store_prerequisite", "app_store_connection", "ios_build", "ios_submit"] }
};
var CLI_IDS_BY_PROVIDER = {
  github: ["github"],
  vercel: ["vercel"],
  neon: ["neon", "postgres"],
  eas: ["eas"]
};
function readYaml(path) {
  return parse5(readFileSync12(path, "utf8"));
}
function loadDefaultProviderConfig(rootDir) {
  const root = resolve19(rootDir);
  const load = (relativePath, parseConfig) => {
    const path = resolve19(root, relativePath);
    if (!existsSync10(path)) {
      throw new ProviderPlanFactoryPrerequisiteError(
        `Missing ${relativePath}. Next: restore the v0.2 typed config or run vh upgrade before planning provider effects`
      );
    }
    let value;
    try {
      value = readYaml(path);
    } catch {
      throw new ProviderPlanFactoryPrerequisiteError(
        `${relativePath} is not valid YAML. Next: repair it and run pnpm verify before planning provider effects`
      );
    }
    try {
      return parseConfig(value);
    } catch {
      throw new ProviderPlanFactoryPrerequisiteError(
        `${relativePath} does not satisfy its v0.2 schema. Next: run pnpm verify, correct the reported fields, and rerun vh doctor`
      );
    }
  };
  return {
    providers: load("config/providers.yaml", (value) => providersSchema.parse(value)),
    venture: load("config/venture.yaml", (value) => ventureSchema.parse(value)),
    mobile: load("config/mobile.yaml", (value) => mobileSchema.parse(value)),
    offer: load("config/offer.yaml", (value) => offerSchema.parse(value))
  };
}
function currentBrief(value) {
  return typeof value === "function" ? value() : value;
}
function providerState(snapshot, provider) {
  const state = snapshot.providers.providers[provider];
  if (!state) {
    throw new Error(`config/providers.yaml has no providers.${provider} entry`);
  }
  return state;
}
function fail(handler, message) {
  throw new ProviderFactoryPrerequisiteError(handler, message);
}
function requireValue(handler, value, path, nextAction2) {
  if (!value) fail(handler, `missing ${path}. Next: ${nextAction2}`);
  return value;
}
function requireExternal(handler, state, key, nextAction2) {
  return requireValue(
    handler,
    state.external_resource_ids[key],
    `config/providers.yaml providers.${DEFAULT_PROVIDER_TARGETS[handler].provider}.external_resource_ids.${key}`,
    nextAction2
  );
}
function requireCredential(handler, provider, value, path = `config/providers.yaml providers.${provider}.credential_ref`) {
  const ref = requireValue(
    handler,
    value,
    path,
    `run vh auth login ${provider}, then record the registered cred:// reference in that field`
  );
  return credentialReferenceSchema.parse(ref);
}
function requireVerifiedExisting(handler, provider, state, resource, lifecycleRecords, requiredCapabilities, requiredResources) {
  const verifiedCapabilities = new Set(lifecycleRecords.map(({ capability }) => capability));
  const scopedResourceRefs = lifecycleRecords.filter(({ capability }) => requiredCapabilities.includes(capability)).flatMap(({ resourceRefs }) => resourceRefs);
  if (state.state !== "verified" && (!requiredCapabilities.every((capability) => verifiedCapabilities.has(capability)) || !requiredResources.every(
    ({ type, value }) => scopedResourceRefs.some((reference) => resourceReferencesMatch(reference, { type, value }))
  ))) {
    fail(
      handler,
      `providers.${provider}.state is ${state.state}, and no matching verified lifecycle record proves the existing ${resource}. Next: verify it with provider read-back in the same environment, set last_verified_at and evidence_artifact_ref, or inject a complete typed factory that can create and verify it`
    );
  }
}
function resourceReferencesMatch(left, right) {
  if (left.type !== right.type) return false;
  if (left.type === "domain") return left.value.toLowerCase() === right.value.toLowerCase();
  if (left.type !== "site_url" && left.type !== "url") return left.value === right.value;
  try {
    return new URL(left.value).toString() === new URL(right.value).toString();
  } catch {
    return left.value === right.value;
  }
}
function lifecycleProvesExisting(lifecycleRecords, capability, resource) {
  return lifecycleRecords.some(
    (record3) => record3.capability === capability && record3.resourceRefs.some((reference) => resourceReferencesMatch(reference, resource))
  );
}
function lifecycleResourceValue(lifecycleRecords, type, capabilities) {
  const acceptedCapabilities = new Set(capabilities);
  const values = new Set(
    lifecycleRecords.filter(({ capability }) => acceptedCapabilities.has(capability)).flatMap(({ resourceRefs }) => resourceRefs).filter((reference) => reference.type === type).map(({ value }) => value)
  );
  return values.size === 1 ? [...values][0] : void 0;
}
function withLifecycleResources(handler, snapshot, lifecycleRecords) {
  const target = DEFAULT_PROVIDER_TARGETS[handler];
  const state = providerState(snapshot, target.provider);
  const reuse = target.provider === "github" ? { repository: ["repository", "repository_settings"] } : target.provider === "vercel" ? { project: ["project"] } : target.provider === "neon" ? {
    project_id: ["project", "database"],
    branch_id: ["database"],
    database_name: ["database"]
  } : target.provider === "bing" ? { site_url: ["site"] } : {};
  const externalResourceIds = { ...state.external_resource_ids };
  for (const [type, capabilities] of Object.entries(reuse)) {
    if (externalResourceIds[type]) continue;
    const value = lifecycleResourceValue(lifecycleRecords, type, capabilities);
    if (value) externalResourceIds[type] = value;
  }
  if (Object.keys(externalResourceIds).length === Object.keys(state.external_resource_ids).length) {
    return snapshot;
  }
  return {
    ...snapshot,
    providers: {
      ...snapshot.providers,
      providers: {
        ...snapshot.providers.providers,
        [target.provider]: { ...state, external_resource_ids: externalResourceIds }
      }
    }
  };
}
function request(target, credentialRef2, capabilities, inputs) {
  return {
    provider: target.provider,
    request: {
      environment: target.environment,
      credentialRef: credentialRef2,
      capabilities,
      inputs,
      dryRun: false
    }
  };
}
function githubRequest(handler, snapshot, lifecycleRecords, rootDir) {
  const target = DEFAULT_PROVIDER_TARGETS[handler];
  const state = providerState(snapshot, "github");
  const credentialRef2 = requireCredential(handler, "github", state.credential_ref);
  const repository = requireExternal(
    handler,
    state,
    "repository",
    "record the exact owner/name target; do not rely on the currently authenticated account"
  );
  const owner = requireValue(
    handler,
    state.team_id ?? state.account_id,
    "config/providers.yaml providers.github.team_id or account_id",
    "record the intended organization or account and confirm it matches the repository owner"
  );
  if (repository.split("/")[0] !== owner) {
    fail(
      handler,
      `repository owner ${repository.split("/")[0] ?? "(missing)"} does not match configured GitHub account/team ${owner}. Next: correct the target before any create or read-back`
    );
  }
  const lifecycleCapability = ["repository_settings", "repository"].find(
    (capability) => lifecycleProvesExisting(lifecycleRecords, capability, {
      type: "repository",
      value: repository
    })
  );
  const intent = state.external_resource_ids.repository_intent ?? (lifecycleCapability ? "use_verified" : requireExternal(
    handler,
    state,
    "repository_intent",
    "set create_from_source for a new repository or use_verified for a read-back-verified existing repository"
  ));
  if (intent === "create_from_source" || intent === "create_from_template") {
    return request(target, credentialRef2, ["repository"], {
      repository,
      sourceDirectory: resolve19(rootDir),
      visibility: snapshot.venture.venture.repository_visibility
    });
  }
  if (intent === "use_verified") {
    requireVerifiedExisting(
      handler,
      "github",
      state,
      "repository",
      lifecycleRecords,
      [lifecycleCapability ?? "repository"],
      [{ type: "repository", value: repository }]
    );
    return request(target, credentialRef2, ["repository_settings"], {
      repository,
      deleteBranchOnMerge: true
    });
  }
  return fail(
    handler,
    `unsupported repository_intent ${intent}. Next: choose create_from_source or use_verified; create_from_template is accepted only as a deprecated compatibility alias that still publishes verified local source`
  );
}
function vercelRequest(handler, snapshot, lifecycleRecords) {
  const target = DEFAULT_PROVIDER_TARGETS[handler];
  const state = providerState(snapshot, "vercel");
  const credentialRef2 = requireCredential(handler, "vercel", state.credential_ref);
  const project = requireExternal(
    handler,
    state,
    "project",
    "record the exact Vercel project slug to create or reuse"
  );
  const scope = requireValue(
    handler,
    state.team_id ?? state.account_id,
    "config/providers.yaml providers.vercel.team_id or account_id",
    "record the exact Vercel team/account scope so the active CLI account is never inferred"
  );
  if (handler === "provider.production-deploy") {
    requireVerifiedExisting(
      handler,
      "vercel",
      state,
      "Vercel project",
      lifecycleRecords,
      ["project"],
      [{ type: "project", value: project }]
    );
    return request(target, credentialRef2, ["deployment"], { project, scope });
  }
  const lifecycleProjectExists = lifecycleProvesExisting(lifecycleRecords, "project", {
    type: "project",
    value: project
  });
  const projectIntent = state.external_resource_ids.project_intent ?? (state.state === "verified" || lifecycleProjectExists ? "use_verified" : requireExternal(
    handler,
    state,
    "project_intent",
    "set create for a new Vercel project or use_verified for an existing project proven by provider read-back"
  ));
  if (projectIntent === "use_verified") {
    requireVerifiedExisting(
      handler,
      "vercel",
      state,
      "Vercel project",
      lifecycleRecords,
      ["project"],
      [{ type: "project", value: project }]
    );
  } else if (projectIntent === "create") {
    if (state.state === "verified" || lifecycleProjectExists) {
      fail(
        handler,
        `project_intent create conflicts with verified Vercel project ${project}. Next: set project_intent to use_verified or choose a new explicit project slug`
      );
    }
  } else {
    fail(
      handler,
      `unsupported project_intent ${projectIntent}. Next: choose create or use_verified`
    );
  }
  return request(
    target,
    credentialRef2,
    ["project", "deployment", ...snapshot.venture.venture.domain ? ["domain"] : []],
    {
      project,
      scope,
      projectIntent,
      ...snapshot.venture.venture.domain ? { domain: snapshot.venture.venture.domain } : {}
    }
  );
}
function vercelEnvironmentRequest(handler, snapshot) {
  const target = DEFAULT_PROVIDER_TARGETS[handler];
  const state = providerState(snapshot, "vercel");
  const credentialRef2 = requireCredential(handler, "vercel", state.credential_ref);
  const project = requireExternal(
    handler,
    state,
    "project",
    "record the exact Vercel project slug to configure"
  );
  const scope = requireValue(
    handler,
    state.team_id ?? state.account_id,
    "config/providers.yaml providers.vercel.team_id or account_id",
    "record the exact Vercel team/account scope"
  );
  const binding = {
    "provider.vercel-database-environment": {
      name: "DATABASE_URL",
      provider: "neon",
      path: "database_credential_ref"
    },
    "provider.vercel-stripe-environment": {
      name: "STRIPE_SECRET_KEY",
      provider: "stripe",
      path: "credential_ref"
    },
    "provider.vercel-stripe-webhook-environment": {
      name: "STRIPE_WEBHOOK_SECRET",
      provider: "stripe",
      path: "webhook_secret_credential_ref"
    },
    "provider.vercel-brevo-environment": {
      name: "BREVO_API_KEY",
      provider: "brevo",
      path: "credential_ref"
    },
    "provider.vercel-ga-environment": {
      name: "NEXT_PUBLIC_GA_MEASUREMENT_ID",
      provider: "google",
      path: "measurement_id_credential_ref"
    }
  };
  const selected = binding[handler];
  if (!selected) fail(handler, "unknown Vercel environment binding");
  const source = providerState(snapshot, selected.provider);
  const valueRef = selected.path === "credential_ref" ? source.credential_ref : source.external_resource_ids[selected.path];
  const environmentValueCredentialRef = requireCredential(
    handler,
    selected.provider,
    valueRef,
    `config/providers.yaml providers.${selected.provider}.${selected.path === "credential_ref" ? "credential_ref" : `external_resource_ids.${selected.path}`}`
  );
  return request(target, credentialRef2, ["environment_variable"], {
    project,
    scope,
    environmentVariableName: selected.name,
    environmentTarget: "production",
    environmentValueCredentialRef
  });
}
function neonRequest(handler, snapshot, lifecycleRecords, rootDir) {
  const target = DEFAULT_PROVIDER_TARGETS[handler];
  const state = providerState(snapshot, "neon");
  const credentialRef2 = requireCredential(handler, "neon", state.credential_ref);
  const projectIntent = state.external_resource_ids.project_intent ?? "use_verified";
  if (projectIntent === "create") {
    const organizationId = requireExternal(
      handler,
      state,
      "organization_id",
      "record the exact Neon organization id from the Founder Stack; do not fall back to the credential account"
    );
    const projectName = state.external_resource_ids.project_name ?? requireValue(
      handler,
      snapshot.venture.venture.name,
      "config/venture.yaml venture.name",
      "record the exact Neon project name"
    );
    const regionId = requireValue(
      handler,
      state.region,
      "config/providers.yaml providers.neon.region",
      "record the exact Neon region id after checking availability and data-location requirements"
    );
    const databaseCredentialRef2 = requireCredential(
      handler,
      "neon",
      state.external_resource_ids.database_credential_ref,
      "config/providers.yaml providers.neon.external_resource_ids.database_credential_ref"
    );
    return request(
      target,
      credentialRef2,
      ["project", "schema_migration", "read_write_health_check"],
      {
        organizationId,
        projectName,
        regionId,
        databaseCredentialRef: databaseCredentialRef2,
        workingDirectory: resolve19(rootDir)
      }
    );
  }
  if (projectIntent !== "use_verified") {
    fail(
      handler,
      `unsupported project_intent ${projectIntent}. Next: choose create or use_verified`
    );
  }
  const projectId = requireExternal(
    handler,
    state,
    "project_id",
    "record the project id returned by Neon read-back"
  );
  const branchId = requireExternal(
    handler,
    state,
    "branch_id",
    "record the branch id returned by Neon read-back"
  );
  const databaseName = requireExternal(
    handler,
    state,
    "database_name",
    "record the database name returned by Neon read-back"
  );
  requireVerifiedExisting(
    handler,
    "neon",
    state,
    "Neon project, branch, and database",
    lifecycleRecords,
    ["project", "database"],
    [
      { type: "project_id", value: projectId },
      { type: "branch_id", value: branchId },
      { type: "database_name", value: databaseName }
    ]
  );
  const databaseCredentialRef = requireCredential(
    handler,
    "neon",
    state.external_resource_ids.database_credential_ref,
    "config/providers.yaml providers.neon.external_resource_ids.database_credential_ref"
  );
  return request(target, credentialRef2, ["schema_migration", "read_write_health_check"], {
    projectId,
    branchId,
    databaseName,
    databaseCredentialRef,
    workingDirectory: resolve19(rootDir)
  });
}
function exactMinorUnits(handler, amount, path) {
  const minorUnits2 = amount * 100;
  if (!Number.isSafeInteger(minorUnits2) || minorUnits2 < 0) {
    fail(
      handler,
      `${path} must be a non-negative amount with no more than two decimal places. Next: correct the approved offer price before creating an immutable Stripe price`
    );
  }
  return minorUnits2;
}
function stripeRequest(handler, snapshot) {
  const target = DEFAULT_PROVIDER_TARGETS[handler];
  const state = providerState(snapshot, "stripe");
  const credentialRef2 = requireCredential(handler, "stripe", state.credential_ref);
  const mode = requireExternal(
    handler,
    state,
    "mode",
    "set mode to test after registering a restricted Stripe test-mode credential"
  );
  if (mode !== "test") {
    fail(
      handler,
      `Stripe mode is ${mode}. Next: use an explicitly registered test-mode credential and set providers.stripe.external_resource_ids.mode to test; the default launch node never creates live prices`
    );
  }
  const configuredPrices = [
    snapshot.offer.pricing.monthly_price === null ? null : {
      amount: snapshot.offer.pricing.monthly_price,
      interval: "month",
      path: "config/offer.yaml pricing.monthly_price"
    },
    snapshot.offer.pricing.annual_price === null ? null : {
      amount: snapshot.offer.pricing.annual_price,
      interval: "year",
      path: "config/offer.yaml pricing.annual_price"
    }
  ].filter((value) => value !== null);
  if (configuredPrices.length !== 1) {
    fail(
      handler,
      configuredPrices.length === 0 ? "no approved monthly or annual price exists in config/offer.yaml. Next: record the exact displayed price before planning Stripe" : "both monthly and annual prices are active, but the built-in node cannot yet prove two immutable Stripe price resources without omitting one. Next: select one launch price or inject a multi-price factory"
    );
  }
  const [configuredPrice] = configuredPrices;
  const domain = requireValue(
    handler,
    snapshot.venture.venture.domain,
    "config/venture.yaml venture.domain",
    "record the exact callback domain before creating the Stripe webhook"
  );
  const productName = requireValue(
    handler,
    snapshot.venture.venture.name,
    "config/venture.yaml venture.name",
    "record the exact product name"
  );
  const webhookSecretCredentialRef = requireCredential(
    handler,
    "stripe",
    state.external_resource_ids.webhook_secret_credential_ref,
    "config/providers.yaml providers.stripe.external_resource_ids.webhook_secret_credential_ref"
  );
  return request(target, credentialRef2, ["product", "price", "webhook", "billing_portal"], {
    productName,
    ...snapshot.offer.offer.sentence ? { productDescription: snapshot.offer.offer.sentence } : {},
    productId: "{dependency.product.id}",
    currency: snapshot.offer.pricing.currency.toLowerCase(),
    unitAmount: exactMinorUnits(handler, configuredPrice.amount, configuredPrice.path),
    recurringInterval: configuredPrice.interval,
    webhookUrl: `https://${domain}/api/stripe/webhook`,
    webhookSecretCredentialRef,
    enabledEvents: [
      "checkout.session.completed",
      "customer.subscription.updated",
      "customer.subscription.deleted",
      "invoice.payment_failed"
    ],
    headline: `Manage ${productName}`
  });
}
function brevoRequest(handler, snapshot, workflow) {
  const target = DEFAULT_PROVIDER_TARGETS[handler];
  const state = providerState(snapshot, "brevo");
  const credentialRef2 = requireCredential(handler, "brevo", state.credential_ref);
  const senderName = requireExternal(
    handler,
    state,
    "sender_name",
    "record the reviewed sender display name"
  );
  const senderEmail = requireExternal(
    handler,
    state,
    "sender_email",
    "record the reviewed sender address on the verified domain"
  );
  const senderDomain = senderEmail.slice(senderEmail.lastIndexOf("@") + 1);
  if (!senderEmail.includes("@") || !senderDomain) {
    fail(handler, "sender_email is not an address on a reviewable sending domain");
  }
  if (handler === "provider.brevo-sending-domain") {
    return request(target, credentialRef2, ["sending_domain"], { domainName: senderDomain });
  }
  if (handler === "provider.brevo-domain-verification") {
    const dns = workflow.dependencyOutputs["dns-records"];
    const records = dns && !Array.isArray(dns) && typeof dns === "object" && Array.isArray(dns.records) ? dns.records : [];
    if (!records.some(
      (record3) => record3 && !Array.isArray(record3) && typeof record3 === "object" && record3.source_provider === "brevo"
    )) {
      fail(
        handler,
        "the same-run DNS evidence does not contain the ordered Brevo records. Next: complete the consolidated DNS node with its exact typed output"
      );
    }
    return request(target, credentialRef2, ["sending_domain_verification"], {
      domainName: senderDomain
    });
  }
  const templateName = requireExternal(
    handler,
    state,
    "template_name",
    "record the reviewed inactive template name"
  );
  const templateSubject = requireExternal(
    handler,
    state,
    "template_subject",
    "record the reviewed template subject"
  );
  const templateHtml = requireExternal(
    handler,
    state,
    "template_html",
    "record reviewed HTML or inject a factory that reads a repository template artifact"
  );
  const webhookUrl = state.external_resource_ids.webhook_url;
  const webhookEvents = state.external_resource_ids.webhook_events?.split(",").map((event) => event.trim()).filter(Boolean);
  return request(
    target,
    credentialRef2,
    ["sender", "template", ...webhookUrl ? ["webhook"] : []],
    {
      senderName,
      senderEmail,
      templateName,
      templateSubject,
      templateHtml,
      ...webhookUrl ? {
        webhookUrl,
        webhookEvents: webhookEvents && webhookEvents.length > 0 ? webhookEvents : ["delivered", "hardBounce", "spam"]
      } : {}
    }
  );
}
function dependencyIdentifier(handler, workflow, dependency, type) {
  return requireValue(
    handler,
    publicIdentifier(workflow.dependencyOutputs[dependency], type),
    `${dependency}.output.publicOutputs.identifiers.${type}`,
    `resume the same run after ${dependency} has provider read-back evidence with one exact ${type}`
  );
}
function googleRequest(handler, snapshot, workflow) {
  const target = DEFAULT_PROVIDER_TARGETS[handler];
  const state = providerState(snapshot, "google");
  const credentialRef2 = requireCredential(handler, "google", state.credential_ref);
  const domain = requireValue(
    handler,
    snapshot.venture.venture.domain,
    "config/venture.yaml venture.domain",
    "record the exact production domain before Google setup"
  );
  const siteUrl = state.external_resource_ids.site_url ?? `sc-domain:${domain}`;
  const sitemapUrl = state.external_resource_ids.sitemap_url ?? `https://${domain}/sitemap.xml`;
  switch (handler) {
    case "provider.google-analytics-property": {
      const analyticsAccountId = requireExternal(
        handler,
        state,
        "analytics_account_id",
        "record the exact GA4 account id returned by account lookup"
      );
      const propertyDisplayName = state.external_resource_ids.property_display_name ?? requireValue(
        handler,
        snapshot.venture.venture.name,
        "config/venture.yaml venture.name",
        "record a reviewed GA4 property name"
      );
      return request(target, credentialRef2, ["analytics_property"], {
        analyticsAccountId,
        propertyDisplayName,
        reportingTimeZone: snapshot.venture.venture.timezone,
        currencyCode: snapshot.venture.venture.currency
      });
    }
    case "provider.google-analytics-stream":
      return request(target, credentialRef2, ["analytics_web_stream"], {
        analyticsPropertyId: dependencyIdentifier(
          handler,
          workflow,
          "google-analytics-property",
          "property_id"
        ),
        streamDisplayName: state.external_resource_ids.stream_display_name ?? `${domain} web`,
        defaultUri: `https://${domain}/`,
        measurementIdCredentialRef: requireCredential(
          handler,
          "google",
          state.external_resource_ids.measurement_id_credential_ref,
          "config/providers.yaml providers.google.external_resource_ids.measurement_id_credential_ref"
        )
      });
    case "provider.google-site-dns-record":
      return request(target, credentialRef2, ["site_verification_token"], {
        siteIdentifier: domain,
        siteType: "INET_DOMAIN",
        verificationMethod: "DNS_TXT",
        dnsTtl: 3600
      });
    case "provider.google-site-verification": {
      const dns = workflow.dependencyOutputs["dns-records"];
      const records = dns && !Array.isArray(dns) && typeof dns === "object" && Array.isArray(dns.records) ? dns.records : [];
      if (!records.some(
        (record3) => record3 && !Array.isArray(record3) && typeof record3 === "object" && record3.source_provider === "google"
      )) {
        fail(
          handler,
          "the same-run DNS evidence does not contain the Google token record. Next: complete the consolidated DNS node with its exact typed output"
        );
      }
      return request(target, credentialRef2, ["site_verification"], {
        siteIdentifier: domain,
        siteType: "INET_DOMAIN",
        verificationMethod: "DNS_TXT"
      });
    }
    case "provider.google-search-console":
      return request(target, credentialRef2, ["search_console_site", "search_console_sitemap"], {
        siteUrl,
        sitemapUrl
      });
    default:
      return fail(handler, "unknown staged Google handler");
  }
}
function bingRequest(handler, snapshot, lifecycleRecords) {
  const target = DEFAULT_PROVIDER_TARGETS[handler];
  const state = providerState(snapshot, "bing");
  const credentialRef2 = requireCredential(handler, "bing", state.credential_ref);
  const domain = requireValue(
    handler,
    snapshot.venture.venture.domain,
    "config/venture.yaml venture.domain",
    "record the exact verified production domain"
  );
  const authMode = requireExternal(
    handler,
    state,
    "auth_mode",
    "set api_key or oauth to match the registered credential kind"
  );
  if (authMode !== "api_key" && authMode !== "oauth") {
    fail(handler, `unsupported Bing auth_mode ${authMode}. Next: choose api_key or oauth`);
  }
  const siteUrl = state.external_resource_ids.site_url ?? `https://${domain}`;
  const sitemapUrl = state.external_resource_ids.sitemap_url ?? `https://${domain}/sitemap.xml`;
  const siteAlreadyVerified = lifecycleProvesExisting(lifecycleRecords, "site", {
    type: "site_url",
    value: siteUrl
  });
  const siteIntent = state.external_resource_ids.site_intent ?? (state.state === "verified" || siteAlreadyVerified ? "use_verified" : "create");
  if (siteIntent === "use_verified") {
    requireVerifiedExisting(
      handler,
      "bing",
      state,
      "owned Bing site",
      lifecycleRecords,
      ["site"],
      [{ type: "site_url", value: siteUrl }]
    );
    return request(target, credentialRef2, ["sitemap"], { authMode, siteUrl, sitemapUrl });
  }
  if (siteIntent !== "create") {
    fail(
      handler,
      `unsupported Bing site_intent ${siteIntent}. Next: choose create or use_verified`
    );
  }
  return request(target, credentialRef2, ["site", "sitemap"], {
    authMode,
    siteUrl,
    sitemapUrl
  });
}
function easRequest(handler, snapshot, workflow, rootDir) {
  const target = DEFAULT_PROVIDER_TARGETS[handler];
  const state = providerState(snapshot, "eas");
  const projectId = requireValue(
    handler,
    snapshot.mobile.mobile.eas.project_id,
    "config/mobile.yaml mobile.eas.project_id",
    "run the reviewed EAS project-link step and record its read-back id"
  );
  const credentialRef2 = requireCredential(
    handler,
    "eas",
    snapshot.mobile.mobile.eas.credential_ref ?? state.credential_ref,
    "config/mobile.yaml mobile.eas.credential_ref or config/providers.yaml providers.eas.credential_ref"
  );
  const buildProfile = snapshot.mobile.mobile.eas.build_profiles.includes("production") ? "production" : fail(
    handler,
    "config/mobile.yaml mobile.eas.build_profiles does not contain production. Next: add a reviewed production build profile"
  );
  const projectDirectory = resolve19(rootDir, "mobile/expo");
  if (handler === "provider.eas-build") {
    return request(target, credentialRef2, ["ios_build"], {
      projectId,
      buildProfile,
      projectDirectory
    });
  }
  const apple = workflow.dependencyOutputs["apple-first-app-record"];
  if (!apple || Array.isArray(apple) || typeof apple !== "object") {
    fail(handler, "the same-run first App Store Connect record output is missing");
  }
  const appStoreAppId = requireValue(
    handler,
    typeof apple.apple_app_id === "string" ? apple.apple_app_id : void 0,
    "apple-first-app-record.output.apple_app_id",
    "complete the explicit Apple record manual node"
  );
  const bundleId = requireValue(
    handler,
    typeof apple.bundle_identifier === "string" ? apple.bundle_identifier : void 0,
    "apple-first-app-record.output.bundle_identifier",
    "complete the explicit Apple record manual node"
  );
  if (snapshot.mobile.mobile.bundle_identifier && snapshot.mobile.mobile.bundle_identifier !== bundleId) {
    fail(
      handler,
      `Apple bundle identifier ${bundleId} does not match config/mobile.yaml ${snapshot.mobile.mobile.bundle_identifier}. Next: correct the mismatch before submission`
    );
  }
  const easBuildId = dependencyIdentifier(handler, workflow, "eas-build", "build_id");
  return request(target, credentialRef2, ["app_store_connection", "ios_submit"], {
    projectId,
    projectDirectory,
    appStoreAppId,
    bundleId,
    easBuildId,
    submitProfile: "production"
  });
}
function testflightRequest(handler, snapshot, workflow) {
  const target = DEFAULT_PROVIDER_TARGETS[handler];
  const state = providerState(snapshot, "app_store_connect");
  const credentialRef2 = requireCredential(
    handler,
    "app_store_connect",
    snapshot.mobile.mobile.app_store_connect.credential_ref ?? state.credential_ref,
    "config/mobile.yaml mobile.app_store_connect.credential_ref or config/providers.yaml providers.app_store_connect.credential_ref"
  );
  const apple = workflow.dependencyOutputs["apple-first-app-record"];
  if (!apple || Array.isArray(apple) || typeof apple !== "object") {
    fail(handler, "the same-run first App Store Connect record output is missing");
  }
  const appStoreAppId = requireValue(
    handler,
    typeof apple.apple_app_id === "string" ? apple.apple_app_id : void 0,
    "apple-first-app-record.output.apple_app_id",
    "complete the explicit Apple record manual node"
  );
  const appVersion = dependencyIdentifier(handler, workflow, "eas-build", "app_version");
  const buildNumber = dependencyIdentifier(handler, workflow, "eas-build", "build_number");
  const betaGroupName = state.external_resource_ids.beta_group_name ?? `${requireValue(handler, snapshot.venture.venture.name, "config/venture.yaml venture.name", "record the venture name")} Internal`;
  return request(
    target,
    credentialRef2,
    ["build_processing", "testflight_group", "build_group_assignment"],
    {
      appStoreAppId,
      appVersion,
      buildNumber,
      betaGroupName,
      isInternalGroup: true,
      appStoreBuildId: "{dependency.build_processing.data.0.id}",
      betaGroupId: "{dependency.testflight_group.data.id}"
    }
  );
}
function unsupportedRequest(handler, snapshot) {
  const target = DEFAULT_PROVIDER_TARGETS[handler];
  void snapshot.offer;
  void snapshot.mobile;
  void providerState(snapshot, target.provider);
  const reasons = {
    "provider.stripe-commerce": "the adapter cannot feed a newly read-back Stripe product id into exact-price, portal, checkout, and webhook operations in one node. Next: complete config/offer.yaml pricing and inject a staged typed factory that verifies each test-mode resource",
    "provider.dns-records": "the complete additive record set is produced by upstream provider read-back and cannot be inferred from a single configured value. Next: use the compiled manual DNS node or inject a record-set factory that preserves mail records and verifies authoritative DNS",
    "provider.revenuecat-entitlements": "the adapter does not yet compose Test Store products and packages with app, entitlement, offering, and webhook read-back. Next: finish the manual RevenueCat project/key prerequisite and inject a complete staged factory"
  };
  return fail(
    handler,
    reasons[handler] ?? `no complete built-in composition is registered for ${target.provider}. Next: inject a typed factory with apply and read-back coverage`
  );
}
function buildDefaultRequest(handler, snapshot, lifecycleRecords, workflow, rootDir) {
  switch (handler) {
    case "provider.github-repository":
      return githubRequest(handler, snapshot, lifecycleRecords, rootDir);
    case "provider.vercel-project":
    case "provider.production-deploy":
      return vercelRequest(handler, snapshot, lifecycleRecords);
    case "provider.vercel-database-environment":
    case "provider.vercel-stripe-environment":
    case "provider.vercel-stripe-webhook-environment":
    case "provider.vercel-brevo-environment":
    case "provider.vercel-ga-environment":
      return vercelEnvironmentRequest(handler, snapshot);
    case "provider.neon-database":
      return neonRequest(handler, snapshot, lifecycleRecords, rootDir);
    case "provider.stripe-commerce":
      return stripeRequest(handler, snapshot);
    case "provider.brevo-sending-domain":
    case "provider.brevo-domain-verification":
    case "provider.brevo-email":
      return brevoRequest(handler, snapshot, workflow);
    case "provider.google-analytics-property":
    case "provider.google-analytics-stream":
    case "provider.google-site-dns-record":
    case "provider.google-site-verification":
    case "provider.google-search-console":
      return googleRequest(handler, snapshot, workflow);
    case "provider.bing-discovery":
      return bingRequest(handler, snapshot, lifecycleRecords);
    case "provider.eas-build":
    case "provider.eas-submit":
      return easRequest(handler, snapshot, workflow, rootDir);
    case "provider.testflight-state":
      return testflightRequest(handler, snapshot, workflow);
    default:
      return unsupportedRequest(handler, snapshot);
  }
}
function createDefaultProviderPlanFactories(options) {
  const handlers = options.definition.nodes.filter((node) => node.kind === "provider" && node.handler).map((node) => node.handler);
  return Object.fromEntries(
    handlers.map((handler) => {
      if (!DEFAULT_PROVIDER_TARGETS[handler]) {
        throw new ProviderFactoryPrerequisiteError(
          handler,
          "the launch graph contains an unknown provider handler. Next: register an explicit typed target"
        );
      }
      const factory = async (workflow) => {
        if (workflow.node.handler !== handler) {
          fail(
            handler,
            `factory was invoked for ${workflow.node.handler ?? workflow.node.id}. Next: fix the runtime handler map`
          );
        }
        const brief = currentBrief(options.brief);
        if (brief.id !== options.definition.id.replace(/^launch-/, "")) {
          fail(
            handler,
            `founder brief ${brief.id} does not match graph ${options.definition.id}. Next: recreate the launch plan from the current brief`
          );
        }
        const snapshot = (options.loadConfig ?? (() => loadDefaultProviderConfig(options.rootDir)))();
        let lifecycleRecords = [];
        if (options.lifecycleStore) {
          try {
            lifecycleRecords = (await options.lifecycleStore.list()).filter(
              ({ provider, environment, capability }) => {
                const target = DEFAULT_PROVIDER_TARGETS[handler];
                if (provider !== target.provider) return false;
                if (environment === target.environment) return true;
                return handler === "provider.production-deploy" && provider === "vercel" && target.environment === "production" && environment === "preview" && capability === "project";
              }
            );
          } catch {
            fail(
              handler,
              "verified provider lifecycle state is corrupt or unreadable. Next: restore it from trusted read-back evidence before planning or applying provider effects"
            );
          }
        }
        return buildDefaultRequest(
          handler,
          withLifecycleResources(handler, snapshot, lifecycleRecords),
          lifecycleRecords,
          workflow,
          options.rootDir
        );
      };
      return [handler, factory];
    })
  );
}
function adapterCapabilities(provider, ventureCapabilities) {
  const mapping = ADAPTER_CAPABILITIES[provider] ?? {};
  return [...new Set(ventureCapabilities.flatMap((capability) => mapping[capability] ?? []))];
}
function compatibleKinds(provider, capabilities, registry) {
  const descriptor2 = registry.get(provider).descriptor;
  const specific = (descriptor2.credentialRequirements ?? []).filter(
    (requirement) => requirement.capabilities.some((capability) => capabilities.includes(capability))
  ).flatMap((requirement) => requirement.acceptedKinds);
  return [
    ...new Set(
      specific.length > 0 ? specific : descriptor2.authMethods.filter(
        (method) => method !== "manual" && method !== "none"
      )
    )
  ];
}
function missingKinds(provider, capabilities, inspections, registry) {
  const descriptor2 = registry.get(provider).descriptor;
  const requirements = (descriptor2.credentialRequirements ?? []).filter(
    (requirement) => requirement.capabilities.some((capability) => capabilities.includes(capability))
  );
  if (requirements.length === 0) {
    const accepted = compatibleKinds(provider, capabilities, registry);
    return accepted.some((kind) => inspections.some((inspection) => inspection.kind === kind)) ? [] : accepted;
  }
  return [
    ...new Set(
      requirements.flatMap(
        (requirement) => requirement.acceptedKinds.some(
          (kind) => inspections.some((inspection) => inspection.kind === kind)
        ) ? [] : requirement.acceptedKinds
      )
    )
  ];
}
function authenticated(inspection) {
  return inspection.status === "available" && (inspection.kind === "cli_session" && inspection.backend === "cli_session" || inspection.testStatus === "passed" && inspection.testedAt !== void 0);
}
function dummyHandlerContext(node) {
  return {
    runId: "doctor-plan-only",
    node,
    attempt: 1,
    dependencyOutputs: {},
    idempotencyKey: `doctor:${node.id}`,
    signal: new AbortController().signal,
    trace: () => void 0
  };
}
function planAvailability(available, status, blockers = [], requirement = null) {
  return { available, status, blockers, requirement };
}
async function inspectCredentialRefs(broker) {
  return Promise.all(
    broker.list().map(async (reference) => {
      try {
        return await broker.inspect(reference.ref);
      } catch (error) {
        return {
          ...reference,
          status: "unavailable",
          writable: false,
          message: broker.redactor.redactText(
            error instanceof Error ? error.message : String(error)
          )
        };
      }
    })
  );
}
function cliChecksForProvider(provider, results) {
  const ids = new Set(CLI_IDS_BY_PROVIDER[provider] ?? []);
  return results.filter(({ id }) => ids.has(id));
}
async function inspectProviderPlan(provider, registry, launch) {
  if (!launch) {
    return {
      handlers: [],
      availability: planAvailability(false, "brief_required", [
        "No founder brief is selected. Next: run vh create --brief <file>."
      ])
    };
  }
  const nodes = launch.definition.nodes.filter(
    (node) => node.kind === "provider" && node.handler && DEFAULT_PROVIDER_TARGETS[node.handler]?.provider === provider
  );
  if (nodes.length === 0) {
    return { handlers: [], availability: planAvailability(false, "not_required") };
  }
  const factories = launch.factories ?? createDefaultProviderPlanFactories({
    rootDir: "",
    brief: launch.brief,
    definition: launch.definition,
    loadConfig: () => {
      throw new Error("doctor factory config loader was not initialized");
    }
  });
  const blockers = [];
  for (const node of nodes) {
    const factory = factories[node.handler];
    if (!factory) {
      blockers.push(`${node.handler}: no provider plan factory is registered`);
      continue;
    }
    try {
      const target = await factory(dummyHandlerContext(node));
      if (target.provider !== provider) {
        blockers.push(`${node.handler}: resolved ${target.provider}, expected ${provider}`);
        continue;
      }
      const plan = (target.adapter ?? registry.get(provider)).plan({
        ...target.request,
        dryRun: true
      });
      if (plan.operations.length === 0) {
        blockers.push(`${node.handler}: the dry-run plan contains no complete operation`);
      }
    } catch (error) {
      blockers.push(error instanceof Error ? error.message : String(error));
    }
  }
  return {
    handlers: nodes.map((node) => node.handler),
    availability: blockers.length === 0 ? planAvailability(true, "available") : planAvailability(false, "blocked", blockers)
  };
}
async function inspectDefaultProviderDoctor(options) {
  const registry = options.registry ?? providerRegistry;
  const snapshot = loadDefaultProviderConfig(options.rootDir);
  const cliPrerequisites = await inspectCliPrerequisites(options.runner, {
    prerequisites: options.prerequisites,
    redactor: options.context.redactor
  });
  const inspections = await inspectCredentialRefs(options.broker);
  const launch = options.launch ? {
    ...options.launch,
    factories: options.launch.factories ?? createDefaultProviderPlanFactories({
      rootDir: options.rootDir,
      brief: options.launch.brief,
      definition: options.launch.definition,
      lifecycleStore: options.lifecycleStore
    })
  } : void 0;
  const providerChecks = await Promise.all(
    registry.list().map(async (adapter) => {
      const provider = adapter.descriptor.id;
      const configured = snapshot.providers.providers[provider];
      const requestedCapabilities = adapterCapabilities(provider, configured?.capability_ids ?? []);
      const refs = inspections.filter((inspection) => inspection.provider === provider);
      const configuredRefs = configured?.credential_ref ? [configured.credential_ref] : [];
      const credentialRefs = [.../* @__PURE__ */ new Set([...configuredRefs, ...refs.map(({ ref }) => ref)])];
      const doctor = await adapter.doctor(
        { credentialRefs, requiredCapabilities: requestedCapabilities },
        options.context
      );
      const cliChecks = cliChecksForProvider(provider, cliPrerequisites);
      const cliBlocked = cliChecks.some(({ status }) => status !== "installed");
      const requiredScopes = [
        .../* @__PURE__ */ new Set([...adapter.descriptor.requiredScopes, ...configured?.required_scopes ?? []])
      ];
      const authenticatedRefs = refs.filter(authenticated);
      const declaredScopes = new Set(authenticatedRefs.flatMap(({ scopes }) => scopes));
      const missingScopes = requiredScopes.filter(
        (scope) => !declaredScopes.has(scope) && !declaredScopes.has("*")
      );
      const acceptedKinds = compatibleKinds(provider, requestedCapabilities, registry);
      const missingCredentialKinds = adapter.descriptor.transports.every(
        (transport) => transport === "manual"
      ) ? [] : missingKinds(provider, requestedCapabilities, authenticatedRefs, registry);
      const plan = await inspectProviderPlan(provider, registry, launch);
      const manualOnly = adapter.descriptor.transports.every((transport) => transport === "manual");
      const authenticationRequired = acceptedKinds.length > 0;
      const authenticationProven = !authenticationRequired || missingCredentialKinds.length === 0;
      const doctorReady = doctor.status === "ready" && !cliBlocked && authenticationProven;
      const applyBlockers = [
        ...plan.availability.blockers,
        ...cliBlocked ? cliChecks.filter(({ status }) => status !== "installed").map(({ nextAction: nextAction2, binary }) => nextAction2 ?? `${binary} is unavailable`) : [],
        ...doctor.status === "ready" ? [] : doctor.issues.map(({ message, remediation }) => `${message}. Next: ${remediation}`),
        ...!authenticationProven ? [
          `No available credential has durable authenticated provider evidence for required kind(s): ${missingCredentialKinds.join(", ")}. Next: for API/OAuth/JWT refs inject an official remote credential tester and run vh auth test ${provider}; local backend availability alone is not authorization proof. For CLI sessions, run the official provider login and repeat vh doctor.`
        ] : []
      ];
      const applyAvailability = manualOnly ? planAvailability(
        false,
        "manual_only",
        applyBlockers,
        "Complete the declared human action and attach read-back evidence."
      ) : plan.availability.available && doctorReady ? planAvailability(
        true,
        "available",
        [],
        "Apply still requires a reviewed, unexpired run authorization envelope."
      ) : planAvailability(false, "blocked", [...new Set(applyBlockers)]);
      return {
        ...doctor,
        effectiveStatus: cliBlocked && doctor.status === "ready" ? "unavailable" : !authenticationProven && doctor.status === "ready" ? "auth_required" : doctor.status,
        configuredState: configured?.state ?? "unconfigured",
        configuredCredentialRef: configured?.credential_ref ?? null,
        configuredAccountId: configured?.account_id ?? null,
        configuredTeamId: configured?.team_id ?? null,
        configuredRegion: configured?.region ?? null,
        requestedCapabilities,
        registeredCredentialRefs: refs.map(({ ref }) => ref),
        authenticatedCredentialRefs: authenticatedRefs.map(({ ref }) => ref),
        expiredCredentialRefs: refs.filter(({ status }) => status === "expired").map(({ ref, expiresAt }) => ({ ref, expiresAt: expiresAt ?? null })),
        missingExpiryCredentialRefs: refs.filter(({ expiresAt }) => expiresAt === void 0).map(({ ref }) => ref),
        testedCredentialRefs: refs.filter(({ testedAt }) => testedAt !== void 0).map(({ ref, testedAt, testStatus }) => ({
          ref,
          testedAt,
          testStatus: testStatus ?? "unknown"
        })),
        missingCredentialKinds,
        missingScopes,
        manualOnly,
        cliPrerequisites: cliChecks,
        planHandlers: plan.handlers,
        dryRunAvailability: plan.availability,
        applyAvailability
      };
    })
  );
  return {
    cliPrerequisites,
    registeredCredentialRefs: inspections.map((inspection) => ({
      ref: inspection.ref,
      provider: inspection.provider,
      kind: inspection.kind,
      backend: inspection.backend,
      status: inspection.status,
      scopes: [...inspection.scopes],
      expiresAt: inspection.expiresAt ?? null,
      testedAt: inspection.testedAt ?? null,
      testStatus: inspection.testStatus ?? null,
      revokedAt: inspection.revokedAt ?? null
    })),
    authenticatedCredentialRefs: inspections.filter(authenticated).map(({ ref, provider, kind }) => ({ ref, provider, kind })),
    testedCredentialRefs: inspections.filter(({ testedAt }) => testedAt !== void 0).map(({ ref, provider, testedAt, testStatus }) => ({
      ref,
      provider,
      testedAt,
      testStatus: testStatus ?? "unknown"
    })),
    authenticationLimitation: "An available official CLI session or a persisted successful result from an injected official remote tester is durable authenticated evidence. Backend availability and failed/unknown tests are metadata only; doctor does not claim it validated them remotely.",
    manualOnlyProviders: registry.list().filter(
      (adapter) => adapter.descriptor.transports.every((transport) => transport === "manual")
    ).map((adapter) => adapter.descriptor.id),
    providerChecks
  };
}

// lib/cli/default-learning-runtime.ts
import { existsSync as existsSync11, readFileSync as readFileSync13 } from "node:fs";
import { resolve as resolve20 } from "node:path";
import { parse as parse6 } from "yaml";
var normalizedScalarSchema2 = external_exports.union([external_exports.string(), external_exports.number().finite(), external_exports.boolean(), external_exports.null()]);
var dataSourceSchema = external_exports.enum(DATA_SOURCE_IDS);
var normalizedDatasetSchema = external_exports.object({
  id: external_exports.string().min(1),
  provenance: external_exports.object({
    source: dataSourceSchema,
    sourceAccount: external_exports.string().min(1),
    fetchedAt: external_exports.string().datetime({ offset: true }),
    reportingWindow: external_exports.object({
      start: external_exports.string().datetime({ offset: true }),
      end: external_exports.string().datetime({ offset: true })
    }).strict(),
    timezone: external_exports.string().min(1),
    dimensions: external_exports.array(external_exports.string()),
    quality: external_exports.enum(["complete", "partial", "sampled", "thresholded", "stale", "unavailable"]),
    limitations: external_exports.array(external_exports.string()),
    releaseVersion: external_exports.string().nullable()
  }).strict(),
  rows: external_exports.array(external_exports.record(normalizedScalarSchema2))
}).strict();
var freshnessSchema = external_exports.object({
  source: dataSourceSchema,
  status: external_exports.enum(["fresh", "stale", "missing"]),
  fetchedAt: external_exports.string().datetime({ offset: true }).nullable(),
  ageHours: external_exports.number().finite().nonnegative().nullable(),
  freshnessHours: external_exports.number().finite().positive(),
  required: external_exports.boolean(),
  limitation: external_exports.string().nullable()
}).strict();
var persistedDataSyncSchema = external_exports.object({
  schemaVersion: external_exports.literal(1),
  status: external_exports.enum(["complete", "incomplete", "not_configured"]),
  generatedAt: external_exports.string().datetime({ offset: true }),
  datasets: external_exports.array(normalizedDatasetSchema),
  failures: external_exports.array(
    external_exports.object({
      source: dataSourceSchema,
      code: external_exports.enum([
        "connector_not_configured",
        "credential_missing",
        "provider_failed",
        "invalid_data"
      ]),
      message: external_exports.string().min(1),
      retryable: external_exports.boolean(),
      nextAction: external_exports.string().min(1)
    }).strict()
  ),
  freshness: external_exports.array(freshnessSchema),
  nextAction: external_exports.string().min(1)
}).strict().superRefine((value, ctx) => {
  value.datasets.forEach((dataset, index) => {
    try {
      const normalized = normalizeDataset({
        source: dataset.provenance.source,
        sourceAccount: dataset.provenance.sourceAccount,
        fetchedAt: dataset.provenance.fetchedAt,
        reportingWindow: dataset.provenance.reportingWindow,
        timezone: dataset.provenance.timezone,
        dimensions: dataset.provenance.dimensions,
        quality: dataset.provenance.quality === "stale" || dataset.provenance.quality === "unavailable" ? "partial" : dataset.provenance.quality,
        limitations: dataset.provenance.limitations,
        releaseVersion: dataset.provenance.releaseVersion,
        rows: dataset.rows
      });
      if (normalized.id !== dataset.id) {
        ctx.addIssue({
          code: external_exports.ZodIssueCode.custom,
          path: ["datasets", index, "id"],
          message: "dataset fingerprint does not match its normalized provenance and rows"
        });
      }
    } catch (error) {
      ctx.addIssue({
        code: external_exports.ZodIssueCode.custom,
        path: ["datasets", index],
        message: error instanceof Error ? error.message : "invalid normalized dataset"
      });
    }
  });
});
function readYaml2(path) {
  return parse6(readFileSync13(path, "utf8"));
}
function loadLoops(rootDir) {
  const path = resolve20(rootDir, "config/loops.yaml");
  return existsSync11(path) ? loopsSchema.parse(readYaml2(path)) : createDefaultLoopsConfig();
}
function loadProviders(rootDir) {
  const path = resolve20(rootDir, "config/providers.yaml");
  return existsSync11(path) ? providersSchema.parse(readYaml2(path)) : null;
}
function loadProviderLifecycle(rootDir) {
  const path = resolve20(rootDir, ".venture/provider-lifecycle.json");
  if (!existsSync11(path)) return [];
  let value;
  try {
    value = JSON.parse(readFileSync13(path, "utf8"));
  } catch {
    throw new Error(
      "Provider lifecycle state is corrupt JSON; restore verified evidence before data sync."
    );
  }
  return parseProviderLifecycleDocument(value);
}
function activeExperimentState(rootDir) {
  const path = resolve20(rootDir, "config/experiments.yaml");
  if (!existsSync11(path)) return { hypotheses: [], experiments: [] };
  const config = experimentsSchema.parse(readYaml2(path));
  const active = config.experiments.filter(
    (experiment) => ["approved", "running"].includes(experiment.status)
  );
  return {
    hypotheses: active.map((experiment) => `${experiment.id}: ${experiment.hypothesis}`),
    experiments: active.filter((experiment) => experiment.status === "running").map((experiment) => experiment.id)
  };
}
function loadTimezone(rootDir) {
  const path = resolve20(rootDir, "config/venture.yaml");
  if (!existsSync11(path)) return "UTC";
  return ventureSchema.parse(readYaml2(path)).venture.timezone;
}
function mergeRequirements(loops, injected) {
  const bySource = /* @__PURE__ */ new Map();
  for (const input of Object.values(loops.loops).flatMap((loop2) => loop2.inputs)) {
    const current = bySource.get(input.source);
    bySource.set(input.source, {
      source: input.source,
      required: input.required || (current?.required ?? false),
      freshnessHours: Math.min(input.freshness_hours, current?.freshnessHours ?? Infinity)
    });
  }
  for (const requirement of injected) {
    const current = bySource.get(requirement.source);
    bySource.set(requirement.source, {
      source: requirement.source,
      required: requirement.required || (current?.required ?? false),
      freshnessHours: Math.min(requirement.freshnessHours, current?.freshnessHours ?? Infinity)
    });
  }
  return [...bySource.values()].sort((left, right) => left.source.localeCompare(right.source));
}
function loopForCadence(loops, cadence) {
  const matches2 = Object.entries(loops.loops).filter(([, loop2]) => loop2.cadence === cadence);
  if (matches2.length !== 1) {
    throw new Error(`Expected exactly one ${cadence} learning loop, found ${matches2.length}.`);
  }
  return matches2[0];
}
function loopRequirements(loop2, injected) {
  const configured = loop2.inputs.map((input) => ({
    source: input.source,
    required: input.required,
    freshnessHours: input.freshness_hours
  }));
  return mergeRequirements(
    {
      contract_version: 1,
      loops: {
        selected: {
          ...loop2,
          inputs: configured.map((item) => ({
            source: item.source,
            required: item.required,
            freshness_hours: item.freshnessHours
          }))
        }
      },
      extensions: {}
    },
    injected
  );
}
function metricDefinitions(loop2) {
  if (loop2.metric_definitions.length === 0) return void 0;
  return loop2.metric_definitions.map((definition2) => ({
    id: definition2.id,
    source: definition2.source,
    filter: definition2.filter,
    value: definition2.operation === "sum" ? { operation: "sum", field: definition2.field } : { operation: "count_rows" },
    sampleSizeField: definition2.sample_size_field ?? void 0,
    limitation: definition2.limitation ?? void 0
  }));
}
function candidateRules(loop2) {
  return loop2.candidate_rules.map((rule) => ({
    id: rule.id,
    metricId: rule.metric_id,
    comparator: rule.comparator,
    threshold: rule.threshold,
    minimumSampleSize: rule.minimum_sample_size,
    journey: rule.journey,
    title: rule.title,
    confidence: rule.confidence,
    risk: rule.risk,
    effectTypes: rule.effect_types,
    protectsWinner: rule.protects_winner
  }));
}
function learningDefinition(id, loop2, requirements) {
  return {
    id,
    cadence: loop2.cadence,
    requiredSources: requirements.filter((requirement) => requirement.required).map(({ source, freshnessHours }) => ({ source, freshnessHours })),
    // An unconfigured loop must stop for evidence, never report a content-free success.
    primaryMetrics: loop2.primary_metrics.length > 0 ? loop2.primary_metrics.map((metric) => metric.id) : ["primary_success_signal"],
    guardrailMetrics: loop2.guardrail_metrics.map((metric) => metric.id),
    decisionRules: [...loop2.decision_rules],
    maximumActions: loop2.maximum_actions,
    maximumIterations: loop2.maximum_iterations,
    autonomy: loop2.autonomy,
    authorizedEffectTypes: [...loop2.authorized_effect_types],
    outputDestination: loop2.output_destination,
    nextRunAt: loop2.next_run_at,
    stopCondition: loop2.stop_condition
  };
}
function neonOptions(providers, requirements, lifecycle) {
  const requirement = requirements.find(({ source }) => source === "neon_commercial_evidence");
  const neon = providers?.providers.neon;
  const lifecycleRecords = lifecycle.filter(
    (record3) => record3.provider === "neon" && record3.state === "verified" && (record3.capability === "database" || record3.capability === "database_provisioning")
  );
  const uniqueLifecycleValue = (type) => {
    const values = new Set(
      lifecycleRecords.flatMap(
        (record3) => record3.resourceRefs.filter((reference) => reference.type === type).map((reference) => reference.value)
      )
    );
    return values.size === 1 ? [...values][0] : void 0;
  };
  if (!requirement || !neon || neon.state !== "verified" && lifecycleRecords.length === 0) {
    return void 0;
  }
  const credentialRef2 = neon.external_resource_ids.database_credential_ref ?? neon.credential_ref;
  const projectId = neon.external_resource_ids.project_id ?? uniqueLifecycleValue("project_id");
  const databaseName = neon.external_resource_ids.database_name ?? uniqueLifecycleValue("database_name");
  if (!credentialRef2 || !projectId || !databaseName) return void 0;
  return {
    credentialRef: credentialRef2,
    sourceAccount: `${projectId}/${databaseName}`,
    windowHours: requirement.freshnessHours,
    releaseVersion: null,
    required: requirement.required
  };
}
function uniqueValue(values) {
  const unique2 = new Set(values.filter((value) => Boolean(value?.trim())));
  return unique2.size === 1 ? [...unique2][0] : void 0;
}
function lifecycleValues(lifecycle, provider, capabilities, type) {
  return lifecycle.filter(
    (record3) => record3.provider === provider && record3.state === "verified" && capabilities.includes(record3.capability)
  ).flatMap(
    (record3) => record3.resourceRefs.filter((reference) => reference.type === type).map((reference) => reference.value)
  );
}
function providerVerified(providers, lifecycle, provider, capabilities) {
  return providers?.providers[provider]?.state === "verified" || lifecycle.some(
    (record3) => record3.provider === provider && capabilities.includes(record3.capability)
  );
}
function unavailableConnector(source, reason, nextAction2) {
  return {
    connector: new UnavailableDataConnector(`${source}.not-configured`, source, reason, nextAction2)
  };
}
function sourceRequirement(requirements, source) {
  return requirements.find((requirement) => requirement.source === source);
}
function directHttpConnectors(input) {
  const entries = [];
  const addUnavailable = (source, provider, detail) => entries.push(
    unavailableConnector(
      source,
      `${source} is declared but its official read connector is not configured: ${detail}`,
      `Run vh auth login ${provider}; obtain authorized provider read-back; record only the cred:// reference and required safe resource IDs in config/providers.yaml; then rerun vh data sync.`
    )
  );
  const gscRequirement = sourceRequirement(input.requirements, "gsc");
  if (gscRequirement) {
    const state = input.providers?.providers.google;
    const capabilities = ["search_console_site", "site"];
    const siteUrl = uniqueValue([
      state?.external_resource_ids.site_url,
      ...lifecycleValues(input.lifecycle, "google", capabilities, "site_url")
    ]);
    if (!providerVerified(input.providers, input.lifecycle, "google", capabilities)) {
      addUnavailable("gsc", "google", "a verified Search Console site is required");
    } else if (!state?.credential_ref || !siteUrl) {
      addUnavailable(
        "gsc",
        "google",
        "providers.google.credential_ref and one unambiguous site_url are required"
      );
    } else {
      entries.push({
        credentialRef: state.credential_ref,
        connector: createGoogleSearchConsoleConnector({
          broker: input.broker,
          fetcher: input.fetcher,
          siteUrl,
          sourceAccount: siteUrl,
          timezone: "America/Los_Angeles",
          windowHours: gscRequirement.freshnessHours
        })
      });
    }
  }
  const ga4Requirement = sourceRequirement(input.requirements, "ga4");
  if (ga4Requirement) {
    const state = input.providers?.providers.google;
    const capabilities = ["analytics_property"];
    const propertyId = uniqueValue([
      state?.external_resource_ids.property_id,
      ...lifecycleValues(input.lifecycle, "google", capabilities, "property_id")
    ]);
    if (!providerVerified(input.providers, input.lifecycle, "google", capabilities)) {
      addUnavailable("ga4", "google", "a verified GA4 property is required");
    } else if (!state?.credential_ref || !propertyId || !/^\d+$/.test(propertyId)) {
      addUnavailable(
        "ga4",
        "google",
        "providers.google.credential_ref and one numeric property_id are required"
      );
    } else {
      entries.push({
        credentialRef: state.credential_ref,
        connector: createGoogleAnalyticsConnector({
          broker: input.broker,
          fetcher: input.fetcher,
          propertyId,
          sourceAccount: propertyId,
          timezone: input.timezone,
          windowHours: ga4Requirement.freshnessHours
        })
      });
    }
  }
  const bingRequirement = sourceRequirement(input.requirements, "bing_webmaster");
  if (bingRequirement) {
    const state = input.providers?.providers.bing;
    const capabilities = ["site"];
    const siteUrl = uniqueValue([
      state?.external_resource_ids.site_url,
      ...lifecycleValues(input.lifecycle, "bing", capabilities, "site_url")
    ]);
    const authMode = state?.external_resource_ids.auth_mode;
    if (!providerVerified(input.providers, input.lifecycle, "bing", capabilities)) {
      addUnavailable("bing_webmaster", "bing", "a verified Bing Webmaster site is required");
    } else if (!state?.credential_ref || !siteUrl || authMode !== "api_key" && authMode !== "oauth") {
      addUnavailable(
        "bing_webmaster",
        "bing",
        "providers.bing.credential_ref, one site_url, and external_resource_ids.auth_mode (api_key or oauth) are required"
      );
    } else {
      entries.push({
        credentialRef: state.credential_ref,
        connector: createBingWebmasterConnector({
          broker: input.broker,
          fetcher: input.fetcher,
          siteUrl,
          authMode,
          sourceAccount: siteUrl,
          timezone: "UTC",
          windowHours: bingRequirement.freshnessHours
        })
      });
    }
  }
  if (sourceRequirement(input.requirements, "bing_ai_performance")) {
    addUnavailable(
      "bing_ai_performance",
      "bing",
      "Bing AI Performance has no stable provider-neutral API contract in this harness; an account-specific official export adapter must be injected"
    );
  }
  const stripeRequirement = sourceRequirement(input.requirements, "stripe");
  if (stripeRequirement) {
    const state = input.providers?.providers.stripe;
    const capabilities = ["product", "price", "webhook", "billing_portal"];
    const accountId = uniqueValue([
      state?.account_id,
      state?.external_resource_ids.account_id,
      ...lifecycleValues(input.lifecycle, "stripe", capabilities, "account_id")
    ]);
    const mode = state?.external_resource_ids.mode;
    if (!providerVerified(input.providers, input.lifecycle, "stripe", capabilities)) {
      addUnavailable("stripe", "stripe", "verified Stripe setup read-back is required");
    } else if (!state?.credential_ref || !accountId || mode !== "test" && mode !== "live") {
      addUnavailable(
        "stripe",
        "stripe",
        "providers.stripe.credential_ref, account_id, and external_resource_ids.mode (test or live) are required to prevent mode mixing"
      );
    } else {
      entries.push({
        credentialRef: state.credential_ref,
        connector: createStripeBalanceConnector({
          broker: input.broker,
          fetcher: input.fetcher,
          mode,
          sourceAccount: `${accountId}:${mode}`,
          timezone: "UTC",
          windowHours: stripeRequirement.freshnessHours
        })
      });
    }
  }
  const brevoRequirement = sourceRequirement(input.requirements, "brevo");
  if (brevoRequirement) {
    const state = input.providers?.providers.brevo;
    const capabilities = ["sending_domain", "sender", "template", "webhook"];
    const accountId = uniqueValue([
      state?.account_id,
      state?.external_resource_ids.account_id,
      ...lifecycleValues(input.lifecycle, "brevo", capabilities, "account_id")
    ]);
    if (!providerVerified(input.providers, input.lifecycle, "brevo", capabilities)) {
      addUnavailable("brevo", "brevo", "verified Brevo setup read-back is required");
    } else if (!state?.credential_ref || !accountId) {
      addUnavailable(
        "brevo",
        "brevo",
        "providers.brevo.credential_ref and account_id are required"
      );
    } else {
      entries.push({
        credentialRef: state.credential_ref,
        connector: createBrevoAggregateConnector({
          broker: input.broker,
          fetcher: input.fetcher,
          sourceAccount: accountId,
          timezone: input.timezone,
          windowHours: brevoRequirement.freshnessHours
        })
      });
    }
  }
  const revenueCatRequirement = sourceRequirement(input.requirements, "revenuecat");
  if (revenueCatRequirement) {
    const state = input.providers?.providers.revenuecat;
    const capabilities = ["app", "entitlement", "offering", "webhook"];
    const projectId = uniqueValue([
      state?.external_resource_ids.project_id,
      ...lifecycleValues(input.lifecycle, "revenuecat", capabilities, "project_id")
    ]);
    if (!providerVerified(input.providers, input.lifecycle, "revenuecat", capabilities)) {
      addUnavailable("revenuecat", "revenuecat", "verified RevenueCat setup read-back is required");
    } else if (!state?.credential_ref || !projectId) {
      addUnavailable(
        "revenuecat",
        "revenuecat",
        "providers.revenuecat.credential_ref and one project_id are required"
      );
    } else {
      entries.push({
        credentialRef: state.credential_ref,
        connector: createRevenueCatMetricsConnector({
          broker: input.broker,
          fetcher: input.fetcher,
          projectId,
          sourceAccount: projectId,
          timezone: "UTC",
          windowHours: revenueCatRequirement.freshnessHours
        })
      });
    }
  }
  if (sourceRequirement(input.requirements, "app_store_connect_analytics")) {
    entries.push(
      unavailableConnector(
        "app_store_connect_analytics",
        "App Store Connect Analytics requires a pre-existing report request plus instance/segment discovery, signed JWT auth, and CSV segment parsing; data sync will not create the report request or guess a segment schema.",
        "Create the Analytics Report Request in an explicitly authorized App Store Connect operation, record its safe request/app IDs, and inject a JWT-signed report-instance/segment connector before rerunning vh data sync."
      )
    );
  }
  return entries;
}
function statusFor(connectors, result2) {
  if (connectors.length === 0) return "not_configured";
  if (result2.datasets.length === 0 && result2.failures.length > 0 && result2.failures.every((failure) => failure.code === "connector_not_configured")) {
    return "not_configured";
  }
  const requiredGap = result2.freshness.some((entry) => entry.required && entry.status !== "fresh");
  return result2.failures.length === 0 && !requiredGap ? "complete" : "incomplete";
}
function nextAction(status) {
  if (status === "complete") {
    return "Run vh learn <cadence>; inspect provenance, sample limits, and every proposed action.";
  }
  if (status === "not_configured") {
    return "Configure an official read-only connector in a declared loop input and its credential_ref; no fixture or missing-as-zero fallback was used.";
  }
  return "Resolve the reported connector failures or stale required sources, then rerun vh data sync; missing is not zero.";
}
function composeRuntime(options, requirements, loop2) {
  const providers = loadProviders(options.rootDir);
  const lifecycle = loadProviderLifecycle(options.rootDir);
  const releaseRequirement = requirements.find(({ source }) => source === "release_log");
  const timezone = loadTimezone(options.rootDir);
  return createDefaultDataLearningRuntime({
    rootDir: options.rootDir,
    broker: options.broker,
    commandRunner: options.commandRunner,
    timezone,
    neon: neonOptions(providers, requirements, lifecycle),
    releaseLog: releaseRequirement ? {
      required: releaseRequirement.required,
      windowHours: releaseRequirement.freshnessHours
    } : false,
    directConnectors: directHttpConnectors({
      providers,
      lifecycle,
      requirements,
      broker: options.broker,
      fetcher: options.httpFetcher,
      timezone
    }),
    requirements,
    metricDefinitions: loop2 ? metricDefinitions(loop2) : void 0,
    candidateRules: loop2 ? candidateRules(loop2) : [],
    maximumCandidates: loop2?.maximum_actions
  });
}
function mergeConnectors(defaults, injected) {
  const bySource = new Map(defaults.map((connector) => [connector.source, connector]));
  for (const connector of injected) bySource.set(connector.source, connector);
  return [...bySource.values()].sort((left, right) => left.source.localeCompare(right.source));
}
function createDefaultLearningRuntime(options) {
  const currentTime = options.now ?? (() => /* @__PURE__ */ new Date());
  const injectedRequirements = options.injectedRequirements ?? [];
  const injectedConnectors = options.injectedConnectors ?? [];
  return {
    async sync() {
      const loops = loadLoops(options.rootDir);
      const requirements = mergeRequirements(loops, injectedRequirements);
      const runtime = composeRuntime(options, requirements);
      const connectors = mergeConnectors(runtime.connectors, injectedConnectors);
      const at = currentTime();
      const result2 = connectors.length > 0 ? await syncDataSources(connectors, requirements, {
        now: at,
        credentialRefs: runtime.credentialRefs
      }) : {
        datasets: [],
        failures: [],
        freshness: buildFreshnessReport(requirements, [], at)
      };
      const status = statusFor(connectors, result2);
      return persistedDataSyncSchema.parse({
        schemaVersion: 1,
        status,
        generatedAt: at.toISOString(),
        ...result2,
        nextAction: nextAction(status)
      });
    },
    learn(cadence, persisted) {
      const loops = loadLoops(options.rootDir);
      const [id, loop2] = loopForCadence(loops, cadence);
      const requirements = loopRequirements(loop2, injectedRequirements);
      const definition2 = learningDefinition(id, loop2, requirements);
      const sync = persistedDataSyncSchema.parse(persisted);
      const runtime = composeRuntime(options, requirements, loop2);
      return {
        definition: definition2,
        report: runtime.learn({ definition: definition2, syncResult: sync, now: currentTime() })
      };
    },
    missingArtifact(cadence) {
      const loops = loadLoops(options.rootDir);
      const [, loop2] = loopForCadence(loops, cadence);
      const requirements = loopRequirements(loop2, injectedRequirements);
      const at = currentTime();
      return persistedDataSyncSchema.parse({
        schemaVersion: 1,
        status: "not_configured",
        generatedAt: at.toISOString(),
        datasets: [],
        failures: [],
        freshness: buildFreshnessReport(requirements, [], at),
        nextAction: "Run vh data sync after configuring direct read-only connectors; missing is not zero."
      });
    },
    operatingCadence(persisted) {
      const loops = loadLoops(options.rootDir);
      const sync = persistedDataSyncSchema.parse(persisted);
      const definitions = Object.entries(loops.loops).map(
        ([id, loop2]) => learningDefinition(id, loop2, loopRequirements(loop2, injectedRequirements))
      );
      const active = activeExperimentState(options.rootDir);
      const configurationBlockers = sync.status === "complete" ? [] : [`data_sync:${sync.status}; ${sync.nextAction}`];
      return buildOperatingCadence({
        loops: definitions,
        freshness: sync.freshness,
        activeHypotheses: active.hypotheses,
        activeExperiments: active.experiments,
        activeBlockers: [
          ...configurationBlockers,
          ...sync.failures.map(
            (failure) => `${failure.source}: ${failure.code}; ${failure.nextAction}`
          )
        ],
        now: currentTime()
      });
    }
  };
}

// lib/cli/default-services.ts
var LAUNCH_ROUTER_VERSION = "0.2.0";
var PROVIDER_BY_NODE = launchProviderByNode;
function mergeBindings(...bindings) {
  const interruptEvidenceVerifier = bindings.map((binding) => binding.interruptEvidenceVerifier).filter(
    (verifier) => verifier !== void 0
  ).at(-1);
  const checkpointEvidenceVerifier = bindings.map((binding) => binding.checkpointEvidenceVerifier).filter(
    (verifier) => verifier !== void 0
  ).at(-1);
  return {
    handlers: Object.assign({}, ...bindings.map((binding) => binding.handlers ?? {})),
    validators: Object.assign({}, ...bindings.map((binding) => binding.validators ?? {})),
    conditions: Object.assign({}, ...bindings.map((binding) => binding.conditions ?? {})),
    compensators: Object.assign({}, ...bindings.map((binding) => binding.compensators ?? {})),
    interruptEvidenceVerifier,
    checkpointEvidenceVerifier,
    secrets: [...new Set(bindings.flatMap((binding) => binding.secrets ?? []))]
  };
}
function providerHandlerNames(definition2) {
  return definition2.nodes.filter((node) => node.kind === "provider" && node.handler).map((node) => node.handler).sort();
}
function productHandlerNames(definition2) {
  return definition2.nodes.filter((node) => node.handler && node.kind !== "provider" && node.handler !== "launch.report").map((node) => node.handler).sort();
}
function inside6(root, path) {
  const absolute = resolve21(root, path);
  const rel = relative13(root, absolute);
  if (rel === "" || !rel.startsWith(`..${sep13}`) && rel !== ".." && !rel.startsWith(sep13)) {
    return absolute;
  }
  throw new Error(`Path escapes the venture root: ${path}`);
}
function writeJsonAtomic2(path, value) {
  mkdirSync8(dirname11(path), { recursive: true, mode: 448 });
  const temporary = `${path}.next-${process.pid}-${Date.now()}`;
  writeFileSync8(temporary, `${JSON.stringify(value, null, 2)}
`, {
    encoding: "utf8",
    mode: 384
  });
  renameSync6(temporary, path);
}
function writeTextAtomic(path, content) {
  mkdirSync8(dirname11(path), { recursive: true, mode: 448 });
  const temporary = `${path}.next-${process.pid}-${Date.now()}`;
  writeFileSync8(temporary, content, {
    encoding: "utf8",
    mode: 384
  });
  renameSync6(temporary, path);
}
function writeYamlAtomic(path, value) {
  writeTextAtomic(path, stringify6(value, { lineWidth: 100 }));
}
function readStructured(path) {
  const text2 = readFileSync14(path, "utf8");
  return path.endsWith(".json") ? JSON.parse(text2) : parse7(text2);
}
function stableJson2(value) {
  if (Array.isArray(value)) return `[${value.map(stableJson2).join(",")}]`;
  if (value !== null && typeof value === "object") {
    return `{${Object.entries(value).sort(([left], [right]) => left.localeCompare(right)).map(([key, child]) => `${JSON.stringify(key)}:${stableJson2(child)}`).join(",")}}`;
  }
  return JSON.stringify(value) ?? "undefined";
}
function sha256Json(value) {
  return createHash17("sha256").update(stableJson2(value)).digest("hex");
}
function founderLaunchTransactionPath(root) {
  return inside6(root, ".venture/founder-launch.json");
}
function founderLaunchGrantRenewalPath(root, authorizationDigest) {
  return inside6(root, `.venture/launch-grant-renewals/${authorizationDigest}.json`);
}
function parseFounderLaunchTransaction(value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    throw new Error("Founder launch transaction is not a JSON object");
  }
  const record3 = value;
  const expectedKeys = [
    "createdAt",
    "grantId",
    "planDigest",
    "runId",
    "schemaVersion",
    "sourceHash",
    "stackConnectionHash",
    "stackProfile",
    "status",
    "updatedAt"
  ];
  if (!isDeepStrictEqual2(Object.keys(record3).sort(), expectedKeys)) {
    throw new Error("Founder launch transaction has unknown or missing fields");
  }
  if (record3.schemaVersion !== 1 || !["launch_pending", "waiting_external_action", "succeeded", "failed"].includes(
    String(record3.status)
  ) || record3.stackProfile !== "founder-default" || typeof record3.sourceHash !== "string" || !/^[a-f0-9]{64}$/u.test(record3.sourceHash) || typeof record3.stackConnectionHash !== "string" || !/^[a-f0-9]{64}$/u.test(record3.stackConnectionHash) || typeof record3.grantId !== "string" || !/^lg_[a-f0-9]{26}$/u.test(record3.grantId) || typeof record3.runId !== "string" || !/^[a-z0-9][a-z0-9-]{5,100}$/u.test(record3.runId) || typeof record3.planDigest !== "string" || !/^[a-f0-9]{64}$/u.test(record3.planDigest) || typeof record3.createdAt !== "string" || Number.isNaN(Date.parse(record3.createdAt)) || typeof record3.updatedAt !== "string" || Number.isNaN(Date.parse(record3.updatedAt)) || Date.parse(record3.updatedAt) < Date.parse(record3.createdAt)) {
    throw new Error("Founder launch transaction is invalid");
  }
  return record3;
}
function loadFounderLaunchTransaction(root) {
  const path = founderLaunchTransactionPath(root);
  if (!existsSync12(path)) {
    throw new Error(
      "Existing founder venture has no durable launch transaction; choose a new --output or inspect it manually before any retry."
    );
  }
  return parseFounderLaunchTransaction(readStructured(path));
}
function saveFounderLaunchTransaction(root, transaction) {
  writeJsonAtomic2(founderLaunchTransactionPath(root), parseFounderLaunchTransaction(transaction));
}
function validateFounderLaunchContinuation(input) {
  const transaction = loadFounderLaunchTransaction(input.childRoot);
  const grantPath = inside6(input.childRoot, ".venture/launch-grant.json");
  const founderInputPath = inside6(input.childRoot, ".venture/founder-input.json");
  if (!existsSync12(grantPath) || !existsSync12(founderInputPath)) {
    throw new Error(
      "Existing founder venture is missing its immutable input or Launch Grant; no continuation was attempted."
    );
  }
  const grant = parseLaunchGrant(readStructured(grantPath));
  const founderInputValue = readStructured(founderInputPath);
  if (!founderInputValue || typeof founderInputValue !== "object" || Array.isArray(founderInputValue)) {
    throw new Error("Existing founder venture input binding is invalid");
  }
  const founderInput = founderInputValue;
  const expectedFounderInputKeys = [
    "assumptions",
    "briefArtifact",
    "compiledAt",
    "schemaVersion",
    "sourceHash",
    "stackProfile"
  ];
  if (!isDeepStrictEqual2(Object.keys(founderInput).sort(), expectedFounderInputKeys) || founderInput.schemaVersion !== 1 || founderInput.briefArtifact !== ".venture/input/founder-brief.yaml" || founderInput.compiledAt !== grant.createdAt || !isDeepStrictEqual2(founderInput.assumptions, input.preparation.idea.assumptionsAdded)) {
    throw new Error("Existing founder venture input binding is invalid");
  }
  const persistedBrief = founderBriefSchema.parse(
    readStructured(inside6(input.childRoot, String(founderInput.briefArtifact)))
  );
  const persistedProject = loadProject(input.childRoot);
  const expectedRunId = `launch-${grant.ventureSlug}-${grant.grantId.slice(3, 15)}`;
  if (transaction.sourceHash !== input.preparation.idea.sourceHash || transaction.stackConnectionHash !== input.stackConnectionHash || transaction.stackProfile !== input.preparation.stackProfile || transaction.grantId !== grant.grantId || transaction.runId !== expectedRunId || transaction.createdAt !== grant.createdAt || founderInput.sourceHash !== transaction.sourceHash || founderInput.stackProfile !== transaction.stackProfile || grant.ideaDigest !== transaction.sourceHash || grant.ventureSlug !== input.preparation.idea.brief.id || grant.ventureName !== input.preparation.idea.brief.name || grant.repository.owner !== input.preparation.repository.owner || grant.repository.name !== input.preparation.repository.name || grant.repository.visibility !== input.preparation.repository.visibility || !isDeepStrictEqual2(persistedBrief, input.preparation.idea.brief) || !isDeepStrictEqual2(persistedProject.brief, input.preparation.idea.brief)) {
    throw new Error(
      "Existing founder venture does not match the current idea, Stack connection, repository, or immutable Launch Grant; no continuation was attempted."
    );
  }
  const plan = compileVentureMaterialization({
    grant,
    at: new Date(grant.createdAt),
    coreVersion: "0.2.0",
    workflowRefSha: input.workflowRefSha,
    effects: []
  });
  if (plan.planDigest !== transaction.planDigest) {
    throw new Error(
      "Existing founder venture transaction does not match its immutable materialization plan; no continuation was attempted."
    );
  }
  return { transaction, grant, plan };
}
function eventPacksFor(brief, decision) {
  return resolveActiveEventPacks({
    capabilities: decision.capabilities,
    appKind: decision.rail.appKind,
    monetizationModel: brief.monetization_model,
    leadJourney: ["lead_generation", "services"].includes(brief.monetization_model)
  });
}
function learningSourcesFor(decision, activeEventPacks) {
  const requested = new Set(
    activeEventPacks.flatMap((pack2) => [...EVENT_PACKS[pack2].freshnessSources])
  );
  if (decision.capabilities.includes("bing_webmaster")) requested.add("bing_webmaster");
  if (decision.capabilities.includes("transactional_email") || decision.capabilities.includes("lifecycle_email")) {
    requested.add("brevo");
  }
  if (decision.payment.provider === "stripe") requested.add("stripe");
  if (decision.payment.provider === "revenuecat") requested.add("revenuecat");
  const required = /* @__PURE__ */ new Set(["neon_commercial_evidence", "release_log"]);
  if (decision.payment.provider !== "none") required.add(decision.payment.provider);
  return DATA_SOURCE_IDS.filter((source) => requested.has(source)).map((source) => ({
    source,
    required: required.has(source)
  }));
}
function synchronizeBriefContracts(root, brief, decision, activeEventPacks, synchronizedAt) {
  const pendingWrites = [];
  const venturePath = inside6(root, "config/venture.yaml");
  if (existsSync12(venturePath)) {
    const current = ventureSchema.parse(readStructured(venturePath));
    const next = ventureSchema.parse({
      ...current,
      venture: {
        ...current.venture,
        name: brief.name,
        domain: brief.domain ?? current.venture.domain,
        market: brief.target_market ?? brief.specific_user_or_audience,
        target_market: brief.target_market ?? brief.specific_user_or_audience,
        language: (brief.locale ?? current.venture.locale).split("-")[0],
        locale: brief.locale ?? current.venture.locale,
        currency: brief.currency ?? current.venture.currency,
        timezone: brief.timezone ?? current.venture.timezone,
        stage: decision.mode.selectedMode === "validate_first" ? "demand_validation" : "build",
        repository_visibility: brief.repository_visibility ?? current.venture.repository_visibility,
        harness_version: "0.2.0",
        app_kind: decision.rail.appKind,
        launch_mode: decision.mode.selectedMode,
        business_model: brief.business_model,
        monetization_model: brief.monetization_model,
        mobile_stack: decision.rail.mobileStack,
        outcomes: {
          ...current.venture.outcomes,
          primary: {
            statement: brief.intended_outcome,
            success_signal: brief.primary_success_signal
          }
        },
        capabilities: {
          ...current.venture.capabilities,
          active: [...decision.capabilities]
        }
      },
      validation: {
        ...current.validation,
        stage: decision.mode.selectedMode === "validate_first" ? "demand_validation" : "build",
        primary_conversion: brief.primary_success_signal
      }
    });
    pendingWrites.push({ path: venturePath, reference: "config/venture.yaml", value: next });
  }
  const launchConfigPath = inside6(root, "config/launch.yaml");
  if (existsSync12(launchConfigPath)) {
    const current = launchSchema.parse(readStructured(launchConfigPath));
    const factorRationale = (factor, level) => `${factor.replaceAll("_", " ")} was supplied as ${level} in the selected founder brief.`;
    const next = launchSchema.parse({
      ...current,
      launch: {
        ...current.launch,
        selected_mode: decision.mode.selectedMode,
        confidence: decision.mode.confidence,
        rationale: decision.mode.rationale,
        rejected_alternatives: decision.mode.rejectedAlternatives,
        assumptions: decision.mode.assumptions,
        evidence_that_could_change_choice: decision.mode.evidenceThatCouldChangeChoice,
        rail: {
          app_kind: decision.rail.appKind,
          mobile_stack: decision.rail.mobileStack,
          rationale: decision.rail.rationale
        },
        routing_factors: Object.fromEntries(
          Object.entries(current.launch.routing_factors).map(([factor]) => {
            const level = brief.factors[factor];
            return [factor, { level, rationale: factorRationale(factor, level) }];
          })
        ),
        progressive_commitment: {
          specific_user_or_audience: brief.specific_user_or_audience,
          problem_or_job: brief.problem_or_job,
          intended_outcome: brief.intended_outcome,
          smallest_core_journey: brief.smallest_core_journey,
          primary_success_signal: brief.primary_success_signal,
          material_constraints: brief.material_constraints,
          known_truths: brief.known_truths,
          unresolved_assumptions: brief.assumptions,
          blocking_issues: []
        }
      }
    });
    pendingWrites.push({
      path: launchConfigPath,
      reference: "config/launch.yaml",
      value: next
    });
  }
  const mobilePath = inside6(root, "config/mobile.yaml");
  if (existsSync12(mobilePath)) {
    const current = mobileSchema.parse(readStructured(mobilePath));
    const mobile = decision.rail.mobileStack !== "none";
    if (mobile && current.mobile.app_store_connect.first_app_record.state === "complete" && brief.bundle_identifier && current.mobile.bundle_identifier && current.mobile.bundle_identifier !== brief.bundle_identifier) {
      throw new Error(
        `The verified Apple app record uses ${current.mobile.bundle_identifier}, but the brief requests ${brief.bundle_identifier}; reconcile the identifier before selecting this brief.`
      );
    }
    const next = mobileSchema.parse({
      ...current,
      mobile: {
        ...current.mobile,
        stack: decision.rail.mobileStack,
        rationale: decision.rail.rationale,
        bundle_identifier: mobile ? brief.bundle_identifier ?? current.mobile.bundle_identifier : current.mobile.bundle_identifier,
        app_scheme: mobile ? brief.app_scheme ?? current.mobile.app_scheme ?? brief.id : current.mobile.app_scheme,
        app_store_connect: {
          ...current.mobile.app_store_connect,
          primary_language: brief.locale ?? current.mobile.app_store_connect.primary_language,
          first_app_record: mobile ? current.mobile.app_store_connect.first_app_record.state === "complete" ? current.mobile.app_store_connect.first_app_record : {
            state: "required",
            manual_action_ref: "reports/launch/<run-id>/manual/apple-first-app-record.json"
          } : { state: "not_required", manual_action_ref: null }
        }
      }
    });
    pendingWrites.push({ path: mobilePath, reference: "config/mobile.yaml", value: next });
  }
  const analyticsPath = inside6(root, "config/analytics.yaml");
  if (existsSync12(analyticsPath)) {
    const current = analyticsSchema.parse(readStructured(analyticsPath));
    const activeJourneys = new Set(resolveActiveCoreJourneys(activeEventPacks));
    const coreJourneys = Object.fromEntries(
      Object.entries(CORE_JOURNEYS).map(([journeyId, journey]) => [
        journeyId,
        {
          active: activeJourneys.has(journeyId),
          required_packs: [...journey.requiredPacks],
          start_events: [...journey.startEvents],
          outcome_events: [...journey.outcomeEvents],
          authoritative_destination: "neon"
        }
      ])
    );
    const next = analyticsSchema.parse({
      ...current,
      event_packs: {
        ...current.event_packs ?? {},
        active: [...activeEventPacks]
      },
      core_journeys: coreJourneys
    });
    pendingWrites.push({
      path: analyticsPath,
      reference: "config/analytics.yaml",
      value: next
    });
  }
  const loopsPath = inside6(root, "config/loops.yaml");
  if (existsSync12(loopsPath)) {
    const current = loopsSchema.parse(readStructured(loopsPath));
    const sources = learningSourcesFor(decision, activeEventPacks);
    const freshnessHours = { daily: 36, weekly: 192, biweekly: 384, monthly: 800 };
    const enabledCadences = /* @__PURE__ */ new Set(["daily", "weekly", "biweekly", "monthly"]);
    const next = loopsSchema.parse({
      ...current,
      loops: Object.fromEntries(
        Object.entries(current.loops).map(([loopId, loop2]) => [
          loopId,
          {
            ...loop2,
            enabled: brief.needs.scheduled_learning && enabledCadences.has(loop2.cadence),
            next_run_at: brief.needs.scheduled_learning && enabledCadences.has(loop2.cadence) ? nextCronOccurrence(loop2.trigger.expression, synchronizedAt) : null,
            inputs: sources.map(({ source, required }) => ({
              source,
              required,
              freshness_hours: freshnessHours[loop2.cadence]
            }))
          }
        ])
      )
    });
    pendingWrites.push({ path: loopsPath, reference: "config/loops.yaml", value: next });
  }
  const originals = new Map(
    pendingWrites.map(({ path }) => [path, readFileSync14(path, "utf8")])
  );
  const written = [];
  try {
    for (const pending of pendingWrites) {
      writeYamlAtomic(pending.path, pending.value);
      written.push(pending.path);
    }
  } catch (error) {
    const rollbackErrors = [];
    for (const path of written.reverse()) {
      try {
        writeTextAtomic(path, originals.get(path));
      } catch (rollbackError) {
        rollbackErrors.push(
          `${path}: ${rollbackError instanceof Error ? rollbackError.message : String(rollbackError)}`
        );
      }
    }
    const message = error instanceof Error ? error.message : String(error);
    throw new Error(
      rollbackErrors.length === 0 ? `Brief contract synchronization failed and prior files were restored: ${message}` : `Brief contract synchronization failed and rollback was incomplete (${rollbackErrors.join("; ")}): ${message}`
    );
  }
  return pendingWrites.map(({ reference }) => reference);
}
function noCredentialMaterial(value, path = "output") {
  if (typeof value === "string") {
    if (looksLikeCredentialValue(value)) {
      throw new Error(`${path} looks like credential material; return only resource identifiers.`);
    }
    return;
  }
  if (Array.isArray(value)) {
    value.forEach((item, index) => noCredentialMaterial(item, `${path}[${index}]`));
    return;
  }
  if (!value || typeof value !== "object") return;
  for (const [key, item] of Object.entries(value)) {
    if (/token|secret|password|private.?key|api.?key/i.test(key)) {
      throw new Error(`${path}.${key} is a forbidden credential field.`);
    }
    noCredentialMaterial(item, `${path}.${key}`);
  }
}
function projectPath(root) {
  return inside6(root, ".venture/project.json");
}
function launchPath(root, runId2) {
  return inside6(root, `.venture/launches/${runId2}.json`);
}
function loadProject(root) {
  const path = projectPath(root);
  if (!existsSync12(path)) {
    throw new Error("No founder brief is selected. Next: run vh create --brief <file>.");
  }
  const value = JSON.parse(readFileSync14(path, "utf8"));
  if (value.schemaVersion !== 1 && value.schemaVersion !== 2) {
    throw new Error("Unsupported .venture/project.json version.");
  }
  const brief = founderBriefSchema.parse(value.brief);
  const canonicalDecision = routeLaunch(brief);
  const canonicalPacks = eventPacksFor(brief, canonicalDecision);
  if (value.schemaVersion === 1) {
    return {
      schemaVersion: 2,
      createdAt: value.createdAt,
      brief,
      decision: canonicalDecision,
      activeEventPacks: canonicalPacks,
      routerVersion: LAUNCH_ROUTER_VERSION
    };
  }
  if (value.routerVersion !== LAUNCH_ROUTER_VERSION) {
    throw new Error(
      `Project routing snapshot uses ${value.routerVersion}; run vh upgrade before launching it with router ${LAUNCH_ROUTER_VERSION}.`
    );
  }
  if (!isDeepStrictEqual2(value.decision, canonicalDecision) || !isDeepStrictEqual2(value.activeEventPacks, canonicalPacks)) {
    throw new Error(
      "Project routing snapshot does not match its selected brief and router version; restore or recreate the project state before launch."
    );
  }
  return { ...value, brief, decision: value.decision, activeEventPacks: canonicalPacks };
}
function loadLaunch(root, runId2) {
  const path = launchPath(root, runId2);
  if (!existsSync12(path)) {
    throw new Error(
      `Launch definition for ${runId2} is missing; restore .venture/launches metadata.`
    );
  }
  const value = JSON.parse(readFileSync14(path, "utf8"));
  if (value.schemaVersion !== 1 && value.schemaVersion !== 2) {
    throw new Error(`Unsupported launch metadata for ${runId2}.`);
  }
  const brief = founderBriefSchema.parse(value.brief);
  const authorization = authorizationEnvelopeSchema.parse(value.authorization);
  const launchGrant = "launchGrant" in value && value.launchGrant ? parseLaunchGrant(value.launchGrant) : void 0;
  validateWorkflow(value.definition);
  const canonicalDecision = routeLaunch(brief);
  const canonicalPacks = eventPacksFor(brief, canonicalDecision);
  if (value.schemaVersion === 1) {
    return {
      schemaVersion: 2,
      brief,
      decision: canonicalDecision,
      activeEventPacks: canonicalPacks,
      routerVersion: LAUNCH_ROUTER_VERSION,
      definition: value.definition,
      authorization,
      launchGrant
    };
  }
  if (value.routerVersion !== LAUNCH_ROUTER_VERSION || !isDeepStrictEqual2(value.decision, canonicalDecision) || !isDeepStrictEqual2(value.activeEventPacks, canonicalPacks)) {
    throw new Error(
      `Launch ${runId2} has an invalid or unsupported routing snapshot; restore the persisted launch metadata before resuming.`
    );
  }
  return { ...value, brief, authorization, launchGrant, activeEventPacks: canonicalPacks };
}
function providerIds2(definition2) {
  return [
    ...new Set(
      definition2.nodes.flatMap(
        (node) => node.kind === "provider" && PROVIDER_BY_NODE[node.id] ? [PROVIDER_BY_NODE[node.id]] : []
      )
    )
  ];
}
function requiredLaunchGrantEffects(brief) {
  const effects = /* @__PURE__ */ new Set([
    "repository.create",
    "company_stack.provision",
    "source.push",
    "preview.deploy",
    "production.deploy"
  ]);
  if (brief.domain) effects.add("domain.configure");
  if (brief.monetization_model !== "none") effects.add("commerce.configure");
  if (brief.needs.scheduled_learning) effects.add("loops.schedule");
  return [...effects];
}
var FOUNDER_REVIEWED_ZERO_DIRECT_CHARGE_OPERATIONS = [
  ["github", "repository", "repository.create_from_source"],
  ["github", "actions_secret", "actions_secret.set"],
  ["github", "repository_settings", "repository.settings.update"],
  ["github", "draft_pull_request", "pull_request.create_draft"],
  ["vercel", "project", "project.create"],
  ["vercel", "project", "project.link"],
  ["vercel", "environment_variable", "environment_variable.set"],
  ["vercel", "deployment", "deployment.preview"],
  ["vercel", "deployment", "deployment.production"],
  ["vercel", "domain", "domain.add"],
  ["vercel", "web_analytics", "web_analytics.enable_manual"],
  ["neon", "project", "project.create"],
  ["neon", "branch", "branch.create"],
  ["neon", "database", "database.create"],
  ["neon", "role", "role.create"],
  ["neon", "schema_migration", "schema.migrate"],
  ["neon", "read_write_health_check", "database.read_write_health_check"],
  ["stripe", "product", "product.create"],
  ["stripe", "price", "price.create"],
  ["stripe", "webhook", "webhook_endpoint.create"],
  ["stripe", "billing_portal", "billing_portal.configuration.create"],
  ["revenuecat", "project_bootstrap", "project.bootstrap_manual"],
  ["revenuecat", "app", "app.create"],
  ["revenuecat", "entitlement", "entitlement.create"],
  ["revenuecat", "offering", "offering.create"],
  ["revenuecat", "webhook", "webhook.create"],
  ["brevo", "sending_domain", "sending_domain.create"],
  ["brevo", "sending_domain_verification", "sending_domain.authenticate"],
  ["brevo", "sender", "sender.create"],
  ["brevo", "template", "template.create"],
  ["brevo", "webhook", "webhook.create"],
  ["google", "analytics_property", "analytics_property.create"],
  ["google", "analytics_web_stream", "analytics_web_stream.create"],
  ["google", "site_verification_token", "site_verification.token.get"],
  ["google", "site_verification", "site_verification.web_resource.insert"],
  ["google", "search_console_site", "search_console.site.add"],
  ["google", "search_console_sitemap", "search_console.sitemap.submit"],
  ["bing", "site", "site.add"],
  ["bing", "sitemap", "feed.add"],
  ["bing", "url_submission", "url.submit"],
  ["dns", "record", "dns_record.change_manual"]
];
function configuredProviderDestination(providers, provider, capability) {
  const state = providers.providers[provider];
  if (!state) return null;
  if (provider === "github" || provider === "vercel") {
    return state.team_id ?? state.account_id;
  }
  if (provider === "neon" || provider === "revenuecat") {
    return state.external_resource_ids.organization_id ?? state.account_id;
  }
  if (provider === "google") {
    return capability.startsWith("analytics_") ? state.external_resource_ids.analytics_account_id ?? state.account_id : state.account_id;
  }
  if (provider === "dns") {
    return state.external_resource_ids.registrar_account_id ?? state.external_resource_ids.organization_id ?? state.account_id;
  }
  return state.account_id ?? state.team_id;
}
function requiredGrantDestinations(definition2, providers) {
  const destinations = /* @__PURE__ */ new Map();
  for (const node of definition2.nodes.filter(
    ({ id, kind }) => kind === "provider" || kind === "manual_action" && id === "dns-records"
  )) {
    const provider = PROVIDER_BY_NODE[node.id];
    if (!provider) {
      throw new Error(`Launch Grant cannot bind unknown provider node ${node.id}`);
    }
    for (const capability of node.authorization.scopes) {
      const externalAccountId = configuredProviderDestination(providers, provider, capability);
      if (!externalAccountId) {
        throw new Error(
          `Launch Grant cannot bind ${provider}/${capability} without an exact configured account, team, organization, or registrar destination`
        );
      }
      const key = `${provider}\0${capability}\0${externalAccountId}`;
      destinations.set(key, { provider, capability, externalAccountId });
    }
  }
  return [...destinations.values()].sort(
    (left, right) => left.provider.localeCompare(right.provider) || left.capability.localeCompare(right.capability) || left.externalAccountId.localeCompare(right.externalAccountId)
  );
}
function providerDestinationKey(input) {
  return `${input.provider}\0${input.capability}\0${input.externalAccountId}`;
}
function assertLaunchGrantBindsGraph(input) {
  const grant = parseLaunchGrant(input.grant);
  if (input.allowExpiredRenewal) {
    if (grant.revokedAt) throw new Error("Launch Grant is revoked");
    if (input.at < new Date(grant.createdAt)) throw new Error("Launch Grant is not active yet");
  } else {
    assertLaunchGrantActive(grant, input.at);
  }
  if (grant.ventureSlug !== input.brief.id || grant.ventureName !== input.brief.name) {
    throw new Error("Launch Grant venture identity does not match the selected founder brief");
  }
  const expectedSeed = input.brief.app_kind === "web" ? "agentic-web-saas" : input.brief.app_kind === "hybrid" ? "hybrid-agentic-service" : "agentic-ios-subscription";
  if (grant.seed.id !== expectedSeed || grant.seed.version !== "0.2.0") {
    throw new Error("Launch Grant seed does not match the selected product rail");
  }
  if (grant.stackProfile.id !== "founder-default" || grant.stackProfile.version !== "0.2.0") {
    throw new Error("Launch Grant does not bind the exact founder-default@0.2.0 Stack Profile");
  }
  const github = input.providers.providers.github;
  const repositoryOwner = github?.team_id ?? github?.account_id;
  const configuredRepository = github?.external_resource_ids.repository;
  if (!repositoryOwner || configuredRepository !== `${grant.repository.owner}/${grant.repository.name}` || grant.repository.owner !== repositoryOwner || grant.repository.name !== input.brief.id || grant.repository.visibility !== (input.brief.repository_visibility ?? "private")) {
    throw new Error(
      "Launch Grant repository owner, name, or visibility does not match the exact configured GitHub destination"
    );
  }
  const requiredDestinations = requiredGrantDestinations(input.definition, input.providers);
  const grantedDestinationKeys = grant.providerAccounts.map(providerDestinationKey);
  if (new Set(grantedDestinationKeys).size !== grantedDestinationKeys.length) {
    throw new Error("Launch Grant contains duplicate provider destination bindings");
  }
  const requiredDestinationKeys = new Set(requiredDestinations.map(providerDestinationKey));
  const grantedDestinationSet = new Set(grantedDestinationKeys);
  const missingDestinations = requiredDestinations.filter(
    (destination) => !grantedDestinationSet.has(providerDestinationKey(destination))
  );
  const extraDestinations = grant.providerAccounts.filter(
    (destination) => !requiredDestinationKeys.has(providerDestinationKey(destination))
  );
  if (missingDestinations.length > 0 || extraDestinations.length > 0) {
    throw new Error(
      "Launch Grant provider/capability/account destinations do not exactly match the compiled graph and current Founder Stack configuration"
    );
  }
  const requiredEffects = requiredLaunchGrantEffects(input.brief).sort();
  const grantedEffects = [...grant.allowedExternalEffects].sort();
  if (!isDeepStrictEqual2(requiredEffects, grantedEffects)) {
    throw new Error("Launch Grant external effects do not exactly match the compiled launch");
  }
  const operationCeiling = launchProviderOperationCeiling(input.definition);
  const minimumModelInvocations = launchBuildAgentTaskCount(input.definition);
  if (!grant.modelExecutionPolicy) {
    throw new Error(
      "Canonical founder Launch Grant is missing its explicit model execution policy"
    );
  }
  if (grant.modelExecutionPolicy.maxBuildAgentTasks !== minimumModelInvocations) {
    throw new Error(
      `Launch Grant build-agent task bound ${grant.modelExecutionPolicy.maxBuildAgentTasks} does not match the ${minimumModelInvocations} compiled tasks`
    );
  }
  if (!grant.providerOperationBudget) {
    throw new Error("Canonical founder Launch Grant is missing its provider-operation budget");
  }
  if (grant.providerOperationBudget.maxOperations !== operationCeiling) {
    throw new Error(
      `Launch Grant provider-operation ceiling ${grant.providerOperationBudget.maxOperations} does not match the compiled ceiling ${operationCeiling}`
    );
  }
  if (input.brief.currency && grant.providerOperationBudget.currency !== input.brief.currency) {
    throw new Error("Launch Grant budget currencies do not match the founder brief");
  }
  if (grant.autonomyProfile !== "owner_live_launch") {
    throw new Error("Launch Grant does not authorize the owner live-launch autonomy profile");
  }
  if (!grant.permissions.productionDeployment) {
    throw new Error("Launch Grant does not authorize a production deployment");
  }
  if (input.brief.domain && !grant.permissions.domainConfiguration) {
    throw new Error("Launch Grant does not authorize the requested domain configuration");
  }
  if (input.brief.monetization_model !== "none" && !grant.permissions.liveCommerceConfiguration) {
    throw new Error("Launch Grant does not authorize the requested commerce configuration");
  }
  return grant;
}
function assertExactPlanInput(target, key, expected) {
  const actual = target.request.inputs[key];
  if (actual !== void 0 && actual !== expected) {
    throw new Error(
      `Launch provider request ${target.provider}.${key} does not match its exact Launch Grant destination`
    );
  }
}
function bindFounderProviderFactories(input) {
  const nodeByHandler = new Map(
    input.definition.nodes.filter((node) => node.kind === "provider" && node.handler).map((node) => [node.handler, node])
  );
  return Object.fromEntries(
    Object.entries(input.factories).map(([handler, factory]) => [
      handler,
      async (workflow) => {
        const node = nodeByHandler.get(handler);
        const expectedProvider = node ? PROVIDER_BY_NODE[node.id] : void 0;
        if (!node || !expectedProvider || workflow.node.handler !== handler) {
          throw new Error(`Launch provider factory ${handler} is not bound to one compiled node`);
        }
        const target = await factory(workflow);
        if (target.provider !== expectedProvider) {
          throw new Error(
            `Launch provider factory ${handler} returned ${target.provider}, expected ${expectedProvider}`
          );
        }
        const capabilities = [...new Set(target.request.capabilities)];
        if (capabilities.length === 0 || capabilities.length !== target.request.capabilities.length || capabilities.some((capability) => !node.authorization.scopes.includes(capability))) {
          throw new Error(
            `Launch provider factory ${handler} returned a capability outside its immutable graph scope`
          );
        }
        const providers = providersSchema.parse(
          readStructured(inside6(input.root, "config/providers.yaml"))
        );
        const configured = providers.providers[target.provider];
        if (!configured) {
          throw new Error(`Launch provider ${target.provider} has no typed configuration`);
        }
        if (target.request.credentialRef !== (configured.credential_ref ?? void 0)) {
          throw new Error(
            `Launch provider ${target.provider} credential reference does not match the Founder Stack configuration`
          );
        }
        if (target.request.credentialRef) {
          const reference = input.broker.getReference(target.request.credentialRef);
          if (!reference || reference.provider !== target.provider) {
            throw new Error(
              `Launch provider ${target.provider} credential reference is not registered to that provider`
            );
          }
          if (reference.accountId && configured.account_id && reference.accountId !== configured.account_id) {
            throw new Error(
              `Launch provider ${target.provider} credential account does not match the configured account`
            );
          }
        }
        for (const capability of capabilities) {
          const externalAccountId = configuredProviderDestination(
            providers,
            target.provider,
            capability
          );
          if (!externalAccountId || !input.grant.providerAccounts.some(
            (destination) => destination.provider === target.provider && destination.capability === capability && destination.externalAccountId === externalAccountId
          )) {
            throw new Error(
              `Launch provider request ${target.provider}/${capability} is outside the exact Launch Grant account destination`
            );
          }
        }
        if (target.provider === "github") {
          assertExactPlanInput(
            target,
            "repository",
            `${input.grant.repository.owner}/${input.grant.repository.name}`
          );
          assertExactPlanInput(target, "visibility", input.grant.repository.visibility);
          assertExactPlanInput(target, "sourceDirectory", resolve21(input.root));
        }
        if (target.provider === "vercel") {
          const scope = configured.team_id ?? configured.account_id;
          if (scope) assertExactPlanInput(target, "scope", scope);
          assertExactPlanInput(target, "project", input.grant.repository.name);
        }
        if (target.provider === "neon" && capabilities.includes("project")) {
          const organizationId = configured.external_resource_ids.organization_id;
          if (!organizationId) {
            throw new Error(
              "Launch provider neon project creation has no exact configured organization destination"
            );
          }
          assertExactPlanInput(target, "organizationId", organizationId);
        }
        if (target.provider === "google" && capabilities.includes("analytics_property")) {
          const analyticsAccount = configured.external_resource_ids.analytics_account_id;
          if (analyticsAccount) {
            assertExactPlanInput(target, "analyticsAccountId", analyticsAccount);
          }
        }
        return {
          ...target,
          operationBudget: {
            maxOperations: Math.max(1, node.authorization.scopes.length),
            missingCostClassification: {
              basis: "reviewed_known_zero_direct_charge",
              currency: input.grant.providerOperationBudget.currency,
              ongoingAccountPlanUsageCovered: false,
              allowedOperations: FOUNDER_REVIEWED_ZERO_DIRECT_CHARGE_OPERATIONS.filter(
                ([provider, capability]) => provider === target.provider && capabilities.includes(capability)
              ).map(([provider, capability, action]) => ({ provider, capability, action }))
            }
          }
        };
      }
    ])
  );
}
function authorizationForLaunchGrant(envelope, grant) {
  const operationBudget = grant.providerOperationBudget;
  if (!operationBudget) {
    throw new Error("Canonical founder Launch Grant has no provider-operation budget");
  }
  let exponent;
  try {
    exponent = new Intl.NumberFormat("en-US", {
      style: "currency",
      currency: operationBudget.currency
    }).resolvedOptions().maximumFractionDigits ?? Number.NaN;
  } catch {
    throw new Error(`Launch Grant uses unsupported budget currency ${operationBudget.currency}`);
  }
  if (!Number.isInteger(exponent) || exponent < 0 || exponent > 4) {
    throw new Error(`Launch Grant uses unsupported budget currency ${operationBudget.currency}`);
  }
  const amount = operationBudget.maxDirectChargeMinorUnits / 10 ** exponent;
  if (!Number.isFinite(amount)) {
    throw new Error("Launch Grant provider direct-operation monetary ceiling is invalid");
  }
  return authorizationEnvelopeSchema.parse({
    ...envelope,
    max_estimated_spend: {
      amount,
      currency: operationBudget.currency
    },
    unknown_external_costs_allowed: false
  });
}
function bindWorkflowBudgetsToLaunchGrant(definition2, grant) {
  const policy = grant.modelExecutionPolicy;
  if (!policy) throw new Error("Canonical founder Launch Grant has no model execution policy");
  const taskBudgetCategory = "launch.build_agent_tasks";
  const bounded = {
    ...definition2,
    nodes: definition2.nodes.map(
      (node) => node.handler && launchBuildAgentHandlers.has(node.handler) ? {
        ...node,
        budgetCategory: taskBudgetCategory,
        cost: { amount: 1, unit: "tasks" }
      } : node
    ),
    budgets: {
      ...definition2.budgets,
      [taskBudgetCategory]: policy.maxBuildAgentTasks
    }
  };
  validateWorkflow(bounded);
  return bounded;
}
async function assertFounderBuildAgentHostPolicy(host, grant) {
  await assertBuildAgentHostAvailable(host);
  const inspection = await host.inspect();
  const policy = grant.modelExecutionPolicy;
  if (!policy) throw new Error("Canonical founder Launch Grant has no model execution policy");
  if (policy.mode === "fixture_no_model_execution") {
    if (inspection.billingMode !== "fixture_no_model_execution" || inspection.billingEvidence !== "fixture_attestation") {
      throw new Error(
        "Fixture founder launch requires an explicit no-model build-host attestation; no child or provider effect was created."
      );
    }
    return;
  }
  if (inspection.billingMode !== "chatgpt_subscription" || inspection.billingEvidence !== "codex_login_status") {
    throw new Error(
      "Founder production launch requires `codex login status` to attest ChatGPT subscription use; API-key or unknown billing is blocked before child creation and provider transport."
    );
  }
}
function configureFounderProviderTargets(root, connection, preparation) {
  const path = inside6(root, "config/providers.yaml");
  const current = providersSchema.parse(readStructured(path));
  const rendered2 = renderFounderStackProviderConfigOverrides(connection, {
    ventureSlug: preparation.idea.brief.id,
    domain: preparation.idea.brief.domain
  });
  const repository = `${preparation.repository.owner}/${preparation.repository.name}`;
  const exactLaunchIds = {
    github: { repository, repository_intent: "create_from_source" },
    vercel: { project: preparation.idea.brief.id, project_intent: "create" },
    neon: { project_name: preparation.idea.brief.id, project_intent: "create" },
    bing: { site_intent: "create" }
  };
  const providers = Object.fromEntries(
    Object.entries(current.providers).map(([providerId, state]) => {
      const override = rendered2.providers[providerId];
      if (!override) return [providerId, state];
      return [
        providerId,
        {
          ...state,
          ...override,
          capability_ids: state.capability_ids,
          external_resource_ids: {
            ...state.external_resource_ids,
            ...override.external_resource_ids,
            ...exactLaunchIds[providerId] ?? {}
          },
          evidence_artifact_ref: null,
          error_code: null,
          error_message: null,
          retryable: null,
          next_action: null,
          manual_action_ref: null,
          extensions: state.extensions
        }
      ];
    })
  );
  writeYamlAtomic(path, providersSchema.parse({ ...current, providers }));
}
function configureFounderOffer(root, preparation) {
  const path = inside6(root, "config/offer.yaml");
  const current = offerSchema.parse(readStructured(path));
  const brief = preparation.idea.brief;
  const next = offerSchema.parse({
    ...current,
    icp: { ...current.icp, description: brief.specific_user_or_audience },
    offer: { ...current.offer, sentence: brief.intended_outcome },
    pricing: {
      ...current.pricing,
      currency: preparation.idea.commercialTerms.currency,
      monthly_price: preparation.idea.commercialTerms.monthlyPrice,
      annual_price: preparation.idea.commercialTerms.annualPrice
    }
  });
  writeYamlAtomic(path, next);
}
function publicFounderPreparation(preparation, extra = {}) {
  return {
    schemaVersion: preparation.schemaVersion,
    status: preparation.status,
    mode: preparation.executionMode,
    ventureSlug: preparation.idea.brief.id,
    selectedVentureType: preparation.selectedVentureType,
    stackProfile: preparation.stackProfile,
    selectedProviders: preparation.selectedProviders,
    repository: preparation.repository,
    resources: preparation.resources,
    environmentVariables: preparation.environmentVariables,
    migrations: preparation.migrations,
    domain: preparation.domain,
    setup: preparation.setup,
    estimatedExternalEffects: preparation.estimatedExternalEffects,
    launchGrant: {
      status: preparation.grantDisposition,
      grantId: preparation.launchGrant.grantId,
      expiresAt: preparation.launchGrant.expiresAt,
      allowedExternalEffects: preparation.launchGrant.allowedExternalEffects,
      providerDestinationCount: preparation.launchGrant.providerAccounts.length,
      providerOperationBudget: preparation.launchGrant.providerOperationBudget ?? null,
      modelExecutionPolicy: preparation.launchGrant.modelExecutionPolicy ?? null
    },
    materialization: {
      planDigest: preparation.materialization.planDigest,
      fileCount: preparation.materialization.files.length,
      files: preparation.materialization.files.map(({ path, ownership }) => ({ path, ownership })),
      externalEffects: preparation.materialization.effects
    },
    graphDryRun: preparation.graphDryRun,
    blockers: preparation.blockers,
    exactFinalCommand: preparation.exactFinalCommand,
    externalEffectsOccurred: false,
    ...extra
  };
}
function generatedRunId(brief, now) {
  return `launch-${brief.id}-${now.getTime().toString(36)}`.toLowerCase();
}
function environmentVariableForRef(ref) {
  return ref.replace(/^cred:\/\//, "").replace(/[^a-z0-9]+/gi, "_").toUpperCase();
}
function providerRevocationAction(reference) {
  const target = reference.label ?? reference.accountId ?? reference.ref;
  const actions = {
    github: `Open GitHub Settings > Developer settings > Personal access tokens and revoke the token for ${target}; also review Settings > Password and authentication > Sessions`,
    vercel: `Open Vercel Account Settings > Tokens and revoke the token for ${target}; also review active login sessions`,
    neon: `Open Neon Console > Account settings > API keys and revoke the key for ${target}`,
    stripe: `Open the matching Stripe test/live Dashboard > Developers > API keys and expire or delete the key for ${target}`,
    revenuecat: `Open the matching RevenueCat project > API keys and revoke the secret key for ${target}`,
    brevo: `Open Brevo Settings > SMTP & API > API Keys and delete the key for ${target}`,
    google: `Open Google Account Security and Google Cloud IAM/Credentials for ${target}; revoke the OAuth grant or disable/delete the service-account key`,
    bing: `Open the Microsoft account/app authorization settings used by Bing Webmaster Tools and revoke the grant or API key for ${target}`,
    eas: `Open Expo account security/access-token settings and revoke the token or session for ${target}`,
    app_store_connect: `Open App Store Connect > Users and Access > Integrations and revoke the API key for ${target}`
  };
  return actions[reference.provider] ?? `Open ${reference.provider} credential/security settings and revoke the credential for ${target}`;
}
function localRemovalAction(reference) {
  if (reference.backend === "environment" || reference.backend === "ci") {
    return `remove ${environmentVariableForRef(reference.ref)} from the process/CI secret configuration, then rerun vh auth revoke ${reference.provider}`;
  }
  if (reference.backend === "cli_session") {
    return `repair the official ${reference.provider} CLI session/logout command, then rerun vh auth revoke ${reference.provider}`;
  }
  return `remove ${reference.ref} from backend ${reference.backend}, then rerun vh auth revoke ${reference.provider}`;
}
function credentialKind(value, backend) {
  const selected = value ?? (backend === "cli_session" ? "cli_session" : "api_key");
  if (!credentialKinds.includes(selected)) {
    throw new Error(
      `Unsupported credential kind ${selected}; choose ${credentialKinds.join(", ")}.`
    );
  }
  return selected;
}
function createDefaultCliServices(options = {}) {
  const root = resolve21(options.rootDir ?? process.cwd());
  const store = options.store ?? new FileWorkflowStore({ rootDir: inside6(root, ".venture/runs") });
  const now = options.now ?? (() => /* @__PURE__ */ new Date());
  const providerLifecycleStore = new FileProviderLifecycleStore(
    inside6(root, ".venture/provider-lifecycle.json")
  );
  const founderStackStore = new FileFounderStackStore(
    options.founderStackRoot ?? (options.rootDir ? inside6(root, ".venture/founder-stacks") : defaultFounderStackStateRoot())
  );
  const requirements = options.dataRequirements ?? [];
  const catalogPath = resolve21(
    options.credentialCatalogPath ?? (options.rootDir ? inside6(root, ".venture/credentials.json") : defaultCredentialCatalogPath())
  );
  let credentialCatalog = loadCredentialCatalog(catalogPath);
  const commandRunner = options.providerCommandRunner ?? new NodeCommandRunner();
  const supportedBackends = /* @__PURE__ */ new Set(["environment", "ci", "cli_session", "onepassword"]);
  const defaultBroker = new CredentialBroker([
    new EnvironmentCredentialBackend(),
    new EnvironmentCredentialBackend({ id: "ci" }),
    new CliSessionCredentialBackend(commandRunner),
    new OnePasswordCredentialBackend({ runner: commandRunner }),
    ...process.platform === "darwin" ? [new MacOSKeychainCredentialBackend({ runner: commandRunner })] : []
  ]);
  if (process.platform === "darwin") supportedBackends.add("macos_keychain");
  const credentialBroker = options.credentialBroker ?? defaultBroker;
  const catalogIssues = [];
  for (const reference of credentialCatalog.references) {
    if (!options.credentialBroker && !supportedBackends.has(reference.backend)) {
      catalogIssues.push(
        `${reference.ref} uses unavailable backend ${reference.backend}; configure that backend before testing it.`
      );
      continue;
    }
    try {
      credentialBroker.register(reference);
    } catch (error) {
      catalogIssues.push(error instanceof Error ? error.message : String(error));
    }
  }
  const httpFetcher = options.dataHttpFetcher ?? new NativeHttpFetcher({ redactor: credentialBroker.redactor });
  const defaultCredentialTesters = (connection) => createDefaultFounderCredentialTesters({ fetcher: httpFetcher, connection });
  const testFounderStackCredentials = async (connection) => {
    const testers = defaultCredentialTesters(connection);
    const refs = [
      ...new Set(
        Object.values(connection.roles).flatMap(
          (role) => role.credentialRef ? [role.credentialRef] : []
        )
      )
    ];
    for (const ref of refs) {
      const reference = credentialBroker.getReference(ref);
      if (!reference || reference.kind === "cli_session") continue;
      const tester = options.credentialTesters?.[reference.provider] ?? testers[reference.provider];
      if (tester) await credentialBroker.test(ref, tester);
    }
    for (const reference of credentialBroker.list()) {
      credentialCatalog = upsertCredentialReference(credentialCatalog, reference);
    }
    saveCredentialCatalog(credentialCatalog, catalogPath);
  };
  const learningRuntime = createDefaultLearningRuntime({
    rootDir: root,
    broker: credentialBroker,
    commandRunner: options.dataCommandRunner ?? commandRunner,
    httpFetcher,
    injectedConnectors: options.dataConnectors,
    injectedRequirements: requirements,
    now
  });
  const productRuntimeHome = inside6(root, ".venture/product-command-home");
  mkdirSync8(productRuntimeHome, { recursive: true, mode: 448 });
  const productCommandRunner = options.productCommandRunner ?? new NodeCommandRunner({ env: productCommandEnvironment(process.env, productRuntimeHome) });
  const buildAgentHost = options.buildAgentHost ?? new CodexCliBuildAgentHost({
    rootDir: root,
    runner: new NodeCommandRunner({ env: codexBuildAgentEnvironment(process.env) }),
    redactor: credentialBroker.redactor
  });
  const defaultProviderRuntimeContext = createOfficialProviderContext({
    commandRunner,
    httpFetcher,
    credentials: credentialBroker,
    redactor: credentialBroker.redactor,
    idempotencyLedger: new FileProviderIdempotencyLedger(
      inside6(root, ".venture/provider-idempotency.json")
    )
  });
  const effectiveProviderRegistry = options.providerRegistry ?? providerRegistry;
  const effectiveProviderContext = options.providerContext ?? {
    ...defaultProviderRuntimeContext,
    authorization: "dry_run"
  };
  const launchContextFor = (launch) => ({
    rootDir: root,
    brief: launch.brief,
    definition: launch.definition,
    authorization: launch.authorization
  });
  const reportCredentialReferences = (launch) => {
    const activeProviders = new Set(providerIds2(launch.definition));
    return credentialBroker.list().filter((reference) => activeProviders.has(reference.provider)).map((reference) => ({
      ref: reference.ref,
      provider: reference.provider,
      status: "registered_not_retested",
      scopes: [...reference.scopes],
      expiresAt: reference.expiresAt,
      accountId: reference.accountId
    }));
  };
  const qualityReportLines = () => {
    const lines = [];
    const limitations = [];
    for (const profile2 of ["fast", "mvp", "release"]) {
      const path = inside6(root, `.venture/reports/quality/${profile2}-latest.json`);
      if (!existsSync12(path)) continue;
      try {
        const report = JSON.parse(readFileSync14(path, "utf8"));
        for (const result2 of report.results ?? []) {
          if (typeof result2.id !== "string" || !["PASS", "FAIL", "SKIP", "NOT_APPLICABLE"].includes(String(result2.status))) {
            throw new Error("result has an invalid id or status");
          }
          const detail = typeof result2.detail === "string" ? `; ${result2.detail}` : "";
          const gap = result2.gap;
          const prerequisite = gap && typeof gap.missing === "string" && typeof gap.exact_command === "string" ? `; missing ${gap.missing}; run ${gap.exact_command}; evidence ${typeof gap.expected_evidence === "string" ? gap.expected_evidence : "not recorded"}` : "";
          lines.push(`${profile2}/${result2.id}: ${String(result2.status)}${detail}${prerequisite}`);
        }
      } catch (error) {
        limitations.push(
          `${profile2} quality artifact could not be parsed; no check state was inferred (${error instanceof Error ? error.message : String(error)}).`
        );
      }
    }
    return { lines, limitations };
  };
  const reportInputFor = (launch, state) => {
    const decision = launch.decision;
    const loopsPath = inside6(root, "config/loops.yaml");
    const loops = existsSync12(loopsPath) ? loopsSchema.parse(readStructured(loopsPath)) : createDefaultLoopsConfig();
    const configuredLoops = Object.entries(loops.loops).map(([id, loop2]) => {
      const sources = loop2.inputs.length > 0 ? loop2.inputs.map(
        (input) => `${input.source}:${input.freshness_hours}h:${input.required ? "required" : "optional"}`
      ).join(", ") : "sources unconfigured";
      return `${id}: ${loop2.enabled ? "enabled" : "disabled"}; ${loop2.trigger.kind} ${loop2.trigger.expression}; ${sources}; autonomy ${loop2.autonomy}; destination ${loop2.output_destination}`;
    });
    const nextReviews = Object.entries(loops.loops).filter(([, loop2]) => ["daily", "weekly", "biweekly", "monthly"].includes(loop2.cadence)).map(([id, loop2]) => `${id}: ${loop2.next_run_at ?? "not scheduled"}`);
    const providerConfigPath = inside6(root, "config/providers.yaml");
    const providerMetadata = existsSync12(providerConfigPath) ? Object.fromEntries(
      Object.entries(providersSchema.parse(readStructured(providerConfigPath)).providers).map(
        ([provider, configured]) => [
          provider,
          {
            accountId: configured.account_id ?? void 0,
            teamId: configured.team_id ?? void 0,
            region: configured.region ?? void 0
          }
        ]
      )
    ) : {};
    const cadencePath = inside6(root, "reports/learning/operating-cadence.json");
    if (existsSync12(cadencePath)) {
      const cadence = JSON.parse(readFileSync14(cadencePath, "utf8"));
      for (const hypothesis of cadence.activeHypotheses ?? []) {
        nextReviews.push(`active hypothesis: ${hypothesis}`);
      }
      for (const experiment of cadence.activeExperiments ?? []) {
        nextReviews.push(`active experiment: ${experiment}`);
      }
      for (const blocker of cadence.activeBlockers ?? []) {
        nextReviews.push(`active blocker: ${blocker}`);
      }
    }
    const quality = qualityReportLines();
    return createLaunchReportInputFromRun({
      generatedAt: now().toISOString(),
      state,
      brief: {
        id: launch.brief.id,
        name: launch.brief.name,
        synthetic: launch.brief.synthetic ?? false,
        scheduledLearning: launch.brief.needs.scheduled_learning
      },
      launch: {
        mode: decision.mode.selectedMode,
        rail: decision.rail.mobileStack === "none" ? decision.rail.appKind : `${decision.rail.appKind}/${decision.rail.mobileStack}`,
        paymentProvider: decision.payment.provider,
        entitlementSource: decision.payment.entitlementSource,
        activeEventPacks: launch.activeEventPacks,
        consentMode: "strict"
      },
      authorization: {
        profile: launch.authorization.profile,
        approvalRef: launch.authorization.approval_ref,
        expiresAt: launch.authorization.expires_at,
        spendCeiling: launch.authorization.max_estimated_spend,
        ...launch.launchGrant?.providerOperationBudget ? { spendScope: "reviewed_direct_provider_operations_only" } : {}
      },
      providerByNode: PROVIDER_BY_NODE,
      providerMetadata,
      credentialReferences: reportCredentialReferences(launch),
      limitations: [
        ...launch.brief.synthetic ? ["Synthetic fixture: no live provider or market state is claimed."] : [],
        ...launch.launchGrant?.providerOperationBudget ? [
          `Provider direct-operation estimate: ${launch.launchGrant.providerOperationBudget.maxDirectChargeMinorUnits} minor units ${launch.launchGrant.providerOperationBudget.currency} (${launch.launchGrant.providerOperationBudget.estimateBasis}); ongoing provider account-plan usage is not covered.`
        ] : [],
        ...launch.launchGrant?.modelExecutionPolicy ? [
          launch.launchGrant.modelExecutionPolicy.mode === "chatgpt_subscription_non_metered" ? `Build-agent execution is bounded to ${launch.launchGrant.modelExecutionPolicy.maxBuildAgentTasks} ChatGPT-subscription tasks; token usage is observational and no token or monetary hard ceiling is claimed.` : `Fixture build execution is bounded to ${launch.launchGrant.modelExecutionPolicy.maxBuildAgentTasks} deterministic tasks and attests that no model was invoked.`
        ] : [],
        ...quality.limitations
      ],
      sections: {
        checksRun: quality.lines,
        scheduledLoops: configuredLoops,
        nextReviews
      }
    });
  };
  const persistStateReport = async (launch, state, runtimeContext) => persistLaunchReport(
    renderLaunchReport(reportInputFor(launch, state), {
      redactor: runtimeContext.redactor
    }),
    inside6(root, `reports/launch/${state.runId}`)
  );
  const bindingsFor = async (launch) => {
    const launchContext = launchContextFor(launch);
    const productBindings = options.launchBindings ? await options.launchBindings(launchContext) : (await (launch.launchGrant ? assertFounderBuildAgentHostPolicy(buildAgentHost, launch.launchGrant) : assertBuildAgentHostAvailable(buildAgentHost)), createLaunchProductBindings({
      rootDir: root,
      brief: launch.brief,
      agentHost: buildAgentHost,
      commandRunner: productCommandRunner,
      redactor: credentialBroker.redactor,
      now
    }));
    const reservedProductHandlers = Object.keys(productBindings.handlers ?? {}).filter(
      (handler) => handler.startsWith("provider.") || handler === "launch.report"
    );
    if (reservedProductHandlers.length > 0) {
      throw new Error(
        `Product bindings attempted to replace reserved handlers: ${reservedProductHandlers.join(", ")}. Provider effects and launch reports must use the official runtime composition.`
      );
    }
    const missingProductHandlers = productHandlerNames(launch.definition).filter(
      (handler) => !productBindings.handlers?.[handler]
    );
    if (missingProductHandlers.length > 0) {
      throw new Error(
        `Launch apply is missing product handler(s): ${missingProductHandlers.join(", ")}; no run or external action was created.`
      );
    }
    const unboundedProviderPlanFactories = options.providerPlanFactories ? typeof options.providerPlanFactories === "function" ? await options.providerPlanFactories(launchContext) : options.providerPlanFactories : createDefaultProviderPlanFactories({
      rootDir: root,
      brief: () => launch.brief,
      definition: launch.definition,
      lifecycleStore: providerLifecycleStore
    });
    const providerPlanFactories = launch.launchGrant ? bindFounderProviderFactories({
      factories: unboundedProviderPlanFactories,
      definition: launch.definition,
      grant: launch.launchGrant,
      root,
      broker: credentialBroker
    }) : unboundedProviderPlanFactories;
    const missingProviderFactories = providerHandlerNames(launch.definition).filter(
      (handler) => !providerPlanFactories[handler]
    );
    if (missingProviderFactories.length > 0) {
      throw new Error(
        `Launch apply is missing authorized provider plan factory/factories: ${missingProviderFactories.join(", ")}; no run or external action was created.`
      );
    }
    const providerRuntimeContext = options.providerRuntimeContext ? typeof options.providerRuntimeContext === "function" ? await options.providerRuntimeContext(launchContext) : options.providerRuntimeContext : options.providerContext ? {
      transports: options.providerContext.transports,
      credentials: options.providerContext.credentials,
      redactor: options.providerContext.redactor,
      idempotencyLedger: options.providerContext.idempotencyLedger
    } : defaultProviderRuntimeContext;
    if (!providerRuntimeContext.idempotencyLedger) {
      throw new Error(
        "Launch apply requires a durable provider idempotency ledger; no run or external action was created."
      );
    }
    const policies = policiesSchema.parse(readStructured(inside6(root, "config/policies.yaml")));
    const providerBindings = createProviderWorkflowBindings({
      planFactories: providerPlanFactories,
      policies,
      authorization: launch.authorization,
      context: providerRuntimeContext,
      resolveAdapter: (provider) => effectiveProviderRegistry.get(provider),
      now,
      lifecycleStore: providerLifecycleStore,
      recordEvidence: ({ evidence, workflow }) => {
        const reference = `reports/launch/${workflow.runId}/providers/${workflow.node.id}.json`;
        const path = inside6(root, reference);
        writeJsonAtomic2(path, evidence);
        return reference;
      }
    });
    const reportDirectoryReference = `reports/launch/${launch.authorization.run_id}`;
    const reportBindings = createLaunchReportWorkflowBinding({
      redactor: providerRuntimeContext.redactor,
      outputDirectory: inside6(root, reportDirectoryReference),
      artifactReferences: {
        json: `${reportDirectoryReference}/final.json`,
        markdown: `${reportDirectoryReference}/final.md`
      },
      input: ({ runId: runId2 }) => reportInputFor(launch, store.load(runId2))
    });
    const manualBindings = {
      ...createLaunchManualBindings(),
      interruptEvidenceVerifier: createRepositoryInterruptEvidenceVerifier({
        rootDir: root,
        redactor: providerRuntimeContext.redactor
      }),
      checkpointEvidenceVerifier: createRepositoryCheckpointEvidenceVerifier({
        rootDir: root,
        redactor: providerRuntimeContext.redactor
      })
    };
    return {
      bindings: mergeBindings(productBindings, providerBindings, reportBindings, manualBindings),
      runtimeContext: providerRuntimeContext
    };
  };
  return {
    async founderLaunch(request2) {
      const connection = founderStackStore.load(request2.stackProfile);
      if (!connection) {
        return {
          schemaVersion: 1,
          status: "blocked",
          mode: request2.mode,
          stackProfile: request2.stackProfile,
          blockers: [
            {
              code: "stack_role_not_ready",
              message: "Founder Stack profile founder-default has not been created.",
              nextAction: "Run vh stack create founder-default --file <connection.json>, then vh stack doctor founder-default."
            }
          ],
          externalEffectsOccurred: false,
          exactFinalCommand: "vh launch --idea ./idea.md --stack founder-default --production --apply --non-interactive"
        };
      }
      const stackDoctor = await doctorFounderStackConnection({
        connection,
        context: { ...effectiveProviderContext, authorization: "dry_run" },
        registry: effectiveProviderRegistry,
        now
      });
      const ideaSource = loadFounderIdeaFile(request2.idea, root);
      const workflowRefSha = resolveFounderWorkflowRefSha(root, options.founderWorkflowRefSha);
      const venturesRoot = options.founderOutputRoot ?? configuredVenturesRoot({ coreRoot: root });
      if (!venturesRoot) throw new Error(VENTURES_ROOT_UNSET_MESSAGE);
      const preparation = compileFounderLaunchPreparation({
        ideaSource,
        ideaPath: request2.idea,
        stack: connection,
        stackDoctor,
        baseDir: resolve21(venturesRoot),
        output: request2.output,
        workflowRefSha,
        executionMode: request2.mode,
        production: request2.production,
        nonInteractive: request2.nonInteractive,
        at: now(),
        allowFixtureStack: options.allowFixtureFounderStack ?? false
      });
      if (request2.mode === "dry-run" || preparation.status === "blocked") {
        return publicFounderPreparation(preparation);
      }
      const childRoot = preparation.repository.localDirectory;
      const stagingRoot = `${childRoot}.vh-staging-${preparation.launchGrant.grantId.slice(3, 15)}`;
      const stackConnectionHash = sha256Json(connection);
      const childOptions = (child) => ({
        rootDir: child,
        founderStackRoot: founderStackStore.rootDir,
        founderOutputRoot: options.founderOutputRoot,
        founderWorkflowRefSha: options.founderWorkflowRefSha,
        allowFixtureFounderStack: options.allowFixtureFounderStack,
        launchBindings: options.launchBindings,
        buildAgentHost: options.buildAgentHost,
        productCommandRunner: options.productCommandRunner,
        providerCommandRunner: options.providerCommandRunner,
        dataCommandRunner: options.dataCommandRunner,
        dataHttpFetcher: options.dataHttpFetcher,
        upgradeCommandRunner: options.upgradeCommandRunner,
        providerPlanFactories: options.providerPlanFactories,
        providerRuntimeContext: options.providerRuntimeContext,
        credentialBroker,
        credentialCatalogPath: catalogPath,
        credentialTesters: options.credentialTesters,
        interactiveCliLogin: options.interactiveCliLogin,
        providerRegistry: options.providerRegistry,
        providerContext: options.providerContext,
        dataConnectors: options.dataConnectors,
        dataRequirements: options.dataRequirements,
        release: options.release,
        now
      });
      let effectivePreparation = preparation;
      let materialized2;
      let transaction;
      let workflow = null;
      let buildHostPreflighted = false;
      let writableTargetsRegistered = false;
      let pendingFounderGrantRenewal = false;
      const childStore = new FileWorkflowStore({ rootDir: inside6(childRoot, ".venture/runs") });
      if (existsSync12(childRoot)) {
        const continuation = validateFounderLaunchContinuation({
          childRoot,
          preparation,
          stackConnectionHash,
          workflowRefSha
        });
        transaction = continuation.transaction;
        effectivePreparation = {
          ...preparation,
          launchGrant: continuation.grant,
          materialization: continuation.plan
        };
        materialized2 = {
          status: "materialized",
          files: continuation.plan.files.map(({ path }) => path),
          planDigest: continuation.plan.planDigest
        };
        if (childStore.exists(transaction.runId)) {
          workflow = childStore.load(transaction.runId);
        } else if (transaction.status !== "launch_pending") {
          throw new Error(
            `Founder launch transaction is ${transaction.status} but workflow ${transaction.runId} is missing; no continuation was attempted.`
          );
        } else if (now().getTime() >= Date.parse(continuation.grant.expiresAt)) {
          pendingFounderGrantRenewal = true;
        }
      } else {
        if (existsSync12(stagingRoot)) {
          throw new Error(
            `Founder launch staging directory already exists: ${stagingRoot}. Inspect that interrupted local materialization before retrying; no provider effect was started by this invocation.`
          );
        }
        if (!options.launchBindings) {
          await assertFounderBuildAgentHostPolicy(buildAgentHost, preparation.launchGrant);
          buildHostPreflighted = true;
        }
        const registeredTargets = await registerFounderStackWritableCredentialRefs(
          connection,
          {
            ventureSlug: preparation.idea.brief.id,
            domain: preparation.idea.brief.domain
          },
          credentialBroker
        );
        for (const target of registeredTargets.inspections) {
          const reference = credentialBroker.getReference(target.ref);
          if (reference) {
            credentialCatalog = upsertCredentialReference(credentialCatalog, reference);
          }
        }
        saveCredentialCatalog(credentialCatalog, catalogPath);
        writableTargetsRegistered = true;
        materialized2 = await materializeFounderVenture(preparation, stagingRoot);
        const briefArtifact = ".venture/input/founder-brief.yaml";
        writeYamlAtomic(inside6(stagingRoot, briefArtifact), preparation.idea.brief);
        writeJsonAtomic2(inside6(stagingRoot, ".venture/founder-input.json"), {
          schemaVersion: 1,
          compiledAt: preparation.launchGrant.createdAt,
          sourceHash: preparation.idea.sourceHash,
          briefArtifact,
          assumptions: preparation.idea.assumptionsAdded,
          stackProfile: preparation.stackProfile
        });
        writeJsonAtomic2(inside6(stagingRoot, ".venture/launch-grant.json"), preparation.launchGrant);
        transaction = {
          schemaVersion: 1,
          status: "launch_pending",
          sourceHash: preparation.idea.sourceHash,
          stackProfile: preparation.stackProfile,
          stackConnectionHash,
          grantId: preparation.launchGrant.grantId,
          runId: `launch-${preparation.idea.brief.id}-${preparation.launchGrant.grantId.slice(3, 15)}`,
          planDigest: preparation.materialization.planDigest,
          createdAt: preparation.launchGrant.createdAt,
          updatedAt: now().toISOString()
        };
        saveFounderLaunchTransaction(stagingRoot, transaction);
        const stagingServices = createDefaultCliServices(childOptions(stagingRoot));
        if (!stagingServices.create) throw new Error("Founder child create service is unavailable");
        await stagingServices.create({ brief: briefArtifact, json: true });
        configureFounderProviderTargets(stagingRoot, connection, preparation);
        configureFounderOffer(stagingRoot, preparation);
        mkdirSync8(dirname11(childRoot), { recursive: true, mode: 448 });
        renameSync6(stagingRoot, childRoot);
      }
      if (!workflow) {
        if (!options.launchBindings && !buildHostPreflighted) {
          await assertFounderBuildAgentHostPolicy(buildAgentHost, effectivePreparation.launchGrant);
        }
        if (!writableTargetsRegistered) {
          const registeredTargets = await registerFounderStackWritableCredentialRefs(
            connection,
            {
              ventureSlug: effectivePreparation.idea.brief.id,
              domain: effectivePreparation.idea.brief.domain
            },
            credentialBroker
          );
          for (const target of registeredTargets.inspections) {
            const reference = credentialBroker.getReference(target.ref);
            if (reference) {
              credentialCatalog = upsertCredentialReference(credentialCatalog, reference);
            }
          }
          saveCredentialCatalog(credentialCatalog, catalogPath);
        }
      }
      const authorization = effectivePreparation.idea.brief.app_kind !== "web" && effectivePreparation.idea.brief.app_kind !== "hybrid" ? "mobile-testflight" : effectivePreparation.idea.brief.monetization_model === "none" ? "standard-launch" : "live-commerce-launch";
      const runId2 = transaction.runId;
      if (!workflow) {
        const childServices = createDefaultCliServices(childOptions(childRoot));
        if (!childServices.launch) throw new Error("Founder child launch service is unavailable");
        try {
          workflow = await childServices.launch({
            mode: "apply",
            authorization,
            runId: runId2,
            json: true,
            launchGrant: effectivePreparation.launchGrant,
            ...pendingFounderGrantRenewal ? { pendingFounderGrantRenewal: true } : {}
          });
        } catch (error) {
          if (childStore.exists(runId2)) {
            const persisted = childStore.load(runId2);
            const persistedStatus = persisted.status === "succeeded" ? "succeeded" : persisted.status === "waiting" ? "waiting_external_action" : ["failed", "cancelled", "superseded"].includes(persisted.status) ? "failed" : "launch_pending";
            transaction = {
              ...transaction,
              status: persistedStatus,
              updatedAt: new Date(
                Math.max(Date.parse(transaction.updatedAt), now().getTime())
              ).toISOString()
            };
            saveFounderLaunchTransaction(childRoot, transaction);
          }
          throw error;
        } finally {
          for (const reference of credentialBroker.list()) {
            credentialCatalog = upsertCredentialReference(credentialCatalog, reference);
          }
          saveCredentialCatalog(credentialCatalog, catalogPath);
        }
      }
      const transactionStatus = workflow.status === "succeeded" ? "succeeded" : workflow.status === "waiting" ? "waiting_external_action" : ["failed", "cancelled", "superseded"].includes(workflow.status) ? "failed" : "launch_pending";
      transaction = {
        ...transaction,
        status: transactionStatus,
        updatedAt: new Date(
          Math.max(Date.parse(transaction.updatedAt), now().getTime())
        ).toISOString()
      };
      saveFounderLaunchTransaction(childRoot, transaction);
      const status = workflow.status === "waiting" ? "waiting_external_action" : workflow.status;
      const nextAction2 = workflow.status === "waiting" ? `From ${childRoot}, run vh resume ${runId2} with the exact evidence or renewed authorization shown in the launch report.` : workflow.status === "succeeded" ? `Read reports/launch/${runId2}/final.md inside ${childRoot}.` : `From ${childRoot}, inspect vh status ${runId2} and the launch report before retrying.`;
      return publicFounderPreparation(effectivePreparation, {
        status,
        childRoot,
        runId: runId2,
        workflowStatus: workflow.status,
        materialized: {
          status: materialized2.status,
          planDigest: materialized2.planDigest,
          fileCount: materialized2.files.length
        },
        launchReport: {
          json: `reports/launch/${runId2}/final.json`,
          markdown: `reports/launch/${runId2}/final.md`
        },
        externalEffectsOccurred: Object.keys(workflow.verifiedEffects).length > 0,
        nextAction: nextAction2
      });
    },
    create(request2) {
      const source = isAbsolute6(request2.brief) ? request2.brief : resolve21(root, request2.brief);
      const brief = founderBriefSchema.parse(readStructured(source));
      const decision = routeLaunch(brief);
      const activeEventPacks = eventPacksFor(brief, decision);
      const existing = existsSync12(projectPath(root)) ? loadProject(root) : null;
      if (existing && existing.brief.id !== brief.id) {
        throw new Error(
          `This working directory already contains venture ${existing.brief.id}; refusing to retain its run, provider, mobile, and learning state for different brief ${brief.id}. Next: create the new venture in a fresh child directory, or archive and remove .venture only after reviewing its external-resource evidence.`
        );
      }
      const synchronizedAt = now();
      const updatedContracts = synchronizeBriefContracts(
        root,
        brief,
        decision,
        activeEventPacks,
        synchronizedAt
      );
      const state = {
        schemaVersion: 2,
        createdAt: existing?.createdAt ?? synchronizedAt.toISOString(),
        brief,
        decision,
        activeEventPacks,
        routerVersion: LAUNCH_ROUTER_VERSION
      };
      writeJsonAtomic2(projectPath(root), state);
      return {
        status: "created",
        projectState: ".venture/project.json",
        updatedContracts,
        briefId: brief.id,
        synthetic: brief.synthetic ?? false,
        selectedMode: decision.mode.selectedMode,
        confidence: decision.mode.confidence,
        rail: decision.rail,
        activeEventPacks,
        assumptions: decision.mode.assumptions,
        nextAction: "Run vh plan, then vh launch --dry-run."
      };
    },
    plan(request2) {
      const brief = request2.brief ? founderBriefSchema.parse(
        readStructured(
          isAbsolute6(request2.brief) ? request2.brief : resolve21(root, request2.brief)
        )
      ) : loadProject(root).brief;
      return compileLaunchDryRun(brief);
    },
    async launch(request2) {
      const project = loadProject(root);
      const brief = project.brief;
      const dryRun = compileLaunchDryRun(brief);
      if (request2.mode === "dry-run") return dryRun;
      const runId2 = request2.runId ?? generatedRunId(brief, now());
      if (store.exists(runId2)) {
        throw new Error(`Run ${runId2} already exists. Next: vh resume ${runId2}`);
      }
      const policies = policiesSchema.parse(readStructured(inside6(root, "config/policies.yaml")));
      const providers = providersSchema.parse(
        readStructured(inside6(root, "config/providers.yaml"))
      );
      const launchAt = now();
      let definition2 = scopeLaunchGraphForAuthorization(
        compileLaunchGraph(brief, project.decision),
        request2.authorization
      );
      const pendingFounderTransaction = request2.pendingFounderGrantRenewal ? loadFounderLaunchTransaction(root) : void 0;
      if (request2.pendingFounderGrantRenewal) {
        if (!request2.launchGrant) {
          throw new Error("Pending founder Grant renewal requires the original Launch Grant");
        }
        const storedGrant = parseLaunchGrant(
          readStructured(inside6(root, ".venture/launch-grant.json"))
        );
        if (pendingFounderTransaction?.status !== "launch_pending" || pendingFounderTransaction.runId !== runId2 || pendingFounderTransaction.grantId !== request2.launchGrant.grantId || !isDeepStrictEqual2(storedGrant, request2.launchGrant) || launchAt.getTime() < Date.parse(request2.launchGrant.expiresAt)) {
          throw new Error(
            "Pending founder Grant renewal does not match an expired launch_pending transaction"
          );
        }
      }
      const launchGrant = request2.launchGrant ? assertLaunchGrantBindsGraph({
        grant: request2.launchGrant,
        brief,
        definition: definition2,
        providers,
        at: launchAt,
        allowExpiredRenewal: request2.pendingFounderGrantRenewal === true
      }) : void 0;
      if (launchGrant) {
        definition2 = bindWorkflowBudgetsToLaunchGrant(definition2, launchGrant);
        const expectedProfile = brief.app_kind !== "web" && brief.app_kind !== "hybrid" ? "mobile_testflight" : brief.monetization_model === "none" ? "standard_launch" : "live_commerce_launch";
        if (request2.authorization.replaceAll("-", "_") !== expectedProfile) {
          throw new Error(
            `One-prompt Launch Grant requires authorization profile ${expectedProfile.replaceAll("_", "-")}`
          );
        }
      }
      const issuedAuthorization = issueAuthorizationEnvelope({
        runId: runId2,
        profile: request2.authorization,
        providers: providerIds2(definition2),
        environments: requiredEnvironmentsForLaunch(definition2),
        capabilities: requiredCapabilitiesForLaunch(definition2),
        policies,
        approvalRef: launchGrant ? request2.pendingFounderGrantRenewal ? `launch-grant-pre-run-renewal:${launchGrant.grantId}:${sha256Json(
          pendingFounderTransaction
        ).slice(0, 24)}` : `launch-grant:${launchGrant.grantId}` : `cli:vh-launch:${request2.authorization}`,
        now: launchAt
      });
      const authorization = launchGrant ? authorizationForLaunchGrant(issuedAuthorization, launchGrant) : issuedAuthorization;
      const launch = {
        schemaVersion: 2,
        brief,
        decision: project.decision,
        activeEventPacks: project.activeEventPacks,
        routerVersion: project.routerVersion,
        definition: definition2,
        authorization,
        ...launchGrant ? { launchGrant } : {}
      };
      if (launchGrant && request2.pendingFounderGrantRenewal && pendingFounderTransaction) {
        const authorizationDigest = sha256Json(authorization);
        writeJsonAtomic2(founderLaunchGrantRenewalPath(root, authorizationDigest), {
          schemaVersion: 1,
          kind: "founder_pre_run_envelope_renewal",
          runId: runId2,
          profile: authorization.profile,
          originalGrantId: launchGrant.grantId,
          originalGrantDigest: sha256Json(launchGrant),
          transactionDigest: sha256Json(pendingFounderTransaction),
          authorizationDigest,
          approvalRef: authorization.approval_ref,
          renewedAt: authorization.issued_at,
          expiresAt: authorization.expires_at
        });
      }
      const runtime = await bindingsFor(launch);
      writeJsonAtomic2(launchPath(root, runId2), launch);
      const state = await new WorkflowExecutor({ store, bindings: runtime.bindings }).start(
        definition2,
        {
          runId: runId2
        }
      );
      await persistStateReport(launch, state, runtime.runtimeContext);
      return state;
    },
    async resume(request2) {
      let launch = loadLaunch(root, request2.runId);
      const persistedState = store.load(request2.runId);
      const unfinishedProviderEffects = Object.values(persistedState.nodes).some(
        (record3) => record3.definition.kind === "provider" && !["succeeded", "compensated", "skipped"].includes(record3.state)
      );
      if (request2.authorization) {
        const requestedProfile = request2.authorization.replaceAll("-", "_");
        if (requestedProfile !== launch.authorization.profile) {
          throw new Error(
            `Run ${request2.runId} was created with ${launch.authorization.profile}; renewal must use the same named profile to preserve the persisted graph scope.`
          );
        }
        const policies = policiesSchema.parse(readStructured(inside6(root, "config/policies.yaml")));
        const renewedAt = now();
        if (launch.launchGrant) {
          const providers = providersSchema.parse(
            readStructured(inside6(root, "config/providers.yaml"))
          );
          assertLaunchGrantBindsGraph({
            grant: launch.launchGrant,
            brief: launch.brief,
            definition: launch.definition,
            providers,
            at: renewedAt,
            allowExpiredRenewal: true
          });
        }
        const issuedAuthorization = issueAuthorizationEnvelope({
          runId: request2.runId,
          profile: request2.authorization,
          providers: providerIds2(launch.definition),
          environments: requiredEnvironmentsForLaunch(launch.definition),
          capabilities: requiredCapabilitiesForLaunch(launch.definition),
          policies,
          approvalRef: launch.launchGrant ? `launch-grant-renewal:${launch.launchGrant.grantId}:cli:${request2.authorization}` : `cli:vh-resume:${request2.authorization}`,
          now: renewedAt
        });
        launch = {
          ...launch,
          authorization: launch.launchGrant ? authorizationForLaunchGrant(issuedAuthorization, launch.launchGrant) : issuedAuthorization
        };
        writeJsonAtomic2(launchPath(root, request2.runId), launch);
      } else {
        if (launch.launchGrant && unfinishedProviderEffects) {
          const checkedAt = now();
          if (checkedAt >= new Date(launch.launchGrant.expiresAt)) {
            throw new Error(
              `Launch Grant ${launch.launchGrant.grantId} expired at ${launch.launchGrant.expiresAt}. Next: vh resume ${request2.runId} --authorization ${launch.authorization.profile.replaceAll("_", "-")} to issue a fresh run envelope within the immutable Grant scope.`
            );
          }
          assertLaunchGrantActive(launch.launchGrant, checkedAt);
        }
        if (Date.parse(launch.authorization.expires_at) <= now().getTime()) {
          if (unfinishedProviderEffects) {
            throw new Error(
              `Authorization envelope for ${request2.runId} expired at ${launch.authorization.expires_at}. Next: vh resume ${request2.runId} --authorization ${launch.authorization.profile.replaceAll("_", "-")}.`
            );
          }
        }
      }
      const runtime = await bindingsFor(launch);
      const executor = new WorkflowExecutor({ store, bindings: runtime.bindings });
      if (request2.nodeId) {
        const evidenceArtifact = artifactReferenceSchema.parse(request2.evidenceArtifact);
        if (request2.resolutionKind === "checkpoint_grant") {
          if (!request2.effect || !request2.operationId) {
            throw new Error("A checkpoint grant requires an exact effect and operation ID.");
          }
          const evidence = loadRepositoryCheckpointEvidence({
            rootDir: root,
            evidenceArtifact,
            runId: request2.runId,
            redactor: runtime.runtimeContext.redactor
          });
          await executor.grantAuthorizationCheckpoint(request2.runId, request2.nodeId, {
            effect: request2.effect,
            operationId: request2.operationId,
            evidenceArtifact,
            approvedBy: evidence.approved_by,
            approvedAt: evidence.approved_at,
            expiresAt: launch.authorization.expires_at
          });
        } else {
          let output;
          if (request2.outputFile) {
            output = readStructured(
              isAbsolute6(request2.outputFile) ? request2.outputFile : resolve21(root, request2.outputFile)
            );
          } else if (request2.resolutionKind === "manual") {
            const evidence = readStructured(inside6(root, evidenceArtifact));
            if (!evidence || typeof evidence !== "object" || Array.isArray(evidence) || !("output" in evidence)) {
              throw new Error(
                `Manual evidence ${evidenceArtifact} must be typed JSON with an output field, or pass --output <json-file>.`
              );
            }
            output = evidence.output;
          } else {
            output = { approved: true };
          }
          noCredentialMaterial(output);
          const resolution = {
            approvedBy: "vh-cli-user",
            note: request2.note ?? "Resolved explicitly through vh resume.",
            output,
            evidenceArtifact
          };
          if (request2.resolutionKind === "approval") {
            await executor.approve(request2.runId, request2.nodeId, resolution);
          } else {
            await executor.completeManualAction(request2.runId, request2.nodeId, resolution);
          }
        }
      }
      const state = await executor.resume(launch.definition, request2.runId);
      await persistStateReport(launch, state, runtime.runtimeContext);
      return state;
    },
    async cancel(runId2, reason) {
      const launch = loadLaunch(root, runId2);
      const runtime = await bindingsFor(launch);
      const state = await new WorkflowExecutor({
        store,
        bindings: runtime.bindings
      }).cancel(runId2, reason, launch.definition);
      await persistStateReport(launch, state, runtime.runtimeContext);
      return state;
    },
    async stack(request2) {
      if (request2.action === "create") {
        if (!request2.file) {
          throw new Error("vh stack create founder-default requires --file <connection.json>");
        }
        const connection2 = loadFounderStackConnectionFile(request2.file, { baseDir: root });
        const saved = founderStackStore.save(connection2);
        return {
          status: "created",
          schemaVersion: saved.schemaVersion,
          profileId: saved.profileId,
          ownerOrganizationId: saved.ownerOrganizationId,
          roleCount: Object.keys(saved.roles).length,
          fixtureOnly: saved.writableCredentialBackend.mode === "fixture",
          valuesExposed: false,
          externalEffects: false,
          nextCommand: "vh stack doctor founder-default"
        };
      }
      const connection = founderStackStore.load(request2.profileId);
      if (connection && (!options.providerContext || options.dataHttpFetcher !== void 0)) {
        await testFounderStackCredentials(connection);
      }
      return await doctorFounderStackConnection({
        connection,
        context: { ...effectiveProviderContext, authorization: "dry_run" },
        registry: effectiveProviderRegistry,
        now
      });
    },
    doctor: async () => {
      const project = existsSync12(projectPath(root)) ? loadProject(root) : null;
      const definition2 = project ? compileLaunchGraph(project.brief) : null;
      const providerInspection = await inspectDefaultProviderDoctor({
        rootDir: root,
        broker: credentialBroker,
        context: effectiveProviderContext,
        runner: commandRunner,
        registry: effectiveProviderRegistry,
        lifecycleStore: providerLifecycleStore,
        launch: project && definition2 ? {
          brief: project.brief,
          definition: definition2,
          factories: options.providerPlanFactories && typeof options.providerPlanFactories !== "function" ? options.providerPlanFactories : void 0
        } : void 0
      });
      return {
        node: process.version,
        platform: process.platform,
        projectSelected: project !== null,
        knownRuns: store.listRuns().length,
        ...providerInspection,
        providerChecksStatus: "executed locally; remote readiness is reported only for authenticated/read-back evidence",
        providerFactoryInspection: typeof options.providerPlanFactories === "function" ? "default factories inspected; the injected context-dependent factory is resolved only for an authorized run" : options.providerPlanFactories ? "injected static factories inspected" : "built-in complete-or-fail factories inspected",
        credentialCatalogIssues: catalogIssues
      };
    },
    async auth(request2) {
      const { action, provider } = request2;
      let references = credentialBroker.list().filter(
        (reference2) => (!provider || reference2.provider === provider) && (!request2.ref || reference2.ref === request2.ref)
      );
      if (action === "status") {
        return {
          references: await Promise.all(
            references.map((reference2) => credentialBroker.inspect(reference2.ref))
          ),
          catalogIssues,
          valuesExposed: false
        };
      }
      if (action === "test") {
        const founderConnection = founderStackStore.load("founder-default");
        const builtInTesters = defaultCredentialTesters(founderConnection);
        const tested = await Promise.all(
          references.map(async (reference2) => {
            const tester = options.credentialTesters?.[reference2.provider] ?? builtInTesters[reference2.provider];
            if (tester && reference2.kind !== "cli_session") {
              return {
                ref: reference2.ref,
                mode: "remote_tester",
                result: await credentialBroker.test(reference2.ref, tester)
              };
            }
            return {
              ref: reference2.ref,
              mode: reference2.kind === "cli_session" ? "official_cli_read" : "availability_only",
              result: await credentialBroker.inspect(reference2.ref),
              limitation: reference2.kind === "cli_session" ? null : "No provider credential tester was injected; availability is not remote authorization proof."
            };
          })
        );
        const adapter = provider ? effectiveProviderRegistry.list().find((item) => item.descriptor.id === provider) : void 0;
        const providerDoctor = adapter ? await adapter.doctor(
          { credentialRefs: references.map((reference2) => reference2.ref) },
          effectiveProviderContext
        ) : null;
        credentialCatalog = {
          ...credentialCatalog,
          references: credentialCatalog.references.map(
            (catalogReference) => credentialBroker.getReference(catalogReference.ref) ?? catalogReference
          )
        };
        saveCredentialCatalog(credentialCatalog, catalogPath);
        return { tested, providerDoctor, valuesExposed: false };
      }
      if (action === "revoke") {
        if (!provider) throw new Error("vh auth revoke requires a provider.");
        if (references.length === 0)
          throw new Error(`No credential refs are registered for ${provider}.`);
        const revoked = await Promise.all(
          references.map(async (reference2) => {
            const result2 = await credentialBroker.revoke(reference2.ref);
            const localRemoval = result2.removed ? "removed" : "failed";
            const catalogReference = result2.removed ? "removed" : "preserved_disabled";
            const remoteAction = providerRevocationAction(reference2);
            const nextAction2 = result2.removed ? `${remoteAction}, then run vh auth status and confirm the reference is absent or revoked.` : `Local removal failed: ${localRemovalAction(reference2)}. The catalog reference was preserved in a disabled state. Separately, ${remoteAction}, then run vh auth status.`;
            return {
              ...result2,
              localRemoval,
              catalogReference,
              providerSideRevocation: "manual_required",
              nextAction: nextAction2
            };
          })
        );
        const removedRefs = revoked.filter(({ removed }) => removed).map(({ ref: ref2 }) => ref2);
        credentialCatalog = removeCredentialReferences(credentialCatalog, removedRefs);
        for (const result2 of revoked.filter(({ removed }) => !removed)) {
          const disabled = credentialBroker.getReference(result2.ref);
          if (disabled) credentialCatalog = upsertCredentialReference(credentialCatalog, disabled);
        }
        saveCredentialCatalog(credentialCatalog, catalogPath);
        return { provider, revoked, valuesExposed: false };
      }
      if (!provider) {
        return {
          status: "provider_required",
          interactiveCliProviders: Object.keys(
            // The exact commands remain in the credential module and are never shell strings.
            { github: true, vercel: true, stripe: true, eas: true }
          ),
          registeredProviders: [...new Set(credentialBroker.list().map((item) => item.provider))],
          nextAction: "Run vh auth login <provider>; add --backend/--kind when not using its official CLI."
        };
      }
      const backend = request2.backend ?? (["github", "vercel", "eas"].includes(provider) && supportsInteractiveCliAuth(provider) ? "cli_session" : "environment");
      if (!options.credentialBroker && !supportedBackends.has(backend)) {
        throw new Error(
          `Backend ${backend} is not available on this host. Choose: ${[...supportedBackends].sort().join(", ")}.`
        );
      }
      const ref = request2.ref ?? `cred://${provider}/default`;
      const reference = {
        ref,
        provider,
        kind: credentialKind(request2.kind, backend),
        backend,
        scopes: request2.scopes ?? []
      };
      if (backend === "cli_session") {
        await (options.interactiveCliLogin ?? runInteractiveCliLogin)(provider);
      }
      credentialBroker.register(reference);
      credentialCatalog = upsertCredentialReference(credentialCatalog, reference);
      saveCredentialCatalog(credentialCatalog, catalogPath);
      references = [reference];
      const inspection = await credentialBroker.inspect(ref);
      return {
        status: inspection.status === "available" ? "authenticated" : "registered",
        reference: inspection,
        valuesExposed: false,
        nextAction: inspection.status === "available" ? `Run vh auth test ${provider}.` : backend === "environment" || backend === "ci" ? `Set ${environmentVariableForRef(ref)} in the process or CI secret store, then run vh auth test ${provider}.` : `Store ${ref} in backend ${backend} without placing the value in Git or argv, then run vh auth test ${provider}.`
      };
    },
    async dataSync() {
      const result2 = await learningRuntime.sync();
      writeJsonAtomic2(inside6(root, ".venture/data/latest.json"), result2);
      return result2;
    },
    learn(cadence) {
      const latestPath = inside6(root, ".venture/data/latest.json");
      const sync = existsSync12(latestPath) ? JSON.parse(readFileSync14(latestPath, "utf8")) : learningRuntime.missingArtifact(cadence);
      const { definition: definition2, report } = learningRuntime.learn(cadence, sync);
      const artifacts = persistLearningReport({ rootDir: root, definition: definition2, report });
      const operatingCadence = learningRuntime.operatingCadence(sync);
      const operatingCadenceArtifacts = persistOperatingCadence({
        rootDir: root,
        cadence: operatingCadence
      });
      return {
        ...report,
        artifacts,
        operatingCadence,
        operatingCadenceArtifacts
      };
    },
    async upgrade({ dryRun, releasePath }) {
      const fileSystem = createNodeMigrationFileSystem(root);
      const lockPath = inside6(root, "harness.lock");
      const lock = existsSync12(lockPath) ? loadHarnessLock(lockPath) : void 0;
      const release = releasePath ? await locateLocalHarnessRelease({ locator: releasePath, baseDir: root }) : options.release;
      if (!release && !lock) return migrateV01ToV02({ fileSystem, dryRun, clock: now });
      if (!release) {
        return {
          status: "already_current",
          harnessVersion: lock.harness_version,
          source: lock.source,
          nextAction: "Choose and review a local release checkout, then run vh upgrade --release <local-release-root> --dry-run."
        };
      }
      return await applyOperationalUpgrade({
        fileSystem,
        currentLock: lock,
        release,
        commandRunner: options.upgradeCommandRunner ?? commandRunner,
        rootDir: root,
        dryRun,
        clock: now
      });
    }
  };
}

// lib/founder-launch/stack-connect.ts
var FOUNDER_STACK_PLAN = [
  {
    role: "source.repository",
    provider: "github",
    authStyle: "cli_session",
    required: true,
    identifiers: ["accountId", "organizationId"],
    scopes: ["repo", "workflow"]
  },
  {
    role: "hosting.web",
    provider: "vercel",
    authStyle: "cli_session",
    required: true,
    identifiers: ["accountId", "teamId"],
    scopes: []
  },
  {
    role: "database.postgres",
    provider: "neon",
    authStyle: "api_key",
    required: true,
    identifiers: ["accountId", "organizationId"],
    scopes: []
  },
  {
    role: "commerce.web",
    provider: "stripe",
    authStyle: "api_key",
    required: true,
    identifiers: ["accountId"],
    scopes: []
  },
  {
    role: "email.transactional",
    provider: "brevo",
    authStyle: "api_key",
    required: false,
    identifiers: ["accountId"],
    scopes: []
  },
  {
    role: "growth.google",
    provider: "google",
    authStyle: "api_key",
    required: false,
    identifiers: ["accountId", "organizationId"],
    scopes: []
  },
  {
    role: "search.bing",
    provider: "bing",
    authStyle: "api_key",
    required: false,
    identifiers: ["accountId"],
    scopes: []
  },
  {
    role: "commerce.native",
    provider: "revenuecat",
    authStyle: "api_key",
    required: false,
    identifiers: ["accountId", "organizationId"],
    scopes: []
  },
  {
    role: "dns.records",
    provider: "dns",
    authStyle: "api_key",
    required: false,
    identifiers: [],
    scopes: []
  }
];
function credentialRefFor(provider, profileId) {
  return `cred://${provider}/${profileId}`;
}
function assertReferenceOnly(value, label) {
  if (!value.startsWith("cred://")) {
    throw new Error(
      `${label} must be a cred:// reference, never a credential value. Next: store the value in your credential backend and pass its reference.`
    );
  }
}
function assertNoSecretsInArgv(argv) {
  for (const argument of argv) {
    if (argument.startsWith("cred://")) continue;
    if (/^(?:sk|rk|pk)_(?:live|test)_/.test(argument) || /^xkeysib-/.test(argument) || /^gh[pousr]_/.test(argument) || /^napi_/.test(argument) || /^--(?:token|secret|api-key|password)=/.test(argument)) {
      throw new Error(
        "vh stack connect never accepts a credential value as an argument, because arguments are visible to other processes and land in shell history. Next: rerun vh stack connect founder-default and paste the value at the hidden prompt."
      );
    }
  }
}
var CLI_LOGIN_COMMAND = {
  github: "gh auth login",
  vercel: "vercel login"
};
var API_KEY_HINT = {
  neon: "Create a Neon API key at https://console.neon.tech/app/settings/api-keys",
  stripe: "Copy the Stripe test-mode secret key from https://dashboard.stripe.com/test/apikeys",
  brevo: "Create a Brevo API key at https://app.brevo.com/settings/keys/api",
  google: "Create a Google service credential with Analytics and Search Console access",
  bing: "Create a Bing Webmaster API key at https://www.bing.com/webmasters/apikeys",
  revenuecat: "Create a RevenueCat secret API key in project settings",
  dns: "Provide DNS adapter credentials, or leave DNS manual"
};
function planFounderStackConnection(input) {
  const sessionByProvider = new Map(input.sessions.map((session) => [session.provider, session]));
  const collectedByRole = new Map(input.collected.map((entry) => [entry.role, entry]));
  const resolved = [];
  const unresolved = [];
  for (const plan of FOUNDER_STACK_PLAN) {
    const collected = collectedByRole.get(plan.role);
    const session = sessionByProvider.get(plan.provider);
    if (plan.authStyle === "cli_session" && !session?.authenticated) {
      unresolved.push({
        role: plan.role,
        provider: plan.provider,
        why: `No authenticated ${plan.provider} CLI session was found.`,
        command: CLI_LOGIN_COMMAND[plan.provider] ?? `${plan.provider} login`,
        blocksLaunch: plan.required
      });
      continue;
    }
    if (!collected) {
      unresolved.push({
        role: plan.role,
        provider: plan.provider,
        why: `No credential reference is recorded for ${plan.role}.`,
        command: plan.authStyle === "cli_session" ? CLI_LOGIN_COMMAND[plan.provider] ?? `${plan.provider} login` : `vh stack connect founder-default  (${API_KEY_HINT[plan.provider] ?? "provide the provider credential"})`,
        blocksLaunch: plan.required
      });
      continue;
    }
    assertReferenceOnly(collected.credentialRef, `${plan.role} credentialRef`);
    const missing = plan.identifiers.filter((name) => !collected.identifiers[name]?.trim());
    if (missing.length > 0) {
      unresolved.push({
        role: plan.role,
        provider: plan.provider,
        why: `${plan.role} is missing ${missing.join(" and ")}.`,
        command: `vh stack connect founder-default  (supply the exact ${plan.provider} ${missing.join(" and ")})`,
        blocksLaunch: plan.required
      });
      continue;
    }
    resolved.push(plan.role);
  }
  return {
    resolved,
    unresolved,
    launchReady: unresolved.every((action) => !action.blocksLaunch)
  };
}
function blockingActions(plan) {
  return plan.unresolved.filter((action) => action.blocksLaunch);
}

// lib/founder-launch/stack-connect-shell.ts
import { execFileSync as execFileSync2 } from "node:child_process";
function tryCommand(command, args) {
  try {
    return execFileSync2(command, [...args], {
      encoding: "utf8",
      stdio: ["ignore", "pipe", "ignore"],
      timeout: 2e4
    }).trim();
  } catch {
    return null;
  }
}
function detectGitHubSession() {
  const login = tryCommand("gh", ["api", "user", "--jq", ".login"]);
  if (!login) {
    return {
      provider: "github",
      authenticated: false,
      detail: "No usable GitHub CLI session."
    };
  }
  return { provider: "github", authenticated: true, account: login };
}
function detectVercelSession() {
  const who = tryCommand("vercel", ["whoami"]);
  if (!who) {
    return {
      provider: "vercel",
      authenticated: false,
      detail: "No usable Vercel CLI session."
    };
  }
  return { provider: "vercel", authenticated: true, account: who.split("\n").at(-1)?.trim() };
}
function detectSessions() {
  return [detectGitHubSession(), detectVercelSession()];
}
function discoverGitHubOwner() {
  return tryCommand("gh", ["api", "user", "--jq", ".login"]) ?? void 0;
}
function discoverVercelScope() {
  return tryCommand("vercel", ["whoami"])?.split("\n").at(-1)?.trim();
}

// lib/fleet/controller.ts
import { stringify as stringify7 } from "yaml";

// lib/fleet/release.ts
var semver2 = external_exports.string().regex(/^\d+\.\d+\.\d+$/);
var releaseBodySchema = external_exports.object({
  schemaVersion: external_exports.literal(1),
  version: semver2,
  sourceRef: external_exports.string().regex(/^v\d+\.\d+\.\d+$/),
  workflowRefSha: external_exports.string().regex(/^[a-f0-9]{40}$/),
  changedPackages: external_exports.record(external_exports.string().min(1), external_exports.object({ from: semver2, to: semver2 }).strict()),
  affectedCapabilities: external_exports.array(external_exports.string().min(1)),
  migrations: external_exports.array(external_exports.string().regex(/^\d{3}-[a-z0-9-]+$/)),
  compatibility: external_exports.object({ minimumCoreVersion: semver2, seedIds: external_exports.array(external_exports.string().min(1)).min(1) }).strict(),
  requiredChecks: external_exports.array(external_exports.string().min(1)).min(1),
  rolloutRisk: external_exports.enum(["low", "medium", "high"]),
  rollback: external_exports.object({
    mode: external_exports.enum(["previous_release", "forward_fix"]),
    version: semver2.nullable()
  }).strict(),
  files: external_exports.array(managedReleaseFileSchema)
}).strict().superRefine((value, context2) => {
  if (value.sourceRef !== `v${value.version}`) {
    context2.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["sourceRef"],
      message: "release sourceRef must be the exact version tag"
    });
  }
  if (Object.keys(value.changedPackages).length === 0 && value.files.length === 0) {
    context2.addIssue({
      code: external_exports.ZodIssueCode.custom,
      message: "release must change at least one package or managed file"
    });
  }
  if (value.rollback.mode === "previous_release") {
    if (value.rollback.version === null) {
      context2.addIssue({
        code: external_exports.ZodIssueCode.custom,
        path: ["rollback", "version"],
        message: "previous_release rollback requires an exact prior version"
      });
    } else {
      const target = value.version.split(".").map(Number);
      const rollback2 = value.rollback.version.split(".").map(Number);
      const comparison = rollback2.findIndex((part, index) => part !== target[index]);
      if (comparison === -1 || (rollback2[comparison] ?? 0) > (target[comparison] ?? 0)) {
        context2.addIssue({
          code: external_exports.ZodIssueCode.custom,
          path: ["rollback", "version"],
          message: "previous_release rollback version must be older than the target release"
        });
      }
    }
  }
  if (value.rollback.mode === "forward_fix" && value.rollback.version !== null) {
    context2.addIssue({
      code: external_exports.ZodIssueCode.custom,
      path: ["rollback", "version"],
      message: "forward_fix rollback cannot declare a replacement version"
    });
  }
});

// lib/cli/index.ts
var HELP = `Venture Harness CLI

Usage:
  vh auth login [provider]         Authenticate through a configured provider adapter
  vh auth status                  Show credential references and provider auth state
  vh auth test [provider]          Test access/scopes without exposing credential values
  vh auth revoke <provider>       Revoke through a configured provider adapter
    --ref cred://provider/name --backend environment|macos_keychain|onepassword|cli_session
    --kind <credential-kind> --scopes <comma-separated>
  vh stack create founder-default --file <connection.json>
                                  Persist one credential-free founder Stack connection
  vh stack connect founder-default
                                  Guided one-time connection of the default founder Stack
  vh stack doctor founder-default Inspect every founder Stack role without provider effects
  vh config show                  Show persistent founder settings
  vh config set ventures-root <absolute-path>
                                  Choose where independent ventures are materialized
  vh doctor                       Inspect local launch prerequisites
  vh create --brief <file>        Validate and persist one progressive-commitment brief
  vh plan [--brief <file>]        Compile a launch plan without side effects
  vh launch --dry-run             Show nodes, effects, approvals, cost, and manual work
  vh launch --apply --authorization <profile>
                                  Execute an authorized launch graph
  vh launch --idea <idea.md> --stack founder-default --production --apply --non-interactive
                                  Materialize and execute the complete founder launch in one command
  vh status [run-id]              Show one run or list known runs
  vh resume <run-id>              Resume the same persisted run
    --authorization <profile>     Explicitly renew an expired run envelope
  vh resume <run-id> --manual <node-id> --evidence <artifact> [--output <json-file>]
                                  Resolve one manual node with evidence, then resume
  vh resume <run-id> --grant <node-id> --effect <effect> --operation <operation-id> --evidence <artifact>
                                  Issue one exact one-shot checkpoint grant, then resume
  vh cancel <run-id>              Persist cancellation and run configured compensation
  vh explain [run-id] <node-id>   Explain a node from durable state
  vh data sync                    Ingest through configured read-only connectors
  vh learn daily|weekly|biweekly|monthly
                                  Run an evidence-based learning cadence
  vh upgrade [--release <local-root>] [--dry-run]
                                  Upgrade from a hash-verified local release

Options:
  --json                          Emit machine-readable JSON where supported
  -h, --help                      Show this help`;
function defaultIo() {
  return {
    stdout: (line) => console.log(line),
    stderr: (line) => console.error(line)
  };
}
function flagValue(args, flag4) {
  const index = args.indexOf(flag4);
  return index >= 0 ? args[index + 1] : void 0;
}
function positional(args) {
  const values = [];
  for (let index = 0; index < args.length; index += 1) {
    if (args[index].startsWith("-")) {
      if ([
        "--brief",
        "--idea",
        "--stack",
        "--output",
        "--authorization",
        "--run-id",
        "--manual",
        "--approve",
        "--grant",
        "--effect",
        "--operation",
        "--evidence",
        "--output",
        "--note",
        "--reason",
        "--ref",
        "--backend",
        "--kind",
        "--scopes",
        "--file",
        "--release"
      ].includes(args[index]))
        index += 1;
      continue;
    }
    values.push(args[index]);
  }
  return values;
}
function emit(io, value, json2) {
  if (json2 || typeof value !== "string") io.stdout(JSON.stringify(value, null, 2) ?? "null");
  else io.stdout(value);
}
function terminalWorkflowExitCode(value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) return 0;
  const status = value.status;
  return status === "failed" || status === "cancelled" ? 1 : 0;
}
function founderLaunchExitCode(value, mode) {
  if (mode === "dry-run" || !value || typeof value !== "object" || Array.isArray(value)) return 0;
  const status = value.status;
  return status === "blocked" || status === "failed" ? 1 : 0;
}
function unsupported(io, command, nextAction2) {
  io.stderr(`${command} is not configured; no external action was taken.`);
  io.stderr(`Next: ${nextAction2}`);
  return { exitCode: 2 };
}
function latestRun(store) {
  const runs = store.listRuns();
  if (runs.length === 0) return void 0;
  return runs.map((runId2) => store.load(runId2)).sort((a, b) => a.updatedAt.localeCompare(b.updatedAt)).at(-1)?.runId;
}
async function runCli(args, options = {}) {
  const io = options.io ?? defaultIo();
  const store = options.store ?? new FileWorkflowStore();
  const services = options.services ?? createDefaultCliServices({ store });
  const json2 = args.includes("--json");
  const [command, ...rest] = args;
  if (!command || command === "help" || command === "--help" || command === "-h") {
    io.stdout(HELP);
    return { exitCode: 0 };
  }
  try {
    if (command === "create") {
      const brief = flagValue(rest, "--brief");
      if (!brief) {
        io.stderr("vh create requires --brief <file>.");
        return { exitCode: 2 };
      }
      if (!services.create)
        return unsupported(io, "vh create", "register the progressive-commitment brief service");
      emit(io, await services.create({ brief, json: json2 }), json2);
      return { exitCode: 0 };
    }
    if (command === "doctor") {
      const builtIn = {
        node: process.version,
        platform: process.platform,
        runStore: store instanceof FileWorkflowStore ? store.rootDir : "injected",
        knownRuns: store.listRuns().length,
        providerChecks: services.doctor ? "configured" : "not configured"
      };
      const result2 = services.doctor ? await services.doctor() : builtIn;
      emit(io, result2, json2);
      if (!services.doctor) {
        io.stderr(
          "Provider checks were not run. Next: register provider doctor checks, then rerun vh doctor."
        );
      }
      return { exitCode: 0 };
    }
    if (command === "stack" && positional(rest)[0] === "connect") {
      try {
        assertNoSecretsInArgv(rest);
      } catch (error) {
        io.stderr(error instanceof Error ? error.message : String(error));
        return { exitCode: 2 };
      }
      const values = positional(rest);
      if (values.length !== 2 || values[1] !== "founder-default") {
        io.stderr("Usage: vh stack connect founder-default");
        return { exitCode: 2 };
      }
      const sessions = detectSessions();
      const collected = [];
      const githubOwner = discoverGitHubOwner();
      const vercelScope = discoverVercelScope();
      if (githubOwner) {
        collected.push({
          role: "source.repository",
          credentialRef: credentialRefFor("github", "founder-default"),
          identifiers: { accountId: githubOwner, organizationId: githubOwner }
        });
      }
      if (vercelScope) {
        collected.push({
          role: "hosting.web",
          credentialRef: credentialRefFor("vercel", "founder-default"),
          identifiers: { accountId: vercelScope, teamId: vercelScope }
        });
      }
      const plan = planFounderStackConnection({
        profileId: "founder-default",
        ownerOrganizationId: githubOwner ?? "founder",
        sessions,
        collected
      });
      const venturesRoot = loadFounderConfig().venturesRoot ?? null;
      const blocking = blockingActions(plan);
      emit(
        io,
        {
          command: "stack.connect",
          profileId: "founder-default",
          sessions: sessions.map(({ provider, authenticated: authenticated2, account }) => ({
            provider,
            authenticated: authenticated2,
            ...account ? { account } : {}
          })),
          resolved: plan.resolved,
          venturesRoot,
          launchReady: plan.launchReady && venturesRoot !== null,
          unresolvedActions: [
            ...plan.unresolved.map((action) => ({
              role: action.role,
              provider: action.provider,
              why: action.why,
              command: action.command,
              blocksLaunch: action.blocksLaunch
            })),
            ...venturesRoot ? [] : [
              {
                role: "workspace",
                provider: "local",
                why: "No ventures root is configured, so a launch has nowhere safe to materialize a venture.",
                command: "vh config set ventures-root <absolute-path>",
                blocksLaunch: true
              }
            ]
          ]
        },
        json2
      );
      return { exitCode: blocking.length === 0 && venturesRoot ? 0 : 1 };
    }
    if (command === "stack") {
      const unknownFlag = rest.find(
        (value) => value.startsWith("-") && value !== "--file" && value !== "--json"
      );
      const fileIndexes = rest.map((value, index) => value === "--file" ? index : -1).filter((index) => index >= 0);
      if (unknownFlag || fileIndexes.length > 1 || fileIndexes.length === 1 && (!rest[fileIndexes[0] + 1] || rest[fileIndexes[0] + 1].startsWith("-"))) {
        io.stderr(
          "Usage: vh stack create founder-default --file <connection.json> | vh stack doctor founder-default"
        );
        return { exitCode: 2 };
      }
      const values = positional(rest);
      const action = values[0];
      const profileId = values[1];
      if (action !== "create" && action !== "doctor" || profileId !== "founder-default" || values.length !== 2) {
        io.stderr(
          "Usage: vh stack create founder-default --file <connection.json> | vh stack doctor founder-default"
        );
        return { exitCode: 2 };
      }
      const file2 = flagValue(rest, "--file");
      if (action === "create" && !file2) {
        io.stderr("vh stack create founder-default requires --file <connection.json>.");
        return { exitCode: 2 };
      }
      if (action === "doctor" && rest.includes("--file")) {
        io.stderr("vh stack doctor founder-default does not accept --file.");
        return { exitCode: 2 };
      }
      if (!services.stack) {
        return unsupported(
          io,
          `vh stack ${action} founder-default`,
          "configure the founder Stack metadata store and credential broker"
        );
      }
      emit(io, await services.stack({ action, profileId, ...file2 ? { file: file2 } : {} }), json2);
      return { exitCode: 0 };
    }
    if (command === "config") {
      const values = positional(rest);
      const action = values[0];
      const usage = "Usage: vh config show | vh config set ventures-root <absolute-path>";
      if (action === "show" && values.length === 1) {
        const config = loadFounderConfig();
        emit(
          io,
          {
            command: "config.show",
            path: defaultFounderConfigPath(),
            venturesRoot: config.venturesRoot ?? null,
            ...config.venturesRoot ? {} : { nextAction: "vh config set ventures-root <absolute-path>" }
          },
          json2
        );
        return { exitCode: 0 };
      }
      if (action !== "set" || values.length !== 3) {
        io.stderr(usage);
        return { exitCode: 2 };
      }
      const [, key, value] = values;
      if (!FOUNDER_CONFIG_KEYS.includes(key)) {
        io.stderr(
          `Unknown founder setting ${key}. Known settings: ${FOUNDER_CONFIG_KEYS.join(", ")}.`
        );
        return { exitCode: 2 };
      }
      try {
        const venturesRoot = resolveVenturesRoot(value, { coreRoot: process.cwd() });
        const existing = loadFounderConfig();
        saveFounderConfig({ ...existing, venturesRoot });
        emit(io, { command: "config.set", key, venturesRoot }, json2);
        return { exitCode: 0 };
      } catch (error) {
        io.stderr(error instanceof Error ? error.message : String(error));
        return { exitCode: 1 };
      }
    }
    if (command === "plan") {
      if (!services.plan)
        return unsupported(
          io,
          "vh plan",
          "register the launch planner and provider registry, then rerun vh plan"
        );
      emit(io, await services.plan({ brief: flagValue(rest, "--brief"), json: json2 }), json2);
      return { exitCode: 0 };
    }
    if (command === "launch") {
      const dryRun = rest.includes("--dry-run");
      const apply = rest.includes("--apply");
      if (dryRun === apply) {
        io.stderr("Choose exactly one of --dry-run or --apply.");
        io.stderr("Next: run vh launch --dry-run before requesting an authorized apply.");
        return { exitCode: 2 };
      }
      const idea = flagValue(rest, "--idea");
      if (idea) {
        const stackProfile = flagValue(rest, "--stack");
        if (stackProfile !== "founder-default") {
          io.stderr("One-prompt launch requires --stack founder-default.");
          return { exitCode: 2 };
        }
        if (!rest.includes("--production") || !rest.includes("--non-interactive")) {
          io.stderr("One-prompt launch requires --production and --non-interactive.");
          return { exitCode: 2 };
        }
        if (rest.includes("--authorization")) {
          io.stderr(
            "One-prompt launch derives its graph authorization from the issued Launch Grant; do not pass --authorization."
          );
          return { exitCode: 2 };
        }
        if (!services.founderLaunch) {
          return unsupported(
            io,
            "vh launch --idea",
            "configure the founder Stack and one-prompt launch service"
          );
        }
        const mode = dryRun ? "dry-run" : "apply";
        const result3 = await services.founderLaunch({
          mode,
          idea,
          stackProfile,
          production: true,
          nonInteractive: true,
          output: flagValue(rest, "--output"),
          json: json2
        });
        emit(io, result3, json2);
        return { exitCode: founderLaunchExitCode(result3, mode) };
      }
      const authorization = flagValue(rest, "--authorization");
      if (apply && !authorization) {
        io.stderr("--apply requires --authorization <profile>; no external action was taken.");
        return { exitCode: 2 };
      }
      if (!services.launch)
        return unsupported(
          io,
          "vh launch",
          "register a launch compiler and capability adapters, then rerun the dry run"
        );
      const request2 = {
        mode: dryRun ? "dry-run" : "apply",
        authorization,
        runId: flagValue(rest, "--run-id"),
        json: json2
      };
      const result2 = await services.launch(request2);
      emit(io, result2, json2);
      return { exitCode: terminalWorkflowExitCode(result2) };
    }
    if (command === "status") {
      const runId2 = positional(rest)[0];
      if (!runId2) {
        const runs = store.listRuns().map((id) => {
          const state = store.load(id);
          return {
            runId: id,
            graph: state.graph.id,
            status: state.status,
            updatedAt: state.updatedAt
          };
        });
        emit(io, runs, true);
      } else {
        emit(io, store.load(runId2), true);
      }
      return { exitCode: 0 };
    }
    if (command === "resume") {
      const runId2 = positional(rest)[0];
      if (!runId2) {
        io.stderr("vh resume requires <run-id>.");
        return { exitCode: 2 };
      }
      if (!services.resume)
        return unsupported(
          io,
          "vh resume",
          `load the persisted graph definition for "${runId2}", register its handlers, and rerun vh resume ${runId2}`
        );
      const manualNode2 = flagValue(rest, "--manual");
      const approvalNode = flagValue(rest, "--approve");
      const grantNode = flagValue(rest, "--grant");
      if ([manualNode2, approvalNode, grantNode].filter(Boolean).length > 1) {
        io.stderr("Choose at most one of --manual, --approve, or --grant.");
        return { exitCode: 2 };
      }
      const resolutionNode = manualNode2 ?? approvalNode ?? grantNode;
      if (resolutionNode && !flagValue(rest, "--evidence")) {
        io.stderr("Resolving a checkpoint requires --evidence <repository-relative-artifact>.");
        return { exitCode: 2 };
      }
      const effectInput = flagValue(rest, "--effect");
      const operationId2 = flagValue(rest, "--operation");
      const effect = effectInput ? sideEffectClassSchema.safeParse(effectInput) : void 0;
      if (grantNode && (!effectInput || !effect?.success || !operationId2)) {
        io.stderr("--grant requires a valid --effect <effect> and --operation <operation-id>.");
        return { exitCode: 2 };
      }
      if (!grantNode && (effectInput || operationId2)) {
        io.stderr("--effect and --operation may only be used with --grant.");
        return { exitCode: 2 };
      }
      const result2 = await services.resume({
        runId: runId2,
        authorization: flagValue(rest, "--authorization"),
        nodeId: resolutionNode,
        resolutionKind: manualNode2 ? "manual" : approvalNode ? "approval" : grantNode ? "checkpoint_grant" : void 0,
        evidenceArtifact: flagValue(rest, "--evidence"),
        effect: effect?.success ? effect.data : void 0,
        operationId: operationId2,
        outputFile: flagValue(rest, "--output"),
        note: flagValue(rest, "--note")
      });
      emit(io, result2, true);
      return { exitCode: terminalWorkflowExitCode(result2) };
    }
    if (command === "cancel") {
      const runId2 = positional(rest)[0];
      if (!runId2) {
        io.stderr("vh cancel requires <run-id>.");
        return { exitCode: 2 };
      }
      const reason = flagValue(rest, "--reason") ?? "cancelled from vh CLI";
      const state = services.cancel ? await services.cancel(runId2, reason) : await new WorkflowExecutor({ store }).cancel(runId2, reason);
      emit(io, { runId: state.runId, status: state.status, reason }, true);
      if (!services.cancel) {
        io.stderr(
          "Cancellation was persisted without provider compensation. Next: configure the launch runtime to run any declared compensation hooks."
        );
      }
      return { exitCode: 0 };
    }
    if (command === "explain") {
      const values = positional(rest);
      const runId2 = values.length >= 2 ? values[0] : latestRun(store);
      const nodeId = values.length >= 2 ? values[1] : values[0];
      if (!runId2 || !nodeId) {
        io.stderr("vh explain requires <node-id> and either <run-id> or an existing latest run.");
        return { exitCode: 2 };
      }
      const state = store.load(runId2);
      const node = state.nodes[nodeId];
      if (!node) {
        io.stderr(`Run "${runId2}" has no node "${nodeId}".`);
        return { exitCode: 1 };
      }
      emit(
        io,
        {
          runId: runId2,
          nodeId,
          purpose: node.definition.purpose,
          capability: node.definition.capability,
          state: node.state,
          dependencies: node.definition.dependencies,
          transport: node.definition.transport,
          effect: node.definition.effect,
          risk: node.definition.risk,
          authorization: node.definition.authorization,
          attempts: node.attempts,
          completion: node.definition.completion,
          evidenceArtifact: node.evidenceArtifact ?? null,
          error: node.error ?? null
        },
        true
      );
      return { exitCode: 0 };
    }
    if (command === "auth") {
      const [action, provider] = positional(rest);
      if (!["login", "status", "test", "revoke"].includes(action)) {
        io.stderr("Usage: vh auth login [provider] | status | test [provider] | revoke <provider>");
        return { exitCode: 2 };
      }
      if (action === "revoke" && !provider) {
        io.stderr("vh auth revoke requires <provider>.");
        return { exitCode: 2 };
      }
      if (!services.auth)
        return unsupported(
          io,
          `vh auth ${action}`,
          "configure the credential broker/provider adapter; credential values must never enter Git"
        );
      emit(
        io,
        await services.auth({
          action,
          provider,
          ref: flagValue(rest, "--ref"),
          backend: flagValue(rest, "--backend"),
          kind: flagValue(rest, "--kind"),
          scopes: flagValue(rest, "--scopes")?.split(",").map((scope) => scope.trim()).filter(Boolean)
        }),
        json2
      );
      return { exitCode: 0 };
    }
    if (command === "data" && positional(rest)[0] === "sync") {
      if (!services.dataSync)
        return unsupported(
          io,
          "vh data sync",
          "configure at least one read-only data connector and its credential reference"
        );
      emit(io, await services.dataSync(), json2);
      return { exitCode: 0 };
    }
    if (command === "learn") {
      const cadence = positional(rest)[0];
      if (!["daily", "weekly", "biweekly", "monthly"].includes(cadence)) {
        io.stderr("Usage: vh learn daily|weekly|biweekly|monthly");
        return { exitCode: 2 };
      }
      if (!services.learn)
        return unsupported(
          io,
          `vh learn ${cadence}`,
          "configure normalized data inputs and the learning-loop runner"
        );
      emit(io, await services.learn(cadence), json2);
      return { exitCode: 0 };
    }
    if (command === "upgrade") {
      if (!services.upgrade)
        return unsupported(
          io,
          "vh upgrade",
          "configure the harness release source and migration registry, then run vh upgrade --dry-run"
        );
      if (rest.includes("--release") && !flagValue(rest, "--release")) {
        io.stderr("Usage: vh upgrade [--release <local-root>] [--dry-run]");
        return { exitCode: 2 };
      }
      const report = await services.upgrade({
        dryRun: rest.includes("--dry-run"),
        releasePath: flagValue(rest, "--release")
      });
      emit(io, report, json2);
      const status = typeof report === "object" && report !== null && "status" in report ? report.status : void 0;
      return { exitCode: status === "failed" || status === "blocked" ? 1 : 0 };
    }
    io.stderr(`Unknown command "${command}".`);
    io.stderr("Next: run vh --help to see supported commands.");
    return { exitCode: 2 };
  } catch (error) {
    io.stderr(error instanceof Error ? error.message : String(error));
    return { exitCode: 1 };
  }
}

// packages/cli-generator/src/bin.ts
import { realpathSync as realpathSync10 } from "node:fs";
import { resolve as resolve25 } from "node:path";
import { fileURLToPath } from "node:url";

// packages/audit/dist/index.js
import { createHash as createHash18 } from "node:crypto";
function digest2(input, sequence, previousHash) {
  return createHash18("sha256").update(stableJson({ ...input, sequence, previousHash })).digest("hex");
}
var InMemoryAuditChain = class {
  durability = "fixture_only";
  #records = /* @__PURE__ */ new Map();
  append(input) {
    const key = tenantKey(input.tenant);
    const records = this.#records.get(key) ?? [];
    if (input.idempotencyKey) {
      const existing = records.find(({ idempotencyKey: idempotencyKey2 }) => idempotencyKey2 === input.idempotencyKey);
      if (existing)
        return structuredClone(existing);
    }
    const sequence = records.length + 1;
    const previousHash = records.at(-1)?.hash ?? "0".repeat(64);
    const record3 = {
      ...structuredClone(input),
      sequence,
      previousHash,
      hash: digest2(input, sequence, previousHash)
    };
    records.push(record3);
    this.#records.set(key, records);
    return structuredClone(record3);
  }
  read(tenant) {
    return (this.#records.get(tenantKey(tenant)) ?? []).map((record3) => structuredClone(record3));
  }
  verify(tenant) {
    let previousHash = "0".repeat(64);
    return this.read(tenant).every((record3, index) => {
      const { sequence, hash, previousHash: recordedPrevious, ...input } = record3;
      const valid = sequence === index + 1 && recordedPrevious === previousHash && hash === digest2(input, sequence, previousHash);
      previousHash = hash;
      return valid;
    });
  }
};

// packages/billing/dist/index.js
function assertActiveSubscription(subscription) {
  if (subscription.status !== "active" && subscription.status !== "trialing") {
    throw new Error(`subscription ${subscription.subscriptionId} is ${subscription.status}`);
  }
  return subscription;
}

// packages/command-bus/dist/index.js
import { createHash as createHash19, randomUUID } from "node:crypto";
function upperCamel(parts) {
  return parts.map((part) => `${part[0]?.toUpperCase() ?? ""}${part.slice(1)}`).join("");
}
function deriveCommandSurfaces(commandId, title) {
  const id = canonicalCommandId(commandId);
  const [namespace, method, ...rest] = id.split(".");
  if (!namespace || !method || rest.length)
    throw new Error(`Command ids must contain one namespace and method: ${id}`);
  const surfaces = {
    rest: {
      method: "POST",
      path: `/v1/commands/${id}`,
      operationId: `${namespace}${upperCamel([method])}`
    },
    cli: { tokens: [namespace, method] },
    mcp: { tool: `${namespace}_${method.replaceAll("-", "_")}` },
    sdk: { namespace, method },
    ui: { actionId: id, label: title }
  };
  return Object.freeze(surfaces);
}
function defineCommandContract(definition2) {
  const id = canonicalCommandId(definition2.id);
  if (!Number.isInteger(definition2.version) || definition2.version < 1)
    throw new Error("command version must be positive");
  return Object.freeze({
    ...definition2,
    id,
    effect: definition2.effect ?? "write",
    surfaces: deriveCommandSurfaces(id, definition2.title)
  });
}
function timestamp(value, label) {
  const parsed = Date.parse(value);
  if (!Number.isFinite(parsed))
    throw new Error(`${label} must be an ISO timestamp`);
  return parsed;
}
function nonEmpty(value, label) {
  const normalized = value.trim();
  if (!normalized)
    throw new Error(`${label} must not be empty`);
  return normalized;
}
var InMemoryIdempotencyStore = class {
  durability = "fixture_only";
  #entries = /* @__PURE__ */ new Map();
  #pendingTimeoutMs;
  constructor(options = {}) {
    this.#pendingTimeoutMs = options.pendingTimeoutMs ?? 5 * 6e4;
    if (!Number.isSafeInteger(this.#pendingTimeoutMs) || this.#pendingTimeoutMs < 1) {
      throw new Error("pendingTimeoutMs must be a positive safe integer");
    }
  }
  claim(key, input) {
    nonEmpty(key, "idempotency ledger key");
    nonEmpty(input.requestHash, "requestHash");
    nonEmpty(input.ownerToken, "ownerToken");
    const nowMs = timestamp(input.now, "claim now");
    const existing = this.#entries.get(key);
    if (!existing) {
      const pendingExpiresAt = new Date(nowMs + this.#pendingTimeoutMs).toISOString();
      this.#entries.set(key, {
        state: "pending",
        requestHash: input.requestHash,
        ownerToken: input.ownerToken,
        claimedAt: input.now,
        pendingExpiresAt
      });
      return {
        kind: "owner",
        ownerToken: input.ownerToken,
        claimedAt: input.now,
        pendingExpiresAt
      };
    }
    if (existing.requestHash !== input.requestHash) {
      return { kind: "conflict", existingRequestHash: existing.requestHash };
    }
    if (existing.state === "completed") {
      return {
        kind: "replay",
        record: {
          requestHash: existing.requestHash,
          output: structuredClone(existing.output),
          occurredAt: existing.occurredAt,
          actorId: existing.actorId,
          artifactsEmittedAt: existing.artifactsEmittedAt
        },
        completedAt: existing.completedAt
      };
    }
    if (existing.state === "ambiguous") {
      return {
        kind: "ambiguous",
        claimedAt: existing.claimedAt,
        ambiguousAt: existing.ambiguousAt
      };
    }
    if (nowMs >= timestamp(existing.pendingExpiresAt, "pending expiry")) {
      const ambiguous = {
        state: "ambiguous",
        requestHash: existing.requestHash,
        claimedAt: existing.claimedAt,
        ambiguousAt: input.now
      };
      this.#entries.set(key, ambiguous);
      return { kind: "ambiguous", claimedAt: ambiguous.claimedAt, ambiguousAt: input.now };
    }
    return {
      kind: "pending",
      claimedAt: existing.claimedAt,
      pendingExpiresAt: existing.pendingExpiresAt
    };
  }
  complete(key, value) {
    const existing = this.#entries.get(key);
    if (!existing)
      throw new Error("cannot complete an idempotency claim that does not exist");
    if (existing.requestHash !== value.requestHash) {
      throw new Error("cannot complete an idempotency claim bound to different input");
    }
    if (existing.state === "completed") {
      if (stableJson(existing.output) !== stableJson(value.output)) {
        throw new Error("completed idempotency output is immutable");
      }
      return;
    }
    if (existing.state === "ambiguous") {
      throw new Error("cannot complete an ambiguous idempotency claim");
    }
    if (existing.ownerToken !== value.ownerToken) {
      throw new Error("only the idempotency claim owner may complete it");
    }
    timestamp(value.completedAt, "completedAt");
    timestamp(value.occurredAt, "occurredAt");
    nonEmpty(value.actorId, "actorId");
    this.#entries.set(key, {
      state: "completed",
      requestHash: value.requestHash,
      output: structuredClone(value.output),
      completedAt: value.completedAt,
      occurredAt: value.occurredAt,
      actorId: value.actorId,
      artifactsEmittedAt: value.artifactsEmittedAt
    });
  }
  markAmbiguous(key, value) {
    const existing = this.#entries.get(key);
    if (!existing)
      throw new Error("cannot mark an idempotency claim that does not exist");
    if (existing.requestHash !== value.requestHash) {
      throw new Error("cannot mark an idempotency claim bound to different input");
    }
    if (existing.state === "completed" || existing.state === "ambiguous")
      return;
    if (existing.ownerToken !== value.ownerToken) {
      throw new Error("only the idempotency claim owner may mark it ambiguous");
    }
    timestamp(value.ambiguousAt, "ambiguousAt");
    this.#entries.set(key, {
      state: "ambiguous",
      requestHash: value.requestHash,
      claimedAt: existing.claimedAt,
      ambiguousAt: value.ambiguousAt
    });
  }
  markArtifactsEmitted(key, value) {
    timestamp(value.artifactsEmittedAt, "artifactsEmittedAt");
    const existing = this.#entries.get(key);
    if (!existing || existing.state !== "completed") {
      throw new Error("only a completed idempotency claim may finish its artifacts");
    }
    if (existing.requestHash !== value.requestHash) {
      throw new Error("cannot finish artifacts for an idempotency claim bound to different input");
    }
    if (existing.artifactsEmittedAt)
      return;
    this.#entries.set(key, { ...existing, artifactsEmittedAt: value.artifactsEmittedAt });
  }
  release(key, value) {
    const existing = this.#entries.get(key);
    if (!existing || existing.state !== "pending") {
      throw new Error("only a pending idempotency claim may be released");
    }
    if (existing.requestHash !== value.requestHash || existing.ownerToken !== value.ownerToken) {
      throw new Error("only the idempotency claim owner may release it");
    }
    this.#entries.delete(key);
  }
};
function commandRequestHash(contract, input) {
  const canonical = stableJson({
    commandId: contract.id,
    commandVersion: contract.version,
    input
  });
  return `sha256:${createHash19("sha256").update(canonical).digest("hex")}`;
}
var COMMAND_SECURITY_AUDIT_TENANT = Object.freeze({
  organizationId: "_venture_harness_security",
  ventureId: "command_bus"
});
var COMMAND_ERROR_SECRET_PATTERNS = [
  /-----BEGIN (?:RSA |EC |OPENSSH )?PRIVATE KEY-----[\s\S]*?(?:-----END|$)/giu,
  /\bBearer\s+[A-Za-z0-9._~+/=-]{8,}/giu,
  /\b(?:sk|rk)_(?:live|test)_[A-Za-z0-9_-]{8,}/giu,
  /\b(?:ghp|github_pat)_[A-Za-z0-9_]{8,}/giu
];
function sanitizeCommandFailureMessage(value) {
  return COMMAND_ERROR_SECRET_PATTERNS.reduce((safe, pattern) => safe.replace(pattern, "[REDACTED]"), value);
}
var COMMAND_BUS_ERROR_CODES = /* @__PURE__ */ new Set([
  "command_unknown",
  "command_duplicate",
  "authorization_denied",
  "idempotency_conflict",
  "idempotency_pending",
  "idempotency_ambiguous",
  "idempotency_store_unsafe",
  "evidence_sink_unsafe",
  "invalid_input",
  "invalid_output",
  "handler_failed"
]);
function commandBusErrorLike(error) {
  if (error instanceof CommandBusError)
    return error;
  if (!error || typeof error !== "object")
    return null;
  const candidate = error;
  if (candidate.name !== "CommandBusError" || typeof candidate.code !== "string" || !COMMAND_BUS_ERROR_CODES.has(candidate.code) || typeof candidate.message !== "string") {
    return null;
  }
  return { code: candidate.code, message: candidate.message };
}
function commandFailureEnvelope(error) {
  const classified2 = commandBusErrorLike(error);
  if (classified2) {
    return {
      error: "command_failed",
      code: classified2.code,
      message: sanitizeCommandFailureMessage(classified2.message)
    };
  }
  return {
    error: "command_failed",
    code: "internal_error",
    message: "Command execution failed without a classified result."
  };
}
var CommandBusError = class extends Error {
  code;
  constructor(message, code) {
    super(sanitizeCommandFailureMessage(message));
    this.code = code;
    this.name = "CommandBusError";
  }
};
var CommandDefinitiveNoEffectError = class extends Error {
  code;
  constructor(message, code = "handler_failed") {
    super(sanitizeCommandFailureMessage(message));
    this.code = code;
    this.name = "CommandDefinitiveNoEffectError";
  }
};
var CommandBus = class {
  #commands = /* @__PURE__ */ new Map();
  #hooks;
  #now;
  #executionMode;
  constructor(hooks, nowOrOptions = {}) {
    this.#hooks = { ...hooks, securityAudit: hooks.securityAudit ?? hooks.audit };
    this.#now = typeof nowOrOptions === "function" ? nowOrOptions : nowOrOptions.now ?? (() => /* @__PURE__ */ new Date());
    this.#executionMode = typeof nowOrOptions === "function" ? "production" : nowOrOptions.executionMode ?? "production";
  }
  register(contract, handler) {
    if (this.#commands.has(contract.id))
      throw new CommandBusError(`Command already registered: ${contract.id}`, "command_duplicate");
    this.#commands.set(contract.id, {
      contract,
      handler
    });
  }
  contracts() {
    return [...this.#commands.values()].map(({ contract }) => contract).sort((left, right) => left.id.localeCompare(right.id));
  }
  async execute(contract, input, options) {
    return await this.executeById(contract.id, input, options);
  }
  async executeById(commandId, input, options) {
    const registered = this.#commands.get(canonicalCommandId(commandId));
    if (!registered)
      throw new CommandBusError(`Unknown command: ${commandId}`, "command_unknown");
    const { contract, handler } = registered;
    if (this.#executionMode === "production" && contract.effect === "write") {
      if (this.#hooks.idempotency.durability !== "durable_atomic") {
        throw new CommandBusError(`Production command ${contract.id} requires a durable atomic idempotency store.`, "idempotency_store_unsafe");
      }
      const unsafeEvidence = [
        ["audit", this.#hooks.audit.durability],
        ["security audit", this.#hooks.securityAudit.durability],
        ["events", this.#hooks.events.durability],
        ["metering", this.#hooks.metering.durability]
      ].filter(([, durability]) => durability !== "durable_atomic").map(([name]) => name);
      if (unsafeEvidence.length > 0) {
        throw new CommandBusError(`Production command ${contract.id} requires durable atomic evidence sinks for: ${unsafeEvidence.join(", ")}.`, "evidence_sink_unsafe");
      }
    }
    const occurredAt = this.#now().toISOString();
    const details = {
      commandId: contract.id,
      commandVersion: contract.version,
      idempotencyKey: options.idempotencyKey
    };
    let tenantAuthorized = false;
    try {
      if (!options.idempotencyKey.trim())
        throw new Error("idempotencyKey must not be empty");
      await this.#hooks.identity(options.context);
      await this.#hooks.tenant(options.context);
      tenantAuthorized = true;
      await this.#hooks.audit.append({
        tenant: options.context.tenant,
        actorId: options.context.identity.actorId,
        action: contract.id,
        outcome: "requested",
        occurredAt,
        details
      });
      await this.#hooks.subscription(contract, options.context);
      await this.#hooks.entitlement(contract, options.context);
      await this.#hooks.grant(contract, options.context, new Date(occurredAt));
      await this.#hooks.scope(contract, options.context);
      let parsed;
      try {
        parsed = contract.input.parse(input);
      } catch (error) {
        throw new CommandBusError(error instanceof Error ? error.message : String(error), "invalid_input");
      }
      const requestHash2 = commandRequestHash(contract, parsed);
      const ledgerKey = `${tenantKey(options.context.tenant)}:${contract.id}:${options.idempotencyKey}`;
      const completionKey = `command-completion:${createHash19("sha256").update(ledgerKey).digest("hex")}`;
      const emitCompletionArtifacts = async (record3) => {
        await this.#hooks.events.append({
          eventId: `${completionKey}:event`,
          tenant: options.context.tenant,
          type: "command.succeeded",
          occurredAt: record3.occurredAt,
          payload: { commandId: contract.id, commandVersion: contract.version }
        });
        if (contract.meter)
          await this.#hooks.metering.record({
            idempotencyKey: `${completionKey}:meter`,
            tenant: options.context.tenant,
            commandId: contract.id,
            meter: contract.meter,
            quantity: 1,
            occurredAt: record3.occurredAt
          });
        await this.#hooks.audit.append({
          idempotencyKey: `${completionKey}:audit`,
          tenant: options.context.tenant,
          actorId: record3.actorId,
          action: contract.id,
          outcome: "succeeded",
          occurredAt: record3.occurredAt,
          details
        });
        await this.#hooks.idempotency.markArtifactsEmitted(ledgerKey, {
          requestHash: requestHash2,
          artifactsEmittedAt: this.#now().toISOString()
        });
      };
      const ownerToken = randomUUID();
      const claim = await this.#hooks.idempotency.claim(ledgerKey, {
        requestHash: requestHash2,
        ownerToken,
        now: occurredAt
      });
      if (claim.kind === "conflict") {
        throw new CommandBusError(`Idempotency key "${options.idempotencyKey}" is already bound to a different ${contract.id} request.`, "idempotency_conflict");
      }
      if (claim.kind === "replay") {
        let replayed;
        try {
          replayed = contract.output.parse(claim.record.output);
        } catch (error) {
          throw new CommandBusError(error instanceof Error ? error.message : String(error), "invalid_output");
        }
        if (!claim.record.artifactsEmittedAt) {
          try {
            await emitCompletionArtifacts(claim.record);
          } catch (error) {
            throw new CommandBusError(`Command ${contract.id} completed, but its replay-safe artifacts remain pending: ${error instanceof Error ? error.message : String(error)}`, "idempotency_pending");
          }
        }
        return replayed;
      }
      if (claim.kind === "pending") {
        throw new CommandBusError(`Idempotency key "${options.idempotencyKey}" is already executing; retry after ${claim.pendingExpiresAt}.`, "idempotency_pending");
      }
      if (claim.kind === "ambiguous") {
        throw new CommandBusError(`Idempotency key "${options.idempotencyKey}" has an ambiguous outcome and requires reconciliation before retry.`, "idempotency_ambiguous");
      }
      let verified;
      const completionRecord = {
        requestHash: requestHash2,
        output: null,
        occurredAt,
        actorId: options.context.identity.actorId,
        artifactsEmittedAt: null
      };
      try {
        const output = await handler(parsed, { ...options, commandId: contract.id, occurredAt });
        verified = contract.output.parse(output);
        completionRecord.output = verified;
        await this.#hooks.idempotency.complete(ledgerKey, {
          ...completionRecord,
          ownerToken: claim.ownerToken,
          completedAt: this.#now().toISOString()
        });
      } catch (error) {
        if (contract.effect === "read" || error instanceof CommandDefinitiveNoEffectError) {
          await this.#hooks.idempotency.release(ledgerKey, {
            requestHash: requestHash2,
            ownerToken: claim.ownerToken
          });
          if (error instanceof CommandBusError)
            throw error;
          if (error instanceof CommandDefinitiveNoEffectError) {
            throw new CommandBusError(error.message, error.code);
          }
          throw new CommandBusError(error instanceof Error ? error.message : String(error), "handler_failed");
        }
        try {
          await this.#hooks.idempotency.markAmbiguous(ledgerKey, {
            requestHash: requestHash2,
            ownerToken: claim.ownerToken,
            ambiguousAt: this.#now().toISOString()
          });
        } catch {
        }
        if (error instanceof CommandBusError)
          throw error;
        throw new CommandBusError(`Idempotency key "${options.idempotencyKey}" has an ambiguous outcome and requires reconciliation before retry.`, "idempotency_ambiguous");
      }
      try {
        await emitCompletionArtifacts(completionRecord);
      } catch (error) {
        throw new CommandBusError(`Command ${contract.id} completed, but its replay-safe artifacts remain pending: ${error instanceof Error ? error.message : String(error)}`, "idempotency_pending");
      }
      return verified;
    } catch (error) {
      const errorCode = error instanceof CommandBusError ? error.code : "authorization_denied";
      const denialDetails = tenantAuthorized ? { ...details, errorCode } : {
        commandId: contract.id,
        commandVersion: contract.version,
        errorCode
      };
      await (tenantAuthorized ? this.#hooks.audit : this.#hooks.securityAudit).append({
        tenant: tenantAuthorized ? options.context.tenant : COMMAND_SECURITY_AUDIT_TENANT,
        actorId: options.context.identity.actorId,
        action: contract.id,
        outcome: error instanceof CommandBusError && (error.code === "invalid_input" || error.code === "invalid_output" || error.code.startsWith("idempotency_")) ? "failed" : "denied",
        occurredAt,
        details: denialDetails
      });
      if (error instanceof CommandBusError)
        throw error;
      throw new CommandBusError(error instanceof Error ? error.message : String(error), "authorization_denied");
    }
  }
};

// packages/connections/dist/index.js
function selectCommandGrant(grants, commandId, requiredScopes, now = /* @__PURE__ */ new Date()) {
  const grant = grants.find((candidate) => !candidate.revokedAt && Date.parse(candidate.expiresAt) > now.getTime() && (candidate.commandIds.includes(commandId) || candidate.commandIds.includes("*")) && requiredScopes.every((scope) => candidate.scopes.includes(scope)));
  if (!grant)
    throw new Error(`no active grant authorizes ${commandId}`);
  return grant;
}

// packages/entitlements/dist/index.js
function assertEntitlements(actual, required) {
  const missing = required.filter((entitlement) => !actual.includes(entitlement));
  if (missing.length)
    throw new Error(`missing entitlements: ${missing.join(", ")}`);
  return actual;
}

// packages/events/dist/index.js
var InMemoryEventLog = class {
  durability = "fixture_only";
  #events = [];
  append(event) {
    if (this.#events.some(({ eventId }) => eventId === event.eventId))
      return;
    this.#events.push(structuredClone(event));
  }
  read(tenant) {
    const key = tenantKey(tenant);
    return this.#events.filter((event) => tenantKey(event.tenant) === key).map((event) => structuredClone(event));
  }
};

// packages/organizations/dist/index.js
function assertOrganizationMembership(identity, tenant, memberships) {
  const membership = memberships.find((candidate) => candidate.active && candidate.actorId === identity.actorId && candidate.organizationId === tenant.organizationId);
  if (!membership)
    throw new Error("identity is not an active member of the tenant organization");
  return membership;
}

// packages/policy/dist/index.js
function decideScopes(context2, requiredScopes) {
  const missing = requiredScopes.filter((scope) => !context2.scopes.includes(scope));
  return missing.length === 0 ? { allowed: true, reason: "all declared scopes are present" } : { allowed: false, reason: `missing actor scopes: ${missing.join(", ")}` };
}

// packages/telemetry/dist/index.js
var InMemoryMeteringSink = class {
  durability = "fixture_only";
  #records = [];
  record(input) {
    if (input.idempotencyKey && this.#records.some(({ idempotencyKey: idempotencyKey2 }) => idempotencyKey2 === input.idempotencyKey)) {
      return;
    }
    this.#records.push(structuredClone(input));
  }
  read(tenant) {
    const key = tenantKey(tenant);
    return this.#records.filter((record3) => tenantKey(record3.tenant) === key).map((record3) => structuredClone(record3));
  }
};

// packages/config/dist/growth-contract.js
var providerIdSchema2 = external_exports.string().regex(/^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*$/, "expected a stable provider ID");
var extensionsSchema2 = external_exports.record(external_exports.unknown()).default({});
function uniqueArray2(item) {
  return external_exports.array(item).refine((values) => new Set(values).size === values.length, {
    message: "values must be unique"
  });
}
var minorUnits = external_exports.number().int().nonnegative();
var ratio = external_exports.number().min(0).max(1);
var currencyCode = external_exports.string().regex(/^[A-Z]{3}$/, "expected an ISO 4217 currency code");
var GROWTH_CONTRACT_VERSION = 2;
var optimizationEventSchema = external_exports.enum([
  "install",
  "onboarding_complete",
  "paywall_view",
  "trial_start",
  "purchase",
  "subscription_active",
  "value"
]);
var goalSchema = external_exports.object({
  primary_event: optimizationEventSchema,
  secondary_events: uniqueArray2(optimizationEventSchema).default([]),
  current_optimization_event: optimizationEventSchema,
  allowed_fallback_events: uniqueArray2(optimizationEventSchema).default([])
}).strict();
var economicsSchema = external_exports.object({
  currency: currencyCode,
  subscription_price_minor: minorUnits,
  billing_period: external_exports.enum(["weekly", "monthly", "quarterly", "annual"]),
  store_fee_rate: ratio,
  tax_rate: ratio,
  refund_rate: ratio,
  variable_serving_cost_minor: minorUnits,
  creative_generation_cost_minor: minorUnits,
  expected_subscriber_lifetime_months: external_exports.number().positive(),
  target_cac_minor: minorUnits,
  hard_max_cac_minor: minorUnits,
  payback_target_days: external_exports.number().int().positive(),
  minimum_contribution_margin_minor: minorUnits,
  d7_retention_floor: ratio,
  d30_retention_floor: ratio,
  d90_retention_floor: ratio,
  refund_rate_ceiling: ratio
}).strict().refine((value) => value.hard_max_cac_minor >= value.target_cac_minor, {
  message: "hard_max_cac_minor must be at least target_cac_minor",
  path: ["hard_max_cac_minor"]
});
var organicSchema = external_exports.object({
  allowed_providers: uniqueArray2(providerIdSchema2),
  allowed_accounts: uniqueArray2(external_exports.string().min(1)),
  max_accounts: external_exports.number().int().positive(),
  max_posts_per_account_per_day: external_exports.number().int().positive(),
  duplicate_content_policy: external_exports.enum(["forbid", "allow_across_accounts", "allow_with_variation"]),
  default_review_mode: external_exports.enum(["AUTOMATIC_WITHIN_POLICY", "REVIEW_BEFORE_PUBLISH", "PLATFORM_DRAFT"]).default("REVIEW_BEFORE_PUBLISH"),
  snapshot_cadence_minutes: uniqueArray2(external_exports.number().int().positive()),
  ai_disclosure_required: external_exports.boolean().default(true)
}).strict();
var paidSchema = external_exports.object({
  allowed_networks: uniqueArray2(external_exports.enum(["tiktok_paid", "meta_paid"])),
  allowed_accounts: uniqueArray2(external_exports.string().min(1)),
  allowed_objectives: uniqueArray2(external_exports.string().min(1)),
  allowed_events: uniqueArray2(optimizationEventSchema),
  test_budget_minor: minorUnits,
  per_creative_cap_minor: minorUnits,
  daily_account_cap_minor: minorUnits,
  daily_venture_cap_minor: minorUnits,
  monthly_venture_cap_minor: minorUnits,
  daily_customer_cap_minor: minorUnits,
  monthly_customer_cap_minor: minorUnits,
  emergency_platform_cap_minor: minorUnits,
  approval_threshold_minor: minorUnits.default(0),
  auto_pause_allowed: external_exports.boolean().default(true),
  auto_scale_allowed: external_exports.literal(false).default(false),
  vbo_policy: external_exports.enum(["forbidden", "requires_value_ready", "allowed"]).default("forbidden"),
  stop_conditions: external_exports.object({
    max_spend_without_trial_minor: minorUnits,
    max_spend_without_purchase_minor: minorUnits,
    max_cac_breach_count: external_exports.number().int().nonnegative()
  }).strict()
}).strict().refine((value) => value.test_budget_minor >= value.per_creative_cap_minor, {
  message: "test_budget_minor must be at least per_creative_cap_minor",
  path: ["test_budget_minor"]
});
var complianceSchema = external_exports.object({
  rights_required: external_exports.boolean().default(true),
  ai_disclosure_required: external_exports.boolean().default(true),
  prohibited_claims: uniqueArray2(external_exports.string().min(1)).default([]),
  allowed_geographies: uniqueArray2(external_exports.string().min(2)),
  restricted_audiences: uniqueArray2(external_exports.string().min(1)).default([]),
  restricted_categories: uniqueArray2(external_exports.string().min(1)).default([]),
  provider_policy_state: external_exports.enum(["unknown", "clear", "warned", "restricted"]).default("unknown")
}).strict();
var growthContractSchema = external_exports.object({
  contract_version: external_exports.literal(GROWTH_CONTRACT_VERSION),
  venture_id: external_exports.string().min(1),
  goal: goalSchema,
  economics: economicsSchema,
  organic: organicSchema,
  paid: paidSchema,
  compliance: complianceSchema,
  extensions: extensionsSchema2
}).strict();
function migrateGrowthContract(value) {
  const current = growthContractSchema.safeParse(value);
  if (current.success)
    return current.data;
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    throw current.error;
  }
  const legacy = value;
  const legacyPaid = legacy.paid;
  if (legacy.contract_version !== 1 || !legacyPaid || typeof legacyPaid !== "object" || Array.isArray(legacyPaid)) {
    throw current.error;
  }
  const paidRecord = legacyPaid;
  const legacyBudget = paidRecord.per_creative_test_budget_minor;
  const paidWithoutLegacyBudget = { ...paidRecord };
  delete paidWithoutLegacyBudget.per_creative_test_budget_minor;
  return growthContractSchema.parse({
    ...legacy,
    contract_version: GROWTH_CONTRACT_VERSION,
    paid: {
      ...paidWithoutLegacyBudget,
      test_budget_minor: legacyBudget,
      per_creative_cap_minor: legacyBudget
    }
  });
}
function parseGrowthContract(value) {
  return migrateGrowthContract(value);
}

// packages/config/dist/index.js
function defineRuntimeSchema(schema) {
  return Object.freeze(schema);
}
function objectValue2(value, name) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    throw new Error(`${name} must be an object`);
  }
  return value;
}
function stringValue(record3, field, options = {}) {
  const value = record3[field];
  if (value === void 0 && options.optional)
    return void 0;
  if (typeof value !== "string" || value.trim() === "")
    throw new Error(`${field} must be a string`);
  if (options.allowed && !options.allowed.includes(value)) {
    throw new Error(`${field} must be one of ${options.allowed.join(", ")}`);
  }
  return value;
}
function schemaObject(properties, required) {
  return {
    type: "object",
    additionalProperties: false,
    properties,
    required
  };
}

// packages/agent-runtime/dist/contracts.js
var campaignInput = defineRuntimeSchema({
  name: "CampaignLaunchInput",
  jsonSchema: schemaObject({
    campaignId: { type: "string", minLength: 1 },
    channel: { type: "string", enum: ["organic", "paid"] },
    objective: { type: "string", minLength: 1 }
  }, ["campaignId", "channel", "objective"]),
  parse(value) {
    const input = objectValue2(value, "CampaignLaunchInput");
    return {
      campaignId: stringValue(input, "campaignId"),
      channel: stringValue(input, "channel", { allowed: ["organic", "paid"] }),
      objective: stringValue(input, "objective")
    };
  }
});
var campaignOutput = defineRuntimeSchema({
  name: "CampaignLaunchOutput",
  jsonSchema: schemaObject({
    commandId: { const: "campaigns.launch" },
    ventureId: { type: "string" },
    campaignId: { type: "string" },
    channel: { type: "string", enum: ["organic", "paid"] },
    status: { const: "planned" }
  }, ["commandId", "ventureId", "campaignId", "channel", "status"]),
  parse(value) {
    const output = objectValue2(value, "CampaignLaunchOutput");
    if (output.commandId !== "campaigns.launch" || output.status !== "planned")
      throw new Error("invalid campaign launch output");
    return {
      commandId: "campaigns.launch",
      ventureId: stringValue(output, "ventureId"),
      campaignId: stringValue(output, "campaignId"),
      channel: stringValue(output, "channel", { allowed: ["organic", "paid"] }),
      status: "planned"
    };
  }
});
var launchInput = defineRuntimeSchema({
  name: "LaunchExecuteInput",
  jsonSchema: schemaObject({
    launchId: { type: "string", minLength: 1 },
    mode: { type: "string", enum: ["preview", "production"] },
    dryRun: { type: "boolean" }
  }, ["launchId", "mode", "dryRun"]),
  parse(value) {
    const input = objectValue2(value, "LaunchExecuteInput");
    if (typeof input.dryRun !== "boolean")
      throw new Error("dryRun must be a boolean");
    return {
      launchId: stringValue(input, "launchId"),
      mode: stringValue(input, "mode", { allowed: ["preview", "production"] }),
      dryRun: input.dryRun
    };
  }
});
var launchOutput = defineRuntimeSchema({
  name: "LaunchExecuteOutput",
  jsonSchema: schemaObject({
    commandId: { const: "launch.execute" },
    ventureId: { type: "string" },
    runId: { type: "string" },
    mode: { type: "string", enum: ["preview", "production"] },
    status: { const: "accepted" },
    dryRun: { type: "boolean" }
  }, ["commandId", "ventureId", "runId", "mode", "status", "dryRun"]),
  parse(value) {
    const output = objectValue2(value, "LaunchExecuteOutput");
    if (output.commandId !== "launch.execute" || output.status !== "accepted" || typeof output.dryRun !== "boolean") {
      throw new Error("invalid launch execution output");
    }
    return {
      commandId: "launch.execute",
      ventureId: stringValue(output, "ventureId"),
      runId: stringValue(output, "runId"),
      mode: stringValue(output, "mode", { allowed: ["preview", "production"] }),
      status: "accepted",
      dryRun: output.dryRun
    };
  }
});
var campaignLaunchCommand = defineCommandContract({
  id: "campaigns.launch",
  version: 1,
  title: "Launch Campaign",
  description: "Plan one venture campaign through its declared channel.",
  input: campaignInput,
  output: campaignOutput,
  requirements: {
    activeSubscription: true,
    entitlements: ["campaigns.launch"],
    grant: true,
    scopes: ["campaigns:write"]
  },
  meter: "campaign_launches"
});
var launchExecuteCommand = defineCommandContract({
  id: "launch.execute",
  version: 1,
  title: "Execute Venture Launch",
  description: "Accept one authorized preview or production launch run.",
  input: launchInput,
  output: launchOutput,
  requirements: {
    activeSubscription: true,
    entitlements: ["launch.execute"],
    grant: true,
    scopes: ["launch:execute"]
  },
  meter: "launch_runs"
});

// packages/agent-runtime/dist/operational.js
import { createHash as createHash21, randomUUID as randomUUID2 } from "node:crypto";
import { closeSync as closeSync4, constants as constants2, existsSync as existsSync13, fstatSync as fstatSync2, fsyncSync as fsyncSync3, lstatSync as lstatSync8, mkdirSync as mkdirSync9, openSync as openSync4, readFileSync as readFileSync15, realpathSync as realpathSync7, renameSync as renameSync7, writeFileSync as writeFileSync9 } from "node:fs";
import { dirname as dirname12, isAbsolute as isAbsolute7, join as join7, relative as relative14, resolve as resolve22, sep as sep14 } from "node:path";
import { parse as parseYaml2 } from "yaml";

// packages/agent-runtime/dist/quality.js
var QUALITY_PROFILE_IDS = ["fast", "mvp", "release"];
var unconfiguredQualityProfileRunner = Object.freeze({
  async run(profile2) {
    return {
      profile: profile2,
      status: "INCOMPLETE",
      exitCode: 1,
      summary: { PASS: 0, FAIL: 0, SKIP: 1, NOT_APPLICABLE: 0 },
      command: [],
      stdout: "",
      stderr: "No repository quality-profile runner is configured; no verification command ran.",
      reportPath: null
    };
  }
});

// packages/agent-runtime/dist/platform-operations.js
var SAFE_ID = /^[A-Za-z0-9][A-Za-z0-9._:-]{0,254}$/u;
var SAFE_SCOPE = /^[A-Za-z0-9][A-Za-z0-9._:/*-]{0,254}$/u;
var CREDENTIAL_REF = /^cred:\/\/[A-Za-z0-9][A-Za-z0-9._/-]*$/u;
var SECRET_VALUE = /(?:\bbearer\s+[a-z0-9._~+/=-]{8,}|-----BEGIN [A-Z ]*PRIVATE KEY-----|\b(?:sk|rk|ghp|github_pat)_(?:live|test)?_?[a-z0-9_-]{8,})/iu;
function exactObject(value, name, allowed) {
  const record3 = objectValue2(value, name);
  const unexpected = Object.keys(record3).filter((key) => !allowed.includes(key));
  if (unexpected.length > 0) {
    throw new Error(`${name} has unsupported field(s): ${unexpected.join(", ")}`);
  }
  return record3;
}
function safeId(record3, field) {
  const value = stringValue(record3, field);
  if (value !== value.trim() || !SAFE_ID.test(value)) {
    throw new Error(`${field} must be a canonical identifier of at most 255 characters`);
  }
  return value;
}
function optionalSafeId(record3, field) {
  if (record3[field] === void 0)
    return void 0;
  return safeId(record3, field);
}
function credentialRef(record3, optional = false) {
  const value = stringValue(record3, "credentialRef", { optional });
  if (value === void 0)
    return void 0;
  if (!CREDENTIAL_REF.test(value)) {
    throw new Error("credentialRef must be a metadata-only cred:// reference");
  }
  return value;
}
function safeStringArray(value, field, pattern) {
  if (!Array.isArray(value) || value.some((item) => typeof item !== "string")) {
    throw new Error(`${field} must be an array of strings`);
  }
  const parsed = value.map((item) => item.trim());
  if (parsed.some((item) => !pattern.test(item))) {
    throw new Error(`${field} contains an invalid identifier`);
  }
  if (new Set(parsed).size !== parsed.length)
    throw new Error(`${field} must not contain duplicates`);
  return parsed;
}
function assertJsonSafe(value, path) {
  if (Array.isArray(value)) {
    value.forEach((item, index) => assertJsonSafe(item, `${path}[${index}]`));
    return;
  }
  if (value !== null && typeof value === "object") {
    for (const [key, item] of Object.entries(value)) {
      if (/^(?:password|secret|accessToken|refreshToken|apiKey|privateKey)$/iu.test(key)) {
        throw new Error(`secret-bearing output field ${path}.${key} is forbidden`);
      }
      assertJsonSafe(item, `${path}.${key}`);
    }
    return;
  }
  if (typeof value === "function" || typeof value === "symbol" || typeof value === "bigint") {
    throw new Error(`${path} must be JSON serializable`);
  }
  if (typeof value === "string" && SECRET_VALUE.test(value)) {
    throw new Error(`credential-like value is forbidden at ${path}`);
  }
}
var authLoginInput = defineRuntimeSchema({
  name: "AuthLoginInput",
  jsonSchema: schemaObject({
    providerId: { type: "string", minLength: 1, maxLength: 255 },
    credentialRef: { type: "string", pattern: "^cred://" },
    backend: { type: "string", minLength: 1, maxLength: 255 },
    kind: { type: "string", minLength: 1, maxLength: 255 },
    scopes: { type: "array", items: { type: "string" }, default: [] }
  }, ["providerId"]),
  parse(value) {
    const input = exactObject(value, "AuthLoginInput", [
      "providerId",
      "credentialRef",
      "backend",
      "kind",
      "scopes"
    ]);
    const parsed = {
      providerId: safeId(input, "providerId"),
      ...credentialRef(input, true) ? { credentialRef: credentialRef(input, true) } : {},
      ...optionalSafeId(input, "backend") ? { backend: optionalSafeId(input, "backend") } : {},
      ...optionalSafeId(input, "kind") ? { kind: optionalSafeId(input, "kind") } : {},
      scopes: input.scopes === void 0 ? [] : safeStringArray(input.scopes, "scopes", SAFE_SCOPE)
    };
    assertJsonSafe(parsed, "auth.login");
    return parsed;
  }
});
var authInspectInput = defineRuntimeSchema({
  name: "AuthInspectInput",
  jsonSchema: schemaObject({
    providerId: { type: "string", minLength: 1, maxLength: 255 },
    credentialRef: { type: "string", pattern: "^cred://" }
  }, []),
  parse(value) {
    const input = exactObject(value, "AuthInspectInput", ["providerId", "credentialRef"]);
    return {
      ...optionalSafeId(input, "providerId") ? { providerId: optionalSafeId(input, "providerId") } : {},
      ...credentialRef(input, true) ? { credentialRef: credentialRef(input, true) } : {}
    };
  }
});
var upgradeReleaseInput = defineRuntimeSchema({
  name: "UpgradeReleaseInput",
  jsonSchema: schemaObject({ releaseLocator: { type: "string", minLength: 1, maxLength: 4096 } }, [
    "releaseLocator"
  ]),
  parse(value) {
    const input = exactObject(value, "UpgradeReleaseInput", ["releaseLocator"]);
    const releaseLocator = stringValue(input, "releaseLocator").trim();
    if (releaseLocator.length > 4096 || releaseLocator.includes("\0")) {
      throw new Error("releaseLocator must be a local path of at most 4096 characters");
    }
    if (/^[a-z][a-z0-9+.-]*:\/\//iu.test(releaseLocator)) {
      throw new Error("releaseLocator must be a trusted local filesystem path, not a URL");
    }
    assertJsonSafe(releaseLocator, "releaseLocator");
    return { releaseLocator };
  }
});
var emptyPlatformInput = defineRuntimeSchema({
  name: "EmptyPlatformInput",
  jsonSchema: schemaObject({}, []),
  parse(value) {
    exactObject(value, "EmptyPlatformInput", []);
    return {};
  }
});
var fleetStatusInput = defineRuntimeSchema({
  name: "FleetStatusInput",
  jsonSchema: schemaObject({ runId: { type: "string", minLength: 1, maxLength: 255 } }, []),
  parse(value) {
    const input = exactObject(value, "FleetStatusInput", ["runId"]);
    const runId2 = optionalSafeId(input, "runId");
    if (runId2)
      return { runId: runId2 };
    return {};
  }
});
var fleetOperationInput = defineRuntimeSchema({
  name: "FleetOperationInput",
  jsonSchema: schemaObject({
    runId: { type: "string", minLength: 1, maxLength: 255 },
    releaseId: { type: "string", minLength: 1, maxLength: 255 },
    ventureIds: { type: "array", items: { type: "string" }, minItems: 1 },
    batchSize: { type: "integer", minimum: 1 }
  }, ["runId", "releaseId", "ventureIds", "batchSize"]),
  parse(value) {
    const input = exactObject(value, "FleetOperationInput", [
      "runId",
      "releaseId",
      "ventureIds",
      "batchSize"
    ]);
    if (!Number.isSafeInteger(input.batchSize) || Number(input.batchSize) < 1) {
      throw new Error("batchSize must be a positive safe integer");
    }
    return {
      runId: safeId(input, "runId"),
      releaseId: safeId(input, "releaseId"),
      ventureIds: safeStringArray(input.ventureIds, "ventureIds", SAFE_ID),
      batchSize: Number(input.batchSize)
    };
  }
});
function outputSchema(commandId) {
  return defineRuntimeSchema({
    name: `${commandId}Output`,
    jsonSchema: schemaObject({
      commandId: { const: commandId },
      mode: { type: "string", enum: ["read_only", "local_write", "external_write"] },
      status: { type: "string", minLength: 1, maxLength: 255 },
      data: { type: "object" }
    }, ["commandId", "mode", "status", "data"]),
    parse(value) {
      const output = exactObject(value, `${commandId}Output`, [
        "commandId",
        "mode",
        "status",
        "data"
      ]);
      if (output.commandId !== commandId)
        throw new Error(`invalid ${commandId} output`);
      const mode = stringValue(output, "mode", {
        allowed: ["read_only", "local_write", "external_write"]
      });
      const data = objectValue2(output.data, "data");
      assertJsonSafe(data, `${commandId}.data`);
      return { commandId, mode, status: safeId(output, "status"), data };
    }
  });
}
function platformCommand(definition2) {
  return defineCommandContract({
    ...definition2,
    version: 1,
    output: outputSchema(definition2.id),
    requirements: {
      activeSubscription: false,
      entitlements: [],
      grant: true,
      scopes: definition2.scopes
    }
  });
}
var authLoginCommand = platformCommand({
  id: "auth.login",
  title: "Authenticate Provider",
  description: "Use an explicitly injected official login adapter and persist metadata-only credential references.",
  input: authLoginInput,
  effect: "write",
  scopes: ["auth.manage"],
  meter: "auth_mutations"
});
var authStatusCommand = platformCommand({
  id: "auth.status",
  title: "Inspect Authentication",
  description: "Inspect credential-reference state without exposing credential values.",
  input: authInspectInput,
  effect: "read",
  scopes: ["auth.read"]
});
var authTestCommand = platformCommand({
  id: "auth.test",
  title: "Test Authentication",
  description: "Run an injected official read check and persist sanitized test evidence.",
  input: authInspectInput,
  effect: "write",
  scopes: ["auth.test"],
  meter: "auth_tests"
});
var authRevokeCommand = platformCommand({
  id: "auth.revoke",
  title: "Revoke Authentication",
  description: "Disable a credential reference locally and invoke only an injected revoke adapter.",
  input: authInspectInput,
  effect: "write",
  scopes: ["auth.manage"],
  meter: "auth_mutations"
});
var upgradePlanCommand = platformCommand({
  id: "upgrade.plan",
  title: "Plan Harness Upgrade",
  description: "Plan a trusted local release without changing the project.",
  input: upgradeReleaseInput,
  effect: "read",
  scopes: ["upgrade.read"]
});
var upgradeDryRunCommand = platformCommand({
  id: "upgrade.dry-run",
  title: "Dry Run Harness Upgrade",
  description: "Validate a trusted local release and report its reversible local changes.",
  input: upgradeReleaseInput,
  effect: "read",
  scopes: ["upgrade.read"]
});
var upgradeApplyCommand = platformCommand({
  id: "upgrade.apply",
  title: "Apply Harness Upgrade",
  description: "Apply one trusted local release through the host upgrade transaction and checks.",
  input: upgradeReleaseInput,
  effect: "write",
  scopes: ["upgrade.apply"],
  meter: "upgrade_applies"
});
var upgradeStatusCommand = platformCommand({
  id: "upgrade.status",
  title: "Inspect Harness Version",
  description: "Inspect the current local harness lock without locating or applying a release.",
  input: emptyPlatformInput,
  effect: "read",
  scopes: ["upgrade.read"]
});
var fleetStatusCommand = platformCommand({
  id: "fleet.status",
  title: "Inspect Fleet Run",
  description: "Read a durable Fleet run or the sanitized configured target catalog.",
  input: fleetStatusInput,
  effect: "read",
  scopes: ["fleet.read"]
});
var fleetPlanCommand = platformCommand({
  id: "fleet.plan",
  title: "Plan Fleet Rollout",
  description: "Resolve an exact tenant-bound release and Fleet selection without running hooks.",
  input: fleetOperationInput,
  effect: "read",
  scopes: ["fleet.read"]
});
var fleetRolloutCommand = platformCommand({
  id: "fleet.rollout",
  title: "Roll Out Fleet Release",
  description: "Run the durable canary and batch Fleet controller through injected venture hooks.",
  input: fleetOperationInput,
  effect: "write",
  scopes: ["fleet.rollout"],
  meter: "fleet_rollouts"
});
var fleetResumeCommand = platformCommand({
  id: "fleet.resume",
  title: "Resume Fleet Rollout",
  description: "Resume one durable Fleet run without repeating completed phase effects.",
  input: fleetOperationInput,
  effect: "write",
  scopes: ["fleet.rollout"],
  meter: "fleet_rollouts"
});
var authCommandContracts = [
  authLoginCommand,
  authStatusCommand,
  authTestCommand,
  authRevokeCommand
];
var upgradeCommandContracts = [
  upgradePlanCommand,
  upgradeDryRunCommand,
  upgradeApplyCommand,
  upgradeStatusCommand
];
var fleetCommandContracts = [
  fleetStatusCommand,
  fleetPlanCommand,
  fleetRolloutCommand,
  fleetResumeCommand
];
var platformOperationCommandContracts = [
  ...authCommandContracts,
  ...upgradeCommandContracts,
  ...fleetCommandContracts
];
function unconfigured(domain, action) {
  return {
    status: "unconfigured",
    effect: "none",
    data: {
      diagnostic: {
        code: `${domain}_runtime_unconfigured`,
        message: `No trusted ${domain} runtime is configured for ${action}`,
        nextAction: `Load an explicit project-owned production runtime module with a ${domain} binding`
      }
    }
  };
}
var unconfiguredAuthCommandRuntime = Object.freeze({
  execute: (action) => unconfigured("auth", action)
});
var unconfiguredUpgradeCommandRuntime = Object.freeze({
  execute: (action) => unconfigured("upgrade", action)
});
var unconfiguredFleetCommandRuntime = Object.freeze({
  execute: (action) => unconfigured("fleet", action)
});
var FAILURE_STATUSES = /* @__PURE__ */ new Set(["unconfigured", "context_unavailable", "blocked", "failed"]);
function failureMessage(boundary, fallback) {
  const diagnostic = boundary.data.diagnostic;
  if (diagnostic && typeof diagnostic === "object" && !Array.isArray(diagnostic)) {
    const record3 = diagnostic;
    const code = typeof record3.code === "string" ? record3.code : "platform_command_failed";
    const message = typeof record3.message === "string" ? record3.message : fallback;
    return `${code}: ${message}`;
  }
  return fallback;
}
function register(options) {
  options.bus.register(options.contract, async (input, context2) => {
    const boundary = await options.invoke(options.action, input, context2);
    assertJsonSafe(boundary.data, `${options.contract.id}.boundary`);
    if (FAILURE_STATUSES.has(boundary.status)) {
      const message = failureMessage(boundary, `${options.contract.id} did not complete successfully`);
      if (boundary.effect === "none") {
        throw new CommandDefinitiveNoEffectError(message, "handler_failed");
      }
      throw new Error(message);
    }
    return {
      commandId: options.contract.id,
      mode: options.mode,
      status: boundary.status,
      data: boundary.data
    };
  });
}
function registerPlatformOperationCommands(bus, runtimes = {}) {
  const auth = runtimes.auth ?? unconfiguredAuthCommandRuntime;
  const upgrade = runtimes.upgrade ?? unconfiguredUpgradeCommandRuntime;
  const fleet = runtimes.fleet ?? unconfiguredFleetCommandRuntime;
  register({
    bus,
    contract: authLoginCommand,
    action: "login",
    mode: "external_write",
    invoke: auth.execute.bind(auth)
  });
  register({
    bus,
    contract: authStatusCommand,
    action: "status",
    mode: "read_only",
    invoke: auth.execute.bind(auth)
  });
  register({
    bus,
    contract: authTestCommand,
    action: "test",
    mode: "local_write",
    invoke: auth.execute.bind(auth)
  });
  register({
    bus,
    contract: authRevokeCommand,
    action: "revoke",
    mode: "external_write",
    invoke: auth.execute.bind(auth)
  });
  register({
    bus,
    contract: upgradePlanCommand,
    action: "plan",
    mode: "read_only",
    invoke: upgrade.execute.bind(upgrade)
  });
  register({
    bus,
    contract: upgradeDryRunCommand,
    action: "dry_run",
    mode: "read_only",
    invoke: upgrade.execute.bind(upgrade)
  });
  register({
    bus,
    contract: upgradeApplyCommand,
    action: "apply",
    mode: "local_write",
    invoke: upgrade.execute.bind(upgrade)
  });
  register({
    bus,
    contract: upgradeStatusCommand,
    action: "status",
    mode: "read_only",
    invoke: upgrade.execute.bind(upgrade)
  });
  register({
    bus,
    contract: fleetStatusCommand,
    action: "status",
    mode: "read_only",
    invoke: fleet.execute.bind(fleet)
  });
  register({
    bus,
    contract: fleetPlanCommand,
    action: "plan",
    mode: "read_only",
    invoke: fleet.execute.bind(fleet)
  });
  register({
    bus,
    contract: fleetRolloutCommand,
    action: "rollout",
    mode: "external_write",
    invoke: fleet.execute.bind(fleet)
  });
  register({
    bus,
    contract: fleetResumeCommand,
    action: "resume",
    mode: "external_write",
    invoke: fleet.execute.bind(fleet)
  });
}

// packages/agent-runtime/dist/loop-operations.js
import { createHash as createHash20 } from "node:crypto";
var LEARNING_CADENCE_LOOP_IDS = Object.freeze({
  daily: "daily_early_signal",
  weekly: "weekly_growth",
  biweekly: "biweekly_product",
  monthly: "monthly_strategy"
});
function runId(input, handler) {
  const binding = stableJson({
    cadence: input.cadence,
    commandId: handler.commandId,
    idempotencyKey: handler.idempotencyKey,
    tenant: {
      organizationId: handler.context.tenant.organizationId,
      ventureId: handler.context.tenant.ventureId
    }
  });
  return `learn-${input.cadence}-${createHash20("sha256").update(binding).digest("hex").slice(0, 32)}`;
}
function sourceEvidenceRefs(run) {
  return [
    ...new Set(run.evaluations.flatMap(({ sources }) => sources.flatMap(({ evidenceRefs }) => evidenceRefs)))
  ].sort();
}
function learningOutput(cadence, run) {
  if (run.actions.some(({ action }) => action.effect !== "none")) {
    throw new Error("report/propose learning cadences cannot apply effects");
  }
  if (run.status === "running" || run.status === "waiting_for_reconciliation") {
    throw new Error("report/propose learning cadence did not reach a terminal result");
  }
  return {
    commandId: "learn.run",
    mode: "local_write",
    status: run.status,
    data: {
      cadence,
      loopId: run.loopId,
      runId: run.runId,
      trigger: run.trigger,
      stopReason: run.stopReason,
      completionSatisfied: run.evaluations.at(-1)?.completionSatisfied === true,
      iterationCount: run.evaluations.length,
      actions: run.actions,
      actionsApplied: 0,
      evidenceRefs: sourceEvidenceRefs(run),
      limitations: run.limitations,
      output: run.output,
      updatedAt: run.updatedAt,
      externalEffects: false
    }
  };
}
async function runLearningCadence(runtime, input, handler) {
  const loopId = LEARNING_CADENCE_LOOP_IDS[input.cadence];
  const run = await runtime.run({
    loopId,
    tenant: handler.context.tenant,
    runId: runId(input, handler)
  });
  if (run.loopId !== loopId)
    throw new Error("learning loop returned a different cadence");
  return learningOutput(input.cadence, run);
}

// packages/agent-runtime/dist/operational.js
function emptyState() {
  return { schemaVersion: 1, ideas: {}, ventures: {}, plans: {}, runs: {} };
}
function cloneState(state) {
  return structuredClone(state);
}
function assertRecord(value, field) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    throw new Error(`${field} must be an object`);
  }
  return value;
}
function parseState(value) {
  const document2 = assertRecord(value, "operational state");
  if (document2.schemaVersion !== 1)
    throw new Error("unsupported operational state schema");
  return {
    schemaVersion: 1,
    ideas: assertRecord(document2.ideas, "ideas"),
    ventures: assertRecord(document2.ventures, "ventures"),
    plans: assertRecord(document2.plans, "plans"),
    runs: assertRecord(document2.runs, "runs")
  };
}
var SECRET_PATTERNS = [
  /-----BEGIN (?:RSA |EC |OPENSSH )?PRIVATE KEY-----/i,
  /\bBearer\s+[A-Za-z0-9._~+/=-]{8,}/i,
  /\b(?:sk|rk)_(?:live|test)_[A-Za-z0-9]{8,}/i,
  /\b(?:ghp|github_pat)_[A-Za-z0-9_]{8,}/i,
  /"(?:password|secret|access[_-]?token|refresh[_-]?token|api[_-]?key)"\s*:/i
];
function assertNoSecrets(value) {
  const serialized = JSON.stringify(value);
  if (SECRET_PATTERNS.some((pattern) => pattern.test(serialized))) {
    throw new Error("credential-like material is forbidden in operational command state");
  }
}
var InMemoryOperationalStateStore = class {
  description = "memory";
  #state = emptyState();
  read() {
    return cloneState(this.#state);
  }
  write(state) {
    assertNoSecrets(state);
    this.#state = cloneState(state);
  }
};
var FileOperationalStateStore = class {
  rootDir;
  path;
  constructor(rootDir = resolve22(process.cwd(), ".venture-harness")) {
    this.rootDir = resolve22(rootDir);
    this.path = join7(this.rootDir, "operational-state.json");
  }
  get description() {
    return this.path;
  }
  read() {
    if (!existsSync13(this.path))
      return emptyState();
    return parseState(JSON.parse(readFileSync15(this.path, "utf8")));
  }
  write(state) {
    assertNoSecrets(state);
    mkdirSync9(dirname12(this.path), { recursive: true });
    const temporary = join7(this.rootDir, `.operational-state-${randomUUID2()}.tmp`);
    const handle = openSync4(temporary, "wx", 384);
    try {
      writeFileSync9(handle, `${JSON.stringify(state, null, 2)}
`, "utf8");
      fsyncSync3(handle);
    } finally {
      closeSync4(handle);
    }
    renameSync7(temporary, this.path);
  }
};
function exactObject2(value, name, allowed) {
  const record3 = objectValue2(value, name);
  const unexpected = Object.keys(record3).filter((key) => !allowed.includes(key));
  if (unexpected.length > 0)
    throw new Error(`${name} has unsupported field(s): ${unexpected.join(", ")}`);
  return record3;
}
function safeId2(record3, field) {
  const value = stringValue(record3, field);
  if (!/^[a-z0-9][a-z0-9._-]{1,63}$/i.test(value)) {
    throw new Error(`${field} must contain 2-64 letters, digits, dots, underscores, or hyphens`);
  }
  return value;
}
var emptyInput = defineRuntimeSchema({
  name: "EmptyOperationalInput",
  jsonSchema: schemaObject({}, []),
  parse(value) {
    exactObject2(value, "EmptyOperationalInput", []);
    return {};
  }
});
var ventureIdentityInput = defineRuntimeSchema({
  name: "VentureIdentityInput",
  jsonSchema: schemaObject({ ventureId: { type: "string", minLength: 2, maxLength: 64 } }, [
    "ventureId"
  ]),
  parse(value) {
    const input = exactObject2(value, "VentureIdentityInput", ["ventureId"]);
    return { ventureId: safeId2(input, "ventureId") };
  }
});
var runIdentityInput = defineRuntimeSchema({
  name: "RunIdentityInput",
  jsonSchema: schemaObject({ runId: { type: "string", minLength: 2, maxLength: 64 } }, ["runId"]),
  parse(value) {
    const input = exactObject2(value, "RunIdentityInput", ["runId"]);
    return { runId: safeId2(input, "runId") };
  }
});
var ideaCompileInput = defineRuntimeSchema({
  name: "IdeaCompileInput",
  jsonSchema: schemaObject({
    idea: { type: "string", minLength: 3, maxLength: 1e4 },
    ventureId: { type: "string", minLength: 2, maxLength: 64 },
    name: { type: "string", minLength: 1, maxLength: 120 }
  }, ["idea", "ventureId", "name"]),
  parse(value) {
    const input = exactObject2(value, "IdeaCompileInput", ["idea", "ventureId", "name"]);
    const parsed = {
      idea: stringValue(input, "idea").trim(),
      ventureId: safeId2(input, "ventureId"),
      name: stringValue(input, "name").trim()
    };
    if (parsed.idea.length > 1e4)
      throw new Error("idea must be at most 10000 characters");
    if (parsed.name.length > 120)
      throw new Error("name must be at most 120 characters");
    assertNoSecrets(parsed);
    return parsed;
  }
});
var ventureCreateInput = defineRuntimeSchema({
  name: "VentureCreateInput",
  jsonSchema: schemaObject({
    ventureId: { type: "string", minLength: 2, maxLength: 64 },
    name: { type: "string", minLength: 1, maxLength: 120 }
  }, ["ventureId", "name"]),
  parse(value) {
    const input = exactObject2(value, "VentureCreateInput", ["ventureId", "name"]);
    const name = stringValue(input, "name").trim();
    if (name.length > 120)
      throw new Error("name must be at most 120 characters");
    return { ventureId: safeId2(input, "ventureId"), name };
  }
});
var ventureLaunchInput = defineRuntimeSchema({
  name: "VentureLaunchInput",
  jsonSchema: schemaObject({
    ventureId: { type: "string", minLength: 2, maxLength: 64 },
    runId: { type: "string", minLength: 2, maxLength: 64 },
    dryRun: { const: true }
  }, ["ventureId", "runId", "dryRun"]),
  parse(value) {
    const input = exactObject2(value, "VentureLaunchInput", ["ventureId", "runId", "dryRun"]);
    if (input.dryRun !== true) {
      throw new Error("the packaged local runtime permits dryRun=true only; no provider effect ran");
    }
    return {
      ventureId: safeId2(input, "ventureId"),
      runId: safeId2(input, "runId"),
      dryRun: true
    };
  }
});
var learningRunInput = defineRuntimeSchema({
  name: "LearningRunInput",
  jsonSchema: schemaObject({ cadence: { type: "string", enum: ["daily", "weekly", "biweekly", "monthly"] } }, ["cadence"]),
  parse(value) {
    const input = exactObject2(value, "LearningRunInput", ["cadence"]);
    return {
      cadence: stringValue(input, "cadence", {
        allowed: ["daily", "weekly", "biweekly", "monthly"]
      })
    };
  }
});
var verifyRunInput = defineRuntimeSchema({
  name: "VerifyRunInput",
  jsonSchema: schemaObject({ profile: { type: "string", enum: [...QUALITY_PROFILE_IDS] } }, [
    "profile"
  ]),
  parse(value) {
    const input = exactObject2(value, "VerifyRunInput", ["profile"]);
    return {
      profile: stringValue(input, "profile", {
        allowed: QUALITY_PROFILE_IDS
      })
    };
  }
});
var growthInspectInput = defineRuntimeSchema({
  name: "GrowthInspectInput",
  jsonSchema: schemaObject({ path: { type: "string", minLength: 1, maxLength: 4096, default: "config/growth.yaml" } }, []),
  parse(value) {
    const input = exactObject2(value, "GrowthInspectInput", ["path"]);
    const path = (stringValue(input, "path", { optional: true }) ?? "config/growth.yaml").trim();
    if (path.length > 4096)
      throw new Error("path must be at most 4096 characters");
    if (!/\.ya?ml$/i.test(path))
      throw new Error("growth contract path must end in .yaml or .yml");
    assertNoSecrets({ path });
    return { path };
  }
});
function outputSchema2(commandId) {
  return defineRuntimeSchema({
    name: `${commandId}Output`,
    jsonSchema: schemaObject({
      commandId: { const: commandId },
      mode: { type: "string", enum: ["read_only", "local_write", "dry_run", "pending"] },
      status: { type: "string", minLength: 1 },
      data: { type: "object" }
    }, ["commandId", "mode", "status", "data"]),
    parse(value) {
      const output = exactObject2(value, `${commandId}Output`, [
        "commandId",
        "mode",
        "status",
        "data"
      ]);
      if (output.commandId !== commandId)
        throw new Error(`invalid ${commandId} output`);
      const mode = stringValue(output, "mode", {
        allowed: ["read_only", "local_write", "dry_run", "pending"]
      });
      const data = objectValue2(output.data, "data");
      return {
        commandId,
        mode,
        status: stringValue(output, "status"),
        data
      };
    }
  });
}
function operationalCommand(definition2) {
  return defineCommandContract({
    ...definition2,
    effect: definition2.effect ?? "read",
    version: 1,
    output: outputSchema2(definition2.id),
    requirements: { activeSubscription: false, entitlements: [], grant: false, scopes: [] }
  });
}
var systemDoctorCommand = operationalCommand({
  id: "system.doctor",
  title: "Inspect Packaged Runtime",
  description: "Inspect local packaged-runtime readiness without contacting a provider.",
  input: emptyInput
});
var organizationListCommand = operationalCommand({
  id: "org.list",
  title: "List Organizations",
  description: "List the current local organization boundary.",
  input: emptyInput
});
var stackListCommand = operationalCommand({
  id: "stack.list",
  title: "List Stack Profiles",
  description: "List bundled provider-neutral stack profiles and their effect posture.",
  input: emptyInput
});
var packListCommand = operationalCommand({
  id: "pack.list",
  title: "List Packs",
  description: "List bundled command and runtime packs without installing anything.",
  input: emptyInput
});
var seedListCommand = operationalCommand({
  id: "seed.list",
  title: "List Venture Seeds",
  description: "List known venture seed rails without claiming that templates were materialized.",
  input: emptyInput
});
var grantListCommand = operationalCommand({
  id: "grant.list",
  title: "List Grants",
  description: "List sanitized grants visible in the current invocation context.",
  input: emptyInput
});
var providerListCommand = operationalCommand({
  id: "provider.list",
  title: "List Providers",
  description: "List provider capabilities as unconfigured; no authentication or network probe occurs.",
  input: emptyInput
});
var verifyRunCommand = operationalCommand({
  id: "verify.run",
  title: "Run Repository Quality Profile",
  description: "Execute the selected fast, MVP, or release quality profile and preserve failures and incomplete checks.",
  input: verifyRunInput
});
var dataSyncCommand = operationalCommand({
  id: "data.sync",
  title: "Inspect Data Sync",
  description: "Return an explicit skipped result when no read-only connector is configured.",
  input: emptyInput
});
var learningRunCommand = operationalCommand({
  id: "learn.run",
  title: "Run Bounded Learning Cadence",
  description: "Fetch connected provider evidence and execute one bounded no-effect report or proposal cadence; return insufficient evidence when unconfigured.",
  input: learningRunInput
});
var growthInspectCommand = operationalCommand({
  id: "growth.inspect",
  title: "Inspect Growth Contract",
  description: "Validate and summarize a local Growth Contract without persisting it or contacting a provider.",
  input: growthInspectInput
});
var ideaCompileCommand = operationalCommand({
  id: "idea.compile",
  title: "Compile Venture Idea",
  description: "Compile and persist one local founder idea with explicit assumptions.",
  input: ideaCompileInput,
  effect: "write"
});
var ventureCreateCommand = operationalCommand({
  id: "venture.create",
  title: "Create Local Venture",
  description: "Create a local venture from a previously compiled idea.",
  input: ventureCreateInput,
  effect: "write"
});
var venturePlanCommand = operationalCommand({
  id: "venture.plan",
  title: "Plan Local Venture",
  description: "Persist a provider-neutral, no-effect local venture plan.",
  input: ventureIdentityInput,
  effect: "write"
});
var ventureLaunchCommand = operationalCommand({
  id: "venture.launch",
  title: "Dry Launch Local Venture",
  description: "Persist a dry launch run; production/provider effects are not available.",
  input: ventureLaunchInput,
  effect: "write"
});
var ventureStatusCommand = operationalCommand({
  id: "venture.status",
  title: "Inspect Venture Status",
  description: "Read one locally persisted venture and its latest plan/run status.",
  input: ventureIdentityInput
});
var ventureResumeCommand = operationalCommand({
  id: "venture.resume",
  title: "Resume Local Dry Run",
  description: "Reload a persisted dry run without repeating or introducing an effect.",
  input: runIdentityInput
});
var runListCommand = operationalCommand({
  id: "run.list",
  title: "List Local Runs",
  description: "List locally persisted dry runs.",
  input: emptyInput
});
var runStatusCommand = operationalCommand({
  id: "run.status",
  title: "Inspect Local Run",
  description: "Read one locally persisted dry run.",
  input: runIdentityInput
});
var operationalCommandContracts = [
  systemDoctorCommand,
  organizationListCommand,
  stackListCommand,
  packListCommand,
  seedListCommand,
  grantListCommand,
  providerListCommand,
  ...platformOperationCommandContracts,
  verifyRunCommand,
  dataSyncCommand,
  learningRunCommand,
  growthInspectCommand,
  ideaCompileCommand,
  ventureCreateCommand,
  venturePlanCommand,
  ventureLaunchCommand,
  ventureStatusCommand,
  ventureResumeCommand,
  runListCommand,
  runStatusCommand
];
function result(commandId, mode, status, data) {
  return { commandId, mode, status, data };
}
function stateValues(records) {
  return Object.values(records).sort((left, right) => stableJson(left).localeCompare(stableJson(right)));
}
function latestForVenture(records, organizationId, ventureId2) {
  return Object.values(records).filter((record3) => record3.organizationId === organizationId && record3.ventureId === ventureId2).sort((left, right) => (left.updatedAt ?? left.createdAt).localeCompare(right.updatedAt ?? right.createdAt)).at(-1) ?? null;
}
function recordKey(organizationId, id) {
  return `${organizationId.length}:${organizationId}:${id}`;
}
var MAX_GROWTH_CONTRACT_BYTES = 1024 * 1024;
function growthContractVersion(value) {
  if (!value || typeof value !== "object" || Array.isArray(value))
    return null;
  const version = value.contract_version;
  return typeof version === "number" && Number.isInteger(version) ? version : null;
}
function pathEscapesRoot(root, candidate) {
  const pathFromRoot = relative14(root, candidate);
  return pathFromRoot === ".." || pathFromRoot.startsWith(`..${sep14}`) || isAbsolute7(pathFromRoot);
}
function assertGrowthPath(root, inputPath) {
  const candidate = resolve22(root.declaredPath, inputPath);
  if (pathEscapesRoot(root.declaredPath, candidate)) {
    throw new Error("growth contract path must stay within the configured root");
  }
  const pathFromRoot = relative14(root.declaredPath, candidate);
  let current = root.declaredPath;
  try {
    for (const component of pathFromRoot.split(sep14).filter(Boolean)) {
      current = join7(current, component);
      if (lstatSync8(current).isSymbolicLink()) {
        throw new Error("growth contract path must not contain symbolic links");
      }
    }
    const details = lstatSync8(candidate);
    if (!details.isFile())
      throw new Error("growth contract path must reference a regular file");
    const canonical = realpathSync7(candidate);
    if (pathEscapesRoot(root.canonicalPath, canonical)) {
      throw new Error("growth contract path must stay within the configured root");
    }
    return { path: canonical, displayPath: relative14(root.canonicalPath, canonical) };
  } catch (error) {
    if (error instanceof Error && (error.message.includes("symbolic links") || error.message.includes("regular file"))) {
      throw error;
    }
    throw new Error("growth contract file could not be read");
  }
}
function inspectGrowthContract(input, growthContractRoot) {
  const source = assertGrowthPath(growthContractRoot, input.path);
  let text2;
  let handle;
  try {
    handle = openSync4(source.path, constants2.O_RDONLY | (constants2.O_NOFOLLOW ?? 0));
    const details = fstatSync2(handle);
    if (!details.isFile())
      throw new Error("growth contract path must reference a regular file");
    if (details.size > MAX_GROWTH_CONTRACT_BYTES) {
      throw new Error("growth contract exceeds the 1 MiB inspection limit");
    }
    text2 = readFileSync15(handle, "utf8");
  } catch (error) {
    if (error instanceof Error && (error.message.includes("1 MiB inspection limit") || error.message.includes("regular file"))) {
      throw error;
    }
    throw new Error("growth contract file could not be read");
  } finally {
    if (handle !== void 0)
      closeSync4(handle);
  }
  if (Buffer.byteLength(text2, "utf8") > MAX_GROWTH_CONTRACT_BYTES) {
    throw new Error("growth contract exceeds the 1 MiB inspection limit");
  }
  let document2;
  try {
    document2 = parseYaml2(text2);
  } catch {
    throw new Error("growth contract YAML is invalid");
  }
  const originalSchemaVersion = growthContractVersion(document2);
  let contract;
  try {
    contract = parseGrowthContract(document2);
  } catch {
    throw new Error("growth contract failed schema validation");
  }
  return result("growth.inspect", "read_only", "valid", {
    source: {
      path: source.displayPath,
      format: "yaml",
      readOnly: true
    },
    schemaVersion: contract.contract_version,
    originalSchemaVersion,
    migrationApplied: originalSchemaVersion === 1 && contract.contract_version === GROWTH_CONTRACT_VERSION,
    venture: {
      ventureId: contract.venture_id,
      currency: contract.economics.currency,
      primaryEvent: contract.goal.primary_event,
      currentOptimizationEvent: contract.goal.current_optimization_event
    },
    budgets: {
      currency: contract.economics.currency,
      totalTestBudgetMinor: contract.paid.test_budget_minor,
      perCreativeCapMinor: contract.paid.per_creative_cap_minor
    },
    organic: {
      allowedProviders: contract.organic.allowed_providers,
      allowedAccountCount: contract.organic.allowed_accounts.length,
      maxAccounts: contract.organic.max_accounts,
      maxPostsPerAccountPerDay: contract.organic.max_posts_per_account_per_day,
      duplicateContentPolicy: contract.organic.duplicate_content_policy,
      defaultReviewMode: contract.organic.default_review_mode,
      snapshotCadenceMinutes: contract.organic.snapshot_cadence_minutes,
      aiDisclosureRequired: contract.organic.ai_disclosure_required
    },
    paid: {
      allowedNetworks: contract.paid.allowed_networks,
      allowedAccountCount: contract.paid.allowed_accounts.length,
      allowedObjectives: contract.paid.allowed_objectives,
      allowedEvents: contract.paid.allowed_events,
      dailyAccountCapMinor: contract.paid.daily_account_cap_minor,
      dailyVentureCapMinor: contract.paid.daily_venture_cap_minor,
      monthlyVentureCapMinor: contract.paid.monthly_venture_cap_minor,
      dailyCustomerCapMinor: contract.paid.daily_customer_cap_minor,
      monthlyCustomerCapMinor: contract.paid.monthly_customer_cap_minor,
      emergencyPlatformCapMinor: contract.paid.emergency_platform_cap_minor,
      approvalThresholdMinor: contract.paid.approval_threshold_minor,
      autoPauseAllowed: contract.paid.auto_pause_allowed,
      autoScaleAllowed: contract.paid.auto_scale_allowed,
      vboPolicy: contract.paid.vbo_policy,
      stopConditions: {
        maxSpendWithoutTrialMinor: contract.paid.stop_conditions.max_spend_without_trial_minor,
        maxSpendWithoutPurchaseMinor: contract.paid.stop_conditions.max_spend_without_purchase_minor,
        maxCacBreachCount: contract.paid.stop_conditions.max_cac_breach_count
      }
    },
    compliance: {
      rightsRequired: contract.compliance.rights_required,
      aiDisclosureRequired: contract.compliance.ai_disclosure_required,
      prohibitedClaims: contract.compliance.prohibited_claims,
      allowedGeographies: contract.compliance.allowed_geographies,
      restrictedAudiences: contract.compliance.restricted_audiences,
      restrictedCategories: contract.compliance.restricted_categories,
      providerPolicyState: contract.compliance.provider_policy_state
    },
    externalEffects: false
  });
}
function requireVenture(state, organizationId, ventureId2) {
  const venture = state.ventures[recordKey(organizationId, ventureId2)];
  if (!venture)
    throw new Error(`venture "${ventureId2}" is not locally created`);
  return venture;
}
function requireRun(state, organizationId, runId2) {
  const run = state.runs[recordKey(organizationId, runId2)];
  if (!run)
    throw new Error(`run "${runId2}" is not locally persisted`);
  return run;
}
function mutate(store, update) {
  const state = store.read();
  const output = update(state);
  store.write(state);
  return output;
}
function registerOperationalCommands(bus, options = {}) {
  const store = options.store ?? new InMemoryOperationalStateStore();
  const timestamp2 = () => (options.now ?? (() => /* @__PURE__ */ new Date()))().toISOString();
  const declaredGrowthContractRoot = resolve22(options.growthContractRoot ?? process.cwd());
  const rootDetails = lstatSync8(declaredGrowthContractRoot);
  if (rootDetails.isSymbolicLink() || !rootDetails.isDirectory()) {
    throw new Error("growth contract root must be a regular directory, not a symbolic link");
  }
  const growthContractRoot = {
    declaredPath: declaredGrowthContractRoot,
    canonicalPath: realpathSync7(declaredGrowthContractRoot)
  };
  bus.register(systemDoctorCommand, (_input, handler) => {
    const state = store.read();
    const organizationId = handler.context.tenant.organizationId;
    return result("system.doctor", "read_only", "ready_local", {
      runtime: "packaged",
      sourceFallback: false,
      stateStore: store.description,
      externalEffects: false,
      providersChecked: false,
      knownVentures: Object.values(state.ventures).filter((venture) => venture.organizationId === organizationId).length,
      knownRuns: Object.values(state.runs).filter((run) => run.organizationId === organizationId).length
    });
  });
  bus.register(organizationListCommand, (_input, handler) => result("org.list", "read_only", "available", {
    organizations: [{ organizationId: handler.context.tenant.organizationId, source: "context" }]
  }));
  bus.register(stackListCommand, () => result("stack.list", "read_only", "available", {
    stacks: options.stackCatalog ?? [
      {
        profileId: "local-safe",
        profileVersion: "0.2.0",
        label: "Local safe",
        verification: "unconfigured",
        implementationConfigured: false,
        credentialState: "unconfigured",
        liveVerification: "pending",
        providerEffectsConfigured: false,
        bindings: {}
      }
    ]
  }));
  bus.register(packListCommand, () => result("pack.list", "read_only", "available", {
    packs: [
      { id: "venture-operations", version: "0.2.0", state: "bundled" },
      { id: "agent-surfaces", version: "0.2.0", state: "bundled" }
    ]
  }));
  bus.register(seedListCommand, () => result("seed.list", "read_only", "catalog_only", {
    seeds: [
      { id: "web", rail: "web", action: "inspect_only" },
      { id: "ios", rail: "ios", action: "inspect_only" },
      { id: "hybrid", rail: "hybrid", action: "inspect_only" }
    ],
    materialized: false
  }));
  bus.register(grantListCommand, (_input, handler) => result("grant.list", "read_only", "available", {
    grants: handler.context.grants.map((grant) => ({
      grantId: grant.grantId,
      commandIds: [...grant.commandIds],
      scopes: [...grant.scopes],
      expiresAt: grant.expiresAt,
      state: grant.revokedAt ? "revoked" : "declared"
    })),
    productionEffectsAuthorized: false
  }));
  bus.register(providerListCommand, () => result("provider.list", "read_only", "unconfigured", {
    providers: [
      "app_store_connect",
      "bing",
      "brevo",
      "eas",
      "github",
      "google",
      "neon",
      "revenuecat",
      "stripe",
      "vercel"
    ].map((provider) => ({ provider, state: "unconfigured", networkChecked: false })),
    externalEffects: false
  }));
  bus.register(verifyRunCommand, async (input) => {
    const state = store.read();
    parseState(state);
    const profile2 = await (options.qualityProfileRunner ?? unconfiguredQualityProfileRunner).run(input.profile);
    return result("verify.run", "read_only", profile2.status, {
      profile: profile2,
      releaseGate: input.profile === "release",
      stateSchemaVersion: state.schemaVersion,
      credentialMaterialPersisted: false
    });
  });
  bus.register(dataSyncCommand, () => result("data.sync", "read_only", "skipped", {
    reason: "no read-only connector is configured in the packaged local runtime",
    records: null,
    externalRequestMade: false
  }));
  bus.register(learningRunCommand, (input, handler) => {
    if (options.learningLoopRuntime) {
      return runLearningCadence(options.learningLoopRuntime, input, handler);
    }
    return result("learn.run", "pending", "insufficient_evidence", {
      cadence: input.cadence,
      actionsApplied: 0,
      externalEffects: false,
      providerRequestMade: false,
      reason: "no normalized provider evidence is configured",
      runtime: "unconfigured"
    });
  });
  bus.register(growthInspectCommand, (input) => inspectGrowthContract(input, growthContractRoot));
  bus.register(ideaCompileCommand, (input, handler) => {
    const organizationId = handler.context.tenant.organizationId;
    const compiled = mutate(store, (state) => {
      const sourceHash = `sha256:${createHash21("sha256").update(input.idea).digest("hex")}`;
      const key = recordKey(organizationId, input.ventureId);
      const existing = state.ideas[key];
      if (existing) {
        if (existing.sourceHash !== sourceHash || existing.name !== input.name) {
          throw new Error(`venture "${input.ventureId}" is bound to a different compiled idea`);
        }
        return existing;
      }
      const record3 = {
        ideaId: `idea-${sourceHash.slice("sha256:".length, "sha256:".length + 12)}`,
        organizationId,
        ventureId: input.ventureId,
        name: input.name,
        summary: input.idea,
        sourceHash,
        compiledAt: timestamp2(),
        assumptions: [
          "Provider credentials and production authorization are not inferred.",
          "The packaged runtime will plan and dry-run locally only."
        ]
      };
      state.ideas[key] = record3;
      return record3;
    });
    return result("idea.compile", "local_write", "compiled", { idea: compiled });
  });
  bus.register(ventureCreateCommand, (input, handler) => {
    const organizationId = handler.context.tenant.organizationId;
    const venture = mutate(store, (state) => {
      const key = recordKey(organizationId, input.ventureId);
      const idea = state.ideas[key];
      if (!idea)
        throw new Error(`compile idea "${input.ventureId}" before creating the venture`);
      if (idea.name !== input.name)
        throw new Error("venture name differs from the compiled idea");
      const existing = state.ventures[key];
      if (existing)
        return existing;
      const createdAt = timestamp2();
      const record3 = {
        organizationId,
        ventureId: input.ventureId,
        name: input.name,
        ideaId: idea.ideaId,
        status: "created",
        createdAt,
        updatedAt: createdAt
      };
      state.ventures[key] = record3;
      return record3;
    });
    return result("venture.create", "local_write", "created", { venture });
  });
  bus.register(venturePlanCommand, (input, handler) => {
    const organizationId = handler.context.tenant.organizationId;
    const plan = mutate(store, (state) => {
      const venture = requireVenture(state, organizationId, input.ventureId);
      const planId = `plan-${input.ventureId}`;
      const key = recordKey(organizationId, planId);
      const existing = state.plans[key];
      if (existing)
        return existing;
      const record3 = {
        planId,
        organizationId,
        ventureId: input.ventureId,
        status: "planned",
        createdAt: timestamp2(),
        externalEffects: 0,
        steps: [
          { id: "validate-input", effect: "none", state: "planned" },
          { id: "materialization-preview", effect: "local", state: "planned" },
          { id: "provider-readiness", effect: "read", state: "blocked_unconfigured" }
        ]
      };
      state.plans[key] = record3;
      venture.status = "planned";
      venture.updatedAt = record3.createdAt;
      return record3;
    });
    return result("venture.plan", "local_write", "planned", { plan });
  });
  bus.register(ventureLaunchCommand, (input, handler) => {
    const organizationId = handler.context.tenant.organizationId;
    const run = mutate(store, (state) => {
      const venture = requireVenture(state, organizationId, input.ventureId);
      const plan = state.plans[recordKey(organizationId, `plan-${input.ventureId}`)];
      if (!plan)
        throw new Error(`plan venture "${input.ventureId}" before launching`);
      const key = recordKey(organizationId, input.runId);
      const existing = state.runs[key];
      if (existing) {
        if (existing.ventureId !== input.ventureId) {
          throw new Error(`run "${input.runId}" belongs to another venture`);
        }
        return existing;
      }
      const createdAt = timestamp2();
      const record3 = {
        runId: input.runId,
        organizationId,
        ventureId: input.ventureId,
        planId: plan.planId,
        status: "dry_run_complete",
        mode: "dry_run",
        createdAt,
        updatedAt: createdAt,
        externalEffects: 0,
        resumable: true
      };
      state.runs[key] = record3;
      venture.status = "dry_run_complete";
      venture.updatedAt = createdAt;
      return record3;
    });
    return result("venture.launch", "dry_run", "dry_run_complete", { run });
  });
  bus.register(ventureStatusCommand, (input, handler) => {
    const organizationId = handler.context.tenant.organizationId;
    const state = store.read();
    const venture = requireVenture(state, organizationId, input.ventureId);
    return result("venture.status", "read_only", venture.status, {
      venture,
      plan: latestForVenture(state.plans, organizationId, input.ventureId),
      run: latestForVenture(state.runs, organizationId, input.ventureId)
    });
  });
  bus.register(ventureResumeCommand, (input, handler) => {
    const state = store.read();
    const run = requireRun(state, handler.context.tenant.organizationId, input.runId);
    return result("venture.resume", "read_only", "no_pending_work", {
      run,
      repeatedEffects: 0,
      nextAction: "configure the full authorized runtime before any provider apply"
    });
  });
  bus.register(runListCommand, (_input, handler) => {
    const state = store.read();
    const runs = Object.fromEntries(Object.entries(state.runs).filter(([, run]) => run.organizationId === handler.context.tenant.organizationId));
    return result("run.list", "read_only", "available", { runs: stateValues(runs) });
  });
  bus.register(runStatusCommand, (input, handler) => {
    const state = store.read();
    return result("run.status", "read_only", "available", {
      run: requireRun(state, handler.context.tenant.organizationId, input.runId)
    });
  });
}

// packages/agent-runtime/dist/provider-operations.js
var WINNER_PROVIDER_COMMAND_IDS = [
  "creative_generation",
  "tiktok_content_posting",
  "tiktok_spark_ads",
  "aggregated_attribution",
  "revenuecat"
];
var WINNER_PROVIDER_COMMAND_FEATURES = [
  "creative.video.generate",
  "distribution.content.draft",
  "distribution.content.publish",
  "ads.organic_post.boost",
  "ads.campaign.pause",
  "attribution.campaign.read",
  "subscription.lifecycle.read"
];
var PROVIDER_COMMAND_ACTIONS = [
  "doctor",
  "plan",
  "dry_run",
  "apply",
  "status",
  "read_back",
  "reconcile"
];
var PROVIDER_ID_VALUES = [...WINNER_PROVIDER_COMMAND_IDS];
var FEATURE_VALUES = [...WINNER_PROVIDER_COMMAND_FEATURES];
var ACTION_VALUES = [...PROVIDER_COMMAND_ACTIONS];
var SAFE_IDENTIFIER = /^[a-zA-Z0-9][a-zA-Z0-9._:~-]{0,254}$/u;
var SECRET_KEY2 = /(?:authorization|api[-_]?key|secret|password|credential|access[-_]?token|refresh[-_]?token|upload[-_]?url)/iu;
var SECRET_VALUE2 = /(?:\bbearer\s+[a-z0-9._~+/=-]{8,}|-----BEGIN [A-Z ]*PRIVATE KEY-----|\b(?:sk|rk|pk|atk)_(?:live|test)?_?[a-z0-9_-]{8,})/iu;
var SECRET_QUERY_KEY = /(?:token|signature|secret|key|authorization)/iu;
function exactObject3(value, name, allowed) {
  const record3 = objectValue2(value, name);
  const unexpected = Object.keys(record3).filter((key) => !allowed.includes(key));
  if (unexpected.length > 0) {
    throw new Error(`${name} has unsupported field(s): ${unexpected.join(", ")}`);
  }
  return record3;
}
function identifier2(record3, field) {
  const value = stringValue(record3, field);
  if (!SAFE_IDENTIFIER.test(value)) {
    throw new Error(`${field} must be a provider-safe identifier of at most 255 characters`);
  }
  return value;
}
function assertNoProviderSecrets(value, path = "value", allowRedacted = false) {
  if (Array.isArray(value)) {
    value.forEach((entry, index) => assertNoProviderSecrets(entry, `${path}[${index}]`, allowRedacted));
    return;
  }
  if (value !== null && typeof value === "object") {
    for (const [key, entry] of Object.entries(value)) {
      const isReference = /(?:^|_)(?:ref|reference)$/iu.test(key);
      if (SECRET_KEY2.test(key) && !isReference && !(allowRedacted && entry === "[REDACTED]")) {
        throw new Error(`secret-bearing field ${path}.${key} is forbidden`);
      }
      assertNoProviderSecrets(entry, `${path}.${key}`, allowRedacted);
    }
    return;
  }
  if (typeof value !== "string")
    return;
  if (SECRET_VALUE2.test(value))
    throw new Error(`credential-like value is forbidden at ${path}`);
  try {
    const url = new URL(value);
    if ([...url.searchParams.keys()].some((key) => SECRET_QUERY_KEY.test(key))) {
      throw new Error(`signed or credential-bearing URL is forbidden at ${path}`);
    }
  } catch (error) {
    if (error instanceof Error && error.message.startsWith("signed or credential-bearing URL")) {
      throw error;
    }
  }
}
function parseSelection(value, name) {
  const input = exactObject3(value, name, [
    "organizationId",
    "providerId",
    "providerAccountId",
    "feature"
  ]);
  const parsed = {
    organizationId: identifier2(input, "organizationId"),
    providerId: stringValue(input, "providerId", {
      allowed: PROVIDER_ID_VALUES
    }),
    providerAccountId: identifier2(input, "providerAccountId"),
    feature: stringValue(input, "feature", {
      allowed: FEATURE_VALUES
    })
  };
  assertNoProviderSecrets(parsed);
  return parsed;
}
function parseOperation(value, name) {
  const input = exactObject3(value, name, [
    "organizationId",
    "providerId",
    "providerAccountId",
    "feature",
    "operationId",
    "providerIdempotencyKey",
    "payload"
  ]);
  const parsed = {
    organizationId: identifier2(input, "organizationId"),
    providerId: stringValue(input, "providerId", {
      allowed: PROVIDER_ID_VALUES
    }),
    providerAccountId: identifier2(input, "providerAccountId"),
    feature: stringValue(input, "feature", {
      allowed: FEATURE_VALUES
    }),
    operationId: identifier2(input, "operationId"),
    providerIdempotencyKey: identifier2(input, "providerIdempotencyKey"),
    payload: objectValue2(input.payload, "payload")
  };
  assertNoProviderSecrets(parsed);
  return parsed;
}
var selectionInput = defineRuntimeSchema({
  name: "ProviderSelectionInput",
  jsonSchema: schemaObject({
    organizationId: { type: "string", minLength: 1, maxLength: 255 },
    providerId: { type: "string", enum: PROVIDER_ID_VALUES },
    providerAccountId: { type: "string", minLength: 1, maxLength: 255 },
    feature: { type: "string", enum: FEATURE_VALUES }
  }, ["organizationId", "providerId", "providerAccountId", "feature"]),
  parse(value) {
    return parseSelection(value, "ProviderSelectionInput");
  }
});
var operationInput = defineRuntimeSchema({
  name: "ProviderOperationInput",
  jsonSchema: schemaObject({
    organizationId: { type: "string", minLength: 1, maxLength: 255 },
    providerId: { type: "string", enum: PROVIDER_ID_VALUES },
    providerAccountId: { type: "string", minLength: 1, maxLength: 255 },
    feature: { type: "string", enum: FEATURE_VALUES },
    operationId: { type: "string", minLength: 1, maxLength: 255 },
    providerIdempotencyKey: { type: "string", minLength: 1, maxLength: 255 },
    payload: { type: "object" }
  }, [
    "organizationId",
    "providerId",
    "providerAccountId",
    "feature",
    "operationId",
    "providerIdempotencyKey",
    "payload"
  ]),
  parse(value) {
    return parseOperation(value, "ProviderOperationInput");
  }
});
function providerOutput(commandId) {
  return defineRuntimeSchema({
    name: `${commandId}Output`,
    jsonSchema: schemaObject({
      commandId: { const: commandId },
      action: { type: "string", enum: ACTION_VALUES },
      organizationId: { type: "string", minLength: 1, maxLength: 255 },
      providerId: { type: "string", enum: PROVIDER_ID_VALUES },
      feature: { type: "string", enum: FEATURE_VALUES },
      status: { type: "string", minLength: 1 },
      providerInvoked: { anyOf: [{ type: "boolean" }, { const: "unknown" }] },
      externalEffectOccurred: { anyOf: [{ type: "boolean" }, { const: "unknown" }] },
      liveVerified: { type: "boolean" },
      data: { type: "object" }
    }, [
      "commandId",
      "action",
      "organizationId",
      "providerId",
      "feature",
      "status",
      "providerInvoked",
      "externalEffectOccurred",
      "liveVerified",
      "data"
    ]),
    parse(value) {
      const output = exactObject3(value, `${commandId}Output`, [
        "commandId",
        "action",
        "organizationId",
        "providerId",
        "feature",
        "status",
        "providerInvoked",
        "externalEffectOccurred",
        "liveVerified",
        "data"
      ]);
      if (output.commandId !== commandId)
        throw new Error(`invalid ${commandId} output`);
      const providerInvoked = output.providerInvoked;
      const externalEffectOccurred = output.externalEffectOccurred;
      if (typeof providerInvoked !== "boolean" && providerInvoked !== "unknown") {
        throw new Error("providerInvoked must be boolean or unknown");
      }
      if (typeof externalEffectOccurred !== "boolean" && externalEffectOccurred !== "unknown") {
        throw new Error("externalEffectOccurred must be boolean or unknown");
      }
      if (typeof output.liveVerified !== "boolean") {
        throw new Error("liveVerified must be boolean");
      }
      const data = objectValue2(output.data, "data");
      assertNoProviderSecrets(data, "data", true);
      return {
        commandId,
        action: stringValue(output, "action", { allowed: ACTION_VALUES }),
        organizationId: identifier2(output, "organizationId"),
        providerId: stringValue(output, "providerId", {
          allowed: PROVIDER_ID_VALUES
        }),
        feature: stringValue(output, "feature", {
          allowed: FEATURE_VALUES
        }),
        status: stringValue(output, "status"),
        providerInvoked,
        externalEffectOccurred,
        liveVerified: output.liveVerified,
        data
      };
    }
  });
}
function providerCommand(definition2) {
  const { grant, scopes, ...contract } = definition2;
  return defineCommandContract({
    ...contract,
    version: 2,
    output: providerOutput(definition2.id),
    requirements: {
      activeSubscription: false,
      entitlements: [],
      grant,
      scopes
    }
  });
}
var providerDoctorCommand = providerCommand({
  id: "provider.doctor",
  title: "Inspect Provider Capability",
  description: "Inspect one exact Winner provider capability through an injected official transport; default is unconfigured and makes no request.",
  input: selectionInput,
  grant: true,
  scopes: ["provider.read"],
  effect: "read"
});
var providerPlanCommand = providerCommand({
  id: "provider.plan",
  title: "Plan Provider Operation",
  description: "Build an immutable capability-specific provider plan without allowing external execution.",
  input: operationInput,
  grant: false,
  scopes: [],
  effect: "read"
});
var providerDryRunCommand = providerCommand({
  id: "provider.dry-run",
  title: "Dry Run Provider Operation",
  description: "Exercise provider plan validation without invoking a provider or creating an effect.",
  input: operationInput,
  grant: false,
  scopes: [],
  effect: "read"
});
var providerApplyCommand = providerCommand({
  id: "provider.apply",
  title: "Apply Authorized Provider Operation",
  description: "Apply once only through an injected official transport, exact grants, and durable atomic operation storage.",
  input: operationInput,
  grant: true,
  scopes: ["provider.apply"],
  effect: "write"
});
var providerStatusCommand = providerCommand({
  id: "provider.status",
  title: "Verify Provider Operation Status",
  description: "Read and verify provider operation status without replaying the original provider mutation.",
  input: operationInput,
  grant: true,
  scopes: ["provider.read"],
  effect: "write"
});
var providerReadBackCommand = providerCommand({
  id: "provider.read-back",
  title: "Read Back Provider Operation",
  description: "Read exact provider state back and validate capability-specific completion invariants.",
  input: operationInput,
  grant: true,
  scopes: ["provider.read"],
  effect: "write"
});
var providerReconcileCommand = providerCommand({
  id: "provider.reconcile",
  title: "Reconcile Provider Operation",
  description: "Reconcile an unresolved provider operation by immutable request hash without reapplying it.",
  input: operationInput,
  grant: true,
  scopes: ["provider.read"],
  effect: "write"
});
function unconfiguredResult(action, input) {
  return {
    status: "unconfigured",
    providerInvoked: false,
    externalEffectOccurred: false,
    liveVerified: false,
    data: {
      organizationId: input.organizationId,
      providerId: input.providerId,
      providerAccountId: input.providerAccountId,
      feature: input.feature,
      action,
      externalRequestMade: false,
      fixtureFallbackUsed: false,
      diagnostic: {
        code: "transport_missing",
        message: "No authorized Winner provider runtime is injected",
        nextAction: "Bind an official provider transport, broker reference, grants, and durable operation store"
      }
    }
  };
}
var unconfiguredProviderCommandRuntime = Object.freeze({
  execute: unconfiguredResult
});
function commandResult(commandId, action, input, boundary) {
  return {
    commandId,
    action,
    organizationId: input.organizationId,
    providerId: input.providerId,
    feature: input.feature,
    status: boundary.status,
    providerInvoked: boundary.providerInvoked,
    externalEffectOccurred: boundary.externalEffectOccurred,
    liveVerified: boundary.liveVerified,
    data: boundary.data
  };
}
function register2(bus, runtime, contract, action) {
  bus.register(contract, async (input, handler) => {
    let boundary;
    try {
      boundary = await runtime.execute(action, input, handler);
    } catch {
      throw new Error("The injected provider runtime failed without a verified outcome; reconcile before retry");
    }
    const direct = boundary.data.diagnostic;
    const nested = boundary.data.result && typeof boundary.data.result === "object" && !Array.isArray(boundary.data.result) ? boundary.data.result.diagnostic : void 0;
    const diagnostic = direct ?? nested;
    const failureStatuses = /* @__PURE__ */ new Set([
      "unconfigured",
      "tenant_mismatch",
      "unsupported_feature",
      "context_unavailable",
      "invalid_request",
      "dry_run_failed",
      "doctor_failed",
      "official_transport_required",
      "provider_outcome_unknown",
      "runtime_failed",
      "blocked",
      "failed",
      "idempotency_conflict"
    ]);
    if (failureStatuses.has(boundary.status) && diagnostic && typeof diagnostic === "object" && !Array.isArray(diagnostic)) {
      const record3 = diagnostic;
      const code = typeof record3.code === "string" ? record3.code : "provider_command_failed";
      const message = typeof record3.message === "string" ? record3.message : "The provider command did not complete successfully";
      const failure = `${code}: ${message}`;
      if (boundary.externalEffectOccurred === false) {
        throw new CommandDefinitiveNoEffectError(failure, "handler_failed");
      }
      throw new Error(failure);
    }
    return commandResult(contract.id, action, input, boundary);
  });
}
function registerProviderOperationCommands(bus, runtime = unconfiguredProviderCommandRuntime) {
  register2(bus, runtime, providerDoctorCommand, "doctor");
  register2(bus, runtime, providerPlanCommand, "plan");
  register2(bus, runtime, providerDryRunCommand, "dry_run");
  register2(bus, runtime, providerApplyCommand, "apply");
  register2(bus, runtime, providerStatusCommand, "status");
  register2(bus, runtime, providerReadBackCommand, "read_back");
  register2(bus, runtime, providerReconcileCommand, "reconcile");
}

// packages/agent-runtime/dist/stack-operations.js
var STACK_COMMAND_ACTIONS = [
  "doctor",
  "plan",
  "dry_run",
  "apply",
  "read_back",
  "reconcile"
];
var ENVIRONMENTS = [
  "local",
  "preview",
  "sandbox",
  "production",
  "testflight"
];
var SAFE_ID2 = /^[a-zA-Z0-9][a-zA-Z0-9._:~-]{0,254}$/u;
var SAFE_ROLE = /^[a-z][a-z0-9_-]*(?:\.[a-z][a-z0-9_-]*)+$/u;
var SECRET_KEY3 = /(?:authorization|api[-_]?key|secret|password|credential|access[-_]?token|refresh[-_]?token)/iu;
var SECRET_VALUE3 = /(?:\bbearer\s+[a-z0-9._~+/=-]{8,}|-----BEGIN [A-Z ]*PRIVATE KEY-----|\b(?:sk|rk|pk|atk)_(?:live|test)?_?[a-z0-9_-]{8,})/iu;
function exactObject4(value, name, allowed) {
  const record3 = objectValue2(value, name);
  const unexpected = Object.keys(record3).filter((key) => !allowed.includes(key));
  if (unexpected.length > 0) {
    throw new Error(`${name} has unsupported field(s): ${unexpected.join(", ")}`);
  }
  return record3;
}
function safeId3(record3, field) {
  const value = stringValue(record3, field);
  if (!SAFE_ID2.test(value)) {
    throw new Error(`${field} must be a safe identifier of at most 255 characters`);
  }
  return value;
}
function assertNoSecrets2(value, path = "value", allowReferences = false) {
  if (Array.isArray(value)) {
    value.forEach((entry, index) => assertNoSecrets2(entry, `${path}[${index}]`, allowReferences));
    return;
  }
  if (value !== null && typeof value === "object") {
    for (const [key, entry] of Object.entries(value)) {
      const reference = allowReferences && /(?:ref|reference)s?$/iu.test(key);
      if (SECRET_KEY3.test(key) && !reference) {
        throw new Error(`secret-bearing field ${path}.${key} is forbidden`);
      }
      assertNoSecrets2(entry, `${path}.${key}`, allowReferences);
    }
    return;
  }
  if (typeof value === "string" && SECRET_VALUE3.test(value)) {
    throw new Error(`credential-like value is forbidden at ${path}`);
  }
}
function parseSelection2(value, name) {
  const input = exactObject4(value, name, [
    "profileId",
    "profileVersion",
    "role",
    "providerId",
    "capability",
    "environment"
  ]);
  const role = stringValue(input, "role");
  if (!SAFE_ROLE.test(role))
    throw new Error("role must be a namespaced capability role");
  const parsed = {
    profileId: safeId3(input, "profileId"),
    profileVersion: safeId3(input, "profileVersion"),
    role,
    providerId: safeId3(input, "providerId"),
    capability: safeId3(input, "capability"),
    environment: stringValue(input, "environment", {
      allowed: ENVIRONMENTS
    })
  };
  assertNoSecrets2(parsed);
  return parsed;
}
function parseOperation2(value, name) {
  const input = exactObject4(value, name, [
    "profileId",
    "profileVersion",
    "role",
    "providerId",
    "capability",
    "environment",
    "operationId",
    "payload"
  ]);
  const selection = parseSelection2(Object.fromEntries(Object.entries(input).filter(([key]) => key !== "operationId" && key !== "payload")), name);
  const parsed = {
    ...selection,
    operationId: safeId3(input, "operationId"),
    payload: objectValue2(input.payload, "payload")
  };
  assertNoSecrets2(parsed, name, true);
  return parsed;
}
var selectionInput2 = defineRuntimeSchema({
  name: "StackSelectionInput",
  jsonSchema: schemaObject({
    profileId: { type: "string", minLength: 1, maxLength: 255 },
    profileVersion: { type: "string", minLength: 1, maxLength: 255 },
    role: { type: "string", minLength: 3, maxLength: 255 },
    providerId: { type: "string", minLength: 1, maxLength: 255 },
    capability: { type: "string", minLength: 1, maxLength: 255 },
    environment: { type: "string", enum: [...ENVIRONMENTS] }
  }, ["profileId", "profileVersion", "role", "providerId", "capability", "environment"]),
  parse(value) {
    return parseSelection2(value, "StackSelectionInput");
  }
});
var operationInput2 = defineRuntimeSchema({
  name: "StackOperationInput",
  jsonSchema: schemaObject({
    ...selectionInput2.jsonSchema.properties,
    operationId: { type: "string", minLength: 1, maxLength: 255 },
    payload: { type: "object" }
  }, [
    "profileId",
    "profileVersion",
    "role",
    "providerId",
    "capability",
    "environment",
    "operationId",
    "payload"
  ]),
  parse(value) {
    return parseOperation2(value, "StackOperationInput");
  }
});
function outputSchema3(commandId) {
  return defineRuntimeSchema({
    name: `${commandId}Output`,
    jsonSchema: schemaObject({
      commandId: { const: commandId },
      action: { type: "string", enum: [...STACK_COMMAND_ACTIONS] },
      profileId: { type: "string" },
      profileVersion: { type: "string" },
      role: { type: "string" },
      providerId: { type: "string" },
      capability: { type: "string" },
      status: { type: "string" },
      providerInvoked: { anyOf: [{ type: "boolean" }, { const: "unknown" }] },
      externalEffectOccurred: { anyOf: [{ type: "boolean" }, { const: "unknown" }] },
      liveVerified: { type: "boolean" },
      data: { type: "object" }
    }, [
      "commandId",
      "action",
      "profileId",
      "profileVersion",
      "role",
      "providerId",
      "capability",
      "status",
      "providerInvoked",
      "externalEffectOccurred",
      "liveVerified",
      "data"
    ]),
    parse(value) {
      const output = exactObject4(value, `${commandId}Output`, [
        "commandId",
        "action",
        "profileId",
        "profileVersion",
        "role",
        "providerId",
        "capability",
        "status",
        "providerInvoked",
        "externalEffectOccurred",
        "liveVerified",
        "data"
      ]);
      if (output.commandId !== commandId)
        throw new Error(`invalid ${commandId} output`);
      const providerInvoked = output.providerInvoked;
      const externalEffectOccurred = output.externalEffectOccurred;
      if (typeof providerInvoked !== "boolean" && providerInvoked !== "unknown") {
        throw new Error("providerInvoked must be boolean or unknown");
      }
      if (typeof externalEffectOccurred !== "boolean" && externalEffectOccurred !== "unknown") {
        throw new Error("externalEffectOccurred must be boolean or unknown");
      }
      if (typeof output.liveVerified !== "boolean")
        throw new Error("liveVerified must be boolean");
      const data = objectValue2(output.data, "data");
      assertNoSecrets2(data, "data", true);
      return {
        commandId,
        action: stringValue(output, "action", {
          allowed: STACK_COMMAND_ACTIONS
        }),
        profileId: stringValue(output, "profileId"),
        profileVersion: stringValue(output, "profileVersion"),
        role: stringValue(output, "role"),
        providerId: stringValue(output, "providerId"),
        capability: stringValue(output, "capability"),
        status: stringValue(output, "status"),
        providerInvoked,
        externalEffectOccurred,
        liveVerified: output.liveVerified,
        data
      };
    }
  });
}
function stackCommand(definition2) {
  const { grant, scopes, ...contract } = definition2;
  return defineCommandContract({
    ...contract,
    version: 1,
    output: outputSchema3(definition2.id),
    requirements: { activeSubscription: false, entitlements: [], grant, scopes }
  });
}
var stackDoctorCommand = stackCommand({
  id: "stack.doctor",
  title: "Inspect Stack Capability",
  description: "Inspect the exact provider adapter selected by an attested Stack Profile.",
  input: selectionInput2,
  grant: true,
  scopes: ["provider.read"],
  effect: "read"
});
var stackPlanCommand = stackCommand({
  id: "stack.plan",
  title: "Plan Stack Capability",
  description: "Build a no-effect provider plan through an exact versioned Stack Profile binding.",
  input: operationInput2,
  grant: false,
  scopes: [],
  effect: "read"
});
var stackDryRunCommand = stackCommand({
  id: "stack.dry-run",
  title: "Dry Run Stack Capability",
  description: "Exercise a selected adapter plan locally without executing a provider effect.",
  input: operationInput2,
  grant: false,
  scopes: [],
  effect: "read"
});
var stackApplyCommand = stackCommand({
  id: "stack.apply",
  title: "Apply Stack Capability",
  description: "Apply an authorized profile-bound provider plan with durable command and provider idempotency.",
  input: operationInput2,
  grant: true,
  scopes: ["provider.apply"],
  effect: "write"
});
var stackReadBackCommand = stackCommand({
  id: "stack.read-back",
  title: "Read Back Stack Capability",
  description: "Read back the exact stored profile-bound operation without repeating its effect.",
  input: operationInput2,
  grant: true,
  scopes: ["provider.read"],
  effect: "write"
});
var stackReconcileCommand = stackCommand({
  id: "stack.reconcile",
  title: "Reconcile Stack Capability",
  description: "Reconcile a previously attempted profile-bound operation through its durable provider ledger.",
  input: operationInput2,
  grant: true,
  scopes: ["provider.read"],
  effect: "write"
});
var unconfiguredCatalog = Object.freeze([
  Object.freeze({
    profileId: "local-safe",
    profileVersion: "0.2.0",
    label: "Local safe",
    verification: "unconfigured",
    implementationConfigured: false,
    credentialState: "unconfigured",
    liveVerification: "pending",
    providerEffectsConfigured: false,
    bindings: {}
  })
]);
var unconfiguredStackCommandRuntime = Object.freeze({
  catalog: unconfiguredCatalog,
  execute(action, input) {
    return {
      status: "unconfigured",
      providerInvoked: false,
      externalEffectOccurred: false,
      liveVerified: false,
      data: {
        action,
        profileId: input.profileId,
        profileVersion: input.profileVersion,
        role: input.role,
        providerId: input.providerId,
        capability: input.capability,
        externalRequestMade: false,
        fixtureFallbackUsed: false,
        diagnostic: {
          code: "stack_runtime_unconfigured",
          message: "No authorized Stack Profile provider runtime is injected",
          nextAction: "Inject the repository provider bridge with exact grants and durable idempotency stores"
        }
      }
    };
  }
});
function commandResult2(commandId, action, input, boundary) {
  return {
    commandId,
    action,
    profileId: input.profileId,
    profileVersion: input.profileVersion,
    role: input.role,
    providerId: input.providerId,
    capability: input.capability,
    status: boundary.status,
    providerInvoked: boundary.providerInvoked,
    externalEffectOccurred: boundary.externalEffectOccurred,
    liveVerified: boundary.liveVerified,
    data: boundary.data
  };
}
function register3(bus, runtime, contract, action) {
  bus.register(contract, async (input, handler) => {
    let boundary;
    try {
      boundary = await runtime.execute(action, input, handler);
    } catch {
      throw new Error("The Stack Profile runtime failed without a verified outcome; reconcile before retry");
    }
    const direct = boundary.data.diagnostic;
    const nested = boundary.data.result && typeof boundary.data.result === "object" && !Array.isArray(boundary.data.result) ? boundary.data.result.diagnostic : void 0;
    const diagnostic = direct ?? nested;
    const failureStatuses = /* @__PURE__ */ new Set([
      "unconfigured",
      "runtime_failed",
      "context_unavailable",
      "blocked",
      "failed",
      "idempotency_conflict"
    ]);
    if (failureStatuses.has(boundary.status) && diagnostic && typeof diagnostic === "object" && !Array.isArray(diagnostic)) {
      const record3 = diagnostic;
      const code = typeof record3.code === "string" ? record3.code : "stack_command_failed";
      const message = typeof record3.message === "string" ? record3.message : "The Stack Profile command did not complete successfully";
      const failure = `${code}: ${message}`;
      if (boundary.externalEffectOccurred === false) {
        throw new CommandDefinitiveNoEffectError(failure, "handler_failed");
      }
      throw new Error(failure);
    }
    return commandResult2(contract.id, action, input, boundary);
  });
}
function registerStackOperationCommands(bus, runtime = unconfiguredStackCommandRuntime) {
  register3(bus, runtime, stackDoctorCommand, "doctor");
  register3(bus, runtime, stackPlanCommand, "plan");
  register3(bus, runtime, stackDryRunCommand, "dry_run");
  register3(bus, runtime, stackApplyCommand, "apply");
  register3(bus, runtime, stackReadBackCommand, "read_back");
  register3(bus, runtime, stackReconcileCommand, "reconcile");
}

// packages/agent-runtime/dist/recursive.js
var SAFE_ID3 = /^[A-Za-z0-9_][A-Za-z0-9._:-]{0,254}$/u;
function exactObject5(value, name, allowed) {
  const record3 = objectValue2(value, name);
  const unexpected = Object.keys(record3).filter((key) => !allowed.includes(key));
  if (unexpected.length > 0) {
    throw new Error(`${name} has unsupported field(s): ${unexpected.join(", ")}`);
  }
  return record3;
}
function identifier3(record3, field) {
  const value = stringValue(record3, field);
  try {
    assertCredentialFree(value, field);
  } catch {
    throw new Error(`credential material is forbidden in ${field}`);
  }
  if (value !== value.trim() || !SAFE_ID3.test(value)) {
    throw new Error(`${field} must be a canonical identifier of at most 255 characters`);
  }
  return value;
}
function assertJsonSafe2(value, path) {
  try {
    assertCredentialFree(value, path);
  } catch {
    throw new Error(`credential or non-JSON material is forbidden in ${path}`);
  }
}
var INPUT_FIELDS = [
  "customerOrganizationId",
  "subscriptionId",
  "entitlementId",
  "serviceGrantId",
  "providerConnectionId",
  "capability",
  "authorizationEnvelopeId",
  "runId",
  "nodeId",
  "correlationId",
  "causationId",
  "usageUnits",
  "payload"
];
var RECONCILE_INPUT_FIELDS = INPUT_FIELDS.filter((field) => field !== "authorizationEnvelopeId");
var recursiveInput = defineRuntimeSchema({
  name: "RecursiveServiceCommandInput",
  jsonSchema: schemaObject({
    customerOrganizationId: { type: "string", minLength: 1, maxLength: 255 },
    subscriptionId: { type: "string", minLength: 1, maxLength: 255 },
    entitlementId: { type: "string", minLength: 1, maxLength: 255 },
    serviceGrantId: { type: "string", minLength: 1, maxLength: 255 },
    providerConnectionId: { type: "string", minLength: 1, maxLength: 255 },
    capability: { type: "string", minLength: 1, maxLength: 255 },
    authorizationEnvelopeId: { type: "string", minLength: 1, maxLength: 255 },
    runId: { type: "string", minLength: 1, maxLength: 255 },
    nodeId: { type: "string", minLength: 1, maxLength: 255 },
    correlationId: { type: "string", minLength: 1, maxLength: 255 },
    causationId: { type: "string", minLength: 1, maxLength: 255 },
    usageUnits: { type: "integer", minimum: 1 },
    payload: { type: "object" }
  }, [...INPUT_FIELDS]),
  parse(value) {
    const input = exactObject5(value, "RecursiveServiceCommandInput", INPUT_FIELDS);
    if (!Number.isSafeInteger(input.usageUnits) || Number(input.usageUnits) < 1) {
      throw new Error("usageUnits must be a positive safe integer");
    }
    const payload = objectValue2(input.payload, "payload");
    assertJsonSafe2(payload, "payload");
    return {
      customerOrganizationId: identifier3(input, "customerOrganizationId"),
      subscriptionId: identifier3(input, "subscriptionId"),
      entitlementId: identifier3(input, "entitlementId"),
      serviceGrantId: identifier3(input, "serviceGrantId"),
      providerConnectionId: identifier3(input, "providerConnectionId"),
      capability: identifier3(input, "capability"),
      authorizationEnvelopeId: identifier3(input, "authorizationEnvelopeId"),
      runId: identifier3(input, "runId"),
      nodeId: identifier3(input, "nodeId"),
      correlationId: identifier3(input, "correlationId"),
      causationId: identifier3(input, "causationId"),
      usageUnits: Number(input.usageUnits),
      payload
    };
  }
});
var recursiveReconcileInput = defineRuntimeSchema({
  name: "RecursiveServiceReconcileInput",
  jsonSchema: schemaObject({
    customerOrganizationId: { type: "string", minLength: 1, maxLength: 255 },
    subscriptionId: { type: "string", minLength: 1, maxLength: 255 },
    entitlementId: { type: "string", minLength: 1, maxLength: 255 },
    serviceGrantId: { type: "string", minLength: 1, maxLength: 255 },
    providerConnectionId: { type: "string", minLength: 1, maxLength: 255 },
    capability: { type: "string", minLength: 1, maxLength: 255 },
    reconciliationAuthorizationEnvelopeId: {
      type: "string",
      minLength: 1,
      maxLength: 255
    },
    runId: { type: "string", minLength: 1, maxLength: 255 },
    nodeId: { type: "string", minLength: 1, maxLength: 255 },
    correlationId: { type: "string", minLength: 1, maxLength: 255 },
    causationId: { type: "string", minLength: 1, maxLength: 255 },
    usageUnits: { type: "integer", minimum: 1 },
    payload: { type: "object" },
    operationIdempotencyKey: { type: "string", minLength: 1, maxLength: 255 }
  }, [...RECONCILE_INPUT_FIELDS, "reconciliationAuthorizationEnvelopeId", "operationIdempotencyKey"]),
  parse(value) {
    const input = exactObject5(value, "RecursiveServiceReconcileInput", [
      ...RECONCILE_INPUT_FIELDS,
      "reconciliationAuthorizationEnvelopeId",
      "operationIdempotencyKey"
    ]);
    const base = recursiveInput.parse(Object.fromEntries(INPUT_FIELDS.map((field) => [
      field,
      field === "authorizationEnvelopeId" ? input.reconciliationAuthorizationEnvelopeId : input[field]
    ])));
    const { authorizationEnvelopeId: _authorizationEnvelopeId, ...immutable } = base;
    void _authorizationEnvelopeId;
    return {
      ...immutable,
      reconciliationAuthorizationEnvelopeId: identifier3(input, "reconciliationAuthorizationEnvelopeId"),
      operationIdempotencyKey: identifier3(input, "operationIdempotencyKey")
    };
  }
});
function recursiveOutput(commandId) {
  return defineRuntimeSchema({
    name: `${commandId}Output`,
    jsonSchema: schemaObject({
      commandId: { const: commandId },
      operatorId: { type: "string" },
      ventureId: { type: "string" },
      customerOrganizationId: { type: "string" },
      status: { const: "completed" },
      data: { type: "object" }
    }, ["commandId", "operatorId", "ventureId", "customerOrganizationId", "status", "data"]),
    parse(value) {
      const output = exactObject5(value, `${commandId}Output`, [
        "commandId",
        "operatorId",
        "ventureId",
        "customerOrganizationId",
        "status",
        "data"
      ]);
      if (output.commandId !== commandId || output.status !== "completed") {
        throw new Error(`invalid ${commandId} output`);
      }
      const data = objectValue2(output.data, "data");
      assertJsonSafe2(data, "data");
      return {
        commandId,
        operatorId: identifier3(output, "operatorId"),
        ventureId: identifier3(output, "ventureId"),
        customerOrganizationId: identifier3(output, "customerOrganizationId"),
        status: "completed",
        data
      };
    }
  });
}
function recursiveReconcileOutput(commandId, executionCommandId) {
  return defineRuntimeSchema({
    name: `${commandId}Output`,
    jsonSchema: schemaObject({
      commandId: { const: commandId },
      executionCommandId: { const: executionCommandId },
      operatorId: { type: "string" },
      ventureId: { type: "string" },
      customerOrganizationId: { type: "string" },
      providerOperationId: { type: "string" },
      status: { type: "string", enum: ["completed", "released", "manual_required"] },
      data: { type: "object" }
    }, [
      "commandId",
      "executionCommandId",
      "operatorId",
      "ventureId",
      "customerOrganizationId",
      "providerOperationId",
      "status",
      "data"
    ]),
    parse(value) {
      const output = exactObject5(value, `${commandId}Output`, [
        "commandId",
        "executionCommandId",
        "operatorId",
        "ventureId",
        "customerOrganizationId",
        "providerOperationId",
        "status",
        "data"
      ]);
      if (output.commandId !== commandId || output.executionCommandId !== executionCommandId || output.status !== "completed" && output.status !== "released" && output.status !== "manual_required") {
        throw new Error(`invalid ${commandId} output`);
      }
      const data = objectValue2(output.data, "data");
      assertJsonSafe2(data, "data");
      return {
        commandId,
        executionCommandId,
        operatorId: identifier3(output, "operatorId"),
        ventureId: identifier3(output, "ventureId"),
        customerOrganizationId: identifier3(output, "customerOrganizationId"),
        providerOperationId: identifier3(output, "providerOperationId"),
        status: output.status,
        data
      };
    }
  });
}
function defineRecursiveServiceCommand(definition2) {
  return defineCommandContract({
    id: definition2.id,
    version: 1,
    title: definition2.title,
    description: definition2.description,
    input: recursiveInput,
    output: recursiveOutput(definition2.id),
    requirements: {
      activeSubscription: false,
      entitlements: [],
      grant: true,
      scopes: definition2.requiredCommandScopes ?? ["service.execute"]
    },
    effect: "write",
    meter: definition2.meter ?? "service_executions"
  });
}
function defineRecursiveServiceReconcileCommand(definition2) {
  const contract = defineCommandContract({
    id: definition2.id,
    version: 1,
    title: definition2.title,
    description: definition2.description,
    input: recursiveReconcileInput,
    output: recursiveReconcileOutput(definition2.id, definition2.executionCommandId),
    requirements: {
      activeSubscription: false,
      entitlements: [],
      grant: true,
      scopes: definition2.requiredCommandScopes ?? ["service.reconcile"]
    },
    effect: "write",
    meter: definition2.meter ?? "service_reconciliations"
  });
  return Object.freeze({ contract, executionCommandId: definition2.executionCommandId });
}
var recursiveServiceExecuteCommand = defineRecursiveServiceCommand({
  id: "service.execute",
  title: "Execute Customer Service",
  description: "Execute one customer-scoped Service Blueprint through subscription, entitlement, Service Grant, Agent Grant, provider-connection, and authorization checks."
});
var recursiveServiceReconcileCommand = defineRecursiveServiceReconcileCommand({
  id: "service.reconcile",
  executionCommandId: recursiveServiceExecuteCommand.id,
  title: "Reconcile Customer Service",
  description: "Read back one durable provider operation and settle its exact result or confirmed no-effect usage without repeating the provider operation."
});
var recursiveCommandContracts = [recursiveServiceExecuteCommand];
var recursiveReconcileCommandRegistrations = [recursiveServiceReconcileCommand];
function registerRecursiveCommands(bus, runtime, contracts = recursiveCommandContracts) {
  for (const contract of contracts) {
    bus.register(contract, (input, context2) => runtime.execute(input, context2));
  }
}
function registerRecursiveReconcileCommands(bus, runtime, registrations = recursiveReconcileCommandRegistrations) {
  for (const { contract, executionCommandId } of registrations) {
    bus.register(contract, (input, context2) => runtime.reconcile(input, context2, executionCommandId));
  }
}

// packages/agent-runtime/dist/index.js
function commandRequirements(contract) {
  return contract.requirements;
}
function createVentureRuntime(options) {
  const executionMode = options.commandExecutionMode ?? "production";
  if (executionMode === "production") {
    const required = [
      ["command idempotency", options.commandIdempotencyStore?.durability],
      ["audit", options.audit?.durability],
      ["event", options.events?.durability],
      ["metering", options.metering?.durability]
    ];
    const unsafe = required.filter(([, durability]) => durability !== "durable_atomic").map(([name]) => name);
    if (unsafe.length > 0) {
      throw new Error(`Production venture runtime requires injected durable atomic stores for: ${unsafe.join(", ")}`);
    }
    if (options.securityAudit !== void 0 && options.securityAudit.durability !== "durable_atomic") {
      throw new Error("Production venture runtime security audit sink must be durable atomic");
    }
  }
  const audit = options.audit ?? new InMemoryAuditChain();
  const securityAudit = options.securityAudit ?? audit;
  const events = options.events ?? new InMemoryEventLog();
  const metering = options.metering ?? new InMemoryMeteringSink();
  const idempotency = options.commandIdempotencyStore ?? new InMemoryIdempotencyStore();
  const bus = new CommandBus({
    identity(context2) {
      assertNonEmpty(context2.identity.actorId, "actorId");
    },
    tenant(context2) {
      tenantKey(context2.tenant);
      assertOrganizationMembership(context2.identity, context2.tenant, options.memberships);
    },
    subscription(contract, context2) {
      if (commandRequirements(contract).activeSubscription)
        assertActiveSubscription(context2.subscription);
    },
    entitlement(contract, context2) {
      assertEntitlements(context2.entitlements, commandRequirements(contract).entitlements);
    },
    grant(contract, context2, now) {
      if (commandRequirements(contract).grant) {
        selectCommandGrant(context2.grants, contract.id, commandRequirements(contract).scopes, now);
      }
    },
    scope(contract, context2) {
      const decision = decideScopes(context2, commandRequirements(contract).scopes);
      if (!decision.allowed)
        throw new Error(decision.reason);
    },
    idempotency,
    audit,
    securityAudit,
    metering,
    events
  }, { now: options.now, executionMode });
  bus.register(campaignLaunchCommand, (input, context2) => ({
    commandId: "campaigns.launch",
    ventureId: context2.context.tenant.ventureId,
    campaignId: input.campaignId,
    channel: input.channel,
    status: "planned"
  }));
  bus.register(launchExecuteCommand, (input, context2) => ({
    commandId: "launch.execute",
    ventureId: context2.context.tenant.ventureId,
    runId: `run-${context2.context.tenant.ventureId}-${input.launchId}`.replace(/[^a-zA-Z0-9._-]/g, "-"),
    mode: input.mode,
    status: "accepted",
    dryRun: input.dryRun
  }));
  const stackRuntime = options.stackCommandRuntime ?? unconfiguredStackCommandRuntime;
  registerOperationalCommands(bus, {
    store: options.operationalStore,
    growthContractRoot: options.growthContractRoot,
    stackCatalog: stackRuntime.catalog,
    qualityProfileRunner: options.qualityProfileRunner,
    learningLoopRuntime: options.learningLoopRuntime,
    now: options.now
  });
  registerPlatformOperationCommands(bus, {
    auth: options.authCommandRuntime,
    upgrade: options.upgradeCommandRuntime,
    fleet: options.fleetCommandRuntime
  });
  registerProviderOperationCommands(bus, options.providerCommandRuntime);
  registerStackOperationCommands(bus, stackRuntime);
  if (options.recursiveCommandRuntime) {
    registerRecursiveCommands(bus, options.recursiveCommandRuntime, options.recursiveCommands ?? recursiveCommandContracts);
    registerRecursiveReconcileCommands(bus, options.recursiveCommandRuntime, options.recursiveReconcileCommands ?? (options.recursiveCommands ? [] : recursiveReconcileCommandRegistrations));
  } else if (options.recursiveCommands || options.recursiveReconcileCommands) {
    throw new Error("recursive commands require an injected recursive command runtime");
  }
  return {
    bus,
    contracts: bus.contracts(),
    executionMode,
    durability: {
      commandIdempotency: idempotency.durability,
      audit: audit.durability ?? "fixture_only",
      securityAudit: securityAudit.durability ?? "fixture_only",
      events: events.durability ?? "fixture_only",
      metering: metering.durability ?? "fixture_only"
    },
    execute: (commandId, input, invocation2) => bus.executeById(commandId, input, invocation2)
  };
}

// packages/cli-generator/src/operational.ts
import { createHash as createHash22 } from "node:crypto";
import { readFileSync as readFileSync16 } from "node:fs";
var EMPTY_INPUT_COMMANDS = /* @__PURE__ */ new Set([
  "system.doctor",
  "org.list",
  "stack.list",
  "pack.list",
  "seed.list",
  "grant.list",
  "provider.list",
  "upgrade.status",
  "data.sync",
  "run.list"
]);
var SECRET_PATTERNS2 = [
  /-----BEGIN (?:RSA |EC |OPENSSH )?PRIVATE KEY-----[\s\S]*?(?:-----END|$)/gi,
  /\bBearer\s+[A-Za-z0-9._~+/=-]{4,}/gi,
  /\b(?:sk|rk)_(?:live|test)_[A-Za-z0-9]+/gi,
  /\b(?:ghp|github_pat)_[A-Za-z0-9_]+/gi
];
function redact(value) {
  return SECRET_PATTERNS2.reduce((safe, pattern) => safe.replace(pattern, "[REDACTED]"), value);
}
function flag(args, name) {
  const index = args.indexOf(name);
  return index >= 0 ? args[index + 1] : void 0;
}
function positionals(args) {
  const valuedFlags = /* @__PURE__ */ new Set([
    "--brief",
    "--cadence",
    "--context",
    "--file",
    "--idempotency-key",
    "--idea",
    "--input",
    "--name",
    "--org-id",
    "--path",
    "--run-id",
    "--runtime-module",
    "--state-dir",
    "--project-root",
    "--provider",
    "--ref",
    "--backend",
    "--kind",
    "--scopes",
    "--release",
    "--release-id",
    "--venture-ids",
    "--batch-size",
    "--venture-id"
  ]);
  const values = [];
  for (let index = 0; index < args.length; index += 1) {
    const value = args[index];
    if (value.startsWith("-")) {
      if (valuedFlags.has(value)) index += 1;
      continue;
    }
    values.push(value);
  }
  return values;
}
function slug3(value) {
  const normalized = value.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "").slice(0, 64);
  return normalized.length >= 2 ? normalized : "local-venture";
}
function resolveCommand(args, contracts) {
  const values = positionals(args);
  const [first, second] = values;
  if (!first) return null;
  if (second) {
    const exact = contracts.find(
      ({ surfaces }) => surfaces.cli.tokens[0] === first && surfaces.cli.tokens[1] === second
    );
    if (exact) {
      return { commandId: exact.id, tokens: exact.surfaces.cli.tokens, bootstrap: false };
    }
  }
  if (first === "doctor")
    return { commandId: "system.doctor", tokens: ["system", "doctor"], bootstrap: false };
  if (first === "create")
    return {
      commandId: "venture.create",
      tokens: ["venture", "create"],
      bootstrap: Boolean(flag(args, "--brief") || flag(args, "--idea"))
    };
  if (first === "plan")
    return { commandId: "venture.plan", tokens: ["venture", "plan"], bootstrap: false };
  if (first === "launch")
    return { commandId: "venture.launch", tokens: ["venture", "launch"], bootstrap: false };
  if (first === "resume")
    return { commandId: "venture.resume", tokens: ["venture", "resume"], bootstrap: false };
  if (first === "status") {
    if (flag(args, "--venture-id"))
      return { commandId: "venture.status", tokens: ["venture", "status"], bootstrap: false };
    if (second || flag(args, "--run-id"))
      return { commandId: "run.status", tokens: ["run", "status"], bootstrap: false };
    return { commandId: "run.list", tokens: ["run", "list"], bootstrap: false };
  }
  if (first === "learn" && second !== "run")
    return { commandId: "learn.run", tokens: ["learn", "run"], bootstrap: false };
  if (first === "verify" && second !== "run")
    return { commandId: "verify.run", tokens: ["verify", "run"], bootstrap: false };
  if (["org", "stack", "pack", "seed", "grant", "provider", "run"].includes(first) && !second) {
    const action = first === "run" ? "list" : "list";
    return { commandId: `${first}.${action}`, tokens: [first, action], bootstrap: false };
  }
  if (!second) return null;
  return { commandId: `${first}.${second}`, tokens: [first, second], bootstrap: false };
}
function parseJsonObject(raw, field) {
  let value;
  try {
    value = JSON.parse(raw);
  } catch {
    throw new Error(`${field} must be valid JSON`);
  }
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    throw new Error(`${field} must contain a JSON object`);
  }
  return value;
}
function ventureId(args, values) {
  return flag(args, "--venture-id") ?? values[2] ?? "local-venture";
}
function inputFor(commandId, args) {
  const rawInput = flag(args, "--input");
  if (rawInput) return parseJsonObject(rawInput, "--input");
  if (EMPTY_INPUT_COMMANDS.has(commandId)) return {};
  const values = positionals(args);
  if (commandId === "idea.compile") {
    const brief = flag(args, "--brief");
    const idea = flag(args, "--idea") ?? (brief ? readFileSync16(brief, "utf8") : values[2]);
    if (!idea) throw new Error("idea compile requires --idea <text> or --brief <file>");
    const name = flag(args, "--name") ?? "Local Venture";
    return { idea, ventureId: flag(args, "--venture-id") ?? slug3(name), name };
  }
  if (commandId === "venture.create") {
    const id = ventureId(args, values);
    return { ventureId: id, name: flag(args, "--name") ?? "Local Venture" };
  }
  if (commandId === "venture.plan" || commandId === "venture.status") {
    return { ventureId: ventureId(args, values) };
  }
  if (commandId === "venture.launch") {
    if (args.includes("--apply")) {
      throw new Error("provider apply is unavailable in the packaged local runtime; no effect ran");
    }
    if (!args.includes("--dry-run")) throw new Error("venture launch requires --dry-run");
    const id = ventureId(args, values);
    return { ventureId: id, runId: flag(args, "--run-id") ?? `run-${id}`, dryRun: true };
  }
  if (commandId === "venture.resume" || commandId === "run.status") {
    const runId2 = flag(args, "--run-id") ?? values[2] ?? values[1];
    if (!runId2) throw new Error(`${commandId} requires a run id`);
    return { runId: runId2 };
  }
  if (commandId === "learn.run") {
    const cadence = flag(args, "--cadence") ?? (values[1] === "run" ? values[2] : values[1]);
    if (!cadence) throw new Error("learn requires daily, weekly, biweekly, or monthly");
    return { cadence };
  }
  if (commandId === "growth.inspect") {
    return { path: flag(args, "--file") ?? flag(args, "--path") ?? "config/growth.yaml" };
  }
  if (commandId === "verify.run") {
    const profile2 = values[1] === "run" ? values[2] : values[1];
    if (!profile2) throw new Error("verify requires fast, mvp, or release");
    return { profile: profile2 };
  }
  if (commandId.startsWith("auth.")) {
    const providerId = flag(args, "--provider") ?? values[2];
    const credentialRef2 = flag(args, "--ref");
    if (commandId === "auth.login") {
      if (!providerId) throw new Error("auth login requires a provider identifier");
      return {
        providerId,
        ...credentialRef2 ? { credentialRef: credentialRef2 } : {},
        ...flag(args, "--backend") ? { backend: flag(args, "--backend") } : {},
        ...flag(args, "--kind") ? { kind: flag(args, "--kind") } : {},
        scopes: (flag(args, "--scopes") ?? "").split(",").map((scope) => scope.trim()).filter(Boolean)
      };
    }
    return {
      ...providerId ? { providerId } : {},
      ...credentialRef2 ? { credentialRef: credentialRef2 } : {}
    };
  }
  if (commandId === "upgrade.plan" || commandId === "upgrade.dry-run" || commandId === "upgrade.apply") {
    const releaseLocator = flag(args, "--release") ?? values[2];
    if (!releaseLocator) {
      throw new Error(`${commandId} requires --release <trusted-local-release-root>`);
    }
    return { releaseLocator };
  }
  if (commandId === "fleet.plan" || commandId === "fleet.rollout" || commandId === "fleet.resume") {
    const runId2 = flag(args, "--run-id");
    const releaseId = flag(args, "--release-id");
    const ventureIds = (flag(args, "--venture-ids") ?? flag(args, "--venture-id") ?? "").split(",").map((venture) => venture.trim()).filter(Boolean);
    const batchSize = Number(flag(args, "--batch-size") ?? "1");
    if (!runId2 || !releaseId || ventureIds.length === 0) {
      throw new Error(
        `${commandId} requires --run-id, --release-id, and --venture-ids <comma-separated>`
      );
    }
    return { runId: runId2, releaseId, ventureIds, batchSize };
  }
  if (commandId === "fleet.status") {
    const runId2 = flag(args, "--run-id") ?? values[2];
    return runId2 ? { runId: runId2 } : {};
  }
  return {};
}
function invocation(commandId, input, args, options) {
  const supplied = flag(args, "--idempotency-key");
  const digest3 = createHash22("sha256").update(stableJson({ commandId, input })).digest("hex").slice(0, 24);
  return { context: options.context, idempotencyKey: supplied ?? `vh-local-${digest3}` };
}
function rendered(value, json2) {
  if (json2) return JSON.stringify(value, null, 2);
  if (value === null || typeof value !== "object" || Array.isArray(value)) return String(value);
  const heading2 = typeof value.commandId === "string" ? value.commandId : "command";
  const status = typeof value.status === "string" ? value.status : "completed";
  const mode = typeof value.mode === "string" ? ` (${value.mode})` : "";
  return `${heading2}: ${status}${mode}`;
}
var OPERATIONAL_CLI_HELP = `Packaged Venture Harness operational CLI

Local and read-only commands:
  vh doctor --json
  vh idea compile --idea <text> --venture-id <id> --name <name> --json
  vh create --venture-id <id> --name <name> --json
  vh venture plan --venture-id <id> --json
  vh venture launch --dry-run --venture-id <id> [--run-id <id>] --json
  vh venture status --venture-id <id> --json
  vh run list | status <run-id>
  vh resume <run-id> --json
  vh org|stack|pack|seed|grant|provider [list]
  vh data sync
  vh learn daily|weekly|biweekly|monthly
  vh growth inspect [--file <growth.yaml>] --json
  vh auth login|status|test|revoke [provider] [--ref <cred://...>]
  vh upgrade plan|dry-run|apply --release <trusted-local-root>
  vh upgrade status
  vh fleet status [--run-id <id>]
  vh fleet plan|rollout|resume --run-id <id> --release-id <id>
      --venture-ids <id,...> [--batch-size <positive-integer>]
  vh verify fast|mvp|release

Every packaged command is noninteractive. The default runtime remains local and
unconfigured for provider effects. A production provider/Stack command requires
--runtime-module <compiled-project-module>, exact --context, and an idempotency
key; publishing, sending, spend, upgrade, and fleet effects remain separately
authorization-gated.`;
async function invokeOperationalCli(bus, args, options) {
  const json2 = args.includes("--json");
  try {
    const resolved = resolveCommand(args, bus.contracts());
    if (!resolved) throw new Error(`unknown packaged command; run vh --help`);
    const contract = bus.contracts().find((candidate) => candidate.id === resolved.commandId);
    if (!contract) {
      const domain = resolved.tokens[0];
      const next = domain === "auth" ? "choose vh auth login|status|test|revoke" : domain === "upgrade" ? "choose vh upgrade plan|dry-run|apply|status" : domain === "fleet" ? "choose vh fleet status|plan|rollout|resume" : "run vh --help";
      throw new Error(`unknown packaged command: ${resolved.commandId}; ${next}`);
    }
    if (resolved.bootstrap) {
      const ideaInput = inputFor("idea.compile", args);
      const compiled = await bus.executeById(
        "idea.compile",
        ideaInput,
        invocation("idea.compile", ideaInput, args, options)
      );
      const createInput = {
        ventureId: ideaInput.ventureId,
        name: ideaInput.name
      };
      const created = await bus.executeById(
        "venture.create",
        createInput,
        invocation("venture.create", createInput, args, options)
      );
      return {
        exitCode: 0,
        stdout: rendered({ commandId: "venture.bootstrap", compiled, created }, json2),
        stderr: ""
      };
    }
    const input = inputFor(resolved.commandId, args);
    const output = await bus.executeById(
      resolved.commandId,
      input,
      invocation(resolved.commandId, input, args, options)
    );
    const verifyPassed = resolved.commandId !== "verify.run" || typeof output === "object" && output !== null && !Array.isArray(output) && output.status === "PASS";
    return { exitCode: verifyPassed ? 0 : 1, stdout: rendered(output, json2), stderr: "" };
  } catch (error) {
    const message = redact(error instanceof Error ? error.message : String(error));
    const failure = error instanceof CommandBusError ? commandFailureEnvelope(error) : {
      error: "operational_command_failed",
      code: "invalid_invocation",
      message
    };
    return {
      exitCode: 1,
      stdout: "",
      stderr: json2 ? JSON.stringify(failure) : message
    };
  }
}

// packages/cli-generator/src/quality-runner.ts
import { randomUUID as randomUUID3 } from "node:crypto";
import { spawn as spawn3 } from "node:child_process";
import { existsSync as existsSync14, lstatSync as lstatSync9, mkdirSync as mkdirSync10, readFileSync as readFileSync17, realpathSync as realpathSync8 } from "node:fs";
import { basename, join as join8, relative as relative15, resolve as resolve23, sep as sep15 } from "node:path";
var SECRET_PATTERNS3 = [
  /-----BEGIN (?:RSA |EC |OPENSSH )?PRIVATE KEY-----[\s\S]*?(?:-----END|$)/gi,
  /\bBearer\s+[A-Za-z0-9._~+/=-]{8,}/gi,
  /\b(?:sk|rk)_(?:live|test)_[A-Za-z0-9_-]{8,}/gi,
  /\b(?:ghp|github_pat)_[A-Za-z0-9_]{8,}/gi
];
function redact2(value) {
  let redacted = SECRET_PATTERNS3.reduce(
    (candidate, pattern) => candidate.replace(pattern, "[REDACTED]"),
    value
  );
  for (const [name, secret] of Object.entries(process.env)) {
    if (!/(?:TOKEN|SECRET|PASSWORD|PRIVATE_KEY|API_KEY|DATABASE_URL)/iu.test(name)) continue;
    if (secret && secret.length >= 8) redacted = redacted.split(secret).join("[REDACTED]");
  }
  return redacted;
}
function canonicalRoot(root) {
  const declared = resolve23(root);
  const details = lstatSync9(declared);
  if (details.isSymbolicLink() || !details.isDirectory()) {
    throw new Error("quality runner root must be a regular directory, not a symbolic link");
  }
  return realpathSync8(declared);
}
function inside7(root, target) {
  const child = relative15(root, target);
  return child === "" || child !== ".." && !child.startsWith(`..${sep15}`);
}
function assertNoSymlinkPath(root, target) {
  if (!inside7(root, target)) throw new Error("quality report path escapes the configured root");
  const child = relative15(root, target);
  let cursor = root;
  for (const segment of child.split(sep15).filter(Boolean)) {
    cursor = join8(cursor, segment);
    if (!existsSync14(cursor)) break;
    if (lstatSync9(cursor).isSymbolicLink()) {
      throw new Error("quality report path must not contain symbolic links");
    }
  }
}
function reportPath(root, profile2) {
  const directory = join8(root, ".venture", "reports", "quality");
  assertNoSymlinkPath(root, directory);
  mkdirSync10(directory, { recursive: true, mode: 448 });
  const canonicalDirectory = realpathSync8(directory);
  if (!inside7(root, canonicalDirectory)) throw new Error("quality report directory escapes root");
  return join8(canonicalDirectory, `vh-${profile2}-${process.pid}-${randomUUID3()}.json`);
}
function assertCommand(command) {
  if (command.length === 0 || command.some((value) => typeof value !== "string" || !value)) {
    throw new Error("quality profile command must be a non-empty argv array");
  }
  const tokens = command.map((value) => value.toLowerCase());
  const executable = basename(tokens[0]);
  if (executable === "vh" && tokens[1] === "verify" || tokens.some((token, index) => token === "vh" && tokens[index + 1] === "verify")) {
    throw new Error("quality profile command must not recurse into vh verify");
  }
}
function materializeCommand(template, profile2, targetReport) {
  const command = template.map(
    (value) => value.replaceAll("{profile}", profile2).replaceAll("{report}", targetReport)
  );
  assertCommand(command);
  return command;
}
function capture(limit) {
  let text2 = "";
  let truncated = false;
  return {
    append(chunk) {
      if (truncated) return;
      const value = chunk.toString();
      const remaining = limit - Buffer.byteLength(text2);
      if (remaining <= 0) {
        truncated = true;
        return;
      }
      const bytes = Buffer.from(value);
      text2 += bytes.subarray(0, remaining).toString();
      if (bytes.byteLength > remaining) truncated = true;
    },
    value() {
      return redact2(`${text2}${truncated ? "\n[OUTPUT TRUNCATED]" : ""}`);
    }
  };
}
function count(summary, field) {
  const value = summary[field];
  return Number.isSafeInteger(value) && Number(value) >= 0 ? Number(value) : 0;
}
function readReport(path, profile2) {
  if (!existsSync14(path)) return null;
  const raw = JSON.parse(readFileSync17(path, "utf8"));
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    throw new Error("quality runner report must be a JSON object");
  }
  const report = raw;
  if (report.profile !== profile2) throw new Error("quality runner report profile mismatch");
  if (!report.summary || typeof report.summary !== "object" || Array.isArray(report.summary)) {
    throw new Error("quality runner report summary is missing");
  }
  const summaryRecord = report.summary;
  const summary = {
    PASS: count(summaryRecord, "PASS"),
    FAIL: count(summaryRecord, "FAIL"),
    SKIP: count(summaryRecord, "SKIP"),
    NOT_APPLICABLE: count(summaryRecord, "NOT_APPLICABLE")
  };
  const reportedStatus = report.status;
  if (!["PASS", "FAIL", "INCOMPLETE"].includes(reportedStatus)) {
    throw new Error("quality runner report status is invalid");
  }
  return { status: reportedStatus, summary };
}
function finalStatus(exitCode, report) {
  if (exitCode !== 0 || report?.status === "FAIL" || Number(report?.summary.FAIL ?? 0) > 0) {
    return "FAIL";
  }
  if (!report || report.status === "INCOMPLETE" || Number(report.summary.SKIP ?? 0) > 0) {
    return "INCOMPLETE";
  }
  return "PASS";
}
function createProcessQualityProfileRunner(options) {
  const root = canonicalRoot(options.root);
  const outputLimit = options.outputLimitBytes ?? 64 * 1024;
  const timeoutMs = options.timeoutMs ?? 30 * 6e4;
  if (!Number.isSafeInteger(outputLimit) || outputLimit < 1024) {
    throw new Error("quality output limit must be at least 1024 bytes");
  }
  if (!Number.isSafeInteger(timeoutMs) || timeoutMs < 1) {
    throw new Error("quality timeout must be a positive safe integer");
  }
  for (const command of Object.values(options.commands)) assertCommand(command);
  return Object.freeze({
    async run(profile2) {
      const targetReport = reportPath(root, profile2);
      const command = materializeCommand(options.commands[profile2], profile2, targetReport);
      const stdout = capture(outputLimit);
      const stderr = capture(outputLimit);
      let timedOut = false;
      const exitCode = await new Promise((resolveExit, reject) => {
        const child = spawn3(command[0], command.slice(1), {
          cwd: root,
          env: process.env,
          shell: false,
          stdio: ["ignore", "pipe", "pipe"]
        });
        child.stdout.on("data", (chunk) => stdout.append(chunk));
        child.stderr.on("data", (chunk) => stderr.append(chunk));
        child.once("error", reject);
        const timeout = setTimeout(() => {
          timedOut = true;
          child.kill("SIGTERM");
        }, timeoutMs);
        child.once("close", (code) => {
          clearTimeout(timeout);
          resolveExit(code ?? 1);
        });
      }).catch((error) => {
        stderr.append(error instanceof Error ? error.message : String(error));
        return 1;
      });
      let report = null;
      try {
        report = readReport(targetReport, profile2);
      } catch (error) {
        stderr.append(error instanceof Error ? error.message : String(error));
      }
      const status = timedOut ? "FAIL" : finalStatus(exitCode, report);
      return {
        profile: profile2,
        status,
        exitCode: status === "PASS" ? 0 : 1,
        summary: report?.summary ?? { PASS: 0, FAIL: status === "FAIL" ? 1 : 0, SKIP: 0 },
        command: command.map((value) => redact2(value)),
        stdout: stdout.value(),
        stderr: `${stderr.value()}${timedOut ? "\nquality profile timed out" : ""}`.trim(),
        reportPath: relative15(root, targetReport)
      };
    }
  });
}
function createRepositoryQualityProfileRunner(root) {
  const canonical = canonicalRoot(root);
  const runnerPath = join8(canonical, "scripts", "run-quality-profile.ts");
  const configured = existsSync14(runnerPath) && !lstatSync9(runnerPath).isSymbolicLink() && lstatSync9(runnerPath).isFile() && inside7(canonical, realpathSync8(runnerPath));
  if (!configured) {
    return Object.freeze({
      async run(profile2) {
        return {
          profile: profile2,
          status: "INCOMPLETE",
          exitCode: 1,
          summary: { PASS: 0, FAIL: 0, SKIP: 1, NOT_APPLICABLE: 0 },
          command: [],
          stdout: "",
          stderr: "The project has no trusted scripts/run-quality-profile.ts runner.",
          reportPath: null
        };
      }
    });
  }
  const command = [
    "pnpm",
    "exec",
    "tsx",
    "scripts/run-quality-profile.ts",
    "{profile}",
    "--report",
    "{report}"
  ];
  return createProcessQualityProfileRunner({
    root: canonical,
    commands: { fast: command, mvp: command, release: command }
  });
}

// packages/cli-generator/src/runtime-module.ts
import { existsSync as existsSync15, lstatSync as lstatSync10, realpathSync as realpathSync9 } from "node:fs";
import { extname, join as join9, relative as relative16, resolve as resolve24, sep as sep16 } from "node:path";
import { pathToFileURL } from "node:url";
function canonicalRoot2(root) {
  const declared = resolve24(root);
  const details = lstatSync10(declared);
  if (details.isSymbolicLink() || !details.isDirectory()) {
    throw new Error("runtime project root must be a regular directory, not a symbolic link");
  }
  return realpathSync9(declared);
}
function inside8(root, target) {
  const child = relative16(root, target);
  return child === "" || child !== ".." && !child.startsWith(`..${sep16}`);
}
function assertNoSymlinkComponents(root, target, allowMissingLeaf) {
  if (!inside8(root, target)) throw new Error("runtime path must stay within the project root");
  const child = relative16(root, target);
  let cursor = root;
  for (const [index, segment] of child.split(sep16).filter(Boolean).entries()) {
    cursor = join9(cursor, segment);
    if (!existsSync15(cursor)) {
      if (allowMissingLeaf) return;
      throw new Error("runtime module does not exist");
    }
    if (lstatSync10(cursor).isSymbolicLink()) {
      throw new Error("runtime path must not contain symbolic links");
    }
    if (index < child.split(sep16).filter(Boolean).length - 1 && !lstatSync10(cursor).isDirectory()) {
      throw new Error("runtime path parent must be a directory");
    }
  }
}
function projectOwnedFile(root, path) {
  const target = resolve24(root, path);
  assertNoSymlinkComponents(root, target, false);
  const child = relative16(root, target);
  const first = child.split(sep16)[0];
  if (["node_modules", ".git", ".pnpm"].includes(first ?? "")) {
    throw new Error("runtime module must be a project-owned file outside dependency metadata");
  }
  if (![".js", ".mjs", ".cjs"].includes(extname(target))) {
    throw new Error("runtime module must be compiled JavaScript (.js, .mjs, or .cjs)");
  }
  const details = lstatSync10(target);
  if (!details.isFile()) throw new Error("runtime module must be a regular file");
  const canonical = realpathSync9(target);
  if (!inside8(root, canonical)) throw new Error("runtime module resolves outside the project root");
  return canonical;
}
function projectOwnedStateDirectory(root, path) {
  const target = resolve24(root, path);
  assertNoSymlinkComponents(root, target, true);
  return target;
}
function assertProductionRuntime(value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    throw new Error("createVhRuntime must return a VentureRuntime object");
  }
  const runtime = value;
  if (runtime.executionMode !== "production") {
    throw new Error("explicit runtime module must compose commandExecutionMode=production");
  }
  const durability = runtime.durability;
  const required = ["commandIdempotency", "audit", "securityAudit", "events", "metering"];
  if (!durability || required.some((field) => durability[field] !== "durable_atomic")) {
    throw new Error(
      "explicit production runtime requires durable atomic command and evidence stores"
    );
  }
  if (!runtime.bus || typeof runtime.bus.contracts !== "function" || !Array.isArray(runtime.contracts) || typeof runtime.execute !== "function") {
    throw new Error("createVhRuntime returned an invalid command runtime shape");
  }
}
async function loadProductionRuntimeModule(options) {
  const root = canonicalRoot2(options.projectRoot);
  const modulePath = projectOwnedFile(root, options.runtimeModule);
  const stateDirectory = projectOwnedStateDirectory(root, options.stateDirectory);
  const loaded = await import(pathToFileURL(modulePath).href);
  if (typeof loaded.createVhRuntime !== "function") {
    throw new Error("runtime module must export a createVhRuntime factory");
  }
  const factory = loaded.createVhRuntime;
  const runtime = await factory(
    Object.freeze({ schemaVersion: 1, projectRoot: root, stateDirectory })
  );
  assertProductionRuntime(runtime);
  return runtime;
}

// packages/cli-generator/src/index.ts
function flag2(args, name) {
  const index = args.indexOf(name);
  return index >= 0 ? args[index + 1] : void 0;
}
function renderCliSuccess(value) {
  if (value === null || typeof value !== "object" || Array.isArray(value)) return String(value);
  const record3 = value;
  const heading2 = typeof record3.commandId === "string" ? record3.commandId : "command";
  const status = typeof record3.status === "string" ? record3.status : "completed";
  const mode = typeof record3.mode === "string" ? ` (${record3.mode})` : "";
  return `${heading2}: ${status}${mode}`;
}
function generateCliHelp(bus) {
  return [
    "Venture Harness generated command surfaces",
    "",
    ...bus.contracts().map(
      (contract) => `  vh ${contract.surfaces.cli.tokens.join(" ")} --input <json> --context <json> --idempotency-key <key>
      ${contract.description}`
    )
  ].join("\n");
}
function createCliSurface(bus) {
  return {
    help: generateCliHelp(bus),
    async invoke(args, options) {
      const contract = bus.contracts().find(
        (candidate) => candidate.surfaces.cli.tokens[0] === args[0] && candidate.surfaces.cli.tokens[1] === args[1]
      );
      if (!contract) return { exitCode: 2, stdout: "", stderr: "unknown generated command" };
      try {
        const rawInput = flag2(args, "--input");
        const rawContext = flag2(args, "--context");
        const context2 = options?.context ?? (rawContext ? JSON.parse(rawContext) : void 0);
        const idempotencyKey2 = options?.idempotencyKey ?? flag2(args, "--idempotency-key");
        if (!rawInput || !context2 || !idempotencyKey2)
          throw new Error("--input, --context, and --idempotency-key are required");
        const output = await bus.executeById(contract.id, JSON.parse(rawInput), {
          context: context2,
          idempotencyKey: idempotencyKey2
        });
        return { exitCode: 0, stdout: JSON.stringify(output), stderr: "" };
      } catch (error) {
        const failure = commandFailureEnvelope(error);
        const json2 = args.includes("--json");
        return {
          exitCode: 1,
          stdout: "",
          stderr: json2 ? JSON.stringify(failure) : failure.message,
          failure
        };
      }
    }
  };
}

// packages/cli-generator/src/bin.ts
function flag3(args, name) {
  const index = args.indexOf(name);
  return index >= 0 ? args[index + 1] : void 0;
}
function context(args, requireExplicit) {
  const raw = flag3(args, "--context");
  if (raw) {
    try {
      return JSON.parse(raw);
    } catch {
      throw new Error("--context must be valid JSON");
    }
  }
  if (requireExplicit) {
    throw new Error("--context is required with an explicit production runtime module");
  }
  const organizationId = flag3(args, "--org-id") ?? "local-org";
  const ventureId2 = flag3(args, "--venture-id") ?? "local-venture";
  return {
    identity: { actorId: "local-operator", kind: "user" },
    tenant: { organizationId, ventureId: ventureId2 },
    subscription: { subscriptionId: "local-none", status: "none", plan: "local" },
    entitlements: [],
    scopes: [],
    grants: []
  };
}
async function runGeneratedCli(inputArgs, options = {}) {
  const args = [...inputArgs];
  if (args[0] === "--") args.shift();
  const io = options.io ?? { stdout: console.log, stderr: console.error };
  try {
    if (!args[0] || args[0] === "help" || args[0] === "--help" || args[0] === "-h") {
      const localContext = context(args, false);
      const localRuntime = createVentureRuntime({
        commandExecutionMode: "fixture",
        memberships: [
          {
            organizationId: localContext.tenant.organizationId,
            actorId: localContext.identity.actorId,
            role: "operator",
            active: true
          }
        ],
        growthContractRoot: options.cwd ?? process.cwd()
      });
      io.stdout(
        `${OPERATIONAL_CLI_HELP}

Typed Agent Surface form:
${createCliSurface(localRuntime.bus).help}`
      );
      return 0;
    }
    const runtimeModule = flag3(args, "--runtime-module");
    const projectRoot = resolve25(options.cwd ?? process.cwd(), flag3(args, "--project-root") ?? ".");
    const stateDirectoryFlag = flag3(args, "--state-dir") ?? ".venture-harness";
    const invocationContext = context(args, Boolean(runtimeModule));
    if (runtimeModule && !flag3(args, "--idempotency-key")) {
      throw new Error("--idempotency-key is required with an explicit production runtime module");
    }
    const stateDirectory = resolve25(projectRoot, stateDirectoryFlag);
    const runtime = runtimeModule ? await loadProductionRuntimeModule({
      projectRoot,
      runtimeModule,
      stateDirectory: stateDirectoryFlag
    }) : createVentureRuntime(
      (() => {
        const runtimeOptions = {
          commandExecutionMode: "fixture",
          memberships: [
            {
              organizationId: invocationContext.tenant.organizationId,
              actorId: invocationContext.identity.actorId,
              role: "operator",
              active: true
            }
          ],
          growthContractRoot: projectRoot,
          operationalStore: new FileOperationalStateStore(stateDirectory),
          qualityProfileRunner: createRepositoryQualityProfileRunner(projectRoot)
        };
        return runtimeOptions;
      })()
    );
    const cli = createCliSurface(runtime.bus);
    if (args[0] === "commands") {
      io.stdout(
        JSON.stringify(
          runtime.contracts.map((contract) => ({
            id: contract.id,
            description: contract.description,
            surfaces: contract.surfaces,
            packagedMode: ["auth.", "upgrade.", "fleet."].some(
              (prefix) => contract.id.startsWith(prefix)
            ) ? "authorized_host_operation" : operationalCommandContracts.some(({ id }) => id === contract.id) ? "local_or_read_only" : "authorized_business_command"
          })),
          null,
          2
        )
      );
      return 0;
    }
    const exact = runtime.contracts.find(
      (contract) => contract.surfaces.cli.tokens[0] === args[0] && contract.surfaces.cli.tokens[1] === args[1]
    );
    const operational = exact ? operationalCommandContracts.some(({ id }) => id === exact.id) : true;
    const invocation2 = {
      context: invocationContext,
      idempotencyKey: flag3(args, "--idempotency-key") ?? "vh-generated-command"
    };
    const result2 = operational ? await invokeOperationalCli(runtime.bus, args, invocation2) : await cli.invoke(args, invocation2);
    if (!operational && result2.exitCode === 0 && result2.stdout && !args.includes("--json")) {
      try {
        result2.stdout = renderCliSuccess(JSON.parse(result2.stdout));
      } catch {
        result2.exitCode = 1;
        result2.stderr = "command runtime returned invalid JSON";
      }
    }
    if (result2.stdout) io.stdout(result2.stdout);
    if (result2.stderr) io.stderr(result2.stderr);
    return result2.exitCode;
  } catch (error) {
    const json2 = args.includes("--json");
    const message = error instanceof Error ? error.message : "packaged CLI failed";
    if (json2) io.stderr(JSON.stringify({ error: "cli_initialization_failed", message }));
    else io.stderr(message);
    return 1;
  }
}
function isDirectGeneratedCliEntry() {
  if (!process.argv[1]) return false;
  const modulePath = realpathSync10(fileURLToPath(import.meta.url));
  if (!modulePath.replaceAll("\\", "/").includes("/cli-generator/")) return false;
  try {
    return realpathSync10(resolve25(process.argv[1])) === modulePath;
  } catch {
    return resolve25(process.argv[1]) === modulePath;
  }
}
if (isDirectGeneratedCliEntry()) {
  void runGeneratedCli(process.argv.slice(2)).then((exitCode) => {
    process.exitCode = exitCode;
  });
}

// scripts/vh-bundle.ts
var IMMUTABLE_GIT_SHA = /^[a-f0-9]{40}$/u;
function founderCoreBuildProvenance() {
  const workflowRefSha = true ? "10541f63d08ee4864fb4cd5cb8d9eb8596a937be" : void 0;
  const packageVersion = true ? "0.2.0" : void 0;
  if (!workflowRefSha || !IMMUTABLE_GIT_SHA.test(workflowRefSha) || !packageVersion) {
    throw new Error(
      "The vh executable is missing immutable Venture Harness Core build provenance. Rebuild it with pnpm workspace:build before packing or installing it."
    );
  }
  return Object.freeze({
    packageName: "venture-harness",
    packageVersion,
    workflowRefSha
  });
}
var FOUNDER_CORE_DOMAINS = /* @__PURE__ */ new Set([
  "auth",
  "create",
  "data",
  "doctor",
  "launch",
  "learn",
  "plan",
  "resume",
  "stack",
  "status",
  "upgrade"
]);
var defaultIo2 = {
  stdout: (line) => console.log(line),
  stderr: (line) => console.error(line)
};
var defaultFounderServicesFactory = (options) => createDefaultCliServices(options);
function runDefaultFounderCli(args, options) {
  const store = new FileWorkflowStore();
  const services = (options.servicesFactory ?? defaultFounderServicesFactory)({
    store,
    founderWorkflowRefSha: founderCoreBuildProvenance().workflowRefSha
  });
  return runCli([...args], { io: options.io, services, store });
}
var defaultAdvancedRunner = (args, options) => runGeneratedCli(args, { io: options.io });
function routesToFounderCli(command) {
  return command === void 0 || command === "help" || command === "--help" || command === "-h" || FOUNDER_CORE_DOMAINS.has(command);
}
async function runVhShell(inputArgs, options = {}) {
  const args = [...inputArgs];
  if (args[0] === "--") args.shift();
  const io = options.io ?? defaultIo2;
  if (routesToFounderCli(args[0])) {
    const result2 = options.founderRunner ? await options.founderRunner(args, { io }) : await runDefaultFounderCli(args, {
      io,
      servicesFactory: options.founderServicesFactory
    });
    return result2.exitCode;
  }
  return (options.advancedRunner ?? defaultAdvancedRunner)(args, { io });
}
function isDirectRootCliEntry() {
  if (!process.argv[1]) return false;
  const modulePath = realpathSync11(fileURLToPath2(import.meta.url));
  try {
    return realpathSync11(resolve26(process.argv[1])) === modulePath;
  } catch {
    return resolve26(process.argv[1]) === modulePath;
  }
}
if (isDirectRootCliEntry()) {
  void runVhShell(process.argv.slice(2)).then((exitCode) => {
    process.exitCode = exitCode;
  }).catch((error) => {
    console.error(error instanceof Error ? error.message : String(error));
    process.exitCode = 1;
  });
}
export {
  FOUNDER_CORE_DOMAINS,
  founderCoreBuildProvenance,
  runVhShell
};
