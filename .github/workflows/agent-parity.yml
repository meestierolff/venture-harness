name: agent-parity

on:
  # Deliberately unfiltered. This job is a required status check, and a
  # path-filtered workflow reports nothing on the pull requests it skips, which
  # leaves those pull requests blocked forever rather than passing.
  pull_request:

permissions:
  contents: read

concurrency:
  group: agent-parity-${{ github.ref }}
  cancel-in-progress: true

jobs:
  parity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Prepare workspace packages
        run: pnpm workspace:prepare
      - name: Skill validity and adapter parity
        run: pnpm agents:check
